/*---------------------------------------------------------------------------------------------
 *  Copyright (c) Microsoft Corporation. All rights reserved.
 *  Licensed under the MIT License. See License.txt in the project root for license information.
 *--------------------------------------------------------------------------------------------*/
export const inputs: string[] = [
	` editorLangId =~ /c$|cpp$|python$/ && vexrobotics.vexcode.isValidProjectID == true `,
	` jupyter.document.hasCodeCells`,
	` resourceExtname == .png || resourceExtname == .jpg`,
	` view == iotHubDevices && viewItem == edge-module`,
	` view == iotHubDevices && viewItem == module`,
	` view == vex-sidebar-view `,
	` viewItem == DefaultStorageFolderInfo`,
	` viewItem == VSCode WorkSpace`,
	`! listMultiSelection && filesExplorerFocus`,
	`!!config.accessToken && !!config.project && !!config.env && view == launchdarklyFeatureFlags`,
	`!(resourceExtname != .json && resourceLangId != json)`,
	`!HTTP_PROXY_SERVER_RUNNING`,
	`!LOGGED_IN`,
	`!LivePreviewServerOn`,
	`!abm.inited`,
	`!activeCustomEditorId && resourceFilename =~ /^.*\\.dash\\.(json|yaml|yml)$/`,
	`!activeCustomEditorId && resourceFilename =~ /^.*\\.dash\\.(json|yaml|yml)/`,
	`!activeCustomEditorId && resourceFilename =~ /^.*\\.sw\\.(json|yaml|yml)$/`,
	`!activeCustomEditorId && resourceFilename =~ /^.*\\.sw\\.(json|yaml|yml)/`,
	`!activeEditor || editorLangId =~ /(php|blade)/`,
	`!activeEditorGroupEmpty`,
	`!activeEditorIsPinned`,
	`!appmap.initialized`,
	`!appworks:projectIsNotTargetFramework && !appworks:projectIsPegasus`,
	`!appworks:projectIsNotTargetType`,
	`!ast-results-Confirmed`,
	`!ast-results-Ignored`,
	`!ast-results-NotExploitable`,
	`!ast-results-NotIgnored`,
	`!ast-results-Proposed`,
	`!ast-results-ToVerify`,
	`!ast-results-Urgent`,
	`!ast-results.isValidCredentials`,
	`!auth0:authenticated`,
	`!azdataAvailable && view == objectExplorer && viewItem =~ /^(disconnectedServer|Server|Database)$/`,
	`!azure-account.signedIn`,
	`!breadcrumbsPossible`,
	`!c3.runTest.isCurrentlyRunning`,
	`!cadquery.enabled`,
	`!calva:outputWindowActive`,
	`!classroom:is-initialized`,
	`!cmake:hideBuildCommand \u0026\u0026 cmake:enableFullFeatureSet`,
	`!cmake:hideDebugCommand \u0026\u0026 cmake:enableFullFeatureSet`,
	`!cmake:hideLaunchCommand \u0026\u0026 cmake:enableFullFeatureSet`,
	`!codesandbox.signedIn`,
	`!codeseeIsLoggedIn && githubExtensionInstalled`,
	`!codesight.setupComplete`,
	`!codeswing:inSwingWorkspace && !codeswing:inSwing`,
	`!codetogether.views.useWebview`,
	`!codi.isAuthenticated`,
	`!commentStyler.boldON&&commentStyler.drawerOpen`,
	`!commentStyler.drawerOpen`,
	`!commentStyler.italicON&&commentStyler.drawerOpen`,
	`!commentStyler.outlineON&&commentStyler.drawerOpen`,
	`!commentStyler.superscriptON&&commentStyler.drawerOpen`,
	`!commentThreadIsEmpty && commentController == vsls-comments`,
	`!commentThreadIsEmpty && commentThread == 1`,
	`!commentThreadIsEmpty && commentThread == 2`,
	`!commentThreadIsEmpty`,
	`!config.MinifyAll.disableCodeContextMenu && editorLangId in extension.supportedFiletypes || !config.MinifyAll.disableCodeContextMenu && editorLangId == php`,
	`!config.MinifyAll.disableCodeContextMenu && editorLangId in extension.supportedFiletypes`,
	`!config.MinifyAll.disableFileExplorerContextMenu`,
	`!config.SFDocs.markdownAssistant.disableCustom`,
	`!config.aws.samcli.legacyDeploy && isFileSystemResource && resourceFilename =~ /^(template\\.(json|yml|yaml))|(samconfig\\.toml)$/`,
	`!config.aws.samcli.legacyDeploy && view == aws.explorer && viewItem =~ /^(awsLambdaNode|awsRegionNode|awsCloudFormationRootNode)$/`,
	`!config.aws.samcli.legacyDeploy && view == aws.explorer`,
	`!config.aws.samcli.legacyDeploy`,
	`!config.bravo.hostEnabled`,
	`!config.breadcrumbs.enabled`,
	`!config.cloudcode.beta.enableOneActivityBar && config.cloudcode.beta.enableApigeeExplorers`,
	`!config.cloudcode.beta.enableOneActivityBar`,
	`!config.codeQL.variantAnalysis.controllerRepo`,
	`!config.codedeploy.isApplicationWorkspace`,
	`!config.cros-ide.boardsAndPackages.showWelcomeMessage && !cros-ide.chrootPath`,
	`!config.discloud.token`,
	`!config.emacs-mcx.cursorMoveOnFindWidget && editorFocus && findWidgetVisible && !isComposing && !config.emacs-mcx.useMetaPrefixMacCmd`,
	`!config.emacs-mcx.cursorMoveOnFindWidget && editorFocus && findWidgetVisible && !isComposing && config.emacs-mcx.useMetaPrefixCtrlLeftBracket`,
	`!config.emacs-mcx.cursorMoveOnFindWidget && editorFocus && findWidgetVisible && !isComposing && config.emacs-mcx.useMetaPrefixEscape`,
	`!config.emacs-mcx.cursorMoveOnFindWidget && editorFocus && findWidgetVisible && !isComposing && config.emacs-mcx.useMetaPrefixMacCmd`,
	`!config.emacs-mcx.cursorMoveOnFindWidget && editorFocus && findWidgetVisible && !replaceInputFocussed && !isComposing && !config.emacs-mcx.useMetaPrefixMacCmd`,
	`!config.emacs-mcx.cursorMoveOnFindWidget && editorFocus && findWidgetVisible && !replaceInputFocussed && !isComposing && config.emacs-mcx.useMetaPrefixCtrlLeftBracket`,
	`!config.emacs-mcx.cursorMoveOnFindWidget && editorFocus && findWidgetVisible && !replaceInputFocussed && !isComposing && config.emacs-mcx.useMetaPrefixEscape`,
	`!config.emacs-mcx.cursorMoveOnFindWidget && editorFocus && findWidgetVisible && !replaceInputFocussed && !isComposing && config.emacs-mcx.useMetaPrefixMacCmd`,
	`!config.emacs-mcx.cursorMoveOnFindWidget && editorFocus && findWidgetVisible && !replaceInputFocussed && !isComposing`,
	`!config.emacs-mcx.useMetaPrefixMacCmd`,
	`!config.endevor.experimental.workspaceSync`,
	`!config.favorites.singleListMode && view == favorites-explorer-view`,
	`!config.favorites.singleListMode && view == favorites-own-view`,
	`!config.fictionWriter.edit.disableKeybindings && textInputFocus && !editorReadonly && editorLangId == markdown && !suggestWidgetVisible`,
	`!config.git.openDiffOnClick && view == azdoprStatus:azdo && viewItem =~ /filechange(?!:DELETE)/`,
	`!config.gitHistory.hideCommitViewExplorer && git.commit.view.show && git.commit.selected`,
	`!config.gitHistory.hideCompareCommitViewExplorer && git.commit.compare.view.show && git.commit.compare.compared`,
	`!config.gitlens.fileAnnotations.command && gitlens:activeFileStatus =~ /blameable/ && !gitlens:annotationStatus && config.gitlens.menus.editorGroup.blame`,
	`!config.gitlens.plusFeatures.enabled`,
	`!config.gitlens.views.repositories.showBranchComparison`,
	`!config.gitlens.views.repositories.showBranches`,
	`!config.gitlens.views.repositories.showCommits`,
	`!config.gitlens.views.repositories.showContributors`,
	`!config.gitlens.views.repositories.showRemotes`,
	`!config.gitlens.views.repositories.showStashes`,
	`!config.gitlens.views.repositories.showTags`,
	`!config.gitlens.views.repositories.showUpstreamStatus`,
	`!config.gitlens.views.repositories.showWorktrees`,
	`!config.hack4impact.recommendations.keybindings.disableAll`,
	`!config.hero-heroku.apiKey`,
	`!config.latex-utilities.formattedPaste.useAsDefault && editorTextFocus && editorLangId == 'latex'`,
	`!config.latex-workshop.bind.altKeymap.enabled && editorTextFocus && !editorReadonly && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$/`,
	`!config.latex-workshop.bind.altKeymap.enabled && editorTextFocus && !editorReadonly && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$|^pweave$/`,
	`!config.latex-workshop.bind.altKeymap.enabled`,
	`!config.maximizeterminal.useAlternativeKeybinding && !activePanel`,
	`!config.memo-life-for-you.memoPath`,
	`!config.memo-life-for-you.withRespectMode`,
	`!config.mock-server.settings.contextMenu.makeRequest`,
	`!config.mock-server.settings.contextMenu.mockIt && resourceExtname == .json || !config.mock-server.settings.contextMenu.mockIt && resourceExtname == .jsonc || !config.mock-server.settings.contextMenu.mockIt && resourceExtname == .js || !config.mock-server.settings.contextMenu.mockIt && resourceExtname == .har`,
	`!config.mock-server.settings.contextMenu.pasteServerConfig`,
	`!config.mock-server.settings.contextMenu.setAsServerConfig && resourceExtname == .json || !config.mock-server.settings.contextMenu.setAsServerConfig && resourceExtname == .jsonc || !config.mock-server.settings.contextMenu.setAsServerConfig && resourceExtname == .js`,
	`!config.mock-server.settings.contextMenu.setPort`,
	`!config.mock-server.settings.contextMenu.stopServer && isServerRunning`,
	`!config.mock-server.settings.contextMenu.switchEnvironment`,
	`!config.mock-server.settings.contextMenu.transformToMockDB && resourceExtname == .json || !config.mock-server.settings.contextMenu.transformToMockDB && resourceExtname == .jsonc || !config.mock-server.settings.contextMenu.transformToMockDB && resourceExtname == .har`,
	`!config.npm.keybindingsChangedWarningShown && !config.npm.oldKeybindings.enable`,
	`!config.objectscript.showExplorer`,
	`!config.powermode.enabled`,
	`!config.sort-json.settings.contextMenu.ascendingSort`,
	`!config.sort-json.settings.contextMenu.customSort`,
	`!config.sort-json.settings.contextMenu.descendingSort`,
	`!config.sort-json.settings.contextMenu.isCaseSensitive && !config.sort-json.settings.isCaseSensitive`,
	`!config.sort-json.settings.contextMenu.isCaseSensitive && config.sort-json.settings.isCaseSensitive`,
	`!config.sort-json.settings.contextMenu.setListSortType`,
	`!config.sort-json.settings.contextMenu.setObjectSortType`,
	`!config.sort-json.settings.contextMenu.setSortLevel`,
	`!config.sqltools.disableChordKeybindings && !(editorTextFocus || editorHasSelection)`,
	`!config.sqltools.disableChordKeybindings && editorTextFocus && !editorReadonly && editorHasSelection`,
	`!config.sqltools.disableChordKeybindings && editorTextFocus && editorHasSelection`,
	`!config.sqltools.disableChordKeybindings`,
	`!config.statsBar.enabled`,
	`!config.terminalAllInOne.disableAllKeybindings`,
	`!config.touchui.enable`,
	`!config.touchuiwx.enable`,
	`!config.vscode-corda.isCordaProject`,
	`!console-ninja.started && console-ninja.canBeStarted`,
	`!count`,
	`!cpp-runner.running`,
	`!dart-code:anyFlutterProjectLoaded && inDebugMode && debugType == dart && config.dart.showDevToolsDebugToolBarButtons`,
	`!dbux.context.activated`,
	`!dbuxWebView.context.dbux-graph.isActive && !dbux.context.isApplicationEmpty && dbux.context.showNavButton`,
	`!debuggersAvailable`,
	`!dendron:pluginActive && shellExecutionSupported`,
	`!devto:authorized && view == devto`,
	`!devtools.isTSProject && !devtools.isJSProject`,
	`!diffMerge.panelFocused && inDiffEditor`,
	`!digitaljs.view_isfocus`,
	`!dockerInstalled && validApigeeWorkspace`,
	`!docsView.documentationView.isPinned`,
	`!documatic.isLoggedIn`,
	`!dvc.commands.available || !dvc.project.available || !dvc.project.hasData`,
	`!dvc.commands.available || dvc.cli.incompatible || !dvc.project.available`,
	`!dvt.isCalledFromEclipseChe`,
	`!dvt.isExtensionActive`,
	`!e4e.treeView.initialized`,
	`!editorFocus && !inQuickOpen`,
	`!editorFocus && !inputFocus && activeEditor == 'WebviewEditor'`,
	`!editorFocus && !searchInputBoxFocus`,
	`!editorFocus && !terminalFocus && !problemFocus && !inDebugRepl`,
	`!editorFocus && activeEditor == 'workbench.editor.extension'`,
	`!editorFocus`,
	`!editorHasMultipleSelections && editorHasSelection && editorLangId==restructuredtext`,
	`!editorHasMultipleSelections`,
	`!editorHasRenameProvider && editorTextFocus && !editorReadonly`,
	`!editorHasSelection && config.shareGitFile.enableMarkdownSupport`,
	`!editorHasSelection && config.vscsm.showMenu.fileHistoryInSublimeMerge == 'withoutSelection' && config.git.enabled && gitOpenRepositoryCount != 0`,
	`!editorHasSelection && config.vscsm.showMenu.myCommitsInSublimeMerge == 'withoutSelection' && config.git.enabled && gitOpenRepositoryCount != 0`,
	`!editorHasSelection && editorHasTranslationHighlighting`,
	`!editorHasSelection && editorLangId == cpp`,
	`!editorHasSelection`,
	`!editorIsOpen && !multipleEditorGroups && config.quitControl.closeTabShouldTryToCloseEmptyWindow && isWindows`,
	`!editorIsOpen && !multipleEditorGroups && config.quitControl.closeTabShouldTryToCloseEmptyWindow`,
	`!editorIsOpen && !multipleEditorGroups`,
	`!editorIsOpen || editorIsOpen`,
	`!editorIsOpen`,
	`!editorReadOnly`,
	`!editorReadonly && editorLangId == cfml`,
	`!editorReadonly && editorLangId == markdown`,
	`!editorReadonly && editorTextFocus && editorLangId == markdown`,
	`!editorReadonly && view == sqltoolsViewConnectionExplorer && viewItem =~ /^connection\\.(.+)/`,
	`!editorReadonly && view == sqltoolsViewConnectionExplorer && viewItem =~ /^connection\\.(table|view)$/`,
	`!editorReadonly &&editorLangId == markdown`,
	`!editorReadonly`,
	`!editorTextFocus`,
	`!electron-build-tools:ready || electron-build-tools:active`,
	`!emacs-mcx.acceptingArgument && editorTextFocus && !config.emacs-mcx.useMetaPrefixMacCmd`,
	`!emacs-mcx.acceptingArgument && editorTextFocus && config.emacs-mcx.enableDigitArgument && !config.emacs-mcx.useMetaPrefixMacCmd`,
	`!emacs-mcx.acceptingArgument && editorTextFocus && config.emacs-mcx.enableDigitArgument && config.emacs-mcx.useMetaPrefixCtrlLeftBracket`,
	`!emacs-mcx.acceptingArgument && editorTextFocus && config.emacs-mcx.enableDigitArgument && config.emacs-mcx.useMetaPrefixEscape`,
	`!emacs-mcx.acceptingArgument && editorTextFocus && config.emacs-mcx.enableDigitArgument && config.emacs-mcx.useMetaPrefixMacCmd`,
	`!emacs-mcx.acceptingArgument && editorTextFocus && config.emacs-mcx.useMetaPrefixCtrlLeftBracket`,
	`!emacs-mcx.acceptingArgument && editorTextFocus && config.emacs-mcx.useMetaPrefixEscape`,
	`!emacs-mcx.acceptingArgument && editorTextFocus && config.emacs-mcx.useMetaPrefixMacCmd`,
	`!emacs-mcx.acceptingArgument && emacs-mcx.prefixArgumentExists && editorTextFocus`,
	`!enableBuild && !enableGit && !enableTools`,
	`!enableExtras`,
	`!enableGit && !enableBuild && !enableTools`,
	`!enableGit && gitOpenRepositoryCount >= 1`,
	`!explorer-exclude.missingWorkspace && !explorer-exclude.hasLoaded`,
	`!explorerResourceIsFolder  && fileReferencesOpen`,
	`!explorerResourceIsFolder &&  in deltanjiSupportedSchemes`,
	`!explorerResourceIsFolder &&  resourceFilename =~ /.*\\.fif/`,
	`!explorerResourceIsFolder && !listMultiSelection && diffMergeFileSelected`,
	`!explorerResourceIsFolder && !listMultiSelection`,
	`!explorerResourceIsFolder && config.code-runner.showRunCommandInExplorerContextMenu`,
	`!explorerResourceIsFolder && config.py2flowchart.showRunCommandInExplorerContextMenu`,
	`!explorerResourceIsFolder && config.tasks-context.showAddFileToActiveTaskInExplorerContext`,
	`!explorerResourceIsFolder && isFileSystemResource`,
	`!explorerResourceIsFolder && isLinux`,
	`!explorerResourceIsFolder && resource =~ //FileCabinet/(SuiteApps|SuiteScripts|Templates/(E-mail%20Templates|Marketing%20Templates)|Web%20Site%20Hosting%20Files)/.+$/`,
	`!explorerResourceIsFolder && resource =~ //FileCabinet/(SuiteScripts|Templates/(E-mail%20Templates|Marketing%20Templates)|Web%20Site%20Hosting%20Files)/.+$/`,
	`!explorerResourceIsFolder && resource =~ //Objects/.+\\.xml$/`,
	`!explorerResourceIsFolder && resourceExtname == .gcl-p`,
	`!explorerResourceIsFolder && resourceExtname =~ /wx[av]pkg$/`,
	`!explorerResourceIsFolder && resourceFilename =~ /.+\\.(zip|vsix|crx|asar)$/i`,
	`!explorerResourceIsFolder && resourceLangId == class`,
	`!explorerResourceIsFolder && resourceLangId == jar`,
	`!explorerResourceIsFolder && resourceLangId == xml`,
	`!explorerResourceIsFolder && resourceScheme in deltanjiSupportedSchemes`,
	`!explorerResourceIsFolder && sfdx:project_opened && resourceLangId != 'forcesourcemanifest'`,
	`!explorerResourceIsFolder `,
	`!explorerResourceIsFolder || !filesExplorerFocus`,
	`!explorerResourceIsFolder&&resourceExtname==.js`,
	`!explorerResourceIsFolder`,
	`!explorerResourceIsRoot && !explorerResourceIsFolder && gitlens:enabled && config.gitlens.menus.explorer.compare`,
	`!explorerResourceIsRoot && !explorerResourceIsFolder && gitlens:enabled && config.gitlens.menus.explorer.history`,
	`!explorerResourceIsRoot && !explorerResourceIsFolder && gitlens:enabled && gitlens:hasRemotes && config.gitlens.menus.explorer.clipboard`,
	`!explorerResourceIsRoot && !explorerResourceIsFolder && gitlens:enabled && gitlens:hasRemotes && config.gitlens.menus.explorer.remote`,
	`!explorerResourceIsRoot && !explorerResourceIsFolder`,
	`!explorerViewletVisible && !filesExplorerFocus && explorerResourceIsRoot && inputFocus`,
	`!extension.graalvm.remoteMode && visualvm.featureSet == -1`,
	`!extension.graalvm.remoteMode && visualvm.featureSet == 0`,
	`!extension.graalvm.remoteMode`,
	`!extension.loadingCompleted`,
	`!fig.isAuthenticated`,
	`!findInputFocussed && !config.emacs-mcx.useMetaPrefixMacCmd`,
	`!findInputFocussed && config.emacs-mcx.useMetaPrefixCtrlLeftBracket`,
	`!findInputFocussed && config.emacs-mcx.useMetaPrefixEscape`,
	`!findInputFocussed && config.emacs-mcx.useMetaPrefixMacCmd`,
	`!findInputFocussed`,
	`!findWidgetVisible`,
	`!frontMatterCanInit`,
	`!fw:hasOpenedNote && view == fw-notes`,
	`!fx-extension.isTeamsFx && !fx-extension.canUpgradeToArmAndMultiEnv && !fx-extension.isNewUser`,
	`!fx-extension.isTeamsFx && !fx-extension.canUpgradeToArmAndMultiEnv && fx-extension.isNewUser`,
	`!fx-extension.isTeamsFx && fx-extension.canUpgradeToArmAndMultiEnv && isWorkspaceTrusted`,
	`!gcloudComponentsInstalling`,
	`!gcloudInstalling && !gcloudLoggedIn`,
	`!gcode:views:navTree:enabled`,
	`!gcode:views:stats:enabled`,
	`!git.commit.compare.view.show`,
	`!git.commit.view.show`,
	`!git.live.Last12Months.selected`,
	`!git.live.Last24Hours.selected`,
	`!git.live.Last2Weeks.selected`,
	`!git.live.Last3Months.selected`,
	`!git.live.Last3Years.selected`,
	`!git.live.Last4Weeks.selected`,
	`!git.live.Last7Days.selected`,
	`!git.live.diff.leftBase.selected`,
	`!git.live.diff.leftTheirRemote.selected`,
	`!git.live.diff.leftYourLocal.selected`,
	`!git.live.diff.rightTheirLocal.selected`,
	`!git.live.diff.rightTheirRemote.selected`,
	`!git.live.inProgress.selected && git.live.view.org`,
	`!git.live.localDoc.Last12Months.selected`,
	`!git.live.localDoc.Last24Hours.selected`,
	`!git.live.localDoc.Last2Weeks.selected`,
	`!git.live.localDoc.Last3Months.selected`,
	`!git.live.localDoc.Last3Years.selected`,
	`!git.live.localDoc.Last4Weeks.selected`,
	`!git.live.localDoc.Last7Days.selected`,
	`!git.live.localDoc.nonConflicting.selected`,
	`!git.live.localDoc.rightTheirLocal.selected`,
	`!git.live.localDoc.rightTheirRemote.selected`,
	`!git.live.offline.selected && git.live.view.org`,
	`!git.live.pushedChanges.selected && git.live.view.org`,
	`!git.live.remoteDiff.pushedChanges.selected`,
	`!git.live.remoteDiff.unpushedChanges.selected`,
	`!git.live.stale.selected && git.live.view.org`,
	`!git.live.todo.selected && git.live.view.org`,
	`!git.live.tree.leftBase.selected && !git.live.view.org`,
	`!git.live.tree.leftTheirRemote.selected && !git.live.view.org`,
	`!git.live.tree.leftYourLocal.selected && !git.live.view.org`,
	`!git.live.tree.rightTheirLocal.selected && !git.live.view.org`,
	`!git.live.tree.rightTheirRemote.selected && !git.live.view.org`,
	`!git.live.unpushedChanges.selected && git.live.view.org`,
	`!git.live.view.org && git.live.Last12Months.selected`,
	`!git.live.view.org && git.live.Last24Hours.selected`,
	`!git.live.view.org && git.live.Last2Weeks.selected`,
	`!git.live.view.org && git.live.Last3Months.selected`,
	`!git.live.view.org && git.live.Last3Years.selected`,
	`!git.live.view.org && git.live.Last4Weeks.selected`,
	`!git.live.view.org && git.live.Last7Days.selected`,
	`!github:stateValidated && github:focusedReview`,
	`!github:stateValidated`,
	`!githubExtensionInstalled`,
	`!gitlab:noAccount`,
	`!gitlab:noToken`,
	`!gitlens:disabled && !gitlens:disabledToggleCodeLens`,
	`!gitlens:disabled && !gitlens:hasVirtualFolders && gitlens:plus:enabled`,
	`!gitlens:disabled && !gitlens:hasVirtualFolders`,
	`!gitlens:disabled && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders`,
	`!gitlens:disabled && !gitlens:readonly`,
	`!gitlens:disabled && gitlens:plus:enabled`,
	`!gitlens:disabled`,
	`!gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:status(\\-branch)?:upstream:none/`,
	`!gitlens:hasRemotes && !gitlens:readonly && viewItem =~ /gitlens:status(\\-branch)?:upstream:none/`,
	`!gitlens:hasVirtualFolders && gitlens:activeFileStatus =~ /revision/`,
	`!gitlens:hasVirtualFolders && gitlens:activeFileStatus =~ /tracked/`,
	`!gitlens:hasVirtualFolders && scmProvider == git && scmResourceGroup =~ /^(workingTree|index|merge)$/ && config.gitlens.menus.scmItem.compare`,
	`!gitlens:hasVirtualFolders && view =~ /^gitlens\\.views\\.(fileHistory|lineHistory)/ && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+committed\\b)/`,
	`!gitlens:hasVirtualFolders && view =~ /^gitlens\\.views\\.commits/`,
	`!gitlens:hasVirtualFolders && view =~ /^gitlens\\.views\\.fileHistory/ && !gitlens:views:fileHistory:cursorFollowing`,
	`!gitlens:hasVirtualFolders && view =~ /^gitlens\\.views\\.fileHistory/ && gitlens:views:fileHistory:cursorFollowing`,
	`!gitlens:hasVirtualFolders && view =~ /^gitlens\\.views\\.fileHistory/`,
	`!gitlens:hasVirtualFolders && view =~ /^gitlens\\.views\\.searchAndCompare\\b/`,
	`!gitlens:hasVirtualFolders && viewItem =~ /gitlens:(branch\\b(?!.*?\\b\\+current\\b)|commit\\b|stash\\b|tag\\b)/`,
	`!gitlens:hasVirtualFolders && viewItem =~ /gitlens:(branch|commit|file\\b(?=.*?\\b\\+committed\\b)|stash|tag)\\b/`,
	`!gitlens:hasVirtualFolders && viewItem =~ /gitlens:(branch|commit|stash|tag)\\b/ && gitlens:views:canCompare`,
	`!gitlens:hasVirtualFolders && viewItem =~ /gitlens:(branch|commit|stash|tag)\\b/`,
	`!gitlens:hasVirtualFolders && viewItem =~ /gitlens:(branch|tag)\\b/`,
	`!gitlens:hasVirtualFolders && viewItem =~ /gitlens:branch\\b(?!.*?\\b\\+current\\b)/`,
	`!gitlens:hasVirtualFolders && viewItem =~ /gitlens:branch\\b(?=.*?\\b\\+tracking\\b)/`,
	`!gitlens:hasVirtualFolders && viewItem =~ /gitlens:repo-folder\\b/`,
	`!gitlens:hasVirtualFolders && viewItem =~ /gitlens:repository\\b/`,
	`!gitlens:hasVirtualFolders && webviewItem =~ /gitlens:(branch\\b(?!.*?\\b\\+current\\b)|commit\\b|stash\\b|tag\\b)/`,
	`!gitlens:hasVirtualFolders && webviewItem =~ /gitlens:(branch|commit|stash|tag)\\b/`,
	`!gitlens:hasVirtualFolders && webviewItem =~ /gitlens:branch\\b(?!.*?\\b\\+current\\b)/`,
	`!gitlens:hasVirtualFolders && webviewItem =~ /gitlens:branch\\b(?=.*?\\b\\+tracking\\b)/`,
	`!gitlens:hasVirtualFolders`,
	`!gitlens:plus:required`,
	`!gitlens:plus`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && scmProvider == git && scmResourceGroup =~ /^(workingTree|index|merge)$/ && config.gitlens.menus.scmItem.compare`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && view =~ /^gitlens\\.views\\.branches/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && view =~ /^gitlens\\.views\\.commits/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && view =~ /^gitlens\\.views\\.contributors/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && view =~ /^gitlens\\.views\\.stashes/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && view =~ /gitlens\\.views\\.branches\\b/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && view =~ /gitlens\\.views\\.remotes/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && view =~ /gitlens\\.views\\.tags\\b/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem == gitlens:stash`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem == gitlens:stashes`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem == gitlens:tag`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /^gitlens:(stashes|status:files)$/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:branch\\b(?!.*?\\b\\+current\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:branch\\b(?=.*?\\b\\+current\\b)(?=.*?\\b\\+tracking\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:branch\\b(?=.*?\\b\\+current\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:branch\\b/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:branches\\b/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:commit\\b(?!.*?\\b\\+current\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:commit\\b(?=.*?\\b\\+HEAD\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:commit\\b(?=.*?\\b\\+current\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:commit\\b/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:contributor\\b(?!.*?\\b\\+current\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:contributors\\b/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:file\\b((?=.*?\\b\\+committed\\b)|:results\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:file\\b(?!.*?\\b\\+conflicted\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+(un)?staged\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+committed\\b)(?=.*?\\b\\+HEAD\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+committed\\b)(?=.*?\\b\\+current\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+committed\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+staged\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+stashed\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+unstaged\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:folder\\b(?=.*?\\b\\+working\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:remote\\b/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:remotes\\b/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:repo-folder\\b/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:repository\\b/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:status:upstream/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:tag\\b/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:tags\\b/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:worktrees\\b/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && webviewItem == gitlens:stash`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && webviewItem == gitlens:tag`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && webviewItem == gitlens:wip`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && webviewItem =~ /gitlens:branch\\b(?!.*?\\b\\+current\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && webviewItem =~ /gitlens:branch\\b(?=.*?\\b\\+current\\b)(?=.*?\\b\\+tracking\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && webviewItem =~ /gitlens:branch\\b(?=.*?\\b\\+current\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && webviewItem =~ /gitlens:branch\\b/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && webviewItem =~ /gitlens:commit\\b(?!.*?\\b\\+current\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && webviewItem =~ /gitlens:commit\\b(?=.*?\\b\\+HEAD\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && webviewItem =~ /gitlens:commit\\b(?=.*?\\b\\+current\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && webviewItem =~ /gitlens:commit\\b/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && webviewItem =~ /gitlens:contributor\\b(?!.*?\\b\\+current\\b)/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && webviewItem =~ /gitlens:tag\\b/`,
	`!gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders`,
	`!gitlens:readonly && view =~ /^gitlens\\.views\\.(fileHistory|lineHistory)/ && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+committed\\b)/`,
	`!gitlens:readonly && view =~ /^gitlens\\.views\\.branches/`,
	`!gitlens:readonly && view =~ /^gitlens\\.views\\.commits/`,
	`!gitlens:readonly && view =~ /^gitlens\\.views\\.stashes/`,
	`!gitlens:readonly && view =~ /gitlens\\.views\\.branches\\b/`,
	`!gitlens:readonly && view =~ /gitlens\\.views\\.remotes/`,
	`!gitlens:readonly && view =~ /gitlens\\.views\\.tags\\b/`,
	`!gitlens:readonly && viewItem == gitlens:stash`,
	`!gitlens:readonly && viewItem == gitlens:stashes`,
	`!gitlens:readonly && viewItem == gitlens:tag`,
	`!gitlens:readonly && viewItem =~ /^gitlens:(stashes|status:files)$/`,
	`!gitlens:readonly && viewItem =~ /gitlens:branch\\b(?!.*?\\b\\+current\\b)/`,
	`!gitlens:readonly && viewItem =~ /gitlens:branch\\b(?=.*?\\b\\+current\\b)(?=.*?\\b\\+tracking\\b)/`,
	`!gitlens:readonly && viewItem =~ /gitlens:branch\\b(?=.*?\\b\\+current\\b)/`,
	`!gitlens:readonly && viewItem =~ /gitlens:branch\\b/`,
	`!gitlens:readonly && viewItem =~ /gitlens:branches\\b/`,
	`!gitlens:readonly && viewItem =~ /gitlens:commit\\b(?!.*?\\b\\+current\\b)/`,
	`!gitlens:readonly && viewItem =~ /gitlens:commit\\b(?=.*?\\b\\+HEAD\\b)/`,
	`!gitlens:readonly && viewItem =~ /gitlens:commit\\b(?=.*?\\b\\+current\\b)/`,
	`!gitlens:readonly && viewItem =~ /gitlens:commit\\b/`,
	`!gitlens:readonly && viewItem =~ /gitlens:file\\b((?=.*?\\b\\+committed\\b)|:results\\b)/`,
	`!gitlens:readonly && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+(un)?staged\\b)/`,
	`!gitlens:readonly && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+committed\\b)(?=.*?\\b\\+HEAD\\b)/`,
	`!gitlens:readonly && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+committed\\b)(?=.*?\\b\\+current\\b)/`,
	`!gitlens:readonly && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+committed\\b)/`,
	`!gitlens:readonly && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+staged\\b)/`,
	`!gitlens:readonly && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+stashed\\b)/`,
	`!gitlens:readonly && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+unstaged\\b)/`,
	`!gitlens:readonly && viewItem =~ /gitlens:remote\\b/`,
	`!gitlens:readonly && viewItem =~ /gitlens:remotes\\b/`,
	`!gitlens:readonly && viewItem =~ /gitlens:repo-folder\\b/`,
	`!gitlens:readonly && viewItem =~ /gitlens:repository\\b/`,
	`!gitlens:readonly && viewItem =~ /gitlens:status:upstream/`,
	`!gitlens:readonly && viewItem =~ /gitlens:tag\\b/`,
	`!gitlens:readonly && viewItem =~ /gitlens:tags\\b/`,
	`!gitlens:readonly && viewItem =~ /gitlens:worktree\\b(?!.*?\\b\\+(active|main)\\b)/`,
	`!gitops:loadingClusters && gitops:failedToLoadClusterContexts`,
	`!gitops:loadingClusters && gitops:noClusters`,
	`!gitops:loadingSources && !gitops:currentClusterGitOpsNotEnabled && !gitops:noClusterSelected && gitops:noSources`,
	`!gitops:loadingWorkloads && !gitops:currentClusterGitOpsNotEnabled && !gitops:noClusterSelected && gitops:noWorkloads`,
	`!hasPackageJson`,
	`!hide-node-modules:isHidden && hide-node-modules:containsPackageJson`,
	`!hideScanCommands`,
	`!hoi4MULoaded || hoi4PreviewType == event`,
	`!hubspot.configPath`,
	`!hubspot.terminal.versions.installed.hs`,
	`!iceworks:projectIsNotTarget && !iceworks:projectIsPegasus`,
	`!iceworks:projectIsNotTarget`,
	`!inDebugMode && !terminalFocus`,
	`!inDebugMode && activeViewlet == 'workbench.view.debug' && debugConfigurationType == 'node' || !inDebugMode && activeViewlet == 'workbench.view.debug' && debugConfigurationType == 'pwa-extensionHost' || !inDebugMode && activeViewlet == 'workbench.view.debug' && debugConfigurationType == 'pwa-node'`,
	`!inDebugMode && config.iar.enabled`,
	`!inDebugMode && debugConfigurationType == 'node' || !inDebugMode && debugConfigurationType == 'pwa-extensionHost' || !inDebugMode && debugConfigurationType == 'pwa-node'`,
	`!inDebugMode && debugConfigurationType == 'php'`,
	`!inDebugMode && debugConfigurationType=='php'`,
	`!inDebugMode && debugState == 'inactive'`,
	`!inDebugMode && debuggersAvailable && fx-extension.runIconActive`,
	`!inDebugMode && editorLangId == python`,
	`!inDebugMode \u0026\u0026 !cmake:hideDebugCommand \u0026\u0026 cmake:enableFullFeatureSet`,
	`!inDebugMode \u0026\u0026 inCMakeProject \u0026\u0026 !cmake:hideDebugCommand \u0026\u0026 cmake:enableFullFeatureSet`,
	`!inDebugMode`,
	`!inDebugRepl`,
	`!inOutput && config.code-runner.showRunCommandInEditorContextMenu`,
	`!inOutput && config.command-runner.showRunCommandInEditorContextMenu`,
	`!inOutput && editorFocus && (editorLangId == php || editorLangId == hack)`,
	`!inOutput && editorFocus && editorLangId == php`,
	`!inOutput && editorLangId == data-weave && bat.runCurrentBatTest.enabled`,
	`!inOutput && editorLangId==fcs && config.fcs-vscode.beta`,
	`!inOutput && editorLangId==fcs`,
	`!inOutput && editorTextFocus  && editorHasSelection`,
	`!inOutput && editorTextFocus && editorHasSelection`,
	`!inOutput && editorTextFocus && editorLangId == python`,
	`!inOutput && editorTextFocus && resourceExtname  == .py && editorHasSelection`,
	`!inOutput && editorTextFocus && resourceExtname  == .vue && editorHasSelection`,
	`!inOutput && editorTextFocus && resourceExtname  == .vue `,
	`!inOutput && editorTextFocus`,
	`!inOutput && resourceLangId == 'NGINX'`,
	`!inOutput && resourceLangId == 'nginx'`,
	`!inOutput`,
	`!inQuickOpen && !terminalFocus && editorLangId == python`,
	`!inQuickOpen && !terminalFocus`,
	`!inQuickOpen`,
	`!infracost:active`,
	`!isAppStudio`,
	`!isAppcanProject && !inDebugMode && 1===2`,
	`!isAppcanProject && !inDebugMode`,
	`!isAuthed`,
	`!isBuiltinExtension && remoteName =~ /^(dev-container|attached-container|wsl|ssh-remote)?$/`,
	`!isBuiltinExtension && remoteName =~ /codespaces*.*/`,
	`!isBuiltinExtension`,
	`!isChoreoAuthEnabled`,
	`!isCloud9`,
	`!isIOS`,
	`!isInDiffEditor && !editorReadonly`,
	`!isInDiffEditor && !markdownPreviewFocus && config.Beautify active file`,
	`!isInDiffEditor && !markdownPreviewFocus && config.Format document with`,
	`!isInDiffEditor && !markdownPreviewFocus && config.Open files list`,
	`!isInDiffEditor && !markdownPreviewFocus && config.Save active file`,
	`!isInDiffEditor && !markdownPreviewFocus && config.ShortcutMenuBar.beautify`,
	`!isInDiffEditor && !markdownPreviewFocus && config.ShortcutMenuBar.formatWith`,
	`!isInDiffEditor && !markdownPreviewFocus && config.ShortcutMenuBar.openFilesList`,
	`!isInDiffEditor && !markdownPreviewFocus && config.ShortcutMenuBar.save`,
	`!isInDiffEditor && !markdownPreviewFocus && editorLangId == cfml || editorLangId == html`,
	`!isInDiffEditor && !markdownPreviewFocus && editorLangId == cfml`,
	`!isInDiffEditor && !markdownPreviewFocus`,
	`!isInDiffEditor && !virtualWorkspace && sol.hasActiveSolSpec`,
	`!isInDiffEditor && config.perforce.editorButtons.diffPrevAndNext =~ /(All|Only)/ && resourceScheme == perforce && perforce.currentFile.hasRevision`,
	`!isInDiffEditor && editorIsOpen && resourceFilename =~ /\\.snap(\\.new)?$/`,
	`!isInDiffEditor && perforce.currentFile.resourceRevision != 1`,
	`!isInDiffEditor && perforce.currentFile.status != OPEN && perforce.currentFile.status != NOT_OPEN && config.perforce.editorButtons.diffPrevAndNext =~ /All/ && resourceScheme == perforce`,
	`!isInDiffEditor && perforce.currentFile.status != OPEN && perforce.currentFile.status != NOT_OPEN && resourceScheme == perforce`,
	`!isInDiffEditor && perforce.currentFile.status =~ /OPEN/ && config.perforce.editorButtons.diffPrevAndNext =~ /All/`,
	`!isInDiffEditor && perforce.currentFile.status =~ /OPEN/ && config.perforce.editorButtons.diffPrevAndNext =~ /Only/ && resourceScheme == perforce`,
	`!isInDiffEditor && perforce.currentFile.status =~ /OPEN/`,
	`!isInDiffEditor && resourceFilename=~ /.*\\.ros\\.json|.*\\.ros\\.yaml/`,
	`!isInDiffEditor`,
	`!isInEmbeddedEditor && editorTextFocus && resourceExtname =~ /\\.feature$/`,
	`!isInEmbeddedEditor && editorTextFocus && resourcePath =~ /steps/ && resourceExtname =~ /\\.py$/`,
	`!isInsiders`,
	`!isLinux`,
	`!isMac && !isWindows && view in ext:allFavoriteViews && viewItem =~ /^resource/`,
	`!isMac && editorLangId == nextbasic`,
	`!isMac && view == l13ProjectsWorkspaces`,
	`!isMac`,
	`!isNxWorkspace && !isAngularWorkspace`,
	`!isNxWorkspace && isAngularWorkspace && config.nxConsole.enableGenerateFromContextMenu`,
	`!isPerfadvisorConfigured && !refreshFailure`,
	`!isPerfadvisorConfigured`,
	`!isPerfadvisorLoggedIn`,
	`!isSkaffolderProject`,
	`!isTreeEmpty`,
	`!isTrustedWorkspace`,
	`!isWeb && !remoteName`,
	`!isWeb && editorLangId == cobol && mfcobol.isCobolSourceFile`,
	`!isWeb && editorLangId == pli && mfcobol.isPliSourceFile`,
	`!isWeb && isWindows && mfcobol.supportsOpenBMS && resourceLangId == bms`,
	`!isWeb && isWindows && resourceLangId == dialogsystem`,
	`!isWeb && mfcobol.supportsSubmitJCL && resourceLangId == jcl`,
	`!isWeb && remoteHub:hasVirtualFolders`,
	`!isWeb && sfdx:project_opened`,
	`!isWeb && view == AzureMLExplorer && viewItem =~ /^AzureMLComputeInstance(Running|Stopped).*CanUse$/`,
	`!isWeb || activeViewlet == workbench.view.remote`,
	`!isWeb`,
	`!isWindows && !explorerResourceIsFolder`,
	`!isWindows && view == fridaApps && viewItem =~ /^app\\|/`,
	`!isWindows && view == fridaPs && viewItem =~ /^process\\|/`,
	`!isWindows`,
	`!isWorkflowRerun && viewItem =~ /circleci:job:build/`,
	`!isWorkflowRerun && viewItem =~ /circleci:workflow/`,
	`!isWorkflowRerun && viewItem =~ /circleci:workflow:failed/ || viewItem =~ /circleci:workflow:error/ || viewItem =~ /circleci:workflow:canceled/`,
	`!isWorkspaceTrusted || !titanium:enabled`,
	`!isWorkspaceTrusted`,
	`!isportconfigured && portipconfig || !isportlogined`,
	`!isportconfigured && portipconfig`,
	`!isportlogined && isportconfigured`,
	`!java:projectManagerActivated || workspaceFolderCount == 0`,
	`!java:projectManagerActivated`,
	`!javascript-repl.started && resourceExtname =~ /(js|mjs|jsx|ts|tsx|vue|coffee|md)$/i`,
	`!jsDebugIsMapSteppingDisabled`,
	`!jupyter.hasKernels && config.jupyter.kernelManagement.enabled`,
	`!kb-macro.recording && config.keyboardMacro.recordingShortcuts == 'Option1'`,
	`!kb-macro.recording && config.keyboardMacro.recordingShortcuts == 'Option2'`,
	`!kestrel.ctx.account`,
	`!kolo.isLoggedIn`,
	`!kunpengStopTask && view == build-tasks-lists && viewItem == taskProfileNode`,
	`!kunpengStopTestCase && view == test-case-lists && viewItem == testProfileNode`,
	`!listMultiSelection && viewItem == ELEMENT_IN_PLACE || !listMultiSelection && viewItem == ELEMENT_UP_THE_MAP`,
	`!listMultiSelection && viewItem == ELEMENT_IN_PLACE`,
	`!listMultiSelection && viewItem == ELEMENT_UP_THE_MAP`,
	`!listMultiSelection && viewItem == Invocation || !listMultiSelection && viewItem == Invocation-selected`,
	`!liveshare:isEnding && liveshare:state != SharingInProgress && liveshare:state != JoiningInProgress`,
	`!liveshare:isWeb && config.liveshare.accessibility.accessibilityFeaturesEnabled != off`,
	`!liveshare:isWeb && isWindows && config.liveshare.accessibility.accessibilityFeaturesEnabled != off`,
	`!liveshare:isWeb && liveshare:isServerShared && liveshare:state == Shared`,
	`!liveshare:isWeb && liveshare:state == Shared`,
	`!liveshare:isWeb`,
	`!markdownTables`,
	`!metals:enabled || workbenchState != empty`,
	`!neteasemusic.on && config.NeteaseMusic.reset.control`,
	`!neteasemusic.on`,
	`!nhctl.upgrade && !extensionActivated`,
	`!noResourceFiles`,
	`!notebookEditorFocused && editorLangId == 'markdown'`,
	`!notebookEditorFocused && editorLangId =~ /^(spec-data|csv-(row|column)|dppmca|chiplot)$/`,
	`!notebookEditorFocused && editorLangId =~ /^(spec-data|csv-(row|column)|dppmca|chiplot)/`,
	`!notebookEditorFocused`,
	`!nrf-connect.apps.loaded`,
	`!nrf-connect.build.loaded`,
	`!nrf-connect.devices.loaded && !nrf-connect.noCommandLineTools`,
	`!nrf-connect.hasDeviceTree`,
	`!octopus.activeEditorIsOcl`,
	`!openDiffOnClick && view == prStatus:github && viewItem =~ /filechange(?!:DELETE)/`,
	`!openFolderWorkspaceSupport && workbenchState == 'workspace'`,
	`!openapiErrors && view == openapiThreeComponentsOutline`,
	`!openapiErrors && view == openapiThreePathOutline && viewItem == path`,
	`!openapiErrors && view == openapiThreePathOutline`,
	`!openapiErrors && view == openapiThreeSecurityOutline`,
	`!openapiErrors && view == openapiThreeServersOutline`,
	`!openapiErrors && view == openapiThreeSpecOutline && openapiMissingInfo`,
	`!openapiErrors && view == openapiTwoDefinitionOutline`,
	`!openapiErrors && view == openapiTwoParametersOutline`,
	`!openapiErrors && view == openapiTwoPathOutline && viewItem == path`,
	`!openapiErrors && view == openapiTwoPathOutline`,
	`!openapiErrors && view == openapiTwoResponsesOutline`,
	`!openapiErrors && view == openapiTwoSecurityDefinitionOutline`,
	`!openapiErrors && view == openapiTwoSecurityOutline`,
	`!openapiErrors && view == openapiTwoSpecOutline && openapiMissingBasePath`,
	`!openapiErrors && view == openapiTwoSpecOutline && openapiMissingHost`,
	`!openapiErrors && view == openapiTwoSpecOutline && openapiMissingInfo`,
	`!openapiTwoEnabled && !openapiThreeEnabled`,
	`!packageMetrics:packageJsonActive`,
	`!ponicodeShowGitDiff:active`,
	`!ponicodeTreeView:active && ponicodeTreeView:ready`,
	`!ponicodeTreeView:active`,
	`!quokka.autoLogEnabled`,
	`!quokka.isLiveShareClient && quokka.hasActiveSession`,
	`!quokka.isLiveShareClient && quokka.isProfilingEnabled && quokka.isActiveEditorRunningQuokka`,
	`!quokka.isLiveShareClient`,
	`!r.liveShare:aborted`,
	`!raycast.workspaceEnabled && raycast.extensionLoaded`,
	`!remoteHub:hasRecentRepositories`,
	`!remoteHub:hasVirtualFolders && isWeb && !remoteName`,
	`!remoteName && !virtualWorkspace`,
	`!remoteName && isWindows && !virtualWorkspace`,
	`!remoteName && isWindows && !wsl.noWSLPath && workspaceFolderCount != 0 && !virtualWorkspace && wslFeatureInstalled`,
	`!remoteName`,
	`!replaceInputFocussed && editorLangId == rx`,
	`!replaceInputFocussed`,
	`!resourceIsFile && resourceIsFile`,
	`!resourceLangId`,
	`!roblox-ts:compilerActive`,
	`!runningInTheia`,
	`!runningOnCloudShell && viewItem == ProjectObject`,
	`!runningOnCloudShell`,
	`!sap.ux.applicationModeler.isSystemTreeEmpty`,
	`!scmProvider || scmProvider == deltanji`,
	`!scmProvider`,
	`!searchInputBoxFocus && !findInputFocussed && !inCommandsPicker && !inQuickOpen && brightscript.isRemoteControlMode`,
	`!searchViewletVisible`,
	`!sfdx:internal_dev`,
	`!shouldHideHoi4Preview && resourceScheme != webview-panel`,
	`!shouldShowHoi4Preview && editorLangId =~ /^(:?plaintext|hoi4|paradox)$/ && !shouldHideHoi4Preview`,
	`!snyk:error && !snyk:loggedIn && snyk:authenticating`,
	`!snyk:error && !snyk:loggedIn`,
	`!snyk:error && snyk:loggedIn && snyk:codeEnabled && !snyk:codeLocalEngineEnabled && snyk:workspaceFound`,
	`!snyk:error && snyk:loggedIn && snyk:featuresSelected && !snyk:workspaceFound`,
	`!snyk:loggedIn || snyk:error || snyk:featuresSelected && !snyk:workspaceFound`,
	`!snyk:loggedIn`,
	`!sqltools.file.connectionAttached && editorLangId == sql`,
	`!statsBar.cpuLoad`,
	`!statsBar.loadavg`,
	`!statsBar.memoUsage`,
	`!statsBar.networkSpeed`,
	`!statsBar.uptime`,
	`!stitch:previewActive && resourceFilename =~ /.integration.json$/`,
	`!storyExplorer.initialLoadComplete || storyExplorer.loadingStories`,
	`!suggestWidgetVisible && resourceScheme == 'vscode-interactive' && interactiveInputCursorAtBoundary != 'bottom' && interactiveInputCursorAtBoundary != 'none'`,
	`!suggestWidgetVisible && resourceScheme == 'vscode-interactive' && interactiveInputCursorAtBoundary != 'none' && interactiveInputCursorAtBoundary != 'top'`,
	`!swift.fileIsSnippet && editorLangId == swift`,
	`!swimm.loginState`,
	`!terminalFocus && !config.npm.oldKeybindings.enable`,
	`!terminalFocus && !filesExplorerFocus && resourceLangId == markdown`,
	`!terminalFocus && !notebookEditorFocused`,
	`!terminalFocus && !quokka.isLiveShareClient && quokka.hasActiveSession && quokka.isActiveEditorRunningQuokka`,
	`!terminalFocus && !quokka.isLiveShareClient && quokka.hasActiveSession`,
	`!terminalFocus && !quokka.isLiveShareClient`,
	`!terminalFocus && config.npm.oldKeybindings.enable`,
	`!terminalFocus && editorLangId == python`,
	`!terminalFocus && editorLangId == raml`,
	`!terminalFocus`,
	`!textCompareEditorVisible`,
	`!textInputFocus && !terminalFocus`,
	`!todo-tree-is-empty`,
	`!treeview.pinned && view == sidebar-outline`,
	`!un.enable`,
	`!validApigeeWorkspace`,
	`!vercelLoggedIn`,
	`!view.arm.cmsis-csolution.context.hidden`,
	`!virtual-folders-empty`,
	`!virtualWorkspace && !config.powerPlatform.experimental.disableActivityBarPanels`,
	`!virtualWorkspace && !liveshare:isWeb`,
	`!virtualWorkspace && !pacCLI.authPanel.interactiveLoginSupported`,
	`!virtualWorkspace && isWorkspaceTrusted`,
	`!virtualWorkspace && pacCLI.authPanel.interactiveLoginSupported`,
	`!virtualWorkspace && shellExecutionSupported && editorLangId == python`,
	`!virtualWorkspace && shellExecutionSupported`,
	`!virtualWorkspace && view == pacCLI.authPanel && pacCLI.authPanel.interactiveLoginSupported`,
	`!virtualWorkspace && view == pacCLI.authPanel && viewItem == DATAVERSE`,
	`!virtualWorkspace && view == pacCLI.authPanel`,
	`!virtualWorkspace && view == pacCLI.envAndSolutionsPanel && viewItem == ENVIRONMENT`,
	`!virtualWorkspace && view == pacCLI.envAndSolutionsPanel && viewItem == SOLUTION`,
	`!virtualWorkspace && view == pacCLI.envAndSolutionsPanel`,
	`!virtualWorkspace`,
	`!vmssoftware.synchronizer.isInAction`,
	`!vrdev:properties:empty && view =~ /^vrdev\\.views\\.properties/`,
	`!vscode-cnb.isAuthorized`,
	`!vscode-cnb.isTargetWorkspace`,
	`!wallaby.paused`,
	`!wallaby.started && wallaby.feature.exclusiveTestRun && explorerResourceIsFolder && resourceFilename != .idea && resourceFilename != .vs && resourceFilename != .vscode && resourceFilename != node_modules || !wallaby.started && wallaby.feature.exclusiveTestRun && resourceLangId == javascript && resourceFilename =~ /^(?!wallaby).*\\.js$/ && resourceFilename != jest.config.js || !wallaby.started && wallaby.feature.exclusiveTestRun && resourceLangId == javascriptreact || !wallaby.started && wallaby.feature.exclusiveTestRun && resourceLangId == typescript || !wallaby.started && wallaby.feature.exclusiveTestRun && resourceLangId == typescriptreact || !wallaby.started && wallaby.feature.exclusiveTestRun && resourceLangId == coffeescript || !wallaby.started && wallaby.feature.exclusiveTestRun && resourceLangId == vue`,
	`!watermelon.isLogged`,
	`!waveAnalysis.signedIn && !loading && waveAnalysis.extensionIsActive`,
	`!workbench.explorer.emptyView.active && hubspot.configPath`,
	`!workbench.explorer.emptyView.active`,
	`!workspace.workspaceFolders`,
	`!workspaceContains:**/Anchor.toml`,
	`!workspaceFolderCount`,
	`!wsl.wslDistrosInstalled`,
	`!wslFeatureInstalled`,
	`!yandexMusic.isPlaying`,
	`!zeplin:loggedIn`,
	`%isActiveConnection%`,
	`%isConnectedConnection%`,
	`%isConnection%`,
	`%isConnectionWithTekton%`,
	`%isDebuggableProject%`,
	`%isDisabledProject%`,
	`%isDisabledRemoteConnection%`,
	`%isDisconnectedRemoteConnection%`,
	`%isEnabledAutoBuildOff%`,
	`%isEnabledAutoBuildOn%`,
	`%isEnabledAutoInjectMetricsOff%`,
	`%isEnabledAutoInjectMetricsOn%`,
	`%isEnabledProject%`,
	`%isEnabledRemoteConnection%`,
	`%isInViewWithNothingSelected%`,
	`%isLocalCodewind%`,
	`%isNoConnections%`,
	`%isNoProjects%`,
	`%isProject%`,
	`%isProjectAndNotWorkspaceFolder%`,
	`%isProjectWithMetricsDashboard%`,
	`%isProjectWithPerfDashboard%`,
	`%isRemoteConnection%`,
	`%isRestartableProject%`,
	`%isShellableProject%`,
	`%isStartedLocalCodewind%`,
	`%isStartedProject%`,
	`%isStoppedLocalCodewind%`,
	`(!editorHasCodeActionsProvider && editorTextFocus) || !editorTextFocus`,
	`(!readmeMarkdownFocused &&(markdownPreviewFocus || resourceLangId == markdown)) || readmeMarkdownFocused`,
	`(config.bravo.hostEnabled && !bravo.canPreview) || bravo.connectionFailed`,
	`(editorLangId == haskell) || (editorLangId == literate haskell)`,
	`(editorLangId == javascript || editorLangId == typescript) && terminalFocus`,
	`(githubActions:activeFile =~ /rdispatch/ || githubActions:activeFile =~ /wdispatch/) && resourceExtname =~ /\\.ya?ml/`,
	`(isLinux || isMac) && isFileSystemResource`,
	`(liveshare:state != Shared && liveshare:state != Joined) || (liveshare:state == SignedOut)`,
	`(resourceLangId == html || resourceLangId == typescript) && !virtualWorkspace`,
	`(resourceLangId == javascript || resourceLangId == typescript) && editorHasSelection`,
	`(view == codemap-own-view || view == codemap-explorer-view) && codemap.autoReveal == false`,
	`(view == gerrit:searchResults || view == gerrit:changeExplorer) && (gerrit:searchQuery || gerrit:searchChangeNumber)`,
	`(view == hanadb || view == hanaeditor) && viewItem`,
	`*`,
	`0 == 1`,
	`0`,
	`1 == 0`,
	`1 == 2`,
	`APXRActive && resourceExtname =~ /(.apex|.soql)/`,
	`APXRActive == false`,
	`AutoRABITActive && CreatePrevalidationRequestActive`,
	`AutoRABITActive`,
	`CCDXActive && resourceExtname == .page`,
	`CCDXActive && resourceExtname =~ /(.flexipage|.xml)/`,
	`CCDXActive`,
	`CSLPreviewActive || cslSourceActive`,
	`CSLPreviewActive`,
	`C_Cpp_Runner:activatedExtension`,
	`ChromeEnabled && jsonOutlineEnabled`,
	`CodeActionMenuVisible`,
	`Convert && resourceExtname == .csproj`,
	`CustomPolicyExplorerEnabled`,
	`DScript.workspaceLoaded && DScript.moduleBrowserEnabled`,
	`DScript.workspaceLoaded && DScript.solutionExplorerEnabled`,
	`DScript.workspaceLoaded && viewItem == DScript.module`,
	`DScript.workspaceLoaded && viewItem == DScript.spec`,
	`EVExtensionActive && !EVCompilerSettingsExist`,
	`EVExtensionActive && ContainerSettingsExist`,
	`EVExtensionActive && EVCompilerSettingsExist`,
	`EVExtensionActive`,
	`ExtensionLoaded && editorHasSelection`,
	`ExtensionLoaded && view == GistTree && GistAscending == false`,
	`ExtensionLoaded && view == GistTree && GistAscending == true`,
	`ExtensionLoaded && view == GistTree`,
	`ExtensionLoaded && view == SubscriptionTree && SubscriptionAscending == false`,
	`ExtensionLoaded && view == SubscriptionTree && SubscriptionAscending == true`,
	`ExtensionLoaded && view == SubscriptionTree`,
	`ExtensionLoaded`,
	`False`,
	`ForceCodeActive && !explorerResourceIsFolder && ForceCodeLoggedIn`,
	`ForceCodeActive && ForceCodeLoggedIn && isInDiffEditor`,
	`ForceCodeActive && ForceCodeLoggedIn`,
	`ForceCodeActive && editorIsOpen && ForceCodeLoggedIn`,
	`ForceCodeActive && editorIsOpen && editorHasSelection && ForceCodeLoggedIn`,
	`ForceCodeActive && resourceLangId == apex && ForceCodeLoggedIn`,
	`ForceCodeActive && resourceLangId == css && ForceCodeLoggedIn`,
	`ForceCodeActive && resourceLangId == html && ForceCodeLoggedIn`,
	`ForceCodeActive && resourceLangId == javascript && ForceCodeLoggedIn`,
	`ForceCodeActive && resourceLangId == svg && ForceCodeLoggedIn`,
	`ForceCodeActive && resourceLangId == visualforce && ForceCodeLoggedIn`,
	`ForceCodeActive`,
	`ForceCodeShowMenu`,
	`GDLOutlineEnabled`,
	`GoogleUserTokenExists && HideCompleted`,
	`GoogleUserTokenExists && ShowCompleted`,
	`GoogleUserTokenExists`,
	`HTTP_PROXY_SERVER_RUNNING`,
	`IsScopeFile`,
	`LDFlagToggle`,
	`LOGGED_IN`,
	`LinkedEditingInputVisible && editorTextFocus`,
	`Live-Workspace.enabled`,
	`LivePreviewServerOn`,
	`MdTableEditor.isTreeViewEnabled`,
	`Migrate && resourceExtname == .csproj`,
	`MinimumOneConnectionIsOnline`,
	`MinimumOneTableInStructure`,
	`None`,
	`PreviewActive && activeWebviewPanelId == 'preview'`,
	`PreviewActive`,
	`RAWLabs.raw.correctWorkspaceState && !RAWLabs.raw.isDemo`,
	`RAWLabs.raw.correctWorkspaceState || RAWLabs.raw.isDemo`,
	`RSS-0-enabled`,
	`RSS-1-enabled`,
	`RSS-2-enabled`,
	`RSS-3-enabled`,
	`RSS-4-enabled`,
	`RSS-5-enabled`,
	`RSS-6-enabled`,
	`RSS-7-enabled`,
	`RSS-8-enabled`,
	`RSS-9-enabled`,
	`ReposManagerStateContext != NeedsAuthentication && !azdo:focusedReview`,
	`ReposManagerStateContext != NeedsAuthentication`,
	`ReposManagerStateContext == NeedsAuthentication && !github:hasGitHubRemotes && gitOpenRepositoryCount`,
	`ReposManagerStateContext == NeedsAuthentication`,
	`RunBySerialport`,
	`SearchEnabled`,
	`TargetFrameworkSwitcherTreeView:showTree`,
	`WebdriverEnabled`,
	`XMLLSReady`,
	`^3.7.8`,
	``,
	`abapfs:extensionActive`,
	`abm.active && (view == abm.welcomeView || view == abm.infoView)`,
	`abm.active && abm.cleanable && view == abm.welcomeView`,
	`abm.active && abm.cleanable`,
	`abm.active && abm.has_debug && view == abm.welcomeView`,
	`abm.active && abm.has_debug`,
	`abm.active && editorLangId =~ /c(pp)?/`,
	`abm.active && view == abm.welcomeView`,
	`abm.active`,
	`abm.err.locate && workbenchState == folder`,
	`abm.err.locate && workbenchState == workspace`,
	`abm.err.parse`,
	`abm.inited && !abm.err.locate`,
	`abm.inited && workbenchState == empty`,
	`acceptSuggestionOnEnter && suggestWidgetHasFocusedSuggestion && suggestWidgetVisible && suggestionMakesTextEdit && textInputFocus`,
	`acceptSuggestionOnEnter && suggestWidgetVisible && suggestionMakesTextEdit && textInputFocus`,
	`accessibilityHelpWidgetVisible && editorFocus`,
	`accessibilityModeEnabled && terminalA11yTreeFocus && terminalNavigationModeActive && terminalProcessSupported || accessibilityModeEnabled && terminalFocus && terminalNavigationModeActive && terminalProcessSupported`,
	`accessibilityModeEnabled && terminalA11yTreeFocus && terminalProcessSupported || accessibilityModeEnabled && terminalFocus && terminalProcessSupported`,
	`accessibilityModeEnabled && terminalA11yTreeFocus && terminalProcessSupported`,
	`actionlock.isInstalledMDTasks`,
	`actionlock.isTrue`,
	`activeCustomEditorId != 'editor.excalidraw' && resourceFilename =~ /^.+\\.excalidraw(.png|.svg)?$/`,
	`activeCustomEditorId != 'svgPreview.previewEditor' && activeEditor != 'workbench.editor.sidebysideEditor' && resourceExtname == .svg`,
	`activeCustomEditorId == 'diffViewer' && config.diffviewer.outputFormat == 'line-by-line'`,
	`activeCustomEditorId == 'diffViewer' && config.diffviewer.outputFormat == 'side-by-side'`,
	`activeCustomEditorId == 'editor.excalidraw' && resourceFilename =~ /^.+\\.excalidraw(.svg)?$/`,
	`activeCustomEditorId == 'editor.excalidraw' && resourceFilename =~ /^.+\\.excalidraw.png$/`,
	`activeCustomEditorId == 'editor.excalidraw'`,
	`activeCustomEditorId == 'quarto.visualEditor'`,
	`activeCustomEditorId == 'svgPreview.previewEditor' && resourceExtname == .svg`,
	`activeCustomEditorId == 'svgPreview.previewEditor' && svgPreviewFocus && !svgPreviewMaxZoom`,
	`activeCustomEditorId == 'svgPreview.previewEditor' && svgPreviewFocus && !svgPreviewMinZoom`,
	`activeCustomEditorId == 'svgPreview.previewEditor' && svgPreviewFocus && && !svgPreviewMinZoom`,
	`activeCustomEditorId == 'sysl.multiView' || activeCustomEditorId == 'sysld.multiView`,
	`activeCustomEditorId == 'tga.previewEditor'`,
	`activeCustomEditorId == hexEditor.hexedit`,
	`activeCustomEditorId == java.formatterSettingsEditor`,
	`activeCustomEditorId == kieKogitoWebviewEditorsServerlessWorkflow`,
	`activeCustomEditorId == vscodeVueCustomEditor.counterEditor3`,
	`activeCustomEditorId == vscodeVueCustomEditor.counterEditor`,
	`activeEditor != 'workbench.editor.settings2'`,
	`activeEditor != workbench.editor.notebook && editorLangId in julia.supportedLanguageIds`,
	`activeEditor && !quokka.isLiveShareClient && quokka.hasActiveSession && quokka.isActiveEditorRunningQuokka`,
	`activeEditor && !quokka.isLiveShareClient && quokka.isActiveEditorRunningQuokka && !quokka.autoLogEnabled`,
	`activeEditor && !quokka.isLiveShareClient && quokka.isActiveEditorRunningQuokka && !quokka.showSingleInlineValueEnabled && !quokka.autoLogEnabled`,
	`activeEditor && !quokka.isLiveShareClient && quokka.isActiveEditorRunningQuokka && !quokka.showValueOnSelectionEnabled && !quokka.autoLogEnabled`,
	`activeEditor && !quokka.isLiveShareClient && quokka.isActiveEditorRunningQuokka && !quokka.traceBeingNavigated && !inDebugMode || activeEditor && !quokka.isLiveShareClient && !quokka.traceBeingNavigated && !inDebugMode && resourceScheme == quokka-code-timeline`,
	`activeEditor && !quokka.isLiveShareClient && quokka.isActiveEditorRunningQuokka && !quokka.traceBeingNavigated`,
	`activeEditor && !quokka.isLiveShareClient && quokka.isActiveEditorRunningQuokka && quokka.autoLogEnabled`,
	`activeEditor && !quokka.isLiveShareClient && quokka.isActiveEditorRunningQuokka && quokka.showSingleInlineValueEnabled && !quokka.autoLogEnabled`,
	`activeEditor && !quokka.isLiveShareClient && quokka.isActiveEditorRunningQuokka && quokka.showValueOnSelectionEnabled && !quokka.autoLogEnabled`,
	`activeEditor && !quokka.isLiveShareClient && quokka.isActiveEditorRunningQuokka && quokka.traceBeingNavigated && !inDebugMode || activeEditor && !quokka.isLiveShareClient && quokka.traceBeingNavigated && !inDebugMode && resourceScheme == quokka-code-timeline`,
	`activeEditor && !quokka.isLiveShareClient && quokka.isActiveEditorRunningQuokka && quokka.traceBeingNavigated && !quokka.traceBeingAutoPlayed && resourceScheme != quokka-code-timeline || activeEditor && !quokka.isLiveShareClient && quokka.traceBeingNavigated && !quokka.traceBeingAutoPlayed && resourceScheme == quokka-code-timeline`,
	`activeEditor && !quokka.isLiveShareClient && quokka.isActiveEditorRunningQuokka && quokka.traceBeingNavigated && !quokka.traceBeingAutoPlayed`,
	`activeEditor && !quokka.isLiveShareClient && quokka.isActiveEditorRunningQuokka && quokka.traceBeingNavigated && quokka.hasAnyEnabledBreakpointsInActiveEditor && !quokka.traceBeingAutoPlayed && resourceScheme != quokka-code-timeline || activeEditor && !quokka.isLiveShareClient && quokka.traceBeingNavigated && quokka.hasAnyEnabledBreakpointsInActiveEditor && !quokka.traceBeingAutoPlayed && resourceScheme == quokka-code-timeline`,
	`activeEditor && !quokka.isLiveShareClient && quokka.isActiveEditorRunningQuokka && quokka.traceBeingNavigated && quokka.traceBeingAutoPlayed`,
	`activeEditor && !quokka.isLiveShareClient && quokka.isActiveEditorRunningQuokka && quokka.traceBeingNavigated`,
	`activeEditor && !quokka.isLiveShareClient && quokka.isActiveEditorRunningQuokka`,
	`activeEditor && config.hexeditor.showOpenFileButton`,
	`activeEditor && editorLangId != magit`,
	`activeEditor && wallaby.feature.exclusiveTestRun`,
	`activeEditor && wallaby.started && wallaby.feature.exclusiveTestRun && !wallaby.paused`,
	`activeEditor == 'workbench.editor.settings2'`,
	`activeEditor == workbench.editors.files.textFileEditor && debuggersAvailable && !inDebugMode`,
	`activeEditor == workbench.editors.files.textFileEditor && inDebugMode && config.run-button.actionWhileRunning == restart`,
	`activeEditor == workbench.editors.files.textFileEditor && inDebugMode && config.run-button.actionWhileRunning == stop`,
	`activeEditor == workbench.editors.textDiffEditor && resourcePath in github:unviewedFiles`,
	`activeEditor == workbench.editors.textDiffEditor && resourcePath in github:viewedFiles`,
	`activeEditor == workbench.editors.textDiffEditor`,
	`activeEditorCanSplitInGroup`,
	`activeEditorGroupEmpty && multipleEditorGroups`,
	`activeEditorIsPinned`,
	`activeEditor`,
	`activePanel`,
	`activeViewlet == 'workbench.view.explorer' && explorerResourceIsFolder`,
	`activeViewlet == 'workbench.view.extension.gondola_explorer'`,
	`activeViewlet == 'workbench.view.extension.hupuMoyuTreeView' || nbaTreeView.active == true || bxjTreeView.active == true`,
	`activeWebviewPanelId == 'bigquery-query-results'`,
	`activeWebviewPanelId == 'browserPreview'`,
	`activeWebviewPanelId == decentraland.PublishScene`,
	`activeWebviewPanelId == decentraland.RunScene`,
	`activeWebviewPanelId == fossil.renderPanel`,
	`activeWebviewPanelId == markdown50.preview`,
	`activeWebviewPanelId == rapidapi.request.view`,
	`adb-helper:adbSupport`,
	`add.when.clicked`,
	`adonisProjectLoaded == true`,
	`agdaMode && !editorHasSelection && editorTextFocus`,
	`agdaMode && !terminalFocus`,
	`agdaMode && agdaModePrompting || agdaModeTyping`,
	`agdaMode && agdaModeTyping`,
	`agdaMode`,
	`agfHasFileSelection`,
	`al-object-helper.hasALExtensions`,
	`al-object-helper.isALExtension`,
	`alExtensionActive && !inDebugMode`,
	`alExtensionActive && inDebugMode`,
	`alExtensionActive`,
	`allowWrapIferr`,
	`android.enabled`,
	`angular_workspace`,
	`antlr4.hasImports`,
	`antlr4.isLexer || antlr4.isCombined`,
	`antlr4.isLexer || antlr4.isParser || antlr4.isCombined`,
	`antlr4.isParser || antlr4.isCombined`,
	`appmap.initialized && !appmap.hasData`,
	`appmap.initialized && !appmap.userAuthenticated`,
	`appmap.initialized && appmap.analysisEnabled && !appmap.numFindings && !appmap.analysisPerformed`,
	`appmap.initialized && appmap.analysisEnabled && appmap.numFindings == 0 && appmap.analysisPerformed`,
	`appmap.initialized && appmap.hasData && appmap.numResults == 0`,
	`appmap.initialized && appmap.userAuthenticated && !appmap.findingsEnabled`,
	`appmap.recordingIsRunning`,
	`appworks:isJSXFile`,
	`appworks:projectIsNotTargetType`,
	`areOpenScadPreviews`,
	`arepl && config.AREPL.showIcons`,
	`argdownPreviewFocus`,
	`asciidocPreviewFocus`,
	`asn1vcx.showTreeView == true`,
	`ast-results-Confirmed`,
	`ast-results-Ignored`,
	`ast-results-NotExploitable`,
	`ast-results-NotIgnored`,
	`ast-results-Proposed`,
	`ast-results-ToVerify`,
	`ast-results-Urgent`,
	`astEnable`,
	`atEndOfWord && textInputFocus && !hasOtherSuggestions && !inSnippetMode && !suggestWidgetVisible && config.editor.tabCompletion == 'on'`,
	`atlascode:bitbucketExplorerEnabled && config.atlascode.bitbucket.enabled`,
	`atlascode:bitbucketIssuesExplorerEnabled && config.atlascode.bitbucket.enabled`,
	`atlascode:isBBAuthenticated && atlascode:bitbucketExplorerEnabled && view == atlascode.views.bb.pullrequestsTreeView`,
	`atlascode:isBBAuthenticated && view == atlascode.views.bb.issuesTreeView`,
	`atlascode:isJiraAuthenticated && config.atlascode.jira.enabled`,
	`atlascode:jiraExplorerEnabled && config.atlascode.jira.enabled`,
	`atlascode:pipelineExplorerEnabled && config.atlascode.bitbucket.enabled`,
	`auth.isLoggedIn == false`,
	`auth.isLoggedIn == true && config.docify.programmingLanguage == auto-detect`,
	`auth.isLoggedIn == true`,
	`auth0:authenticated  && auth0:isLabWorkspace`,
	`auth0:authenticated && auth0:isLabWorkspace`,
	`auth0:authenticated && explorerResourceIsFolder && resourcePath =~ /lab/`,
	`auth0:authenticated && resourceFilename =~ /auth0\\.ya?ml|tenant\\.ya?ml/`,
	`auth0:authenticated && view == apisView`,
	`auth0:authenticated && view == appsView`,
	`auth0:authenticated`,
	`authenticatedvar`,
	`aws.isDevMode || isCloud9`,
	`azALDevTools.extensionActive`,
	`azALDevTools.findDuplicateCodeActive`,
	`azALDevTools.warningDirectivesActive`,
	`azdataAvailable && view == dataworkspace.views.main && viewItem == databaseProject.itemType.file.table`,
	`azdo:focusedReview`,
	`azdo:inReviewMode`,
	`azurePipelineOverviewEnabled`,
	`azureSphereExplorerActivated && tenantExplorerSupported`,
	`azureSphereExplorerActivated`,
	`azurerm-vscode-tools-isParamFile`,
	`bannerFocused`,
	`behave-vsc.stepReferences.navKeysEnabled`,
	`behave-vsc.stepReferences.visible`,
	`bicepVisualizerFocus`,
	`bigfish.frameworkName == 'Bigfish' && editorLangId =~ /^typescriptreact$|^javascriptreact$/`,
	`bigfish.frameworkName == 'Bigfish' && resourceExtname =~ /^.tsx$|^.jsx$/`,
	`bigfish.frameworkName == 'Bigfish' || bigfish.frameworkName == 'Umi'`,
	`bigfish.frameworkName == 'Bigfish'`,
	`bigfish.routes.empty`,
	`blabla`,
	`blitzmax:ready != true`,
	`bng.processManagerActive`,
	`bpmnEditorActive`,
	`bravo.isTemplateWorkspace`,
	`breadcrumbsActive && breadcrumbsVisible && listFocus && !inputFocus`,
	`breadcrumbsActive && breadcrumbsVisible`,
	`breadcrumbsPossible && breadcrumbsVisible`,
	`breadcrumbsPossible`,
	`breakpointWidgetVisible && editorFocus`,
	`breakpointWidgetVisible && inBreakpointWidget`,
	`breakpointWidgetVisible && textInputFocus`,
	`breakpointsFocused && !breakpointInputFocused`,
	`breakpointsFocused && !breakpointSelected`,
	`breakpointsFocused && !inputFocus`,
	`breakpointsFocused`,
	`buckProjectLoaded_edit_when_completed`,
	`buckProjectLoaded`,
	`bucketExplorer:enabled`,
	`buildCodeFocus`,
	`buildViewEnabled`,
	`c3.packagesTree.empty`,
	`c3.runTest.isCurrentlyRunning`,
	`cadquery.enabled`,
	`calendar-in-explorer`,
	`callGraphViewEnabled`,
	`callHierarchyVisible && !config.editor.stablePeek`,
	`callHierarchyVisible && callHierarchyDirection == 'incomingCalls'`,
	`callHierarchyVisible && callHierarchyDirection == 'outgoingCalls'`,
	`callHierarchyVisible`,
	`calva:connected && calva:keybindingsEnabled && editorLangId == clojure`,
	`calva:connected && calva:outputWindowActive`,
	`calva:keybindingsEnabled && calva:connected && !calva:outputWindowActive`,
	`calva:keybindingsEnabled && calva:connected && calva:outputWindowActive`,
	`calva:keybindingsEnabled && calva:hasInlineResults && editorTextFocus && !editorHasMultipleSelections && !editorHasSelection && !editorReadOnly && !hasOtherSuggestions && !parameterHintsVisible && !selectionAnchorSet && !suggestWidgetVisible && !inlineSuggestionVisible && editorLangId == clojure`,
	`calva:keybindingsEnabled && calva:outputWindowActive && calva:outputWindowSubmitOnEnter && editorTextFocus && !suggestWidgetVisible`,
	`calva:keybindingsEnabled && editorLangId == clojure && calva:connected && !calva:outputWindowActive`,
	`calva:keybindingsEnabled && editorLangId == clojure && calva:connected && calva:outputWindowActive`,
	`calva:keybindingsEnabled && editorLangId == clojure && calva:connected`,
	`calva:keybindingsEnabled && editorLangId == clojure && editorTextFocus && !editorHasSelection`,
	`calva:keybindingsEnabled && editorLangId == clojure && editorTextFocus && !editorReadOnly && !inSnippetMode && !suggestWidgetVisible && !hasOtherSuggestions && !inSnippetMode && !inlineSuggestionVisible`,
	`calva:keybindingsEnabled && editorLangId == clojure && editorTextFocus && !editorReadOnly && !suggestWidgetVisible && !hasOtherSuggestions`,
	`calva:keybindingsEnabled && editorLangId == clojure && editorTextFocus && !selectionAnchorSet && paredit:keyMap =~ /original|strict/`,
	`calva:keybindingsEnabled && editorLangId == clojure && editorTextFocus && !suggestWidgetVisible`,
	`calva:keybindingsEnabled && editorLangId == clojure && editorTextFocus && editorHasSelection`,
	`calva:keybindingsEnabled && editorLangId == clojure && editorTextFocus && paredit:keyMap == strict && !editorReadOnly && !editorHasMultipleSelections && !calva:cursorInComment`,
	`calva:keybindingsEnabled && editorLangId == clojure && editorTextFocus && paredit:keyMap == strict && !editorReadOnly && !editorHasMultipleSelections`,
	`calva:keybindingsEnabled && editorLangId == clojure && editorTextFocus && paredit:keyMap =~ /original|strict/ && !calva:cursorInComment`,
	`calva:keybindingsEnabled && editorLangId == clojure && editorTextFocus && paredit:keyMap =~ /original|strict/ && !config.calva.paredit.hijackVSCodeDefaults && !calva:cursorInComment || calva:cursorAfterComment`,
	`calva:keybindingsEnabled && editorLangId == clojure && editorTextFocus && paredit:keyMap =~ /original|strict/ && !config.calva.paredit.hijackVSCodeDefaults && !calva:cursorInComment || calva:cursorBeforeComment`,
	`calva:keybindingsEnabled && editorLangId == clojure && editorTextFocus && paredit:keyMap =~ /original|strict/ && config.calva.paredit.hijackVSCodeDefaults && !calva:cursorInComment || calva:cursorAfterComment`,
	`calva:keybindingsEnabled && editorLangId == clojure && editorTextFocus && paredit:keyMap =~ /original|strict/ && config.calva.paredit.hijackVSCodeDefaults && !calva:cursorInComment || calva:cursorBeforeComment`,
	`calva:keybindingsEnabled && editorLangId == clojure && editorTextFocus && paredit:keyMap =~ /original|strict/ && config.calva.paredit.hijackVSCodeDefaults && !calva:cursorInComment`,
	`calva:keybindingsEnabled && editorLangId == clojure && editorTextFocus && paredit:keyMap =~ /original|strict/`,
	`calva:keybindingsEnabled && editorLangId == clojure && editorTextFocus`,
	`calva:keybindingsEnabled`,
	`canNavigateBack`,
	`canNavigateForward`,
	`canReopenInSSH && view == targetsContainers && viewItem =~ /active(d|D)ev/`,
	`canReopenInSSH`,
	`canReopenInWSL && view == targetsContainers && viewItem =~ /active(d|D)ev/`,
	`canReopenInWSL`,
	`canReopenLocally && view == targetsContainers && viewItem =~ /active(d|D)ev/`,
	`canReopenLocally`,
	`canShowSpotExplorer`,
	`cancellableOperation`,
	`cblite.explorer.show`,
	`chat:discord`,
	`chat:slack`,
	`chat:vslsEnabled && viewItem == channel`,
	`checkin-criteria`,
	`checkout-criteria`,
	`cl.eide.projectActived && !isInDiffEditor`,
	`cl.eide.projectActived && config.EIDE.Option.ShowToolbarInEditerTitle`,
	`cl.eide.projectActived`,
	`clangd.ast.hasData`,
	`clangd.ast.supported`,
	`clangd.memoryUsage.hasData`,
	`clangd.memoryUsage.supported`,
	`clangd.typeHierarchyVisible`,
	`classroom:active-assignment-is-open`,
	`classroom:command-enabled:liveshareInvite && view == github.classroom.activeAssignment && viewItem =~ /liveshareInviteEnabled/`,
	`classroom:command-enabled:openAssignment && view == github.classroom.assignmentExplorer && viewItem =~ /openAssignmentEnabled/`,
	`classroom:command-enabled:refresh && view == github.classroom.activeAssignment`,
	`classroom:command-enabled:refresh && view == github.classroom.assignmentExplorer`,
	`classroom:command-enabled:syncChanges && view == github.classroom.activeAssignment`,
	`classroom:command-enabled:syncChanges && view == github.classroom.assignmentExplorer && viewItem =~ /syncChangesEnabled/`,
	`classroom:has-no-assignments && !classroom:is-logged-out`,
	`classroom:is-logged-out`,
	`clicknium.login.status != signed`,
	`clicknium.login.status == signed`,
	`cloudcode.exportCoverageVisible`,
	`cmake:enableFullFeatureSet \u0026\u0026 !useCMakePresets`,
	`cmake:enableFullFeatureSet \u0026\u0026 useCMakePresets`,
	`cmake:enableFullFeatureSet \u0026\u0026 view == cmake.outline \u0026\u0026 viewItem =~ /nodeType=file/ \u0026\u0026 viewItem =~ /cmakelists=true/`,
	`cmake:enableFullFeatureSet \u0026\u0026 view == cmake.outline \u0026\u0026 viewItem =~ /nodeType=file/ \u0026\u0026 viewItem =~ /compilable=true/`,
	`cmake:enableFullFeatureSet \u0026\u0026 view == cmake.outline \u0026\u0026 viewItem =~ /nodeType=workspace/ \u0026\u0026 viewItem =~ /selected=false/`,
	`cmake:enableFullFeatureSet`,
	`cmake:enableTouchBar \u0026\u0026 !cmake:enableFullFeatureSet`,
	`cmake:enableTouchBar.build \u0026\u0026 cmake:enableFullFeatureSet \u0026\u0026 !cmake:isBuilding \u0026\u0026 !cmake:isConfiguring`,
	`cmake:enableTouchBar.build \u0026\u0026 cmake:enableFullFeatureSet \u0026\u0026 cmake:isBuilding`,
	`cmake:enableTouchBar.configure \u0026\u0026 !cmake:isBuilding \u0026\u0026 !cmake:isConfiguring`,
	`cmake:enableTouchBar.debug \u0026\u0026 cmake:enableFullFeatureSet \u0026\u0026 !cmake:hideDebugCommand`,
	`cmake:enableTouchBar.launch \u0026\u0026 cmake:enableFullFeatureSet \u0026\u0026 !cmake:hideLaunchCommand`,
	`cmake:multiProject \u0026\u0026 !cmake:hideDebugCommand \u0026\u0026 cmake:enableFullFeatureSet`,
	`cmake:multiProject \u0026\u0026 !cmake:hideLaunchCommand \u0026\u0026 cmake:enableFullFeatureSet`,
	`cmake:multiProject \u0026\u0026 cmake:enableFullFeatureSet \u0026\u0026 !useCMakePresets`,
	`cmake:multiProject \u0026\u0026 cmake:enableFullFeatureSet`,
	`cmake:multiProject`,
	`cmd-gdUnit3:is-ready == true`,
	`cobolLSReady && resourceLangId == cobol`,
	`code-for-ibmi:connected == false`,
	`code-for-ibmi:connected == true && code-for-ibmi:hasProfiles == true`,
	`code-for-ibmi:connected == true`,
	`code-for-ibmi:searchViewVisible == true`,
	`codeActionMenuVisible`,
	`codeCoverageViewFocused`,
	`codeReview:displayCodeReviewExplorer`,
	`codeSnippetPreviewFocus`,
	`codechecker.sidebar.showReports`,
	`codeql.supportsEvalLog && viewItem == rawResultsItem || codeql.supportsEvalLog && viewItem == interpretedResultsItem || codeql.supportsEvalLog && viewItem == cancelledResultsItem`,
	`codesandbox.connectedToCodeSandbox && codesandbox.isFollowingUser && viewItem == following-client`,
	`codesandbox.connectedToCodeSandbox && codesandbox.isFollowingUser`,
	`codesandbox.connectedToCodeSandbox`,
	`codesandbox.projectType == branch`,
	`codesandbox.projects.state == exception`,
	`codesandbox.projects.state == idle`,
	`codesandbox.signedIn && !codesandbox.connectedToCodeSandbox`,
	`codesandbox.signedIn && codesandbox.workspaceState == branch && codesandbox.projectType == branch`,
	`codesandbox.signedIn`,
	`codeseeIsLoggedIn && githubExtensionInstalled`,
	`codesight.setupComplete && !codesight.coverityInstalled`,
	`codesight.setupComplete && !codesight.sigmaInstalled`,
	`codesight.setupComplete && codesight.coverityInstalled && codesight.scanMode == 'auto'`,
	`codesight.setupComplete && codesight.coverityInstalled && codesight.scanMode == 'manual'`,
	`codesight.setupComplete && codesight.sigmaInstalled && codesight.scanMode == 'auto'`,
	`codesight.setupComplete && codesight.sigmaInstalled && codesight.scanMode == 'manual'`,
	`codesight.setupComplete`,
	`codespaces:command-enabled:addEnvironment == true && !remoteName && !virtualWorkspace`,
	`codespaces:command-enabled:addEnvironment == true && codespaces:connectionState != Connected`,
	`codespaces:command-enabled:addEnvironment == true && view == github.codespaces.explorer && codespaces:explorerState == Initialized && remoteName !=~ /codespaces*.*/`,
	`codespaces:command-enabled:configureDevContainer == true && remoteName =~ /codespaces*.*/`,
	`codespaces:command-enabled:configureDevContainer == true && view == github.codespaces.explorer && remoteName =~ /codespaces*.*/`,
	`codespaces:command-enabled:configureDevContainer == true && view == github.codespaces.explorer && viewItem =~ /configureDevContainerEnabled/`,
	`codespaces:command-enabled:connect == true && !remoteName && !virtualWorkspace`,
	`codespaces:command-enabled:connect == true && codespaces:connectionState != Connected`,
	`codespaces:command-enabled:connect == true && view == github.codespaces.explorer && viewItem =~ /connectToEnabled/`,
	`codespaces:command-enabled:connectJupyterServer == true`,
	`codespaces:command-enabled:deleteEnvironment == true && !remoteName && !virtualWorkspace`,
	`codespaces:command-enabled:deleteEnvironment == true && codespaces:connectionState != Connected`,
	`codespaces:command-enabled:deleteEnvironment == true && view == github.codespaces.explorer && viewItem =~ /deleteEnabled/`,
	`codespaces:command-enabled:disconnectEnvironment == true && view == github.codespaces.explorer && remoteName =~ /codespaces*.*/`,
	`codespaces:command-enabled:disconnectEnvironment == true && view == github.codespaces.explorer && viewItem =~ /disconnectFromEnabled/`,
	`codespaces:command-enabled:disconnectSuspend == true && remoteName =~ /codespaces*.*/`,
	`codespaces:command-enabled:goToRepo == true`,
	`codespaces:command-enabled:manageUserSecrets == true && remoteName =~ /codespaces*.*/`,
	`codespaces:command-enabled:myCodespaces == true`,
	`codespaces:command-enabled:openInBrowser == true && view == github.codespaces.explorer && viewItem =~ /openInBrowserEnabled/`,
	`codespaces:command-enabled:openInBrowser == true`,
	`codespaces:command-enabled:openInInsiders == true && remoteName =~ /codespaces*.*/`,
	`codespaces:command-enabled:openInInsiders == true`,
	`codespaces:command-enabled:openInNewWindow == true && remoteName =~ /codespaces*.*/`,
	`codespaces:command-enabled:openInNewWindow == true && view == github.codespaces.explorer && viewItem =~ /openInNewWindowEnabled/`,
	`codespaces:command-enabled:openInNewWindow == true`,
	`codespaces:command-enabled:openInStable == true && remoteName =~ /codespaces*.*/`,
	`codespaces:command-enabled:openInStable == true`,
	`codespaces:command-enabled:rebuildEnvironment == true && remoteName =~ /codespaces*.*/`,
	`codespaces:command-enabled:rebuildEnvironment == true && view == github.codespaces.explorer && remoteName =~ /codespaces*.*/`,
	`codespaces:command-enabled:refresh == true && codespaces:connectionState != Connected`,
	`codespaces:command-enabled:refresh == true && view == github.codespaces.explorer && codespaces:explorerState == Initialized && codespaces:connectionState != Connected`,
	`codespaces:command-enabled:reloadServer == true && remoteName =~ /codespaces*.*/`,
	`codespaces:command-enabled:shutdownEnvironment == true && codespaces:connectionState != Connected`,
	`codespaces:command-enabled:shutdownEnvironment == true && view == github.codespaces.explorer && viewItem =~ /shutdownEnabled/`,
	`codespaces:command-enabled:signIn == true`,
	`codespaces:command-enabled:viewCreationLog == true && remoteName =~ /codespaces*.*/`,
	`codespaces:command-enabled:viewCreationLog == true`,
	`codespaces:connectionState != Connected`,
	`codespaces:performance-explorer-enabled && codespaces:views-welcome:version-notification != Unsupported`,
	`codespaces:views-welcome:no-codespaces == true`,
	`codespaces:views-welcome:not-signed-in == true`,
	`codespaces:views-welcome:version-notification == Deprecated`,
	`codespaces:views-welcome:version-notification == Unsupported`,
	`codestream:status != signedIn && codestream:status != signingIn`,
	`codestream:status != signedOut`,
	`codestream:status == signedIn && resourceScheme == file`,
	`codestream:status == signedIn`,
	`codestream:status == signedOut`,
	`codeswing:hasTemplateMRU`,
	`codeswing:inSwing && !codeswing:inSwingWorkspace`,
	`codeswing:inSwing && !isWeb`,
	`codeswing:inSwing && codeswing:codeTourEnabled`,
	`codeswing:inSwingWorkspace && !codeswing:inSwing`,
	`codeswing:inSwing`,
	`codetogehterRoomActive == true`,
	`codetogether.av.join && !codetogether.av.join.active`,
	`codetogether.av.join && codetogether.av.join.active`,
	`codetogether.av.on && codetogether.av.audio.available && !codetogether.av.audio`,
	`codetogether.av.on && codetogether.av.audio.available && codetogether.av.audio`,
	`codetogether.av.on && codetogether.av.camera.available && !codetogether.av.camera`,
	`codetogether.av.on && codetogether.av.camera.available && codetogether.av.camera`,
	`codetogether.av.on && codetogether.av.chat.unread`,
	`codetogether.av.on && codetogether.av.screenshare`,
	`codetogether.host.mode == true && !codetogether.session.active`,
	`codetogether.session.active == false && codetogether.session.anotherParticipant == false && codetogether.state.privateDeployment && codetogether.team.connection.status =~ /^(?!(^disconnected|connecting|ssoauthenticationrequired)$).*$/`,
	`codetogether.session.active == false && codetogether.session.anotherParticipant == false && codetogether.state.privateDeployment == false`,
	`codetogether.session.active == false && codetogether.session.anotherParticipant == false && codetogether.state.ready && codetogether.state.privateDeployment && codetogether.team.connection.status =~ /^(?!(^disconnected|connecting|ssoauthenticationrequired)$).*$/`,
	`codetogether.session.active == false && codetogether.session.anotherParticipant == false && codetogether.state.ready && codetogether.state.privateDeployment == false`,
	`codetogether.session.active == false && codetogether.session.anotherParticipant == true && codetogether.cursor.editor.state == alone`,
	`codetogether.session.active == false && codetogether.session.anotherParticipant == true && codetogether.cursor.editor.state == driving`,
	`codetogether.session.active == false && codetogether.session.anotherParticipant == true && codetogether.cursor.editor.state == pseudo-alone`,
	`codetogether.session.active == false && codetogether.session.anotherParticipant == true && codetogether.cursor.editor.state == waiting-for-others`,
	`codetogether.session.active == false && codetogether.session.anotherParticipant == true && codetogether.cursor.editor.state == watching`,
	`codetogether.session.active == false && codetogether.session.anotherParticipant == true`,
	`codetogether.session.active == true && codetogether.cursor.editor.state == alone`,
	`codetogether.session.active == true && codetogether.cursor.editor.state == driving`,
	`codetogether.session.active == true && codetogether.cursor.editor.state == pseudo-alone`,
	`codetogether.session.active == true && codetogether.cursor.editor.state == waiting-for-others`,
	`codetogether.session.active == true && codetogether.cursor.editor.state == watching`,
	`codetogether.session.active == true && codetogether.session.anotherParticipant == false && codetogether.state.socketTimeout == false`,
	`codetogether.session.active == true && codetogether.state.isCodingAlone == false`,
	`codetogether.session.active == true`,
	`codetogether.session.anotherParticipant == true && codetogether.abap.canActivate`,
	`codetogether.state.loggedIn == true && codetogether.state.privateDeployment == false && codetogether.session.active == false && codetogether.session.anotherParticipant == false`,
	`codetogether.state.loggedIn == true && codetogether.state.privateDeployment == true`,
	`codetogether.state.privateDeployment == false && codetogether.session.active == false && codetogether.state.loggedIn == false && codetogether.session.anotherParticipant == false`,
	`codetogether.state.privateDeployment == true && codetogether.session.ssoAuthUrl && codetogether.state.loggedIn == false && codetogether.session.anotherParticipant == false`,
	`codetogether.team.connection.status == connected && viewItem == participantSessionContainer && codetogether.team.hasActiveTeams == true`,
	`codetogether.team.connection.status == connected && viewItem == sessionContainer && codetogether.team.hasActiveTeams == true`,
	`codetogether.team.connection.status == connected && viewItem =~ /action-inviteMultiple/ && codetogether.team.hasActiveTeams == true`,
	`codetogether.team.connection.status =~ /^(?!(^disconnected|connecting|ssoauthenticationrequired)$).*$/`,
	`codetogether.views.useWebview`,
	`codetogether.welcomeViews.session == extensionStarting`,
	`codetogether.welcomeViews.session == noSessionNoLogin || codetogether.welcomeViews.session == noSessionSocketTimeOutLogin`,
	`codetogether.welcomeViews.session == participantSessionEnded`,
	`codetogether.welcomeViews.session == reconnecting`,
	`codetogether.welcomeViews.session == saasNoTeams || codetogether.welcomeViews.session == noSessionLogin`,
	`codetogether.welcomeViews.session == saasReady || codetogether.welcomeViews.session == onPremiseNoLogin`,
	`codetogether.welcomeViews.session == sessionUnavailableConnectionLost`,
	`codetogether.welcomeViews.session == ssoAuthRequired`,
	`codetogether.welcomeViews.teams == extensionStarting`,
	`codetogether.welcomeViews.teams == networkIssue`,
	`codetogether.welcomeViews.teams == onPremiseDisconnected`,
	`codetogether.welcomeViews.teams == saasDisconnectedLoggedInActivePro`,
	`codetogether.welcomeViews.teams == saasDisconnectedLoggedInExpiredPro`,
	`codetogether.welcomeViews.teams == saasDisconnectedLoggedInNotPro`,
	`codetogether.welcomeViews.teams == saasDisconnectedNotLoggedIn`,
	`codetogether.welcomeViews.teams == ssoAuthRequired`,
	`codetogether.welcomeViews.teams == teamsOfflineParticipantOnSession`,
	`codetogether.welcomeViews.teams == teamsOfflineParticipantSessionEnded`,
	`codetour:hasProgress`,
	`codetour:hasTours`,
	`codetour:inTour && !textInputFocus && !terminalFocus`,
	`codetour:inTour && codetour:recording && codetour:canEditTour`,
	`codetour:inTour`,
	`codetour:recording && editorHasSelection`,
	`codetour:recording && explorerResourceIsFolder`,
	`codi-codeCompletion:enabled`,
	`codi-continueGen:enabled`,
	`codi-explainCode:enabled`,
	`codi-syntax:enabled`,
	`codi-test:enabled`,
	`codi-unidoc:enabled`,
	`codi.isAuthenticated`,
	`commandbox:showScriptExplorer || config.commandbox.enableScriptExplorer`,
	`comment =~ /Like/`,
	`comment =~ /Unlike/`,
	`comment =~ /canAddReply/ && resourceScheme == atlascode.bbpr`,
	`comment =~ /canAddTask/ && resourceScheme == atlascode.bbpr`,
	`comment =~ /canDelete/ && resourceScheme == atlascode.bbpr`,
	`comment =~ /canEdit/ && resourceScheme == atlascode.bbpr`,
	`comment =~ /canModifyTask/ && resourceScheme == atlascode.bbpr`,
	`comment =~ /canRemoveTask/ && resourceScheme == atlascode.bbpr`,
	`comment =~ /deletable/ && commentController == vsls-comments`,
	`comment =~ /editable/ && commentController == vsls-comments `,
	`comment =~ /editable/`,
	`comment =~ /markComplete/ && resourceScheme == atlascode.bbpr`,
	`comment =~ /markIncomplete/ && resourceScheme == atlascode.bbpr`,
	`commentController == codetour && !codetour:isEditing && codetour:canEditTour`,
	`commentController == codetour && codetour:canEditTour && commentThread =~ /hasNext/`,
	`commentController == codetour && codetour:canEditTour && commentThread =~ /hasPrevious/`,
	`commentController == codetour && codetour:canEditTour`,
	`commentController == codetour && codetour:isEditing && !commentThreadIsEmpty`,
	`commentController == codetour && codetour:recording`,
	`commentController == codetour && commentThread =~ /hasNext/`,
	`commentController == codetour && commentThread =~ /hasPrevious/`,
	`commentController == codetour && commentThreadIsEmpty`,
	`commentController == codetour`,
	`commentController == comment-sample && !commentThreadIsEmpty`,
	`commentController == comment-sample && comment == canDelete`,
	`commentController == comment-sample && commentThread == draft`,
	`commentController == comment-sample && commentThreadIsEmpty`,
	`commentController == comment-sample`,
	`commentController == comment-scribeai && !commentThreadIsEmpty`,
	`commentController == comment-scribeai && comment == canDelete`,
	`commentController == comment-scribeai && commentThreadIsEmpty`,
	`commentController == comment-scribeai`,
	`commentController == gerrit && comment =~ /deletable/`,
	`commentController == gerrit && comment =~ /editable/`,
	`commentController == gerrit && comment =~ /quickActionable/`,
	`commentController == gerrit && commentIsEmpty && commentThread =~ /yesLastCommentDraft/ && commentThread =~ /noResolved/`,
	`commentController == gerrit && commentIsEmpty && commentThread =~ /yesLastCommentDraft/ && commentThread =~ /yesResolved/`,
	`commentController == gerrit && gerrit:hasCommentFeature`,
	`commentController == gerrit`,
	`commentController == gistpad && !commentThreadIsEmpty && gistpad:state == SignedIn`,
	`commentController == gistpad && comment == canEdit`,
	`commentController == gistpad && commentThreadIsEmpty && gistpad:state == SignedIn`,
	`commentController == gistpad:repo && comment == canEdit`,
	`commentController == gistpad:repo && commentThreadIsEmpty && gistpad:state == SignedIn`,
	`commentController == gistpad:repo`,
	`commentController == gistpad`,
	`commentController == i18n-ally-review && !commentThreadIsEmpty`,
	`commentController == i18n-ally-review && viewItem =~ /self/`,
	`commentController == i18n-ally-review`,
	`commentController == revspec-marker && !commentThreadIsEmpty`,
	`commentController == revspec-marker && comment == canDelete`,
	`commentController == tabnine.commentController`,
	`commentController == vsls-comments`,
	`commentController == wingman-code-explanation`,
	`commentController =~ /^azdopr-(browse|review)/ && !commentThreadIsEmpty`,
	`commentController =~ /^azdopr-(browse|review)/ && comment =~ /canDelete/`,
	`commentController =~ /^azdopr-(browse|review)/ && comment =~ /canEdit/`,
	`commentController =~ /^azdopr-(browse|review)/`,
	`commentController =~ /^github-(browse|review)/ && comment =~ /canDelete/`,
	`commentController =~ /^github-(browse|review)/ && comment =~ /canEdit/`,
	`commentController =~ /^github-(browse|review)/ && commentThread == canResolve`,
	`commentController =~ /^github-(browse|review)/ && commentThread == canUnresolve`,
	`commentController =~ /^github-(browse|review)/`,
	`commentController =~ /^github-browse/ && !prInDraft && config.githubPullRequests.defaultCommentType != review`,
	`commentController =~ /^github-browse/ && !prInDraft && config.githubPullRequests.defaultCommentType == review`,
	`commentController =~ /^github-browse/ && prInDraft`,
	`commentController =~ /^github-review/ && !reviewInDraftMode && config.githubPullRequests.defaultCommentType != review`,
	`commentController =~ /^github-review/ && !reviewInDraftMode && config.githubPullRequests.defaultCommentType == review`,
	`commentController =~ /^github-review/ && comment =~ /hasSuggestion/`,
	`commentController =~ /^github-review/ && reviewInDraftMode`,
	`commentController =~ /^gitlab-mr-/ && comment =~ /canAdmin/`,
	`commentController =~ /^gitlab-mr-/ && comment =~ /failed-comment/`,
	`commentController =~ /^gitlab-mr-/ && comment =~ /synced-comment/`,
	`commentController =~ /^gitlab-mr-/ && commentThread == resolved`,
	`commentController =~ /^gitlab-mr-/ && commentThread == unresolved`,
	`commentController =~ /^gitlab-mr-/`,
	`commentEditorFocused`,
	`commentStyler.boldON&&commentStyler.drawerOpen`,
	`commentStyler.drawerOpen`,
	`commentStyler.italicON&&commentStyler.drawerOpen`,
	`commentStyler.outlineON&&commentStyler.drawerOpen`,
	`commentStyler.superscriptON&&commentStyler.drawerOpen`,
	`commentThread =~ /deletable/ && !commentThreadIsEmpty && commentController == vsls-comments`,
	`commentThreadIsEmpty && commentController == vsls-comments`,
	`commentThreadIsEmpty`,
	`composerCompanion.showScriptsInExplorer`,
	`config-view && config.ConfigView.showIcons`,
	`config.C_Cpp.intelliSenseEngine != 'disabled' && config.C_Cpp.intelliSenseEngine != 'Disabled' && editorLangId == 'c' || config.C_Cpp.intelliSenseEngine != 'disabled' && config.C_Cpp.intelliSenseEngine != 'Disabled' && editorLangId == 'cpp' || config.C_Cpp.intelliSenseEngine != 'disabled' && config.C_Cpp.intelliSenseEngine != 'Disabled' && editorLangId == 'cuda-cpp'`,
	`config.C_Cpp.intelliSenseEngine == 'Default' && editorLangId == 'c' || config.C_Cpp.intelliSenseEngine == 'default' && editorLangId == 'c' || config.C_Cpp.intelliSenseEngine == 'Default' && editorLangId == 'cpp' || config.C_Cpp.intelliSenseEngine == 'default' && editorLangId == 'cpp' || config.C_Cpp.intelliSenseEngine == 'Default' && editorLangId == 'cuda-cpp' || config.C_Cpp.intelliSenseEngine == 'default' && editorLangId == 'cuda-cpp'`,
	`config.CodeJumper.JumpToHeaderFileInMenu`,
	`config.CodeJumper.SearchFileInMenu`,
	`config.CodeJumper.SearchSymbolInMenu`,
	`config.Compare with saved`,
	`config.ConEmu.showTitlebarIcon`,
	`config.Copy`,
	`config.Cut`,
	`config.Find replace`,
	`config.Go to definition && editorHasDefinitionProvider && editorTextFocus && !isInEmbeddedEditor`,
	`config.GoNavBar.balanceIn`,
	`config.GoNavBar.balanceOut`,
	`config.GoNavBar.goBack`,
	`config.GoNavBar.goForward`,
	`config.GoNavBar.goLastEditLocation`,
	`config.GoNavBar.goToBracket`,
	`config.GoNavBar.saveWithoutFormatting`,
	`config.JCL Language Support.JCLCheckFeatures`,
	`config.Love2d Run`,
	`config.Navigate back`,
	`config.Navigate forward`,
	`config.New file`,
	`config.Open command pallet`,
	`config.Open file`,
	`config.PSRule.experimental.enabled`,
	`config.Paste`,
	`config.Quick open`,
	`config.RedditViewerWidget.Frontpage`,
	`config.RedditViewerWidget.Subreddit`,
	`config.Save all`,
	`config.ServiceModeler.hiddenCommand == true`,
	`config.ShortcutMenuBar.changeEncoding`,
	`config.ShortcutMenuBar.compareWithSaved`,
	`config.ShortcutMenuBar.copy`,
	`config.ShortcutMenuBar.cut`,
	`config.ShortcutMenuBar.findReplace`,
	`config.ShortcutMenuBar.goToDefinition && editorHasDefinitionProvider && editorTextFocus && !isInEmbeddedEditor`,
	`config.ShortcutMenuBar.indentLines`,
	`config.ShortcutMenuBar.navigateBack`,
	`config.ShortcutMenuBar.navigateForward`,
	`config.ShortcutMenuBar.newFile`,
	`config.ShortcutMenuBar.openFile`,
	`config.ShortcutMenuBar.openSettings`,
	`config.ShortcutMenuBar.outdentLines`,
	`config.ShortcutMenuBar.paste`,
	`config.ShortcutMenuBar.powershellRestartSession`,
	`config.ShortcutMenuBar.quickOpen`,
	`config.ShortcutMenuBar.saveAll`,
	`config.ShortcutMenuBar.showCommands`,
	`config.ShortcutMenuBar.startDebugging && debuggersAvailable && !inDebugMode`,
	`config.ShortcutMenuBar.toggleActivityBar`,
	`config.ShortcutMenuBar.toggleRenderWhitespace`,
	`config.ShortcutMenuBar.toggleTerminal`,
	`config.ShortcutMenuBar.toggleWordWrap`,
	`config.ShortcutMenuBar.userButton01Command`,
	`config.ShortcutMenuBar.userButton02Command`,
	`config.ShortcutMenuBar.userButton03Command`,
	`config.ShortcutMenuBar.userButton04Command`,
	`config.ShortcutMenuBar.userButton05Command`,
	`config.ShortcutMenuBar.userButton06Command`,
	`config.ShortcutMenuBar.userButton07Command`,
	`config.ShortcutMenuBar.userButton08Command`,
	`config.ShortcutMenuBar.userButton09Command`,
	`config.ShortcutMenuBar.userButton10Command`,
	`config.Start debugging && debuggersAvailable && !inDebugMode`,
	`config.Toggle activity bar`,
	`config.Toggle render whitespace`,
	`config.Toggle terminal`,
	`config.TotalCommander.showTitlebarIcon`,
	`config.UVCH.Global.UseBrowser && !editorHasSelection`,
	`config.UVCH.Global.UseBrowser && editorHasSelection`,
	`config.UVCH.Global.UseBrowser && editorTextFocus && editorHasSelection`,
	`config.UVCH.Global.UseBrowser`,
	`config.UVCH.Global.UseSwitchFile`,
	`config.UVCH.Global.UseToolbar && isWindows`,
	`config.acmx.execution.showCompileIconInEditorTitleMenu`,
	`config.acmx.execution.showRunIconInEditorTitleMenu`,
	`config.acmx.execution.showStressIconInEditorTitleMenu`,
	`config.acmx.execution.showSubmitIconInEditorTitleMenu`,
	`config.acmx.execution.showUpgradeIconInEditorTitleMenu`,
	`config.aliyun.fnf.remoteResource.enable`,
	`config.angular-cli.appShell.enabled`,
	`config.angular-cli.application.enabled`,
	`config.angular-cli.class.enabled`,
	`config.angular-cli.component.enabled`,
	`config.angular-cli.directive.enabled`,
	`config.angular-cli.enum.enabled`,
	`config.angular-cli.guard.enabled`,
	`config.angular-cli.interceptor.enabled`,
	`config.angular-cli.interface.enabled`,
	`config.angular-cli.library.enabled`,
	`config.angular-cli.module.enabled`,
	`config.angular-cli.pipe.enabled`,
	`config.angular-cli.service.enabled`,
	`config.angular-cli.serviceWorker.enabled`,
	`config.angular-cli.webWorker.enabled`,
	`config.angular2-files.menu.class`,
	`config.angular2-files.menu.component`,
	`config.angular2-files.menu.directive`,
	`config.angular2-files.menu.enum`,
	`config.angular2-files.menu.interface`,
	`config.angular2-files.menu.module`,
	`config.angular2-files.menu.pipe`,
	`config.angular2-files.menu.route`,
	`config.angular2-files.menu.service`,
	`config.appMap.inspectEnabled == true && ( viewItem == appmap.views.codeObjects.package || viewItem == appmap.views.codeObjects.class || viewItem == appmap.views.codeObjects.function || viewItem == appmap.views.codeObjects.query || viewItem == appmap.views.codeObjects.route )`,
	`config.appworks.enableView`,
	`config.as3mxml.quickCompile.enabled`,
	`config.asm-code-lens.hexCalculator.showInDebug`,
	`config.asm-code-lens.hexCalculator.showInExplorer`,
	`config.atlascode.bitbucket.contextMenus.enabled && config.atlascode.bitbucket.enabled`,
	`config.atlascode.bitbucket.contextMenus.enabled && config.atlascode.jira.enabled`,
	`config.atlascode.helpExplorerEnabled`,
	`config.auto-tfs.tf.path && !explorerResourceIsFolder && !listMultiSelection`,
	`config.auto-tfs.tf.path && !explorerResourceIsFolder || config.auto-tfs.tf.path && explorerResourceIsFolder && !listMultiSelection`,
	`config.auto-tfs.tf.path && !listMultiSelection`,
	`config.auto-tfs.tf.path && scmProvider == 'auto-tfs' && scmResourceGroup == 'excluded'`,
	`config.auto-tfs.tf.path && scmProvider == 'auto-tfs' && scmResourceGroup == 'included'`,
	`config.auto-tfs.tf.path && scmProvider == 'auto-tfs'`,
	`config.auto-tfs.tf.path`,
	`config.aws.experiments.samSyncCode && !config.aws.samcli.legacyDeploy && isFileSystemResource && resourceFilename =~ /^(template\\.(json|yml|yaml))|(samconfig\\.toml)$/`,
	`config.aws.experiments.samSyncCode && !config.aws.samcli.legacyDeploy && view == aws.explorer && viewItem =~ /^(awsLambdaNode|awsRegionNode|awsCloudFormationRootNode)$/`,
	`config.aws.experiments.samSyncCode && !config.aws.samcli.legacyDeploy && view == aws.explorer`,
	`config.aws.experiments.samSyncCode && !config.aws.samcli.legacyDeploy`,
	`config.aws.samcli.legacyDeploy && isFileSystemResource && resourceFilename =~ /^template\\.(json|yml|yaml)$/`,
	`config.aws.samcli.legacyDeploy && view == aws.explorer && viewItem =~ /^(awsLambdaNode|awsRegionNode|awsCloudFormationRootNode)$/`,
	`config.aws.samcli.legacyDeploy && view == aws.explorer`,
	`config.aws.samcli.legacyDeploy`,
	`config.azureApiManagement.showExplorer == true`,
	`config.azureFunctions.enableJavaRemoteDebugging == true`,
	`config.azureFunctions.enableRemoteDebugging == true`,
	`config.azureLogicApps.showExplorer == true`,
	`config.azureLogicAppsStandard.enableRemoteDebugging == true`,
	`config.azureLogicAppsStandard.showExplorer == true`,
	`config.back-n-forth.iconStyle == 'arrow' && config.back-n-forth.withLastEditLocation == true`,
	`config.back-n-forth.iconStyle == 'arrow'`,
	`config.back-n-forth.iconStyle == 'chevron' && config.back-n-forth.withLastEditLocation == true`,
	`config.back-n-forth.iconStyle == 'chevron'`,
	`config.bravo.hostEnabled && bravo.canPreview && !bravo.hasReport && !bravo.connectionFailed`,
	`config.bravo.hostEnabled && bravo.canPreview && bravo.hasReport && !bravo.connectionFailed`,
	`config.bsqm.tools.git != `,
	`config.cSpell.experimental.enableRegexpView`,
	`config.cblite.enableJSON`,
	`config.checkpoints.location == explorer`,
	`config.checkpoints.location == scm`,
	`config.clariy-lsp.panels.insights-panel`,
	`config.cloudcode.beta.enableOneActivityBar && config.cloudcode.beta.enableApigeeExplorers`,
	`config.cloudcode.beta.enableOneActivityBar && config.cloudcode.beta.enableAwsLambdaImporter`,
	`config.cloudcode.beta.enableOneActivityBar && config.cloudcode.beta.enableCloudStorage`,
	`config.cloudcode.beta.enableOneActivityBar && config.cloudcode.beta.enableGoogleCloudFunctions`,
	`config.cloudcode.beta.enableOneActivityBar && config.cloudcode.beta.enableVMExplorer`,
	`config.cloudcode.beta.enableOneActivityBar`,
	`config.cloudlatex.enabled || !config.cloudlatex.supressIcon`,
	`config.cmsis-csolution.experimentalFeatures`,
	`config.cnblogsClientForVSCode.ing.enablePublishSelectionToIng`,
	`config.coboleditor.comment_line && editorTextFocus && editorLangId =~ /^COBOL$|^BITLANG-COBOL$|^ACUCOBOL$|^COBOLIT$/ && !editorReadonly`,
	`config.coboleditor.enable_tab_inline && inlineSuggestionVisible && editorLangId =~ /^COBOL$|^BITLANG-COBOL$|^ACUCOBOL$|^COBOLIT$/ && !editorTabMovesFocus && !inlineSuggestionHasIndentation`,
	`config.coboleditor.enable_tab_next_or_previous && suggestWidgetVisible && editorLangId =~ /^COBOL$|^BITLANG-COBOL$|^ACUCOBOL$|^COBOLIT$/ && suggestWidgetMultipleSuggestions`,
	`config.coboleditor.feedbackview`,
	`config.coboleditor.sourceview`,
	`config.coboleditor.xedit_keymap &&  &&editorLangId =~ /^COBOL$|^BITLANG-COBOL$|^ACUCOBOL$|^COBOLIT$/ && !inSnippetMode`,
	`config.coboleditor.xedit_keymap && editorLangId =~ /^COBOL$|^BITLANG-COBOL$|^ACUCOBOL$|^COBOLIT$/ && !inSnippetMode`,
	`config.code-runner.showRunIconInEditorTitleMenu`,
	`config.code-runner.showStopIconInEditorTitleMenu && code-runner.codeRunning`,
	`config.codeQL.canary && codeql.supportsEvalLog && viewItem == rawResultsItem || config.codeQL.canary && codeql.supportsEvalLog && viewItem == interpretedResultsItem || config.codeQL.canary && codeql.supportsEvalLog && viewItem == cancelledResultsItem`,
	`config.codeQL.canary && config.codeQL.variantAnalysis.repositoriesPanel`,
	`config.codeQL.canary && editorLangId == ql && resourceExtname == .ql`,
	`config.codeQL.canary`,
	`config.codeQL.mockGitHubApiServer.enabled && !codeQL.mockGitHubApiServer.recording`,
	`config.codeQL.mockGitHubApiServer.enabled && codeQL.mockGitHubApiServer.recording`,
	`config.codeQL.mockGitHubApiServer.enabled && codeQL.mockGitHubApiServer.scenarioLoaded`,
	`config.codeStorm.revealInSideBarButton.show`,
	`config.codeblock.showBeSystemsExplorer`,
	`config.codedeploy.isApplicationWorkspace`,
	`config.codemap.location == codemap`,
	`config.codemap.location == explorer`,
	`config.color-manager.contextMenu.edit`,
	`config.color-manager.contextMenu.findColors`,
	`config.color-manager.contextMenu.openPalette`,
	`config.color-manager.contextMenu.openPicker`,
	`config.command-runner.showRunCommandInExplorerContextMenu`,
	`config.commandbox.autoDetect != off`,
	`config.commandbox.autoDetect == off`,
	`config.commandbox.enableRunFromFolder && explorerViewletVisible && explorerResourceIsFolder`,
	`config.compareFolders.showIdentical`,
	`config.copy-path-unixstyle.copyPathCommand.enable && isFileSystemResource`,
	`config.copy-path-unixstyle.copyPathCygwinFormatCommand.enable && isFileSystemResource`,
	`config.copy-path-unixstyle.copyPathGitBashFormatCommand.enable && isFileSystemResource`,
	`config.copy-path-unixstyle.copyPathWSLFormatCommand.enable && isFileSystemResource`,
	`config.copy-path-unixstyle.copyPathWindowsDblBackslashFormatCommand.enable && isFileSystemResource`,
	`config.copy-path-unixstyle.copyPathWindowsForwardSlashFormatCommand.enable && isFileSystemResource`,
	`config.cos.showExplorer == true`,
	`config.coverage-gutters.customizable.context-menu`,
	`config.cros-ide.boardsAndPackages.showWelcomeMessage`,
	`config.cros-ide.underDevelopment.platformEC`,
	`config.cypressHelper.menuItems.GenerateCustomCommandTypes`,
	`config.dezog.showHelp`,
	`config.dmbinder.treeViewStyle == composite`,
	`config.dmbinder.treeViewStyle == split`,
	`config.docker-compose.showExplorer == true`,
	`config.docker-explorer.showAzureRegistries == true`,
	`config.docker-explorer.showDockerContainers == true`,
	`config.docker-explorer.showDockerHubTreeView == true`,
	`config.docker-explorer.showDockerImages == true`,
	`config.docker-explorer.showSuggestedDockerImages == true`,
	`config.docker.containers.groupBy == 'Compose Project Name'`,
	`config.dota2-tools.A1.module_list.css_api_tree`,
	`config.dota2-tools.A1.module_list.js_api_tree`,
	`config.dota2-tools.A1.module_list.lua_api_tree`,
	`config.dota2-tools.A1.module_list.panel_tree`,
	`config.dota2-tools.A2.display.ability_icon`,
	`config.dota2-tools.A2.display.items_game`,
	`config.dota2-tools.A2.display.vsnd_picker`,
	`config.dynamo.showExplorer == true`,
	`config.editor.rename.enablePreview && editorFocus && renameInputVisible`,
	`config.emacs-mcx.useMetaPrefixCtrlLeftBracket`,
	`config.emacs-mcx.useMetaPrefixEscape`,
	`config.emacs-mcx.useMetaPrefixMacCmd`,
	`config.emmet.triggerExpansionOnTab && editorTextFocus && !editorReadonly && !editorTabMovesFocus`,
	`config.emulator.hasEditorTitleIcon`,
	`config.enablePlayer && viewItem =~ /(project|active)/`,
	`config.enablePlayer && viewItem =~ /active/`,
	`config.enablePlayer && viewItem =~ /project/`,
	`config.endevor.experimental.workspaceSync && e4e.scm.status != 'initialized'`,
	`config.endevor.experimental.workspaceSync && e4e.scm.status == 'initialized'`,
	`config.endevor.experimental.workspaceSync && workbenchState == 'empty'`,
	`config.endevor.experimental.workspaceSync && workbenchState == 'folder'`,
	`config.endevor.experimental.workspaceSync`,
	`config.eoside.menu`,
	`config.explorerTerminal.showDockerCommands == true`,
	`config.extension.prophet.cartridges.view.enabled`,
	`config.fancyReact.treeViewEnabled`,
	`config.favorites.ownExplorer == false`,
	`config.favorites.ownExplorer == true`,
	`config.fcs-vscode.showRunIconInEditorTitleMenu && editorLangId==fcs`,
	`config.fetchUserEnv.palEnableSaveEnv`,
	`config.fictionWriter.smartRename.enabled && resourceScheme == file && resourcePath =~ /\\.md(\\.yml|\\.txt)*$/gim`,
	`config.fileutils.menus.context.editor =~ /copyFileName/`,
	`config.fileutils.menus.context.editor =~ /duplicateFile/`,
	`config.fileutils.menus.context.editor =~ /moveFile/`,
	`config.fileutils.menus.context.editor =~ /removeFile/`,
	`config.fileutils.menus.context.editor =~ /renameFile/`,
	`config.fileutils.menus.context.editorTitle =~ /copyFileName/`,
	`config.fileutils.menus.context.editorTitle =~ /duplicateFile/`,
	`config.fileutils.menus.context.editorTitle =~ /moveFile/`,
	`config.fileutils.menus.context.editorTitle =~ /removeFile/`,
	`config.fileutils.menus.context.editorTitle =~ /renameFile/`,
	`config.fileutils.menus.context.explorer =~ /copyFileName/`,
	`config.fileutils.menus.context.explorer =~ /duplicateFile/`,
	`config.fileutils.menus.context.explorer =~ /moveFile/`,
	`config.fileutils.menus.context.explorer =~ /newFileAtRoot/`,
	`config.fileutils.menus.context.explorer =~ /newFolderAtRoot/`,
	`config.find-and-transform.enableContextMenus  && explorerResourceIsFolder`,
	`config.find-and-transform.enableContextMenus && !explorerResourceIsFolder`,
	`config.findAllInFile.editorContextMenu`,
	`config.firestore-explorer.serviceAccountKeyPath != ''`,
	`config.flutter-files.menu.bigpack`,
	`config.flutter-files.menu.bloc`,
	`config.flutter-files.menu.dynamic`,
	`config.flutter-files.menu.event`,
	`config.flutter-files.menu.index`,
	`config.flutter-files.menu.model`,
	`config.flutter-files.menu.page`,
	`config.flutter-files.menu.provider`,
	`config.flutter-files.menu.repository`,
	`config.flutter-files.menu.screen`,
	`config.flutter-files.menu.smallpack`,
	`config.flutter-files.menu.state`,
	`config.flutter-files.menu.templates`,
	`config.force.showApexJavadocContext && resourceLangId == apex`,
	`config.fossil.enabled && !scmProvider && fossilOpenRepositoryCount == 0`,
	`config.fossil.enabled && fossilOpenRepositoryCount != 0 && !isInDiffEditor && resourceExtname =~ /^\\.(md|wiki)$/ || config.fossil.enabled && resourceScheme == untitled`,
	`config.fossil.enabled && fossilOpenRepositoryCount != 0 && !isInDiffEditor && resourceScheme == file && scmProvider == fossil`,
	`config.fossil.enabled && fossilOpenRepositoryCount != 0 && !isInDiffEditor`,
	`config.fossil.enabled && fossilOpenRepositoryCount != 0 && isInDiffEditor && resourceScheme != extension && scmProvider == fossil`,
	`config.fossil.enabled && fossilOpenRepositoryCount != 0`,
	`config.fossil.enabled && originalResourceScheme == fossil`,
	`config.fossil.enabled && scmProvider == fossil && scmResourceGroup != untracked`,
	`config.fossil.enabled && scmProvider == fossil && scmResourceGroup == merge`,
	`config.fossil.enabled && scmProvider == fossil && scmResourceGroup == staging`,
	`config.fossil.enabled && scmProvider == fossil && scmResourceGroup == untracked`,
	`config.fossil.enabled && scmProvider == fossil && scmResourceGroup == working`,
	`config.fossil.enabled && scmProvider == fossil`,
	`config.fossil.enabled`,
	`config.geradorCpfCnpj.habilitarMenuDeContexto`,
	`config.git.enabled && !config.gitHistory.editorTitleButtonOpenRepo && resourceScheme == file && gitOpenRepositoryCount != 0 && config.gitHistory.showEditorTitleMenuBarIcons`,
	`config.git.enabled && !git.missing && gitOpenRepositoryCount != 0`,
	`config.git.enabled && config.gitHistory.editorTitleButtonOpenRepo && resourceScheme == file && gitOpenRepositoryCount != 0 && config.gitHistory.showEditorTitleMenuBarIcons`,
	`config.git.enabled && git.state == initialized && workbenchState == empty`,
	`config.git.enabled && gitOpenRepositoryCount != 0 && config.gitHistory.showEditorTitleMenuBarIcons`,
	`config.git.enabled && gitOpenRepositoryCount != 0 && config.gitHistory.sourceCodeProviderIntegrationLocation == 'Inline'`,
	`config.git.enabled && gitOpenRepositoryCount != 0 && config.gitHistory.sourceCodeProviderIntegrationLocation == 'More Actions'`,
	`config.git.enabled && gitOpenRepositoryCount != 0`,
	`config.git.enabled`,
	`config.git.openDiffOnClick && view == azdoprStatus:azdo && viewItem =~ /filechange(?!:DELETE)/`,
	`config.gitExtensions.editor.contextmenu`,
	`config.gitExtensions.editor.title.contextmenu`,
	`config.gitExtensions.explorer.contextmenu`,
	`config.gitExtensions.scm.title.menu`,
	`config.githubinator.contextMenu == enabled && config.githubinator.contextMenuBlameOnMaster`,
	`config.githubinator.contextMenu == enabled && config.githubinator.contextMenuBlamePermalink`,
	`config.githubinator.contextMenu == enabled && config.githubinator.contextMenuBlame`,
	`config.githubinator.contextMenu == enabled && config.githubinator.contextMenuCompare`,
	`config.githubinator.contextMenu == enabled && config.githubinator.contextMenuCopyMasterPermalink`,
	`config.githubinator.contextMenu == enabled && config.githubinator.contextMenuCopyMaster`,
	`config.githubinator.contextMenu == enabled && config.githubinator.contextMenuCopyPermalink`,
	`config.githubinator.contextMenu == enabled && config.githubinator.contextMenuCopy`,
	`config.githubinator.contextMenu == enabled && config.githubinator.contextMenuGithubinator`,
	`config.githubinator.contextMenu == enabled && config.githubinator.contextMenuHistory`,
	`config.githubinator.contextMenu == enabled && config.githubinator.contextMenuOnMaster`,
	`config.githubinator.contextMenu == enabled && config.githubinator.contextMenuOpenPR`,
	`config.githubinator.contextMenu == enabled && config.githubinator.contextMenuPermalink`,
	`config.githubinator.contextMenu == enabled && config.githubinator.contextMenuRepository`,
	`config.githubinator.contextMenu == submenu && config.githubinator.contextMenuBlameOnMaster`,
	`config.githubinator.contextMenu == submenu && config.githubinator.contextMenuBlamePermalink`,
	`config.githubinator.contextMenu == submenu && config.githubinator.contextMenuBlame`,
	`config.githubinator.contextMenu == submenu && config.githubinator.contextMenuCompare`,
	`config.githubinator.contextMenu == submenu && config.githubinator.contextMenuCopyMasterPermalink`,
	`config.githubinator.contextMenu == submenu && config.githubinator.contextMenuCopyMaster`,
	`config.githubinator.contextMenu == submenu && config.githubinator.contextMenuCopyPermalink`,
	`config.githubinator.contextMenu == submenu && config.githubinator.contextMenuCopy`,
	`config.githubinator.contextMenu == submenu && config.githubinator.contextMenuGithubinator`,
	`config.githubinator.contextMenu == submenu && config.githubinator.contextMenuHistory`,
	`config.githubinator.contextMenu == submenu && config.githubinator.contextMenuOnMaster`,
	`config.githubinator.contextMenu == submenu && config.githubinator.contextMenuOpenPR`,
	`config.githubinator.contextMenu == submenu && config.githubinator.contextMenuPermalink`,
	`config.githubinator.contextMenu == submenu && config.githubinator.contextMenuRepository`,
	`config.gitlab.showProjectIssues`,
	`config.gitlab.showProjectMergeRequests`,
	`config.gitlens.fileAnnotations.command == blame && gitlens:activeFileStatus =~ /blameable/ && !gitlens:annotationStatus && config.gitlens.menus.editorGroup.blame`,
	`config.gitlens.fileAnnotations.command == changes && gitlens:activeFileStatus =~ /blameable/ && !gitlens:annotationStatus && config.gitlens.menus.editorGroup.blame`,
	`config.gitlens.fileAnnotations.command == changes && gitlens:activeFileStatus =~ /blameable/ && !gitlens:hasVirtualFolders && !gitlens:annotationStatus && config.gitlens.menus.editorGroup.blame`,
	`config.gitlens.fileAnnotations.command == heatmap && gitlens:activeFileStatus =~ /blameable/ && !gitlens:annotationStatus && config.gitlens.menus.editorGroup.blame`,
	`config.gitlens.integrations.enabled && gitlens:hasRichRemotes && !gitlens:hasConnectedRemotes`,
	`config.gitlens.integrations.enabled && gitlens:hasRichRemotes && gitlens:hasConnectedRemotes`,
	`config.gitlens.integrations.enabled && viewItem =~ /gitlens:remote\\b(?=.*?\\b\\+connected\\b)/`,
	`config.gitlens.integrations.enabled && viewItem =~ /gitlens:remote\\b(?=.*?\\b\\+disconnected\\b)/`,
	`config.gitlens.keymap == alternate && !gitlens:disabled`,
	`config.gitlens.keymap == alternate && editorTextFocus && !gitlens:disabled && !gitlens:disabledToggleCodeLens`,
	`config.gitlens.keymap == alternate && editorTextFocus && !gitlens:disabled`,
	`config.gitlens.keymap == alternate && editorTextFocus && gitlens:activeFileStatus =~ /blameable/`,
	`config.gitlens.keymap == alternate && editorTextFocus && gitlens:activeFileStatus =~ /revision/ && !isInDiffEditor`,
	`config.gitlens.keymap == alternate && editorTextFocus && gitlens:activeFileStatus =~ /revision/ && isInDiffEditor && !isInDiffRightEditor`,
	`config.gitlens.keymap == alternate && editorTextFocus && gitlens:activeFileStatus =~ /revision/ && isInDiffRightEditor`,
	`config.gitlens.keymap == alternate && editorTextFocus && gitlens:activeFileStatus =~ /revision/`,
	`config.gitlens.keymap == alternate && editorTextFocus && gitlens:activeFileStatus =~ /tracked/ && !isInDiffEditor`,
	`config.gitlens.keymap == alternate && editorTextFocus && gitlens:activeFileStatus =~ /tracked/ && isInDiffEditor && !isInDiffRightEditor`,
	`config.gitlens.keymap == alternate && editorTextFocus && gitlens:activeFileStatus =~ /tracked/ && isInDiffRightEditor`,
	`config.gitlens.keymap == alternate && editorTextFocus && gitlens:activeFileStatus =~ /tracked/`,
	`config.gitlens.keymap == chorded && !gitlens:disabled`,
	`config.gitlens.keymap == chorded && editorTextFocus && !gitlens:disabled && !gitlens:disabledToggleCodeLens`,
	`config.gitlens.keymap == chorded && editorTextFocus && !gitlens:disabled`,
	`config.gitlens.keymap == chorded && editorTextFocus && gitlens:activeFileStatus =~ /blameable/`,
	`config.gitlens.keymap == chorded && editorTextFocus && gitlens:activeFileStatus =~ /revision/ && !isInDiffEditor`,
	`config.gitlens.keymap == chorded && editorTextFocus && gitlens:activeFileStatus =~ /revision/ && isInDiffEditor && !isInDiffRightEditor`,
	`config.gitlens.keymap == chorded && editorTextFocus && gitlens:activeFileStatus =~ /revision/ && isInDiffRightEditor`,
	`config.gitlens.keymap == chorded && editorTextFocus && gitlens:activeFileStatus =~ /revision/`,
	`config.gitlens.keymap == chorded && editorTextFocus && gitlens:activeFileStatus =~ /tracked/ && !isInDiffEditor`,
	`config.gitlens.keymap == chorded && editorTextFocus && gitlens:activeFileStatus =~ /tracked/ && isInDiffEditor && !isInDiffRightEditor`,
	`config.gitlens.keymap == chorded && editorTextFocus && gitlens:activeFileStatus =~ /tracked/ && isInDiffRightEditor`,
	`config.gitlens.keymap == chorded && editorTextFocus && gitlens:activeFileStatus =~ /tracked/`,
	`config.gitlens.outputLevel != debug`,
	`config.gitlens.outputLevel != errors`,
	`config.gitlens.plusFeatures.enabled`,
	`config.gitlens.views.repositories.showBranchComparison`,
	`config.gitlens.views.repositories.showBranches`,
	`config.gitlens.views.repositories.showCommits`,
	`config.gitlens.views.repositories.showContributors`,
	`config.gitlens.views.repositories.showRemotes`,
	`config.gitlens.views.repositories.showStashes`,
	`config.gitlens.views.repositories.showTags`,
	`config.gitlens.views.repositories.showUpstreamStatus`,
	`config.gitlens.views.repositories.showWorktrees`,
	`config.hero-heroku.apiKey`,
	`config.hg.enabled && !hg.missing && !scmProvider && hgOpenRepositoryCount == 0`,
	`config.hg.enabled && !hg.missing && hgOpenRepositoryCount != 0 && !isInDiffEditor && resourceScheme == file`,
	`config.hg.enabled && !hg.missing && hgOpenRepositoryCount != 0 && !isInDiffEditor`,
	`config.hg.enabled && !hg.missing && hgOpenRepositoryCount != 0 && isInDiffEditor && resourceScheme != extension`,
	`config.hg.enabled && !hg.missing && hgOpenRepositoryCount != 0`,
	`config.hg.enabled && !hg.missing && originalResourceScheme == hg`,
	`config.hg.enabled && !hg.missing && scmProvider == hg && scmResourceGroup != untracked`,
	`config.hg.enabled && !hg.missing && scmProvider == hg && scmResourceGroup == conflict`,
	`config.hg.enabled && !hg.missing && scmProvider == hg && scmResourceGroup == merge`,
	`config.hg.enabled && !hg.missing && scmProvider == hg && scmResourceGroup == staging`,
	`config.hg.enabled && !hg.missing && scmProvider == hg && scmResourceGroup == untracked`,
	`config.hg.enabled && !hg.missing && scmProvider == hg && scmResourceGroup == working`,
	`config.hg.enabled && !hg.missing && scmProvider == hg`,
	`config.hg.enabled && !hg.missing`,
	`config.hg.enabled && !scmProvider && hgOpenRepositoryCount == 0`,
	`config.hg.enabled && hgOpenRepositoryCount != 0 && !isInDiffEditor && resourceScheme == file`,
	`config.hg.enabled && hgOpenRepositoryCount != 0 && !isInDiffEditor`,
	`config.hg.enabled && hgOpenRepositoryCount != 0 && isInDiffEditor && resourceScheme != extension`,
	`config.hg.enabled && hgOpenRepositoryCount != 0`,
	`config.hg.enabled && scmProvider == hg && scmResourceGroup != untracked`,
	`config.hg.enabled && scmProvider == hg && scmResourceGroup == conflict`,
	`config.hg.enabled && scmProvider == hg && scmResourceGroup == merge`,
	`config.hg.enabled && scmProvider == hg && scmResourceGroup == staging`,
	`config.hg.enabled && scmProvider == hg && scmResourceGroup == untracked`,
	`config.hg.enabled && scmProvider == hg && scmResourceGroup == working`,
	`config.hg.enabled && scmProvider == hg`,
	`config.hg.enabled`,
	`config.hungryDelete.enableSmartBackspace && editorTextFocus && !editorReadonly`,
	`config.intersystemsServerManager.authentication.provider == intersystems-server-credentials`,
	`config.io-run.showRunIconInEditorTitleMenu`,
	`config.iot_editor.show_explorer_views == true`,
	`config.javascript-repl.menuItemsOnContext && !javascript-repl.started && resourceExtname =~ /(js|mjs|jsx|ts|tsx|vue|coffee|md)$/i`,
	`config.javascript-repl.menuItemsOnContext && javascript-repl.expl && javascript-repl.started`,
	`config.javascript-repl.menuItemsOnContext && javascript-repl.started && editorHasSelection`,
	`config.javascript-repl.menuItemsOnContext && javascript-repl.started`,
	`config.jenkins.views.nodes.enabled`,
	`config.jenkins.views.system.enabled`,
	`config.jenkins.views.views.enabled`,
	`config.jupyter.contextualHelp.enabled`,
	`config.jupyter.enableKeyboardShortcuts && notebookCellListFocused && notebookCellType == 'code' || config.jupyter.enableKeyboardShortcuts && editorTextFocus && inputFocus && notebookEditorFocused`,
	`config.jupyter.enableKeyboardShortcuts && notebookEditorFocused && !inputFocus`,
	`config.jupyter.inspectKernelMessages.enabled && view == jupyterKernelsView && viewItem =~ /activeKernelRoot/`,
	`config.jupyter.inspectKernelMessages.enabled && view == jupyterKernelsView && viewItem =~ /kernelMessage/ && viewItem =~ /canCopyToClipboard/`,
	`config.jupyter.inspectKernelMessages.enabled && view == jupyterKernelsView && viewItem =~ /kernelMessagesRoot/`,
	`config.jupyter.kernelManagement.enabled && false && (jupyter.ispythonorinteractiveeactive || jupyter.isnativeactive)`,
	`config.jupyter.logging.level != debug`,
	`config.jupyter.logging.level != error`,
	`config.jupyter.notebookRunGroups.groupCount > 0 && config.jupyter.notebookRunGroups.enabled`,
	`config.jupyter.notebookRunGroups.groupCount > 0 && config.jupyter.notebookRunGroups.runIconsOnEditorToolbar && config.jupyter.notebookRunGroups.enabled`,
	`config.jupyter.notebookRunGroups.groupCount > 0 && notebookCellResource in notebookRunGroups.groupOneCells && config.jupyter.notebookRunGroups.enabled`,
	`config.jupyter.notebookRunGroups.groupCount > 0 && notebookCellResource in notebookRunGroups.groupOneCells && config.jupyter.notebookRunGroups.runIconsInExecute && config.jupyter.notebookRunGroups.enabled`,
	`config.jupyter.notebookRunGroups.groupCount > 0 && notebookCellResource in notebookRunGroups.groupOneCells && config.jupyter.notebookRunGroups.runIconsOnCell && config.jupyter.notebookRunGroups.enabled`,
	`config.jupyter.notebookRunGroups.groupCount > 0 && notebookCellResource not in notebookRunGroups.groupOneCells && config.jupyter.notebookRunGroups.enabled`,
	`config.jupyter.notebookRunGroups.groupCount > 1 && config.jupyter.notebookRunGroups.enabled`,
	`config.jupyter.notebookRunGroups.groupCount > 1 && config.jupyter.notebookRunGroups.runIconsOnEditorToolbar && config.jupyter.notebookRunGroups.enabled`,
	`config.jupyter.notebookRunGroups.groupCount > 1 && notebookCellResource in notebookRunGroups.groupTwoCells && config.jupyter.notebookRunGroups.enabled`,
	`config.jupyter.notebookRunGroups.groupCount > 1 && notebookCellResource in notebookRunGroups.groupTwoCells && config.jupyter.notebookRunGroups.runIconsInExecute && config.jupyter.notebookRunGroups.enabled`,
	`config.jupyter.notebookRunGroups.groupCount > 1 && notebookCellResource in notebookRunGroups.groupTwoCells && config.jupyter.notebookRunGroups.runIconsOnCell && config.jupyter.notebookRunGroups.enabled`,
	`config.jupyter.notebookRunGroups.groupCount > 1 && notebookCellResource not in notebookRunGroups.groupTwoCells && config.jupyter.notebookRunGroups.enabled`,
	`config.jupyter.notebookRunGroups.groupCount > 2 && config.jupyter.notebookRunGroups.enabled`,
	`config.jupyter.notebookRunGroups.groupCount > 2 && config.jupyter.notebookRunGroups.runIconsOnEditorToolbar && config.jupyter.notebookRunGroups.enabled`,
	`config.jupyter.notebookRunGroups.groupCount > 2 && notebookCellResource in notebookRunGroups.groupThreeCells && config.jupyter.notebookRunGroups.enabled`,
	`config.jupyter.notebookRunGroups.groupCount > 2 && notebookCellResource in notebookRunGroups.groupThreeCells && config.jupyter.notebookRunGroups.runIconsInExecute && config.jupyter.notebookRunGroups.enabled`,
	`config.jupyter.notebookRunGroups.groupCount > 2 && notebookCellResource in notebookRunGroups.groupThreeCells && config.jupyter.notebookRunGroups.runIconsOnCell && config.jupyter.notebookRunGroups.enabled`,
	`config.jupyter.notebookRunGroups.groupCount > 2 && notebookCellResource not in notebookRunGroups.groupThreeCells && config.jupyter.notebookRunGroups.enabled`,
	`config.kaleidoscope.compareTool && !explorerResourceIsFolder`,
	`config.kaleidoscope.compareTool`,
	`config.kx-vscode-angular-context-creator.angular.component.showInContextMenu`,
	`config.kx-vscode-angular-context-creator.angular.directive.showInContextMenu`,
	`config.kx-vscode-angular-context-creator.angular.module.showInContextMenu`,
	`config.kx-vscode-angular-context-creator.angular.pipe.showInContextMenu`,
	`config.kx-vscode-angular-context-creator.angular.service.showInContextMenu`,
	`config.kx-vscode-angular-context-creator.typescript.showClassInContextMenu`,
	`config.kx-vscode-angular-context-creator.typescript.showEnumInContextMenu`,
	`config.kx-vscode-angular-context-creator.typescript.showInterfaceInContextMenu`,
	`config.latex-utilities.formattedPaste.useAsDefault && editorTextFocus && editorLangId == 'latex'`,
	`config.latex-workshop.bind.altKeymap.enabled && editorTextFocus && !editorReadonly && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$/`,
	`config.latex-workshop.bind.altKeymap.enabled && editorTextFocus && !editorReadonly && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$|^pweave$/`,
	`config.latex-workshop.bind.altKeymap.enabled`,
	`config.latex-workshop.bind.enter.key && editorTextFocus && !acceptSuggestionOnEnter && !editorReadonly && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$/ && !vim.active`,
	`config.latex-workshop.bind.enter.key && editorTextFocus && !acceptSuggestionOnEnter && !editorReadonly && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$/ && vim.active && vim.mode == 'Insert'`,
	`config.latex-workshop.bind.enter.key && editorTextFocus && !acceptSuggestionOnEnter && !editorReadonly && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$/`,
	`config.latex-workshop.bind.enter.key && editorTextFocus && !acceptSuggestionOnEnter && !editorReadonly && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$|^pweave$/ && !vim.active`,
	`config.latex-workshop.bind.enter.key && editorTextFocus && !acceptSuggestionOnEnter && !editorReadonly && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$|^pweave$/ && vim.active && vim.mode == 'Insert'`,
	`config.latex-workshop.bind.enter.key && editorTextFocus && !acceptSuggestionOnEnter && !editorReadonly && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$|^pweave$/`,
	`config.latex-workshop.bind.enter.key && editorTextFocus && acceptSuggestionOnEnter && !suggestWidgetVisible && !editorReadonly && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$/ && !vim.active`,
	`config.latex-workshop.bind.enter.key && editorTextFocus && acceptSuggestionOnEnter && !suggestWidgetVisible && !editorReadonly && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$/ && vim.active && vim.mode == 'Insert'`,
	`config.latex-workshop.bind.enter.key && editorTextFocus && acceptSuggestionOnEnter && !suggestWidgetVisible && !editorReadonly && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$/`,
	`config.latex-workshop.bind.enter.key && editorTextFocus && acceptSuggestionOnEnter && !suggestWidgetVisible && !editorReadonly && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$|^pweave$/ && !vim.active`,
	`config.latex-workshop.bind.enter.key && editorTextFocus && acceptSuggestionOnEnter && !suggestWidgetVisible && !editorReadonly && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$|^pweave$/ && vim.active && vim.mode == 'Insert'`,
	`config.latex-workshop.bind.enter.key && editorTextFocus && acceptSuggestionOnEnter && !suggestWidgetVisible && !editorReadonly && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$|^pweave$/`,
	`config.latex-workshop.showContextMenu && editorLangId == latex`,
	`config.latex-workshop.showContextMenu && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$/ && !virtualWorkspace`,
	`config.latex-workshop.showContextMenu && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$|^pweave$/ && !virtualWorkspace`,
	`config.lava.showMenus && !explorerResourceIsRoot && !explorerResourceIsFolder`,
	`config.lava.showMenus && !explorerResourceIsRoot && explorerResourceIsFolder`,
	`config.lava.showMenus && isFileSystemResource`,
	`config.lifeboatapi.stormworks.isMicrocontrollerProject || config.lifeboatapi.stormworks.isAddonProject`,
	`config.lifeboatapi.stormworks.isMicrocontrollerProject`,
	`config.liveshare.teamStatus || liveshare:isTeamMember`,
	`config.locateThisDocument.editorTitleContext && resourceScheme == file`,
	`config.locateThisDocument.editorTitleIcons.show && config.locateThisDocument.editorTitleIcons.locateActiveDocument && resourceScheme == file`,
	`config.locateThisDocument.editorTitleIcons.show && config.locateThisDocument.editorTitleIcons.openActiveDocumentInFileExplorer && resourceScheme == file && remoteName == ''`,
	`config.markJump.showExplorerView == true`,
	`config.masmRunner.enableContextMenus`,
	`config.materialTheme.accent == true`,
	`config.maven.showInExplorerContextMenu && explorerResourceIsFolder`,
	`config.maximizeterminal.useAlternativeKeybinding && !activePanel`,
	`config.maximizeterminal.useAlternativeKeybinding && activePanel`,
	`config.mdb.showMongoDBConnectionExplorer == true`,
	`config.mdb.showMongoDBHelpExplorer == true`,
	`config.mdb.showMongoDBPlaygrounds == true`,
	`config.memo-life-for-you.withRespectMode`,
	`config.mocha.showInExplorer == false`,
	`config.mocha.showInExplorer == true`,
	`config.mock-server.settings.contextMenu.makeRequest`,
	`config.mock-server.settings.contextMenu.mockIt && resourceExtname == .json || config.mock-server.settings.contextMenu.mockIt && resourceExtname == .jsonc || config.mock-server.settings.contextMenu.mockIt && resourceExtname == .js || config.mock-server.settings.contextMenu.mockIt && resourceExtname == .har`,
	`config.mock-server.settings.contextMenu.mockServerSubMenu`,
	`config.mock-server.settings.contextMenu.pasteServerConfig`,
	`config.mock-server.settings.contextMenu.setAsServerConfig && resourceExtname == .json || config.mock-server.settings.contextMenu.setAsServerConfig && resourceExtname == .jsonc || config.mock-server.settings.contextMenu.setAsServerConfig && resourceExtname == .js`,
	`config.mock-server.settings.contextMenu.setPort`,
	`config.mock-server.settings.contextMenu.stopServer && isServerRunning`,
	`config.mock-server.settings.contextMenu.switchEnvironment`,
	`config.mock-server.settings.contextMenu.transformToMockDB && resourceExtname == .json || config.mock-server.settings.contextMenu.transformToMockDB && resourceExtname == .jsonc || config.mock-server.settings.contextMenu.transformToMockDB && resourceExtname == .har`,
	`config.mql_tools.Help.HelpON == true && resourceExtname == .mq4 || config.mql_tools.Help.HelpON == true && resourceExtname == .mq5 || config.mql_tools.Help.HelpON == true && resourceExtname == .mqh`,
	`config.mql_tools.context == true`,
	`config.msr.menu.visible && config.msr.compareFileListsWithGitIgnore.menu.visible`,
	`config.msr.menu.visible && config.msr.cookCmdAlias.menu.visible`,
	`config.msr.menu.visible && config.msr.cookCmdAliasByProject.menu.visible`,
	`config.msr.menu.visible && config.msr.cookCmdAliasDumpWithOthersToFiles.menu.visible`,
	`config.msr.menu.visible && config.msr.cookCmdAliasDumpWithOthersToFilesByProject.menu.visible`,
	`config.msr.menu.visible && config.msr.cookCmdAliasFiles.menu.visible`,
	`config.msr.menu.visible && config.msr.cookCmdAliasFilesByProject.menu.visible`,
	`config.msr.menu.visible && config.msr.findSelectedPlainTextInAllSmallFiles.menu.visible`,
	`config.msr.menu.visible && config.msr.findSelectedPlainTextInAllSourceFiles.menu.visible`,
	`config.msr.menu.visible && config.msr.findSelectedPlainTextInCodeAndConfigFiles.menu.visible`,
	`config.msr.menu.visible && config.msr.findSelectedPlainTextInCodeFiles.menu.visible`,
	`config.msr.menu.visible && config.msr.findSelectedPlainTextInConfigFiles.menu.visible`,
	`config.msr.menu.visible && config.msr.findSelectedPlainTextInDocFiles.menu.visible`,
	`config.msr.menu.visible && config.msr.findTopCodeFolder.menu.visible`,
	`config.msr.menu.visible && config.msr.findTopCodeType.menu.visible`,
	`config.msr.menu.visible && config.msr.findTopFolder.menu.visible`,
	`config.msr.menu.visible && config.msr.findTopSourceFolder.menu.visible`,
	`config.msr.menu.visible && config.msr.findTopSourceType.menu.visible`,
	`config.msr.menu.visible && config.msr.findTopType.menu.visible`,
	`config.msr.menu.visible && config.msr.regexFindAsClassOrMethodDefinitionInCodeFiles.menu.visible`,
	`config.msr.menu.visible && config.msr.regexFindDefinitionInCurrentFile.menu.visible`,
	`config.msr.menu.visible && config.msr.regexFindInAllSmallFiles.menu.visible`,
	`config.msr.menu.visible && config.msr.regexFindInAllSourceFiles.menu.visible`,
	`config.msr.menu.visible && config.msr.regexFindInSameTypeFiles.menu.visible`,
	`config.msr.menu.visible && config.msr.regexFindPureReferencesInAllSourceFiles.menu.visible`,
	`config.msr.menu.visible && config.msr.regexFindPureReferencesInCodeFiles.menu.visible`,
	`config.msr.menu.visible && config.msr.regexFindReferencesInCodeAndConfig.menu.visible`,
	`config.msr.menu.visible && config.msr.regexFindReferencesInCodeFiles.menu.visible`,
	`config.msr.menu.visible && config.msr.regexFindReferencesInConfigFiles.menu.visible`,
	`config.msr.menu.visible && config.msr.regexFindReferencesInCurrentFile.menu.visible`,
	`config.msr.menu.visible && config.msr.regexFindReferencesInDocs.menu.visible`,
	`config.msr.menu.visible && config.msr.sortBySize.menu.visible`,
	`config.msr.menu.visible && config.msr.sortByTime.menu.visible`,
	`config.msr.menu.visible && config.msr.sortCodeBySize.menu.visible`,
	`config.msr.menu.visible && config.msr.sortCodeByTime.menu.visible`,
	`config.msr.menu.visible && config.msr.sortSourceBySize.menu.visible`,
	`config.msr.menu.visible && config.msr.sortSourceByTime.menu.visible`,
	`config.msr.menu.visible && config.msr.tmpToggleEnableFindingDefinition.menu.visible`,
	`config.mssql.enableQueryHistoryFeature && !config.mssql.enableQueryHistoryCapture`,
	`config.mssql.enableQueryHistoryFeature && config.mssql.enableQueryHistoryCapture`,
	`config.mssql.enableQueryHistoryFeature`,
	`config.nasc-touchbar.addCursorAbove`,
	`config.nasc-touchbar.addCursorBelow && !enabledGroup`,
	`config.nasc-touchbar.blockComment && !enabledGroup`,
	`config.nasc-touchbar.commentLine && !enabledGroup`,
	`config.nasc-touchbar.copyLineDown && !enabledGroup`,
	`config.nasc-touchbar.docs && !enabledGroup`,
	`config.nasc-touchbar.enableCursorsGroup && !enabledGroup`,
	`config.nasc-touchbar.enableEditorGroup && !enabledGroup`,
	`config.nasc-touchbar.enableFuncGroup && !enabledGroup`,
	`config.nasc-touchbar.enableSrcGroup && !enabledGroup`,
	`config.nasc-touchbar.gitSync && !enabledGroup`,
	`config.nasc-touchbar.goToDefinition && !enabledGroup`,
	`config.nasc-touchbar.goToNext && !enabledGroup`,
	`config.nasc-touchbar.indent && !enabledGroup`,
	`config.nasc-touchbar.jumpToBracket && !enabledGroup`,
	`config.nasc-touchbar.nextInFiles`,
	`config.nasc-touchbar.outdent && !enabledGroup`,
	`config.nasc-touchbar.peekDefinition && !enabledGroup`,
	`config.nasc-touchbar.rename && !enabledGroup`,
	`config.nasc-touchbar.selectNext && !enabledGroup`,
	`config.nasc-touchbar.settings && !enabledGroup`,
	`config.nasc-touchbar.showCommands && !enabledGroup`,
	`config.nasc-touchbar.showReferences && !enabledGroup`,
	`config.nasc-touchbar.togglePanel && !enabledGroup`,
	`config.nasc-touchbar.toggleSidebar && !enabledGroup`,
	`config.nasc-touchbar.toggleWhiteSpace && !enabledGroup`,
	`config.nesttypeorm.menu.controller`,
	`config.nesttypeorm.menu.entity`,
	`config.nesttypeorm.menu.full`,
	`config.nesttypeorm.menu.module`,
	`config.nesttypeorm.menu.service`,
	`config.netbeans.javaSupport.enabled`,
	`config.noExist`,
	`config.noExists`,
	`config.no_exits`,
	`config.not_exists`,
	`config.notebook.kernelPicker.type != mru`,
	`config.notebook.navigation.allowNavigateToSurroundingCells && editorTextFocus && inputFocus && notebookEditorFocused && notebookEditorCursorAtBoundary != 'bottom' && notebookEditorCursorAtBoundary != 'none'`,
	`config.notebook.navigation.allowNavigateToSurroundingCells && editorTextFocus && inputFocus && notebookEditorFocused && notebookEditorCursorAtBoundary != 'none' && notebookEditorCursorAtBoundary != 'top'`,
	`config.notebook.navigation.allowNavigateToSurroundingCells && notebookCursorNavigationMode && notebookEditorFocused && !notebookCellMarkdownEditMode && notebookCellType == 'markup'`,
	`config.notion.key == ''`,
	`config.now.showExplorer == true`,
	`config.npm.enableTouchbar`,
	`config.nrf-connect.activeVersion`,
	`config.nrf-terminal.terminalMode == 'character'`,
	`config.nrf-terminal.terminalMode == 'line'`,
	`config.nuxt.isNuxtApp`,
	`config.objectscript.showExplorer`,
	`config.open-native-terminal.show-current-folder-option`,
	`config.open-native-terminal.show-root-folder-option`,
	`config.open-native-terminal.show-terminal-icon`,
	`config.openInNpp.extendEditorContextMenu`,
	`config.openInNpp.extendExplorerContextMenu`,
	`config.opened-editors.copyFileName`,
	`config.opened-editors.copyFolderName`,
	`config.opened-editors.foldEnable && editorTextFocus && foldingEnabled`,
	`config.opened-editors.openedEditors`,
	`config.opened-editors.revealSidebar`,
	`config.openhab.useRestApi == true`,
	`config.openjdkDevel.github.apiToken == ''`,
	`config.openjdkDevel.github.username == ''`,
	`config.openjdkDevel.labelFilter == '' && config.openjdkDevel.repoFilter == ''`,
	`config.p42.showGetStarted`,
	`config.p42.touchBar.quickFix.enabled && isMac && resourceExtname =~ /^(\\.js|\\.jsx|\\.cjs|\\.mjs|\\.ts|\\.tsx|\\.cts|\\.mts|\\.vue)$/`,
	`config.p42.touchBar.refactor.enabled && isMac && resourceExtname =~ /^(\\.js|\\.jsx|\\.cjs|\\.mjs|\\.ts|\\.tsx|\\.cts|\\.mts|\\.vue)$/`,
	`config.p42.touchBar.rename.enabled && isMac && resourceExtname =~ /^(\\.js|\\.jsx|\\.cjs|\\.mjs|\\.ts|\\.tsx|\\.cts|\\.mts|\\.vue)$/`,
	`config.p42.touchBar.sourceAction.enabled && isMac && resourceExtname =~ /^(\\.js|\\.jsx|\\.cjs|\\.mjs|\\.ts|\\.tsx|\\.cts|\\.mts|\\.vue)$/`,
	`config.papyrus.fallout4.enabled && explorerResourceIsFolder`,
	`config.papyrus.skyrim.enabled && explorerResourceIsFolder`,
	`config.papyrus.skyrimSpecialEdition.enabled && explorerResourceIsFolder`,
	`config.perforce.activationMode != off && !perforce.activation.status`,
	`config.perforce.activationMode != off && perforce.activation.status == complete`,
	`config.perforce.activationMode != off && perforce.activation.status == noworkspace`,
	`config.perforce.activationMode == off`,
	`config.perforce.explorer.showSyncCommands && perforce.activation.hasScmProvider || config.perforce.explorer.showBasicOpCommands && perforce.activation.hasScmProvider || config.perforce.explorer.showFileOpCommands && perforce.activation.hasScmProvider`,
	`config.pnpm.enableTouchbar`,
	`config.poshProTools.sideBar.astVisibility`,
	`config.poshProTools.sideBar.customVisibility`,
	`config.poshProTools.sideBar.historyVisibility`,
	`config.poshProTools.sideBar.hostProcessVisibility`,
	`config.poshProTools.sideBar.jobVisibility`,
	`config.poshProTools.sideBar.moduleVisibility`,
	`config.poshProTools.sideBar.providerVisibility`,
	`config.poshProTools.sideBar.quickScriptVisibility`,
	`config.poshProTools.sideBar.reflectionVisibility`,
	`config.poshProTools.sideBar.sessionVisibility`,
	`config.poshProTools.sideBar.variableVisibility`,
	`config.poshProTools.toolbar.formDesigner && resourceExtname == .ps1 || resourceExtname == .xaml`,
	`config.postgres.never`,
	`config.postgres.showExplorer == true`,
	`config.powermode.enabled`,
	`config.powershell.sideBar.CommandExplorerVisibility`,
	`config.profiles.keepInSync`,
	`config.projects.viewAllEnabled == true`,
	`config.promyze.connection.apiKey != ''`,
	`config.puppet.titleBar.pdkNewModule.enable`,
	`config.py2flowchart.showRunIconInEditorTitleMenu`,
	`config.rainbow_csv.enable_context_menu_head || resourceLangId =~ /plaintext|^[ct]sv/`,
	`config.rainbow_csv.enable_context_menu_tail`,
	`config.remote-build.apiToken && resourceFilename =~ /.+\\.cprj$/`,
	`config.remote.SSH.useLocalServer`,
	`config.remoteHub.logLevel != debug`,
	`config.remoteHub.logLevel != off && config.remoteHub.logLevel != error && config.remoteHub.logLevel != warn`,
	`config.reveal.active`,
	`config.sap.ux.applicationModeler.testBetaFeatures.manifestEditor && resourceFilename == manifest.json`,
	`config.sap.ux.applicationModeler.testBetaFeatures.manifestEditor`,
	`config.sap.ux.internal.enableInternalFeatures`,
	`config.sc2layout.treeview.visible == true`,
	`config.search.show.Duckduckgo`,
	`config.search.show.Google`,
	`config.search.show.Stackoverflow`,
	`config.search.show.Technet`,
	`config.search.show.Thwack`,
	`config.search.show.VSCodeAPI`,
	`config.search.show.VSCodeWorkspace`,
	`config.search.show.dbatools`,
	`config.search.show.docs`,
	`config.snapTextEditor.showSnapRightButton`,
	`config.snippets.useWorkspaceFolder && snippets.workspaceState != fileAvailable`,
	`config.snippets.useWorkspaceFolder`,
	`config.sol.LanguageServer.EnableHistoryLogTree`,
	`config.sort-json.settings.contextMenu.ascendingSort`,
	`config.sort-json.settings.contextMenu.customSort`,
	`config.sort-json.settings.contextMenu.descendingSort`,
	`config.sort-json.settings.contextMenu.isCaseSensitive && !config.sort-json.settings.isCaseSensitive`,
	`config.sort-json.settings.contextMenu.isCaseSensitive && config.sort-json.settings.isCaseSensitive`,
	`config.sort-json.settings.contextMenu.setListSortType`,
	`config.sort-json.settings.contextMenu.setObjectSortType`,
	`config.sort-json.settings.contextMenu.setSortLevel`,
	`config.sort-json.settings.contextMenu.sortJsonSubMenu`,
	`config.spark-ar-studio.synchronize`,
	`config.splunk.enableSplunkExplorer == true`,
	`config.statsBar.enabled`,
	`config.svn.enabled && originalResourceScheme == svn`,
	`config.svn.enabled && scmProvider == svn && isSvn19orGreater`,
	`config.svn.enabled && scmProvider == svn && scmResourceGroup != external`,
	`config.svn.enabled && scmProvider == svn && scmResourceGroup != unversioned && scmResourceGroup != external && scmResourceGroup != conflicts && scmResourceGroup != remotechanges`,
	`config.svn.enabled && scmProvider == svn && scmResourceGroup != unversioned && scmResourceGroup != external`,
	`config.svn.enabled && scmProvider == svn && scmResourceGroup == changes`,
	`config.svn.enabled && scmProvider == svn && scmResourceGroup == conflicts`,
	`config.svn.enabled && scmProvider == svn && scmResourceGroup == remotechanges`,
	`config.svn.enabled && scmProvider == svn && scmResourceGroup == unversioned`,
	`config.svn.enabled && scmProvider == svn`,
	`config.svn.enabled && svnOpenRepositoryCount != 0 && explorerViewletVisible && filesExplorerFocus && !explorerResourceIsRoot && !inputFocus`,
	`config.svn.enabled && svnOpenRepositoryCount != 0 && isInDiffEditor && resourceScheme != extension && resourceScheme != merge-conflicts.conflicts-diff`,
	`config.svn.enabled && svnOpenRepositoryCount != 0 && isSvn18orGreater`,
	`config.svn.enabled && svnOpenRepositoryCount != 0 && isSvn19orGreater`,
	`config.svn.enabled && svnOpenRepositoryCount != 0 && svnActiveEditorHasChanges && !isInDiffEditor && resourceScheme == file`,
	`config.svn.enabled && svnOpenRepositoryCount != 0 && svnActiveEditorHasChanges`,
	`config.svn.enabled && svnOpenRepositoryCount != 0`,
	`config.svn.enabled`,
	`config.svnext.enabled && config.svn.enabled && svnOpenRepositoryCount != 0 && !isInDiffEditor && explorerViewletVisible && filesExplorerFocus && !explorerResourceIsRoot && !inputFocus`,
	`config.svnext.enabled && config.svn.enabled && svnOpenRepositoryCount != 0 && !isInDiffEditor && resourceScheme == file`,
	`config.svnext.enabled && config.svn.enabled && svnOpenRepositoryCount != 0 && resourceScheme == file && svnActiveEditorHasChanges`,
	`config.svnext.enabled && config.svn.enabled && svnOpenRepositoryCount != 0 && svnActiveEditorHasChanges && explorerViewletVisible && filesExplorerFocus && !explorerResourceIsRoot && !inputFocus`,
	`config.svnext.enabled && svnOpenRepositoryCount != 0 && !isInDiffEditor && explorerViewletVisible && filesExplorerFocus && !explorerResourceIsRoot && !inputFocus`,
	`config.svnext.enabled && svnOpenRepositoryCount != 0 && !isInDiffEditor && resourceScheme == file`,
	`config.tabulous.touchBar && config.tabulous.createNamedTerminalTouchBarButton`,
	`config.tabulous.touchBar && config.tabulous.reloadDefaultTerminalsTouchBarButton`,
	`config.tabulous.touchBar && config.tabulous.renameTerminalTouchBarButton`,
	`config.taskExplorer.enableExplorerView && config.taskExplorer.showExecuteWithArgumentsButton && view == taskExplorer && viewItem == scriptFileS`,
	`config.taskExplorer.enableExplorerView && config.taskExplorer.showExecuteWithArgumentsButton && view == taskExplorer && viewItem == scriptFile`,
	`config.taskExplorer.enableExplorerView && config.taskExplorer.showExecuteWithNoTerminalButton && view == taskExplorer && viewItem == scriptFileS`,
	`config.taskExplorer.enableExplorerView && config.taskExplorer.showExecuteWithNoTerminalButton && view == taskExplorer && viewItem == scriptFile`,
	`config.taskExplorer.enableExplorerView && config.taskExplorer.showExecuteWithNoTerminalButton && view == taskExplorer && viewItem == scriptS`,
	`config.taskExplorer.enableExplorerView && config.taskExplorer.showExecuteWithNoTerminalButton && view == taskExplorer && viewItem == script`,
	`config.taskExplorer.enableExplorerView && config.taskExplorer.showFavoritesButton && view == taskExplorer && viewItem == scriptFileS`,
	`config.taskExplorer.enableExplorerView && config.taskExplorer.showFavoritesButton && view == taskExplorer && viewItem == scriptFile`,
	`config.taskExplorer.enableExplorerView && config.taskExplorer.showFavoritesButton && view == taskExplorer && viewItem == scriptRunningS`,
	`config.taskExplorer.enableExplorerView && config.taskExplorer.showFavoritesButton && view == taskExplorer && viewItem == scriptRunning`,
	`config.taskExplorer.enableExplorerView && config.taskExplorer.showFavoritesButton && view == taskExplorer && viewItem == scriptS`,
	`config.taskExplorer.enableExplorerView && config.taskExplorer.showFavoritesButton && view == taskExplorer && viewItem == script`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == favorites`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == lasttasks`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == scriptFileS`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == scriptFile`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == scriptRunningS`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == scriptRunning`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == scriptS`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == script`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskFileAnt`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskFileApp-publisher`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskFileBash`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskFileBatch`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskFileGradle`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskFileGrunt`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskFileGulp`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskFileMake`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskFileNpm`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskFileNsis`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskFilePerl`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskFilePowershell`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskFilePython`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskFileRuby`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskFileWorkspace`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskGroupAnt`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskGroupApp-publisher`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskGroupBash`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskGroupBatch`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskGroupGradle`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskGroupGrunt`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskGroupGulp`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskGroupMake`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskGroupNpm`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskGroupNsis`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskGroupPerl`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskGroupPipenv`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskGroupPowershell`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskGroupPython`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskGroupRuby`,
	`config.taskExplorer.enableExplorerView && view == taskExplorer && viewItem == taskGroupWorkspace`,
	`config.taskExplorer.enableExplorerView`,
	`config.taskExplorer.enableSideBar && config.taskExplorer.showExecuteWithArgumentsButton && view == taskExplorerSideBar && viewItem == scriptFileS`,
	`config.taskExplorer.enableSideBar && config.taskExplorer.showExecuteWithArgumentsButton && view == taskExplorerSideBar && viewItem == scriptFile`,
	`config.taskExplorer.enableSideBar && config.taskExplorer.showExecuteWithNoTerminalButton && view == taskExplorerSideBar && viewItem == scriptFileS`,
	`config.taskExplorer.enableSideBar && config.taskExplorer.showExecuteWithNoTerminalButton && view == taskExplorerSideBar && viewItem == scriptFile`,
	`config.taskExplorer.enableSideBar && config.taskExplorer.showExecuteWithNoTerminalButton && view == taskExplorerSideBar && viewItem == scriptS`,
	`config.taskExplorer.enableSideBar && config.taskExplorer.showExecuteWithNoTerminalButton && view == taskExplorerSideBar && viewItem == script`,
	`config.taskExplorer.enableSideBar && config.taskExplorer.showFavoritesButton && view == taskExplorerSideBar && viewItem == scriptFileS`,
	`config.taskExplorer.enableSideBar && config.taskExplorer.showFavoritesButton && view == taskExplorerSideBar && viewItem == scriptFile`,
	`config.taskExplorer.enableSideBar && config.taskExplorer.showFavoritesButton && view == taskExplorerSideBar && viewItem == scriptRunningS`,
	`config.taskExplorer.enableSideBar && config.taskExplorer.showFavoritesButton && view == taskExplorerSideBar && viewItem == scriptRunning`,
	`config.taskExplorer.enableSideBar && config.taskExplorer.showFavoritesButton && view == taskExplorerSideBar && viewItem == scriptS`,
	`config.taskExplorer.enableSideBar && config.taskExplorer.showFavoritesButton && view == taskExplorerSideBar && viewItem == script`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == favorites`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == lasttasks`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == scriptFileS`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == scriptFile`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == scriptRunningS`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == scriptRunning`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == scriptS`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == script`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskFileAnt`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskFileApp-publisher`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskFileBash`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskFileBatch`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskFileGradle`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskFileGrunt`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskFileGulp`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskFileMake`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskFileNpm`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskFileNsis`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskFilePerl`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskFilePowershell`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskFilePython`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskFileRuby`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskFileWorkspace`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskGroupAnt`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskGroupApp-publisher`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskGroupBash`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskGroupBatch`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskGroupGradle`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskGroupGrunt`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskGroupGulp`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskGroupMake`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskGroupNpm`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskGroupNsis`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskGroupPerl`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskGroupPipenv`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskGroupPowershell`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskGroupPython`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskGroupRuby`,
	`config.taskExplorer.enableSideBar && view == taskExplorerSideBar && viewItem == taskGroupWorkspace`,
	`config.taskExplorer.enableSideBar`,
	`config.tdqt.key == false`,
	`config.templates.showExplorer == true`,
	`config.terraform.languageServer.enable == false`,
	`config.terraform.languageServer.enable == true`,
	`config.text-utils.aes.enabled`,
	`config.text-utils.base64.enabled`,
	`config.text-utils.case.enabled`,
	`config.text-utils.deduplicate.enabled`,
	`config.text-utils.hex.enabled`,
	`config.text-utils.list.enabled`,
	`config.text-utils.sort.enabled`,
	`config.text-utils.theDate.enabled`,
	`config.text-utils.unicode.enabled`,
	`config.text-utils.uri.enabled`,
	`config.textx.showExplorer == true`,
	`config.toggle.iconEnabled`,
	`config.touchBar.closeOtherEditors`,
	`config.touchBar.commands`,
	`config.touchBar.comment`,
	`config.touchBar.copyLinesDownAction`,
	`config.touchBar.foldAll`,
	`config.touchBar.ftpSync`,
	`config.touchBar.goToReferences`,
	`config.touchBar.organizeImports`,
	`config.touchBar.qopen`,
	`config.touchBar.rename`,
	`config.touchBar.showActiveFileInExplorer`,
	`config.touchBar.showActivityBar && config.touchBar.debug`,
	`config.touchBar.showActivityBar && config.touchBar.extensions`,
	`config.touchBar.showActivityBar && config.touchBar.scm`,
	`config.touchBar.showActivityBar && config.touchBar.search`,
	`config.touchBar.showActivityBar && config.touchBar.showExplorer`,
	`config.touchBar.term`,
	`config.touchBar.terminal`,
	`config.touchBar.themeToggle`,
	`config.touchBar.toggleSidebar`,
	`config.touchBar.toggleZenMode`,
	`config.touchBar.unfoldAll`,
	`config.touchbarCommandPalette.commandPalette`,
	`config.touchbarCommandPalette.quickOpen`,
	`config.touchbarCommandPalette.sidebar`,
	`config.touchbarCommandPalette.terminal`,
	`config.touchui.enable&&!config.touchui.deving`,
	`config.touchui.enable&&config.touchui.builded`,
	`config.touchui.enable&&config.touchui.deving`,
	`config.touchui.enable&&false`,
	`config.touchui.enable`,
	`config.touchuiwx.enable`,
	`config.treeview.location == explorer`,
	`config.treeview.location == sidebar`,
	`config.twitchHighlighter.showHighlightsInActivityBar`,
	`config.udstudio.sideBar.udstudioVisibility`,
	`config.ue-python.execute.enableShortcut`,
	`config.ungit.showButton`,
	`config.var-conv.contextMenu.enabled`,
	`config.vc6-keybindings.enableExtraKeys`,
	`config.verilogrunner.Verilog.showRunIconInEditorTitleMenu && resourceExtname in verilogrunner.Verilog.Supported`,
	`config.viewX.canPreviewActiveDocument && editorTextFocus`,
	`config.viewX.canPreviewActiveDocument`,
	`config.vlocity.salesforce.enabled`,
	`config.vlocity.salesforce.profileActionsInContextMenu && config.vlocity.salesforce.enabled && explorerResourceIsFolder && resourceDirname =~ /(objects|classes)/`,
	`config.vlocity.salesforce.profileActionsInContextMenu && config.vlocity.salesforce.enabled && resourceScheme == file && resourceFilename =~ /(\\.object|\\.field-meta\\.xml|\\.cls|\\.cls-meta\\.xml)$/`,
	`config.vsc-memer.contextMenu.enabled`,
	`config.vscode-corda.isCordaProject`,
	`config.vscode-corda.nodesAreRunning`,
	`config.vscode-easy-shortcuts.LaunchInNewVSCode.visible`,
	`config.vscode-easy-shortcuts.OpenInExplorer.visible`,
	`config.vscode-easy-shortcuts.RemoveLines.visible`,
	`config.vscode-easy-shortcuts.ReopenClosedEditor.visible`,
	`config.vscode-easy-shortcuts.SelectLines.visible`,
	`config.vscode-easy-shortcuts.ToggleBlockComment.visible`,
	`config.vscode-easy-shortcuts.ToggleBreakpoint.visible`,
	`config.vscode-easy-shortcuts.ToggleLineComment.visible`,
	`config.vscode-postgres.never`,
	`config.vscode-postgres.showExplorer == true`,
	`config.vscode-uuid-generator.contextMenu.enabled`,
	`config.vscodeRandom.contextMenu.enabled`,
	`config.vscsm.showMenu.blameInSublimeMerge == 'always' && config.git.enabled && gitOpenRepositoryCount != 0`,
	`config.vscsm.showMenu.fileHistoryInSublimeMerge == 'always' && config.git.enabled && gitOpenRepositoryCount != 0`,
	`config.vscsm.showMenu.lineHistoryInSublimeMerge == 'always' && config.git.enabled && gitOpenRepositoryCount != 0`,
	`config.vscsm.showMenu.myCommitsInSublimeMerge == 'always' && config.git.enabled && gitOpenRepositoryCount != 0`,
	`config.vscsm.showMenu.openInSublimeMerge && config.git.enabled && gitOpenRepositoryCount != 0`,
	`config.vstest.enable`,
	`config.vue-typescript-files.menu.class`,
	`config.vue-typescript-files.menu.component`,
	`config.vue-typescript-files.menu.declare`,
	`config.vue-typescript-files.menu.enum`,
	`config.vue-typescript-files.menu.interface`,
	`config.vue-typescript-files.menu.vuex`,
	`config.web-CAT.snarfURLs != [] && web-CAT.snarfsErrored`,
	`config.web-CAT.snarfURLs != [] && web-CAT.snarfsLoaded`,
	`config.web-CAT.snarfURLs == []`,
	`config.web-CAT.submitURLs != [] && web-CAT.targetsErrored`,
	`config.web-CAT.submitURLs != [] && web-CAT.targetsLoaded`,
	`config.web-CAT.submitURLs == []`,
	`config.workbench.enablePreviewFeatures || config.sqlDatabaseProjects.enablePreviewFeatures`,
	`config.workspace-manager.views.favorites.enabled && config.workspace-manager.views.favorites.location == workspaceManager`,
	`config.workspace-manager.views.groups.enabled && config.workspace-manager.views.groups.location == workspaceManager`,
	`config.workspace-manager.views.workspaces.enabled && config.workspace-manager.views.workspaces.location == workspaceManager`,
	`config.yarn.enableTouchbar`,
	`config.zopeneditor.userbuild.enabled && resourceLangId == cobol || resourceLangId == pl1 || resourceLangId == hlasm`,
	`console-ninja.browserRuntimeConnected`,
	`console-ninja.hasAnError`,
	`console-ninja.started && console-ninja.outputMode.view`,
	`console-ninja.started`,
	`context == thiscontextdoesnotexist7603253285`,
	`context-legato-lsp-ready`,
	`cortex-debug:showRTOS`,
	`cpp-proj.hasCMake`,
	`cpp-runner.running`,
	`cpp.ast.hasData`,
	`cpp.ast.supported`,
	`cpp.memoryUsage.hasData`,
	`cpp.memoryUsage.supported`,
	`cpp.typeHierarchyVisible`,
	`cppReferenceTypes:hasResults`,
	`crumbs.context.activeDocumentCount > 0`,
	`csharpstretch.class`,
	`csharpstretch.interface`,
	`csharpstretch.oldClass`,
	`csharpstretch.oldInterface`,
	`csharpstretch.showMenuOnFiles || explorerResourceIsFolder && csharpstretch.showMenu`,
	`ctsscript.isManagedFile`,
	`customExecutionSupported`,
	`customTreeView && listFocus && !inputFocus`,
	`cvs-plugin.started == false`,
	`cvs-plugin.started == true && view == changed-files && cvs-plugin.view_changed_status == false`,
	`cvs-plugin.started == true && view == changed-files && cvs-plugin.view_changed_status == true`,
	`cvs-plugin.started == true && view == changed-files && cvs-plugin.view_conflict_status == false`,
	`cvs-plugin.started == true && view == changed-files && cvs-plugin.view_conflict_status == true`,
	`cvs-plugin.started == true && view == changed-files && cvs-plugin.view_questionable_status == false`,
	`cvs-plugin.started == true && view == changed-files && cvs-plugin.view_questionable_status == true`,
	`cvs-plugin.started == true && view == changed-files && cvs-plugin.view_updated_status == false`,
	`cvs-plugin.started == true && view == changed-files && cvs-plugin.view_updated_status == true`,
	`cvs-plugin.started == true && view == changed-files`,
	`cvs-plugin.started == true`,
	`cvs-scm.enabled == true`,
	`cwtoolsEventFile == true && cwtoolsWebview == false`,
	`cwtoolsGraphFile == true && cwtoolsWebview == false`,
	`cwtoolsWebview == true`,
	`cxSettings.showWorkspaceOnly == false && !explorerResourceIsFolder`,
	`cxSettings.showWorkspaceOnly == false && explorerResourceIsFolder`,
	`d.hasDubProject`,
	`d.isActive`,
	`dance.mode == 'normal'`,
	`dart-code:anyFlutterProjectLoaded && !inDebugMode && dart-code:flutterSupportsAttach`,
	`dart-code:anyFlutterProjectLoaded && !inDebugMode`,
	`dart-code:anyFlutterProjectLoaded && dart-code:isRunningLocally && config.dart.flutterShowEmulators == local || dart-code:anyFlutterProjectLoaded && config.dart.flutterShowEmulators == always`,
	`dart-code:anyFlutterProjectLoaded && dart-code:pubOutdatedSupported`,
	`dart-code:anyFlutterProjectLoaded && dart-code:showFlutterOutline`,
	`dart-code:anyFlutterProjectLoaded && inDebugMode && debugType == dart && dart-code:isInFlutterDebugModeDebugSession && config.dart.showDevToolsDebugToolBarButtons`,
	`dart-code:anyFlutterProjectLoaded && inDebugMode && debugType == dart && dart-code:isInFlutterProfileModeDebugSession && config.dart.showDevToolsDebugToolBarButtons`,
	`dart-code:anyFlutterProjectLoaded && inDebugMode && debugType == dart && dart-code:service.reloadSources`,
	`dart-code:anyFlutterProjectLoaded && inDebugMode && debugType == dart`,
	`dart-code:anyFlutterProjectLoaded`,
	`dart-code:anyProjectLoaded && !inDebugMode`,
	`dart-code:anyProjectLoaded && dart-code:anyFlutterProjectLoaded`,
	`dart-code:anyProjectLoaded && dart-code:canGoToTestOrImplementationFile`,
	`dart-code:anyProjectLoaded && dart-code:hasLastDebugConfig && !inDebugMode`,
	`dart-code:anyProjectLoaded && dart-code:hasLastDebugConfig`,
	`dart-code:anyProjectLoaded && dart-code:hasLastTestDebugConfig`,
	`dart-code:anyProjectLoaded && dart-code:isCapturingLogs`,
	`dart-code:anyProjectLoaded && dart-code:pubOutdatedSupported`,
	`dart-code:anyProjectLoaded && editorHasSelection`,
	`dart-code:anyProjectLoaded && editorLangId == dart && !dart-code:isLsp`,
	`dart-code:anyProjectLoaded && editorLangId == dart`,
	`dart-code:anyProjectLoaded && inDebugMode && debugType == dart && dart-code:anyFlutterProjectLoaded`,
	`dart-code:anyProjectLoaded && inDebugMode && debugType == dart`,
	`dart-code:anyProjectLoaded`,
	`dart-code:flutterProjectLoaded && inDebugMode`,
	`dart-support.showView`,
	`dbux.context.activated && dbux.context.nodeEnv == development && false`,
	`dbux.context.activated && dbux.context.nodeEnv == development`,
	`dbux.context.activated && dbux.context.researchEnabled`,
	`dbux.context.activated && dbux.context.showPracticeViews && !dbux.context.hasPracticeSession`,
	`dbux.context.activated && dbux.context.showPracticeViews && dbux.context.hasPracticeSession`,
	`dbux.context.activated && dbuxTraceDetailsView.context.hasTracesAtCursor && dbux.context.showNavButton`,
	`dbux.context.activated && editorLangId == javascript && dbux.context.showNavButton`,
	`dbux.context.activated`,
	`dbuxWebView.context.dbux-graph.isActive && !dbux.context.isApplicationEmpty && dbux.context.showNavButton`,
	`debugConfigurationType == 'autohotkey' && debugProtocolVariableMenuContext == 'number'`,
	`debugConfigurationType == 'autohotkey' && debugProtocolVariableMenuContext == 'string' || debugProtocolVariableMenuContext == 'number'`,
	`debugConfigurationType == 'autohotkey' && debugProtocolVariableMenuContext == 'string'`,
	`debugConfigurationType == 'lua' && debugProtocolVariableMenuContext == 'integer/dec'`,
	`debugConfigurationType == 'lua' && debugProtocolVariableMenuContext == 'integer/hex'`,
	`debugConfigurationType == 'pwa-chrome'`,
	`debugConfigurationType == pwa-node && !inDebugMode && activeViewlet == workbench.view.debug || debugConfigurationType == pwa-extensionHost && !inDebugMode && activeViewlet == workbench.view.debug || debugConfigurationType == node && !inDebugMode && activeViewlet == workbench.view.debug`,
	`debugConfigurationType == pwa-node && !inDebugMode || debugConfigurationType == pwa-extensionHost && !inDebugMode || debugConfigurationType == node && !inDebugMode`,
	`debugProtocolVariableMenuContext == 'ocamlearlybird.function'`,
	`debugProtocolVariableMenuContext == 'viewableInDataViewer'`,
	`debugProtocolVariableMenuContext == Device`,
	`debugProtocolVariableMenuContext == String || debugProtocolVariableMenuContext == Number`,
	`debugProtocolVariableMenuContext == viewableInGraphicViewer`,
	`debugStartLanguage == cpp || debugStartLanguage == c || debugStartLanguage == cuda-cpp`,
	`debugStartLanguage == csharp`,
	`debugStartLanguage == javascript && !isWeb || debugStartLanguage == typescript && !isWeb || debugStartLanguage == javascriptreact && !isWeb || debugStartLanguage == typescriptreact && !isWeb`,
	`debugStartLanguage == javascript && isWeb || debugStartLanguage == typescript && isWeb || debugStartLanguage == javascriptreact && isWeb || debugStartLanguage == typescriptreact && isWeb`,
	`debugState != 'inactive'`,
	`debugState == 'running'`,
	`debugState == 'stopped'`,
	`debugState == inactive && resourceScheme =~ /^serenji(-readonly)?$/ && resourceLangId =~ /^serenji\\.(int|mac|cls)$/`,
	`debugState == stopped && resource in jsDebugCanPrettyPrint`,
	`debugType == 'R-Debugger'`,
	`debugType == 'bazis2'`,
	`debugType == 'brightscript' && view == rendezvousView`,
	`debugType == 'brightscript'`,
	`debugType == 'chrome'`,
	`debugType == 'cppdbg' && udbActive && callStackItemType == 'thread'`,
	`debugType == 'cppdbg' && udbActive`,
	`debugType == 'java' && javadebug:showHex == 'off'`,
	`debugType == 'java' && javadebug:showHex == 'on'`,
	`debugType == 'java' && javadebug:showLogicalStructure == 'off'`,
	`debugType == 'java' && javadebug:showLogicalStructure == 'on'`,
	`debugType == 'java' && javadebug:showQualifiedNames == 'off'`,
	`debugType == 'java' && javadebug:showQualifiedNames == 'on'`,
	`debugType == 'java' && javadebug:showStaticVariables == 'off'`,
	`debugType == 'java' && javadebug:showStaticVariables == 'on'`,
	`debugType == 'java' && javadebug:showToString == 'off'`,
	`debugType == 'java' && javadebug:showToString == 'on'`,
	`debugType == 'lldb'`,
	`debugType == 'luat' && debugProtocolVariableMenuContext == 'simple'`,
	`debugType == 'mock' && debugProtocolVariableMenuContext == 'simple'`,
	`debugType == 'neo-contract'`,
	`debugType == 'nwjs'`,
	`debugType == 'powercpp'`,
	`debugType == 'qml' && !qmldebug.filterFunctions`,
	`debugType == 'qml' && !qmldebug.sortMembers`,
	`debugType == 'qml' && qmldebug.filterFunctions`,
	`debugType == 'qml' && qmldebug.sortMembers`,
	`debugType == 'radjav'`,
	`debugType == 'shadered' && debugProtocolVariableMenuContext == 'simple'`,
	`debugType == 'squirrel' && debugProtocolVariableMenuContext == 'simple'`,
	`debugType == 'winuae' || debugType == 'fs-uae'`,
	`debugType == amiga`,
	`debugType == cfm-debugger && debugProtocolVariableMenuContext == simple`,
	`debugType == cortex-arm-learning`,
	`debugType == cortex-debug`,
	`debugType == cspy && iar-debug.sessionIsMulticore`,
	`debugType == csse3010-debug`,
	`debugType == iotlink-debug`,
	`debugType == kratos`,
	`debugType == node2`,
	`debugType == platformio-debug && view == platformio-debug.memory`,
	`debugType == platformio-debug`,
	`debugType == pwa-chrome && callStackItemType == 'session' || debugType == pwa-msedge && callStackItemType == 'session'`,
	`debugType == pwa-chrome || debugType == pwa-msedge`,
	`debugType == pwa-extensionHost && !jsDebugIsProfiling && callStackItemType == 'session' || debugType == node-terminal && !jsDebugIsProfiling && callStackItemType == 'session' || debugType == pwa-node && !jsDebugIsProfiling && callStackItemType == 'session' || debugType == pwa-chrome && !jsDebugIsProfiling && callStackItemType == 'session' || debugType == pwa-msedge && !jsDebugIsProfiling && callStackItemType == 'session'`,
	`debugType == pwa-extensionHost && !jsDebugIsProfiling || debugType == node-terminal && !jsDebugIsProfiling || debugType == pwa-node && !jsDebugIsProfiling || debugType == pwa-chrome && !jsDebugIsProfiling || debugType == pwa-msedge && !jsDebugIsProfiling`,
	`debugType == pwa-extensionHost && callStackItemType == 'session' || debugType == node-terminal && callStackItemType == 'session' || debugType == pwa-node && callStackItemType == 'session' || debugType == pwa-chrome && callStackItemType == 'session' || debugType == pwa-msedge && callStackItemType == 'session'`,
	`debugType == pwa-extensionHost && callStackItemType == 'stackFrame' || debugType == node-terminal && callStackItemType == 'stackFrame' || debugType == pwa-node && callStackItemType == 'stackFrame' || debugType == pwa-chrome && callStackItemType == 'stackFrame' || debugType == pwa-msedge && callStackItemType == 'stackFrame'`,
	`debugType == pwa-extensionHost && debugState == \"stopped\" || debugType == node-terminal && debugState == \"stopped\" || debugType == pwa-node && debugState == \"stopped\" || debugType == pwa-chrome && debugState == \"stopped\" || debugType == pwa-msedge && debugState == \"stopped\"`,
	`debugType == pwa-extensionHost && debugState == stopped || debugType == node-terminal && debugState == stopped || debugType == pwa-node && debugState == stopped || debugType == pwa-chrome && debugState == stopped || debugType == pwa-msedge && debugState == stopped`,
	`debugType == pwa-extensionHost && inDebugMode && !jsDebugIsProfiling || debugType == node-terminal && inDebugMode && !jsDebugIsProfiling || debugType == pwa-node && inDebugMode && !jsDebugIsProfiling || debugType == pwa-chrome && inDebugMode && !jsDebugIsProfiling || debugType == pwa-msedge && inDebugMode && !jsDebugIsProfiling`,
	`debugType == pwa-extensionHost && inDebugMode && jsDebugIsProfiling || debugType == node-terminal && inDebugMode && jsDebugIsProfiling || debugType == pwa-node && inDebugMode && jsDebugIsProfiling || debugType == pwa-chrome && inDebugMode && jsDebugIsProfiling || debugType == pwa-msedge && inDebugMode && jsDebugIsProfiling`,
	`debugType == pwa-extensionHost && inDebugMode || debugType == node-terminal && inDebugMode || debugType == pwa-node && inDebugMode || debugType == pwa-chrome && inDebugMode || debugType == pwa-msedge && inDebugMode`,
	`debugType == pwa-extensionHost && jsDebugHasExcludedCallers || debugType == node-terminal && jsDebugHasExcludedCallers || debugType == pwa-node && jsDebugHasExcludedCallers || debugType == pwa-chrome && jsDebugHasExcludedCallers || debugType == pwa-msedge && jsDebugHasExcludedCallers`,
	`debugType == pwa-extensionHost && jsDebugIsProfiling && callStackItemType == 'session' || debugType == node-terminal && jsDebugIsProfiling && callStackItemType == 'session' || debugType == pwa-node && jsDebugIsProfiling && callStackItemType == 'session' || debugType == pwa-chrome && jsDebugIsProfiling && callStackItemType == 'session' || debugType == pwa-msedge && jsDebugIsProfiling && callStackItemType == 'session'`,
	`debugType == pwa-extensionHost && jsDebugIsProfiling || debugType == node-terminal && jsDebugIsProfiling || debugType == pwa-node && jsDebugIsProfiling || debugType == pwa-chrome && jsDebugIsProfiling || debugType == pwa-msedge && jsDebugIsProfiling`,
	`debugType == pwa-extensionHost && view == workbench.debug.callStackView && !jsDebugIsMapSteppingDisabled || debugType == node-terminal && view == workbench.debug.callStackView && !jsDebugIsMapSteppingDisabled || debugType == pwa-node && view == workbench.debug.callStackView && !jsDebugIsMapSteppingDisabled || debugType == pwa-chrome && view == workbench.debug.callStackView && !jsDebugIsMapSteppingDisabled || debugType == pwa-msedge && view == workbench.debug.callStackView && !jsDebugIsMapSteppingDisabled`,
	`debugType == pwa-extensionHost && view == workbench.debug.callStackView && jsDebugIsMapSteppingDisabled || debugType == node-terminal && view == workbench.debug.callStackView && jsDebugIsMapSteppingDisabled || debugType == pwa-node && view == workbench.debug.callStackView && jsDebugIsMapSteppingDisabled || debugType == pwa-chrome && view == workbench.debug.callStackView && jsDebugIsMapSteppingDisabled || debugType == pwa-msedge && view == workbench.debug.callStackView && jsDebugIsMapSteppingDisabled`,
	`debugType == pwa-msedge`,
	`debugType == scrypt`,
	`debugType == totvs_tdsreplay_debug`,
	`debugType == tuya-debug && view == tuya-debug.memory`,
	`debugType == tuya-debug`,
	`debugType =~ /^pwa-/ && !vscodeJsProfileFlameSplitCharts`,
	`debugType =~ /^pwa-/ && vscodeJsProfileFlameSplitCharts`,
	`debugType =~ /^pwa-/ || debugType == node-terminal`,
	`debuggersAvailable && !config.debugControlsInTitlebar.hideDebugStart && !inDebugMode || debugState == 'initializing'`,
	`debuggersAvailable && !inDebugMode && !terminalFocus`,
	`debuggersAvailable && debugState != 'initializing'`,
	`debuggersAvailable && debugState == 'inactive'`,
	`debuggersAvailable && editorTextFocus && editorHasSelection`,
	`debuggersAvailable && editorTextFocus`,
	`debuggersAvailable && inDebugMode && debugState == 'running'`,
	`debuggersAvailable && inDebugMode && debugState == 'stopped'`,
	`debuggersAvailable && inDebugMode`,
	`debuggersAvailable`,
	`decentraland.isDCL`,
	`defaultInstanceEnv == 'v4_streamingAnalytics'`,
	`defaultInstanceEnv == 'v5_cpd' && consoleEnabled == true`,
	`defaultInstanceEnv == 'v5_cpd'`,
	`defaultInstanceEnv == 'v5_standalone'`,
	`defaultInstanceEnv =~ /(v5_cpd|v5_standalone)/ && explorerResourceIsFolder || defaultInstanceEnv =~ /(v5_cpd|v5_standalone)/ && resourceFilename == toolkit.xml`,
	`defaultInstanceEnv =~ /(v5_cpd|v5_standalone)/ && view == streamsToolkits && viewItem == buildService_label_toolkitTreeItem`,
	`defaultInstanceEnv =~ /(v5_cpd|v5_standalone)/`,
	`deltanjiObjectStatus && deltanjiObjectStatus != '?'`,
	`deltanjiObjectStatus == '?'`,
	`deltanjiObjectStatus == 'checkedOut' || deltanjiObjectStatus == 'unregistered'`,
	`deltanjiObjectStatus == 'checkedOut'`,
	`deltanjiObjectStatus == 'registered'`,
	`deltanjiObjectStatus == 'unregistered'`,
	`deltanjiServerURL`,
	`dendron:devMode`,
	`dendron:pluginActive && dendron:hasLegacyPreview`,
	`dendron:pluginActive && dendron:noteLookupActive && !editorFocus && !view`,
	`dendron:pluginActive && dendron:noteLookupActive && dendron:shouldShowLookupView`,
	`dendron:pluginActive && shellExecutionSupported`,
	`dendron:pluginActive`,
	`denoExtensionActivated`,
	`device-manager:hasDevicePack && resourceFilename =~ /.+\\.(axf|elf)$/`,
	`device-manager:isDaplink && resourceFilename =~ /.+\\.(bin|hex)$/`,
	`devto:authorized && view == devto && viewItem != unpublished`,
	`devto:authorized && view == devto && viewItem == unpublished`,
	`devto:authorized && view == devto`,
	`devtools.isJSProject && explorerResourceIsFolder`,
	`devtools.isTSProject && explorerResourceIsFolder`,
	`diffMerge.panelFocused && diffMerge.hasChanges`,
	`diffMerge.panelFocused`,
	`diffMergeFileSelected`,
	`digitaljs.view_isactive && resourceExtname =~ /^\\.sv$|^\\.v$|^\\.vh$|^\\.lua$/`,
	`digitaljs.view_isactive`,
	`digitaljs.view_isfocus && digitaljs.view_hascircuit && !digitaljs.view_running`,
	`digitaljs.view_isfocus && digitaljs.view_hascircuit && digitaljs.view_running`,
	`digitaljs.view_isfocus`,
	`dimensionscm.DimensionsContainer.DefineConnection && view =~ /^dimensionscm\\..*Container/`,
	`dimensionscm.DimensionsContainer.DefineConnection == false`,
	`dimensionscm.RequestTree.Visible && dimensionscm.HasDefaultRequest`,
	`dimensionscm.RequestTree.Visible`,
	`dired.open && !findWidgetVisible && !inQuickOpen`,
	`dired.open && editorTextFocus && !inDebugRepl`,
	`dirtyDiffVisible`,
	`discord-channel-focused`,
	`discord-channel-selected`,
	`discord-chat-in-explorer && discord-chat-tree-not-empty`,
	`divinity.storyOutline.enabled`,
	`dockerInstalled && !validApigeeWorkspace`,
	`dockerInstalled && viewItem =~ /^.*\\'apigee.environments.deploy\\'.*$/`,
	`dockerInstalled && viewItem =~ /^.*\\'apigee.tests.setupTest\\'.*$/`,
	`dockerInstalled`,
	`docsView.documentationView.isPinned`,
	`documatic.isLoggedIn && view =~ /^documatic/`,
	`documatic.isLoggedIn`,
	`dothttpRunMode == full && notebookType == 'dothttp-book'`,
	`dothttpRunMode == full`,
	`dreammakerObjtreeReady`,
	`dvc.cli.incompatible`,
	`dvc.commands.available && !dvc.cli.incompatible && !dvc.project.available && !dvc.scm.command.running`,
	`dvc.commands.available && dvc.project.available && !dvc.experiment.running && !dvc.experiment.checkpoints`,
	`dvc.commands.available && dvc.project.available && !dvc.experiment.running && dvc.experiment.checkpoints`,
	`dvc.commands.available && dvc.project.available && !dvc.experiment.running`,
	`dvc.commands.available && dvc.project.available && !dvc.experiments.filter.selected`,
	`dvc.commands.available && dvc.project.available && !dvc.scm.command.running`,
	`dvc.commands.available && dvc.project.available && dvc.experiment.stoppable`,
	`dvc.commands.available && dvc.project.available && dvc.experiments.filter.selected`,
	`dvc.commands.available && dvc.project.available && dvc.multiple.projects`,
	`dvc.commands.available && dvc.project.available && dvc.project.hasData`,
	`dvc.commands.available && dvc.project.available`,
	`dvc.experiment.stoppable && dvc.commands.available`,
	`dvc.experiments.webview.active && !dvc.experiment.running && dvc.commands.available && !dvc.experiment.checkpoints`,
	`dvc.experiments.webview.active && !dvc.experiment.running && dvc.commands.available && dvc.experiment.checkpoints`,
	`dvc.experiments.webview.active && !dvc.experiment.running && dvc.commands.available`,
	`dvc.params.file.active && !dvc.experiment.running && dvc.commands.available && !dvc.experiment.checkpoints`,
	`dvc.params.file.active && !dvc.experiment.running && dvc.commands.available && dvc.experiment.checkpoints`,
	`dvc.params.file.active && !dvc.experiment.running && dvc.commands.available`,
	`dvt.isCalledFromVscode && dvt.visibleVerificationHierarchy && dvt.isShowOnlyComponentsVH`,
	`dvt.isCalledFromVscode && view == dvt.CompileOrderView && !dvt.showFilesWithErrorsCO`,
	`dvt.isCalledFromVscode && view == dvt.CompileOrderView && dvt.showFilesWithErrorsCO`,
	`dvt.isCalledFromVscode && view == dvt.CompileOrderView`,
	`dvt.isCalledFromVscode && view == dvt.DesignHierarchyView && !dvt.hideGeneratesDesignHierarchy`,
	`dvt.isCalledFromVscode && view == dvt.DesignHierarchyView && dvt.hideGeneratesDesignHierarchy`,
	`dvt.isCalledFromVscode && view == dvt.DesignHierarchyView`,
	`dvt.isCalledFromVscode && view == dvt.VerificationHierarchyView && !dvt.isShowOnlyComponentsVH`,
	`dvt.isCalledFromVscode && view == dvt.VerificationHierarchyView && dvt.isShowOnlyComponentsVH`,
	`dvt.isCalledFromVscode && view == dvt.VerificationHierarchyView`,
	`dvt.isCalledFromVscode`,
	`dvt.isExtensionActive && !dvt.buildInProgress`,
	`dvt.isExtensionActive && !dvt.isCalledFromVscode`,
	`dvt.isExtensionActive && !dvt.isThreadDumpCollecting && dvt.isCalledFromVscode`,
	`dvt.isExtensionActive && dvt.buildInProgress`,
	`dvt.isExtensionActive && dvt.isCalledFromVscode`,
	`dvt.isExtensionActive && dvt.isDiagnosticsViewInitialized`,
	`dvt.isExtensionActive && dvt.isThreadDumpCollecting && dvt.isCalledFromVscode`,
	`dvt.isExtensionActive && dvt.visibleCompileOrder && !dvt.showFilesWithErrorsCO`,
	`dvt.isExtensionActive && dvt.visibleCompileOrder && dvt.showFilesWithErrorsCO`,
	`dvt.isExtensionActive && dvt.visibleCompileOrder`,
	`dvt.isExtensionActive && dvt.visibleDesignHierarchy && !dvt.hideGeneratesDesignHierarchy`,
	`dvt.isExtensionActive && dvt.visibleDesignHierarchy && dvt.hideGeneratesDesignHierarchy`,
	`dvt.isExtensionActive && dvt.visibleDesignHierarchy`,
	`dvt.isExtensionActive && dvt.visibleVerificationHierarchy && !dvt.isShowOnlyComponentsVH`,
	`dvt.isExtensionActive && dvt.visibleVerificationHierarchy`,
	`dvt.isExtensionActive && editorIsOpen && dvt.breadcrumbHasMultipleSegments && !dvt.visibleDesignBreadcrumb && dvt.visibleVerificationBreadcrumb`,
	`dvt.isExtensionActive && editorIsOpen && dvt.breadcrumbHasMultipleSegments && dvt.visibleDesignBreadcrumb && !dvt.visibleVerificationBreadcrumb`,
	`dvt.isExtensionActive && editorIsOpen && dvt.breadcrumbHasOtherInstances && dvt.visibleDesignBreadcrumb`,
	`dvt.isExtensionActive && editorIsOpen && dvt.breadcrumbHasOtherInstances && dvt.visibleVerificationBreadcrumb`,
	`dvt.isExtensionActive && editorIsOpen && dvt.visibleDesignBreadcrumb`,
	`dvt.isExtensionActive && editorIsOpen && dvt.visibleVerificationBreadcrumb`,
	`dvt.isExtensionActive && editorIsOpen && editorLangId == systemverilog && !dvt.isCalledFromEclipseChe`,
	`dvt.isExtensionActive && editorIsOpen && editorLangId == systemverilog && dvt.isCalledFromVscode || dvt.isExtensionActive && editorIsOpen && editorLangId == vhdl && dvt.isCalledFromVscode`,
	`dvt.isExtensionActive && editorIsOpen && editorLangId == systemverilog && dvt.isCalledFromVscode`,
	`dvt.isExtensionActive && editorIsOpen && editorLangId == systemverilog || dvt.isExtensionActive && editorIsOpen && editorLangId == vhdl`,
	`dvt.isExtensionActive && editorIsOpen && editorLangId == systemverilog`,
	`dvt.isExtensionActive && editorIsOpen && editorLangId == vhdl`,
	`dvt.isExtensionActive && editorLangId == systemverilog && dvt.isCalledFromVscode`,
	`dvt.isExtensionActive && editorLangId == systemverilog`,
	`dvt.isExtensionActive && editorLangId == vhdl && dvt.isCalledFromVscode`,
	`dvt.isExtensionActive && editorLangId == vhdl`,
	`dvt.isExtensionActive`,
	`e4e.treeView.initialized`,
	`editorColumnSelection && textInputFocus`,
	`editorFocus != true`,
	`editorFocus &&  resourceLangId == javascript`,
	`editorFocus &&  resourceLangId == vue`,
	`editorFocus && !config.emacs-mcx.useMetaPrefixMacCmd`,
	`editorFocus && !editorHasSelection && python.datascience.hascodecells && python.datascience.featureenabled`,
	`editorFocus && !editorHasSelection`,
	`editorFocus && !editorReadonly`,
	`editorFocus && !findWidgetVisible && editorLangId == 'elm'`,
	`editorFocus && !findWidgetVisible`,
	`editorFocus && !replaceActive && !searchViewletVisible && !findWidgetVisible`,
	`editorFocus && !tableMode`,
	`editorFocus && (resourceLangId == groovy || resourceLangId == jenkinsfile)`,
	`editorFocus && (resourceLangId == json || resourceLangId == jsonc || resourceLangId == arm-template)`,
	`editorFocus && ForceCodeActive  && editorIsOpen && ForceCodeLoggedIn`,
	`editorFocus && ForceCodeActive && editorHasSelection && ForceCodeLoggedIn`,
	`editorFocus && ForceCodeActive`,
	`editorFocus && config.emacs-mcx.useMetaPrefixCtrlLeftBracket`,
	`editorFocus && config.emacs-mcx.useMetaPrefixEscape`,
	`editorFocus && config.emacs-mcx.useMetaPrefixMacCmd`,
	`editorFocus && dendron:pluginActive`,
	`editorFocus && editorHasSelection && editorLangId == python && !findInputFocussed && !replaceInputFocussed && python.datascience.ownsSelection && python.datascience.featureenabled`,
	`editorFocus && editorHasSelection && resourceLangId == vue`,
	`editorFocus && editorHasSelection`,
	`editorFocus && editorIsOpen && !editorHasSelection && !editorReadonly`,
	`editorFocus && editorLangId == 'haskell'`,
	`editorFocus && editorLangId == 'sql'`,
	`editorFocus && editorLangId == 'wdl'`,
	`editorFocus && editorLangId == 'xit'`,
	`editorFocus && editorLangId == applescript`,
	`editorFocus && editorLangId == bridlensis`,
	`editorFocus && editorLangId == cbrother`,
	`editorFocus && editorLangId == cfml || editorLangId == html`,
	`editorFocus && editorLangId == cfml`,
	`editorFocus && editorLangId == csharp`,
	`editorFocus && editorLangId == gs`,
	`editorFocus && editorLangId == idris`,
	`editorFocus && editorLangId == innosetup`,
	`editorFocus && editorLangId == kusto && !notebookEditorFocused`,
	`editorFocus && editorLangId == lidris`,
	`editorFocus && editorLangId == logstash`,
	`editorFocus && editorLangId == nim && !findInputFocussed && !replaceInputFocussed`,
	`editorFocus && editorLangId == nsis`,
	`editorFocus && editorLangId == nsl-assembler`,
	`editorFocus && editorLangId == python &&  python.datascience.featureenabled && python.datascience.ownsSelection`,
	`editorFocus && editorLangId == python && !findInputFocussed && !replaceInputFocussed && !python.datascience.ownsSelection`,
	`editorFocus && editorLangId == python && !notebookEditorFocused && isWorkspaceTrusted`,
	`editorFocus && editorLangId == python && !python.datascience.ownsSelection`,
	`editorFocus && editorLangId == python && !virtualWorkspace && shellExecutionSupported`,
	`editorFocus && editorLangId == python && jupyter.hascodecells && !notebookEditorFocused && isWorkspaceTrusted`,
	`editorFocus && editorLangId == python && python.datascience.hascodecells && python.datascience.featureenabled`,
	`editorFocus && editorLangId == python && resourceExtname == .ipynb && !notebookEditorFocused && isWorkspaceTrusted`,
	`editorFocus && editorLangId == python && resourceLangId == jupyter && python.datascience.featureenabled`,
	`editorFocus && editorLangId == python || activeEditor == 'workbench.editor.interactive'`,
	`editorFocus && editorLangId == python`,
	`editorFocus && editorLangId == r`,
	`editorFocus && editorLangId == sas && !SAS.hideRunMenuItem`,
	`editorFocus && editorLangId == typescriptreact`,
	`editorFocus && editorLangId =~ /latex|markdown/`,
	`editorFocus && findInputFocussed`,
	`editorFocus && findWidgetVisible && !isComposing && !config.emacs-mcx.useMetaPrefixMacCmd`,
	`editorFocus && findWidgetVisible && !isComposing && config.emacs-mcx.useMetaPrefixCtrlLeftBracket`,
	`editorFocus && findWidgetVisible && !isComposing && config.emacs-mcx.useMetaPrefixEscape`,
	`editorFocus && findWidgetVisible && !isComposing && config.emacs-mcx.useMetaPrefixMacCmd`,
	`editorFocus && findWidgetVisible && !isComposing`,
	`editorFocus && findWidgetVisible && !replaceInputFocussed && !isComposing`,
	`editorFocus && findWidgetVisible && replaceInputFocussed`,
	`editorFocus && findWidgetVisible`,
	`editorFocus && foldingEnabled`,
	`editorFocus && isWindows && remoteName == wsl`,
	`editorFocus && markersNavigationVisible`,
	`editorFocus && parameterHintsMultipleSignatures && parameterHintsVisible`,
	`editorFocus && parameterHintsVisible`,
	`editorFocus && quickFixWidgetVisible`,
	`editorFocus && renameInputVisible`,
	`editorFocus && resourceExtname== '.geojson'`,
	`editorFocus && resourceExtname== '.json'`,
	`editorFocus && resourceExtname== '.txt'`,
	`editorFocus && resourceFilename =~ /Untitled-/`,
	`editorFocus && resourceFilename =~ /[.](didact)[.](md|adoc)$/`,
	`editorFocus && resourceLangId == csharp && editorHasSelection && isWindows`,
	`editorFocus && resourceLangId == html`,
	`editorFocus && resourceLangId == javascript`,
	`editorFocus && resourceLangId == json5`,
	`editorFocus && resourceLangId == json`,
	`editorFocus && resourceLangId == p4`,
	`editorFocus && resourceLangId == python`,
	`editorFocus && resourceLangId == robot`,
	`editorFocus && resourceLangId == sourcepawn`,
	`editorFocus && resourceLangId == sql`,
	`editorFocus && resourceLangId == typescript`,
	`editorFocus && resourceLangId == vue`,
	`editorFocus && resourceLangId =~ /^sql$|^jinja-sql$/`,
	`editorFocus && resourceScheme != 'untitled'`,
	`editorFocus && suggestWidgetVisible`,
	`editorFocus && tableMode`,
	`editorFocus && testing.isPeekVisible`,
	`editorFocus && vim.active && vim.mode != 'Insert' && !inDebugRepl`,
	`editorFocus && vim.mode == 'Insert'`,
	`editorFocus &&!editorReadonly && inlineSuggestionVisible`,
	`editorFocus == true`,
	`editorFocus \u0026\u0026 !findWidgetVisible \u0026\u0026 editorLangId == \u0027fsharp\u0027`,
	`editorFocus \u0026\u0026 editorLangId == \u0027fsharp\u0027`,
	`editorFocus || editorIsOpen`,
	`editorFocus&& resourceLangId == xml`,
	`editorFocus`,
	`editorHasCallHierarchyProvider && editorTextFocus && !inReferenceSearchEditor`,
	`editorHasCallHierarchyProvider`,
	`editorHasCodeActionsProvider && !editorReadonly`,
	`editorHasCodeActionsProvider && editorTextFocus && !editorReadonly && config.netbeans.javaSupport.enabled`,
	`editorHasCodeActionsProvider && editorTextFocus && !editorReadonly`,
	`editorHasCodeActionsProvider && editorTextFocus && config.netbeans.javaSupport.enabled`,
	`editorHasCompletionItemProvider && editorTextFocus && !editorReadonly`,
	`editorHasCompletionItemProvider && textInputFocus && !editorReadonly`,
	`editorHasDeclarationProvider && editorTextFocus && !isInEmbeddedEditor`,
	`editorHasDefinitionProvider && editorTextFocus && !inReferenceSearchEditor && !isInEmbeddedEditor`,
	`editorHasDefinitionProvider && editorTextFocus && !inReferenceSearchEditor`,
	`editorHasDefinitionProvider && editorTextFocus && !isInEmbeddedEditor`,
	`editorHasDefinitionProvider && editorTextFocus`,
	`editorHasDocumentFormattingProvider && editorHasDocumentFormattingProvider && editorTextFocus && !editorReadonly`,
	`editorHasDocumentFormattingProvider && editorHasSelection && editorTextFocus && !editorReadonly`,
	`editorHasDocumentFormattingProvider && editorTextFocus && !editorReadonly && !inCompositeEditor`,
	`editorHasDocumentFormattingProvider && editorTextFocus && !editorReadonly`,
	`editorHasDocumentFormattingProvider && editorTextFocus && inCompositeEditor && notebookEditable && !editorReadonly && activeEditor == 'workbench.editor.notebook'`,
	`editorHasDocumentFormattingProvider`,
	`editorHasDocumentSelectionFormattingProvider && editorHasSelection && editorTextFocus && !editorReadonly`,
	`editorHasDocumentSelectionFormattingProvider && editorTextFocus && !editorReadonly`,
	`editorHasFocus && editorHasSelection`,
	`editorHasFormattingProvider && editorTextFocus && !editorReadonly`,
	`editorHasHighlights`,
	`editorHasImplementationProvider && editorTextFocus && !inReferenceSearchEditor && !isInEmbeddedEditor`,
	`editorHasImplementationProvider && editorTextFocus && !isInEmbeddedEditor`,
	`editorHasImplementationProvider`,
	`editorHasMultipleSelections && editorTextFocus`,
	`editorHasMultipleSelections && textInputFocus`,
	`editorHasMultipleSelections`,
	`editorHasReferenceProvider && editorTextFocus && !inReferenceSearchEditor && !isInEmbeddedEditor`,
	`editorHasReferenceProvider && editorTextFocus && !inReferenceSearchEditor`,
	`editorHasReferenceProvider`,
	`editorHasRenameProvider && editorTextFocus && !editorReadonly`,
	`editorHasSelection && !editorHasMultipleSelections`,
	`editorHasSelection && !editorIsReadonly`,
	`editorHasSelection && !editorReadOnly`,
	`editorHasSelection && !inOutput && config.command-runner.showRunCommandInEditorContextMenu`,
	`editorHasSelection && !isInDiffEditor`,
	`editorHasSelection && !suggestWidgetVisible`,
	`editorHasSelection && ForceCodeActive && ForceCodeLoggedIn`,
	`editorHasSelection && config.customcoloring.contextMenu.showQuickAddKeywords && !config.customcoloring.contextMenu.showQuickAddRanges`,
	`editorHasSelection && config.customcoloring.contextMenu.showQuickAddKeywords`,
	`editorHasSelection && config.customcoloring.contextMenu.showQuickAddRanges && !config.customcoloring.contextMenu.showQuickAddKeywords`,
	`editorHasSelection && config.customcoloring.contextMenu.showQuickAddRanges`,
	`editorHasSelection && config.open-native-terminal.show-current-folder-option`,
	`editorHasSelection && config.open-native-terminal.show-root-folder-option`,
	`editorHasSelection && config.shareGitFile.enableMarkdownSupport`,
	`editorHasSelection && config.text-utils.reverseString.enabled`,
	`editorHasSelection && config.vscsm.showMenu.blameInSublimeMerge == 'withSelection'  && config.git.enabled && gitOpenRepositoryCount != 0`,
	`editorHasSelection && config.vscsm.showMenu.lineHistoryInSublimeMerge == 'withSelection' && config.git.enabled && gitOpenRepositoryCount != 0`,
	`editorHasSelection && config.webSearch.enableContextMenu`,
	`editorHasSelection && editorFocus`,
	`editorHasSelection && editorHasMultipleSelections`,
	`editorHasSelection && editorLangId == 'csharp' && config.unity-tools.enableRightClickSearch`,
	`editorHasSelection && editorLangId == 'fsharp'`,
	`editorHasSelection && editorLangId == 'pde'`,
	`editorHasSelection && editorLangId == 'sqf'`,
	`editorHasSelection && editorLangId == 'sql'`,
	`editorHasSelection && editorLangId == cpp`,
	`editorHasSelection && editorLangId == gee`,
	`editorHasSelection && editorLangId == jpl`,
	`editorHasSelection && editorLangId == json || editorHasSelection && editorLangId == jsonc`,
	`editorHasSelection && editorLangId == lua && luaIdeDebugState == true`,
	`editorHasSelection && editorLangId == lua`,
	`editorHasSelection && editorLangId == markdown`,
	`editorHasSelection && editorLangId == python && python.isDjangoProject && !virtualWorkspace && shellExecutionSupported`,
	`editorHasSelection && editorLangId == python && python.isDjangoProject`,
	`editorHasSelection && editorLangId == python`,
	`editorHasSelection && editorLangId == r`,
	`editorHasSelection && editorLangId == scss`,
	`editorHasSelection && editorLangId == tib`,
	`editorHasSelection && editorLangId == zpl`,
	`editorHasSelection && editorLangId =~ /^typescript$|^python$|^php$|^java$|^javascript$/`,
	`editorHasSelection && editorLangId =~ /dynamic csv|plaintext|^[ct]sv/`,
	`editorHasSelection && editorLangId==cpp`,
	`editorHasSelection && editorLangId==go`,
	`editorHasSelection && editorLangId=~/php|blade/`,
	`editorHasSelection && editorLangId=~/php|blade|less|css|html|scss|sass/`,
	`editorHasSelection && editorLangId=~/py/`,
	`editorHasSelection && editorTextFocus && !editorReadonly`,
	`editorHasSelection && editorTextFocus && resourceLangId == maxscript`,
	`editorHasSelection && editorTextFocus`,
	`editorHasSelection && isPullRequest`,
	`editorHasSelection && resourceExtname == .asm`,
	`editorHasSelection && resourceExtname == .feature`,
	`editorHasSelection && resourceFilename =~ /.js|.vue|.ts|.css|.scss|.less|.stylus|.html/`,
	`editorHasSelection && resourceLangId == 'javascript' || resourceLangId == 'typescript'`,
	`editorHasSelection && resourceLangId == gimmick`,
	`editorHasSelection && resourceLangId == javascript`,
	`editorHasSelection && resourceLangId == javascriptreact`,
	`editorHasSelection && resourceLangId == scala`,
	`editorHasSelection && resourceLangId == vue`,
	`editorHasSelection && resourceLangId =~ /(javascript|sql)/`,
	`editorHasSelection && resourceLangId =~ /(javascript|typescript|python)/`,
	`editorHasSelection && resourceLangId =~ /^javascript(react)?$|^typescript(react)?$/`,
	`editorHasSelection && resourceLangId =~ /^less$|^scss$|^jade$|^pug$|^stylus$/`,
	`editorHasSelection && resourceLangId =~ REPLACEWITHTHEABOVETODEBUGPARSING`,
	`editorHasSelection && textInputFocus`,
	`editorHasSelection == false && resourceLangId =~ /(javascript|sql)/`,
	`editorHasSelection == false`,
	`editorHasSelection == true`,
	`editorHasSelection || editorHasMultipleSelections`,
	`editorHasSelection || editorLangId == python || editorLangId == java || editorLangId == typescript || editorLangId == c || editorLangId == cpp`,
	`editorHasSelection`,
	`editorHasSignatureHelpProvider && editorTextFocus`,
	`editorHasTextFocus && editorHasSelection`,
	`editorIsOpen && ! activeEditorIsDirty && !isInDiffEditor && isWorkspaceTrusted && !inSettingsEditor && isFileSystemResource && !isInRightDiffEditor`,
	`editorIsOpen && ! explorerResourceIsFolder`,
	`editorIsOpen && !textCompareEditorActive`,
	`editorIsOpen && codelingo:decorations != visible && !codelingo:snippetCount`,
	`editorIsOpen && codelingo:decorations != visible && codelingo:snippetCount > 0`,
	`editorIsOpen && codelingo:decorations == visible`,
	`editorIsOpen && config.find-and-transform.enableContextMenus`,
	`editorIsOpen && editorLangId == python`,
	`editorIsOpen && inDebugMode && debugType == 'firefox'`,
	`editorIsOpen && resourceScheme != 'untitled' && resourceScheme != 'walkThrough'`,
	`editorIsOpen && resourceScheme == file`,
	`editorIsOpen && view == discloud-apps`,
	`editorIsOpen == false`,
	`editorIsOpen == true && auth.isLoggedIn == true`,
	`editorIsOpen == true`,
	`editorIsOpen`,
	`editorLangId  == maxscript  && resourceExtname == .ms || resourceExtname == .mcr`,
	`editorLangId  == maxscript && resourceExtname == .ms || resourceExtname == .mcr`,
	`editorLangId  == proto3 || editorLangId  == json`,
	`editorLangId  == proto3`,
	`editorLangId  =~ /javascript|typescript|python|java/`,
	`editorLangId != 'Log' && resourceScheme != 'device'`,
	`editorLangId != 'Log' && resourceScheme == 'device'`,
	`editorLangId != 'Log' && resourceScheme == 'ioe'`,
	`editorLangId != 'markdown' && !notebookEditorFocused`,
	`editorLangId != ballerina && !isBallerinaDiagram`,
	`editorLangId != blitzmax`,
	`editorLangId != gdl-xml && editorLangId != gdl-hsf && resourceExtname != .gdl`,
	`editorLangId != gdl-xml && editorLangId != gdl-hsf && resourceExtname != .xml`,
	`editorLangId != markdown && editorLangId != html`,
	`editorLangId != postgres && editorHasSelection && !editorHasMultipleSelections`,
	`editorLangId != retypewriter && reTypewriter.isNotPlaying`,
	`editorLangId !== markdown`,
	`editorLangId && ecmPrint == Bottom`,
	`editorLangId && ecmPrint == Top`,
	`editorLangId && etmButton`,
	`editorLangId ==  c`,
	`editorLangId ==  cc`,
	`editorLangId ==  cpp`,
	`editorLangId == 'CSV (semicolon)' && resourceScheme != csv-edit && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`editorLangId == 'abap' && editorTextFocus && !editorReadonly`,
	`editorLangId == 'abap' && editorTextFocus`,
	`editorLangId == 'abap'`,
	`editorLangId == 'abl'`,
	`editorLangId == 'azcli'`,
	`editorLangId == 'c' && config.C_Cpp.debugShortcut && BuildAndDebug.isSourceFile && BuildAndDebug.isFolderOpen || editorLangId == 'cpp' && config.C_Cpp.debugShortcut && BuildAndDebug.isSourceFile && BuildAndDebug.isFolderOpen || editorLangId == 'cuda-cpp' && config.C_Cpp.debugShortcut && BuildAndDebug.isSourceFile && BuildAndDebug.isFolderOpen`,
	`editorLangId == 'c' && config.C_Cpp.debugShortcut && BuildAndDebug.isSourceFile || editorLangId == 'cpp' && config.C_Cpp.debugShortcut && BuildAndDebug.isSourceFile || editorLangId == 'cuda-cpp' && config.C_Cpp.debugShortcut && BuildAndDebug.isSourceFile`,
	`editorLangId == 'c' && config.C_Cpp.intelliSenseEngine != 'disabled' && config.C_Cpp.intelliSenseEngine != 'Disabled' || editorLangId == 'cpp' && config.C_Cpp.intelliSenseEngine != 'disabled' && config.C_Cpp.intelliSenseEngine != 'Disabled' || editorLangId == 'cuda-cpp' && config.C_Cpp.intelliSenseEngine != 'disabled' && config.C_Cpp.intelliSenseEngine != 'Disabled'`,
	`editorLangId == 'c' && editorTextFocus || editorLangId == 'cpp' && editorTextFocus || editorLangId == 'cuda-cpp' && editorTextFocus`,
	`editorLangId == 'c'`,
	`editorLangId == 'cacheobjectscript'`,
	`editorLangId == 'coffeescript' && !notebookEditorFocused || editorLangId == 'dockerfile' && !notebookEditorFocused || editorLangId == 'git-commit' && !notebookEditorFocused || editorLangId == 'git-rebase' && !notebookEditorFocused || editorLangId == 'diff' && !notebookEditorFocused || editorLangId == 'ignore' && !notebookEditorFocused || editorLangId == 'properties' && !notebookEditorFocused || editorLangId == 'makefile' && !notebookEditorFocused || editorLangId == 'perl' && !notebookEditorFocused || editorLangId == 'perl6' && !notebookEditorFocused || editorLangId == 'powershell' && !notebookEditorFocused || editorLangId == 'python' && !notebookEditorFocused || editorLangId == 'r' && !notebookEditorFocused || editorLangId == 'ruby' && !notebookEditorFocused || editorLangId == 'shellscript' && !notebookEditorFocused || editorLangId == 'yaml' && !notebookEditorFocused || editorLangId == 'cython' && !notebookEditorFocused || editorLangId == 'julia' && !notebookEditorFocused || editorLangId == 'cmake' && !notebookEditorFocused || editorLangId == 'c' && !notebookEditorFocused || editorLangId == 'cpp' && !notebookEditorFocused || editorLangId == 'csharp' && !notebookEditorFocused || editorLangId == 'css' && !notebookEditorFocused || editorLangId == 'go' && !notebookEditorFocused || editorLangId == 'groovy' && !notebookEditorFocused || editorLangId == 'hlsl' && !notebookEditorFocused || editorLangId == 'java' && !notebookEditorFocused || editorLangId == 'javascriptreact' && !notebookEditorFocused || editorLangId == 'javascript' && !notebookEditorFocused || editorLangId == 'json' && !notebookEditorFocused || editorLangId == 'jsonc' && !notebookEditorFocused || editorLangId == 'less' && !notebookEditorFocused || editorLangId == 'objective-c' && !notebookEditorFocused || editorLangId == 'objective-cpp' && !notebookEditorFocused || editorLangId == 'php' && !notebookEditorFocused || editorLangId == 'rust' && !notebookEditorFocused || editorLangId == 'scss' && !notebookEditorFocused || editorLangId == 'shaderlab' && !notebookEditorFocused || editorLangId == 'sql' && !notebookEditorFocused || editorLangId == 'swift' && !notebookEditorFocused || editorLangId == 'typescript' && !notebookEditorFocused || editorLangId == 'typescriptreact' && !notebookEditorFocused || editorLangId == 'antlr' && !notebookEditorFocused || editorLangId == 'fsharp' && !notebookEditorFocused || editorLangId == 'qasm-lang' && !notebookEditorFocused || editorLangId == 'matlab' && !notebookEditorFocused || editorLangId == 'bibtex' && !notebookEditorFocused || editorLangId == 'tex' && !notebookEditorFocused || editorLangId == 'latex' && !notebookEditorFocused`,
	`editorLangId == 'csharp' && !editorReadonly`,
	`editorLangId == 'csharp'`,
	`editorLangId == 'csv (caret)' && resourceScheme != csv-edit && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`editorLangId == 'csv (colon)' && resourceScheme != csv-edit && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`editorLangId == 'csv (dot)' && resourceScheme != csv-edit && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`editorLangId == 'csv (double quote)' && resourceScheme != csv-edit && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`editorLangId == 'csv (equals)' && resourceScheme != csv-edit && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`editorLangId == 'csv (hyphen)' && resourceScheme != csv-edit && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`editorLangId == 'csv (pipe)' && resourceScheme != csv-edit && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`editorLangId == 'csv (pipe)'`,
	`editorLangId == 'csv (semicolon)' && resourceScheme != csv-edit && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`editorLangId == 'csv (semicolon)'`,
	`editorLangId == 'csv (tilde)' && resourceScheme != csv-edit && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`editorLangId == 'csv (whitespace)' && resourceScheme != csv-edit && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`editorLangId == 'dbc' && dbcLangSyntax.clientActive`,
	`editorLangId == 'dm'`,
	`editorLangId == 'dolphindb'`,
	`editorLangId == 'erlang'`,
	`editorLangId == 'gnuplot'`,
	`editorLangId == 'hql'`,
	`editorLangId == 'latex' && !config.latex-workshop.bind.altKeymap.enabled`,
	`editorLangId == 'latex' && config.latex-workshop.bind.altKeymap.enabled`,
	`editorLangId == 'latex'`,
	`editorLangId == 'markdown'`,
	`editorLangId == 'mongo' && editorTextFocus`,
	`editorLangId == 'mongo'`,
	`editorLangId == 'nim'`,
	`editorLangId == 'org'`,
	`editorLangId == 'paket-dependencies'`,
	`editorLangId == 'paket-references'`,
	`editorLangId == 'postgres'`,
	`editorLangId == 'puppet' `,
	`editorLangId == 'puppet'`,
	`editorLangId == 'python'`,
	`editorLangId == 'qasm-lang'`,
	`editorLangId == 'rib'`,
	`editorLangId == 'rsl'`,
	`editorLangId == 'ruby' || editorLangId == 'gemfile'`,
	`editorLangId == 'ruby'`,
	`editorLangId == 'snowflake-sql'`,
	`editorLangId == 'solidity' && solidity.inHardhatProject`,
	`editorLangId == 'solidity'`,
	`editorLangId == 'sql'`,
	`editorLangId == 'states'`,
	`editorLangId == 'tonsolidity'`,
	`editorLangId == 'vso' && editorTextFocus`,
	`editorLangId == 'vue' && veturExtended.enableFindReferencesButton`,
	`editorLangId == 'xml'`,
	`editorLangId == 4gl`,
	`editorLangId == COBOL && !inSnippetMode && editorTextFocus && config.cobol-lsp.smart-tab`,
	`editorLangId == COBOL && !inSnippetMode`,
	`editorLangId == COBOL`,
	`editorLangId == CSV && resourceScheme != csv-edit && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`editorLangId == CSV && resourceScheme != sky-csv`,
	`editorLangId == DXL`,
	`editorLangId == Dart`,
	`editorLangId == TSV && resourceScheme != csv-edit && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`editorLangId == \u0027fsharp\u0027 \u0026\u0026 config.FSharp.enableTouchBar`,
	`editorLangId == \u0027fsharp\u0027 \u0026\u0026 resourceExtname == \u0027.fsx\u0027`,
	`editorLangId == \u0027fsharp\u0027`,
	`editorLangId == ada && editorTextFocus`,
	`editorLangId == ada`,
	`editorLangId == advpl`,
	`editorLangId == agda && !editorHasSelection && editorTextFocus`,
	`editorLangId == agda`,
	`editorLangId == ahk && config.ahk++.menu.showDebugButton`,
	`editorLangId == ahk && editorHasSelection`,
	`editorLangId == ahk2 && !isWeb && !editorHasSelection && resourceScheme == file`,
	`editorLangId == ahk2 && !isWeb && !editorHasSelection`,
	`editorLangId == ahk2 && !isWeb && !inDebugMode`,
	`editorLangId == ahk2 && !isWeb && ahk2:isRunning`,
	`editorLangId == ahk2 && !isWeb && editorHasSelection`,
	`editorLangId == ahk2 && !isWeb && resourceScheme == file`,
	`editorLangId == ahk2 && !isWeb`,
	`editorLangId == ahk2`,
	`editorLangId == ahk`,
	`editorLangId == al && alCodeActions.findRelated`,
	`editorLangId == al && editorTextFocus`,
	`editorLangId == al`,
	`editorLangId == ansi`,
	`editorLangId == apex`,
	`editorLangId == apiblueprint`,
	`editorLangId == argdown`,
	`editorLangId == arm-template`,
	`editorLangId == asaql`,
	`editorLangId == asciidoc && !isWeb`,
	`editorLangId == asciidoc || asciidocPreviewFocus`,
	`editorLangId == asciidoc`,
	`editorLangId == asl || editorLangId == asl-yaml`,
	`editorLangId == asm6502`,
	`editorLangId == asm6x09`,
	`editorLangId == asn1 || editorLangId == asn1-value-notation`,
	`editorLangId == asn1`,
	`editorLangId == aspnetcorerazor || resourceExtname == .cshtml`,
	`editorLangId == astro`,
	`editorLangId == atasm || resourceLangId == atasm || resourceLangId == json`,
	`editorLangId == autoit`,
	`editorLangId == autolisp-prime && editorHasSelection`,
	`editorLangId == autolisp-prime`,
	`editorLangId == autolispdcl-prime && editorHasSelection`,
	`editorLangId == axibasecharts`,
	`editorLangId == backlog`,
	`editorLangId == bat && !inSnippetMode && !suggestWidgetVisible`,
	`editorLangId == bat && !inSnippetMode`,
	`editorLangId == bat`,
	`editorLangId == bd`,
	`editorLangId == bf`,
	`editorLangId == bibtex`,
	`editorLangId == bicep`,
	`editorLangId == bigquery`,
	`editorLangId == blade`,
	`editorLangId == blitzmax && !blitzmax:debugging`,
	`editorLangId == blitzmax && blitzmax:debugging`,
	`editorLangId == blitzmax`,
	`editorLangId == bqsql`,
	`editorLangId == brighterscript && resourceScheme == file`,
	`editorLangId == brighterscript`,
	`editorLangId == c && inDebugMode && editorTextFocus`,
	`editorLangId == c || editorLangId == cpp`,
	`editorLangId == c4`,
	`editorLangId == c`,
	`editorLangId == cfml`,
	`editorLangId == choicescript && choicescript.projectLoaded && !choicescript.testRunning`,
	`editorLangId == choicescript && choicescript.projectLoaded && choicescript.previousRandomtestSettingsExist && !choicescript.testRunning`,
	`editorLangId == choicescript && choicescript.projectLoaded`,
	`editorLangId == choicescript && choicescript.testRunning`,
	`editorLangId == choicescript`,
	`editorLangId == chuck && editorFocus`,
	`editorLangId == clojure && calva:connected && calva:keybindingsEnabled`,
	`editorLangId == clojure && calva:connected`,
	`editorLangId == clojure && calva:keybindingsEnabled && paredit:keyMap =~ /original|strict/`,
	`editorLangId == clojure && calva:keybindingsEnabled`,
	`editorLangId == clojure && calva:showReplUi`,
	`editorLangId == clojure && paredit:keyMap =~ /original|strict/`,
	`editorLangId == clojure`,
	`editorLangId == cml`,
	`editorLangId == cobconfig`,
	`editorLangId == cobol && !editorReadonly && mfcobol.languageServer.running`,
	`editorLangId == cobol && !editorReadonly`,
	`editorLangId == cobol && !inSnippetMode && editorTextFocus && config.cobol-lsp.smart-tab`,
	`editorLangId == cobol && !inSnippetMode || editorLangId == hlasm && !inSnippetMode`,
	`editorLangId == cobol && !inSnippetMode`,
	`editorLangId == cobol && cobolLSReady &&  editorTextFocus && !editorReadonly`,
	`editorLangId == cobol && config.microFocusCOBOL.editor.improvedTabStops && !inSnippetMode && editorTextFocus && !editorReadonly && !editorTabMovesFocus && !suggestWidgetVisible`,
	`editorLangId == cobol && config.microFocusCOBOL.editor.improvedTabStops && !inSnippetMode && editorTextFocus && !editorReadonly && !editorTabMovesFocus`,
	`editorLangId == cobol && config.microFocusCOBOL.editor.improvedTabStops && editorTextFocus && !editorReadonly`,
	`editorLangId == cobol && editorTextFocus && !editorReadonly`,
	`editorLangId == cobol`,
	`editorLangId == codeblock && config.codeblock.languageServer.active`,
	`editorLangId == commonlisp && editorTextFocus && !editorReadOnly`,
	`editorLangId == commonlisp && editorTextFocus`,
	`editorLangId == composer`,
	`editorLangId == confluence`,
	`editorLangId == cpp && inDebugMode && editorTextFocus`,
	`editorLangId == cpp`,
	`editorLangId == cql`,
	`editorLangId == csharp && editorTextFocus`,
	`editorLangId == csharp && resourceScheme != untitled`,
	`editorLangId == csharp || editorLangId == vb`,
	`editorLangId == csharp`,
	`editorLangId == css && editorHasSelection && !editorReadonly`,
	`editorLangId == css && editorHasSelection`,
	`editorLangId == css && editorTextFocus`,
	`editorLangId == css || editorLangId == scss || editorLangId == html`,
	`editorLangId == css`,
	`editorLangId == csv && resourceScheme != csv-edit && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`editorLangId == csv && resourceScheme != sky-csv`,
	`editorLangId == csv`,
	`editorLangId == ctrl || editorLangId == ctrlpp`,
	`editorLangId == ctsscript && ctsscript.isManagedFile`,
	`editorLangId == ctsscriptreact && ctsscript.isManagedFile`,
	`editorLangId == ctsscriptreact`,
	`editorLangId == cut && !inSnippetMode`,
	`editorLangId == cwl`,
	`editorLangId == d2`,
	`editorLangId == daml`,
	`editorLangId == dart && !dart-code:isLsp`,
	`editorLangId == dart`,
	`editorLangId == data-weave`,
	`editorLangId == db2_luw && !editorHasSelection || editorLangId == db2_z && !editorHasSelection || editorLangId == db2_i && !editorHasSelection`,
	`editorLangId == db2_luw && editorHasSelection || editorLangId == db2_z && editorHasSelection || editorLangId == db2_i && editorHasSelection`,
	`editorLangId == db2_luw || editorLangId == db2_i || editorLangId == db2_z`,
	`editorLangId == dbml`,
	`editorLangId == dfa`,
	`editorLangId == dhall`,
	`editorLangId == diagram`,
	`editorLangId == dlt-log`,
	`editorLangId == dot`,
	`editorLangId == dsl || resourceExtname == .dashaapp`,
	`editorLangId == dsl`,
	`editorLangId == dts && !editorReadonly`,
	`editorLangId == dts`,
	`editorLangId == edifact`,
	`editorLangId == elixir || editorLangId == eex || editorLangId == html-eex`,
	`editorLangId == elixir`,
	`editorLangId == env || resourceFilename == .env`,
	`editorLangId == erdiagram`,
	`editorLangId == es`,
	`editorLangId == excel`,
	`editorLangId == feature`,
	`editorLangId == fountain`,
	`editorLangId == fsh`,
	`editorLangId == ftl-xml`,
	`editorLangId == gcode`,
	`editorLangId == gdl-hsf && isFileSystemResource && resourceExtname == .gdl && workbenchState != empty`,
	`editorLangId == gdl-xml || editorLangId == gdl-hsf`,
	`editorLangId == gdl-xml`,
	`editorLangId == gee`,
	`editorLangId == gift`,
	`editorLangId == git-commit && config.git.enabled && !git.missing && gitOpenRepositoryCount != 0`,
	`editorLangId == git-rebase`,
	`editorLangId == glowscript`,
	`editorLangId == glsl`,
	`editorLangId == go`,
	`editorLangId == groovy || editorLangId == jenkinsfile`,
	`editorLangId == groq || editorLangId == plaintext`,
	`editorLangId == haskell && editorTextFocus && config.ghcSimple.feature.inlineRepl`,
	`editorLangId == haskell`,
	`editorLangId == helm-template`,
	`editorLangId == hexdump`,
	`editorLangId == hl7`,
	`editorLangId == hlasm && editorTextFocus && !editorReadonly`,
	`editorLangId == hlasm`,
	`editorLangId == hsp3`,
	`editorLangId == html && !notebookEditorFocused`,
	`editorLangId == html && !virtualWorkspace`,
	`editorLangId == html && editorHasSelection`,
	`editorLangId == html && editorTextFocus`,
	`editorLangId == html && resourceExtname == .html`,
	`editorLangId == html || resourceExtname == .yrf && !notebookEditorFocused`,
	`editorLangId == html`,
	`editorLangId == ic10`,
	`editorLangId == icX`,
	`editorLangId == idris`,
	`editorLangId == in`,
	`editorLangId == inkling`,
	`editorLangId == inmanta`,
	`editorLangId == ion`,
	`editorLangId == ipython`,
	`editorLangId == isabelle-ml`,
	`editorLangId == isabelle`,
	`editorLangId == java && editorTextFocus`,
	`editorLangId == java && javaLSReady && editorHasImplementationProvider && editorTextFocus && !isInEmbeddedEditor`,
	`editorLangId == java && javaLSReady && editorTextFocus`,
	`editorLangId == java && resourceExtname == .java`,
	`editorLangId == java || editorLangId == xml`,
	`editorLangId == java`,
	`editorLangId == javascript && config.jsx-sfc.icon.splitEditors`,
	`editorLangId == javascript && editorHasSelection`,
	`editorLangId == javascript && editorTextFocus`,
	`editorLangId == javascript && qunitrunner.readyForUX && qunitrunner.chromeInstalled`,
	`editorLangId == javascript && qunitrunner.readyForUX && qunitrunner.edgeInstalled`,
	`editorLangId == javascript && qunitrunner.readyForUX && qunitrunner.firefoxInstalled`,
	`editorLangId == javascript && qunitrunner.readyForUX && qunitrunner.ieInstalled`,
	`editorLangId == javascript && resourceDirname =~ /background/`,
	`editorLangId == javascript && resourceFilename =~ background`,
	`editorLangId == javascript && resourceScheme != wenyan`,
	`editorLangId == javascript && testcaferunner.readyForUX && testcaferunner.canRerun`,
	`editorLangId == javascript && testcaferunner.readyForUX && testcaferunner.chrome-canaryInstalled`,
	`editorLangId == javascript && testcaferunner.readyForUX && testcaferunner.chromeInstalled`,
	`editorLangId == javascript && testcaferunner.readyForUX && testcaferunner.chromiumInstalled`,
	`editorLangId == javascript && testcaferunner.readyForUX && testcaferunner.edgeInstalled`,
	`editorLangId == javascript && testcaferunner.readyForUX && testcaferunner.firefoxInstalled`,
	`editorLangId == javascript && testcaferunner.readyForUX && testcaferunner.ieInstalled`,
	`editorLangId == javascript && testcaferunner.readyForUX && testcaferunner.operaInstalled`,
	`editorLangId == javascript && testcaferunner.readyForUX && testcaferunner.portableChromeInstalled`,
	`editorLangId == javascript && testcaferunner.readyForUX && testcaferunner.portableFirefoxInstalled`,
	`editorLangId == javascript && testcaferunner.readyForUX && testcaferunner.safariInstalled`,
	`editorLangId == javascript || editorLangId == css`,
	`editorLangId == javascript || editorLangId == javascriptreact || editorLangId == jsx || editorLangId == typescriptreact`,
	`editorLangId == javascript || editorLangId == javascriptreact || editorLangId == typescript || editorLangId == typescriptreact || editorLangId == jsx`,
	`editorLangId == javascript || editorLangId == json || editorLangId == jsonc`,
	`editorLangId == javascript || editorLangId == typescript || editorLangId == html || editorLangId == javascriptreact || editorLangId == typescriptreact || editorLangId == jsx || editorLangId == vue || editorLangId == vue-html || editorLangId == svelte`,
	`editorLangId == javascript || editorLangId == typescript`,
	`editorLangId == javascript`,
	`editorLangId == javascriptreact && config.jsx-sfc.icon.splitEditors`,
	`editorLangId == javascriptreact`,
	`editorLangId == jcl`,
	`editorLangId == jpl`,
	`editorLangId == json && editorHasSelection`,
	`editorLangId == json || editorLangId == arm-template`,
	`editorLangId == json || editorLangId == jsonc`,
	`editorLangId == json || editorLangId == yaml`,
	`editorLangId == json`,
	`editorLangId == jsonc`,
	`editorLangId == jsonl`,
	`editorLangId == jsonnet`,
	`editorLangId == jupyter`,
	`editorLangId == kagex`,
	`editorLangId == kickassembler`,
	`editorLangId == ksp`,
	`editorLangId == kusto`,
	`editorLangId == latex`,
	`editorLangId == lean && lean.input.isActive`,
	`editorLangId == lean || infoViewFocused`,
	`editorLangId == lean4 && lean4.input.isActive`,
	`editorLangId == lean4`,
	`editorLangId == lean`,
	`editorLangId == leibilang`,
	`editorLangId == liquid`,
	`editorLangId == lisp`,
	`editorLangId == lmps&&commandOnCursor`,
	`editorLangId == logcat`,
	`editorLangId == lua && luaIdeDebugState == true`,
	`editorLangId == lua || resourceExtname == .lua || resourceExtname == .luau`,
	`editorLangId == lua`,
	`editorLangId == magik`,
	`editorLangId == magit`,
	`editorLangId == makefile`,
	`editorLangId == markdown && !editorReadonly`,
	`editorLangId == markdown && !notebookEditorFocused && dendron:pluginActive`,
	`editorLangId == markdown && !notebookEditorFocused`,
	`editorLangId == markdown && MdTableEditor.changeAlignCenter && MdTableEditor.changeAlignCenter-context`,
	`editorLangId == markdown && MdTableEditor.changeAlignLeft && MdTableEditor.changeAlignLeft-context`,
	`editorLangId == markdown && MdTableEditor.changeAlignRight && MdTableEditor.changeAlignRight-context`,
	`editorLangId == markdown && MdTableEditor.columnSelect && MdTableEditor.columnSelect-context`,
	`editorLangId == markdown && MdTableEditor.columnSelectAll && MdTableEditor.columnSelectAll-context`,
	`editorLangId == markdown && MdTableEditor.columnSelectEmpty && MdTableEditor.columnSelectEmpty-context`,
	`editorLangId == markdown && MdTableEditor.insertBottom && MdTableEditor.insertBottom-context`,
	`editorLangId == markdown && MdTableEditor.insertLeft && MdTableEditor.insertLeft-context`,
	`editorLangId == markdown && MdTableEditor.insertRight && MdTableEditor.insertRight-context`,
	`editorLangId == markdown && MdTableEditor.insertTop && MdTableEditor.insertTop-context`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.beautifulFormat-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.changeAlignCenter-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.changeAlignLeft-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.changeAlignRight-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.columnSelect-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.columnSelectAll-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.columnSelectEmpty-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.fillCells-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.insertBottom-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.insertLeft-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.insertRight-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.insertTop-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.moveBottom-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.moveLeft-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.moveRight-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.moveTop-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.removeColumn-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.removeRow-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.sortNumberAsc-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.sortNumberDesc-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.sortTextAsc-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.sortTextAsc.ignore-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.sortTextDesc-toolbar`,
	`editorLangId == markdown && MdTableEditor.isIconMenuEnabled && MdTableEditor.sortTextDesc.ignore-toolbar`,
	`editorLangId == markdown && MdTableEditor.moveBottom && MdTableEditor.moveBottom-context`,
	`editorLangId == markdown && MdTableEditor.moveLeft && MdTableEditor.moveLeft-context`,
	`editorLangId == markdown && MdTableEditor.moveRight && MdTableEditor.moveRight-context`,
	`editorLangId == markdown && MdTableEditor.moveTop && MdTableEditor.moveTop-context`,
	`editorLangId == markdown && MdTableEditor.removeColumn && MdTableEditor.removeColumn-context`,
	`editorLangId == markdown && MdTableEditor.removeRow && MdTableEditor.removeRow-context`,
	`editorLangId == markdown && MdTableEditor.sortNumberAsc && MdTableEditor.sortNumberAsc-context`,
	`editorLangId == markdown && MdTableEditor.sortNumberDesc && MdTableEditor.sortNumberDesc-context`,
	`editorLangId == markdown && MdTableEditor.sortTextAsc && MdTableEditor.sortTextAsc-context`,
	`editorLangId == markdown && MdTableEditor.sortTextAsc.ignore && MdTableEditor.sortTextAsc.ignore-context`,
	`editorLangId == markdown && MdTableEditor.sortTextDesc && MdTableEditor.sortTextDesc-context`,
	`editorLangId == markdown && MdTableEditor.sortTextDesc.ignore && MdTableEditor.sortTextDesc.ignore-context`,
	`editorLangId == markdown && MdTableEditor.visibled && MdTableEditor.beautifulFormat-context`,
	`editorLangId == markdown && MdTableEditor.visibled && MdTableEditor.deleteComment-context`,
	`editorLangId == markdown && MdTableEditor.visibled && MdTableEditor.fillCells-context`,
	`editorLangId == markdown && MdTableEditor.visibled && MdTableEditor.naturalFormat-context`,
	`editorLangId == markdown && config.openInTypora.showContextMenuInEditor`,
	`editorLangId == markdown && config.typora-open.showIconInEditorTitleMenu`,
	`editorLangId == markdown && editorHasSelection`,
	`editorLangId == markdown && editorTextFocus && !editorReadonly && !editorTabMovesFocus && !suggestWidgetMultipleSuggestions && !suggestWidgetVisible && !inSnippetMode && markdowntable.contextkey.selection.InMarkdownTable`,
	`editorLangId == markdown && editorTextFocus && !editorReadonly && !editorTabMovesFocus && !suggestWidgetMultipleSuggestions && !suggestWidgetVisible && !inSnippetMode`,
	`editorLangId == markdown && markdowntable.contextkey.selection.InMarkdownTable`,
	`editorLangId == markdown && memo:refFocusedOrHovered`,
	`editorLangId == markdown && memo:refUnderCursorExists`,
	`editorLangId == markdown && resourceFilename == SUMMARY.md`,
	`editorLangId == markdown && workspaceFolderCount != 0`,
	`editorLangId == markdown || editorLangId == html`,
	`editorLangId == markdown || editorLangId == mdx || resourceExtname == .ipynb`,
	`editorLangId == markdown`,
	`editorLangId == markwhen`,
	`editorLangId == matlab`,
	`editorLangId == mcfunction`,
	`editorLangId == mdal`,
	`editorLangId == mdx && !editorHasSelection && markdowntable.contextkey.selection.InMarkdownTable`,
	`editorLangId == mdx && editorHasSelection`,
	`editorLangId == mdx && editorTextFocus && !editorReadonly && !editorTabMovesFocus && !suggestWidgetMultipleSuggestions && !suggestWidgetVisible && !inSnippetMode && markdowntable.contextkey.selection.InMarkdownTable`,
	`editorLangId == mdx && markdowntable.contextkey.selection.InMarkdownTable`,
	`editorLangId == mdx`,
	`editorLangId == mermaid || editorLangId == dot`,
	`editorLangId == mist && !mist:isDebugging`,
	`editorLangId == mist && mist:isDebugging`,
	`editorLangId == mist && resourceScheme != mist-compile`,
	`editorLangId == mist`,
	`editorLangId == mjml`,
	`editorLangId == mongodbRunner`,
	`editorLangId == mongodb`,
	`editorLangId == mumps && !suggestWidgetVisible`,
	`editorLangId == mumps && editorTextFocus`,
	`editorLangId == mumps`,
	`editorLangId == objective-c`,
	`editorLangId == objective-cpp`,
	`editorLangId == objectscript-class`,
	`editorLangId == objectscript`,
	`editorLangId == ocaml || editorLangId == ocaml.interface || editorLangId == reason || editorLangId == ocaml.ocamllex || editorLangId == ocaml.menhir`,
	`editorLangId == ocaml || editorLangId == ocaml.interface || editorLangId == reason || editorLangId == ocaml.ocamllex`,
	`editorLangId == ocaml || editorLangId == reason`,
	`editorLangId == ocaml.interface || editorLangId == reason`,
	`editorLangId == ocaml`,
	`editorLangId == oraclesql && editorHasDocumentFormattingProvider && !editorReadonly && !inCompositeEditor`,
	`editorLangId == oraclesql && editorTextFocus`,
	`editorLangId == oraclesql && resourceScheme != oracle || editorLangId == oraclesql && resourceScheme == oracle && resourceExtname == .sql`,
	`editorLangId == oraclesql && resourceScheme != oracle`,
	`editorLangId == oraclesql && resourceScheme == oracle && resourceDirname =~ /^\\\\[^\\\\]+\\\\(Procedure|Function)\\\\/`,
	`editorLangId == oraclesql`,
	`editorLangId == oscript && resourceFilename =~ /\\.(?:e|lxe)$/`,
	`editorLangId == papyrus`,
	`editorLangId == pdll`,
	`editorLangId == perl`,
	`editorLangId == pharo`,
	`editorLangId == php && php-implementor.showInContextMenu`,
	`editorLangId == php`,
	`editorLangId == pl1`,
	`editorLangId == plaintext`,
	`editorLangId == plantuml`,
	`editorLangId == postgres`,
	`editorLangId == powerquery`,
	`editorLangId == powershell && config.poshProTools.toolbar.quickScripts`,
	`editorLangId == powershell && config.powershell.buttons.showPanelMovementButtons`,
	`editorLangId == powershell && config.powershell.buttons.showRunButtons`,
	`editorLangId == powershell`,
	`editorLangId == pvs`,
	`editorLangId == python && !arepl && !editorHasSelection && config.AREPL.showIcons`,
	`editorLangId == python && !arepl && editorHasSelection && config.AREPL.showIcons`,
	`editorLangId == python && !config-view && !editorHasSelection && config.ConfigView.showIcons`,
	`editorLangId == python && !config-view && editorHasSelection && config.ConfigView.showIcons`,
	`editorLangId == python && !config.doctestbtn.buttonColor.fancy && !config.doctestbtn.buttonColor.xtraFancy`,
	`editorLangId == python && !inFoxContext`,
	`editorLangId == python && !inWolfContext`,
	`editorLangId == python && !livecode && !editorHasSelection && config.livecode.showIcons`,
	`editorLangId == python && !livecode && editorHasSelection && config.livecode.showIcons`,
	`editorLangId == python && !notebookEditorFocused && !virtualWorkspace && shellExecutionSupported`,
	`editorLangId == python && !notebookEditorFocused && isWorkspaceTrusted`,
	`editorLangId == python && config.doctestbtn.buttonColor.fancy && !config.doctestbtn.buttonColor.xtraFancy`,
	`editorLangId == python && config.doctestbtn.buttonColor.xtraFancy`,
	`editorLangId == python && config.pyright.development`,
	`editorLangId == python && jupyter.hascodecells && isWorkspaceTrusted`,
	`editorLangId == python && lego-spikeprime-mindstorms-vscode.isConnectedIn`,
	`editorLangId == python && pyright.development`,
	`editorLangId == python || editorLangId == java || editorLangId == c || editorLangId == cpp`,
	`editorLangId == python || editorLangId == java || editorLangId == typescript || editorLangId == c || editorLangId == cpp || editorLangId == vue`,
	`editorLangId == python || editorLangId == properties`,
	`editorLangId == python`,
	`editorLangId == pyz80`,
	`editorLangId == q`,
	`editorLangId == ql && resourceExtname == .ql`,
	`editorLangId == ql-summary && config.codeQL.canary`,
	`editorLangId == ql`,
	`editorLangId == qlik-expressions`,
	`editorLangId == qlik`,
	`editorLangId == quarto && !quarto.assistView.isPinned`,
	`editorLangId == quarto && quarto.assistView.isPinned`,
	`editorLangId == quarto || editorLangId == markdown || activeCustomEditorId == 'quarto.visualEditor'`,
	`editorLangId == quarto || editorLangId == mermaid || editorLangId == dot`,
	`editorLangId == quarto`,
	`editorLangId == query-editor`,
	`editorLangId == r`,
	`editorLangId == racket && config.racket-repl.showRunCommandInEditorContextMenu`,
	`editorLangId == racket && config.racket-repl.showStopCommandInEditorContextMenu`,
	`editorLangId == racket`,
	`editorLangId == raml`,
	`editorLangId == razor || resourceExtname == .razor`,
	`editorLangId == rc-script`,
	`editorLangId == reStructuredText`,
	`editorLangId == rego`,
	`editorLangId == rescript`,
	`editorLangId == restructuredtext`,
	`editorLangId == rethinkdb`,
	`editorLangId == rexx`,
	`editorLangId == rmd && editorFocus`,
	`editorLangId == robotframework && editorTextFocus && !editorHasSelection && !inSnippetMode && !suggestWidgetVisible && config.robotcode.editor.4SpacesTab && !inlineSuggestionVisible`,
	`editorLangId == rpgle`,
	`editorLangId == rst`,
	`editorLangId == ruby`,
	`editorLangId == rust && resourceScheme != rustexpand`,
	`editorLangId == rust`,
	`editorLangId == rx && !replaceInputFocussed`,
	`editorLangId == sas  && isSyncingDirectories && isWorkspaceOpened`,
	`editorLangId == sas  && isWorkspaceOpened`,
	`editorLangId == sas && !SAS.hideRunMenuItem`,
	`editorLangId == sas && !isSasjsCodeExecuting && isWorkspaceOpened`,
	`editorLangId == sas && !isSyncingDirectories && isWorkspaceOpened`,
	`editorLangId == sas && isSasjsCodeExecuting && isWorkspaceOpened`,
	`editorLangId == sas && isWorkspaceOpened`,
	`editorLangId == sas`,
	`editorLangId == sass || editorLangId == scss`,
	`editorLangId == scad && config.openscad.interface.showExportIconInEditorTitleMenu`,
	`editorLangId == scad && config.openscad.interface.showKillIconInEditorTitleMenu`,
	`editorLangId == scad && config.openscad.interface.showPreviewIconInEditorTitleMenu`,
	`editorLangId == scad || resourceLangId == scad`,
	`editorLangId == scad`,
	`editorLangId == scala || editorLangId == java || editorLangId == sbt || editorLangId == sc`,
	`editorLangId == scala`,
	`editorLangId == scilla`,
	`editorLangId == scl`,
	`editorLangId == scrypt`,
	`editorLangId == scss || editorLangId == sass`,
	`editorLangId == serenji.int && activeViewlet == 'workbench.view.debug'`,
	`editorLangId == shellscript`,
	`editorLangId == silq`,
	`editorLangId == sixtyfps`,
	`editorLangId == slint`,
	`editorLangId == smart-log`,
	`editorLangId == smarty`,
	`editorLangId == smithy`,
	`editorLangId == solidity`,
	`editorLangId == spl && config.ibm-streams.environment.enableOSStreamsBuilds == true`,
	`editorLangId == spl && config.ibm-streams.environment.enableV43Builds == true`,
	`editorLangId == spl`,
	`editorLangId == sqf`,
	`editorLangId == sql && resourceExtname != .spsql`,
	`editorLangId == sql && resourcePath in mssql.runningQueries`,
	`editorLangId == sql || editorLangId == cql`,
	`editorLangId == sql || resourceFilename =~ /.sql$/ `,
	`editorLangId == sql`,
	`editorLangId == squiggle`,
	`editorLangId == svelte && resourceScheme == file`,
	`editorLangId == svelte`,
	`editorLangId == svg && !config.svg.preview.disable`,
	`editorLangId == svg`,
	`editorLangId == swCB`,
	`editorLangId == texinfo`,
	`editorLangId == textile && !notebookEditorFocused`,
	`editorLangId == tib && editorHasSelection`,
	`editorLangId == tib`,
	`editorLangId == tjs`,
	`editorLangId == tlaplus || editorLangId == tlaplus_cfg`,
	`editorLangId == tlaplus`,
	`editorLangId == todo`,
	`editorLangId == toml`,
	`editorLangId == tosca`,
	`editorLangId == tsv && resourceScheme != csv-edit && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`editorLangId == tsv`,
	`editorLangId == twee3 || editorLangId == twee3-chapbook-1 || editorLangId == twee3-harlowe-3 || editorLangId == twee3-sugarcube-2`,
	`editorLangId == typescript && !virtualWorkspace`,
	`editorLangId == typescript && editorHasSelection`,
	`editorLangId == typescript && testcaferunner.readyForUX && testcaferunner.canRerun`,
	`editorLangId == typescript && testcaferunner.readyForUX && testcaferunner.chrome-canaryInstalled`,
	`editorLangId == typescript && testcaferunner.readyForUX && testcaferunner.chromeInstalled`,
	`editorLangId == typescript && testcaferunner.readyForUX && testcaferunner.chromiumInstalled`,
	`editorLangId == typescript && testcaferunner.readyForUX && testcaferunner.edgeInstalled`,
	`editorLangId == typescript && testcaferunner.readyForUX && testcaferunner.firefoxInstalled`,
	`editorLangId == typescript && testcaferunner.readyForUX && testcaferunner.ieInstalled`,
	`editorLangId == typescript && testcaferunner.readyForUX && testcaferunner.operaInstalled`,
	`editorLangId == typescript && testcaferunner.readyForUX && testcaferunner.portableChromeInstalled`,
	`editorLangId == typescript && testcaferunner.readyForUX && testcaferunner.portableFirefoxInstalled`,
	`editorLangId == typescript && testcaferunner.readyForUX && testcaferunner.safariInstalled`,
	`editorLangId == typescript && textInputFocus`,
	`editorLangId == typescript || editorLangId == javascript`,
	`editorLangId == typescript || editorLangId == typescriptreact`,
	`editorLangId == typescript`,
	`editorLangId == typescriptreact && config.jsx-sfc.icon.splitEditors`,
	`editorLangId == typescriptreact && textInputFocus`,
	`editorLangId == typescriptreact || editorLangId == javascriptreact || editorLangId == typescript || editorLangId == javascript`,
	`editorLangId == typescriptreact`,
	`editorLangId == tyrano`,
	`editorLangId == utterance && !googleHomeExtension.scriptPath && googleHomeExtension.scriptStatus == 'STOP'`,
	`editorLangId == utterance && resourcePath in googleHomeExtension.scriptPath && googleHomeExtension.scriptStatus != 'STOP'`,
	`editorLangId == utterance && resourcePath in googleHomeExtension.scriptPath && googleHomeExtension.scriptStatus == 'PAUSE'`,
	`editorLangId == utterance && resourcePath in googleHomeExtension.scriptPath && googleHomeExtension.scriptStatus == 'RUN'`,
	`editorLangId == v && config.v.vls.enable`,
	`editorLangId == v`,
	`editorLangId == vbs`,
	`editorLangId == verilog || editorLangId == systemverilog || editorLangId == vhdl`,
	`editorLangId == vhdl`,
	`editorLangId == visualforce && editorTextFocus`,
	`editorLangId == visualforce`,
	`editorLangId == viz || editorLangId == viz-con || editorLangId == viz4 || editorLangId == viz4-con`,
	`editorLangId == vt100`,
	`editorLangId == vue && config.volar.icon.preview && volar.foundNuxtDir`,
	`editorLangId == vue && config.volar.icon.preview && volar.foundViteDir`,
	`editorLangId == vue`,
	`editorLangId == wenyan && resourceScheme != wenyan`,
	`editorLangId == wikitext`,
	`editorLangId == winkyscript-bin`,
	`editorLangId == winkyscript`,
	`editorLangId == wms-dfd`,
	`editorLangId == wms-load`,
	`editorLangId == wms-mas`,
	`editorLangId == wms-men`,
	`editorLangId == wms-pls`,
	`editorLangId == wms-voice`,
	`editorLangId == wxml`,
	`editorLangId == xml && !notebookEditorFocused`,
	`editorLangId == xml && resourceExtname == .svg && svgPreviewIsOpen == false`,
	`editorLangId == xml && resourceExtname == .xlf`,
	`editorLangId == xml && vscode-objectscript.connectActive`,
	`editorLangId == xml || editorLangId == xsl`,
	`editorLangId == xml`,
	`editorLangId == xquery`,
	`editorLangId == yaml  || editorLangId == json`,
	`editorLangId == yaml && resource =~ /apis/(oas3|swagger2)/gm`,
	`editorLangId == yaml`,
	`editorLangId == yarnspinner`,
	`editorLangId == zenuml`,
	`editorLangId == zip`,
	`editorLangId =~ /(alloy-tss|javascript)/`,
	`editorLangId =~ /(alloy-tss|javascript|xml)/`,
	`editorLangId =~ /(c|cpp)/ && !cpp-runner.running`,
	`editorLangId =~ /(c|cpp)/ && cpp-runner.running`,
	`editorLangId =~ /(c|cpp)/`,
	`editorLangId =~ /(javascript|xml)/`,
	`editorLangId =~ /(json|jsonc)/ && config.sortJSON.contextMenu.sortJSONAlphaNumReverse`,
	`editorLangId =~ /(json|jsonc)/ && config.sortJSON.contextMenu.sortJSONAlphaNum`,
	`editorLangId =~ /(json|jsonc)/ && config.sortJSON.contextMenu.sortJSONKeyLengthReverse`,
	`editorLangId =~ /(json|jsonc)/ && config.sortJSON.contextMenu.sortJSONKeyLength`,
	`editorLangId =~ /(json|jsonc)/ && config.sortJSON.contextMenu.sortJSONReverse`,
	`editorLangId =~ /(json|jsonc)/ && config.sortJSON.contextMenu.sortJSONTypeReverse`,
	`editorLangId =~ /(json|jsonc)/ && config.sortJSON.contextMenu.sortJSONType`,
	`editorLangId =~ /(json|jsonc)/ && config.sortJSON.contextMenu.sortJSONValuesReverse`,
	`editorLangId =~ /(json|jsonc)/ && config.sortJSON.contextMenu.sortJSONValues`,
	`editorLangId =~ /(json|jsonc)/ && config.sortJSON.contextMenu.sortJSON`,
	`editorLangId =~ /(json|yaml)/ && isPreviewable`,
	`editorLangId =~ /(latex|bibtex)/`,
	`editorLangId =~ /(lua)/ && !inOutput`,
	`editorLangId =~ /(spl|makefile)/ && defaultInstanceEnv == 'v4_streamingAnalytics'`,
	`editorLangId =~ /(spl|makefile)/ && defaultInstanceEnv == 'v5_cpd' && consoleEnabled == true`,
	`editorLangId =~ /(spl|makefile)/ && defaultInstanceEnv == 'v5_cpd'`,
	`editorLangId =~ /(spl|makefile)/ && defaultInstanceEnv == 'v5_standalone'`,
	`editorLangId =~ /(sql|sqlite)/`,
	`editorLangId =~ /(xml|alloy-tss)/`,
	`editorLangId =~ /^(spec-data|csv-(row|column)|dppmca|chiplot)$/`,
	`editorLangId =~ /^(ssm-yaml|ssm-json)$/`,
	`editorLangId =~ /^COBOL$|^BITLANG-COBOL$|^ACUCOBOL$|^COBOLIT$/ && !inSnippetMode`,
	`editorLangId =~ /^cobol$/i && editorTextFocus && !editorReadonly`,
	`editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$/ && !config.latex-workshop.bind.altKeymap.enabled && !virtualWorkspace`,
	`editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$/ && !config.latex-workshop.bind.altKeymap.enabled`,
	`editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$/ && !virtualWorkspace`,
	`editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$/ && config.latex-workshop.bind.altKeymap.enabled && !virtualWorkspace`,
	`editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$/ && config.latex-workshop.bind.altKeymap.enabled`,
	`editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$|^pweave$/ && !config.latex-workshop.bind.altKeymap.enabled && !virtualWorkspace`,
	`editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$|^pweave$/ && !config.latex-workshop.bind.altKeymap.enabled`,
	`editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$|^pweave$/ && !virtualWorkspace`,
	`editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$|^pweave$/ && config.latex-workshop.bind.altKeymap.enabled && !virtualWorkspace`,
	`editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$|^pweave$/ && config.latex-workshop.bind.altKeymap.enabled`,
	`editorLangId =~ /^objective-c(pp)?$|^logos$/`,
	`editorLangId =~ /^objectscript/ && vscode-objectscript.connectActive`,
	`editorLangId =~ /^objectscript/`,
	`editorLangId =~ /^sql$|^jinja-sql$|^sql-bigquery$/ && !editorReadonly`,
	`editorLangId =~ /^winkyscript/`,
	`editorLangId =~ /^xml/`,
	`editorLangId =~ /commonlisp|clojure|scheme|lisp/ && paredit:keyMap == original && editorTextFocus && !editorReadOnly`,
	`editorLangId =~ /commonlisp|clojure|scheme|lisp/ && paredit:keyMap == strict && editorTextFocus && !editorReadOnly && !editorHasMultipleSelections`,
	`editorLangId =~ /commonlisp|clojure|scheme|lisp/ && paredit:keyMap == strict && editorTextFocus && !editorReadOnly`,
	`editorLangId =~ /commonlisp|clojure|scheme|lisp/ && paredit:keyMap =~ /original|strict/ && editorTextFocus && !editorReadOnly`,
	`editorLangId =~ /json/`,
	`editorLangId =~ /sml/`,
	`editorLangId =~ /sqlpp/ || (config.cblite.enableJSON && editorLangId =~ /json/)`,
	`editorLangId =~ /sqlpp/ || editorLangId =~ /json/`,
	`editorLangId in ac.documentSelector && ac.isApiFile`,
	`editorLangId in ccf.supportedLangID && !ccf.isCopybook`,
	`editorLangId in code-builder.build.languageSelector`,
	`editorLangId in httpyac.supportedLanguages`,
	`editorLangId in json-to-go:contextMenuLanguages || json-to-go:contextMenuAlways`,
	`editorLangId in julia.supportedLanguageIds`,
	`editorLangId in liveCode.supportedLanguageIds && !liveCode.isPreviewFocus`,
	`editorLangId in liveCode.supportedLanguageIds`,
	`editorLangId in testely.supportedLangIds && resourceFilename =~ /^.+(.test.(\\w+))$/g`,
	`editorLangId in testely.supportedLangIds && resourceFilename =~ /^.+(?<!.test.(\\w+))$/g`,
	`editorLangId in xml.supportedLanguageIds && XMLLSReady`,
	`editorLangId=='javascript'`,
	`editorLangId=='sonic-pi' && notebookType!='sonic-pi-book'`,
	`editorLangId=='sonic-pi' && notebookType=='sonic-pi-book'`,
	`editorLangId=='sonic-pi'`,
	`editorLangId=='typescript'`,
	`editorLangId==arm-template`,
	`editorLangId==fcs`,
	`editorLangId==gauge || editorLangId==markdown`,
	`editorLangId==go`,
	`editorLangId==javascript || editorLangId==javascriptreact || editorLangId==typescript || editorLangId==typescriptreact || editorLangId==vue`,
	`editorLangId==javascriptreact||editorLangId==javascript &&editorTextFocus && !editorTabMovesFocus && !inSnippetMode`,
	`editorLangId==json`,
	`editorLangId==mutablediff`,
	`editorLangId==python && editorHasSelection`,
	`editorLangId==python`,
	`editorLangId==restructuredtext && isWindows`,
	`editorLangId==restructuredtext`,
	`editorLangId==yang`,
	`editorLangId`,
	`editorReadonly && editorHasSelection && resourceScheme == mongodb-dly`,
	`editorReadonly && editorLangId == java`,
	`editorReadonly && vscode-kubewatch`,
	`editorTextFocus  && editorLangId==python`,
	`editorTextFocus  && resourceLangId == go`,
	`editorTextFocus && !config.cSpell.showSuggestionsLinkInEditorContextMenu && cSpell.editorMenuContext.showSuggestions`,
	`editorTextFocus && !config.emacs-mcx.useMetaPrefixMacCmd`,
	`editorTextFocus && !config.partialDiff.hideCommandsOnContextMenu && config.partialDiff.commandsOnContextMenu.diffSelectionWithClipboard`,
	`editorTextFocus && !config.partialDiff.hideCommandsOnContextMenu && config.partialDiff.commandsOnContextMenu.diffVisibleEditors`,
	`editorTextFocus && !config.partialDiff.hideCommandsOnContextMenu && config.partialDiff.commandsOnContextMenu.markSection1`,
	`editorTextFocus && !config.partialDiff.hideCommandsOnContextMenu && config.partialDiff.commandsOnContextMenu.markSection2AndTakeDiff`,
	`editorTextFocus && !config.partialDiff.hideCommandsOnContextMenu && config.partialDiff.commandsOnContextMenu.togglePreComparisonTextNormalizationRules`,
	`editorTextFocus && !editorHasDocumentFormattingProvider && !editorReadonly`,
	`editorTextFocus && !editorHasMultiSelection && !editorReadOnly`,
	`editorTextFocus && !editorHasMultipleSelections && editorHasSelection && editorLangId==restructuredtext`,
	`editorTextFocus && !editorHasMultipleSelections && editorLangId == python && !suggestWidgetVisible && !vim.active && !neovim.mode`,
	`editorTextFocus && !editorHasMultipleSelections && editorLangId == python && !suggestWidgetVisible && neovim.mode == insert`,
	`editorTextFocus && !editorHasMultipleSelections && editorLangId == python && !suggestWidgetVisible && vim.active == true && vim.mode =~ /(Insert|Replace|SurroundInputMode)/`,
	`editorTextFocus && !editorHasMultipleSelections && inStickySelectionMode`,
	`editorTextFocus && !editorHasSelection && !editorHasMultipleSelections`,
	`editorTextFocus && !editorHasSelection && !findWidgetVisible && !markerNavigationVisible && !parameterHintsVisible && !inSnippetMode && !isInEmbeddedEditor && !suggestWidgetVisible && !onTypeRenameInputVisible && !renameInputVisible && julia.hasInlineResult && editorLangId in julia.supportedLanguageIds`,
	`editorTextFocus && !editorHasSelection && !terminalFocus && !problemFocus && !keybindingFocus`,
	`editorTextFocus && !editorHasSelection && config.github.copilot.inlineSuggest.enable && !inlineSuggestionsVisible`,
	`editorTextFocus && !editorHasSelection && editorLangId==go`,
	`editorTextFocus && !editorHasSelection && editorLangId==python `,
	`editorTextFocus && !editorHasSelection && editorLangId==python`,
	`editorTextFocus && !editorHasSelection && jupyter.hascodecells && !notebookEditorFocused && !jupyter.havenativecells`,
	`editorTextFocus && !editorHasSelection && jupyter.hascodecells && !notebookEditorFocused`,
	`editorTextFocus && !editorHasSelection && vim.mode == 'Insert'`,
	`editorTextFocus && !editorHasSelection `,
	`editorTextFocus && !editorHasSelection`,
	`editorTextFocus && !editorReadOnly && !config.emacs-mcx.useMetaPrefixMacCmd`,
	`editorTextFocus && !editorReadOnly && !editorTabMovesFocus && !inSnippetMode && config.editor.tabCompletion!='on'`,
	`editorTextFocus && !editorReadOnly && !editorTabMovesFocus && !inSnippetMode && config.editor.tabCompletion=='on' && !hasOtherSuggestions`,
	`editorTextFocus && !editorReadOnly && !editorTabMovesFocus && !suggestWidgetVisible && !inSnippetMode && config.editor.tabCompletion!='on'`,
	`editorTextFocus && !editorReadOnly && !editorTabMovesFocus && !suggestWidgetVisible && !inSnippetMode && config.editor.tabCompletion=='on' && !hasOtherSuggestions`,
	`editorTextFocus && !editorReadOnly && !inSnippetMode && !editorHasSelection`,
	`editorTextFocus && !editorReadOnly && !suggestWidgetVisible`,
	`editorTextFocus && !editorReadOnly && config.emacs-mcx.useMetaPrefixCtrlLeftBracket`,
	`editorTextFocus && !editorReadOnly && config.emacs-mcx.useMetaPrefixEscape`,
	`editorTextFocus && !editorReadOnly && config.emacs-mcx.useMetaPrefixMacCmd`,
	`editorTextFocus && !editorReadOnly && editorHasSelection`,
	`editorTextFocus && !editorReadOnly && editorLangId == twee3-harlowe-2`,
	`editorTextFocus && !editorReadOnly && editorLangId == twee3-sugarcube-2`,
	`editorTextFocus && !editorReadOnly`,
	`editorTextFocus && !editorReadonly && !config.emacs-mcx.useMetaPrefixMacCmd`,
	`editorTextFocus && !editorReadonly && !editorHasMultipleSelections`,
	`editorTextFocus && !editorReadonly && !editorHasSelection`,
	`editorTextFocus && !editorReadonly && !editorTabMovesFocus`,
	`editorTextFocus && !editorReadonly && !inCompositeEditor`,
	`editorTextFocus && !editorReadonly && !suggestWidgetVisible && !parameterHintsVisible`,
	`editorTextFocus && !editorReadonly && !suggestWidgetVisible && editorLangId == 'css'`,
	`editorTextFocus && !editorReadonly && !suggestWidgetVisible && editorLangId == 'scss'`,
	`editorTextFocus && !editorReadonly && !suggestWidgetVisible`,
	`editorTextFocus && !editorReadonly && config.ShortcutMenuBar.commentLine`,
	`editorTextFocus && !editorReadonly && config.Toggle line comment`,
	`editorTextFocus && !editorReadonly && config.emacs-mcx.useMetaPrefixCtrlLeftBracket`,
	`editorTextFocus && !editorReadonly && config.emacs-mcx.useMetaPrefixEscape`,
	`editorTextFocus && !editorReadonly && config.emacs-mcx.useMetaPrefixMacCmd`,
	`editorTextFocus && !editorReadonly && editorHasSelection && editorLangId == 'latex'`,
	`editorTextFocus && !editorReadonly && editorHasSelection && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$/`,
	`editorTextFocus && !editorReadonly && editorHasSelection && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$|^pweave$/`,
	`editorTextFocus && !editorReadonly && editorHasSelection`,
	`editorTextFocus && !editorReadonly && editorLangId == 'css'`,
	`editorTextFocus && !editorReadonly && editorLangId == 'jsonc'`,
	`editorTextFocus && !editorReadonly && editorLangId == 'latex' && !config.latex-workshop.bind.altKeymap.enabled`,
	`editorTextFocus && !editorReadonly && editorLangId == 'latex' && !suggestWidgetVisible &&  !vim.active`,
	`editorTextFocus && !editorReadonly && editorLangId == 'latex' && !suggestWidgetVisible &&  vim.active && vim.mode == 'Insert'`,
	`editorTextFocus && !editorReadonly && editorLangId == 'latex' && !suggestWidgetVisible`,
	`editorTextFocus && !editorReadonly && editorLangId == 'latex' && config.latex-workshop.bind.altKeymap.enabled`,
	`editorTextFocus && !editorReadonly && editorLangId == 'latex'&& !config.latex-workshop.bind.altKeymap.enabled`,
	`editorTextFocus && !editorReadonly && editorLangId == 'latex'&& config.latex-workshop.bind.altKeymap.enabled`,
	`editorTextFocus && !editorReadonly && editorLangId == 'latex'`,
	`editorTextFocus && !editorReadonly && editorLangId == 'objectpascal'`,
	`editorTextFocus && !editorReadonly && editorLangId == 'python'`,
	`editorTextFocus && !editorReadonly && editorLangId == 'restructuredtext'`,
	`editorTextFocus && !editorReadonly && editorLangId == 'scss'`,
	`editorTextFocus && !editorReadonly && editorLangId == 'sql'`,
	`editorTextFocus && !editorReadonly && editorLangId == cfml`,
	`editorTextFocus && !editorReadonly && editorLangId == dbc`,
	`editorTextFocus && !editorReadonly && editorLangId == html`,
	`editorTextFocus && !editorReadonly && editorLangId == javascript || editorLangId == javascriptreact`,
	`editorTextFocus && !editorReadonly && editorLangId == markdown && !config.SFDocs.markdownAssistant.disableCustom`,
	`editorTextFocus && !editorReadonly && editorLangId == markdown && !isMac`,
	`editorTextFocus && !editorReadonly && editorLangId == markdown && !suggestWidgetVisible && !editorHasMultipleSelections && !editorTabMovesFocus && !inSnippetMode && !hasSnippetCompletions && !hasOtherSuggestions`,
	`editorTextFocus && !editorReadonly && editorLangId == markdown && !suggestWidgetVisible && !editorHasMultipleSelections && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine' && vim.mode != 'SearchInProgressMode' && vim.mode != 'CommandlineInProgress' && vim.mode != 'Replace' && vim.mode != 'EasyMotionMode' && vim.mode != 'EasyMotionInputMode' && vim.mode != 'SurroundInputMode'`,
	`editorTextFocus && !editorReadonly && editorLangId == markdown && !suggestWidgetVisible && !editorHasMultipleSelections`,
	`editorTextFocus && !editorReadonly && editorLangId == markdown && !suggestWidgetVisible && !inlineSuggestionVisible && !editorHasMultipleSelections && !editorTabMovesFocus && !inSnippetMode && !hasSnippetCompletions && !hasOtherSuggestions`,
	`editorTextFocus && !editorReadonly && editorLangId == markdown && !suggestWidgetVisible`,
	`editorTextFocus && !editorReadonly && editorLangId == markdown`,
	`editorTextFocus && !editorReadonly && editorLangId == quarto`,
	`editorTextFocus && !editorReadonly && editorLangId == racket`,
	`editorTextFocus && !editorReadonly && editorLangId == restructuredtext && !suggestWidgetVisible && !editorTabMovesFocus && !inSnippetMode && !hasSnippetCompletions && !hasOtherSuggestions && !inlineSuggestionVisible`,
	`editorTextFocus && !editorReadonly && editorLangId == restructuredtext && !suggestWidgetVisible && !editorTabMovesFocus && !inSnippetMode && !hasSnippetCompletions && !hasOtherSuggestions`,
	`editorTextFocus && !editorReadonly && editorLangId == restructuredtext && !suggestWidgetVisible && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine' && vim.mode != 'SearchInProgressMode' && vim.mode != 'CommandlineInProgress' && vim.mode != 'Replace' && vim.mode != 'EasyMotionMode' && vim.mode != 'EasyMotionInputMode' && vim.mode != 'SurroundInputMode'`,
	`editorTextFocus && !editorReadonly && editorLangId == restructuredtext && !suggestWidgetVisible`,
	`editorTextFocus && !editorReadonly && editorLangId == rmarkdown && !isMac`,
	`editorTextFocus && !editorReadonly && editorLangId == rmarkdown`,
	`editorTextFocus && !editorReadonly && editorLangId == ruby`,
	`editorTextFocus && !editorReadonly && editorLangId == scss`,
	`editorTextFocus && !editorReadonly && editorLangId == sigma && !suggestWidgetVisible && !editorHasMultipleSelections && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine' && vim.mode != 'SearchInProgressMode' && vim.mode != 'CommandlineInProgress' && vim.mode != 'Replace' && vim.mode != 'EasyMotionMode' && vim.mode != 'EasyMotionInputMode' && vim.mode != 'SurroundInputMode'`,
	`editorTextFocus && !editorReadonly && editorLangId == sigma && !suggestWidgetVisible && !editorHasMultipleSelections`,
	`editorTextFocus && !editorReadonly && editorLangId == svelte`,
	`editorTextFocus && !editorReadonly && editorLangId == typescript`,
	`editorTextFocus && !editorReadonly && editorLangId == vue`,
	`editorTextFocus && !editorReadonly && editorLangId == yaml`,
	`editorTextFocus && !editorReadonly && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$/`,
	`editorTextFocus && !editorReadonly && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$|^pweave$/`,
	`editorTextFocus && !editorReadonly && editorLangId =~ /^markdown$|^rmd$|^qmd$/ && !isMac`,
	`editorTextFocus && !editorReadonly && editorLangId =~ /^markdown$|^rmd$|^qmd$/ && !suggestWidgetVisible && !editorHasMultipleSelections && !editorTabMovesFocus && !inSnippetMode && !hasSnippetCompletions && !hasOtherSuggestions && markdown.extension.editor.cursor.inList && !markdown.extension.editor.cursor.inFencedCodeBlock && !markdown.extension.editor.cursor.inMathEnv`,
	`editorTextFocus && !editorReadonly && editorLangId =~ /^markdown$|^rmd$|^qmd$/ && !suggestWidgetVisible && !editorHasMultipleSelections && !markdown.extension.editor.cursor.inFencedCodeBlock && !markdown.extension.editor.cursor.inMathEnv`,
	`editorTextFocus && !editorReadonly && editorLangId =~ /^markdown$|^rmd$|^qmd$/ && !suggestWidgetVisible && !editorHasMultipleSelections && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine' && vim.mode != 'SearchInProgressMode' && vim.mode != 'CommandlineInProgress' && vim.mode != 'Replace' && vim.mode != 'EasyMotionMode' && vim.mode != 'EasyMotionInputMode' && vim.mode != 'SurroundInputMode' && !markdown.extension.editor.cursor.inFencedCodeBlock && !markdown.extension.editor.cursor.inMathEnv`,
	`editorTextFocus && !editorReadonly && editorLangId =~ /^markdown$|^rmd$|^qmd$/ && !suggestWidgetVisible && !inlineSuggestionVisible && !editorHasMultipleSelections && !editorTabMovesFocus && !inSnippetMode && !hasSnippetCompletions && !hasOtherSuggestions && markdown.extension.editor.cursor.inList && !markdown.extension.editor.cursor.inFencedCodeBlock && !markdown.extension.editor.cursor.inMathEnv`,
	`editorTextFocus && !editorReadonly && editorLangId =~ /^markdown$|^rmd$|^qmd$/ && !suggestWidgetVisible`,
	`editorTextFocus && !editorReadonly && editorLangId =~ /^markdown$|^rmd$|^qmd$/`,
	`editorTextFocus && !editorReadonly && editorLangId =~ /sql/ `,
	`editorTextFocus && !editorReadonly && supportedCodeAction =~ /(\\s|^)quickfix\\b/`,
	`editorTextFocus && !editorReadonly && supportedCodeAction =~ /(\\s|^)source\\.organizeImports\\b/`,
	`editorTextFocus && !editorReadonly && vim.mode == 'Insert'`,
	`editorTextFocus && !editorReadonly`,
	`editorTextFocus && !editorTabMovesFocus && !inSnippetMode && !hasSnippetCompletions && !editorTabMovesFocus && !editorReadonly`,
	`editorTextFocus && !extension.simpleVim.insertMode`,
	`editorTextFocus && !findWidgetVisible`,
	`editorTextFocus && !hasNextTabstop && !suggestWidgetVisible && editorLangId==c`,
	`editorTextFocus && !hasNextTabstop && !suggestWidgetVisible && editorLangId==cpp`,
	`editorTextFocus && !hasNextTabstop && !suggestWidgetVisible && editorLangId==cs`,
	`editorTextFocus && !hasNextTabstop && !suggestWidgetVisible && editorLangId==java`,
	`editorTextFocus && !hasNextTabstop && !suggestWidgetVisible && editorLangId==javascript`,
	`editorTextFocus && !hasNextTabstop && !suggestWidgetVisible && editorLangId==js`,
	`editorTextFocus && !hasNextTabstop && !suggestWidgetVisible && editorLangId==php`,
	`editorTextFocus && !hasNextTabstop && !suggestWidgetVisible && editorLangId==systemverilog`,
	`editorTextFocus && !hasNextTabstop && !suggestWidgetVisible && editorLangId==verilog`,
	`editorTextFocus && !inDebugMode && !replaceInputFocussed && editorLangId == 'python'`,
	`editorTextFocus && !inDebugRepl`,
	`editorTextFocus && !inOutput && seito-openfile:enabled`,
	`editorTextFocus && !inOutput && vscode-clearcase:enabled && resourcePath in vscode-clearcase:CheckedoutObjects`,
	`editorTextFocus && !inOutput && vscode-clearcase:enabled && resourcePath not in vscode-clearcase:CheckedoutObjects`,
	`editorTextFocus && !inOutput && vscode-clearcase:enabled`,
	`editorTextFocus && !inRegionMode && !vim.active`,
	`editorTextFocus && !inRegionMode && vim.active`,
	`editorTextFocus && !inRegionMode && vim.mode == 'Insert'`,
	`editorTextFocus && !inRegionMode`,
	`editorTextFocus && !inlineSuggestionVisible && !suggestWidgetVisible && !editorTabMovesFocus && !inSnippetMode && !hasSnippetCompletions && !editorTabMovesFocus && !editorReadonly`,
	`editorTextFocus && !isInEmbeddedEditor`,
	`editorTextFocus && !isMac && resourceExtname =~ /^(\\.js|\\.jsx|\\.cjs|\\.mjs|\\.ts|\\.tsx|\\.cts|\\.mts|\\.vue)$/`,
	`editorTextFocus && !isMac`,
	`editorTextFocus && !jumpy.isInJumpMode`,
	`editorTextFocus && !notebookEditable`,
	`editorTextFocus && !notebookEditorFocused && !python.datascience.hascodecells`,
	`editorTextFocus && !suggestWidgetMultipleSuggestions && !suggestWidgetVisible`,
	`editorTextFocus && !suggestWidgetVisible && !config.emacs-mcx.useMetaPrefixMacCmd`,
	`editorTextFocus && !suggestWidgetVisible && !inRegionMode && vim.mode == 'Insert'`,
	`editorTextFocus && !suggestWidgetVisible && !inSnippetMode && !editorHasMultipleSelections`,
	`editorTextFocus && !suggestWidgetVisible && !inlineSuggestionVisible && !inSnippetMode && !editorHasMultipleSelections`,
	`editorTextFocus && !suggestWidgetVisible && config.emacs-mcx.useMetaPrefixCtrlLeftBracket`,
	`editorTextFocus && !suggestWidgetVisible && config.emacs-mcx.useMetaPrefixEscape`,
	`editorTextFocus && !suggestWidgetVisible && config.emacs-mcx.useMetaPrefixMacCmd`,
	`editorTextFocus && !suggestWidgetVisible && editorLangId == lisp && !inSnippetMode`,
	`editorTextFocus && !suggestWidgetVisible && editorLangId == lisp && !vim.active`,
	`editorTextFocus && !suggestWidgetVisible && editorLangId == lisp && vim.mode == 'Insert'`,
	`editorTextFocus && !suggestWidgetVisible && editorLangId == racket && !inSnippetMode`,
	`editorTextFocus && !suggestWidgetVisible && editorLangId == racket && !vim.active`,
	`editorTextFocus && !suggestWidgetVisible && editorLangId == racket && vim.mode == 'Insert'`,
	`editorTextFocus && !suggestWidgetVisible && editorLangId == scheme && !inSnippetMode`,
	`editorTextFocus && !suggestWidgetVisible && editorLangId == scheme && !vim.active`,
	`editorTextFocus && !suggestWidgetVisible && editorLangId == scheme && vim.mode == 'Insert'`,
	`editorTextFocus && !suggestWidgetVisible && editorLangId==c`,
	`editorTextFocus && !suggestWidgetVisible && editorLangId==cpp`,
	`editorTextFocus && !suggestWidgetVisible && editorLangId==cs`,
	`editorTextFocus && !suggestWidgetVisible && editorLangId==java`,
	`editorTextFocus && !suggestWidgetVisible && editorLangId==javascript`,
	`editorTextFocus && !suggestWidgetVisible && editorLangId==js`,
	`editorTextFocus && !suggestWidgetVisible && editorLangId==php`,
	`editorTextFocus && !suggestWidgetVisible && editorLangId==systemverilog`,
	`editorTextFocus && !suggestWidgetVisible && editorLangId==verilog`,
	`editorTextFocus && !suggestWidgetVisible`,
	`editorTextFocus && !textCompareEditorActive`,
	`editorTextFocus && ( editorLangId == css || editorLangId == scss || editorLangId == html )`,
	`editorTextFocus && (!vim.active || vim.mode == 'Insert')`,
	`editorTextFocus && (editorLangId == 'typescript' || editorLangId == 'javascript' || editorLangId == 'typescriptreact' || editorLangId == 'javascriptreact')`,
	`editorTextFocus && CODEWHISPERER_SERVICE_ACTIVE`,
	`editorTextFocus && aceJumpInput`,
	`editorTextFocus && activeEditor != workbench.editor.notebook && editorLangId in julia.supportedLanguageIds`,
	`editorTextFocus && appworks:isJSXFile`,
	`editorTextFocus && auto-align-enabled`,
	`editorTextFocus && cSpell.editorMenuContext.addIgnoreWord`,
	`editorTextFocus && cSpell.editorMenuContext.addIssuesToDictionary`,
	`editorTextFocus && cSpell.editorMenuContext.addWordToCSpellConfig`,
	`editorTextFocus && cSpell.editorMenuContext.addWordToDictionary`,
	`editorTextFocus && cSpell.editorMenuContext.addWordToFolderDictionary`,
	`editorTextFocus && cSpell.editorMenuContext.addWordToFolderSettings`,
	`editorTextFocus && cSpell.editorMenuContext.addWordToUserDictionary`,
	`editorTextFocus && cSpell.editorMenuContext.addWordToUserSettings`,
	`editorTextFocus && cSpell.editorMenuContext.addWordToWorkspaceDictionary`,
	`editorTextFocus && cSpell.editorMenuContext.addWordToWorkspaceSettings`,
	`editorTextFocus && cSpell.editorMenuContext.createCSpellConfig`,
	`editorTextFocus && cSpell.editorMenuContext.createCustomDictionary`,
	`editorTextFocus && codestream:status == signedIn`,
	`editorTextFocus && codi-syntax:enabled`,
	`editorTextFocus && config.ShortcutMenuBar.switchHeaderSource`,
	`editorTextFocus && config.Switch header source`,
	`editorTextFocus && config.abl.editorContextMenuCommands.checkSyntax && resourceLangId == abl`,
	`editorTextFocus && config.abl.useProcedureEditorKeyBindings && resourceLangId == abl`,
	`editorTextFocus && config.bookmarks.showCommandsInContextMenu`,
	`editorTextFocus && config.cSpell.showCommandsInEditorContextMenu`,
	`editorTextFocus && config.cSpell.showSuggestionsLinkInEditorContextMenu && cSpell.editorMenuContext.showSuggestions`,
	`editorTextFocus && config.class-helper.show.titleIcon`,
	`editorTextFocus && config.emacs-mcx.useMetaPrefixCtrlLeftBracket`,
	`editorTextFocus && config.emacs-mcx.useMetaPrefixEscape`,
	`editorTextFocus && config.emacs-mcx.useMetaPrefixMacCmd`,
	`editorTextFocus && config.gitlens.menus.editor.compare`,
	`editorTextFocus && config.go.editorContextMenuCommands.addImport && resourceLangId == go`,
	`editorTextFocus && config.go.editorContextMenuCommands.addTags && resourceLangId == go`,
	`editorTextFocus && config.go.editorContextMenuCommands.benchmarkAtCursor && resourceLangId == go`,
	`editorTextFocus && config.go.editorContextMenuCommands.debugTestAtCursor && resourceLangId == go`,
	`editorTextFocus && config.go.editorContextMenuCommands.fillStruct && resourceLangId == go`,
	`editorTextFocus && config.go.editorContextMenuCommands.generateTestForFile && resourceLangId == go`,
	`editorTextFocus && config.go.editorContextMenuCommands.generateTestForFunction && resourceLangId == go`,
	`editorTextFocus && config.go.editorContextMenuCommands.generateTestForPackage && resourceLangId == go`,
	`editorTextFocus && config.go.editorContextMenuCommands.playground && resourceLangId == go`,
	`editorTextFocus && config.go.editorContextMenuCommands.removeTags && resourceLangId == go`,
	`editorTextFocus && config.go.editorContextMenuCommands.testAtCursor && resourceLangId == go`,
	`editorTextFocus && config.go.editorContextMenuCommands.testCoverage && resourceLangId == go`,
	`editorTextFocus && config.go.editorContextMenuCommands.testFile && resourceLangId == go`,
	`editorTextFocus && config.go.editorContextMenuCommands.testPackage && resourceLangId == go`,
	`editorTextFocus && config.go.editorContextMenuCommands.toggleTestFile && resourceLangId == go`,
	`editorTextFocus && config.motionbuilder.execute.enableShortcut`,
	`editorTextFocus && config.sidekick.showCommandsInContextMenu`,
	`editorTextFocus && config.text-utils.checkTaskList.enabled`,
	`editorTextFocus && config.textmarker.commandsOnContextMenu.clearAllHighlight`,
	`editorTextFocus && config.textmarker.commandsOnContextMenu.goToNextHighlight`,
	`editorTextFocus && config.textmarker.commandsOnContextMenu.goToPreviousHighlight`,
	`editorTextFocus && config.textmarker.commandsOnContextMenu.highlightUsingRegex`,
	`editorTextFocus && config.textmarker.commandsOnContextMenu.saveAllHighlights`,
	`editorTextFocus && config.textmarker.commandsOnContextMenu.toggleCaseSensitivity`,
	`editorTextFocus && config.textmarker.commandsOnContextMenu.toggleHighlight`,
	`editorTextFocus && config.textmarker.commandsOnContextMenu.toggleWholeMatch`,
	`editorTextFocus && config.textmarker.commandsOnContextMenu.unhighlight`,
	`editorTextFocus && config.textmarker.commandsOnContextMenu.updateHighlight`,
	`editorTextFocus && cursorToolsAnchors`,
	`editorTextFocus && dance.mode == 'input'`,
	`editorTextFocus && dance.mode == 'insert'`,
	`editorTextFocus && dance.mode == 'normal' && !dance.isRecording && !markersNavigationVisible`,
	`editorTextFocus && dance.mode == 'normal' && !dance.isRecording`,
	`editorTextFocus && dance.mode == 'normal' && dance.isRecording`,
	`editorTextFocus && dance.mode == 'normal'`,
	`editorTextFocus && editorHasMultipleSelections && !config.emacs-mcx.useMetaPrefixEscape`,
	`editorTextFocus && editorHasMultipleSelections`,
	`editorTextFocus && editorHasSelection  `,
	`editorTextFocus && editorHasSelection && !config.emacs-mcx.useMetaPrefixEscape`,
	`editorTextFocus && editorHasSelection && !editorReadonly && vim.mode == 'Insert'`,
	`editorTextFocus && editorHasSelection && !editorReadonly`,
	`editorTextFocus && editorHasSelection && config.gpg.showEditorContextMenus`,
	`editorTextFocus && editorHasSelection && editorLangId == 'sql'`,
	`editorTextFocus && editorHasSelection && editorLangId==python`,
	`editorTextFocus && editorHasSelection && resourceLangId == gimmick`,
	`editorTextFocus && editorHasSelection && resourceLangId == markdown`,
	`editorTextFocus && editorHasSelection `,
	`editorTextFocus && editorHasSelection`,
	`editorTextFocus && editorLangId != 'markdown'`,
	`editorTextFocus && editorLangId == 'COBOL'`,
	`editorTextFocus && editorLangId == 'abl'`,
	`editorTextFocus && editorLangId == 'alloy'`,
	`editorTextFocus && editorLangId == 'ampl'`,
	`editorTextFocus && editorLangId == 'archetype'`,
	`editorTextFocus && editorLangId == 'azcli'`,
	`editorTextFocus && editorLangId == 'bsl'`,
	`editorTextFocus && editorLangId == 'c'`,
	`editorTextFocus && editorLangId == 'calendar'`,
	`editorTextFocus && editorLangId == 'coffeescript' && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine'`,
	`editorTextFocus && editorLangId == 'coffeescript'`,
	`editorTextFocus && editorLangId == 'coq'`,
	`editorTextFocus && editorLangId == 'cpp'`,
	`editorTextFocus && editorLangId == 'crystal' && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine'`,
	`editorTextFocus && editorLangId == 'csharp'`,
	`editorTextFocus && editorLangId == 'd'`,
	`editorTextFocus && editorLangId == 'dart' && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine'`,
	`editorTextFocus && editorLangId == 'dart'`,
	`editorTextFocus && editorLangId == 'dolphindb'`,
	`editorTextFocus && editorLangId == 'ecr' && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine'`,
	`editorTextFocus && editorLangId == 'elixir' && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine'`,
	`editorTextFocus && editorLangId == 'elixir'`,
	`editorTextFocus && editorLangId == 'erb' && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine'`,
	`editorTextFocus && editorLangId == 'erb'`,
	`editorTextFocus && editorLangId == 'feature'`,
	`editorTextFocus && editorLangId == 'fitnesse'`,
	`editorTextFocus && editorLangId == 'fsharp'`,
	`editorTextFocus && editorLangId == 'fthtml'`,
	`editorTextFocus && editorLangId == 'go'`,
	`editorTextFocus && editorLangId == 'groovy' && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine'`,
	`editorTextFocus && editorLangId == 'haml' && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine'`,
	`editorTextFocus && editorLangId == 'http'`,
	`editorTextFocus && editorLangId == 'j'`,
	`editorTextFocus && editorLangId == 'javascript' && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine'`,
	`editorTextFocus && editorLangId == 'javascript'`,
	`editorTextFocus && editorLangId == 'javascriptreact' && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine'`,
	`editorTextFocus && editorLangId == 'jqpg'`,
	`editorTextFocus && editorLangId == 'json' || editorTextFocus && editorLangId == 'jsonc'`,
	`editorTextFocus && editorLangId == 'json'`,
	`editorTextFocus && editorLangId == 'kotlin' && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine'`,
	`editorTextFocus && editorLangId == 'latex' && !config.latex-workshop.bind.altKeymap.enabled && !virtualWorkspace`,
	`editorTextFocus && editorLangId == 'latex' && !config.latex-workshop.bind.altKeymap.enabled`,
	`editorTextFocus && editorLangId == 'latex' && config.latex-workshop.bind.altKeymap.enabled && !virtualWorkspace`,
	`editorTextFocus && editorLangId == 'latex' && config.latex-workshop.bind.altKeymap.enabled`,
	`editorTextFocus && editorLangId == 'latex'`,
	`editorTextFocus && editorLangId == 'livescript'`,
	`editorTextFocus && editorLangId == 'lua'`,
	`editorTextFocus && editorLangId == 'magit' && vim.mode =~ /^(?!SearchInProgressMode|CommandlineInProgress).*$/`,
	`editorTextFocus && editorLangId == 'magit' && vim.mode =~ /^(?!SearchInProgressMode|CommandlineInProgress|Visual).*$/`,
	`editorTextFocus && editorLangId == 'markdown' && !vim.active`,
	`editorTextFocus && editorLangId == 'markdown'`,
	`editorTextFocus && editorLangId == 'mel'`,
	`editorTextFocus && editorLangId == 'nextbasic'`,
	`editorTextFocus && editorLangId == 'nushell'`,
	`editorTextFocus && editorLangId == 'octave'`,
	`editorTextFocus && editorLangId == 'org'`,
	`editorTextFocus && editorLangId == 'plaintext'`,
	`editorTextFocus && editorLangId == 'powershell'`,
	`editorTextFocus && editorLangId == 'purescript'`,
	`editorTextFocus && editorLangId == 'python'`,
	`editorTextFocus && editorLangId == 'qasm-lang'`,
	`editorTextFocus && editorLangId == 'qrunes'`,
	`editorTextFocus && editorLangId == 'r'`,
	`editorTextFocus && editorLangId == 'rmd'`,
	`editorTextFocus && editorLangId == 'ruby' && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine'`,
	`editorTextFocus && editorLangId == 'scala' && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine'`,
	`editorTextFocus && editorLangId == 'slim' && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine'`,
	`editorTextFocus && editorLangId == 'snowflake-sql' && !notebookCellListFocused`,
	`editorTextFocus && editorLangId == 'snowflake-sql'`,
	`editorTextFocus && editorLangId == 'solidity'`,
	`editorTextFocus && editorLangId == 'sql'`,
	`editorTextFocus && editorLangId == 'squirrel'`,
	`editorTextFocus && editorLangId == 'stack'`,
	`editorTextFocus && editorLangId == 'strest'`,
	`editorTextFocus && editorLangId == 'tcl'`,
	`editorTextFocus && editorLangId == 'typescript' && !suggestWidgetVisible && !inSnippetMode`,
	`editorTextFocus && editorLangId == 'typescript' && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine'`,
	`editorTextFocus && editorLangId == 'typescript'`,
	`editorTextFocus && editorLangId == 'typescriptreact' && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine'`,
	`editorTextFocus && editorLangId == 'vue' && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine'`,
	`editorTextFocus && editorLangId == ampl`,
	`editorTextFocus && editorLangId == arm-template`,
	`editorTextFocus && editorLangId == asl || editorTextFocus && editorLangId == asl-yaml`,
	`editorTextFocus && editorLangId == asm`,
	`editorTextFocus && editorLangId == autoit`,
	`editorTextFocus && editorLangId == bazel`,
	`editorTextFocus && editorLangId == blitzmax`,
	`editorTextFocus && editorLangId == choicescript`,
	`editorTextFocus && editorLangId == composer`,
	`editorTextFocus && editorLangId == coq`,
	`editorTextFocus && editorLangId == csharp`,
	`editorTextFocus && editorLangId == css || editorTextFocus && editorLangId == scss || editorTextFocus && editorLangId == less`,
	`editorTextFocus && editorLangId == css`,
	`editorTextFocus && editorLangId == dafny && !dafny.counterexampleMenu.CanShowCounterExample`,
	`editorTextFocus && editorLangId == dafny && dafny.counterexampleMenu.CanShowCounterExample`,
	`editorTextFocus && editorLangId == dafny`,
	`editorTextFocus && editorLangId == dart`,
	`editorTextFocus && editorLangId == divinity-story-goal`,
	`editorTextFocus && editorLangId == dot`,
	`editorTextFocus && editorLangId == ecl`,
	`editorTextFocus && editorLangId == erb`,
	`editorTextFocus && editorLangId == extempore`,
	`editorTextFocus && editorLangId == fql`,
	`editorTextFocus && editorLangId == gdl-xml || editorTextFocus && editorLangId == gdl-hsf`,
	`editorTextFocus && editorLangId == git-commit`,
	`editorTextFocus && editorLangId == git-rebase`,
	`editorTextFocus && editorLangId == go`,
	`editorTextFocus && editorLangId == hexdump`,
	`editorTextFocus && editorLangId == html`,
	`editorTextFocus && editorLangId == http`,
	`editorTextFocus && editorLangId == imba`,
	`editorTextFocus && editorLangId == java`,
	`editorTextFocus && editorLangId == javascript && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine' && vim.mode != 'SearchInProgressMode' && vim.mode != 'CommandlineInProgress' && vim.mode != 'EasyMotionMode' && vim.mode != 'EasyMotionInputMode' && vim.mode != 'SurroundInputMode'`,
	`editorTextFocus && editorLangId == javascriptreact && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine' && vim.mode != 'SearchInProgressMode' && vim.mode != 'CommandlineInProgress' && vim.mode != 'EasyMotionMode' && vim.mode != 'EasyMotionInputMode' && vim.mode != 'SurroundInputMode'`,
	`editorTextFocus && editorLangId == jitdump`,
	`editorTextFocus && editorLangId == jsl`,
	`editorTextFocus && editorLangId == json`,
	`editorTextFocus && editorLangId == karate`,
	`editorTextFocus && editorLangId == kel`,
	`editorTextFocus && editorLangId == kusto && !findInputFocussed && !replaceInputFocussed && !notebookEditorFocused`,
	`editorTextFocus && editorLangId == lang-cfml`,
	`editorTextFocus && editorLangId == lean && lean.input.isActive`,
	`editorTextFocus && editorLangId == lean4 && lean4.input.isActive`,
	`editorTextFocus && editorLangId == lean4`,
	`editorTextFocus && editorLangId == lean`,
	`editorTextFocus && editorLangId == less`,
	`editorTextFocus && editorLangId == lua`,
	`editorTextFocus && editorLangId == magik`,
	`editorTextFocus && editorLangId == markdown && !suggestWidgetVisible && !vim.active && !inSnippetMode`,
	`editorTextFocus && editorLangId == markdown && !suggestWidgetVisible`,
	`editorTextFocus && editorLangId == markdown && editorHasSelection`,
	`editorTextFocus && editorLangId == markdown && memo:refFocusedOrHovered`,
	`editorTextFocus && editorLangId == markdown`,
	`editorTextFocus && editorLangId == nextbasic`,
	`editorTextFocus && editorLangId == objectpascal`,
	`editorTextFocus && editorLangId == ocaml || editorLangId == ocaml.interface `,
	`editorTextFocus && editorLangId == ocaml || editorTextFocus && editorLangId == ocaml.interface || editorTextFocus && editorLangId == reason || editorTextFocus && editorLangId == ocaml.ocamllex || editorTextFocus && editorLangId == ocaml.menhir`,
	`editorTextFocus && editorLangId == ocaml || editorTextFocus && editorLangId == ocaml.interface`,
	`editorTextFocus && editorLangId == opendss`,
	`editorTextFocus && editorLangId == oraclesql && resourceScheme != oracle`,
	`editorTextFocus && editorLangId == oraclesql && resourceScheme == oracle && resource =~ /(objectType%3D13|objectType%3D15)/`,
	`editorTextFocus && editorLangId == oraclesql && resourceScheme == oracle && resourceExtname == .plsql`,
	`editorTextFocus && editorLangId == oraclesql`,
	`editorTextFocus && editorLangId == p4`,
	`editorTextFocus && editorLangId == parser3ext`,
	`editorTextFocus && editorLangId == pgsql`,
	`editorTextFocus && editorLangId == php`,
	`editorTextFocus && editorLangId == python && !findInputFocussed && !replaceInputFocussed && !jupyter.ownsSelection && !notebookEditorFocused`,
	`editorTextFocus && editorLangId == python && !findInputFocussed && !replaceInputFocussed && jupyter.ownsSelection && !notebookEditorFocused`,
	`editorTextFocus && editorLangId == python && !findInputFocussed && !replaceInputFocussed`,
	`editorTextFocus && editorLangId == python`,
	`editorTextFocus && editorLangId == qrunes`,
	`editorTextFocus && editorLangId == quarto && !findInputFocussed && !replaceInputFocussed`,
	`editorTextFocus && editorLangId == quarto && editorHasSelection && !findInputFocussed && !replaceInputFocussed`,
	`editorTextFocus && editorLangId == racket`,
	`editorTextFocus && editorLangId == restructuredtext && !suggestWidgetVisible`,
	`editorTextFocus && editorLangId == restructuredtext`,
	`editorTextFocus && editorLangId == robot && !editorHasSelection && !inSnippetMode && !suggestWidgetVisible && config.rfLanguageServer.4spacesTab`,
	`editorTextFocus && editorLangId == robotframework && !editorHasSelection && !inSnippetMode && !suggestWidgetVisible && config.robot.editor.4spacesTab && !inlineSuggestionVisible`,
	`editorTextFocus && editorLangId == rust`,
	`editorTextFocus && editorLangId == scss`,
	`editorTextFocus && editorLangId == sql`,
	`editorTextFocus && editorLangId == todo`,
	`editorTextFocus && editorLangId == tree && !editorReadonly && !editorTabMovesFocus`,
	`editorTextFocus && editorLangId == tree && !editorReadonly`,
	`editorTextFocus && editorLangId == tree`,
	`editorTextFocus && editorLangId == typescript && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine' && vim.mode != 'SearchInProgressMode' && vim.mode != 'CommandlineInProgress' && vim.mode != 'EasyMotionMode' && vim.mode != 'EasyMotionInputMode' && vim.mode != 'SurroundInputMode'`,
	`editorTextFocus && editorLangId == typescript`,
	`editorTextFocus && editorLangId == typescriptreact && vim.mode != 'Normal' && vim.mode != 'Visual' && vim.mode != 'VisualBlock' && vim.mode != 'VisualLine' && vim.mode != 'SearchInProgressMode' && vim.mode != 'CommandlineInProgress' && vim.mode != 'EasyMotionMode' && vim.mode != 'EasyMotionInputMode' && vim.mode != 'SurroundInputMode'`,
	`editorTextFocus && editorLangId == usm`,
	`editorTextFocus && editorLangId == wxml`,
	`editorTextFocus && editorLangId == wxss`,
	`editorTextFocus && editorLangId == zenscript`,
	`editorTextFocus && editorLangId == zil`,
	`editorTextFocus && editorLangId =~ /(sqlpp|json)/`,
	`editorTextFocus && editorLangId =~ /(sql|sqlite)/`,
	`editorTextFocus && editorLangId =~ /^COBOL$|^BITLANG-COBOL$|^ACUCOBOL$|^COBOLIT$/ && !editorHasSelection`,
	`editorTextFocus && editorLangId =~ /^COBOL$|^BITLANG-COBOL$|^ACUCOBOL$|^COBOLIT$/ && editorHasSelection`,
	`editorTextFocus && editorLangId =~ /^COBOL$|^BITLANG-COBOL$|^ACUCOBOL$|^COBOLIT$/`,
	`editorTextFocus && editorLangId =~ /^css$|^qss$/`,
	`editorTextFocus && editorLangId =~ /^markdown$|^rmd$|^qmd$/ && !suggestWidgetVisible`,
	`editorTextFocus && editorLangId =~ /^markdown$|^rmd$|^qmd$/ && editorHasSelection`,
	`editorTextFocus && editorLangId =~ /^markdown$|^rmd$|^qmd$/`,
	`editorTextFocus && editorLangId =~ /^ruby$|^crystal$/ && suggestWidgetVisible && suggestionMakesTextEdit`,
	`editorTextFocus && editorLangId =~ /^ruby$|^crystal$/ && vim.mode != 'Normal' && vim.mode != 'SearchInProgressMode' && vim.mode != 'CommandlineInProgress' `,
	`editorTextFocus && editorLangId =~ /^ruby$|^crystal$/ && vim.mode != 'Normal' && vim.mode != 'SearchInProgressMode' && vim.mode != 'CommandlineInProgress^' `,
	`editorTextFocus && editorLangId =~ /^ruby/`,
	`editorTextFocus && editorLangId =~ /dynamic csv|^[ct]sv/`,
	`editorTextFocus && editorLangId =~ /erb/`,
	`editorTextFocus && editorLangId =~ /javascript|csharp|typescript|java|dart|cpp|c/)`,
	`editorTextFocus && editorLangId =~ /typescript|javascript|python|php|java|c|cpp|csharp|dart|ruby|go|rust/`,
	`editorTextFocus && editorLangId in httpyac.supportedLanguages`,
	`editorTextFocus && editorLangId in testely.supportedLangIds && resourceFilename =~ /^.+(?<!.test.(\\w+))$/g`,
	`editorTextFocus && editorLangId in wingman.docstring.supported-languages`,
	`editorTextFocus && editorLangId=='cython'`,
	`editorTextFocus && editorLangId==c`,
	`editorTextFocus && editorLangId==cpp`,
	`editorTextFocus && editorLangId==cs`,
	`editorTextFocus && editorLangId==fcs`,
	`editorTextFocus && editorLangId==java`,
	`editorTextFocus && editorLangId==javascript`,
	`editorTextFocus && editorLangId==js`,
	`editorTextFocus && editorLangId==php`,
	`editorTextFocus && editorLangId==python`,
	`editorTextFocus && editorLangId==restructuredtext`,
	`editorTextFocus && editorLangId==systemverilog`,
	`editorTextFocus && editorLangId==verilog`,
	`editorTextFocus && editorTextFocus && !editorReadonly`,
	`editorTextFocus && extension.simpleVim.visualMode`,
	`editorTextFocus && focusedView != 'workbench.panel.output'`,
	`editorTextFocus && foldingEnabled && editorLangId == imba`,
	`editorTextFocus && foldingEnabled`,
	`editorTextFocus && github.copilot.activated`,
	`editorTextFocus && gitlens:activeFileStatus =~ /blameable/ && config.gitlens.menus.editor.blame`,
	`editorTextFocus && gitlens:activeFileStatus =~ /blameable/ && config.gitlens.menus.editor.clipboard`,
	`editorTextFocus && gitlens:activeFileStatus =~ /blameable/`,
	`editorTextFocus && gitlens:activeFileStatus =~ /remotes/ && config.gitlens.menus.editor.remote`,
	`editorTextFocus && gltfFileActive`,
	`editorTextFocus && google-pinyin.enabled && !google-pinyin.seleting`,
	`editorTextFocus && hasNextTabstop && inSnippetMode && !suggestWidgetVisible`,
	`editorTextFocus && hasNextTabstop && inSnippetMode`,
	`editorTextFocus && hasPrevTabstop && inSnippetMode && !suggestWidgetVisible`,
	`editorTextFocus && hasPrevTabstop && inSnippetMode`,
	`editorTextFocus && hasSnippetCompletions && !editorTabMovesFocus && !inSnippetMode`,
	`editorTextFocus && hasWordHighlights`,
	`editorTextFocus && inDebugMode && !replaceInputFocussed && editorLangId == 'python'`,
	`editorTextFocus && inDebugMode`,
	`editorTextFocus && inDebugRepl && onFirsteDebugReplLine`,
	`editorTextFocus && inDebugRepl && onLastDebugReplLine`,
	`editorTextFocus && inRegionMode && vim.mode == 'Insert'`,
	`editorTextFocus && inRegionMode`,
	`editorTextFocus && inSnippetMode && !suggestWidgetVisible`,
	`editorTextFocus && inSnippetMode`,
	`editorTextFocus && inStickySelectionMode && isMac`,
	`editorTextFocus && inStickySelectionMode && isWindows`,
	`editorTextFocus && inStickySelectionMode`,
	`editorTextFocus && incrementalSearchInlineInput`,
	`editorTextFocus && inputFocus && notebookEditorFocused || notebookCellListFocused && notebookCellType == 'code'`,
	`editorTextFocus && inputFocus && notebookEditorFocused || notebookCellListFocused`,
	`editorTextFocus && inputFocus && notebookEditorFocused`,
	`editorTextFocus && is-running-storybooks-vscode`,
	`editorTextFocus && isMac && resourceExtname =~ /^(\\.js|\\.jsx|\\.cjs|\\.mjs|\\.ts|\\.tsx|\\.cts|\\.mts|\\.vue)$/`,
	`editorTextFocus && isMac`,
	`editorTextFocus && java:serverMode == Standard`,
	`editorTextFocus && java:testRunnerActivated`,
	`editorTextFocus && jump.isInJumpMode`,
	`editorTextFocus && jumpy.isInJumpMode`,
	`editorTextFocus && jupyter.hascodecells && !notebookEditorFocused`,
	`editorTextFocus && markdownShortcuts:enabled`,
	`editorTextFocus && metaGoInput`,
	`editorTextFocus && metaGoJumping`,
	`editorTextFocus && modaledit.searching`,
	`editorTextFocus && neovim.init && neovim.mode != insert && neovim.ctrlKeysNormal`,
	`editorTextFocus && neovim.init && neovim.mode != insert || neovim.recording`,
	`editorTextFocus && neovim.init && neovim.mode != normal && neovim.ctrlKeysInsert`,
	`editorTextFocus && neovim.init && neovim.mode != normal`,
	`editorTextFocus && neovim.init && neovim.mode == normal && !markersNavigationVisible && !parameterHintsVisible && !inReferenceSearchEditor && !referenceSearchVisible && !dirtyDiffVisible && !notebookCellFocused && !findWidgetVisible && !notificationCenterVisible`,
	`editorTextFocus && neovim.init && neovim.mode == normal && neovim.ctrlKeysNormal && !markersNavigationVisible && !parameterHintsVisible && !inReferenceSearchEditor && !referenceSearchVisible && !dirtyDiffVisible && !notebookCellFocused && !findWidgetVisible && !notificationCenterVisible`,
	`editorTextFocus && neovim.init`,
	`editorTextFocus && neovim.mode == insert && neovim.ctrlKeysInsert`,
	`editorTextFocus && neovim.mode == visual`,
	`editorTextFocus && parameterHintsMultipleSignatures && parameterHintsVisible`,
	`editorTextFocus && parameterHintsVisible`,
	`editorTextFocus && resourceDirname =~ /fauna-vscode-tmp/`,
	`editorTextFocus && resourceExtname != .md`,
	`editorTextFocus && resourceExtname == .fsql`,
	`editorTextFocus && resourceExtname == .fxs`,
	`editorTextFocus && resourceExtname == .groovy`,
	`editorTextFocus && resourceExtname == .impex`,
	`editorTextFocus && resourceExtname == .js`,
	`editorTextFocus && resourceExtname == .jsx`,
	`editorTextFocus && resourceExtname == .php && resourceFilename =~ /(web|api)\\.php/`,
	`editorTextFocus && resourceExtname == .php && resourceFilename =~ /\\.(blade)\\.php/`,
	`editorTextFocus && resourceExtname == .php && resourceFilename =~ /\\.php/`,
	`editorTextFocus && resourceExtname == .php`,
	`editorTextFocus && resourceExtname == .puml`,
	`editorTextFocus && resourceExtname == .sol`,
	`editorTextFocus && resourceExtname == .sql`,
	`editorTextFocus && resourceExtname == .tcl`,
	`editorTextFocus && resourceExtname == .ts`,
	`editorTextFocus && resourceExtname == .vue`,
	`editorTextFocus && resourceExtname == .yaml && resourceFilename =~ /pubspec.yaml/`,
	`editorTextFocus && resourceExtname== .yml`,
	`editorTextFocus && resourceFilename != 'package.json'`,
	`editorTextFocus && resourceFilename == 'package.json'`,
	`editorTextFocus && resourceFilename =~ /.go/`,
	`editorTextFocus && resourceFilename =~ /Untitled-/`,
	`editorTextFocus && resourceLangId == 'es'`,
	`editorTextFocus && resourceLangId == 'sheerpower-basic'`,
	`editorTextFocus && resourceLangId == abl`,
	`editorTextFocus && resourceLangId == blk`,
	`editorTextFocus && resourceLangId == bpmn`,
	`editorTextFocus && resourceLangId == go`,
	`editorTextFocus && resourceLangId == javascript || resourceLangId ==javascriptreact || resourceLangId ==typescriptreact || resourceLangId == typescript`,
	`editorTextFocus && resourceLangId == javascript`,
	`editorTextFocus && resourceLangId == javascriptreact`,
	`editorTextFocus && resourceLangId == markdown || resourceLangId == yaml`,
	`editorTextFocus && resourceLangId == markdown`,
	`editorTextFocus && resourceLangId == php`,
	`editorTextFocus && resourceLangId == po`,
	`editorTextFocus && resourceLangId == python`,
	`editorTextFocus && resourceLangId == reason`,
	`editorTextFocus && resourceLangId == scala`,
	`editorTextFocus && resourceLangId == sourcepawn && parameterHintsVisible`,
	`editorTextFocus && resourceLangId == txt`,
	`editorTextFocus && resourceLangId == typescript`,
	`editorTextFocus && resourceLangId == typescriptreact`,
	`editorTextFocus && resourceLangId==es`,
	`editorTextFocus && resourceScheme == adt && debugState != 'inactive'`,
	`editorTextFocus && resourceScheme == adt && editorLangId == abap `,
	`editorTextFocus && resourceScheme == adt`,
	`editorTextFocus && resourceScheme == file && resourceExtname == .jcl`,
	`editorTextFocus && resourceScheme == file && resourceExtname == .rexx`,
	`editorTextFocus && resourceScheme == oracle && resource =~ /(objectType%3D13|objectType%3D15)/`,
	`editorTextFocus && scroll.isStart && !suggestWidgetMultipleSuggestions && !suggestWidgetVisible`,
	`editorTextFocus && selectionAnchorSet`,
	`editorTextFocus && suggestWidgetMultipleSuggestions && suggestWidgetVisible`,
	`editorTextFocus && suggestWidgetVisible && !config.emacs-mcx.useMetaPrefixMacCmd`,
	`editorTextFocus && suggestWidgetVisible && config.emacs-mcx.useMetaPrefixCtrlLeftBracket`,
	`editorTextFocus && suggestWidgetVisible && config.emacs-mcx.useMetaPrefixEscape`,
	`editorTextFocus && suggestWidgetVisible && config.emacs-mcx.useMetaPrefixMacCmd`,
	`editorTextFocus && suggestWidgetVisible && editorLangId==c`,
	`editorTextFocus && suggestWidgetVisible && editorLangId==cpp`,
	`editorTextFocus && suggestWidgetVisible && editorLangId==cs`,
	`editorTextFocus && suggestWidgetVisible && editorLangId==java`,
	`editorTextFocus && suggestWidgetVisible && editorLangId==javascript`,
	`editorTextFocus && suggestWidgetVisible && editorLangId==js`,
	`editorTextFocus && suggestWidgetVisible && editorLangId==php`,
	`editorTextFocus && suggestWidgetVisible && editorLangId==systemverilog`,
	`editorTextFocus && suggestWidgetVisible && editorLangId==verilog`,
	`editorTextFocus && suggestWidgetVisible`,
	`editorTextFocus && todomd:isActive`,
	`editorTextFocus && vToolFileActive`,
	`editorTextFocus && vim.active && !inDebugRepl && !suggestWidgetVisible && !parameterHintsVisible`,
	`editorTextFocus && vim.active && !inDebugRepl && !suggestWidgetVisible && !suggestWidgetMultipleSuggestions`,
	`editorTextFocus && vim.active && !inDebugRepl && vim.mode != 'Insert'`,
	`editorTextFocus && vim.active && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.mode != 'Insert' && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.mode != 'Insert'`,
	`editorTextFocus && vim.active && vim.mode == 'Replace'`,
	`editorTextFocus && vim.active && vim.use<C-6> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-BS> && vim.mode != 'Insert' && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-[> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-]> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-^> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-a> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-b> && vim.mode != 'Insert' && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-c> && !inDebugRepl && vim.overrideCtrlC`,
	`editorTextFocus && vim.active && vim.use<C-d> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-e> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-f> && !inDebugRepl && vim.mode != 'Insert'`,
	`editorTextFocus && vim.active && vim.use<C-f> && vim.mode != 'Insert' && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-g> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-h> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-i> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-j> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-k> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-l> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-m> && !inDebugRepl || vim.mode == 'CommandlineInProgress' && vim.active && vim.use<C-m> && !inDebugRepl || vim.mode == 'SearchInProgressMode' && vim.active && vim.use<C-m> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-n> && !inDebugRepl && vim.mode != 'Insert'`,
	`editorTextFocus && vim.active && vim.use<C-n> && !inDebugRepl || vim.mode == 'CommandlineInProgress' && vim.active && vim.use<C-n> && !inDebugRepl || vim.mode == 'SearchInProgressMode' && vim.active && vim.use<C-n> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-n> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-o> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-pagedown> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-pageup> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-q> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-r> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-s> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-shift+2>`,
	`editorTextFocus && vim.active && vim.use<C-space> && !inDebugRepl && vim.mode != 'Insert'`,
	`editorTextFocus && vim.active && vim.use<C-t> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-u> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-v> && !inDebugRepl && vim.mode != 'Insert'`,
	`editorTextFocus && vim.active && vim.use<C-v> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-w> && !inDebugRepl && vim.mode != 'Insert'`,
	`editorTextFocus && vim.active && vim.use<C-w> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-x> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-y> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<C-z> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<D-a> && !inDebugRepl && vim.mode != 'Insert'`,
	`editorTextFocus && vim.active && vim.use<D-c> && vim.overrideCopy && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<D-d> && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<D-left> && !inDebugRepl && vim.mode != 'Insert'`,
	`editorTextFocus && vim.active && vim.use<D-right> &&  !inDebugRepl && vim.mode != 'Insert'`,
	`editorTextFocus && vim.active && vim.use<D-right> && !inDebugRepl && vim.mode != 'Insert'`,
	`editorTextFocus && vim.active && vim.use<D-v> && vim.mode == 'CommandlineInProgress' && !inDebugRepl || editorTextFocus && vim.active && vim.use<D-v> && vim.mode == 'SearchInProgressMode' && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<D-v> && vim.overrideCmdV && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<S-BS> && vim.mode != 'Insert' && !inDebugRepl`,
	`editorTextFocus && vim.active && vim.use<shift+BS> && vim.mode == 'SearchInProgressMode' && !inDebugRepl`,
	`editorTextFocus && vim.enabled`,
	`editorTextFocus && vim.mode == 'Insert'`,
	`editorTextFocus && wingman.bug-fixing.bugged`,
	`editorTextFocus && workspaceContains:.openpublishing.publish.config.json && !vim.active`,
	`editorTextFocus &&resourceLangId == javascript || resourceLangId ==javascriptreact || resourceLangId ==typescriptreact || resourceLangId == typescript`,
	`editorTextFocus == true && editorLangId == javascript && referenceContext == true`,
	`editorTextFocus == true && editorReadonly == false`,
	`editorTextFocus \u0026\u0026 editorLangId == \u0027fsharp\u0027`,
	`editorTextFocus `,
	`editorTextFocus editorLangId == typescript`,
	`editorTextFocus || editorHasSelection || editorHasMultipleSelections`,
	`editorTextFocus || editorHasSelection || editorReadonly || isInDiffEditor || isInEmbeddedEditor`,
	`editorTextFocus || editorHasSelection`,
	`editorTextFocus || editorLangId == javascript || editorLangId == typescript`,
	`editorTextFocus || editorLangId == rc-script || resourceLangId == rc-script`,
	`editorTextFocus || filesExplorerFocus`,
	`editorTextFocus || findWidgetVisible`,
	`editorTextFocus&& editorHasSelection `,
	`editorTextFocus&&editorHasSelection`,
	`editorTextFocus`,
	`elan.state == uninitialized`,
	`electron-build-tools:active`,
	`electron-build-tools:build-tools-installed`,
	`electron-build-tools:ready && !electron-build-tools:build-tools-installed`,
	`electron-build-tools:ready && electron-build-tools:build-tools-installed && !electron-build-tools:is-electron-workspace `,
	`electron-build-tools:ready && resourceDirname =~ /.*electron(?:\\\\|\\/)patches(?:\\\\|\\/).*$/ && resourceExtname == .patch`,
	`electron-build-tools:ready && resourceLangId == markdown`,
	`emacs-mcx.acceptingArgument && editorTextFocus && config.emacs-mcx.enableDigitArgument && !config.emacs-mcx.useMetaPrefixMacCmd`,
	`emacs-mcx.acceptingArgument && editorTextFocus && config.emacs-mcx.enableDigitArgument && config.emacs-mcx.useMetaPrefixCtrlLeftBracket`,
	`emacs-mcx.acceptingArgument && editorTextFocus && config.emacs-mcx.enableDigitArgument && config.emacs-mcx.useMetaPrefixEscape`,
	`emacs-mcx.acceptingArgument && editorTextFocus && config.emacs-mcx.enableDigitArgument && config.emacs-mcx.useMetaPrefixMacCmd`,
	`emacs-mcx.acceptingArgument && editorTextFocus`,
	`emacs-mcx.acceptingRectCommand && editorTextFocus && !config.emacs-mcx.useMetaPrefixMacCmd`,
	`emacs-mcx.acceptingRectCommand && editorTextFocus && config.emacs-mcx.useMetaPrefixCtrlLeftBracket`,
	`emacs-mcx.acceptingRectCommand && editorTextFocus && config.emacs-mcx.useMetaPrefixEscape`,
	`emacs-mcx.acceptingRectCommand && editorTextFocus && config.emacs-mcx.useMetaPrefixMacCmd`,
	`emacs-mcx.acceptingRectCommand && editorTextFocus`,
	`emacs-mcx.inRectMarkMode && editorTextFocus && !editorReadonly`,
	`emacs-mcx.minibufferReading`,
	`emacs-mcx.prefixArgumentExists && editorTextFocus && !editorReadonly`,
	`emberFileUsagesEnabled`,
	`emptyWorkspaceSupport && workbenchState != 'empty'`,
	`emptyWorkspaceSupport`,
	`enableBuild`,
	`enableCommands`,
	`enableCppSshTargetsView`,
	`enableExplorerExtension && showGoOutlinerMainView`,
	`enableExtras`,
	`enableGit && !enableExtras `,
	`enableGit && !enableExtras`,
	`enableGit`,
	`enableLunaticPreviewWithClairvoyant`,
	`enableTools`,
	`enabledCursorsGroup`,
	`enabledEditorGroup`,
	`enabledFuncGroup`,
	`enabledGroup`,
	`enabledSrcGroup`,
	`erdiagram-diagram-focused`,
	`ev3devBrowser.context.connected`,
	`exceptionWidgetVisible`,
	`exercism:isValidWorkspace`,
	`exercism:showButtons`,
	`existsActiveTextDocument`,
	`explorer-exclude.hasLoaded && !explorer-exclude.missingWorkspace && activeViewlet == 'workbench.view.explorer'`,
	`explorer-exclude.hasLoaded && !explorer-exclude.missingWorkspace && view == explorerExclude.pane.items && !explorer-exclude.enabled`,
	`explorer-exclude.hasLoaded && !explorer-exclude.missingWorkspace && view == explorerExclude.pane.items && explorer-exclude.enabled`,
	`explorer-exclude.hasLoaded && !explorer-exclude.missingWorkspace && view == explorerExclude.pane.items && viewItem && viewItem != '**/.git' && viewItem != '**/.svn'&& viewItem != '**/.hg'&& viewItem != '**/CVS'&& viewItem != '**/.DS_Store' && viewItem != '**/Thumbs.db' && viewItem != '**/*.git'`,
	`explorer-exclude.hasLoaded && !explorer-exclude.missingWorkspace && view == explorerExclude.pane.items`,
	`explorer-exclude.missingWorkspace`,
	`explorerResourceCut && explorerViewletVisible && filesExplorerFocus && !inputFocus`,
	`explorerResourceIsFolder != true && resourceExtname == .config`,
	`explorerResourceIsFolder != true && resourceExtname == .csproj || resourceExtname == .vbproj || resourceExtname == .dll`,
	`explorerResourceIsFolder != true && resourceLangId != gkd.archive && resourceLangId != gkd.xlog  && resourceLangId != gkd.alauda`,
	`explorerResourceIsFolder != true && resourceLangId == csharp || resourceLangId == vb`,
	`explorerResourceIsFolder != true`,
	`explorerResourceIsFolder && !explorerResourceIsRoot && resourceLangId != xml`,
	`explorerResourceIsFolder && !isWeb`,
	`explorerResourceIsFolder && !staticServerFolderPath`,
	`explorerResourceIsFolder && !virtualWorkspace`,
	`explorerResourceIsFolder && CCDXActive`,
	`explorerResourceIsFolder && autolisp.hasProject`,
	`explorerResourceIsFolder && config.exportall.config.includeFoldersToExport`,
	`explorerResourceIsFolder && config.flutterTools.getxDisplayContextMenu == true`,
	`explorerResourceIsFolder && config.flutterTools.mobxDisplayContextMenu == true`,
	`explorerResourceIsFolder && config.importSorter.generalConfiguration.sortImportsInDirectory`,
	`explorerResourceIsFolder && config.ng42.barrels.menus.createBarrel`,
	`explorerResourceIsFolder && config.ng42.barrels.menus.createDirectoryBarrel`,
	`explorerResourceIsFolder && config.ng42.barrels.menus.createFileBarrel`,
	`explorerResourceIsFolder && config.nsvueExtend.showAddAngularNativeFilesExtended`,
	`explorerResourceIsFolder && config.nsvueExtend.showAddAngularNativeFiles`,
	`explorerResourceIsFolder && config.nsvueExtend.showAddVuePage`,
	`explorerResourceIsFolder && config.openFolderInExplorer.enableOpenFolderInNewInstance`,
	`explorerResourceIsFolder && config.p42.feature.dependencyVisualization.enabled`,
	`explorerResourceIsFolder && config.svelte.ui.svelteKitFilesContextMenu.enable == always || explorerResourceIsFolder && svelte.uiContext.svelteKitFilesContextMenu.enable`,
	`explorerResourceIsFolder && dendron:pluginActive && shellExecutionSupported`,
	`explorerResourceIsFolder && dendron:pluginActive && workspaceFolderCount > 1 && shellExecutionSupported`,
	`explorerResourceIsFolder && explorerResourceIsRoot`,
	`explorerResourceIsFolder && explorerViewletVisible && filesExplorerFocus && !inputFocus`,
	`explorerResourceIsFolder && fileReferencesOpen`,
	`explorerResourceIsFolder && filesExplorerFocus`,
	`explorerResourceIsFolder && gitlens:enabled && config.gitlens.menus.explorer.history`,
	`explorerResourceIsFolder && isKFFProject`,
	`explorerResourceIsFolder && isStencilProject`,
	`explorerResourceIsFolder && isWPILibProject`,
	`explorerResourceIsFolder && listDoubleSelection`,
	`explorerResourceIsFolder && listHasSelectionOrFocus && !listDoubleSelection && l13DiffSelectedFolder`,
	`explorerResourceIsFolder && listHasSelectionOrFocus && !listDoubleSelection`,
	`explorerResourceIsFolder && phpCreateClass.activated`,
	`explorerResourceIsFolder && phpstan:enabled`,
	`explorerResourceIsFolder && resource =~ //FileCabinet/(SuiteApps/[a-z0-9]+\\.[a-z0-9]+\\.[a-z0-9]+|SuiteScripts|Web%20Site%20Hosting%20Files/.+)(/.*)*$/`,
	`explorerResourceIsFolder && resource =~ //FileCabinet/(SuiteScripts|Templates/(E-mail%20Templates|Marketing%20Templates)|Web%20Site%20Hosting%20Files)(/.*)*$/`,
	`explorerResourceIsFolder && resource =~ //Objects(/.+)*$/`,
	`explorerResourceIsFolder && resource =~ /^(?!.*node_modules.*)/`,
	`explorerResourceIsFolder && resourceFilename != 'flows' && resourceFilename != 'connections' && resourceFilename != 'models'`,
	`explorerResourceIsFolder && resourceFilename != 'node_modules'`,
	`explorerResourceIsFolder && resourceFilename != .idea && resourceFilename != .vs && resourceFilename != .vscode && resourceFilename != node_modules && !wallaby.paused`,
	`explorerResourceIsFolder && resourceFilename != modules && resourceFilename != extends`,
	`explorerResourceIsFolder && resourceFilename != modules && resourcePath =~ /modules/`,
	`explorerResourceIsFolder && resourceFilename != output && resourceFilename != debug && resourceFilename != target && resourceFilename != js-snippets && resourceFilename != interaction`,
	`explorerResourceIsFolder && resourceFilename != output && resourceFilename != debug && resourceFilename != target && resourceFilename != js-snippets`,
	`explorerResourceIsFolder && resourceFilename == android && dart-code:anyFlutterProjectLoaded && dart-code:isRunningLocally`,
	`explorerResourceIsFolder && resourceFilename == aura && sfdx:project_opened`,
	`explorerResourceIsFolder && resourceFilename == changesets && CCDXActive`,
	`explorerResourceIsFolder && resourceFilename == components && sfdx:project_opened`,
	`explorerResourceIsFolder && resourceFilename == extends`,
	`explorerResourceIsFolder && resourceFilename == ios || resourceFilename == macos && dart-code:anyFlutterProjectLoaded && dart-code:dartPlatformName == mac && dart-code:isRunningLocally`,
	`explorerResourceIsFolder && resourceFilename == lwc && sfdx:project_opened`,
	`explorerResourceIsFolder && resourceFilename == modules`,
	`explorerResourceIsFolder && resourceFilename == objectsJSON`,
	`explorerResourceIsFolder && resourceFilename == pages && sfdx:project_opened`,
	`explorerResourceIsFolder && resourceFilename == src && CCDXActive`,
	`explorerResourceIsFolder && resourceFilename == triggers && sfdx:project_opened`,
	`explorerResourceIsFolder && resourceFilename == waveTemplates && sfdx:project_opened`,
	`explorerResourceIsFolder && resourceFilename =~ /^([a-z0-9\\-]{3})$/ && config.anthem.bddTest.enabled`,
	`explorerResourceIsFolder && resourceFilename =~ /^([a-z0-9\\-]{4,20})$/ && config.anthem.unitTest.enabled`,
	`explorerResourceIsFolder && resourceFilename =~ /^([a-z]{3})$/ && config.anthem.unitTest.enabled`,
	`explorerResourceIsFolder && resourceFilename =~ /features/ && config.anthem.bddTest.enabled`,
	`explorerResourceIsFolder && resourceFilename in ext.imageExplorer.supportedFolders`,
	`explorerResourceIsFolder && resourceFilename in ext:supportedFolders`,
	`explorerResourceIsFolder && resourcePath =~ /classes/ && sfdx:project_opened`,
	`explorerResourceIsFolder && resourcePath =~ /components?$/`,
	`explorerResourceIsFolder && resourcePath =~ /devices\\\\.*\\(.*\\)\\s*$/`,
	`explorerResourceIsFolder && resourcePath =~ /entities/`,
	`explorerResourceIsFolder && resourcePath in ui5-tools:generatorPath`,
	`explorerResourceIsFolder && resourcePath in ui5-tools:resourcesPath`,
	`explorerResourceIsFolder && resourcePath in webosose.supportedAppFolders`,
	`explorerResourceIsFolder && resourcePath in webosose.supportedFolders`,
	`explorerResourceIsFolder && resourceScheme == ssh`,
	`explorerResourceIsFolder && ressouceScheme != ^.$ && ressourceScheme != ^templates$ && ressouceScheme != ^migrations$`,
	`explorerResourceIsFolder && rust-mod-generator.explorerResourceIsFolder|| resourceLangId == rust`,
	`explorerResourceIsFolder && sap.ux.applicationModeler.dataEditor.showDataEditorCommands`,
	`explorerResourceIsFolder && savConfig.showCommand && savConfig.showCreateBlocCommand`,
	`explorerResourceIsFolder && savConfig.showCommand && savConfig.showCreatePageCommand`,
	`explorerResourceIsFolder && savConfig.showCommand`,
	`explorerResourceIsFolder && sfdx:internal_dev`,
	`explorerResourceIsFolder && sfdx:project_opened`,
	`explorerResourceIsFolder && staticServerFolderPath`,
	`explorerResourceIsFolder && testcaferunner.readyForUX && testcaferunner.canRerun`,
	`explorerResourceIsFolder && testcaferunner.readyForUX && testcaferunner.chrome-canaryInstalled`,
	`explorerResourceIsFolder && testcaferunner.readyForUX && testcaferunner.chromeInstalled`,
	`explorerResourceIsFolder && testcaferunner.readyForUX && testcaferunner.chromiumInstalled`,
	`explorerResourceIsFolder && testcaferunner.readyForUX && testcaferunner.edgeInstalled`,
	`explorerResourceIsFolder && testcaferunner.readyForUX && testcaferunner.firefoxInstalled`,
	`explorerResourceIsFolder && testcaferunner.readyForUX && testcaferunner.ieInstalled`,
	`explorerResourceIsFolder && testcaferunner.readyForUX && testcaferunner.operaInstalled`,
	`explorerResourceIsFolder && testcaferunner.readyForUX && testcaferunner.portableChromeInstalled`,
	`explorerResourceIsFolder && testcaferunner.readyForUX && testcaferunner.portableFirefoxInstalled`,
	`explorerResourceIsFolder && testcaferunner.readyForUX && testcaferunner.safariInstalled`,
	`explorerResourceIsFolder && vdm-submenus-show == true || resourceLangId == vdmsl && vdm-submenus-show == true || resourceLangId == vdmpp && vdm-submenus-show == true || resourceLangId == vdmrt && vdm-submenus-show == true`,
	`explorerResourceIsFolder && view !== kubernetes.cloudExplorer`,
	`explorerResourceIsFolder == false && mcdutil.showContextMenu == true`,
	`explorerResourceIsFolder == false && resourceExtname == .war`,
	`explorerResourceIsFolder == false && resourceExtname =~ /.[j|w]ar$/`,
	`explorerResourceIsFolder == false`,
	`explorerResourceIsFolder == true && mcdutil.showContextMenu == true`,
	`explorerResourceIsFolder == true || resourceExtname == .csproj || resourceExtname == .vbproj`,
	`explorerResourceIsFolder == true || resourceExtname == .properties || resourceExtname == .mf || resourceExtname == .tag || resourceLangId == tld || resourceExtname == .jsp || resourceLangId == xml || resourceLangId == java || resourceExtname == .war || resourceExtname == .jar || resourceExtname == .ear || resourceExtname == .rar`,
	`explorerResourceIsFolder == true || resourceLangId == java || resourceFilename == pom.xml || resourceFilename == build.gradle || resourceExtname == .war || resourceExtname == .jar`,
	`explorerResourceIsFolder == true`,
	`explorerResourceIsFolder `,
	`explorerResourceIsFolder || config.openInNewInstance.alsoShowForFiles`,
	`explorerResourceIsFolder || editorLangId == java || editorLangId == makefile`,
	`explorerResourceIsFolder || editorLangId == java || resourceLangId == makefile`,
	`explorerResourceIsFolder || explorerResourceIsRoot`,
	`explorerResourceIsFolder || isFileSystemResource && resourceFilename =~ /^template\\.(json|yml|yaml)$/`,
	`explorerResourceIsFolder || resourceExtname == '.md' `,
	`explorerResourceIsFolder || resourceExtname == .svg`,
	`explorerResourceIsFolder || resourceExtname =~ /(.cgt|.pm|.xml)/ || editorLangId == makefile`,
	`explorerResourceIsFolder || resourceExtname =~ /(.cgt|.pm|.xml)/ || resourceLangId == makefile`,
	`explorerResourceIsFolder || resourceFilename =~ /(toolkit.xml|info.xml)/ || editorLangId == makefile`,
	`explorerResourceIsFolder || resourceFilename =~ /(toolkit.xml|info.xml)/ || resourceLangId == makefile`,
	`explorerResourceIsFolder || resourceFilename =~ /.*\\.(spec|test)\\.(js|jsx|ts|tsx)$/`,
	`explorerResourceIsFolder || resourceFilename =~ /.+\\.svg$/i`,
	`explorerResourceIsFolder || resourceFilename =~ /\\.pdx$|\\.odx-?.*$/i`,
	`explorerResourceIsFolder || resourceLangId == dart`,
	`explorerResourceIsFolder || resourceLangId == java || resourceLangId == makefile`,
	`explorerResourceIsFolder || resourceLangId == vdmpp || resourceLangId == vdmrt`,
	`explorerResourceIsFolder || resourceLangId == vdmsl`,
	`explorerResourceIsFolder || resourceLangId == yaml || resourceLangId == ytt`,
	`explorerResourceIsFolder!=true`,
	`explorerResourceIsFolder&&javaLSReady`,
	`explorerResourceIsFolder`,
	`explorerResourceIsRoot && supportsWorkspaces`,
	`explorerResourceIsRoot == true`,
	`explorerResourceIsRoot`,
	`explorerResourceMoveableToTrash && explorerViewletVisible && filesExplorerFocus && !explorerResourceReadonly && !inputFocus`,
	`explorerViewletCompressedFocus && explorerViewletVisible && filesExplorerFocus && !explorerViewletCompressedFirstFocus && !inputFocus`,
	`explorerViewletCompressedFocus && explorerViewletVisible && filesExplorerFocus && !explorerViewletCompressedLastFocus && !inputFocus`,
	`explorerViewletFocus && explorerResourceIsFolder`,
	`explorerViewletFocus && explorerViewletVisible && !explorerResourceIsFolder`,
	`explorerViewletFocus && explorerViewletVisible && !inputFocus`,
	`explorerViewletFocus`,
	`explorerViewletVisible && !searchViewletFocus `,
	`explorerViewletVisible && cookworks.config-helper.panelAcitve`,
	`explorerViewletVisible && explorerResourceIsFolder`,
	`explorerViewletVisible && explorerViewletFocus && !explorerResourceIsFolder && dvt.isExtensionActive`,
	`explorerViewletVisible && explorerViewletFocus && explorerResourceIsFolder && dvt.isExtensionActive`,
	`explorerViewletVisible && filesExplorerFocus && !explorerResourceIsFolder && !inputFocus`,
	`explorerViewletVisible && filesExplorerFocus && !explorerResourceIsFolder`,
	`explorerViewletVisible && filesExplorerFocus && !explorerResourceIsRoot && !explorerResourceReadonly && !inputFocus`,
	`explorerViewletVisible && filesExplorerFocus && !explorerResourceIsRoot && !inputFocus && resourcePath in vscodeExtJs.watchedProjectFiles`,
	`explorerViewletVisible && filesExplorerFocus && !explorerResourceIsRoot && !inputFocus`,
	`explorerViewletVisible && filesExplorerFocus && !explorerResourceMoveableToTrash && !explorerResourceReadonly && !inputFocus`,
	`explorerViewletVisible && filesExplorerFocus && !explorerResourceReadonly && !inputFocus`,
	`explorerViewletVisible && filesExplorerFocus && !inputFocus`,
	`explorerViewletVisible && filesExplorerFocus && explorerResourceIsRoot && !inputFocus && resourcePath in vscodeExtJs.watchedProjects`,
	`explorerViewletVisible && filesExplorerFocus && explorerResourceIsRoot && !inputFocus && resourcePath`,
	`explorerViewletVisible && filesExplorerFocus`,
	`explorerViewletVisible && listMultiSelection`,
	`explorerViewletVisible && resourceExtname == .ts && resourceFilename =~ /^((?!(spec|module|test|mock)).)*$/`,
	`explorerViewletVisible == true`,
	`explorerViewletVisible`,
	`explorer`,
	`extension == alefragnani.bookmarks && extensionStatus == installed`,
	`extension == alefragnani.jenkins-status && extensionStatus == installed`,
	`extension == alefragnani.numbered-bookmarks && extensionStatus==installed`,
	`extension == alefragnani.pascal && extensionStatus == installed`,
	`extension == alefragnani.project-manager && extensionStatus == installed`,
	`extension == alefragnani.separators && extensionStatus == installed`,
	`extension == tatosjb.fuzzy-search`,
	`extension =~ /^eamodio.gitlens(-insiders)?$/ && extensionStatus == installed`,
	`extension =~ /^extension-manager\\./`,
	`extension.ccls.callHierarchyVisible`,
	`extension.ccls.inheritanceHierarchyVisible`,
	`extension.ccls.memberHierarchyVisible`,
	`extension.cquery.callHierarchyVisible`,
	`extension.cquery.inheritanceHierarchyVisible`,
	`extension.graalvm.remoteMode`,
	`extension.kils.callHierarchyVisible`,
	`extension.kils.inheritanceHierarchyVisible`,
	`extension.loggedIn == false && extension.loadingCompleted == true`,
	`extension.loggedIn == true`,
	`extensionActivated && !Nocalhost.visibleTree`,
	`extensionActivated && !refreshing && view == Nocalhost`,
	`extensionActivated && emptyCluster`,
	`extensionActivated && explorerResourceIsFolder`,
	`extensionActivated && refreshing && view == Nocalhost`,
	`extensionActivated && view == Nocalhost`,
	`extensionActivated`,
	`extensionEnabled`,
	`extensionLangId == sql || extensionLangId == sql-bigquery || extensionLangId == jinja-sql`,
	`extensionStatus == installed && extension == wallabyjs.wallaby-vscode`,
	`extensionStatus==installed && extension==atlassian.atlascode`,
	`extensionStatus==installed`,
	`f5.as3Installed`,
	`f5.device && resourceExtname == .rpm`,
	`f5.device`,
	`f5.doInstalled`,
	`f5.fastInstalled && explorerResourceIsFolder`,
	`f5.fastInstalled`,
	`f5.isBigip`,
	`f5.isBigiq`,
	`f5.tcl && resourceExtname == .tmpl`,
	`f5.tcl && resourceExtname =~ /\\.(tcl|conf|rule)/`,
	`f5.tcl`,
	`f5.tsInstalled`,
	`falcon:activated == true`,
	`falcon:activated`,
	`false && !isWeb && remoteHub:hasVirtualFolders`,
	`false && TODO`,
	`false && dbux.context.activated`,
	`false && editorLangId == pvs`,
	`false && editorLangId == python`,
	`false && fw:isDevelopmentMode`,
	`false && isWeb && remoteHub:hasVirtualFolders`,
	`false && neverShowThisInTheCommandPalette`,
	`false && notebookType == 'node-notebook' && isWorkspaceTrusted`,
	`false && view == proof-explorer-view && viewItem != root`,
	`false && view == proof-explorer-view`,
	`false && view == proof-mate-view && viewItem == proofmate-sketchpad`,
	`false && view == proof-mate-view`,
	`false && view == workspace-explorer-view && viewItem == workspace-overview`,
	`false && view == workspace-explorer-view`,
	`false && viewItem =~ /dbuxProjectView\\.exerciseNode/`,
	`false && viewItem =~ /jobWithSsh/`,
	`false && virtualWorkspace`,
	`false || runningTest`,
	`false`,
	`fancy:active`,
	`fast-sfdc-active && fast-sfdc-configured && resourceExtname == .cmp`,
	`fast-sfdc-active && fast-sfdc-configured`,
	`fast-sfdc-active`,
	`fatshark-code-assist:isStingrayProject`,
	`fig.isAuthenticated`,
	`fileMatchFocus && replaceActive && searchViewletVisible`,
	`fileMatchOrFolderMatchWithResourceFocus`,
	`fileMatchOrMatchFocus && searchViewletVisible`,
	`fileMatchOrMatchFocus`,
	`fileReferencesEnabled`,
	`fileReferencesOpen`,
	`filesExplorerFocus && !editorFocus`,
	`filesExplorerFocus && !inputFocus`,
	`filesExplorerFocus && explorerResourceIsFolder && workspaceFolderCount >= 1 `,
	`filesExplorerFocus && resourceExtname == .int`,
	`filesExplorerFocus && resourceExtname == .mfu`,
	`filesExplorerFocus && resourceLangId == markdown`,
	`filesExplorerFocus == true`,
	`filesExplorerFocus || editorTextFocus`,
	`filesExplorerFocus`,
	`findInputFocussed && !config.emacs-mcx.useMetaPrefixMacCmd`,
	`findInputFocussed && config.emacs-mcx.useMetaPrefixCtrlLeftBracket`,
	`findInputFocussed && config.emacs-mcx.useMetaPrefixEscape`,
	`findInputFocussed && config.emacs-mcx.useMetaPrefixMacCmd`,
	`findInputFocussed`,
	`findJumpActive && editorTextFocus`,
	`findJumpActive`,
	`findWidgetVisible`,
	`findrelated:key:right`,
	`firstMatchFocus && searchViewletVisible`,
	`flatpakHasActiveManifest && !flatpakApplicationBuilt`,
	`flatpakHasActiveManifest && !flatpakRunnerActive`,
	`flatpakHasActiveManifest && flatpakApplicationBuilt && !flatpakRunnerActive`,
	`flatpakHasActiveManifest && flatpakApplicationBuilt`,
	`flatpakHasActiveManifest && flatpakRunnerActive`,
	`flatpakHasActiveManifest`,
	`flutter-commands-is-active`,
	`focusedSessionIsAttach && inDebugMode`,
	`focusedView != ''`,
	`focusedView != gomod`,
	`focusedView != joplin`,
	`focusedView == '' && activeCustomEditorId == 'luna.editor'`,
	`focusedView == 'workbench.panel.markers.view'`,
	`focusedView == 'workbench.view.testing'`,
	`focusedView == fcLocalResource`,
	`focusedView == gomod`,
	`focusedView == joplin`,
	`focusedView == kafkaExplorer && !listMultiSelection`,
	`focusedView == kafkaExplorer`,
	`focusedView == sigasi.projectView && !sigasi.projectViewVirtualSelection`,
	`focusedView == sigasi.projectView`,
	`focusedView == sqltools-view-connectionExplorer && sideBarFocus`,
	`focusedView == workbench.view.testing && !wallaby.paused`,
	`focusedView == zowe.ds.explorer && listSupportsMultiselect`,
	`focusedView == zowe.jobs.explorer`,
	`focusedView == zowe.uss.explorer || viewItem =~ /^(?!.*_fav.*)(textFile.*|binaryFile.*)/`,
	`focusedView == zowe.uss.explorer || viewItem =~ /^(directory.*|ussSession.*)/ && !listSupportsMultiselect`,
	`focusedView == zowe.uss.explorer`,
	`folderMatchFocus && replaceActive && searchViewletVisible`,
	`frontMatter:dashboard:data:enabled`,
	`frontMatter:dashboard:snippets:enabled`,
	`frontMatter:enabled == true && frontMatter:dashboard:open == false`,
	`frontMatter:enabled == true && frontMatter:dashboard:open == true`,
	`frontMatter:file:isValid == true && frontMatter:dashboard:snippets:enabled`,
	`frontMatter:file:isValid == true && frontMatter:markdown:wysiwyg`,
	`frontMatter:file:isValid == true`,
	`frontMatter:git:enabled`,
	`frontMatterCanInit`,
	`frontMatterCanOpenPreview`,
	`fsharp.project.any \u0026\u0026 config.FSharp.enableTouchBar`,
	`fsharp.project.any \u0026\u0026 editorFocus \u0026\u0026 editorLangId == \u0027fsharp\u0027`,
	`fsharp.project.any \u0026\u0026 editorLangId == \u0027fsharp\u0027`,
	`fsharp.project.any \u0026\u0026 fsharp.showProjectExplorerInExplorerActivity`,
	`fsharp.project.any \u0026\u0026 fsharp.showProjectExplorerInFsharpActivity`,
	`fsharp.project.any`,
	`fsharp.showProjectExplorerInFsharpActivity`,
	`fsmViewer:canExport`,
	`fw:hasOpenedNote && !fw:isNotePinned && view == fw-notes`,
	`fw:hasOpenedNote && fw:isNotePinned && view == fw-notes`,
	`fw:hasOpenedNote && view == fw-notes`,
	`fw:isDevelopmentMode`,
	`fw:isSupportedMetadata && config.fictionWriter.metadata.enabled`,
	`fw:showNotes && config.fictionWriter.notes.enabled`,
	`fx-extension.canUpgradeToArmAndMultiEnv`,
	`fx-extension.isTeamsFx && !fx-extension.isV3Enabled && isWorkspaceTrusted`,
	`fx-extension.isTeamsFx && fx-extension.customizedTreeview`,
	`fx-extension.isTeamsFx`,
	`galaxytools:isActive && config.galaxyTools.planemo.enabled`,
	`gauge:activated`,
	`gauge:executing`,
	`gauge:multipleProjects?`,
	`gcloudComponentsInstalling`,
	`gcloudInstalling`,
	`gcloudLoggedIn && !gcloudProjectId && !remoteCloudFunctionsAvailable && !validFunctionsWorkspace`,
	`gcloudLoggedIn && !gcloudProjectId && !runningOnCloudShell`,
	`gcloudLoggedIn && !gcloudProjectId`,
	`gcloudLoggedIn && gcloudProjectId && !remoteCloudFunctionsAvailable && !validFunctionsWorkspace`,
	`gcloudLoggedIn && gcloudProjectId && cloudfunctionsExplorerNeedsApiEnable`,
	`gcloudLoggedIn && gcloudProjectId && computeExplorerNeedsApiEnable`,
	`gcloudLoggedIn && gcloudProjectId && runExplorerNeedsApiEnable`,
	`gcloudLoggedIn && gcloudProjectId && secretmanagerExplorerNeedsApiEnable`,
	`gcloudLoggedIn && gcloudProjectId && storageExplorerNeedsApiEnable`,
	`genv.env.activated`,
	`genv.installation.installed && !genv.env.activated`,
	`genv.installation.installed`,
	`genv.installation.uninstalled`,
	`gerrit-view-show-tree == true && gerrit-view-in-explorer == true`,
	`gerrit-view-show-tree == true`,
	`gerrit:connected && view == gerrit:changeExplorer`,
	`gerrit:isUsingGerrit`,
	`gerrit:searchQuery || gerrit:searchChangeNumber`,
	`gifPlayerActive`,
	`giftPreviewFocus`,
	`gistpad:hasRepos`,
	`gistpad:state != SignedIn`,
	`gistpad:state == SignIn && gistpad:hasWiki`,
	`gistpad:state == SignedIn && gistpad:codeSwingEnabled`,
	`gistpad:state == SignedIn`,
	`git.commit.compare.view.show`,
	`git.commit.view.show`,
	`git.live.Last12Months.selected`,
	`git.live.Last24Hours.selected`,
	`git.live.Last2Weeks.selected`,
	`git.live.Last3Months.selected`,
	`git.live.Last3Years.selected`,
	`git.live.Last4Weeks.selected`,
	`git.live.Last7Days.selected`,
	`git.live.callPalette.enabled`,
	`git.live.diff.leftBase.selected`,
	`git.live.diff.leftTheirRemote.selected`,
	`git.live.diff.leftYourLocal.selected`,
	`git.live.diff.rightTheirLocal.selected`,
	`git.live.diff.rightTheirRemote.selected`,
	`git.live.inProgress.selected && git.live.view.org`,
	`git.live.localDoc.Last12Months.selected`,
	`git.live.localDoc.Last24Hours.selected`,
	`git.live.localDoc.Last2Weeks.selected`,
	`git.live.localDoc.Last3Months.selected`,
	`git.live.localDoc.Last3Years.selected`,
	`git.live.localDoc.Last4Weeks.selected`,
	`git.live.localDoc.Last7Days.selected`,
	`git.live.localDoc.nonConflicting.selected`,
	`git.live.localDoc.rightTheirLocal.selected`,
	`git.live.localDoc.rightTheirRemote.selected`,
	`git.live.offline.selected && git.live.view.org`,
	`git.live.pushedChanges.selected && git.live.view.org`,
	`git.live.remoteDiff.pushedChanges.selected`,
	`git.live.remoteDiff.unpushedChanges.selected`,
	`git.live.sharePalette.enabled`,
	`git.live.stale.selected && git.live.view.org`,
	`git.live.todo.selected && git.live.view.org`,
	`git.live.tree.leftBase.selected && !git.live.view.org`,
	`git.live.tree.leftTheirRemote.selected && !git.live.view.org`,
	`git.live.tree.leftYourLocal.selected && !git.live.view.org`,
	`git.live.tree.rightTheirLocal.selected && !git.live.view.org`,
	`git.live.tree.rightTheirRemote.selected && !git.live.view.org`,
	`git.live.unpushedChanges.selected && git.live.view.org`,
	`git.state != initialized && !github:initialized && workspaceFolderCount > 0`,
	`git.state == initialized && gitOpenRepositoryCount == 0 && workspaceFolderCount > 0`,
	`gitHubOpenRepositoryCount != 0 && github:authenticated && github:hasGitHubRemotes`,
	`gitHubOpenRepositoryCount != 0 && github:authenticated`,
	`gitHubOpenRepositoryCount != 0 && github:hasGitHubRemotes`,
	`gitHubOpenRepositoryCount != 0 && github:inReviewMode`,
	`gitHubOpenRepositoryCount != 0 && github:initialized && github:authenticated`,
	`gitHubOpenRepositoryCount != 0 && github:initialized && view == issues:github`,
	`gitHubOpenRepositoryCount != 0 && github:initialized && view == pr:github`,
	`gitHubOpenRepositoryCount != 0`,
	`gitOpenRepositoryCount != 0 && !gitdoc:enabled`,
	`gitOpenRepositoryCount != 0 && azdo:authenticated && azdo:hasAzdoRemotes`,
	`gitOpenRepositoryCount != 0 && azdo:authenticated`,
	`gitOpenRepositoryCount != 0 && azdo:hasAzdoRemotes`,
	`gitOpenRepositoryCount != 0 && azdo:inReviewMode`,
	`gitOpenRepositoryCount != 0 && azdo:initialized && view == azdopr:azdo`,
	`gitOpenRepositoryCount != 0 && azdo:initialized && view =~ /(azdopr|issues):azdo/`,
	`gitOpenRepositoryCount != 0 && gitdoc:enabled`,
	`gitOpenRepositoryCount != 0`,
	`gitOpenRepositoryCount == 1 && config.git-buttons.pullContext || config.git-buttons.pull`,
	`gitOpenRepositoryCount == 1 && config.git-buttons.pushContext || config.git-buttons.push`,
	`gitOpenRepositoryCount > 1 && config.git-buttons.pullContext`,
	`gitOpenRepositoryCount > 1 && config.git-buttons.pushContext`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.addRemote`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.branchFrom`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.branch`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.checkoutDetached`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.checkout`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.cherryPick`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.cleanAllTracked`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.cleanAllUntracked`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.cleanAll`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.clean`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.cloneRecursive`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.clone`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.closeAllDiffEditors`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.close`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.commitAllAmendNoVerify`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.commitAllAmend`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.commitAllNoVerify`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.commitAllSignedNoVerify`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.commitAllSigned`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.commitAll`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.commitEmptyNoVerify`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.commitEmpty`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.commitNoVerify`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.commitStagedAmendNoVerify`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.commitStagedAmend`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.commitStagedNoVerify`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.commitStagedSignedNoVerify`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.commitStagedSigned`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.commitStaged`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.commit`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.createTag`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.deleteBranch`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.deleteTag`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.fetchAll`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.fetchPrune`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.fetch`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.ignore`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.init`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.merge`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.openAllChanges`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.openChange`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.openFile2`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.openFile`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.openHEADFile`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.openRepository`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.publish`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.pullFrom`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.pullRebase`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.pushForce`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.pushTags`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.pushToForce`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.pushTo`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.pushWithTagsForce`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.pushWithTags`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.rebaseAbort`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.rebase`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.refresh`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.removeRemote`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.renameBranch`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.rename`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.restoreCommitTemplate`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.revealInExplorer`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.revertChange`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.revertSelectedRanges`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.setLogLevel`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.showOutput`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.stageAllMerge`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.stageAllTracked`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.stageAllUntracked`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.stageAll`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.stageChange`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.stageSelectedRanges`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.stage`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.stashApplyLatest`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.stashApply`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.stashDropAll`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.stashDrop`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.stashIncludeUntracked`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.stashPopLatest`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.stashPop`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.stash`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.syncRebase`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.sync`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.undoCommit`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.unstageAll`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.unstageSelectedRanges`,
	`gitOpenRepositoryCount >= 1 && config.git-buttons.unstage`,
	`gitdoc:enabled && timelineItem =~ /git:file:commit\\b/`,
	`gitduckCodeShareState == 4 && gitduckCodeShareIsHost`,
	`gitduckCodeShareState == 4`,
	`gitflow.initialized == true`,
	`gitflow.initialized`,
	`github.copilot.activated`,
	`github:createPullRequest`,
	`github:hasGitHubRemotes && github:authenticated`,
	`github:hasGitHubRemotes && github:inReviewMode`,
	`github:hasGitHubRemotes`,
	`github:inReviewMode && !github:createPullRequest`,
	`github:inReviewMode && github:focusedReview && !github:createPullRequest && github:activePRCount <= 1`,
	`github:inReviewMode`,
	`github:noUpstream`,
	`githubExplorer:showTreeView`,
	`githubLinker.showInContextMenu`,
	`githubRepoMgr.userState == 'notLogged'`,
	`gitlab:hasAnyToken`,
	`gitlab:noAccount`,
	`gitlab:noInstanceRemote`,
	`gitlab:noRepository`,
	`gitlab:noTokens`,
	`gitlab:validState && editorIsOpen`,
	`gitlab:validState && resourceScheme != 'gl-merged-ci-yaml' && resourceFilename == '.gitlab-ci.yml'`,
	`gitlab:validState`,
	`gitlens:action:openPullRequest > 1 && viewItem =~ /gitlens:pullrequest\\b/`,
	`gitlens:activeFileStatus =~ /blameable/ && !gitlens:hasVirtualFolders`,
	`gitlens:activeFileStatus =~ /blameable/ && !isInDiffEditor`,
	`gitlens:activeFileStatus =~ /blameable/ && config.gitlens.menus.editor.clipboard`,
	`gitlens:activeFileStatus =~ /blameable/ && gitlens:activeFileStatus =~ /remotes/`,
	`gitlens:activeFileStatus =~ /blameable/ && gitlens:annotationStatus == computed && config.gitlens.menus.editorGroup.blame`,
	`gitlens:activeFileStatus =~ /blameable/ && gitlens:annotationStatus == computed`,
	`gitlens:activeFileStatus =~ /blameable/ && gitlens:annotationStatus == computing && config.gitlens.menus.editorGroup.blame`,
	`gitlens:activeFileStatus =~ /blameable/ && gitlens:annotationStatus`,
	`gitlens:activeFileStatus =~ /blameable/ && isInDiffEditor && !isInDiffRightEditor`,
	`gitlens:activeFileStatus =~ /blameable/ && isInDiffRightEditor`,
	`gitlens:activeFileStatus =~ /blameable/`,
	`gitlens:activeFileStatus =~ /revision/ && !isInDiffEditor`,
	`gitlens:activeFileStatus =~ /revision/ && isInDiffEditor && !isInDiffRightEditor`,
	`gitlens:activeFileStatus =~ /revision/ && isInDiffEditor`,
	`gitlens:activeFileStatus =~ /revision/ && isInDiffRightEditor`,
	`gitlens:activeFileStatus =~ /revision/ && resourceScheme == git && !isInDiffEditor`,
	`gitlens:activeFileStatus =~ /revision/ && resourceScheme =~ /^(?!(file|git)$).*$/ && !isInDiffEditor`,
	`gitlens:activeFileStatus =~ /revision/ && resourceScheme =~ /^(?!(file|git)$).*$/ && isInDiffEditor && !isInDiffRightEditor`,
	`gitlens:activeFileStatus =~ /revision/ && resourceScheme =~ /^(?!(file|git)$).*$/ && isInDiffRightEditor`,
	`gitlens:activeFileStatus =~ /revision/`,
	`gitlens:activeFileStatus =~ /tracked/ && !isInDiffEditor`,
	`gitlens:activeFileStatus =~ /tracked/ && config.gitlens.menus.editor.history`,
	`gitlens:activeFileStatus =~ /tracked/ && config.gitlens.menus.editorGroup.compare && !isInDiffEditor`,
	`gitlens:activeFileStatus =~ /tracked/ && config.gitlens.menus.editorGroup.compare && isInDiffEditor && !isInDiffRightEditor`,
	`gitlens:activeFileStatus =~ /tracked/ && config.gitlens.menus.editorGroup.compare && isInDiffRightEditor`,
	`gitlens:activeFileStatus =~ /tracked/ && gitlens:activeFileStatus =~ /remotes/`,
	`gitlens:activeFileStatus =~ /tracked/ && isInDiffEditor && !isInDiffRightEditor`,
	`gitlens:activeFileStatus =~ /tracked/ && isInDiffRightEditor`,
	`gitlens:activeFileStatus =~ /tracked/`,
	`gitlens:debugging`,
	`gitlens:enabled && !gitlens:hasVirtualFolders`,
	`gitlens:enabled && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && config.gitlens.menus.scmRepository.authors && scmProvider == git`,
	`gitlens:enabled && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && config.gitlens.menus.scmRepository.authors`,
	`gitlens:enabled && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && scmProvider == git && scmResourceGroup =~ /^(workingTree|index)$/ && config.gitlens.menus.scmGroup.stash`,
	`gitlens:enabled && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && scmProvider == git && scmResourceGroup =~ /^(workingTree|index)$/ && config.gitlens.menus.scmGroupInline.stash`,
	`gitlens:enabled && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && scmProvider == git && scmResourceGroup =~ /^(workingTree|index)$/ && config.gitlens.menus.scmItem.stash`,
	`gitlens:enabled && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && scmProvider == git && scmResourceGroup =~ /^(workingTree|index)$/ && config.gitlens.menus.scmItemInline.stash`,
	`gitlens:enabled && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders`,
	`gitlens:enabled && !gitlens:readonly && config.gitlens.menus.scm.authors`,
	`gitlens:enabled && !gitlens:readonly && scmProvider == git && scmResourceGroup =~ /^(workingTree|index)$/ && config.gitlens.menus.scmGroup.stash`,
	`gitlens:enabled && !gitlens:readonly && scmProvider == git && scmResourceGroup =~ /^(workingTree|index)$/ && config.gitlens.menus.scmGroupInline.stash`,
	`gitlens:enabled && !gitlens:readonly && scmProvider == git && scmResourceGroup =~ /^(workingTree|index)$/ && config.gitlens.menus.scmItem.stash`,
	`gitlens:enabled && !gitlens:readonly`,
	`gitlens:enabled && config.gitlens.menus.editorTab.compare && isFileSystemResource`,
	`gitlens:enabled && config.gitlens.menus.editorTab.history && isFileSystemResource`,
	`gitlens:enabled && config.gitlens.menus.scm.graph && gitlens:plus:enabled && scmProvider == git && scmProviderRootUri not in gitlens:plus:disallowedRepos`,
	`gitlens:enabled && config.gitlens.menus.scmRepository.graph && gitlens:plus:enabled && scmProvider == git && scmProviderRootUri not in gitlens:plus:disallowedRepos`,
	`gitlens:enabled && config.gitlens.menus.scmRepositoryInline.graph && gitlens:plus:enabled && scmProvider == git && scmProviderRootUri not in gitlens:plus:disallowedRepos`,
	`gitlens:enabled && focusedView =~ /^gitlens\\.views\\.branches/`,
	`gitlens:enabled && focusedView =~ /^gitlens\\.views\\.commits/`,
	`gitlens:enabled && focusedView =~ /^gitlens\\.views\\.contributors/`,
	`gitlens:enabled && focusedView =~ /^gitlens\\.views\\.fileHistory/`,
	`gitlens:enabled && focusedView =~ /^gitlens\\.views\\.lineHistory/`,
	`gitlens:enabled && focusedView =~ /^gitlens\\.views\\.remotes/`,
	`gitlens:enabled && focusedView =~ /^gitlens\\.views\\.repositories/`,
	`gitlens:enabled && focusedView =~ /^gitlens\\.views\\.searchAndCompare\\b/`,
	`gitlens:enabled && focusedView =~ /^gitlens\\.views\\.stashes/`,
	`gitlens:enabled && focusedView =~ /^gitlens\\.views\\.tags/`,
	`gitlens:enabled && focusedView =~ /^gitlens\\.views\\.worktrees/`,
	`gitlens:enabled && gitlens:activeFileStatus =~ /tracked/`,
	`gitlens:enabled && gitlens:hasRemotes && config.gitlens.menus.editorTab.clipboard && isFileSystemResource`,
	`gitlens:enabled && gitlens:hasRemotes && config.gitlens.menus.editorTab.remote && isFileSystemResource`,
	`gitlens:enabled && gitlens:hasRemotes && scmProvider == git && scmResourceGroup =~ /^(workingTree|index|merge)$/ && config.gitlens.menus.scmItem.clipboard`,
	`gitlens:enabled && gitlens:hasRemotes && scmProvider == git && scmResourceGroup =~ /^(workingTree|index|merge)$/ && config.gitlens.menus.scmItem.remote`,
	`gitlens:enabled && gitlens:hasRemotes && timelineItem =~ /git:file:commit\\b/`,
	`gitlens:enabled && scmProvider == git && scmResourceGroup =~ /^(workingTree|index)$/ && config.gitlens.menus.scmGroup.compare`,
	`gitlens:enabled && scmProvider == git && scmResourceGroup =~ /^(workingTree|index)$/ && config.gitlens.menus.scmGroup.openClose`,
	`gitlens:enabled && scmProvider == git && scmResourceGroup =~ /^(workingTree|index|merge)$/ && config.gitlens.menus.scmItem.compare`,
	`gitlens:enabled && scmProvider == git && scmResourceGroup =~ /^(workingTree|index|merge)$/ && config.gitlens.menus.scmItem.history`,
	`gitlens:enabled`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && gitlens:action:createPullRequest && viewItem =~ /gitlens:status:upstream:(?!none)/`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && view =~ /^gitlens\\.views\\.commits/`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && view =~ /^gitlens\\.views\\.repositories/`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem == gitlens:status:upstream:ahead`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem == gitlens:status:upstream:behind`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:branch\\b(?!.*?\\b\\+remote\\b)(?!.*?\\b\\+tracking\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:branch\\b(?=.*?\\b\\+(remote|tracking)\\b)(?!.*?\\b\\+ahead\\b)(?!.*?\\b\\+behind\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:branch\\b(?=.*?\\b\\+ahead\\b)(?!.*?\\b\\+behind\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:branch\\b(?=.*?\\b\\+behind\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:commit\\b(?=.*?\\b\\+current\\b)(?=.*?\\b\\+unpublished\\b)(?!.*?\\b\\+HEAD\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:commit\\b(?=.*?\\b\\+current\\b)(?=.*?\\b\\+unpublished\\b)(?=.*?\\b\\+HEAD\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+committed\\b)(?=.*?\\b\\+current\\b)(?=.*?\\b\\+unpublished\\b)(?!.*?\\b\\+HEAD\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+committed\\b)(?=.*?\\b\\+current\\b)(?=.*?\\b\\+unpublished\\b)(?=.*?\\b\\+HEAD\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:repo-folder\\b(?!.*?\\b\\+ahead\\b)(?!.*?\\b\\+behind\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:repo-folder\\b(?=.*?\\b\\+ahead\\b)(?!.*?\\b\\+behind\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:repo-folder\\b(?=.*?\\b\\+behind\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:repo-folder\\b/`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:repository\\b/`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:status(\\-branch)?:upstream:(?!none)/`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:status(\\-branch)?:upstream:ahead/`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:status(\\-branch)?:upstream:behind/`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:status(\\-branch)?:upstream:none/`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders && viewItem =~ /gitlens:status:upstream:(?!none)/`,
	`gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders`,
	`gitlens:hasRemotes && !gitlens:readonly && gitlens:action:createPullRequest && viewItem =~ /gitlens:branch\\b(?=.*?\\b\\+(tracking|remote)\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && gitlens:action:createPullRequest && viewItem =~ /gitlens:status:upstream:(?!none)/`,
	`gitlens:hasRemotes && !gitlens:readonly && view =~ /^gitlens\\.views\\.commits/`,
	`gitlens:hasRemotes && !gitlens:readonly && view =~ /^gitlens\\.views\\.repositories/`,
	`gitlens:hasRemotes && !gitlens:readonly && viewItem == gitlens:status:upstream:ahead`,
	`gitlens:hasRemotes && !gitlens:readonly && viewItem == gitlens:status:upstream:behind`,
	`gitlens:hasRemotes && !gitlens:readonly && viewItem =~ /gitlens:branch\\b(?!.*?\\b\\+remote\\b)(?!.*?\\b\\+tracking\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && viewItem =~ /gitlens:branch\\b(?=.*?\\b\\+(remote|tracking)\\b)(?!.*?\\b\\+ahead\\b)(?!.*?\\b\\+behind\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && viewItem =~ /gitlens:branch\\b(?=.*?\\b\\+ahead\\b)(?!.*?\\b\\+behind\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && viewItem =~ /gitlens:branch\\b(?=.*?\\b\\+behind\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && viewItem =~ /gitlens:commit\\b(?=.*?\\b\\+current\\b)(?=.*?\\b\\+unpublished\\b)(?!.*?\\b\\+HEAD\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && viewItem =~ /gitlens:commit\\b(?=.*?\\b\\+current\\b)(?=.*?\\b\\+unpublished\\b)(?=.*?\\b\\+HEAD\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+committed\\b)(?=.*?\\b\\+current\\b)(?=.*?\\b\\+unpublished\\b)(?!.*?\\b\\+HEAD\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+committed\\b)(?=.*?\\b\\+current\\b)(?=.*?\\b\\+unpublished\\b)(?=.*?\\b\\+HEAD\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && viewItem =~ /gitlens:repo-folder\\b(?!.*?\\b\\+ahead\\b)(?!.*?\\b\\+behind\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && viewItem =~ /gitlens:repo-folder\\b(?=.*?\\b\\+ahead\\b)(?!.*?\\b\\+behind\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && viewItem =~ /gitlens:repo-folder\\b(?=.*?\\b\\+behind\\b)/`,
	`gitlens:hasRemotes && !gitlens:readonly && viewItem =~ /gitlens:repo-folder\\b/`,
	`gitlens:hasRemotes && !gitlens:readonly && viewItem =~ /gitlens:repository\\b/`,
	`gitlens:hasRemotes && !gitlens:readonly && viewItem =~ /gitlens:status(\\-branch)?:upstream:(?!none)/`,
	`gitlens:hasRemotes && !gitlens:readonly && viewItem =~ /gitlens:status(\\-branch)?:upstream:ahead/`,
	`gitlens:hasRemotes && !gitlens:readonly && viewItem =~ /gitlens:status(\\-branch)?:upstream:behind/`,
	`gitlens:hasRemotes && !gitlens:readonly && viewItem =~ /gitlens:status(\\-branch)?:upstream:none/`,
	`gitlens:hasRemotes && !gitlens:readonly && viewItem =~ /gitlens:status:upstream:(?!none)/`,
	`gitlens:hasRemotes && !gitlens:readonly`,
	`gitlens:hasRemotes && gitlens:action:createPullRequest && viewItem =~ /gitlens:branch\\b(?=.*?\\b\\+(tracking|remote)\\b)/`,
	`gitlens:hasRemotes && gitlens:action:createPullRequest && webviewItem =~ /gitlens:branch\\b(?=.*?\\b\\+(tracking|remote)\\b)/`,
	`gitlens:hasRemotes && viewItem =~ /gitlens:(commit|file\\b(?=.*?\\b\\+committed\\b))/`,
	`gitlens:hasRemotes && viewItem =~ /gitlens:(file\\b(?=.*?\\b\\+committed\\b)|history:(file|line)|status:file)\\b/`,
	`gitlens:hasRemotes && viewItem =~ /gitlens:branch/`,
	`gitlens:hasRemotes && viewItem =~ /gitlens:commit\\b/`,
	`gitlens:hasRemotes && webviewItem =~ /gitlens:branch/`,
	`gitlens:hasRemotes && webviewItem =~ /gitlens:branch\\b(?=.*?\\b\\+(tracking|remote)\\b)/`,
	`gitlens:hasRemotes && webviewItem =~ /gitlens:commit/`,
	`gitlens:hasRemotes && webviewItem =~ /gitlens:commit\\b/`,
	`gitlens:hasRemotes`,
	`gitlens:hasVirtualFolders`,
	`gitlens:key:,`,
	`gitlens:key:.`,
	`gitlens:key:alt+left`,
	`gitlens:key:alt+right`,
	`gitlens:key:ctrl+left`,
	`gitlens:key:ctrl+right`,
	`gitlens:key:escape && editorTextFocus && !findWidgetVisible && !renameInputVisible && !suggestWidgetVisible && !isInEmbeddedEditor`,
	`gitlens:key:left`,
	`gitlens:key:right`,
	`gitlens:plus:required && gitlens:plus:state == 0`,
	`gitlens:plus:required && gitlens:plus:state == 2`,
	`gitlens:plus:required && gitlens:plus:state == 4`,
	`gitlens:plus:state == -1`,
	`gitlens:plus:state == 0`,
	`gitlens:plus:state == 1 || gitlens:plus:state == 3`,
	`gitlens:plus:state == 2`,
	`gitlens:plus:state == 4`,
	`gitlens:plus`,
	`gitlens:views:welcome:visible != false`,
	`gitlens:vsls && gitlens:vsls != guest && viewItem =~ /gitlens:contributor\\b(?!.*?\\b\\+current\\b)/`,
	`gitlens:webview:graph:active && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders`,
	`gitlens:webview:graph:active && gitlens:hasRemotes && !gitlens:readonly && !gitlens:untrusted && !gitlens:hasVirtualFolders`,
	`gitlens:webview:graph:active`,
	`gitlens:webview:timeline:active`,
	`gitops:currentClusterGitOpsNotEnabled && !gitops:noClusterSelected`,
	`gitops:currentClusterGitOpsNotEnabled`,
	`gitops:isDev`,
	`gitops:loadingClusters`,
	`gitops:loadingSources && !gitops:noClusterSelected && !gitops:currentClusterGitOpsNotEnabled`,
	`gitops:loadingWorkloads && !gitops:noClusterSelected && !gitops:currentClusterGitOpsNotEnabled`,
	`gitops:noClusterSelected`,
	`gitops:openFolderGitRepositoryExistDetermined && !gitops:openFolderGitRepositoryExist`,
	`gitops:openFolderGitRepositoryExistDetermined && gitops:openFolderGitRepositoryExist`,
	`gitpod.inWorkspace == true && gitpod.UIKind == 'desktop'`,
	`gitpod.inWorkspace == true && gitpod.workspaceOwned == true && gitpod.workspaceShared == false`,
	`gitpod.inWorkspace == true && gitpod.workspaceOwned == true && gitpod.workspaceShared == true`,
	`gitpod.inWorkspace == true && gitpod.workspaceOwned == true`,
	`gitpod.inWorkspace == true`,
	`gitpod.run-gp.enabled && gitpod.run-gp.dockerfile && resourceFilename =~ /^Dockerfile$|\\.Dockerfile$/ && resourceScheme == file`,
	`gitpod.run-gp.enabled && resourceFilename == '.gitpod.yml' && resourceScheme == file`,
	`gitpod.run-gp.enabled`,
	`gitweblinks:hasRepositories && gitweblinks:canCopy`,
	`gitweblinks:hasRepositories && gitweblinks:canOpen`,
	`gkd_ctx:hasViz == ON`,
	`globalMessageVisible`,
	`gltfActive`,
	`gltfFileActive && gltfShowToolbar3D`,
	`gltfFileActive`,
	`gltfPreviewActive && !gltfDebugActive`,
	`gltfPreviewActive && gltfDebugActive`,
	`go.hasProfiles`,
	`go.showExplorer`,
	`gondolaEditor || editorLangId == typescript`,
	`gondolaEditor`,
	`google-pinyin.seleting`,
	`googleHomeExtension.activated`,
	`gppProceduresTreeViewEnabled !== true`,
	`gradle:activated && !gradle:defaultView`,
	`gradle:activated && gradle:defaultView`,
	`gradle:activated`,
	`gradle:extensionActivated`,
	`groupOutlineEnabled`,
	`gsDebugIsProfiling`,
	`gulptasks:enabled`,
	`haoide.hasDefaultProject`,
	`haoide.hasOpenProject && !haoide.activeFile.isCodeFile && haoide.activeFile.isSFDCFile`,
	`haoide.hasOpenProject && (resourceExtname == .cls || resourceExtname == .trigger) && haoide.activeFile.isSFDCFile`,
	`haoide.hasOpenProject && editorHasSelection`,
	`haoide.hasOpenProject && editorIsOpen && haoide.activeFile.isSFDCFile`,
	`haoide.hasOpenProject && editorIsOpen`,
	`haoide.hasOpenProject && explorerResourceIsFolder`,
	`haoide.hasOpenProject && haoide.activeFile.isCodeFile`,
	`haoide.hasOpenProject && haoide.activeFile.isSFDCFile`,
	`haoide.hasOpenProject && haoide.activeFile.isTestClass && haoide.activeFile.isSFDCFile`,
	`haoide.hasOpenProject && haoide.activeFile.isTestClass`,
	`haoide.hasOpenProject && haoide.hasIdSelected`,
	`haoide.hasOpenProject && resourceExtname == .cls`,
	`haoide.hasOpenProject && resourceExtname == .xml`,
	`haoide.hasOpenProject`,
	`hasCompiled`,
	`hasComponent`,
	`hasCustom`,
	`hasGitRepo && !disableInEditor`,
	`hasGitRepo && editorIsOpen`,
	`hasGitRepo`,
	`hasGitRepository && gitstash.explorer.enabled`,
	`hasGitRepository`,
	`hasMinified`,
	`hasModel`,
	`hasOtherSuggestions && textInputFocus && !inSnippetMode && !suggestWidgetVisible && config.editor.tabCompletion == 'on'`,
	`hasOtherSuggestions && textInputFocus && textInputFocus && !inSnippetMode && !suggestWidgetVisible && config.editor.tabCompletion == 'on'`,
	`hasPackageJson && !isMaterial`,
	`hasPage`,
	`hasSearchResult && config.find-and-transform.enableContextMenus`,
	`hasSearchResult && searchViewletFocus`,
	`hasSearchResult || inSearchEditor`,
	`hasSearchResult`,
	`hasSource`,
	`hasSppkg`,
	`hasStylesheet`,
	`hasSut && !hasComponent`,
	`hasSut`,
	`hasSymbols`,
	`hasTastConfig`,
	`hasTemplate`,
	`hasTest`,
	`hdinsight.hive`,
	`hdinsight.pysparkBatch`,
	`hdinsight.pysparkInteractive`,
	`hediet-power-tools.apply-rename.applicable`,
	`hediet-power-tools.apply-rename.cancelable`,
	`hediet.vscode-drawio.active`,
	`helpDisplay != False`,
	`hexEditor:isActive`,
	`hexEditor:openEditor`,
	`hexEditor:showSidebarInspector`,
	`hexViewer:openEditor`,
	`hexdump:btnEnabled && resourceScheme == file`,
	`hexdump:btnEnabled && resourceScheme == hexdump`,
	`hide-node-modules:isHidden && hide-node-modules:containsPackageJson`,
	`hideCommands.never`,
	`hideComments.commentsEnabled == false`,
	`hideComments.commentsEnabled == true`,
	`hideComments.regexEnabled == true && hideComments.regexUsed == false`,
	`hideComments.regexEnabled == true && hideComments.regexUsed == true`,
	`hideComments.regexEnabled == true`,
	`historyNavigationBackwardsEnabled && historyNavigationWidgetFocus && !suggestWidgetVisible`,
	`historyNavigationEnabled && historyNavigationWidget`,
	`historyNavigationForwardsEnabled && historyNavigationWidgetFocus && !suggestWidgetVisible`,
	`hoganLensTreeViewEnabled`,
	`hoi4MUInDev`,
	`hpccPlatformActive`,
	`htmlPreviewFocus`,
	`htmlRelatedLinks:fileIsHTML && config.html-related-links.showIfHTML || config.html-related-links.alwaysShow`,
	`httpResponsePreviewFocus`,
	`httpyac.anyHttpFileOpen`,
	`httpyac.historyEnabled`,
	`httpyac.sessionEnabled`,
	`hubspot.configPath && explorerResourceIsFolder && resource =~ /^(.*)\\.functions(.*)$/`,
	`hubspot.configPath && explorerResourceIsFolder && resource =~ /^(?!.*\\.functions).*$/`,
	`hubspot.configPath && explorerResourceIsFolder`,
	`hubspot.configPath`,
	`hubspot.updateAvailableForCLI`,
	`hubspot.versionChecksComplete && hubspot.terminal.versions.installed.npm && !hubspot.terminal.versions.installed.hs || hubspot.updateAvailableForCLI`,
	`i18n-ally-enabled`,
	`iceworks:projectIsNotTarget`,
	`iceworks:showScriptIconInEditorTitleMenu && iceworks:isAliInternal`,
	`iceworks:showScriptIconInEditorTitleMenu`,
	`igCompExtensionRdy`,
	`iiq.context == BeanShellSelection`,
	`iiq.context == LogConfig`,
	`iiq.context == Rule || iiq.context == Task || iiq.context == ProjectXMLObject || iiq.context = TempXMLObject`,
	`iiq.context == Rule`,
	`iiq.context == Task`,
	`iisexpress:siterunning != true && isWindows`,
	`iisexpress:siterunning && isWindows`,
	`iisexpress:userIsLoggedIn != true && isWindows`,
	`ilspy.backendAvailable`,
	`inAngularProject && !schematicspro`,
	`inArcanistProject && editorTextFocus`,
	`inArcanistProject`,
	`inDebugMode && !focusedSessionIsAttach`,
	`inDebugMode && debugState == 'running'`,
	`inDebugMode && debugType == 'bazis2' && callStackItemType == 'stackFrame'`,
	`inDebugMode && debugType == 'chrome' && callStackItemType == 'stackFrame'`,
	`inDebugMode && debugType == 'cortex-debug' && debugState == stopped &&  cortex-debug:variableUseNaturalFormat`,
	`inDebugMode && debugType == 'cortex-debug' && debugState == stopped && !cortex-debug:variableUseNaturalFormat`,
	`inDebugMode && debugType == 'cppdbg'`,
	`inDebugMode && debugType == 'cppvsdbg'`,
	`inDebugMode && debugType == 'firefox' && callStackItemType == 'stackFrame'`,
	`inDebugMode && debugType == 'firefox'`,
	`inDebugMode && debugType == 'gdb'`,
	`inDebugMode && debugType == 'godot'`,
	`inDebugMode && debugType == 'java'`,
	`inDebugMode && debugType == 'kratos' && kratos.scopeAllowed`,
	`inDebugMode && debugType == 'legacy-chrome' && callStackItemType == 'stackFrame'`,
	`inDebugMode && debugType == 'legacy-node' && callStackItemType == 'stackFrame'`,
	`inDebugMode && debugType == 'lldb'`,
	`inDebugMode && debugType == 'lldb-mi'`,
	`inDebugMode && debugType == 'node2' && callStackItemType == 'stackFrame'`,
	`inDebugMode && debugType == 'nrf-connect'`,
	`inDebugMode && debugType == 'nwjs' && callStackItemType == 'stackFrame'`,
	`inDebugMode && debugType == 'radjav' && callStackItemType == 'stackFrame'`,
	`inDebugMode && debugType == dart && !dart-code:flutter.isInspectingWidget`,
	`inDebugMode && debugType == dart && dart-code:flutter.isInspectingWidget`,
	`inDebugMode && debugType == dart && dart-code:service.reloadSources || dart-code:isInDartDebugSession`,
	`inDebugMode && debugType == dart && dart-code:serviceExtension.ext.flutter.brightnessOverride`,
	`inDebugMode && debugType == dart && dart-code:serviceExtension.ext.flutter.debugAllowBanner`,
	`inDebugMode && debugType == dart && dart-code:serviceExtension.ext.flutter.debugPaintBaselinesEnabled`,
	`inDebugMode && debugType == dart && dart-code:serviceExtension.ext.flutter.debugPaint`,
	`inDebugMode && debugType == dart && dart-code:serviceExtension.ext.flutter.platformOverride`,
	`inDebugMode && debugType == dart && dart-code:serviceExtension.ext.flutter.repaintRainbow`,
	`inDebugMode && debugType == dart && dart-code:serviceExtension.ext.flutter.showPerformanceOverlay`,
	`inDebugMode && debugType == dart && dart-code:serviceExtension.ext.flutter.timeDilation`,
	`inDebugMode && debugType == dart && dart-code:supportsDebugValueFormat && !config.dart.showDebuggerNumbersAsHex`,
	`inDebugMode && debugType == dart && dart-code:supportsDebugValueFormat && config.dart.showDebuggerNumbersAsHex`,
	`inDebugMode && debugType == dart`,
	`inDebugMode && debugType == esdbg && !debug.instructionStep && editorLangId != memory`,
	`inDebugMode && debugType == esdbg && !debug.instructionStep`,
	`inDebugMode && debugType == esdbg && debug.instructionStep && editorLangId != memory`,
	`inDebugMode && debugType == esdbg && debug.instructionStep`,
	`inDebugMode && debugType == esdbg && editorLangId != memory && editorLangId != disassembly`,
	`inDebugMode && debugType == esdbg && editorLangId == memory`,
	`inDebugMode && debugType == esdbg&&view == esprojects && debug.datawatchpoint`,
	`inDebugMode && debugType == esdbg`,
	`inDebugMode && debugType == java && callStackItemType == 'thread'`,
	`inDebugMode && debugType == java && javaHotReload == 'manual' && javaHotReloadOn`,
	`inDebugMode && debugType == scrypt`,
	`inDebugMode && debugType =~ /^cppdbg$|^cspy$|^embedded-debug$|^cortex-debug$/ && vscode-embedded.registerView.hasSvd`,
	`inDebugMode && debugType =~ /^cppdbg$|^cspy$|^embedded-debug$|^cortex-debug$/ && vscode-embedded.rtosViewer.activeRtos != 'unknown'`,
	`inDebugMode && editorLangId == scrypt`,
	`inDebugMode && resourceLangId == asm-collection || inDebugMode && resourceLangId == asm-list-file`,
	`inDebugMode && resourcePath in dezog.disassembler.disasmPath`,
	`inDebugMode && showProgramScriptsExplorer`,
	`inDebugMode && stepIntoTargetsSupported && debugState == 'stopped'`,
	`inDebugMode && view == references`,
	`inDebugMode && view == stackScopes`,
	`inDebugMode&&debugType==by-gdb`,
	`inDebugMode`,
	`inDebugRepl && textInputFocus`,
	`inDebugRepl`,
	`inEditorsPicker && inQuickOpen`,
	`inFileBrowser`,
	`inFileBunny`,
	`inFilesPicker && inQuickOpen`,
	`inFoxContext`,
	`inGitUrlRepo && !inGitUrlDefaultBranch`,
	`inGitUrlRepo`,
	`inInstaSnapshotsProject`,
	`inKeybindings && inKeybindingsSearch`,
	`inKeybindings && keybindingFocus && !inputFocus`,
	`inKeybindings && keybindingFocus && !whenFocus`,
	`inKeybindings && keybindingFocus`,
	`inKeybindings`,
	`inOradewProject && resourceExtname =~ /.sql|.SQL/ && !editorHasSelection`,
	`inOradewProject && resourceExtname =~ /.sql|.SQL/ && editorHasSelection`,
	`inOradewProject && resourceExtname =~ /.sql|.SQL/`,
	`inOradewProject`,
	`inOutput && !config.code-runner.runInTerminal`,
	`inOutput`,
	`inPresentationMode`,
	`inQuickOpen && inRecentFilesPicker`,
	`inQuickOpen && inViewsPicker`,
	`inQuickOpen`,
	`inReferenceSearchEditor && !config.editor.stablePeek`,
	`inReferenceSearchEditor && neovim.mode == normal || referenceSearchVisible`,
	`inReferenceSearchEditor && neovim.mode == normal`,
	`inReferenceSearchEditor || referenceSearchVisible`,
	`inRegionMode`,
	`inRomeProject`,
	`inRustProject`,
	`inSearchEditor && inSearchEditor`,
	`inSearchEditor && inputBoxFocus || inputBoxFocus && searchViewletVisible && !searchInputBoxFocus`,
	`inSearchEditor && inputBoxFocus || inputBoxFocus && searchViewletVisible`,
	`inSearchEditor && searchInputBoxFocus`,
	`inSearchEditor || searchViewletFocus`,
	`inSearchEditor`,
	`inSettingsEditor && !inSettingsJSONEditor && !inSettingsSearch`,
	`inSettingsEditor && inSettingsSearch`,
	`inSettingsEditor && settingRowFocus`,
	`inSettingsEditor && settingsTocRowFocus`,
	`inSettingsEditor`,
	`inSettingsSearch && !suggestWidgetVisible`,
	`inSettingsSearch`,
	`inSkillWorkspace`,
	`inWelcome && activeEditor == 'gettingStartedPage'`,
	`inWolfContext`,
	`inZenMode`,
	`incrementalSearch`,
	`infoPanelFocused \u0026\u0026 !infoPanelLocked`,
	`infoPanelFocused \u0026\u0026 infoPanelLocked`,
	`infracost:active && !infracost:loggedIn`,
	`infracost:active && infracost:loggedIn`,
	`inlineSuggestionHasIndentationLessThanTabSize && inlineSuggestionVisible && !editorTabMovesFocus`,
	`inlineSuggestionVisible && !editorReadonly && CODEWHISPERER_ENABLED`,
	`inlineSuggestionVisible && !editorReadonly`,
	`inlineSuggestionVisible && !editorTabMovesFocus && !inlineSuggestionHasIndentation`,
	`inlineSuggestionVisible`,
	`inlineSvgPanelActive`,
	`inlineSvgPanelOpen && !inlineSvgPanelActive`,
	`inputBoxFocus && searchViewletVisible && !searchInputBoxFocus`,
	`inputBoxFocus && searchViewletVisible`,
	`inputFocus && !isMac || editorFocus && !isMac`,
	`inputFocus && !textInputFocus`,
	`inputFocus && notebookEditorFocused && !editorHasMultipleSelections && !editorHasSelection && !editorHoverVisible && notebookCellType == 'markup'`,
	`inputFocus && notebookEditorFocused && !editorHasMultipleSelections && !editorHasSelection && !editorHoverVisible`,
	`inputFocus && notebookEditorFocused && !editorHasSelection && !editorHoverVisible && vim.active && vim.mode == 'Normal'`,
	`inputFocus && notebookEditorFocused && notebookCellType == 'markup'`,
	`inputFocus || editorFocus`,
	`inputFocus`,
	`inspectedProject == false && isAnonymous == false && noProjectFound == false`,
	`interactivePlaygroundFocus && !editorTextFocus`,
	`interfaceOverviewVisible`,
	`ipython.isUse && !inDebugMode && editorTextFocus && !findInputFocussed && !replaceInputFocussed && editorLangId == python`,
	`ipython.isUse && !inDebugMode && editorTextFocus && !notebookEditorFocused && editorLangId == python`,
	`ipython.isUse && !inDebugMode && editorTextFocus && editorLangId == python`,
	`ipython.isUse && editorTextFocus && editorLangId == python`,
	`is-running-storybooks-vscode`,
	`isActiveDevServer && !explorerResourceIsFolder && !listMultiSelection`,
	`isActiveDevServer && !explorerResourceIsFolder`,
	`isActiveDevServer && explorerResourceIsFolder && !listMultiSelection && resourcePath in ext.path`,
	`isActiveDevServer`,
	`isActive`,
	`isAgfProject`,
	`isAngularProject`,
	`isAngularWorkspace`,
	`isAnonymous == true && isLoggingIn == false`,
	`isAnonymous == true && isLoggingIn == true`,
	`isAppcanProject && !inDebugMode && resourceFilename != dist && resourceExtname != .zip && 1===2`,
	`isAppcanProject && !inDebugMode && resourceFilename != dist && resourceExtname != .zip`,
	`isAppcanProject && inDebugMode && resourceFilename != dist && resourceExtname != .zip`,
	`isAppcanProject && resourceFilename == dist && !inDebugMode`,
	`isAuthed`,
	`isAzureAccountInstalled`,
	`isBallerinaDiagram`,
	`isBlitzDebugMode`,
	`isBlitzRedoable`,
	`isBlitzUndoable`,
	`isBlueprintsEnabled && resourceFilename == Kptfile`,
	`isCargoProject`,
	`isChoreoAuthEnabled`,
	`isControllersListReceived && isBarExpanded`,
	`isCordovaDebugging && debugType == 'pwa-chrome' || isCordovaDebugging && debugType == 'pwa-node' || isCordovaDebugging && debugType == 'cordova'`,
	`isDevelopment`,
	`isDocumentScanedWithClairvoyant`,
	`isEditorEnabled`,
	`isExtensionReady && resourcePath =~ .*/.circleci/.* && resourceLangId == yaml && configIsValid == false`,
	`isExtensionReady && resourcePath =~ .*/.circleci/.* && resourceLangId == yaml && configIsValid == true`,
	`isFileSystemResource && editorFocus`,
	`isFileSystemResource && editorLangId == ansible`,
	`isFileSystemResource && resourceExtname == .avm`,
	`isFileSystemResource && resourceExtname == .nef`,
	`isFileSystemResource && resourceLangId == ansible`,
	`isFileSystemResource == true`,
	`isFileSystemResource`,
	`isForm == true`,
	`isInDiffEditor && !editorReadonly && resourceFilename=~ /.*\\.ros\\.json|.*\\.ros\\.yaml/`,
	`isInDiffEditor && perforce.activation.hasScmProvider && config.perforce.editorButtons.diffPrevAndNext =~ /(All|Only)/`,
	`isInDiffEditor && perforce.activation.hasScmProvider || resourceScheme == perforce`,
	`isInDiffEditor && perforce.activation.hasScmProvider`,
	`isInDiffEditor && perforce.currentFile.resourceRevision != 1 && !perforce.currentFile.isRightDiffOnRev2`,
	`isInDiffEditor && resourceDirname in e4e.editedFolders && isWeb != true`,
	`isInDiffEditor && resourceScheme == git-graph && git-graph:codiconsSupported`,
	`isInDiffEditor || editorIsOpen && resourceFilename =~ /\\.snap(\\.new)?$/`,
	`isInDiffEditor`,
	`isInsiders`,
	`isKFFProject`,
	`isLinux && config.docwriter.hotkey.window == 'Alt + .'`,
	`isLinux && config.docwriter.hotkey.window == 'Ctrl + .'`,
	`isLinux && editorTextFocus && editorLangId =~ /dart/ && config.Insider.hotkey.window == 'Alt + .'`,
	`isLinux && editorTextFocus && editorLangId =~ /dart/ && config.Insider.hotkey.window == 'Ctrl + .'`,
	`isLinux && editorTextFocus && editorLangId =~ /typescript|javascript|python|php|java|c|cpp|csharp|dart|ruby|go|rust/ && config.docwriter.hotkey.window == 'Alt + .'`,
	`isLinux && editorTextFocus && editorLangId =~ /typescript|javascript|python|php|java|c|cpp|csharp|dart|ruby|go|rust/ && config.docwriter.hotkey.window == 'Ctrl + .'`,
	`isLinux && isWeb || isLinux`,
	`isLinux && view == l13DiffFavorites && viewItem =~ /favorite$/`,
	`isLinux && view == l13DiffHistory && viewItem =~ /^history(-file|-folder)?$/`,
	`isLinux && view =~ /^l13Projects(Favorites|Workspaces)$/ && viewItem =~ /^((current-)?(favorite|project|subfavorite|subproject)-(folders?|git|subfolder|vscode|workspace)|unknown-project)$/`,
	`isLinux || isWindows || isMac`,
	`isLinux || isWindows`,
	`isLinux`,
	`isMac && !editorIsOpen && !multipleEditorGroups && !config.quitControl.closeTabShouldTryToCloseEmptyWindow`,
	`isMac && config.docwriter.hotkey.mac == '⌘ + .'`,
	`isMac && config.docwriter.hotkey.mac == '⌥ + .'`,
	`isMac && editorLangId == nextbasic`,
	`isMac && editorTextFocus && editorLangId =~ /dart/ && config.Insider.hotkey.mac == '⌘ + .'`,
	`isMac && editorTextFocus && editorLangId =~ /dart/ && config.Insider.hotkey.mac == '⌥ + .'`,
	`isMac && editorTextFocus && editorLangId =~ /erb/`,
	`isMac && editorTextFocus && editorLangId =~ /typescript|javascript|python|php|java|c|cpp|csharp|dart|ruby|go|rust/ && config.docwriter.hotkey.mac == '⌘ + .'`,
	`isMac && editorTextFocus && editorLangId =~ /typescript|javascript|python|php|java|c|cpp|csharp|dart|ruby|go|rust/ && config.docwriter.hotkey.mac == '⌥ + .'`,
	`isMac && editorTextFocus`,
	`isMac && resourceDirname =~ //doc/prj$/ && explorerResourceIsFolder`,
	`isMac && resourceDirname =~ //doc/prj$/ && resourceFilename == prj.json`,
	`isMac && view == l13DiffFavorites && viewItem =~ /favorite$/`,
	`isMac && view == l13DiffHistory && viewItem =~ /^history(-file|-folder)?$/`,
	`isMac && view =~ /^l13Projects(Favorites|Workspaces)$/ && viewItem =~ /^((current-)?(favorite|project|subfavorite|subproject)-(folders?|git|subfolder|vscode|workspace)|unknown-project)$/`,
	`isMac && view in ext:allFavoriteViews && viewItem =~ /^resource/`,
	`isMac || isWindows`,
	`isMacNative && openFolderWorkspaceSupport`,
	`isMac`,
	`isMaterial && resourcePath =~ /blocks[\\\\|\/][^\\\\|\/]+$/ || isMaterial && resourcePath =~ /components[\\\\|\/][^\\\\|\/]+$/ || isMaterial && resourcePath =~ /pages[\\\\|\/][^\\\\|\/]+$/ || isMaterial && resourcePath =~ /scaffolds[\\\\|\/][^\\\\|\/]+$/`,
	`isMaterial`,
	`isMonoRepo == true`,
	`isNotebookVariableViewEnabled`,
	`isNxWorkspace && config.nxConsole.enableGenerateFromContextMenu`,
	`isNxWorkspace && explorerResourceIsFolder && resourcePath in nxAppsDir && config.nxConsole.enableGenerateFromContextMenu && nx.hasApplicationGenerators`,
	`isNxWorkspace && explorerResourceIsFolder && resourcePath in nxLibsDir && config.nxConsole.enableGenerateFromContextMenu && nx.hasLibraryGenerators`,
	`isNxWorkspace && nx.hasApplicationGenerators`,
	`isNxWorkspace && nx.hasLibraryGenerators`,
	`isNxWorkspace || isAngularWorkspace`,
	`isNxWorkspace`,
	`isOutputLoggingEnabled && !config.cloudcode.beta.enableOneActivityBar`,
	`isOutputLoggingEnabled && config.cloudcode.beta.enableOneActivityBar`,
	`isOutputLoggingEnabled`,
	`isParticleProject && resourceLangId == cpp`,
	`isParticleProject`,
	`isPerfadvisorConfigured && !isPerfadvisorLoggedIn && !refreshFailure`,
	`isPerfadvisorConfigured && !isPerfadvisorLoggedIn`,
	`isPerfadvisorConfigured && isPerfadvisorLoggedIn && !refreshFailure`,
	`isPerfadvisorConfigured && isPerfadvisorLoggedIn`,
	`isPerfadvisorConfigured && refreshFailure`,
	`isPerfadvisorConfigured`,
	`isProjectInit && !isProjectViewInit && resourcePath =~ /src(/|\\\\)views(/|\\\\)pages$/`,
	`isProjectInit && !isProjectViewInit`,
	`isProjectOpened && !isBarExpanded`,
	`isProjectOpened && isBarExpanded`,
	`isProjectOpened`,
	`isProjectProviderAvailable`,
	`isProjectViewInit && view == qpProjectTree && appType == project`,
	`isProjectViewInit && view == qpProjectTree`,
	`isPubspecYamlExist`,
	`isRNProject && config.react-native-tools.displayDebuggingCommandsButton`,
	`isRNProject && isRNAndroidHermesProject && config.react-native-tools.displayDebuggingCommandsButton`,
	`isRNProject && isRNHermesProject && config.react-native-tools.displayDebuggingCommandsButton`,
	`isRNProject && isRNIosHermesProject && config.react-native-tools.displayDebuggingCommandsButton`,
	`isRNProject && isRNMacOSProject && config.react-native-tools.displayDebuggingCommandsButton`,
	`isRNProject && isRNMacosHermesProject && config.react-native-tools.displayDebuggingCommandsButton`,
	`isRNProject && isRNWindowsHermesProject && config.react-native-tools.displayDebuggingCommandsButton`,
	`isRNProject && isRNWindowsProject && config.react-native-tools.displayDebuggingCommandsButton`,
	`isReadingLineWithInlayHints`,
	`isRefresh`,
	`isSkaffolderProject && !isYamlParsable`,
	`isSkaffolderProject && isYamlParsable`,
	`isSkaffolderProject && view==skaffolderExplorerHeader`,
	`isSkaffolderProject && view==skaffolderExplorerPage`,
	`isSkaffolderProject`,
	`isStencilProject && resourceExtname == .tsx`,
	`isStencilProject`,
	`isTransmitEnabled && explorerResourceIsFolder`,
	`isTransmitEnabled`,
	`isTreeEmpty`,
	`isWPILibProject`,
	`isWPILibProvidedCpp`,
	`isWeb && !remoteName && !remoteHub:hasVirtualFolders && !debuggersAvailable`,
	`isWeb && !remoteName && !remoteHub:hasVirtualFolders`,
	`isWeb && !remoteName`,
	`isWeb && remoteHub:hasVirtualFolders && !isMobile`,
	`isWeb && remoteHub:hasVirtualFolders`,
	`isWeb && viewItem =~ /remoteHub:viewItem:workspaceRepository\\b(?=.*?\\b\\+active\\b)/`,
	`isWeb && virtualWorkspace`,
	`isWeb`,
	`isWindows && !wsl.noWSLPath && workspaceFolderCount != 0 && !virtualWorkspace && remoteName != wsl`,
	`isWindows && config.UVCH.Global.UseToolbar`,
	`isWindows && config.docwriter.hotkey.windows == 'Alt + .'`,
	`isWindows && config.docwriter.hotkey.windows == 'Ctrl + .'`,
	`isWindows && config.emacs-mcx.cursorMoveOnFindWidget && findInputFocussed && !isComposing`,
	`isWindows && config.terminal.explorerKind == 'integrated'`,
	`isWindows && config.windowsTerminal.explorer.showOpen && config.terminal.explorerKind == 'integrated'`,
	`isWindows && config.windowsTerminal.explorer.showOpenWithProfile`,
	`isWindows && editorTextFocus && editorLangId =~ /dart/ && config.Insider.hotkey.windows == 'Alt + .'`,
	`isWindows && editorTextFocus && editorLangId =~ /dart/ && config.Insider.hotkey.windows == 'Ctrl + .'`,
	`isWindows && editorTextFocus && editorLangId =~ /typescript|javascript|python|php|java|c|cpp|csharp|dart|ruby|go|rust/ && config.docwriter.hotkey.windows == 'Alt + .'`,
	`isWindows && editorTextFocus && editorLangId =~ /typescript|javascript|python|php|java|c|cpp|csharp|dart|ruby|go|rust/ && config.docwriter.hotkey.windows == 'Ctrl + .'`,
	`isWindows && editorTextFocus`,
	`isWindows && isFileSystemResource`,
	`isWindows && processExecutionSupported`,
	`isWindows && remoteName == wsl && !wsl.noWSLPath && workspaceFolderCount != 0`,
	`isWindows && remoteName == wsl && !wsl.noWSLPath`,
	`isWindows && replaceInputFocussed && !isComposing`,
	`isWindows && resourceDirname =~ /\\\\doc\\\\prj$/ && explorerResourceIsFolder`,
	`isWindows && resourceDirname =~ /\\\\doc\\\\prj$/ && resourceFilename == prj.json`,
	`isWindows && resourceExtname == .nlp`,
	`isWindows && resourcePath =~ /apps/`,
	`isWindows && scmProvider == git`,
	`isWindows && view == iisexpress.controls`,
	`isWindows && view == l13DiffFavorites && viewItem =~ /favorite$/`,
	`isWindows && view == l13DiffHistory && viewItem =~ /^history(-file|-folder)?$/`,
	`isWindows && view =~ /^l13Projects(Favorites|Workspaces)$/ && viewItem =~ /^((current-)?(favorite|project|subfavorite|subproject)-(folders?|git|subfolder|vscode|workspace)|unknown-project)$/`,
	`isWindows && view in ext:allFavoriteViews && viewItem =~ /^resource/`,
	`isWindows && workspaceFolderCount != 0`,
	`isWindows && wslFeatureInstalled`,
	`isWindows || isMac || isWeb && !isLinux`,
	`isWindows`,
	`isWordPressSuite && !hasWordPressSuiteRoot`,
	`isWordPressSuite`,
	`isWorkspaceTrusted && config.notebook.kernelPicker.type != mru`,
	`isWorkspaceTrusted && editorLangId == dockercompose`,
	`isWorkspaceTrusted && editorLangId == dockerfile && isAzureAccountInstalled`,
	`isWorkspaceTrusted && editorLangId == dockerfile`,
	`isWorkspaceTrusted && editorLangId == yaml && isAzureAccountInstalled`,
	`isWorkspaceTrusted && isKFFProject`,
	`isWorkspaceTrusted && notebookKernel =~ /^ms-toolsai.jupyter\\// || isWorkspaceTrusted && !notebookKernel`,
	`isWorkspaceTrusted && resourceFilename =~ /dockerfile/i && isAzureAccountInstalled`,
	`isWorkspaceTrusted && resourceLangId == dockercompose`,
	`isWorkspaceTrusted && resourceLangId == dockerfile`,
	`isWorkspaceTrusted && resourceLangId == yaml && isAzureAccountInstalled`,
	`isWorkspaceTrusted && titanium:enabled`,
	`isWorkspaceTrusted == true`,
	`isWorkspaceTrusted`,
	`java:needSetupTests`,
	`java:noJavaProjects`,
	`java:projectManagerActivated && !java:languageSupportInstalled`,
	`java:projectManagerActivated && workspaceFolderCount > 0`,
	`java:projectManagerActivated`,
	`java:reloadProjectActive && javaLSReady`,
	`java:serverMode != LightWeight`,
	`java:serverMode && spring.memoryGraphs:showMode == defined && spring.memoryGraphs:hasLiveProcess == true`,
	`java:serverMode == LightWeight`,
	`java:serverMode == Standard && !java:noJavaProjects`,
	`java:serverMode == Standard && focusedView == javaProjectExplorer && !explorerResourceMoveableToTrash`,
	`java:serverMode == Standard && focusedView == javaProjectExplorer && explorerResourceMoveableToTrash`,
	`java:serverMode == Standard && focusedView == javaProjectExplorer`,
	`java:serverMode || workbenchState==empty`,
	`java:showDeprecatedTasks`,
	`java:testRunnerActivated && resourceExtname == .java`,
	`java:testRunnerActivated && resourcePath =~ /.*src[/|\\\\]main[/|\\\\]java[/|\\\\].*\\.java/`,
	`java:testRunnerActivated && resourcePath =~ /.*src[/|\\\\]test[/|\\\\]java[/|\\\\].*\\.java/`,
	`javaLSReady && editorIsOpen`,
	`javaLSReady && editorLangId == java`,
	`javaLSReady && editorTextFocus && editorLangId == java`,
	`javaLSReady && explorerResourceIsFolder`,
	`javaLSReady`,
	`javascript-repl.expl && javascript-repl.started`,
	`javascript-repl.started`,
	`jbspaceViewsConfig.showWelcome == false`,
	`jbspaceViewsConfig.showWelcome == true`,
	`jekyll-run&&!isRunning&&!isBuilding`,
	`jekyll-run&&isRunning&&!isBuilding`,
	`jest:never`,
	`jest:run.interactive && editorLangId =~ /(javascript|javascriptreact|typescript|typescriptreact|vue)/ `,
	`jest:run.interactive && workspaceFolderCount > 1`,
	`jest:run.interactive`,
	`jestRunItActive`,
	`jfrog.connection.status == connectionLost `,
	`jfrog.connection.status == signedOut || !jfrog.connection.status`,
	`journal-tree-has-content && vscode-journal-view-in-explorer`,
	`jsDebugIsMapSteppingDisabled`,
	`jsProfileVisualizer.hasCodeLenses == true`,
	`jsmmActive`,
	`jsonOutlineEnabled`,
	`jsonTreeViewEnabled`,
	`julia.hasREPL`,
	`julia.isActive`,
	`julia.isEvaluating`,
	`julia.plotpaneFocus && !config.julia.usePlotPane`,
	`julia.plotpaneFocus && config.julia.usePlotPane`,
	`julia.plotpaneFocus`,
	`julia.profilerFocus`,
	`juliaDocumentationPaneActive`,
	`jumpy2.jump-mode && editorTextFocus`,
	`jupyter.dataViewerActive`,
	`jupyter.development && isWorkspaceTrusted`,
	`jupyter.document.hasCodeCells`,
	`jupyter.hasNativeNotebookOrInteractiveWindowOpen`,
	`jupyter.hascodecells && !notebookEditorFocused`,
	`jupyter.hascodecells && jupyter.ispythonorinteractiveeactive && !notebookEditorFocused && isWorkspaceTrusted`,
	`jupyter.hascodecells && jupyter.ispythonornativeactive && !jupyter.havenativecells && isWorkspaceTrusted`,
	`jupyter.hascodecells && jupyter.ispythonornativeactive && isWorkspaceTrusted`,
	`jupyter.hascodecells && jupyter.ispythonornativeactive`,
	`jupyter.havenativecells && jupyter.isnativeactive && isWorkspaceTrusted`,
	`jupyter.isinteractiveactive`,
	`jupyter.isnativeactive && isWorkspaceTrusted && jupyter.ispythonnotebook && isWorkspaceTrusted`,
	`jupyter.isnativeactive && isWorkspaceTrusted`,
	`jupyter.ispythonorinteractiveeactive && isWorkspaceTrusted`,
	`jupyter.showInstallPythonCommand && config.notebook.kernelPicker.type != mru`,
	`jupyter.showInstallPythonExtensionCommand && config.notebook.kernelPicker.type != mru`,
	`jupyter.showingLocalOrWebEmpty && config.notebook.kernelPicker.type != mru`,
	`karatelabs-karate.signedIn && activeWebviewPanelId != 'karate-import' && resourceExtname != '.feature'`,
	`karatelabs-karate.signedIn && explorerResourceIsFolder || karatelabs-karate.signedIn && resourceExtname == '.feature'`,
	`karatelabs-karate.signedIn && resourceFilename =~ /.+\\.(feature|js)/`,
	`karatelabs-karate.signedIn`,
	`karavan:loaded`,
	`kb-macro.active && !activeEditorGroupEmpty`,
	`kb-macro.active && !activeEditorIsPinned`,
	`kb-macro.active && !config.breadcrumbs.enabled`,
	`kb-macro.active && !debuggersAvailable`,
	`kb-macro.active && !editorFocus && activeEditor == 'workbench.editor.extension'`,
	`kb-macro.active && !editorFocus`,
	`kb-macro.active && !editorIsOpen && !multipleEditorGroups`,
	`kb-macro.active && !inDebugMode && activeViewlet == 'workbench.view.debug' && debugConfigurationType == 'node' || kb-macro.active && !inDebugMode && activeViewlet == 'workbench.view.debug' && debugConfigurationType == 'pwa-extensionHost' || kb-macro.active && !inDebugMode && activeViewlet == 'workbench.view.debug' && debugConfigurationType == 'pwa-node'`,
	`kb-macro.active && !inDebugMode && debugConfigurationType == 'node' || kb-macro.active && !inDebugMode && debugConfigurationType == 'pwa-extensionHost' || kb-macro.active && !inDebugMode && debugConfigurationType == 'pwa-node'`,
	`kb-macro.active && !isIOS`,
	`kb-macro.active && !isLinux && editorHasDefinitionProvider && editorTextFocus && !inReferenceSearchEditor && !isInEmbeddedEditor`,
	`kb-macro.active && !isLinux && editorHasDocumentFormattingProvider && editorTextFocus && !editorReadonly && !inCompositeEditor`,
	`kb-macro.active && !isLinux && editorHasDocumentFormattingProvider && editorTextFocus && inCompositeEditor && notebookEditable && !editorReadonly && activeEditor == 'workbench.editor.notebook'`,
	`kb-macro.active && !isLinux && editorTextFocus && !editorHasDocumentFormattingProvider && !editorReadonly`,
	`kb-macro.active && !isLinux && editorTextFocus && !editorReadonly`,
	`kb-macro.active && !isLinux && inQuickOpen && inViewsPicker`,
	`kb-macro.active && !isLinux && notebookEditable && !editorTextFocus && activeEditor == 'workbench.editor.notebook'`,
	`kb-macro.active && !isLinux && resourceScheme == 'vscode-interactive'`,
	`kb-macro.active && !isLinux`,
	`kb-macro.active && !isMac && breadcrumbsActive && breadcrumbsVisible && listFocus && !inputFocus`,
	`kb-macro.active && !isMac && breadcrumbsActive && breadcrumbsVisible`,
	`kb-macro.active && !isMac && breakpointsFocused`,
	`kb-macro.active && !isMac && commentEditorFocused`,
	`kb-macro.active && !isMac && commentsFilterFocus`,
	`kb-macro.active && !isMac && editorTextFocus && !editorReadonly`,
	`kb-macro.active && !isMac && explorerViewletVisible && filesExplorerFocus && !explorerResourceIsFolder && !inputFocus`,
	`kb-macro.active && !isMac && explorerViewletVisible && filesExplorerFocus && !explorerResourceIsRoot && !explorerResourceReadonly && !inputFocus`,
	`kb-macro.active && !isMac && inFilesPicker && inQuickOpen`,
	`kb-macro.active && !isMac && inKeybindings && inKeybindingsSearch`,
	`kb-macro.active && !isMac && listFocus && !inputFocus`,
	`kb-macro.active && !isMac && notebookCellListFocused && !inputFocus`,
	`kb-macro.active && !isMac && openFolderWorkspaceSupport`,
	`kb-macro.active && !isMac && remoteFileDialogVisible`,
	`kb-macro.active && !isMac && scmRepository`,
	`kb-macro.active && !isMac && searchViewletFocus`,
	`kb-macro.active && !isMac && suggestWidgetMultipleSuggestions && suggestWidgetVisible && textInputFocus`,
	`kb-macro.active && !isMac && terminalFocus && terminalHasBeenCreated && terminalTextSelected || kb-macro.active && !isMac && terminalFocus && terminalProcessSupported && terminalTextSelected`,
	`kb-macro.active && !isMac && terminalFocus && terminalHasBeenCreated || kb-macro.active && !isMac && terminalFocus && terminalProcessSupported`,
	`kb-macro.active && !isMac && terminalFocus`,
	`kb-macro.active && !isMac && terminalHasBeenCreated && terminalTabsFocus && terminalTabsSingularSelection || kb-macro.active && !isMac && terminalProcessSupported && terminalTabsFocus && terminalTabsSingularSelection`,
	`kb-macro.active && !isMac && textInputFocus && !editorReadonly`,
	`kb-macro.active && !isMac && textInputFocus`,
	`kb-macro.active && !isMac && true`,
	`kb-macro.active && !isMac && tunnelViewFocus && tunnelType == 'Forwarded' && tunnelViewMultiSelection == 'undefined'`,
	`kb-macro.active && !isMac && variablesFocused`,
	`kb-macro.active && !isMac && watchExpressionsFocused`,
	`kb-macro.active && !isMac`,
	`kb-macro.active && !isWindows && canNavigateForward`,
	`kb-macro.active && !kb-macro.headOfLine && isWindows`,
	`kb-macro.active && !kb-macro.headOfLine`,
	`kb-macro.active && !notebookEditorFocused && editorLangId == 'markdown'`,
	`kb-macro.active && !notebookEditorFocused`,
	`kb-macro.active && !openFolderWorkspaceSupport && workbenchState == 'workspace'`,
	`kb-macro.active && !suggestWidgetVisible && resourceScheme == 'vscode-interactive' && interactiveInputCursorAtBoundary != 'bottom' && interactiveInputCursorAtBoundary != 'none'`,
	`kb-macro.active && !suggestWidgetVisible && resourceScheme == 'vscode-interactive' && interactiveInputCursorAtBoundary != 'none' && interactiveInputCursorAtBoundary != 'top'`,
	`kb-macro.active && !terminalFocus`,
	`kb-macro.active && LinkedEditingInputVisible && editorTextFocus`,
	`kb-macro.active && acceptSuggestionOnEnter && suggestWidgetHasFocusedSuggestion && suggestWidgetVisible && suggestionMakesTextEdit && textInputFocus`,
	`kb-macro.active && accessibilityHelpWidgetVisible && editorFocus`,
	`kb-macro.active && accessibilityModeEnabled && terminalA11yTreeFocus && terminalNavigationModeActive && terminalProcessSupported || kb-macro.active && accessibilityModeEnabled && terminalFocus && terminalNavigationModeActive && terminalProcessSupported`,
	`kb-macro.active && accessibilityModeEnabled && terminalA11yTreeFocus && terminalProcessSupported || kb-macro.active && accessibilityModeEnabled && terminalFocus && terminalProcessSupported`,
	`kb-macro.active && accessibilityModeEnabled && terminalA11yTreeFocus && terminalProcessSupported`,
	`kb-macro.active && activeEditorCanSplitInGroup`,
	`kb-macro.active && activeEditorGroupEmpty && multipleEditorGroups`,
	`kb-macro.active && activeEditorIsPinned`,
	`kb-macro.active && atEndOfWord && textInputFocus && !hasOtherSuggestions && !inSnippetMode && !suggestWidgetVisible && config.editor.tabCompletion == 'on'`,
	`kb-macro.active && bannerFocused`,
	`kb-macro.active && breadcrumbsActive && breadcrumbsVisible && listFocus && !inputFocus`,
	`kb-macro.active && breadcrumbsActive && breadcrumbsVisible`,
	`kb-macro.active && breadcrumbsPossible && breadcrumbsVisible`,
	`kb-macro.active && breakpointWidgetVisible && inBreakpointWidget`,
	`kb-macro.active && breakpointWidgetVisible && textInputFocus`,
	`kb-macro.active && breakpointsFocused && !breakpointInputFocused`,
	`kb-macro.active && breakpointsFocused && !inputFocus`,
	`kb-macro.active && breakpointsFocused`,
	`kb-macro.active && callHierarchyVisible && !config.editor.stablePeek`,
	`kb-macro.active && callHierarchyVisible && callHierarchyDirection == 'incomingCalls'`,
	`kb-macro.active && callHierarchyVisible && callHierarchyDirection == 'outgoingCalls'`,
	`kb-macro.active && callHierarchyVisible`,
	`kb-macro.active && cancellableOperation`,
	`kb-macro.active && codeActionMenuVisible`,
	`kb-macro.active && commentEditorFocused`,
	`kb-macro.active && commentsFilterFocus`,
	`kb-macro.active && config.editor.rename.enablePreview && editorFocus && renameInputVisible`,
	`kb-macro.active && config.emmet.triggerExpansionOnTab && editorTextFocus && !editorReadonly && !editorTabMovesFocus`,
	`kb-macro.active && config.notebook.navigation.allowNavigateToSurroundingCells && editorTextFocus && inputFocus && notebookEditorFocused && notebookEditorCursorAtBoundary != 'bottom' && notebookEditorCursorAtBoundary != 'none'`,
	`kb-macro.active && config.notebook.navigation.allowNavigateToSurroundingCells && editorTextFocus && inputFocus && notebookEditorFocused && notebookEditorCursorAtBoundary != 'none' && notebookEditorCursorAtBoundary != 'top'`,
	`kb-macro.active && config.notebook.navigation.allowNavigateToSurroundingCells && notebookCursorNavigationMode && notebookEditorFocused && !notebookCellMarkdownEditMode && notebookCellType == 'markup'`,
	`kb-macro.active && customTreeView && listFocus && !inputFocus`,
	`kb-macro.active && debugState != 'inactive'`,
	`kb-macro.active && debugState == 'running'`,
	`kb-macro.active && debugState == 'stopped'`,
	`kb-macro.active && debuggersAvailable && debugState != 'initializing'`,
	`kb-macro.active && debuggersAvailable && debugState == 'inactive'`,
	`kb-macro.active && debuggersAvailable && editorTextFocus`,
	`kb-macro.active && dirtyDiffVisible`,
	`kb-macro.active && editorColumnSelection && textInputFocus`,
	`kb-macro.active && editorFocus && findInputFocussed`,
	`kb-macro.active && editorFocus && findWidgetVisible && !isComposing`,
	`kb-macro.active && editorFocus && findWidgetVisible && replaceInputFocussed`,
	`kb-macro.active && editorFocus && findWidgetVisible`,
	`kb-macro.active && editorFocus && markersNavigationVisible`,
	`kb-macro.active && editorFocus && parameterHintsMultipleSignatures && parameterHintsVisible`,
	`kb-macro.active && editorFocus && parameterHintsVisible`,
	`kb-macro.active && editorFocus && renameInputVisible`,
	`kb-macro.active && editorFocus && testing.isPeekVisible`,
	`kb-macro.active && editorFocus || kb-macro.active && editorIsOpen`,
	`kb-macro.active && editorFocus`,
	`kb-macro.active && editorHasCallHierarchyProvider && editorTextFocus && !inReferenceSearchEditor`,
	`kb-macro.active && editorHasCallHierarchyProvider`,
	`kb-macro.active && editorHasCodeActionsProvider && editorTextFocus && !editorReadonly`,
	`kb-macro.active && editorHasCompletionItemProvider && textInputFocus && !editorReadonly`,
	`kb-macro.active && editorHasDefinitionProvider && editorTextFocus && !isInEmbeddedEditor`,
	`kb-macro.active && editorHasDocumentSelectionFormattingProvider && editorTextFocus && !editorReadonly`,
	`kb-macro.active && editorHasImplementationProvider && editorTextFocus && !inReferenceSearchEditor && !isInEmbeddedEditor`,
	`kb-macro.active && editorHasImplementationProvider && editorTextFocus && !isInEmbeddedEditor`,
	`kb-macro.active && editorHasMultipleSelections && textInputFocus`,
	`kb-macro.active && editorHasReferenceProvider && editorTextFocus && !inReferenceSearchEditor && !isInEmbeddedEditor`,
	`kb-macro.active && editorHasReferenceProvider`,
	`kb-macro.active && editorHasRenameProvider && editorTextFocus && !editorReadonly`,
	`kb-macro.active && editorHasSelection && textInputFocus`,
	`kb-macro.active && editorHasSignatureHelpProvider && editorTextFocus`,
	`kb-macro.active && editorTextFocus && !editorReadonly && !editorTabMovesFocus`,
	`kb-macro.active && editorTextFocus && !editorReadonly && !suggestWidgetVisible && !renameInputVisible && !codeActionMenuVisible`,
	`kb-macro.active && editorTextFocus && !editorReadonly && editorLangId == 'jsonc'`,
	`kb-macro.active && editorTextFocus && !editorReadonly && supportedCodeAction =~ /(\\s|^)quickfix\\b/`,
	`kb-macro.active && editorTextFocus && !editorReadonly && supportedCodeAction =~ /(\\s|^)source\\.organizeImports\\b/`,
	`kb-macro.active && editorTextFocus && !editorReadonly`,
	`kb-macro.active && editorTextFocus && !notebookEditable`,
	`kb-macro.active && editorTextFocus && !textCompareEditorActive`,
	`kb-macro.active && editorTextFocus && foldingEnabled`,
	`kb-macro.active && editorTextFocus && hasNextTabstop && inSnippetMode`,
	`kb-macro.active && editorTextFocus && hasPrevTabstop && inSnippetMode`,
	`kb-macro.active && editorTextFocus && hasSnippetCompletions && !editorTabMovesFocus && !inSnippetMode`,
	`kb-macro.active && editorTextFocus && hasWordHighlights`,
	`kb-macro.active && editorTextFocus && inDebugMode`,
	`kb-macro.active && editorTextFocus && inSnippetMode`,
	`kb-macro.active && editorTextFocus && inputFocus && notebookEditorFocused`,
	`kb-macro.active && editorTextFocus && selectionAnchorSet`,
	`kb-macro.active && editorTextFocus`,
	`kb-macro.active && emptyWorkspaceSupport && workbenchState != 'empty'`,
	`kb-macro.active && emptyWorkspaceSupport`,
	`kb-macro.active && exceptionWidgetVisible`,
	`kb-macro.active && explorerResourceCut && explorerViewletVisible && filesExplorerFocus && !inputFocus`,
	`kb-macro.active && explorerResourceIsFolder && explorerViewletVisible && filesExplorerFocus && !inputFocus`,
	`kb-macro.active && explorerResourceMoveableToTrash && explorerViewletVisible && filesExplorerFocus && !explorerResourceReadonly && !inputFocus`,
	`kb-macro.active && explorerViewletCompressedFocus && explorerViewletVisible && filesExplorerFocus && !explorerViewletCompressedFirstFocus && !inputFocus`,
	`kb-macro.active && explorerViewletCompressedFocus && explorerViewletVisible && filesExplorerFocus && !explorerViewletCompressedLastFocus && !inputFocus`,
	`kb-macro.active && explorerViewletFocus && explorerViewletVisible && !inputFocus`,
	`kb-macro.active && explorerViewletVisible && filesExplorerFocus && !explorerResourceIsFolder && !inputFocus`,
	`kb-macro.active && explorerViewletVisible && filesExplorerFocus && !explorerResourceIsRoot && !explorerResourceReadonly && !inputFocus`,
	`kb-macro.active && explorerViewletVisible && filesExplorerFocus && !explorerResourceIsRoot && !inputFocus`,
	`kb-macro.active && explorerViewletVisible && filesExplorerFocus && !explorerResourceMoveableToTrash && !explorerResourceReadonly && !inputFocus`,
	`kb-macro.active && explorerViewletVisible && filesExplorerFocus && !explorerResourceReadonly && !inputFocus`,
	`kb-macro.active && false`,
	`kb-macro.active && fileMatchFocus && replaceActive && searchViewletVisible`,
	`kb-macro.active && fileMatchOrFolderMatchWithResourceFocus`,
	`kb-macro.active && fileMatchOrMatchFocus && searchViewletVisible`,
	`kb-macro.active && fileMatchOrMatchFocus`,
	`kb-macro.active && firstMatchFocus && searchViewletVisible`,
	`kb-macro.active && focusedSessionIsAttach && inDebugMode`,
	`kb-macro.active && focusedView != ''`,
	`kb-macro.active && focusedView == 'workbench.panel.comments'`,
	`kb-macro.active && focusedView == 'workbench.panel.markers.view'`,
	`kb-macro.active && focusedView == 'workbench.view.testing'`,
	`kb-macro.active && folderMatchFocus && replaceActive && searchViewletVisible`,
	`kb-macro.active && folderMatchWithResourceFocus && searchViewletVisible`,
	`kb-macro.active && hasOtherSuggestions && textInputFocus && !inSnippetMode && !suggestWidgetVisible && config.editor.tabCompletion == 'on'`,
	`kb-macro.active && hasSearchResult && searchViewletFocus`,
	`kb-macro.active && hasSearchResult || kb-macro.active && inSearchEditor`,
	`kb-macro.active && hasSymbols`,
	`kb-macro.active && historyNavigationBackwardsEnabled && historyNavigationWidgetFocus && !suggestWidgetVisible`,
	`kb-macro.active && historyNavigationForwardsEnabled && historyNavigationWidgetFocus && !suggestWidgetVisible`,
	`kb-macro.active && inDebugMode && !focusedSessionIsAttach`,
	`kb-macro.active && inDebugMode && stepIntoTargetsSupported && debugState == 'stopped'`,
	`kb-macro.active && inDebugMode`,
	`kb-macro.active && inDebugRepl && textInputFocus`,
	`kb-macro.active && inDebugRepl`,
	`kb-macro.active && inEditorsPicker && inQuickOpen`,
	`kb-macro.active && inFilesPicker && inQuickOpen`,
	`kb-macro.active && inKeybindings && inKeybindingsSearch`,
	`kb-macro.active && inKeybindings && keybindingFocus && !inputFocus`,
	`kb-macro.active && inKeybindings && keybindingFocus && !whenFocus`,
	`kb-macro.active && inKeybindings && keybindingFocus`,
	`kb-macro.active && inKeybindings`,
	`kb-macro.active && inQuickOpen && inRecentFilesPicker`,
	`kb-macro.active && inQuickOpen`,
	`kb-macro.active && inReferenceSearchEditor && !config.editor.stablePeek`,
	`kb-macro.active && inReferenceSearchEditor || kb-macro.active && referenceSearchVisible`,
	`kb-macro.active && inSearchEditor && inputBoxFocus || kb-macro.active && inputBoxFocus && searchViewletVisible && !searchInputBoxFocus`,
	`kb-macro.active && inSearchEditor && inputBoxFocus || kb-macro.active && inputBoxFocus && searchViewletVisible`,
	`kb-macro.active && inSearchEditor && searchInputBoxFocus`,
	`kb-macro.active && inSearchEditor || kb-macro.active && searchViewletFocus`,
	`kb-macro.active && inSearchEditor`,
	`kb-macro.active && inSettingsEditor && !inSettingsJSONEditor && !inSettingsSearch`,
	`kb-macro.active && inSettingsEditor && inSettingsSearch`,
	`kb-macro.active && inSettingsEditor && settingRowFocus`,
	`kb-macro.active && inSettingsEditor && settingsTocRowFocus`,
	`kb-macro.active && inSettingsEditor`,
	`kb-macro.active && inSettingsSearch && !suggestWidgetVisible`,
	`kb-macro.active && inWelcome && activeEditor == 'gettingStartedPage'`,
	`kb-macro.active && inZenMode`,
	`kb-macro.active && inlineSuggestionHasIndentationLessThanTabSize && inlineSuggestionVisible && !editorTabMovesFocus`,
	`kb-macro.active && inlineSuggestionVisible && !editorReadonly`,
	`kb-macro.active && inlineSuggestionVisible`,
	`kb-macro.active && inputFocus && notebookEditorFocused && !editorHasMultipleSelections && !editorHasSelection && !editorHoverVisible`,
	`kb-macro.active && inputFocus && notebookEditorFocused && notebookCellType == 'markup'`,
	`kb-macro.active && interactivePlaygroundFocus && !editorTextFocus`,
	`kb-macro.active && interfaceOverviewVisible`,
	`kb-macro.active && isDevelopment`,
	`kb-macro.active && isInDiffEditor`,
	`kb-macro.active && isLinux && !config.breadcrumbs.enabled`,
	`kb-macro.active && isLinux && !editorFocus`,
	`kb-macro.active && isLinux && breadcrumbsPossible && breadcrumbsVisible`,
	`kb-macro.active && isLinux && canNavigateBack`,
	`kb-macro.active && isLinux && editorFocus`,
	`kb-macro.active && isLinux && editorHasDefinitionProvider && editorTextFocus && !inReferenceSearchEditor && !isInEmbeddedEditor`,
	`kb-macro.active && isLinux && editorHasDocumentFormattingProvider && editorTextFocus && !editorReadonly && !inCompositeEditor`,
	`kb-macro.active && isLinux && editorHasDocumentFormattingProvider && editorTextFocus && inCompositeEditor && notebookEditable && !editorReadonly && activeEditor == 'workbench.editor.notebook'`,
	`kb-macro.active && isLinux && editorTextFocus && !editorHasDocumentFormattingProvider && !editorReadonly`,
	`kb-macro.active && isLinux && editorTextFocus && !editorReadonly && supportedCodeAction =~ /(\\s|^)quickfix\\b/`,
	`kb-macro.active && isLinux && editorTextFocus && !editorReadonly`,
	`kb-macro.active && isLinux && editorTextFocus`,
	`kb-macro.active && isLinux && fileMatchOrFolderMatchWithResourceFocus`,
	`kb-macro.active && isLinux && inQuickOpen`,
	`kb-macro.active && isLinux && notebookEditable && !editorTextFocus && activeEditor == 'workbench.editor.notebook'`,
	`kb-macro.active && isLinux && terminalFocus && terminalHasBeenCreated && !terminalAltBufferActive || kb-macro.active && isLinux && terminalFocus && terminalProcessSupported && !terminalAltBufferActive`,
	`kb-macro.active && isLinux && terminalFocus && terminalHasBeenCreated || kb-macro.active && isLinux && terminalFocus && terminalProcessSupported`,
	`kb-macro.active && isLinux && textInputFocus`,
	`kb-macro.active && isLinux && workbench.panel.output.active`,
	`kb-macro.active && isLinux`,
	`kb-macro.active && isMac && breadcrumbsActive && breadcrumbsVisible && listFocus && !inputFocus`,
	`kb-macro.active && isMac && breadcrumbsActive && breadcrumbsVisible`,
	`kb-macro.active && isMac && canNavigateBack`,
	`kb-macro.active && isMac && codeActionMenuVisible`,
	`kb-macro.active && isMac && editorFocus && parameterHintsMultipleSignatures && parameterHintsVisible`,
	`kb-macro.active && isMac && editorHasCompletionItemProvider && textInputFocus && !editorReadonly`,
	`kb-macro.active && isMac && editorTextFocus && !editorReadonly`,
	`kb-macro.active && isMac && explorerViewletVisible && filesExplorerFocus && !explorerResourceIsRoot && !explorerResourceReadonly && !inputFocus`,
	`kb-macro.active && isMac && inQuickOpen`,
	`kb-macro.active && isMac && inputFocus && notebookEditorFocused && notebookCellType == 'markup'`,
	`kb-macro.active && isMac && listFocus && !inputFocus`,
	`kb-macro.active && isMac && notebookCellListFocused && notebookMissingKernelExtension && !notebookCellExecuting && notebookCellType == 'code' || kb-macro.active && isMac && notebookCellListFocused && !notebookCellExecuting && notebookCellType == 'code' && notebookKernelCount > 0 || kb-macro.active && isMac && notebookCellListFocused && !notebookCellExecuting && notebookCellType == 'code' && notebookKernelSourceCount > 0`,
	`kb-macro.active && isMac && notebookEditorFocused`,
	`kb-macro.active && isMac && suggestWidgetMultipleSuggestions && suggestWidgetVisible && textInputFocus`,
	`kb-macro.active && isMac && terminalFocus`,
	`kb-macro.active && isMac && terminalHasBeenCreated && terminalTabsFocus && terminalTabsSingularSelection || kb-macro.active && isMac && terminalProcessSupported && terminalTabsFocus && terminalTabsSingularSelection`,
	`kb-macro.active && isMac && textInputFocus && !editorReadonly`,
	`kb-macro.active && isMac && textInputFocus`,
	`kb-macro.active && isMac && tunnelViewFocus && tunnelType == 'Forwarded' && tunnelViewMultiSelection == 'undefined'`,
	`kb-macro.active && isMac && variablesFocused`,
	`kb-macro.active && isMac && watchExpressionsFocused`,
	`kb-macro.active && isMacNative && openFolderWorkspaceSupport`,
	`kb-macro.active && isMac`,
	`kb-macro.active && isReadingLineWithInlayHints`,
	`kb-macro.active && isWindows && !editorFocus`,
	`kb-macro.active && isWindows && activeEditorGroupEmpty && multipleEditorGroups`,
	`kb-macro.active && isWindows && canNavigateBack`,
	`kb-macro.active && isWindows && canNavigateForward`,
	`kb-macro.active && isWindows && editorFocus`,
	`kb-macro.active && isWindows && editorTextFocus`,
	`kb-macro.active && isWindows && fileMatchOrFolderMatchWithResourceFocus`,
	`kb-macro.active && isWindows && inQuickOpen`,
	`kb-macro.active && isWindows && inputFocus && notebookEditorFocused && notebookCellType == 'markup'`,
	`kb-macro.active && isWindows && notebookCellListFocused && notebookMissingKernelExtension && !notebookCellExecuting && notebookCellType == 'code' || kb-macro.active && isWindows && notebookCellListFocused && !notebookCellExecuting && notebookCellType == 'code' && notebookKernelCount > 0 || kb-macro.active && isWindows && notebookCellListFocused && !notebookCellExecuting && notebookCellType == 'code' && notebookKernelSourceCount > 0`,
	`kb-macro.active && isWindows && terminalEditorFocus && terminalFocus && terminalHasBeenCreated && resourceScheme == 'vscode-terminal' || kb-macro.active && isWindows && terminalEditorFocus && terminalFocus && terminalProcessSupported && resourceScheme == 'vscode-terminal'`,
	`kb-macro.active && isWindows && terminalFocus && !accessibilityModeEnabled && terminalShellType == 'pwsh'`,
	`kb-macro.active && isWindows && terminalFocus && terminalHasBeenCreated && !terminalAltBufferActive || kb-macro.active && isWindows && terminalFocus && terminalProcessSupported && !terminalAltBufferActive`,
	`kb-macro.active && isWindows && terminalFocus && terminalHasBeenCreated && terminalTextSelected || kb-macro.active && isWindows && terminalFocus && terminalProcessSupported && terminalTextSelected`,
	`kb-macro.active && isWindows && terminalFocus && terminalHasBeenCreated || kb-macro.active && isWindows && terminalFocus && terminalProcessSupported`,
	`kb-macro.active && isWindows && terminalFocus && terminalShellType == 'cmd'`,
	`kb-macro.active && isWindows && textInputFocus && !accessibilityModeEnabled`,
	`kb-macro.active && isWindows && textInputFocus`,
	`kb-macro.active && isWindows && workbench.panel.output.active`,
	`kb-macro.active && isWindows`,
	`kb-macro.active && kb-macro.headOfLine && isWindows`,
	`kb-macro.active && kb-macro.headOfLine`,
	`kb-macro.active && listFocus && !inputFocus`,
	`kb-macro.active && listFocus && listHasSelectionOrFocus && !inputFocus`,
	`kb-macro.active && listFocus && listSupportsFind`,
	`kb-macro.active && listFocus && listSupportsMultiselect && !inputFocus`,
	`kb-macro.active && listFocus && refactorPreview.enabled && !inputFocus`,
	`kb-macro.active && listFocus && referenceSearchVisible && !inputFocus && !treeElementCanCollapse && !treeElementCanExpand`,
	`kb-macro.active && listFocus && searchViewletVisible && !inputFocus && searchState != '0'`,
	`kb-macro.active && listFocus && treeElementCanCollapse && !inputFocus || kb-macro.active && listFocus && treeElementHasParent && !inputFocus`,
	`kb-macro.active && listFocus && treeElementCanExpand && !inputFocus || kb-macro.active && listFocus && treeElementHasChild && !inputFocus`,
	`kb-macro.active && listFocus && treeFindOpen`,
	`kb-macro.active && matchFocus && replaceActive && searchViewletVisible`,
	`kb-macro.active && messageVisible`,
	`kb-macro.active && notebookCellEditable && notebookCellHasOutputs && notebookEditable && notebookEditorFocused && !inputFocus`,
	`kb-macro.active && notebookCellEditable && notebookEditable && notebookEditorFocused`,
	`kb-macro.active && notebookCellEditable && notebookEditable`,
	`kb-macro.active && notebookCellHasOutputs && notebookCellListFocused && !inputFocus && !notebookCellOutputIsCollapsed`,
	`kb-macro.active && notebookCellHasOutputs && notebookEditorFocused`,
	`kb-macro.active && notebookCellInputIsCollapsed && notebookCellListFocused`,
	`kb-macro.active && notebookCellListFocused && !inputFocus && !notebookCellInputIsCollapsed`,
	`kb-macro.active && notebookCellListFocused && !inputFocus`,
	`kb-macro.active && notebookCellListFocused && notebookCellOutputIsCollapsed`,
	`kb-macro.active && notebookCellListFocused && notebookCellType == 'markup' || kb-macro.active && notebookCellListFocused && notebookMissingKernelExtension && !notebookCellExecuting && notebookCellType == 'code' || kb-macro.active && notebookCellListFocused && !notebookCellExecuting && notebookCellType == 'code' && notebookKernelCount > 0 || kb-macro.active && notebookCellListFocused && !notebookCellExecuting && notebookCellType == 'code' && notebookKernelSourceCount > 0`,
	`kb-macro.active && notebookCellListFocused && notebookEditable && !inputFocus`,
	`kb-macro.active && notebookCellListFocused && notebookMissingKernelExtension && !notebookCellExecuting && notebookCellType == 'code' || kb-macro.active && notebookCellListFocused && !notebookCellExecuting && notebookCellType == 'code' && notebookKernelCount > 0 || kb-macro.active && notebookCellListFocused && !notebookCellExecuting && notebookCellType == 'code' && notebookKernelSourceCount > 0`,
	`kb-macro.active && notebookEditorFocused && !editorFocus && activeEditor == 'workbench.editor.interactive' || kb-macro.active && notebookEditorFocused && !editorFocus && activeEditor == 'workbench.editor.notebook'`,
	`kb-macro.active && notebookEditorFocused && !inputFocus && activeEditor == 'workbench.editor.notebook' && notebookCellType == 'code'`,
	`kb-macro.active && notebookEditorFocused && !inputFocus && activeEditor == 'workbench.editor.notebook' && notebookCellType == 'markup'`,
	`kb-macro.active && notebookEditorFocused && !inputFocus && activeEditor == 'workbench.editor.notebook'`,
	`kb-macro.active && notebookEditorFocused && !inputFocus`,
	`kb-macro.active && notebookEditorFocused && notebookFindWidgetFocused`,
	`kb-macro.active && notebookEditorFocused && notebookOutputFocused`,
	`kb-macro.active && notebookEditorFocused`,
	`kb-macro.active && notificationCenterVisible`,
	`kb-macro.active && notificationFocus && notificationToastsVisible`,
	`kb-macro.active && notificationFocus`,
	`kb-macro.active && notificationToastsVisible`,
	`kb-macro.active && problemFocus`,
	`kb-macro.active && problemsFilterFocus`,
	`kb-macro.active && problemsVisibility && !relatedInformationFocus && focusedView == 'workbench.panel.markers.view'`,
	`kb-macro.active && refactorPreview.enabled && refactorPreview.hasCheckedChanges && focusedView == 'refactorPreview'`,
	`kb-macro.active && reference-list.hasResult && references-view.canNavigate`,
	`kb-macro.active && referenceSearchVisible && !config.editor.stablePeek`,
	`kb-macro.active && remoteFileDialogVisible`,
	`kb-macro.active && replaceActive && searchViewletVisible && !findWidgetVisible`,
	`kb-macro.active && replaceInputBoxFocus && searchViewletVisible`,
	`kb-macro.active && resourceScheme == 'vscode-interactive'`,
	`kb-macro.active && scmInputIsInFirstPosition && scmRepository && !suggestWidgetVisible`,
	`kb-macro.active && scmInputIsInLastPosition && scmRepository && !suggestWidgetVisible`,
	`kb-macro.active && scmRepository`,
	`kb-macro.active && searchViewletFocus && !fileMatchOrFolderMatchFocus`,
	`kb-macro.active && searchViewletFocus`,
	`kb-macro.active && settingRowFocus`,
	`kb-macro.active && sideBySideEditorActive`,
	`kb-macro.active && statusBarFocused`,
	`kb-macro.active && suggestWidgetHasFocusedSuggestion && suggestWidgetVisible && textInputFocus`,
	`kb-macro.active && suggestWidgetMultipleSuggestions && suggestWidgetVisible && textInputFocus`,
	`kb-macro.active && suggestWidgetVisible && textInputFocus && !config.editor.suggest.selectQuickSuggestions && !suggestWidgetHasFocusedSuggestion`,
	`kb-macro.active && suggestWidgetVisible && textInputFocus`,
	`kb-macro.active && suggestWidgetVisible`,
	`kb-macro.active && taskCommandsRegistered`,
	`kb-macro.active && terminal.active`,
	`kb-macro.active && terminalEditorFocus && terminalFocus && terminalHasBeenCreated && resourceScheme == 'vscode-terminal' || kb-macro.active && terminalEditorFocus && terminalFocus && terminalProcessSupported && resourceScheme == 'vscode-terminal'`,
	`kb-macro.active && terminalFindFocused && terminalHasBeenCreated || kb-macro.active && terminalFindFocused && terminalProcessSupported || kb-macro.active && terminalFocus && terminalHasBeenCreated || kb-macro.active && terminalFocus && terminalProcessSupported`,
	`kb-macro.active && terminalFindInputFocused && terminalHasBeenCreated || kb-macro.active && terminalFindInputFocused && terminalProcessSupported`,
	`kb-macro.active && terminalFindVisible && terminalFocus && terminalHasBeenCreated || kb-macro.active && terminalFindVisible && terminalFocus && terminalProcessSupported`,
	`kb-macro.active && terminalFocus && terminalHasBeenCreated && !accessibilityModeEnabled || kb-macro.active && terminalFocus && terminalProcessSupported && !accessibilityModeEnabled`,
	`kb-macro.active && terminalFocus && terminalHasBeenCreated && !terminalAltBufferActive || kb-macro.active && terminalFocus && terminalProcessSupported && !terminalAltBufferActive`,
	`kb-macro.active && terminalFocus && terminalHasBeenCreated && !terminalEditorFocus || kb-macro.active && terminalFocus && terminalProcessSupported && !terminalEditorFocus`,
	`kb-macro.active && terminalFocus && terminalHasBeenCreated && terminalIsOpen || kb-macro.active && terminalFocus && terminalIsOpen && terminalProcessSupported`,
	`kb-macro.active && terminalFocus && terminalHasBeenCreated && terminalTextSelected && !terminalFindVisible || kb-macro.active && terminalFocus && terminalProcessSupported && terminalTextSelected && !terminalFindVisible`,
	`kb-macro.active && terminalFocus && terminalHasBeenCreated && terminalTextSelected || kb-macro.active && terminalFocus && terminalProcessSupported && terminalTextSelected`,
	`kb-macro.active && terminalFocus && terminalHasBeenCreated || kb-macro.active && terminalFocus && terminalProcessSupported || kb-macro.active && terminalHasBeenCreated && terminalTabsFocus || kb-macro.active && terminalProcessSupported && terminalTabsFocus`,
	`kb-macro.active && terminalFocus && terminalHasBeenCreated || kb-macro.active && terminalFocus && terminalProcessSupported`,
	`kb-macro.active && terminalFocus && terminalProcessSupported && terminalTextSelected`,
	`kb-macro.active && terminalFocus && terminalProcessSupported || kb-macro.active && terminalFocus && terminalWebExtensionContributedProfile`,
	`kb-macro.active && terminalFocus && terminalShellIntegrationEnabled && !accessibilityModeEnabled && terminalShellType == 'pwsh'`,
	`kb-macro.active && terminalFocus && terminalShellType == 'pwsh'`,
	`kb-macro.active && terminalFocus`,
	`kb-macro.active && terminalHasBeenCreated && terminalTabsFocus || kb-macro.active && terminalIsOpen && terminalTabsFocus || kb-macro.active && terminalProcessSupported && terminalTabsFocus`,
	`kb-macro.active && terminalProcessSupported && terminalTabsFocus`,
	`kb-macro.active && terminalProcessSupported || kb-macro.active && terminalWebExtensionContributedProfile`,
	`kb-macro.active && testing.canRefresh`,
	`kb-macro.active && testing.hasAnyResults`,
	`kb-macro.active && testing.isInPeek && !config.editor.stablePeek || kb-macro.active && testing.isPeekVisible && !config.editor.stablePeek`,
	`kb-macro.active && testing.isPeekVisible`,
	`kb-macro.active && textCompareEditorVisible`,
	`kb-macro.active && textInputFocus && !editorReadonly`,
	`kb-macro.active && textInputFocus`,
	`kb-macro.active && tunnelCloseable && tunnelViewFocus`,
	`kb-macro.active && tunnelViewFocus && tunnelType == 'Detected' && tunnelViewMultiSelection == 'undefined' || kb-macro.active && tunnelViewFocus && tunnelType == 'Forwarded' && tunnelViewMultiSelection == 'undefined'`,
	`kb-macro.active && typeHierarchyVisible && !config.editor.stablePeek`,
	`kb-macro.active && typeHierarchyVisible && typeHierarchyDirection == 'subtypes'`,
	`kb-macro.active && typeHierarchyVisible && typeHierarchyDirection == 'supertypes'`,
	`kb-macro.active && typeHierarchyVisible`,
	`kb-macro.active && viewContainer.workbench.view.debug.enabled`,
	`kb-macro.active && viewContainer.workbench.view.explorer.enabled`,
	`kb-macro.active && viewContainer.workbench.view.extensions.enabled`,
	`kb-macro.active && watchExpressionsFocused && !expressionSelected`,
	`kb-macro.active && webviewFindWidgetEnabled && !editorFocus && activeEditor == 'WebviewEditor'`,
	`kb-macro.active && webviewFindWidgetFocused && !editorFocus && activeEditor == 'WebviewEditor'`,
	`kb-macro.active && webviewFindWidgetFocused && !editorFocus && activeEditor == 'workbench.editor.extension'`,
	`kb-macro.active && webviewFindWidgetVisible && !editorFocus && activeEditor == 'WebviewEditor'`,
	`kb-macro.active && workbench.explorer.openEditorsView.active`,
	`kb-macro.active && workbench.panel.markers.view.active`,
	`kb-macro.active && workbench.panel.output.active`,
	`kb-macro.active && workbench.panel.repl.view.active`,
	`kb-macro.active && workbench.scm.active`,
	`kb-macro.active && workbench.view.search.active && neverMatch =~ /doesNotMatch/`,
	`kb-macro.active`,
	`kb-macro.playing`,
	`kb-macro.recording && config.keyboardMacro.recordingShortcuts == 'Option1'`,
	`kb-macro.recording && config.keyboardMacro.recordingShortcuts == 'Option2'`,
	`keith-diagram-focused && !klighd-vscode.syncWithEditor`,
	`keith-diagram-focused && klighd-vscode.syncWithEditor`,
	`keith-diagram-focused`,
	`kestrel.ctx.account && !virtualWorkspace && !isWeb`,
	`kestrel.ctx.account`,
	`kolo.isLoggedIn`,
	`kp-plugin:started == true && kp-plugin:show-new-version-tips == false`,
	`kp-plugin:started == true && kp-plugin:show-new-version-tips == true`,
	`kp-plugin:started == true`,
	`kubectl.available`,
	`kunpengStopTask && view == build-tasks-lists && viewItem == taskProfileNode`,
	`kunpengStopTestCase && view == test-case-lists && viewItem == testProfileNode`,
	`l13DiffFocus && !config.l13Diff.ignoreEndOfLine`,
	`l13DiffFocus && !l13DiffInputFocus && !l13DiffSearchFocus && !inputFocus`,
	`l13DiffFocus && !l13DiffInputFocus && !l13DiffSearchFocus`,
	`l13DiffFocus && config.l13Diff.ignoreEndOfLine`,
	`l13DiffFocus && config.l13Diff.ignoreTrimWhitespace == 'default' && !config.diffEditor.ignoreTrimWhitespace`,
	`l13DiffFocus && config.l13Diff.ignoreTrimWhitespace == 'default' && config.diffEditor.ignoreTrimWhitespace`,
	`l13DiffFocus && config.l13Diff.ignoreTrimWhitespace == 'off'`,
	`l13DiffFocus && config.l13Diff.ignoreTrimWhitespace == 'on'`,
	`l13DiffFocus && config.l13Diff.useCaseSensitiveFileName == 'off'`,
	`l13DiffFocus && config.l13Diff.useCaseSensitiveFileName == 'on'`,
	`l13DiffFocus && l13DiffInputFocus`,
	`l13DiffFocus && l13DiffListFocus`,
	`l13DiffFocus && l13DiffSearchFocus`,
	`l13DiffFocus`,
	`l13DiffSelectedFolder && view =~ /^l13Projects(Favorites|Workspaces)$/ && viewItem =~ /^(current-)?(favorite|project|subfavorite|subproject)-(folder|git|vscode)$/`,
	`lab50:showReadme`,
	`latex-workshop:enabled`,
	`launch-buttons.enabled`,
	`launchJsonStatus != Supported`,
	`launchJsonStatus == Supported && isWorkspaceTrusted`,
	`launchJsonStatus == Unsupported && isWorkspaceTrusted`,
	`launchdarkly:enableFlagExplorer`,
	`launchdarkly:enableMetricExplorer`,
	`leadermode.isActive`,
	`leaper.inLeaperMode && editorTextFocus`,
	`leaper.inLeaperMode && leaper.hasLineOfSight && editorTextFocus && !editorHasSelection && !editorTabMovesFocus && !suggestWidgetVisible`,
	`leaper.inLeaperMode && leaper.hasLineOfSight && editorTextFocus && !editorHasSelection`,
	`lifeboatapi.isSWWorkspace`,
	`light == true`,
	`linear-connect.hasLinearSessions`,
	`lingua:enabled`,
	`listDoubleSelection`,
	`listFocus && !inputFocus && !notebookCellListFocused`,
	`listFocus && !inputFocus && listSupportsKeyboardNavigation`,
	`listFocus && !inputFocus`,
	`listFocus && listHasSelectionOrFocus && !inputFocus`,
	`listFocus && listHasSelectionOrFocus`,
	`listFocus && listSupportsFind && !inputFocus`,
	`listFocus && listSupportsFind`,
	`listFocus && listSupportsMultiselect && !inputFocus`,
	`listFocus && refactorPreview.enabled && !inputFocus`,
	`listFocus && referenceSearchVisible && !inputFocus && !treeElementCanCollapse && !treeElementCanExpand`,
	`listFocus && referenceSearchVisible && !inputFocus`,
	`listFocus && searchViewletVisible && !inputFocus && searchState != '0'`,
	`listFocus && treeElementCanCollapse && !inputFocus || listFocus && treeElementHasParent && !inputFocus`,
	`listFocus && treeElementCanExpand && !inputFocus || listFocus && treeElementHasChild && !inputFocus`,
	`listFocus && treeFindOpen && inputFocus`,
	`listFocus && treeFindOpen`,
	`listHasSelectionOrFocus && !listDoubleSelection && resourceExtname == .php`,
	`listMultiSelection && filesExplorerFocus`,
	`listMultiSelection && resourceExtname == .json`,
	`listMultiSelection && resourceExtname == .yaml`,
	`listMultiSelection && resourceExtname == .yml`,
	`listMultiSelection`,
	`liveCode.isPreviewFocus`,
	`livecode && config.livecode.showIcons`,
	`liveshare.audio:state == LiveShareWithAudio`,
	`liveshare.audio:state == LiveShareWithoutAudio`,
	`liveshare.pomodoro.state == running || liveshare.pomodoro.state == paused`,
	`liveshare.pomodoro.state == running`,
	`liveshare.pomodoro.state == stopped || liveshare.pomodoro.state == paused`,
	`liveshare:diagnosticModeEnabled`,
	`liveshare:hasCollaborators && !liveshare:isFollowing`,
	`liveshare:hasCollaborators && liveshare:isFollowing`,
	`liveshare:hasCollaborators`,
	`liveshare:hasPresenceProviders`,
	`liveshare:hasSearchContactsProviders`,
	`liveshare:hasSharedTerminals && liveshare:state == Joined`,
	`liveshare:isAnonymousUser`,
	`liveshare:isCollaborating`,
	`liveshare:isFollowable && liveshare:hasCollaborators && !liveshare:isFollowing`,
	`liveshare:isFollowable && liveshare:hasCollaborators && liveshare:isFollowing`,
	`liveshare:isNode`,
	`liveshare:isWeb && liveshare:isAnonymousUser && !liveshare:isNativeAuthEnabled`,
	`liveshare:isWeb && liveshare:state == ExternallySigningIn && !liveshare:isNativeAuthEnabled`,
	`liveshare:isWeb && liveshare:state == Joined`,
	`liveshare:isWeb && liveshare:state == SignedOut && !liveshare:isNativeAuthEnabled`,
	`liveshare:isWeb && liveshare:state == SigningIn && !liveshare:isNativeAuthEnabled`,
	`liveshare:isWeb`,
	`liveshare:presenceSignedStatus == signedIn && !liveshare:isNativeAuthEnabled`,
	`liveshare:presenceSignedStatus == signedOut`,
	`liveshare:presenceSignedStatus == signingIn && !liveshare:isNativeAuthEnabled`,
	`liveshare:showExplorer && liveshare:isCollaborating`,
	`liveshare:showExplorer && liveshare:showHelpViewlet && liveshare:isCollaborating`,
	`liveshare:showExplorer && view == liveshare.contacts && viewItem == contacts.RecentContacts`,
	`liveshare:showExplorer && view == liveshare.contacts && viewItem == contacts.contact.offline`,
	`liveshare:showExplorer && view == liveshare.contacts && viewItem == contacts.contact`,
	`liveshare:showExplorer && view == liveshare.contacts && viewItem == contacts.recentContact.offline`,
	`liveshare:showExplorer && view == liveshare.contacts && viewItem == contacts.recentContact`,
	`liveshare:showExplorer && view == liveshare.contacts && viewItem =~ /^contacts.recentContact?/`,
	`liveshare:showExplorer`,
	`liveshare:state != Shared && liveshare:state != Joined`,
	`liveshare:state != SignedOut && !liveshare:isAnonymousUser && !liveshare:isCollaborating && !liveshare:isNativeAuthEnabled`,
	`liveshare:state == ExternallySigningIn`,
	`liveshare:state == Joined`,
	`liveshare:state == Shared`,
	`liveshare:state == SignedOut`,
	`liveshare:state == SigningIn`,
	`local-history:treeLocation == explorer`,
	`local-history:treeLocation == localHistory`,
	`luna:focused && focusedView == '' && !sideBarFocus && activeCustomEditorId == 'luna.editor'`,
	`luna:focused && focusedView == '' && activeCustomEditorId == 'luna.editor' && !luna:textFocused`,
	`luna:focused && focusedView == '' && activeCustomEditorId == 'luna.editor' && luna:layerEntryFocused`,
	`luna:focused && focusedView == '' && activeCustomEditorId == 'luna.editor'`,
	`lynxStudio:Activate`,
	`lyticsAccountConnected && editorLangId == lql && resourceScheme == file`,
	`lyticsAccountConnected && explorerResourceIsFolder`,
	`lyticsAccountConnected && resourceExtname == .campaign_override && editorReadOnly == false`,
	`lyticsAccountConnected && resourceExtname == .campaign_override`,
	`lyticsAccountConnected && resourceLangId == csv`,
	`lyticsAccountConnected && resourceLangId == lql && editorReadOnly == false`,
	`lyticsAccountConnected && resourceLangId == lql`,
	`lyticsAccountConnected && resourceScheme == file`,
	`lyticsAccountConnected`,
	`makefile:fullFeatureSet`,
	`makefile:localDebugFeature`,
	`makefile:localRunFeature`,
	`malloy.webviewPanelFocused == true || resourceFilename =~ /\\.malloy$/`,
	`manpages:hasResults`,
	`markdownPreviewFocus`,
	`markdownShortcuts:enabled && config.markdownShortcuts.icons.bold`,
	`markdownShortcuts:enabled && config.markdownShortcuts.icons.bullets`,
	`markdownShortcuts:enabled && config.markdownShortcuts.icons.citations`,
	`markdownShortcuts:enabled && config.markdownShortcuts.icons.image`,
	`markdownShortcuts:enabled && config.markdownShortcuts.icons.italic`,
	`markdownShortcuts:enabled && config.markdownShortcuts.icons.link`,
	`markdownShortcuts:enabled && config.markdownShortcuts.icons.strikethrough`,
	`markdownShortcuts:enabled`,
	`markdownTables`,
	`matchFocus && replaceActive && searchViewletVisible`,
	`mauditCompatibleWorkspace`,
	`mbaas-valid-folder`,
	`mcu-debug.rtos-views:showRTOS`,
	`mdContext`,
	`mdxPreviewFocus`,
	`meld-diff.FileSelectedForMeldDiff && ! listMultiSelection && filesExplorerFocus`,
	`memo:backlinksPanel.enabled`,
	`memory-view:showMemoryPanel && memory-view:memoryPanelLocation == panel`,
	`merge-conflict`,
	`mergeConflictsCount && mergeConflictsCount != 0 && config.git-buttons-conflict.conflict_accept_all-both`,
	`mergeConflictsCount && mergeConflictsCount != 0 && config.git-buttons-conflict.conflict_accept_all-current`,
	`mergeConflictsCount && mergeConflictsCount != 0 && config.git-buttons-conflict.conflict_accept_all-incoming`,
	`mergeConflictsCount && mergeConflictsCount != 0 && config.git-buttons-conflict.conflict_accept_both`,
	`mergeConflictsCount && mergeConflictsCount != 0 && config.git-buttons-conflict.conflict_accept_current`,
	`mergeConflictsCount && mergeConflictsCount != 0 && config.git-buttons-conflict.conflict_accept_incoming`,
	`mergeConflictsCount && mergeConflictsCount != 0 && config.git-buttons-conflict.conflict_accept_selection`,
	`mermaidPreviewActive`,
	`mermaidPreviewEnabled && resourceExtname == .mmd`,
	`mermaidPreviewVisible`,
	`mesonbuild.hasProject`,
	`messageVisible`,
	`metals.showFindInFiles`,
	`metals:enabled && resourceExtname==.java || metals:enabled && resourceExtname==.scala || metals:enabled && resourceExtname==.class || metals:enabled && resourceExtname==.cfr || metals:enabled && resourceExtname==.sbt || metals:enabled && resourceExtname==.sc`,
	`metals:enabled && resourceExtname==.java || metals:enabled && resourceExtname==.scala || metals:enabled && resourceExtname==.class || metals:enabled && resourceExtname==.javap || metals:enabled && resourceExtname==.sbt || metals:enabled && resourceExtname==.sc`,
	`metals:enabled && resourceExtname==.java || metals:enabled && resourceExtname==.scala || metals:enabled && resourceExtname==.class || metals:enabled && resourceExtname==.javap-verbose || metals:enabled && resourceExtname==.sbt || metals:enabled && resourceExtname==.sc`,
	`metals:enabled && resourceExtname==.java || metals:enabled && resourceExtname==.scala || metals:enabled && resourceExtname==.class || metals:enabled && resourceExtname==.sbt || metals:enabled && resourceExtname==.sc`,
	`metals:enabled && resourceExtname==.java || metals:enabled && resourceExtname==.scala || metals:enabled && resourceExtname==.class || metals:enabled && resourceExtname==.semanticdb || metals:enabled && resourceExtname==.tasty || metals:enabled && resourceExtname==.sbt || metals:enabled && resourceExtname==.sc`,
	`metals:enabled && resourceExtname==.java || metals:enabled && resourceExtname==.scala || metals:enabled && resourceExtname==.semanticdb || metals:enabled && resourceExtname==.sbt || metals:enabled && resourceExtname==.sc`,
	`metals:enabled && resourceExtname==.java || metals:enabled && resourceExtname==.scala || metals:enabled && resourceExtname==.semanticdb || metals:enabled && resourceExtname==.semanticdb-compact || metals:enabled && resourceExtname==.sbt || metals:enabled && resourceExtname==.sc`,
	`metals:enabled && resourceExtname==.java || metals:enabled && resourceExtname==.scala || metals:enabled && resourceExtname==.semanticdb || metals:enabled && resourceExtname==.semanticdb-detailed || metals:enabled && resourceExtname==.sbt || metals:enabled && resourceExtname==.sc`,
	`metals:enabled && resourceExtname==.java || metals:enabled && resourceExtname==.scala || metals:enabled && resourceExtname==.semanticdb || metals:enabled && resourceExtname==.semanticdb-proto || metals:enabled && resourceExtname==.sbt || metals:enabled && resourceExtname==.sc`,
	`metals:enabled && resourceExtname==.scala || metals:enabled && resourceExtname==.tasty || metals:enabled && resourceExtname==.tasty-decoded || metals:enabled && resourceExtname==.sbt || metals:enabled && resourceExtname==.sc`,
	`metals:enabled`,
	`metricsReportActiveContext`,
	`mfcobol.breakpointsView && debugType != 'coreclr'`,
	`mfcobol.breakpointsView`,
	`micronautProjectExists && javaHomeSet`,
	`milkdown.active && !inDebugRepl`,
	`minapp-vscode:init && resourceExtname == '' && activeViewlet == 'workbench.view.explorer'`,
	`mint:isActivated`,
	`mintlify.hasDocuments`,
	`mirrord.activated`,
	`mist:hasConfig`,
	`mkelem-criteria`,
	`mobioos-forge.user.isConnected`,
	`moduleHelper.mode !== 'yarn'`,
	`mutable-ai.ready && editorLangId in MutableAI.filesuggest.documentation.supportedLanguages`,
	`mutable-ai.ready && editorLangId in MutableAI.filesuggest.instruction.supportedLanguages`,
	`mutable-ai.ready && editorTextFocus && config.MutableAI.autocomplete.enabled`,
	`mutable-ai.ready && inDiffEditor`,
	`mvcReferencesEnabled`,
	`mybatisx:activated`,
	`mybatisx:showGotoJava`,
	`mybatisx:showGotoXml`,
	`nativeImageInitialized`,
	`nbJavaLSReady && config.netbeans.javaSupport.enabled && view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.resource\\.pod/i`,
	`nbJavaLSReady && config.netbeans.javaSupport.enabled`,
	`nbJavaLSReady && editorLangId == java && config.netbeans.javaSupport.enabled`,
	`nbJavaLSReady && editorLangId == java && editorTextFocus && config.netbeans.javaSupport.enabled`,
	`nbJavaLSReady && explorerResourceIsFolder`,
	`nbJavaLSReady && view == foundProjects && config.netbeans.javaSupport.enabled`,
	`nbJavaLSReady`,
	`neovim.mode == cmdline_normal || neovim.mode == cmdline_insert || neovim.mode == cmdline_replace`,
	`neteasemusic.on && !neteasemusic.logged`,
	`neteasemusic.on && neteasemusic.logged && !neteasemusic.radio`,
	`neteasemusic.on && neteasemusic.logged && !neteasemusic.signed`,
	`neteasemusic.on && neteasemusic.logged && neteasemusic.track && !neteasemusic.liked && !neteasemusic.program`,
	`neteasemusic.on && neteasemusic.logged && neteasemusic.track && neteasemusic.liked`,
	`neteasemusic.on && neteasemusic.logged`,
	`neteasemusic.on && neteasemusic.track && !neteasemusic.muted`,
	`neteasemusic.on && neteasemusic.track && !neteasemusic.playing`,
	`neteasemusic.on && neteasemusic.track && !neteasemusic.radio`,
	`neteasemusic.on && neteasemusic.track && neteasemusic.exposed`,
	`neteasemusic.on && neteasemusic.track && neteasemusic.mode == 0 && !neteasemusic.radio`,
	`neteasemusic.on && neteasemusic.track && neteasemusic.mode == 1 && !neteasemusic.radio`,
	`neteasemusic.on && neteasemusic.track && neteasemusic.mode == 2 && !neteasemusic.radio`,
	`neteasemusic.on && neteasemusic.track && neteasemusic.mode == 3 && !neteasemusic.radio`,
	`neteasemusic.on && neteasemusic.track && neteasemusic.muted`,
	`neteasemusic.on && neteasemusic.track && neteasemusic.playing`,
	`neteasemusic.on && neteasemusic.track && neteasemusic.radio`,
	`neteasemusic.on && neteasemusic.track`,
	`neteasemusic.on`,
	`never && gitflow.initialized`,
	`never`,
	`ngin:showWebviewExecutionActions == true`,
	`nhctl.upgrade || extensionActivated && Nocalhost.visibleTree`,
	`nhctl.upgrade`,
	`nim.connected`,
	`noAccount`,
	`noConnectedServers`,
	`noLicense`,
	`noProjectFound == true`,
	`noReferencesEnabled`,
	`node_notebook.tfjs-vis.used && !config.node_notebook.disableTensorflowVis`,
	`nodemcu-tools:isConnected`,
	`none`,
	`nordic-semiconductor.nrf-terminal:developer-mode`,
	`nordic-semiconductor.nrf-terminal:running`,
	`notebookCellEditable && notebookCellHasOutputs && notebookEditable && notebookEditorFocused && !inputFocus`,
	`notebookCellEditable && notebookEditable && notebookEditorFocused`,
	`notebookCellEditable && notebookEditable`,
	`notebookCellHasOutputs && notebookCellListFocused && !inputFocus && !notebookCellOutputIsCollapsed`,
	`notebookCellHasOutputs && notebookEditorFocused`,
	`notebookCellInputIsCollapsed && notebookCellListFocused`,
	`notebookCellListFocused && !inputFocus && !notebookCellInputIsCollapsed`,
	`notebookCellListFocused && !inputFocus`,
	`notebookCellListFocused && notebookCellOutputIsCollapsed`,
	`notebookCellListFocused && notebookCellType == 'code' && notebookCellExecutionState == 'failed' || notebookCellExecutionState == 'idle' || notebookCellExecutionState == 'succeeded' && notebookKernelCount > 0`,
	`notebookCellListFocused && notebookCellType == 'markup' || notebookCellListFocused && notebookCellType == 'code' && notebookCellExecutionState == 'failed' || notebookCellExecutionState == 'idle' || notebookCellExecutionState == 'succeeded' && notebookKernelCount > 0`,
	`notebookCellListFocused && notebookCellType == 'markup' || notebookCellListFocused && notebookMissingKernelExtension && !notebookCellExecuting && notebookCellType == 'code' || notebookCellListFocused && !notebookCellExecuting && notebookCellType == 'code' && notebookKernelCount > 0 || notebookCellListFocused && !notebookCellExecuting && notebookCellType == 'code' && notebookKernelSourceCount > 0`,
	`notebookCellListFocused && notebookCellType == 'markup' || notebookCellListFocused && notebookMissingKernelExtension && !notebookCellExecuting && notebookCellType == 'code' || notebookCellListFocused && !notebookCellExecuting && notebookCellType == 'code' && notebookKernelCount > 0`,
	`notebookCellListFocused && notebookEditable && !inputFocus`,
	`notebookCellListFocused && notebookMissingKernelExtension && !notebookCellExecuting && notebookCellType == 'code' || notebookCellListFocused && !notebookCellExecuting && notebookCellType == 'code' && notebookKernelCount > 0 || notebookCellListFocused && !notebookCellExecuting && notebookCellType == 'code' && notebookKernelSourceCount > 0`,
	`notebookCellListFocused && notebookMissingKernelExtension && !notebookCellExecuting && notebookCellType == 'code' || notebookCellListFocused && !notebookCellExecuting && notebookCellType == 'code' && notebookKernelCount > 0`,
	`notebookCellResource in jupyter.notebookeditor.runByLineCells && notebookCellToolbarLocation == left`,
	`notebookCellResource in jupyter.notebookeditor.runByLineCells && notebookCellToolbarLocation == right`,
	`notebookCellResource in jupyter.notebookeditor.runByLineCells`,
	`notebookEditable && !editorTextFocus && activeEditor == 'workbench.editor.notebook'`,
	`notebookEditable && notebookEditorFocused && !inputFocus`,
	`notebookEditorFocused && !editorFocus && activeEditor == 'workbench.editor.notebook'`,
	`notebookEditorFocused && !inputFocus && activeEditor == 'workbench.editor.notebook' && notebookCellType == 'code'`,
	`notebookEditorFocused && !inputFocus && activeEditor == 'workbench.editor.notebook' && notebookCellType == 'markup'`,
	`notebookEditorFocused && !inputFocus && activeEditor == 'workbench.editor.notebook'`,
	`notebookEditorFocused && !inputFocus && notebookCellType == 'code'`,
	`notebookEditorFocused && !inputFocus && notebookCellType == 'markup'`,
	`notebookEditorFocused && !inputFocus`,
	`notebookEditorFocused && notebookFindWidgetFocused`,
	`notebookEditorFocused && notebookOutputFocused`,
	`notebookEditorFocused && notebookType == 'jupyter-notebook'`,
	`notebookEditorFocused || !editorFocus && activeEditor == 'workbench.editor.notebook'`,
	`notebookEditorFocused`,
	`notebookKernel =~ /^ms-toolsai.jupyter\\// && jupyter.ispythonnotebook && notebookCellType == code && isWorkspaceTrusted && resource not in jupyter.notebookeditor.debugDocuments || !notebookKernel && jupyter.ispythonnotebook && notebookCellType == code && isWorkspaceTrusted`,
	`notebookKernel =~ /^ms-toolsai.jupyter\\// && notebookType == 'jupyter-notebook' && config.jupyter.showOutlineButtonInNotebookToolbar && config.notebook.globalToolbar != true`,
	`notebookKernel =~ /^ms-toolsai.jupyter\\// && notebookType == 'jupyter-notebook' && isWorkspaceTrusted && config.notebook.globalToolbar != true && jupyter.ispythonnotebook`,
	`notebookKernel =~ /^ms-toolsai.jupyter\\// && notebookType == 'jupyter-notebook' && isWorkspaceTrusted && jupyter.ispythonnotebook && jupyter.kernel.isjupyter`,
	`notebookKernel =~ /^ms-toolsai.jupyter\\// && notebookType == 'jupyter-notebook' && isWorkspaceTrusted && jupyter.kernel.isjupyter`,
	`notebookKernel =~ /^ms-toolsai.jupyter\\// && notebookType == 'jupyter-notebook' && isWorkspaceTrusted && jupyter.notebookeditor.canrestartNotebookkernel && config.notebook.globalToolbar != true`,
	`notebookKernel =~ /^ms-toolsai.jupyter\\// && notebookType == jupyter-notebook && jupyter.ispythonnotebook && notebookCellType == code && isWorkspaceTrusted && resource not in jupyter.notebookeditor.runByLineDocuments || !notebookKernel && notebookType == jupyter-notebook && jupyter.ispythonnotebook && notebookCellType == code && isWorkspaceTrusted`,
	`notebookKernel =~ /^paiqo.databricks-vscode\\// && isWorkspaceTrusted || isWorkspaceTrusted && !notebookKernel`,
	`notebookKernel =~ /^paiqo.databricks-vscode\\// && notebookType == 'jupyter-notebook' && isWorkspaceTrusted`,
	`notebookKernel =~ /polyglot-notebook/ && isWorkspaceTrusted`,
	`notebookOpen`,
	`notebookOutputFocused`,
	`notebookType == 'ballerina-notebook' && isNotebookDebugModeEnabled`,
	`notebookType == 'ballerina-notebook'`,
	`notebookType == 'c3-notebook'`,
	`notebookType == 'jupyter-notebook' && config.jupyter.showOutlineButtonInNotebookToolbar`,
	`notebookType == 'jupyter-notebook' && isWorkspaceTrusted && config.notebook.globalToolbar != true && config.notebook.kernelPicker.type != mru`,
	`notebookType == 'jupyter-notebook' && isWorkspaceTrusted && config.notebook.globalToolbar != true`,
	`notebookType == 'jupyter-notebook' && isWorkspaceTrusted && jupyter.replayLogLoaded`,
	`notebookType == 'jupyter-notebook' && isWorkspaceTrusted && notebookKernel =~ /julialang/`,
	`notebookType == 'jupyter-notebook' && isWorkspaceTrusted`,
	`notebookType == 'node-notebook' && isWorkspaceTrusted && notebookCellType == code`,
	`notebookType == 'node-notebook' && isWorkspaceTrusted`,
	`notebookType == 'wolfram-language-notebook'`,
	`notebookType == cap-notebook`,
	`notebookType == ecl-notebook && cellLangId == ecl && isPrivate`,
	`notebookType == ecl-notebook && cellLangId == ecl && isPublic`,
	`notebookType == ecl-notebook && cellLangId == sql`,
	`notebookType == github-issues && notebookCellType == code && notebookCellHasOutputs`,
	`notebookType == interactive && notebookCellType == code`,
	`notebookType == jupyter-notebook && isWorkspaceTrusted`,
	`notebookType == jupyter-notebook && notebookCellType == code`,
	`notebookType == jupyter-notebookxxx && notebookCellType == code`,
	`notebookType == kusto-notebook || notebookType == jupyter-notebook`,
	`notebookType == kusto-notebook`,
	`notebookType == ojs-notebook && cellLangId == sql`,
	`notebookType == q-notebook`,
	`notebookType == runme`,
	`notebookType=='sonic-pi-book'`,
	`notificationCenterVisible`,
	`notificationFocus && notificationToastsVisible`,
	`notificationFocus`,
	`notificationToastsVisible`,
	`notionPageFocus && !notionPageBookmark`,
	`notionPageFocus && notionPageBookmark`,
	`notionPageFocus`,
	`nowProjectActive && editorHasSelection && resourceExtname == .js`,
	`nowProjectActive && resourceExtname =~ /(.js)/ && resource =~ /(/background(.*)scripts/)/`,
	`nowProjectActive && resourceExtname =~ /(.js|.css|.html|.xml)/ && resource =~ /((/scratch/(?!update)(.*)/)|((/src/).*/)).*$/`,
	`nowProjectActive && resourceExtname =~ /(.js|.css|.html|.xml)/ && resource =~ /((/scratch/(?!update)(.*)/)|(/src/))(?=(Forms(.*)UI/UI(.*)Pages/)|(Service(.*)Portal/Widgets/)).*$/`,
	`nowProjectActive && resourceExtname =~ /(.js|.css|.html|.xml|.now)/ && resource =~ /((/scratch/(?!update)(.*)/)|((/src/).*/)).*$/`,
	`npxScriptsModulePresent && !npxScriptsRunning`,
	`npxScriptsModulePresent && npxScriptsRunning`,
	`npxScriptsModulePresent`,
	`nrf-connect.application.active.images > 1 || nrf-connect.apps > 1`,
	`nrf-connect.application.activeImage && !nrf-connect.build.isStale`,
	`nrf-connect.application.activeImage && !nrf-connect.build.isTFM`,
	`nrf-connect.application.activeImage && nrf-connect.hasDeviceTree && !nrf-connect.build.isTFM`,
	`nrf-connect.application.activeImage`,
	`nrf-connect.apps > 0 && nrf-connect.application.activeImage && !nrf-connect.build.isTFM`,
	`nrf-connect.apps.loaded`,
	`nrf-connect.build.loaded && !nrf-connect.build.isStale`,
	`nrf-connect.build.loaded && nrf-connect.build.isStale`,
	`nrf-connect.noCommandLineTools`,
	`null`,
	`ocaml.documentation-server-on`,
	`op-vscode.injectable == true`,
	`openDiffOnClick && view == prStatus:github && viewItem =~ /filechange(?!:DELETE)/`,
	`openFolderWorkspaceSupport`,
	`openapi.platform.credentials == present`,
	`openapiThreeEnabled`,
	`openapiTwoEnabled && resourceScheme =~ /^untitled$|^file$/ || openapiThreeEnabled && resourceScheme =~ /^untitled$|^file$/`,
	`openapiTwoEnabled || openapiThreeEnabled`,
	`openapiTwoEnabled`,
	`oracleToolbar.cancelDisabledVisibility`,
	`oracleToolbar.cancelVisibility`,
	`oracleToolbar.clearDisabledVisibility`,
	`oracleToolbar.clearVisibility`,
	`originalResourceScheme == vsls-scc && !liveshare:isReadOnlySession`,
	`os.platform != win32 && workbenchState != empty && trunk.initRequired`,
	`os.platform != win32 && workbenchState == empty || os.platform != win32 && trunk.notRepo`,
	`os.platform == win32`,
	`p5-server.runIconEnabled && p5-server.available && p5-server.browser != integrated && resourceExtname =~ /\\.(js|html)/`,
	`p5-server.runIconEnabled && p5-server.available && p5-server.browser == integrated && resourceExtname =~ /\\.(js|html)/`,
	`pacCLI.authPanel.interactiveLoginSupported`,
	`packageMetrics:packageJsonActive && !packageMetrics:packageMetricsRunning`,
	`packageMetrics:packageJsonActive && packageMetrics:packageMetricsRunning`,
	`panel.activeView.id == 'references' && !config.codeIntel.disableSearchBased`,
	`panelFocus && activePanel == 'workbench.panel.output'`,
	`panelFocus && activeViewlet == 'workbench.panel.terminal'`,
	`parameterHintsMultipleSignatures && parameterHintsVisible && textInputFocus`,
	`paste-html-as-jsx:showSwapClipboardAction == true`,
	`pcf-builder:isPCF && !isWeb`,
	`pcf-builder:isPCF`,
	`perforce.activation.foundOutOfRoot`,
	`perforce.activation.hasScmProvider && config.perforce.explorer.showBasicOpCommands`,
	`perforce.activation.hasScmProvider && config.perforce.explorer.showFileOpCommands`,
	`perforce.activation.hasScmProvider && config.perforce.explorer.showSyncCommands`,
	`perforce.activation.hasScmProvider`,
	`perforce.activation.noP4Config`,
	`pfrIsRunning`,
	`pin-up.have-pinned-files`,
	`pioCoreReady && config.platformio-ide.showEditorTitleShortcuts`,
	`pioCoreReady`,
	`pioMultiEnvProject && view == platformio-ide.projectTasks`,
	`pioProjectReady && config.platformio-ide.showEditorTitleShortcuts`,
	`pioProjectReady && view == platformio-ide.projectTasks`,
	`pioProjectReady`,
	`plcc:isIEC61131Proj`,
	`ponicodeShowGitDiff:active`,
	`ponicodeTreeView:active`,
	`pontContext.initManager&&!pontContext.initError&&view == pontOrigins&&pontContext.multipleOrigins`,
	`pontContext.initManager&&!pontContext.initError&&view == pontOrigins`,
	`pontContext.isInit&&view == pontOrigins`,
	`pontContext.noConfigFile`,
	`pontContext.versionError`,
	`preview-jp2Focus`,
	`preview-mp4Focus`,
	`preview-tiffFocus`,
	`preview:isFocused && preview:isViteStarted`,
	`preview:isFocused || resourceLangId == vue`,
	`preview:isFocused`,
	`previewjs.componentsDetected`,
	`problemFocus`,
	`problemsFilterFocus`,
	`problemsVisibility && !relatedInformationFocus && focusedView == 'workbench.panel.markers.view'`,
	`projectManager.canShowTreeViewAny`,
	`projectManager.canShowTreeViewGit`,
	`projectManager.canShowTreeViewMercurial`,
	`projectManager.canShowTreeViewSVN`,
	`projectManager.canShowTreeViewVSCode`,
	`pros.isPROSProject`,
	`protoOutlineEnabled`,
	`psl.isProfileElement`,
	`puppet:puppetfileEnabled`,
	`pvs-server-active && editorLangId == pvs`,
	`pvs-server-active && proof-explorer.visible`,
	`pvs-server-active && proof-mate.visible`,
	`pvs-server-active`,
	`python.datascience.featureenabled`,
	`python.datascience.hascodecells && python.datascience.featureenabled`,
	`python.datascience.haveinteractive && python.datascience.featureenabled`,
	`python.datascience.haveinteractivecells && python.datascience.featureenabled`,
	`python.datascience.haveredoablecells && python.datascience.featureenabled`,
	`python.hasActiveTensorBoardSession && !virtualWorkspace && shellExecutionSupported`,
	`python.hasActiveTensorBoardSession`,
	`python2go:isPythonInstalled && python2go.showIpythonIcon`,
	`python2go:isPythonInstalled && resourceLangId == python && python2go.showGreenPlayIcon`,
	`python2go:isPythonInstalled && resourceLangId == python && python2go.showYellowPlayIcon`,
	`python2go:isPythonInstalled`,
	`pythonPreviewFocus`,
	`qbs:extension-activated`,
	`quarkusProjectExistsOrLightWeight && editorLangIdSupportsQute && !editorQuteValidationEnabled`,
	`quarkusProjectExistsOrLightWeight && editorLangIdSupportsQute && editorQuteValidationEnabled`,
	`quarkusProjectExistsOrLightWeight && resourceExtname == .java`,
	`quarkusProjectExistsOrLightWeight`,
	`queryViewEnabled`,
	`quickAction.isAvailable == true && editorTextFocus`,
	`quickbookPreviewActive`,
	`quitControl.isActive && isWindows && config.quitControl.enableQuitKeybindingOnWindows && config.quitControl.closeTabShouldTryToCloseEmptyWindow && !editorIsOpen && !multipleEditorGroups || quitControl.isActive && isMac && config.quitControl.closeTabShouldTryToCloseEmptyWindow && !editorIsOpen && !multipleEditorGroups || quitControl.isActive && isLinux && config.quitControl.closeTabShouldTryToCloseEmptyWindow && !editorIsOpen && !multipleEditorGroups`,
	`quitControl.isActive && isWindows && config.quitControl.enableQuitKeybindingOnWindows || quitControl.isActive && isMac || quitControl.isActive && isLinux`,
	`quokka.isActiveEditorRunningQuokka && !quokka.lineHasRemovableInlineValues && quokka.fileHasRemovableInlineValues && editorTextFocus && !suggestWidgetVisible && !findWidgetVisible && !renameInputVisible && !referenceSearchVisible && !inReferenceSearchEditor && !quickFixWidgetVisible && !parameterHintsVisible && !editorHasSelection`,
	`quokka.isActiveEditorRunningQuokka && quokka.lineHasRemovableInlineValues && editorTextFocus && !suggestWidgetVisible && !findWidgetVisible && !renameInputVisible && !referenceSearchVisible && !inReferenceSearchEditor && !quickFixWidgetVisible && !parameterHintsVisible`,
	`quokka.isCodeStoryEnabled && quokka.isPro && activeEditor && !quokka.isLiveShareClient && quokka.isActiveEditorRunningQuokka`,
	`quokka.isPro && activeEditor && !quokka.isLiveShareClient && quokka.isActiveEditorRunningQuokka && !quokka.traceBeingNavigated`,
	`quokka.startedAtLeastOnce`,
	`r.WorkspaceViewer:show && !r.liveShare:isGuest`,
	`r.WorkspaceViewer:show`,
	`r.helpViewer.show`,
	`r.liveShare:aborted`,
	`rad4.currentproject.apptype != FNCApplication`,
	`rad4.currentproject.apptype == FNCApplication`,
	`rapidapi:paw:signedIn`,
	`rapidapi:request:focus == true && rapidapi:view != 'fullscreen'`,
	`rapidapi:request:focus == true && rapidapi:view != 'request'`,
	`rapidapi:request:focus == true && rapidapi:view != 'response'`,
	`rapidapi:request:focus == true && rapidapi:view == 'fullscreen'`,
	`rapidapi:request:focus == true && rapidapi:view == 'request'`,
	`rapidapi:request:focus == true && rapidapi:view == 'response'`,
	`raycast.workspaceEnabled`,
	`reTypewriter.isNotPlaying`,
	`reTypewriter.isPlaying`,
	`reactHierarchy.loadError == 'badEntryFile'`,
	`reactHierarchy.loadError == 'noEntryFile'`,
	`reactHierarchy.loadError == 'noWorkspace'`,
	`reactHierarchy.loadingTreeView != true`,
	`reactHierarchy.loadingTreeView == true`,
	`readmeMarkdownFocused`,
	`refactorPreview.enabled && refactorPreview.hasCheckedChanges && focusedView == 'refactorPreview'`,
	`reference-list.hasResult && references-view.canNavigate`,
	`referenceSearchVisible && !config.editor.stablePeek`,
	`remix-light.debug == true`,
	`remote-containers:needsDockerStartOrInstall`,
	`remote-containers:needsSSHCredentials`,
	`remote-containers:noContainersWithFolder`,
	`remote-containers:noContainersWithoutFolder`,
	`remote-containers:noDevVolumes`,
	`remote-containers:showDetails`,
	`remote-wsl-recommender.gettingStarted.available`,
	`remote-wsl-recommender.openWSLFolder.available`,
	`remoteFileDialogVisible`,
	`remoteHub:hasVirtualFolders && !gitHub:hasPullRequestExtension  || scmProvider == remoteHub:azurerepos`,
	`remoteHub:hasVirtualFolders && !gitHub:hasPullRequestExtension || scmProvider == remoteHub:azurerepos`,
	`remoteHub:hasVirtualFolders && !isMobile`,
	`remoteHub:hasVirtualFolders && isInDiffEditor && resourceScheme == vscode-vfs`,
	`remoteHub:hasVirtualFolders && remoteHub.timeline.selectedForCompare && timelineItem =~ /vscode-vfs:(.+?):file\\b/ && !listMultiSelection`,
	`remoteHub:hasVirtualFolders && timelineItem =~ /vscode-vfs:(.+?):file:commit\\b/ && !listMultiSelection`,
	`remoteHub:hasVirtualFolders && timelineItem =~ /vscode-vfs:(.+?):file\\b/ && !listMultiSelection`,
	`remoteHub:hasVirtualFolders && timelineItem =~ /vscode-vfs:azurerepos:file:commit\\b/ && !listMultiSelection`,
	`remoteHub:hasVirtualFolders && timelineItem =~ /vscode-vfs:github:file:commit\\b/ && !listMultiSelection`,
	`remoteHub:hasVirtualFolders`,
	`remoteName !=~ /codespaces*.*/ && github.hasGitHubRepo || remoteName !=~ /codespaces*.*/ && remoteHub:virtualFolderProviders =~ /\\bgithub\\b/`,
	`remoteName == attached-container`,
	`remoteName == wsl && workspaceFolderCount != 0`,
	`remoteName == wsl`,
	`remoteName =~ /^(dev-container|attached-container|exec|k8s-container)$/ && remoteConnectionState == connected`,
	`remoteName =~ /^(dev-container|attached-container|exec|k8s-container)$/`,
	`remoteName =~ /^(dev-container|attached-container|exec|k8s-container|wsl|ssh-remote)?$/`,
	`remoteName =~ /^(dev-container|wsl|ssh-remote)$/`,
	`remoteName =~ /^(dev|attached)-container$/ && tunnelType == Forwarded`,
	`remoteName =~ /^(dev|attached)-container$/`,
	`remoteName =~ /^(wsl)?$/ && view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.resource\\.pod/i`,
	`remoteName =~ /^(wsl|ssh-remote)$/`,
	`remoteName =~ /^dev-container$/`,
	`remoteName =~ /^ssh-remote$/ && remoteConnectionState == connected && config.remote.SSH.enableWebAccess`,
	`remoteName =~ /^ssh-remote$/ && remoteConnectionState == connected`,
	`remoteName =~ /^ssh-remote$/ && remoteConnectionState == disconnected`,
	`remoteName =~ /^ssh-remote$/`,
	`replaceActive && searchViewletVisible && !findWidgetVisible`,
	`replaceInputBoxFocus && searchViewletVisible`,
	`resource && !explorerResourceIsFolder && !listMultiSelection && codesight.setupComplete && !codesight.coverityInstalled`,
	`resource && !explorerResourceIsFolder && !listMultiSelection && codesight.setupComplete && !codesight.sigmaInstalled`,
	`resource && !explorerResourceIsFolder && !listMultiSelection && codesight.setupComplete && codesight.coverityInstalled && codesight.scanMode == 'auto'`,
	`resource && !explorerResourceIsFolder && !listMultiSelection && codesight.setupComplete && codesight.coverityInstalled && codesight.scanMode == 'manual'`,
	`resource && !explorerResourceIsFolder && !listMultiSelection && codesight.setupComplete && codesight.sigmaInstalled && codesight.scanMode == 'auto'`,
	`resource && !explorerResourceIsFolder && !listMultiSelection && codesight.setupComplete && codesight.sigmaInstalled && codesight.scanMode == 'manual'`,
	`resource && !explorerResourceIsFolder`,
	`resource && !inDebugMode`,
	`resource && explorerResourceIsFolder`,
	`resource && explorerResourceIsRoot && !listMultiSelection && codesight.setupComplete && !codesight.bdToolAvailable`,
	`resource && explorerResourceIsRoot && !listMultiSelection && codesight.setupComplete && !codesight.coverityInstalled`,
	`resource && explorerResourceIsRoot && !listMultiSelection && codesight.setupComplete && !codesight.sigmaInstalled`,
	`resource && explorerResourceIsRoot && !listMultiSelection && codesight.setupComplete && codesight.bdToolAvailable && codesight.scanMode == 'auto'`,
	`resource && explorerResourceIsRoot && !listMultiSelection && codesight.setupComplete && codesight.bdToolAvailable && codesight.scanMode == 'manual'`,
	`resource && explorerResourceIsRoot && !listMultiSelection && codesight.setupComplete && codesight.coverityInstalled && codesight.scanMode == 'auto'`,
	`resource && explorerResourceIsRoot && !listMultiSelection && codesight.setupComplete && codesight.coverityInstalled && codesight.scanMode == 'manual'`,
	`resource && explorerResourceIsRoot && !listMultiSelection && codesight.setupComplete && codesight.sigmaInstalled && codesight.scanMode == 'auto'`,
	`resource && explorerResourceIsRoot && !listMultiSelection && codesight.setupComplete && codesight.sigmaInstalled && codesight.scanMode == 'manual'`,
	`resource =~ '/.*[A-Z][1-9]?_.*/[A-Z][1-9]?_.*\\.hs$/' || resource =~ '/.*[A-Z][1-9]?_.*/[A-Z][1-9]?_.*\\.kt$/' || resource =~ '/.*[A-Z][1-9]?_.*/[A-Z][1-9]?_.*\\.cpp$/' || resource =~ '/.*[A-Z][1-9]?_.*/[A-Z][1-9]?_.*\\.c$/' || resource =~ '/.*[A-Z][1-9]?_.*/[A-Z][1-9]?_.*\\.py$/' || resource =~ '/.*[A-Z][1-9]?_.*/[A-Z][1-9]?_.*\\.java$/'`,
	`resource =~ /.+\\.labels-meta.xml/`,
	`resource =~ /.+\\.profile-meta.xml/ || resource =~ /.+\\.permissionset-meta.xml/`,
	`resource =~ /./aura/ && resourceExtname =~ /^(?!(.app|.cmp|.auradoc|.evt|.intf|.css|.design|.html|.js|.svg)$)/`,
	`resource =~ /./force-app/ && (resourceExtname == '' || resourceExtname == .cls || resourceExtname == .trigger || resourceExtname == .js)`,
	`resource =~ /./force-app/ && resourceExtname == ''`,
	`resource =~ /./force-app/ && resourceExtname == .xml`,
	`resource =~ //FileCabinet/(SuiteScripts|Templates/(E-mail%20Templates|Marketing%20Templates)|Web%20Site%20Hosting%20Files|SuiteApps)/.+$/`,
	`resource =~ //Objects/.+\\.xml$/`,
	`resource =~ //_build/libs// && lifeboatapi.isSWWorkspace`,
	`resource =~ /^(.(?!/_build/))*$/ && !explorerResourceIsFolder && resourceExtname == .lua && lifeboatapi.isSWWorkspace`,
	`resource =~ /^(.*)/src/pages/(.*)$/ && resourceExtname == .json && resourceScheme == ftfs && editorLangId == json`,
	`resource =~ /^.*[\\/]{1}Actions[\\/]{1}.*$|^.*[\\/]{1}Actions$/`,
	`resource =~ /^.*[\\/]{1}Extensions$/`,
	`resource =~ /^.*[\\/]{1}Globals[\\/]{1}.*$|^.*[\\/]{1}Globals$/`,
	`resource =~ /^.*[\\/]{1}Pages[\\/]{1}.*$|^.*[\\/]{1}Pages$/`,
	`resource =~ /^.*[\\/]{1}Rules[\\/]{1}.*$|^.*[\\/]{1}Rules$/`,
	`resource =~ /^.*[\\/]{1}Services[\\/]{1}.*$|^.*[\\/]{1}Services$/`,
	`resource =~ /^.*[\\/]{1}Styles[\\/]{1}.*$|^.*[\\/]{1}Styles$/`,
	`resource =~ /^.*[\\/]{1}i18n[\\/]{1}.*$|^.*[\\/]{1}i18n$/`,
	`resource =~ /^acid:gitlabfile1/`,
	`resource =~ /_ctx_gerritDiff/ || resource =~ /_ctx_hasUnresolved/ || resource =~ /_ctx_patchsetLevel/`,
	`resource =~ /abilities/ && resourceExtname == '.lua' && config.dota2-tools.A1.module_list.kv_lua_associated`,
	`resource in jsDebugCanPrettyPrint`,
	`resourceDirname =~ /(cn|en)(\\/|\\\\)(JavaScript|Java|TypeScript|Go|Python3|Java|C\\+\\+)/`,
	`resourceDirname =~ /\\.tinkerun/ && resourceLangId == php`,
	`resourceDirname=~/.logs/export && resourceFilename=~/exportValidation.json/`,
	`resourceExtname != .asc && resourceExtname != .gpg && resourceExtname != .sig`,
	`resourceExtname != .csv`,
	`resourceExtname != .json`,
	`resourceExtname != .xml`,
	`resourceExtname && !editorReadonly && raycast.workspaceEnabled`,
	`resourceExtname == ''`,
	`resourceExtname == '.cls'`,
	`resourceExtname == '.code-snippets' && !snippetsmanagerCodeSnippetsEditorFocus`,
	`resourceExtname == '.code-snippets'&& !snippetsmanagerCodeSnippetsEditorFocus`,
	`resourceExtname == '.elf'`,
	`resourceExtname == '.erd'`,
	`resourceExtname == '.exe'`,
	`resourceExtname == '.log'`,
	`resourceExtname == '.mp4'`,
	`resourceExtname == '.scss' || resourceExtname == '.css' || resourceExtname == '.less' || resourceExtname == '.vue' || resourceExtname == '.jsx' || resourceExtname == '.html'`,
	`resourceExtname == '.sm'`,
	`resourceExtname == '.txt' || resourceExtname == '.kv'`,
	`resourceExtname == '.yang'`,
	`resourceExtname == .ASM || resourceExtname == .asm && editorFocus`,
	`resourceExtname == .abi || resourceExtname == .json`,
	`resourceExtname == .abi`,
	`resourceExtname == .algo`,
	`resourceExtname == .apk`,
	`resourceExtname == .app && ForceCodeActive && ForceCodeLoggedIn`,
	`resourceExtname == .applet && resource =~ /\\.applet$/`,
	`resourceExtname == .applet && resource =~ /\\.applet\\?OpenAsText$/`,
	`resourceExtname == .arb && dart-code:anyFlutterProjectLoaded`,
	`resourceExtname == .arrow`,
	`resourceExtname == .asc`,
	`resourceExtname == .ascx && config.webforms.resources.open`,
	`resourceExtname == .ascx`,
	`resourceExtname == .asm`,
	`resourceExtname == .auradoc`,
	`resourceExtname == .avm`,
	`resourceExtname == .axml`,
	`resourceExtname == .bclicense`,
	`resourceExtname == .beml`,
	`resourceExtname == .bin`,
	`resourceExtname == .blp`,
	`resourceExtname == .bpmn`,
	`resourceExtname == .bxml`,
	`resourceExtname == .bytecode && config.jvm-bytecode-viewer.editorTitleBarContextMenu`,
	`resourceExtname == .bytecode && config.jvm-bytecode-viewer.editorTitleBar`,
	`resourceExtname == .cfc`,
	`resourceExtname == .changespec`,
	`resourceExtname == .class && !config.jvm-bytecode-viewer.defaultToVerboseOutput && config.jvm-bytecode-viewer.editorTitleBar`,
	`resourceExtname == .class && !explorerResourceIsFolder && config.jvm-bytecode-viewer.explorerContextMenu`,
	`resourceExtname == .class && config.jvm-bytecode-viewer.defaultToVerboseOutput && config.jvm-bytecode-viewer.editorTitleBar`,
	`resourceExtname == .class && config.jvm-bytecode-viewer.editorTitleBarContextMenu`,
	`resourceExtname == .class || resourceExtname == .bytecode`,
	`resourceExtname == .class || resourceExtname == .verbose`,
	`resourceExtname == .cls || resource =~ /.+\\.app-meta.xml/ || resource =~ /.+\\.md-meta.xml/ || resource =~ /.+\\.customPermission-meta.xml/ || resource =~ /.+\\.object-meta.xml/ || resource =~ /.+\\.dataSource-meta.xml/ || resource =~ /.+\\.field-meta.xml/ || resource =~ /.+\\.page/ || resource =~ /.+\\.recordType-meta.xml/ || resource =~ /.+\\.tab-meta.xml/ || resource =~ /.+\\.layout-meta.xml/`,
	`resourceExtname == .cls`,
	`resourceExtname == .cmp`,
	`resourceExtname == .cpp && config.codepal.enableAclDocumentationButton`,
	`resourceExtname == .cshtml`,
	`resourceExtname == .csproj || resourceExtname == .cs || explorerResourceIsFolder`,
	`resourceExtname == .csproj || resourceExtname == .fsproj || resourceExtname == .vbproj`,
	`resourceExtname == .csproj || resourceExtname == .vbproj || resourceExtname == .fsproj || resourceExtname == .sln`,
	`resourceExtname == .csproj || resourceExtname == .vbproj || resourceExtname == .fsproj`,
	`resourceExtname == .csproj`,
	`resourceExtname == .css || resourceExtname == .less || resourceExtname == .scss || resourceExtname == .sass`,
	`resourceExtname == .csv && resourceScheme == mcfs`,
	`resourceExtname == .csv`,
	`resourceExtname == .cwe`,
	`resourceExtname == .dgibi && config.gibiane-vscode.afficherBouttonDExecution`,
	`resourceExtname == .dsl || resourceExtname == .vl`,
	`resourceExtname == .env`,
	`resourceExtname == .esenv`,
	`resourceExtname == .esind`,
	`resourceExtname == .esquery`,
	`resourceExtname == .exe`,
	`resourceExtname == .exml`,
	`resourceExtname == .feature && config.anthem.bddTest.enabled`,
	`resourceExtname == .feature`,
	`resourceExtname == .fsproj`,
	`resourceExtname == .fsql`,
	`resourceExtname == .fx || resourceExtname == .fxh`,
	`resourceExtname == .fx`,
	`resourceExtname == .fxml`,
	`resourceExtname == .gdat || resourceExtname == .cdat || resourceExtname == .scan`,
	`resourceExtname == .geojson || resourceExtname == .kml || resourceExtname == .csv || resourceExtname == .gpx || resourceExtname == .igc || resourceExtname == .gml`,
	`resourceExtname == .gf`,
	`resourceExtname == .ghw || resourceExtname == .vcd`,
	`resourceExtname == .glb`,
	`resourceExtname == .gltf || resourceExtname == .glb`,
	`resourceExtname == .gltf`,
	`resourceExtname == .gml`,
	`resourceExtname == .gpg`,
	`resourceExtname == .groovy`,
	`resourceExtname == .gs`,
	`resourceExtname == .gz`,
	`resourceExtname == .har && config.cypressHelper.menuItems.ParseHAR`,
	`resourceExtname == .har`,
	`resourceExtname == .hl7 || resourceExtname == .ccda || resourceExtname == .xml || resourceExtname == .json`,
	`resourceExtname == .hpp`,
	`resourceExtname == .hprof`,
	`resourceExtname == .hs || resourceExtname == .kt || resourceExtname == .cpp || resourceExtname == .c || resourceExtname == .py || resourceExtname == .java`,
	`resourceExtname == .html && config.http-server.chromeType == 'canary'`,
	`resourceExtname == .html && config.http-server.chromeType == 'developer'`,
	`resourceExtname == .html && config.http-server.chromeType == 'normal'`,
	`resourceExtname == .html && config.http-server.firefoxType == 'developer'`,
	`resourceExtname == .html && config.http-server.firefoxType == 'nightly'`,
	`resourceExtname == .html && config.http-server.firefoxType == 'normal'`,
	`resourceExtname == .html && isMac`,
	`resourceExtname == .html && isWindows`,
	`resourceExtname == .html || resourceExtname == .css || resourceExtname == .js || resourceExtName == .json || resourceExtname == .xml || resourceExtname == .png || resourceExtname == .jpg || resourceExtname == .gif || resourceExtname == .xap || resourceExtname == .xsl || resourceExtname == .ico || resourceExtname == .svg || resourceExtname == .resx`,
	`resourceExtname == .html || resourceExtname == .wxml || resourceExtname == .vue`,
	`resourceExtname == .html`,
	`resourceExtname == .http || resourceExtname == .dhttp && dothttpRunMode == full`,
	`resourceExtname == .httpbook || resourceExtname == .hnbk && dothttpRunMode == full`,
	`resourceExtname == .iarc`,
	`resourceExtname == .icon`,
	`resourceExtname == .impex`,
	`resourceExtname == .in`,
	`resourceExtname == .ino`,
	`resourceExtname == .ioc`,
	`resourceExtname == .ipk`,
	`resourceExtname == .ipynb && isWorkspaceTrusted`,
	`resourceExtname == .jar && config.jvm-bytecode-viewer.editorTitleBarContextMenu`,
	`resourceExtname == .jar && config.jvm-bytecode-viewer.editorTitleBar`,
	`resourceExtname == .jar || resourceExtname == .war || resourceExtname == .ear || resourceExtname == .zip || resourceExtname == .apk`,
	`resourceExtname == .jar`,
	`resourceExtname == .java && java:serverMode == Standard`,
	`resourceExtname == .java`,
	`resourceExtname == .jimple || resourceExtname == .grimple`,
	`resourceExtname == .jobspec`,
	`resourceExtname == .jpeg || resourceExtname == .jpg || resourceExtname == .png`,
	`resourceExtname == .js || resourceExtname == .ts`,
	`resourceExtname == .js`,
	`resourceExtname == .jsmm`,
	`resourceExtname == .json && config.cypressHelper.menuItems.SchemaGenerator`,
	`resourceExtname == .json && lingua:enabled`,
	`resourceExtname == .json && resourceFilename != config.json`,
	`resourceExtname == .json || resourceExtname == .digitaljs`,
	`resourceExtname == .json || resourceExtname == .yaml`,
	`resourceExtname == .json`,
	`resourceExtname == .jsx`,
	`resourceExtname == .key`,
	`resourceExtname == .ksy`,
	`resourceExtname == .launch`,
	`resourceExtname == .less`,
	`resourceExtname == .lib`,
	`resourceExtname == .liquid`,
	`resourceExtname == .log || resourceExtname == .tree || resourceExtname == .kbb`,
	`resourceExtname == .log || resourceExtname == .tree`,
	`resourceExtname == .lyx`,
	`resourceExtname == .m && editorHasSelection`,
	`resourceExtname == .m`,
	`resourceExtname == .map`,
	`resourceExtname == .mcfunction`,
	`resourceExtname == .md && dendron:pluginActive && shellExecutionSupported || resourceExtname == .yml && dendron:pluginActive && shellExecutionSupported`,
	`resourceExtname == .md && dendron:pluginActive && shellExecutionSupported`,
	`resourceExtname == .md`,
	`resourceExtname == .mdl`,
	`resourceExtname == .mdx`,
	`resourceExtname == .mgcb`,
	`resourceExtname == .mmd`,
	`resourceExtname == .mq4 && config.mql_tools.ShowButton.Check == true|| resourceExtname == .mq5 && config.mql_tools.ShowButton.Check == true || resourceExtname == .mqh && config.mql_tools.ShowButton.Check == true`,
	`resourceExtname == .mq4 && config.mql_tools.ShowButton.Compile == true || resourceExtname == .mq5 && config.mql_tools.ShowButton.Compile == true || resourceExtname == .mqh && config.mql_tools.ShowButton.Compile == true`,
	`resourceExtname == .mq4 && config.mql_tools.ShowButton.Script == true || resourceExtname == .mq5 && config.mql_tools.ShowButton.Script == true || resourceExtname == .mqh && config.mql_tools.ShowButton.Script == true`,
	`resourceExtname == .mq4 || resourceExtname == .mq5 || resourceExtname == .mqh`,
	`resourceExtname == .mq4 || resourceExtname == .mq5`,
	`resourceExtname == .mqh`,
	`resourceExtname == .ms || resourceExtname == .mcr`,
	`resourceExtname == .nb || resourceExtname == .m || resourceExtname == .wl || resourceExtname == .wls || resourceExtname == .cdf`,
	`resourceExtname == .now`,
	`resourceExtname == .npy`,
	`resourceExtname == .npz`,
	`resourceExtname == .object || resourceFilename =~ /.object-meta.xml/`,
	`resourceExtname == .out`,
	`resourceExtname == .page && ForceCodeActive && ForceCodeLoggedIn`,
	`resourceExtname == .pat || resourceExtname == .nlp`,
	`resourceExtname == .patch`,
	`resourceExtname == .pdb || resourceExtname == .pdb.gz || resourceExtname == .PDB || resourceExtname == .mol2 || resourceExtname == .MOL2 || resourceExtname == .sdf || resourceExtname == .SDF || resourceExtname == .mmCIF || resourceExtname == .mmcif || resourceExtname == .cif.gz || resourceExtname == .MMCIF || resourceExtname == .Mol || resourceExtname == .mol || resourceExtname == .MOL || resourceExtname == .xyz || resourceExtname == .XYZ || resourceExtname == .ent || resourceExtname == .ENT || resourceExtname == .pdbqt || resourceExtname == .PDBQT || resourceExtname == .cif || resourceExtname == .CIF || resourceExtname == .mcif || resourceExtname == .MCIF || resourceExtname == .gro || resourceExtname == .GRO`,
	`resourceExtname == .php`,
	`resourceExtname == .png || resourceExtname == .gif || resourceExtname == .jpg || resourceExtname == .bmp || resourceExtname == .tif || resourceExtname == .svg || resourceExtname == .webp || resourceExtname == .jfif`,
	`resourceExtname == .png || resourceExtname == .gif || resourceExtname == .jpg`,
	`resourceExtname == .png || resourceExtname == .jpg || resourceExtname == .jpeg || resourceExtname == .svg || resourceExtname == .gif || resourceExtname == .ico`,
	`resourceExtname == .png || resourceExtname == .jpg || resourceExtname == .jpeg`,
	`resourceExtname == .ppm`,
	`resourceExtname == .prj || resourceExtname == .fu || resourceExtname == .pu || resourceExtname == .tu`,
	`resourceExtname == .prj`,
	`resourceExtname == .ps1 && config.poshProTools.toolbar.packageAsExecutable`,
	`resourceExtname == .ps1 && config.poshProTools.toolbar.psScriptPad`,
	`resourceExtname == .ps1 && config.poshProTools.toolbar.runInNewTerminal`,
	`resourceExtname == .ps1 || resourceExtname == .psm1 `,
	`resourceExtname == .ps1`,
	`resourceExtname == .pt`,
	`resourceExtname == .puml`,
	`resourceExtname == .pvs && editorLangId == pvs || resourceExtname == .tccs && editorLangId == pvs || resourceExtname == .hpvs && editorLangId == pvs`,
	`resourceExtname == .pvs && pvs-server-active && editorLangId == pvs`,
	`resourceExtname == .py`,
	`resourceExtname == .pyw`,
	`resourceExtname == .qhelp && isWorkspaceTrusted`,
	`resourceExtname == .qlref`,
	`resourceExtname == .qml`,
	`resourceExtname == .qmod`,
	`resourceExtname == .qrc && resourceLangId == xml`,
	`resourceExtname == .qrc || resourceExtname == .ui`,
	`resourceExtname == .razor`,
	`resourceExtname == .resource`,
	`resourceExtname == .robot && !isInDiffEditor`,
	`resourceExtname == .robot || explorerResourceIsFolder`,
	`resourceExtname == .robot || resourceExtname == .resource || explorerResourceIsFolder`,
	`resourceExtname == .rqlScratchpad && RAWLabs.raw.correctWorkspaceState && !RAWLabs.raw.isDemo`,
	`resourceExtname == .rqlScratchpad && RAWLabs.raw.correctWorkspaceState`,
	`resourceExtname == .rs`,
	`resourceExtname == .rsh`,
	`resourceExtname == .rst`,
	`resourceExtname == .rul`,
	`resourceExtname == .sab && defaultInstanceEnv == 'v4_streamingAnalytics'`,
	`resourceExtname == .sab && defaultInstanceEnv == 'v5_cpd' && consoleEnabled == true`,
	`resourceExtname == .sab && defaultInstanceEnv == 'v5_cpd'`,
	`resourceExtname == .sab && defaultInstanceEnv == 'v5_standalone'`,
	`resourceExtname == .sab`,
	`resourceExtname == .sass && editorHasSelection`,
	`resourceExtname == .sass`,
	`resourceExtname == .scd`,
	`resourceExtname == .scss`,
	`resourceExtname == .sh && sheller.showMenuMakeScriptExecutable`,
	`resourceExtname == .sig`,
	`resourceExtname == .sln`,
	`resourceExtname == .smali || editorLangId == smali`,
	`resourceExtname == .snapiScratchpad && RAWLabs.raw.correctWorkspaceState && !RAWLabs.raw.isDemo`,
	`resourceExtname == .snapiScratchpad && RAWLabs.raw.isDemo`,
	`resourceExtname == .sol`,
	`resourceExtname == .spk`,
	`resourceExtname == .sprj`,
	`resourceExtname == .spsql`,
	`resourceExtname == .sql && resourceScheme == mcfs`,
	`resourceExtname == .sql && sap-database-explorer:vsc_db_switch`,
	`resourceExtname == .sql`,
	`resourceExtname == .styl`,
	`resourceExtname == .svg`,
	`resourceExtname == .swift || explorerResourceIsFolder`,
	`resourceExtname == .sysl && activeCustomEditorId != 'sysl.multiView'`,
	`resourceExtname == .target`,
	`resourceExtname == .tf || explorerResourceIsFolder`,
	`resourceExtname == .tldr`,
	`resourceExtname == .tosca && activeCustomEditorId == bp.OpenToscaJsonEditor`,
	`resourceExtname == .tosca`,
	`resourceExtname == .ts || resourceExtname == .js || resourceExtname == ''`,
	`resourceExtname == .ts || resourceExtname == .js`,
	`resourceExtname == .ts || resourceExtname == .tsx`,
	`resourceExtname == .ts`,
	`resourceExtname == .ttf || resourceExtname == .woff || resourceExtname == .svg`,
	`resourceExtname == .txt || resourceExtname == .nlp || resourceExtname == .dict || resourceExtname == .kbb`,
	`resourceExtname == .txt || resourceExtname == .nlp || resourceExtname == .dict`,
	`resourceExtname == .txt`,
	`resourceExtname == .txxt || resourceExtname == .txt || resourceExtname == .nlp || resourceExtname == .pat`,
	`resourceExtname == .txxt || resourceExtname == .txt`,
	`resourceExtname == .txxt`,
	`resourceExtname == .ui && resourceLangId == xml`,
	`resourceExtname == .ui`,
	`resourceExtname == .update`,
	`resourceExtname == .url`,
	`resourceExtname == .vbproj`,
	`resourceExtname == .verbose && config.jvm-bytecode-viewer.editorTitleBarContextMenu`,
	`resourceExtname == .verbose && config.jvm-bytecode-viewer.editorTitleBar`,
	`resourceExtname == .vhdl || resourceExtname == .vhd || resourceExtname == .vht`,
	`resourceExtname == .vhdl || resourceExtname == .vhd`,
	`resourceExtname == .vue || resourceExtname == .nvue || resourceExtname == .html`,
	`resourceExtname == .vue || resourceExtname == .nvue || resourceExtname == .pug || resourceExtname == .jade`,
	`resourceExtname == .vue`,
	`resourceExtname == .wgt`,
	`resourceExtname == .ws`,
	`resourceExtname == .wsdl`,
	`resourceExtname == .xaml`,
	`resourceExtname == .xar`,
	`resourceExtname == .xlf`,
	`resourceExtname == .xls || resourceExtname  == .xlsx`,
	`resourceExtname == .xml && sap.ux.applicationModeler.dataEditor.showDataEditorCommands`,
	`resourceExtname == .xml`,
	`resourceExtname == .xrj`,
	`resourceExtname == .yaml || karavan:loaded`,
	`resourceExtname == .yaml || resourceExtname == .yml`,
	`resourceExtname == .yaml`,
	`resourceExtname == .yma`,
	`resourceExtname == .yml || resourceExtname == .yaml`,
	`resourceExtname == .yml`,
	`resourceExtname == .zip && alExtensionActive && !inDebugMode`,
	`resourceExtname == .zip && isAppcanProject && !inDebugMode`,
	`resourceExtname == .zip || resourceExtname == .csar`,
	`resourceExtname == .zip`,
	`resourceExtname =~ /(cds|csn|xml)/ && editorTextFocus`,
	`resourceExtname =~ /(cds|csn|xml|edmx)/ || explorerResourceIsFolder`,
	`resourceExtname =~ /.tsx?$|.jsx?$/`,
	`resourceExtname =~ /\\.(avi|wmv|flv|mkv)$/`,
	`resourceExtname =~ /\\.(conf|ucs|qkview)/`,
	`resourceExtname =~ /\\.(groovy|java|xml|js|kts|yaml)$/`,
	`resourceExtname =~ /\\.(java|xml|yaml|yml)$/`,
	`resourceExtname =~ /\\.(png|jpg|jpeg|svg|gif)$/`,
	`resourceExtname =~ /\\.(png|tga|tpic)$/`,
	`resourceExtname =~ /\\.(properties)$/`,
	`resourceExtname =~ /\\.(ts|js|tsx|jsx|vue|html|json|yaml|yml)$/`,
	`resourceExtname =~ /\\.(wav|wma)$/`,
	`resourceExtname =~ /\\.(ya?ml)/`,
	`resourceExtname =~ /\\.c$|\\.cpp$|\\.c\\+\\+$|\\.cc$|\\.cxx$|\\.axf$|\\.elf$/ && cl.eide.projectActived && !config.EIDE.Option.DisableExplorerContextMenu`,
	`resourceExtname =~ /\\.c$|\\.cpp$|\\.c\\+\\+$|\\.cc$|\\.cxx$|\\.info$|\\.axf$|\\.elf$/ && cl.eide.projectActived && !config.EIDE.Option.DisableEditorContextMenu`,
	`resourceExtname =~ /\\.elf-symbols$/ && cl.eide.projectActived`,
	`resourceExtname =~ /\\.icns$/`,
	`resourceExtname =~ /\\.iff/i`,
	`resourceExtname =~ /\\.json$/`,
	`resourceExtname =~ /\\.png$|\\.jpg$|\\.jpeg$/`,
	`resourceExtname =~ /^.jsx$|^.tsx$|^.css$|^.less$/`,
	`resourceExtname =~ /^.png$|^.jpg$|^.gif$|^.jpeg$|^.svg$|^.tif$|^.bmp$/`,
	`resourceExtname =~ /^\\.(babylon|gltf|glb|obj)$/`,
	`resourceExtname =~ /^\\.sv$|^\\.v$|^\\.vh$|^\\.lua$/`,
	`resourceExtname =~/^\\.(png|jpg|jpeg|webp|gif|bmp|tiff|ico|svg)$/i`,
	`resourceExtname in codegen-cpp.headerFileExt && editorLangId == cpp || editorLangId == c`,
	`resourceExtname in devtools.resourcesExtn && resourceFilename in devtools.linkedResources`,
	`resourceExtname in devtools.resourcesExtn`,
	`resourceExtname!=.dfm && resourceExtname!=.dpr && resourceExtname!=.dproj`,
	`resourceExtname==.java`,
	`resourceFilename != combined.cpp && resourceExtname == .cpp`,
	`resourceFilename != final.tree && resourceExtname == .tree`,
	`resourceFilename != gen.hs && resourceFilename != brute.hs && resourceExtname == .hs || resourceFilename != gen.kt && resourceFilename != brute.kt && resourceExtname == .kt || resourceFilename != gen.cpp && resourceFilename != brute.cpp && resourceExtname == .cpp || resourceFilename != gen.c && resourceFilename != brute.c && resourceExtname == .c || resourceFilename != gen.py && resourceFilename != brute.py && resourceExtname == .py || resourceFilename != gen.java && resourceFilename != brute.java && resourceExtname == .java`,
	`resourceFilename != gen.kt && resourceFilename != brute.kt && resourceExtname == .kt || resourceFilename != gen.cpp && resourceFilename != brute.cpp && resourceExtname == .cpp || resourceFilename != gen.c && resourceFilename != brute.c && resourceExtname == .c || resourceFilename != gen.py && resourceFilename != brute.py && resourceExtname == .py || resourceFilename != gen.java && resourceFilename != brute.java && resourceExtname == .java`,
	`resourceFilename == 'Application.app'`,
	`resourceFilename == 'app.json' && resourceScheme == ftfs && editorLangId == json`,
	`resourceFilename == 'box.json'`,
	`resourceFilename == 'dub.json'`,
	`resourceFilename == 'node_modules'`,
	`resourceFilename == 'package.json'`,
	`resourceFilename == .env`,
	`resourceFilename == APIResource`,
	`resourceFilename == App.Variables.qlikview-vars`,
	`resourceFilename == CMakeLists.txt \u0026\u0026 cmake:enableFullFeatureSet`,
	`resourceFilename == CMakeLists.txt`,
	`resourceFilename == Dockerfile || resourceFilename =~ /\\.Dockerfile$/`,
	`resourceFilename == Functions`,
	`resourceFilename == Inputs`,
	`resourceFilename == MAPINFO.txt`,
	`resourceFilename == MapInfo.txt`,
	`resourceFilename == Mapinfo.txt`,
	`resourceFilename == NewIssue.md`,
	`resourceFilename == Outputs`,
	`resourceFilename == aad.template.json && fx-extension.isAadManifestEnabled`,
	`resourceFilename == angular.json`,
	`resourceFilename == api`,
	`resourceFilename == apktool.yml`,
	`resourceFilename == azure-iot-device-twin.json`,
	`resourceFilename == azure-iot-module-twin.json`,
	`resourceFilename == b.ts || resourceFilename == p.ts || resourceFilename == n.ts`,
	`resourceFilename == build.gradle`,
	`resourceFilename == bundle.cnab`,
	`resourceFilename == bundle.json`,
	`resourceFilename == componentType.xml`,
	`resourceFilename == dartdoc_options.yaml && dart-code:anyProjectLoaded`,
	`resourceFilename == deployment.template.json`,
	`resourceFilename == deployment.xml`,
	`resourceFilename == document.json`,
	`resourceFilename == expect.imljson`,
	`resourceFilename == global-var.less`,
	`resourceFilename == globalConfig.json`,
	`resourceFilename == go.mod`,
	`resourceFilename == host.json`,
	`resourceFilename == index.ts || resourceFilename == index.js || resourceFilename == index.tsx || resourceFilename == index.jsx`,
	`resourceFilename == interface.imljson`,
	`resourceFilename == libman.json`,
	`resourceFilename == manifest.json`,
	`resourceFilename == manifest.local.template.json || resourceFilename == manifest.remote.template.json || resourceFilename == manifest.template.json && !fx-extension.isV3Enabled`,
	`resourceFilename == manifest.template.json && fx-extension.isConfigUnifyEnabled`,
	`resourceFilename == mapinfo.txt`,
	`resourceFilename == melos.yaml`,
	`resourceFilename == models.jsonc || resourceFilename =~ /.json_models/`,
	`resourceFilename == module.json`,
	`resourceFilename == multiversx.json`,
	`resourceFilename == node_modules`,
	`resourceFilename == orgs.json && CCDXActive`,
	`resourceFilename == package.json`,
	`resourceFilename == parameters.imljson`,
	`resourceFilename == pom.xml && config.boot-java.rewrite.refactorings.on == true`,
	`resourceFilename == pom.xml`,
	`resourceFilename == project.config.json`,
	`resourceFilename == proxy.js`,
	`resourceFilename == pubspec.yaml && dart-code:anyFlutterProjectLoaded && dart-code:pubOutdatedSupported`,
	`resourceFilename == pubspec.yaml && dart-code:anyFlutterProjectLoaded`,
	`resourceFilename == pubspec.yaml && dart-code:anyProjectLoaded && !dart-code:anyFlutterProjectLoaded && dart-code:pubOutdatedSupported`,
	`resourceFilename == pubspec.yaml && dart-code:anyProjectLoaded && !dart-code:anyFlutterProjectLoaded`,
	`resourceFilename == pubspec.yaml`,
	`resourceFilename == quarkReport.json`,
	`resourceFilename == requirements.txt`,
	`resourceFilename == requirements.yaml`,
	`resourceFilename == sdkconfig`,
	`resourceFilename == service.xml`,
	`resourceFilename == smart.json`,
	`resourceFilename == sounds || resourceFilename == sprites`,
	`resourceFilename == task.json`,
	`resourceFilename == tcbuild.yaml`,
	`resourceFilename == testnet.toml`,
	`resourceFilename == thisshouldneverexist`,
	`resourceFilename == tpl.tpl || resourceFilename =~ /p\\d+\\.tpl/`,
	`resourceFilename == tpl.tsx || resourceFilename =~ /(tpl|p\\d+)\\.ts/ || resourceFilename =~ /(tpl|p\\d+)\\.tpl/ || resourceFilename =~ /\\w*\\.wxml/`,
	`resourceFilename == user-info`,
	`resourceFilename == vcpkg-configuration.json && resourcePath in vcpkg.activeConfigurationPaths`,
	`resourceFilename == vcpkg-configuration.json && resourcePath not in vcpkg.activeConfigurationPaths`,
	`resourceFilename == vex_project_settings.json && resource in vexrobotics.vexcode.vexProjectSettingsFolderArr`,
	`resourceFilename == yarn.lock`,
	`resourceFilename =~ /(.*Application)/`,
	`resourceFilename =~ /(.*Domain)/`,
	`resourceFilename =~ /(.*\\.csx)|(.*\\.tt)$/`,
	`resourceFilename =~ /(.*\\.gradle)|(.*\\.gradle\\.kts)|(pom\\.xml)$/ && java:serverMode == Standard`,
	`resourceFilename =~ /(.*\\.gradle)|(.*\\.gradle\\.kts)|(pom\\.xml)$/`,
	`resourceFilename =~ /(.sql)|(.hql)|(Untitled)/`,
	`resourceFilename =~ /(\\.action)|(\\.reducer)|(\\.effect)/`,
	`resourceFilename =~ /(\\.action)|(\\.reducer)|(\\.effect)|(\\.component\\.css)|(\\.component\\.sass)|(\\.component\\.less)|(\\.component\\.html)|(\\.component\\.scss)|(\\.component\\.ts)|(\\.component\\.spec\\.ts)$/`,
	`resourceFilename =~ /(\\.component\\.css)|(\\.component\\.sass)|(\\.component\\.less)|(\\.component\\.html)|(\\.component\\.scss)|(\\.component\\.ts)|(\\.component\\.spec\\.ts)$/`,
	`resourceFilename =~ /(idx)$/`,
	`resourceFilename =~ /(vxcore)$/`,
	`resourceFilename =~ /(vxe|out|elfcore)$/ || explorerResourceIsRoot`,
	`resourceFilename =~ /(wvr|wva)$/`,
	`resourceFilename =~ /.*(-meta\\.xml|\\.cls)$/`,
	`resourceFilename =~ /.*(.fgb|.gpx|.geo.json|.geojson|.gml|.igc|.kgl.json|keplergl.json|keplergl_time.json|.kml|.map.json|.shp|.topo.json|.topojson|.wkt)/`,
	`resourceFilename =~ /.*(permissionset|profile)-meta\\.xml$/`,
	`resourceFilename =~ /.*\\.(bpmn|bpmn2)/`,
	`resourceFilename =~ /.*\\.(chart.json5)/`,
	`resourceFilename =~ /.*\\.(csdl.xml)/`,
	`resourceFilename =~ /.*\\.(csv|fgb|igc|gml|gpx|json|geojson|kgl|kml|shp|topojson|wkt)/`,
	`resourceFilename =~ /.*\\.(csv|fgb|igc|json|gml|gpx|geojson|kgl|kml|shp|topojson|wkt)/`,
	`resourceFilename =~ /.*\\.(edmx)/`,
	`resourceFilename =~ /.*\\.(edmx.xml)/`,
	`resourceFilename =~ /.*\\.(jsc.json5?|md)/`,
	`resourceFilename =~ /.*\\.(json|jsonl|json5|hjson|ndjson|arrow|arr|avro|parquet|parq|config|env|properties|ini|yaml|yml|md|csv|tsv|txt|tab|dif|ods|xls|xlsb|xlsm|xlsx|xml|html)/`,
	`resourceFilename =~ /.*\\.(metadata.xml)/`,
	`resourceFilename =~ /.*\\.(vega|vg.json|vl.json|vg|vl)/`,
	`resourceFilename =~ /.*\\.(vuerd.json)/`,
	`resourceFilename =~ /.*\\.dashasp$/`,
	`resourceFilename =~ /.*\\.exml/`,
	`resourceFilename =~ /.*\\.log/`,
	`resourceFilename =~ /.*\\.prj/i`,
	`resourceFilename =~ /.*\\.ptm/i`,
	`resourceFilename =~ /.*\\.txt/i`,
	`resourceFilename =~ /.*mht$/`,
	`resourceFilename =~ /.+\\.cprj$/`,
	`resourceFilename =~ /.+\\.csolution\\.ya?ml$/`,
	`resourceFilename =~ /.+\\.py$/`,
	`resourceFilename =~ /.+\\.svg$/i`,
	`resourceFilename =~ /.cls/`,
	`resourceFilename =~ /.scen.json/`,
	`resourceFilename =~ /.state.dart/`,
	`resourceFilename =~ /@2x\\.(jpg|png|webp)$/`,
	`resourceFilename =~ /ApplicationSet_.+\\.properties/`,
	`resourceFilename =~ /Mapinfo\\.txt/ig`,
	`resourceFilename =~ /Merchant\\.txt/ig`,
	`resourceFilename =~ /[.](didact)[.](md|adoc)$/`,
	`resourceFilename =~ /[Ss]kaffold/`,
	`resourceFilename =~ /\\.(hbs|handlebars)$/`,
	`resourceFilename =~ /\\.(jsonc?|yaml|yml)$/ && resourceScheme == file`,
	`resourceFilename =~ /\\.(jsonc?|yaml|yml)?$/ && resourceScheme == file`,
	`resourceFilename =~ /\\.(yaml|yml)$/ && resourceScheme == file`,
	`resourceFilename =~ /\\.abi$|\\.abi\\.json$|\\.tvc$/i`,
	`resourceFilename =~ /\\.abi\\.json$/i`,
	`resourceFilename =~ /\\.bc$/ || resourceFilename =~ /^a\\.out$/ || resourceFilename =~ /\\.d\\.byte$/`,
	`resourceFilename =~ /\\.cds$/i`,
	`resourceFilename =~ /\\.cdsprettier.json/`,
	`resourceFilename =~ /\\.cpp$/i`,
	`resourceFilename =~ /\\.drawio$/i`,
	`resourceFilename =~ /\\.snap\\.new$/`,
	`resourceFilename =~ /\\.sol$/i`,
	`resourceFilename =~ /\\.spec\\.ts$/ && config.anthem.unitTest.enabled`,
	`resourceFilename =~ /\\.tests\\.ps1$/i`,
	`resourceFilename =~ /\\.tsx?/`,
	`resourceFilename =~ /\\.xlf$|\\.xliff$/`,
	`resourceFilename =~ /\\w*/`,
	`resourceFilename =~ /\\w*\\.ts/`,
	`resourceFilename =~ /^((?!settings).)*\\.gradle(\\.kts)?$/`,
	`resourceFilename =~ /^(Ut|St)/ && resourceLangId == psl && psl.runCoverageContext`,
	`resourceFilename =~ /^(Ut|St)/ && resourceLangId == psl && psl.runTestContext`,
	`resourceFilename =~ /^.+\\.component\\.(spec.ts|scss|css|sass|less|html|ts)$/`,
	`resourceFilename =~ /^.+\\.directive\\.(spec.ts|ts)$/`,
	`resourceFilename =~ /^.+\\.guard\\.(spec.ts|ts)$/`,
	`resourceFilename =~ /^.+\\.module\\.(spec.ts|ts)$/`,
	`resourceFilename =~ /^.+\\.service\\.(spec.ts|ts)$/`,
	`resourceFilename =~ /^[^.]+$/ || resourceExtname == .jsx || resourceExtname == .js || resourceExtname == .tsx || resourceExtname == .ts`,
	`resourceFilename =~ /^api$/`,
	`resourceFilename =~ /^changelog(.md)?$/i`,
	`resourceFilename =~ /^dataservice$/`,
	`resourceFilename =~ /^deployment(?!.*\\.template\\.json)(\\.debug)?(\\..+)*\\.json$/`,
	`resourceFilename =~ /^deployment(\\..+)?\\.template\\.json$/`,
	`resourceFilename =~ /^endpoints$/`,
	`resourceFilename =~ /^inbound-endpoints$/`,
	`resourceFilename =~ /^local-entries$/`,
	`resourceFilename =~ /^manifest.(local|remote).template.json$/ && !fx-extension.isConfigUnifyEnabled`,
	`resourceFilename =~ /^message-processors$/`,
	`resourceFilename =~ /^message-stores$/`,
	`resourceFilename =~ /^proxy-services$/`,
	`resourceFilename =~ /^sequences$/`,
	`resourceFilename =~ /^tasks$/`,
	`resourceFilename =~ /^templates$/`,
	`resourceFilename =~ /a\\d+\\.ts/ || resourceFilename =~ /s\\d+\\.ts/`,
	`resourceFilename =~ /addon_.*/ && resourceExtname == '.txt'`,
	`resourceFilename =~ /azure.yaml/i`,
	`resourceFilename =~ /cucumber_report\\.json$/ && config.anthem.bddTest.enabled`,
	`resourceFilename =~ /datapackage.json/`,
	`resourceFilename =~ /docs/`,
	`resourceFilename =~ /dub\\.json|dub\\.sdl/`,
	`resourceFilename =~ /html\\.ts/`,
	`resourceFilename =~ /job.json/`,
	`resourceFilename =~ /manifest.[\\w]*.json/ && resourceDirname =~ /build/ && !fx-extension.isV3Enabled`,
	`resourceFilename =~ /mock.json$/ `,
	`resourceFilename =~ /mock\\.json$/ `,
	`resourceFilename =~ /porter\\.yaml/i`,
	`resourceFilename =~ /scrypttest\\.(js|ts)$/`,
	`resourceFilename =~ /xml/ && editorIsOpen && XMLLSReady`,
	`resourceFilename in ext.supportedDeployFiles`,
	`resourceFilename in ext.supportedUpFilenames`,
	`resourceFilename in veracode.pipelineScanResultsFilenameMenu`,
	`resourceFilename==config.js`,
	`resourceFilename==cosmos-result.json`,
	`resourceFilename==function.json`,
	`resourceFilename==local.settings.json`,
	`resourceFilename==serverless.yaml`,
	`resourceFilename==workflow.json`,
	`resourceFilename=~ /.*(?<!\\.cshtml|\\.razor)\\.cs$/ && isController`,
	`resourceFilename=~ /.*\\.cshtml$/ && isPage`,
	`resourceFilename=~ /.*\\.cshtml$/ && isView`,
	`resourceFilename=~ /.*\\.cshtml\\.cs$/ && isPageModel`,
	`resourceFilename=~ /.*\\.eventGenerator\\.jsonc?/i`,
	`resourceFilename=~ /.*\\.razor$/ && resourceScheme == file`,
	`resourceFilename=~ /.*\\.razor\\.cs$/ && resourceScheme == file`,
	`resourceFilename=~/(.*cosmos-collection[.]json)(?![a-z])/i`,
	`resourceFilename=~/(.*cosmos-collection[.]json)/`,
	`resourceFilename=~/(.*cosmos-document[.]json)(?![a-z])/i`,
	`resourceFilename=~/(.*cosmos-document[.]json)/`,
	`resourceFilename=~/(.*cosmos-stored-procedure[.]js)(?![a-z])/i`,
	`resourceFilename=~/(.*cosmos-stored-procedure[.]js)/`,
	`resourceIsFile && resourceExtname == .tsx && isStencilProject`,
	`resourceIsFile && resourceFilename == SUMMARY.md`,
	`resourceIsFile`,
	`resourceLangId  == 'json'`,
	`resourceLangId  == html`,
	`resourceLangId  == json || resourceLangId == yaml`,
	`resourceLangId  == typescript || resourceLangId  == typescriptreact`,
	`resourceLangId  == xml`,
	`resourceLangId != 'forcesourcemanifest'`,
	`resourceLangId != *`,
	`resourceLangId != gkd.archive && resourceLangId != gkd.xlog  && resourceLangId != gkd.alauda`,
	`resourceLangId != markdown`,
	`resourceLangId ==  dart`,
	`resourceLangId ==  javascript`,
	`resourceLangId ==  javascriptreact`,
	`resourceLangId ==  q`,
	`resourceLangId ==  typescript`,
	`resourceLangId ==  typescriptreact`,
	`resourceLangId ==  vue`,
	`resourceLangId == 'CSV (semicolon)' && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`resourceLangId == 'apex' || resourceLangId == 'visualforce' || resourceLangId == 'xml'`,
	`resourceLangId == 'apex'`,
	`resourceLangId == 'cfml'|| explorerResourceIsFolder`,
	`resourceLangId == 'cm'`,
	`resourceLangId == 'csv (caret)' && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`resourceLangId == 'csv (colon)' && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`resourceLangId == 'csv (dot)' && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`resourceLangId == 'csv (double quote)' && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`resourceLangId == 'csv (equals)' && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`resourceLangId == 'csv (hyphen)' && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`resourceLangId == 'csv (pipe)' && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`resourceLangId == 'csv (pipe)'`,
	`resourceLangId == 'csv (semicolon)' && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`resourceLangId == 'csv (semicolon)'`,
	`resourceLangId == 'csv (tilde)' && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`resourceLangId == 'csv (whitespace)' && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`resourceLangId == 'dothttp-vscode' && dothttpRunMode == full`,
	`resourceLangId == 'esquery'`,
	`resourceLangId == 'java'`,
	`resourceLangId == 'javascript'`,
	`resourceLangId == 'json'`,
	`resourceLangId == 'markdown'`,
	`resourceLangId == 'michelson' && debugProtocolVariableMenuContext == 'address'`,
	`resourceLangId == 'michelson' && debugProtocolVariableMenuContext == 'contract'`,
	`resourceLangId == 'obj'`,
	`resourceLangId == 'spritefile'`,
	`resourceLangId == 'spriteimage'`,
	`resourceLangId == 'uiflow'`,
	`resourceLangId == 'yaml'`,
	`resourceLangId == 3dformat`,
	`resourceLangId == 4gl`,
	`resourceLangId == 7800basic || resourceLangId == batariBasic || resourceLangId == dasm || resourceLangId == makefile || resourceLangId == bat || resourceLangId == shellscript`,
	`resourceLangId == 7800basic || resourceLangId == batariBasic || resourceLangId == dasm || resourceLangId == makefile ||resourceLangId == bat || resourceLangId == shellscript`,
	`resourceLangId == 7800basic || resourceLangId == batariBasic`,
	`resourceLangId == AngularDoc`,
	`resourceLangId == CSV && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`resourceLangId == CSV && resourceScheme != sky-csv`,
	`resourceLangId == CSV`,
	`resourceLangId == Frida`,
	`resourceLangId == TSV && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`resourceLangId == aar`,
	`resourceLangId == advpl`,
	`resourceLangId == aegs`,
	`resourceLangId == ael`,
	`resourceLangId == ahk && !isInDiffEditor && !virtualWorkspace && shellExecutionSupported`,
	`resourceLangId == al`,
	`resourceLangId == alapp`,
	`resourceLangId == alk && !alk.canRun`,
	`resourceLangId == alk && alk.canRun`,
	`resourceLangId == alk`,
	`resourceLangId == amdil`,
	`resourceLangId == aml || resourceLangId == aml-json`,
	`resourceLangId == angelscript`,
	`resourceLangId == antlr`,
	`resourceLangId == apex && ForceCodeActive && ForceCodeLoggedIn`,
	`resourceLangId == apex && editorHasSelection && ForceCodeActive && ForceCodeLoggedIn`,
	`resourceLangId == apex && editorHasSelection`,
	`resourceLangId == apex || resourceFilename =~ /-meta.xml/`,
	`resourceLangId == apex`,
	`resourceLangId == applescript`,
	`resourceLangId == argdown`,
	`resourceLangId == arm-template`,
	`resourceLangId == arm`,
	`resourceLangId == armabin`,
	`resourceLangId == asa || resourceLangId == asax || resourceLangId == ascx || resourceLangId == ashx || resourceLangId == asmx || resourceLangId == asp || resourceLangId == aspx || resourceLangId == axd || resourceLangId == cshtml || resourceLangId == ejs || resourceLangId == htm || resourceLangId == html || resourceLangId == inc || resourceLangId == jsp || resourceLangId == jspf || resourceLangId == jspx || resourceLangId == mas || resourceLangId == master || resourceLangId == mi || resourceLangId == php || resourceLangId == shtml || resourceLangId == skin || resourceLangId == tag || resourceLangId == vm || resourceLangId == xhtml || resourceLangId == as || resourceLangId == javascriptreact || resourceLangId == javascript || resourceLangId == typescriptreact`,
	`resourceLangId == asaql`,
	`resourceLangId == asciidoc`,
	`resourceLangId == asm && config.nand2tetris.showRunIconInEditorTitleMenu`,
	`resourceLangId == asm && config.nand2tetris.showTranslateIconInEditorTitleMenu`,
	`resourceLangId == asm-collection || resourceLangId == asm-list-file`,
	`resourceLangId == asp`,
	`resourceLangId == aspnetcorerazor`,
	`resourceLangId == astro && resourceScheme == file`,
	`resourceLangId == astro`,
	`resourceLangId == atasm || resourceLangId == json`,
	`resourceLangId == autolisp && autolisp.hasProject`,
	`resourceLangId == autolisp || resourceLangId == autolispdcl`,
	`resourceLangId == autolisp`,
	`resourceLangId == backlog`,
	`resourceLangId == ballerina || isBallerinaDiagram`,
	`resourceLangId == ballerina`,
	`resourceLangId == bash`,
	`resourceLangId == bat && !isInDiffEditor && shellExecutionSupported`,
	`resourceLangId == bat && shellExecutionSupported`,
	`resourceLangId == bat`,
	`resourceLangId == bazel`,
	`resourceLangId == beam`,
	`resourceLangId == bibtex`,
	`resourceLangId == bicep && config.bicep.experimental.enablePasteAsBicep`,
	`resourceLangId == bicep`,
	`resourceLangId == blitzmax`,
	`resourceLangId == blk`,
	`resourceLangId == bngl && resourceExtname == .bngl`,
	`resourceLangId == bngl && resourceExtname == .cdat`,
	`resourceLangId == bngl && resourceExtname == .gdat`,
	`resourceLangId == bngl && resourceExtname == .graphml`,
	`resourceLangId == bngl && resourceExtname == .scan`,
	`resourceLangId == bpmn`,
	`resourceLangId == bridlensis`,
	`resourceLangId == bsl && editorHasSelection`,
	`resourceLangId == bsl`,
	`resourceLangId == c \u0026\u0026 cmake:enableFullFeatureSet`,
	`resourceLangId == c || resourceLangId == asm || resourceLangId == gas || resourceLangId == sinomcu`,
	`resourceLangId == c || resourceLangId == cpp || explorerResourceIsFolder`,
	`resourceLangId == c || resourceLangId == cpp || resourceLangId == cuda-cpp || resourceLangId == objective-c || resourceLangId == objective-cpp`,
	`resourceLangId == c || resourceLangId == cpp || resourceLangId == h || resourceLangId == hpp || explorerResourceIsFolder`,
	`resourceLangId == c || resourceLangId == cpp || resourceLangId == h || resourceLangId == hpp`,
	`resourceLangId == c || resourceLangId == cpp || resourceLangId == python || resourceLangId == java`,
	`resourceLangId == c || resourceLangId == cpp`,
	`resourceLangId == c || resourceLangId == dts || resourceLangId == dtsi`,
	`resourceLangId == c4`,
	`resourceLangId == c`,
	`resourceLangId == captions && resourceScheme == 'file'`,
	`resourceLangId == cblite && explorerResourceIsFolder`,
	`resourceLangId == cds`,
	`resourceLangId == cfml`,
	`resourceLangId == ciceroMark || resourceLangId == concerto`,
	`resourceLangId == clips && !clips-ide.terminalOpen`,
	`resourceLangId == clips && clips-ide.terminalOpen`,
	`resourceLangId == cm`,
	`resourceLangId == cml`,
	`resourceLangId == cobol`,
	`resourceLangId == codscript`,
	`resourceLangId == composer`,
	`resourceLangId == compressionTargetFile && !listMultipleSelection`,
	`resourceLangId == concerto`,
	`resourceLangId == coq`,
	`resourceLangId == cosmosScreen`,
	`resourceLangId == cpp && clangd.enableTypeHierarchy`,
	`resourceLangId == cpp && cpp.enableTypeHierarchy`,
	`resourceLangId == cpp && editorHasSelection`,
	`resourceLangId == cpp \u0026\u0026 cmake:enableFullFeatureSet`,
	`resourceLangId == cpp || resourceLangId == proto3`,
	`resourceLangId == cpp`,
	`resourceLangId == cql`,
	`resourceLangId == cscs`,
	`resourceLangId == csharp && cmd-gdUnit3:is-ready == true`,
	`resourceLangId == csharp || resourceLangId == javascript || resourceLangId == typescript || resourceLangId == cpp || resourceLangId == c || resourceLangId == python`,
	`resourceLangId == csharp || resourceLangId == typescript`,
	`resourceLangId == csharp || resourceLangId == vb || explorerResourceIsFolder`,
	`resourceLangId == csharp`,
	`resourceLangId == css && ForceCodeActive && ForceCodeLoggedIn`,
	`resourceLangId == css && editorHasSelection`,
	`resourceLangId == css `,
	`resourceLangId == css || editorLangId == css`,
	`resourceLangId == css`,
	`resourceLangId == csv && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`resourceLangId == csv && resourceScheme != sky-csv`,
	`resourceLangId == csv`,
	`resourceLangId == d2`,
	`resourceLangId == d`,
	`resourceLangId == daml`,
	`resourceLangId == dart && !flutter_preview.isActive`,
	`resourceLangId == dart && dart-code:anyProjectLoaded && dart-code:currentFileIsRunnable`,
	`resourceLangId == dart && dart-code:anyProjectLoaded`,
	`resourceLangId == dart && resourceFilename =~ /(?<!_test)\\.dart/`,
	`resourceLangId == dart && resourceFilename =~ /_test.dart/`,
	`resourceLangId == dart && resourceScheme == file && resource =~ /\\/lib\\//`,
	`resourceLangId == dart`,
	`resourceLangId == db && !explorerResourceIsFolder`,
	`resourceLangId == db`,
	`resourceLangId == dbz`,
	`resourceLangId == dfa`,
	`resourceLangId == dicom || config.dicom.alwaysShowMenu`,
	`resourceLangId == dll`,
	`resourceLangId == dlt-log`,
	`resourceLangId == dockerfile`,
	`resourceLangId == dot || resourceLangId == dotuml`,
	`resourceLangId == dotLottie`,
	`resourceLangId == dot`,
	`resourceLangId == ecl && resourceExtname == .ecl`,
	`resourceLangId == ecl`,
	`resourceLangId == ecmarkup`,
	`resourceLangId == edi`,
	`resourceLangId == edk2_dsc`,
	`resourceLangId == edk2_inf`,
	`resourceLangId == edk2_uni`,
	`resourceLangId == electron-builder-js`,
	`resourceLangId == electron-builder-json`,
	`resourceLangId == electron-builder-toml`,
	`resourceLangId == electron-builder-yaml`,
	`resourceLangId == emulicious-debugger.supportedRoms`,
	`resourceLangId == encodingImgTargetFile && !listMultiSelection`,
	`resourceLangId == erlang`,
	`resourceLangId == excel`,
	`resourceLangId == eyepattern`,
	`resourceLangId == fdl`,
	`resourceLangId == feature || resourceLangId == behave`,
	`resourceLangId == feature || resourceLangId == python`,
	`resourceLangId == feature`,
	`resourceLangId == fitnesse`,
	`resourceLangId == fortran-modern || resourceLangId == fortran`,
	`resourceLangId == fsharp`,
	`resourceLangId == galaxy`,
	`resourceLangId == gantt`,
	`resourceLangId == gcl-p`,
	`resourceLangId == git-commit`,
	`resourceLangId == gkd.Alauda`,
	`resourceLangId == gkd.alauda`,
	`resourceLangId == gkd.archive`,
	`resourceLangId == gkd.json || resourceExtname == .json`,
	`resourceLangId == gkd.xlog || resourceLangId == gkd.alauda || resourceLangId == gkd.json || resourceExtname == .json`,
	`resourceLangId == gkd.xlog`,
	`resourceLangId == glsl`,
	`resourceLangId == go && tinygoHasPreview`,
	`resourceLangId == go`,
	`resourceLangId == greyscript`,
	`resourceLangId == groovy || resourceLangId == jenkinsfile`,
	`resourceLangId == gtkw_wavebin || resourceLangId == gtkw_waveconfig`,
	`resourceLangId == hack && config.nand2tetris.showRunIconInEditorTitleMenu`,
	`resourceLangId == handlebars`,
	`resourceLangId == happenings && false`,
	`resourceLangId == happenings`,
	`resourceLangId == harmony`,
	`resourceLangId == haskell && !inDebugMode`,
	`resourceLangId == haskell && editorTextFocus && !inDebugMode`,
	`resourceLangId == haskell`,
	`resourceLangId == hcl`,
	`resourceLangId == hdl && config.nand2tetris.showRunIconInEditorTitleMenu`,
	`resourceLangId == hlsl`,
	`resourceLangId == hsp3`,
	`resourceLangId == html && !virtualWorkspace`,
	`resourceLangId == html && ForceCodeActive && ForceCodeLoggedIn`,
	`resourceLangId == html && editorHasSelection`,
	`resourceLangId == html || resourceExtname == .yrf`,
	`resourceLangId == html || resourceLangId == javascript || resourceLangId == css || resourceLangId == json`,
	`resourceLangId == html || resourceLangId == javascript || resourceLangId == vue`,
	`resourceLangId == html || resourceLangId == xml || resourceExtname == .pdf`,
	`resourceLangId == html`,
	`resourceLangId == ic10`,
	`resourceLangId == icX`,
	`resourceLangId == iconfont`,
	`resourceLangId == iec_src`,
	`resourceLangId == image`,
	`resourceLangId == imagesFiles`,
	`resourceLangId == inform6`,
	`resourceLangId == inform7 || resourceLangId == inform7extension`,
	`resourceLangId == inform7`,
	`resourceLangId == innosetup`,
	`resourceLangId == irite`,
	`resourceLangId == isabelle-ml`,
	`resourceLangId == isabelle`,
	`resourceLangId == isml`,
	`resourceLangId == jack && config.nand2tetris.showTranslateIconInEditorTitleMenu`,
	`resourceLangId == java && includeBeta`,
	`resourceLangId == java || resourceLangId == feature`,
	`resourceLangId == java`,
	`resourceLangId == javascript && !llse:termianl`,
	`resourceLangId == javascript && ForceCodeActive && ForceCodeLoggedIn`,
	`resourceLangId == javascript && config.jsx-sfc.icon.splitEditors`,
	`resourceLangId == javascript && editorHasSelection`,
	`resourceLangId == javascript && llse:termianl`,
	`resourceLangId == javascript && ponicode:hasLastActiveWebview`,
	`resourceLangId == javascript && qunitrunner.readyForUX && qunitrunner.chromeInstalled`,
	`resourceLangId == javascript && qunitrunner.readyForUX && qunitrunner.edgeInstalled`,
	`resourceLangId == javascript && qunitrunner.readyForUX && qunitrunner.firefoxInstalled`,
	`resourceLangId == javascript && qunitrunner.readyForUX && qunitrunner.ieInstalled`,
	`resourceLangId == javascript && testcaferunner.readyForUX && testcaferunner.canRerun`,
	`resourceLangId == javascript && testcaferunner.readyForUX && testcaferunner.chrome-canaryInstalled`,
	`resourceLangId == javascript && testcaferunner.readyForUX && testcaferunner.chromeInstalled`,
	`resourceLangId == javascript && testcaferunner.readyForUX && testcaferunner.chromiumInstalled`,
	`resourceLangId == javascript && testcaferunner.readyForUX && testcaferunner.edgeInstalled`,
	`resourceLangId == javascript && testcaferunner.readyForUX && testcaferunner.firefoxInstalled`,
	`resourceLangId == javascript && testcaferunner.readyForUX && testcaferunner.ieInstalled`,
	`resourceLangId == javascript && testcaferunner.readyForUX && testcaferunner.operaInstalled`,
	`resourceLangId == javascript && testcaferunner.readyForUX && testcaferunner.portableChromeInstalled`,
	`resourceLangId == javascript && testcaferunner.readyForUX && testcaferunner.portableFirefoxInstalled`,
	`resourceLangId == javascript && testcaferunner.readyForUX && testcaferunner.safariInstalled`,
	`resourceLangId == javascript || resourceLangId == css`,
	`resourceLangId == javascript || resourceLangId == javascriptreact || resourceLangId == typescript || (resourceLangId == typescriptreact && ponicode:supportTsx) || resourceLangId == java`,
	`resourceLangId == javascript || resourceLangId == javascriptreact || resourceLangId == typescript || (resourceLangId == typescriptreact && ponicode:supportTsx)`,
	`resourceLangId == javascript || resourceLangId == javascriptreact || resourceLangId == typescript || resourceLangId == typescriptreact`,
	`resourceLangId == javascript || resourceLangId == json || resourceLangId == jsonc`,
	`resourceLangId == javascript || resourceLangId == python || resourceLangId == php || resourceLangId == lua`,
	`resourceLangId == javascript || resourceLangId == typescript || resourceLangId == javascriptreact || resourceLangId == typescriptreact`,
	`resourceLangId == javascript || resourceLangId == typescript`,
	`resourceLangId == javascript || resourceLangId == vue`,
	`resourceLangId == javascript`,
	`resourceLangId == javascriptreact && config.jsx-sfc.icon.splitEditors`,
	`resourceLangId == javascriptreact && editorHasSelection`,
	`resourceLangId == javascriptreact || resourceLangId == typescriptreact`,
	`resourceLangId == javascriptreact`,
	`resourceLangId == jinja-sql`,
	`resourceLangId == json && editorTextFocus`,
	`resourceLangId == json && mcmodel-viewer.activeTextEditorIsModel`,
	`resourceLangId == json && resource =~ /recipes/`,
	`resourceLangId == json && resourceExtname == .dashy`,
	`resourceLangId == json || resourceLangId == jsonc || resourceLangId == arm-template`,
	`resourceLangId == json || resourceLangId == xml`,
	`resourceLangId == json || resourceLangId == yaml && asyncapi.isAsyncAPI`,
	`resourceLangId == json || resourceLangId == yaml || resourceLangId == yml || resourceLangId == template || resourceLangId == tf`,
	`resourceLangId == json5`,
	`resourceLangId == json`,
	`resourceLangId == jsonl`,
	`resourceLangId == julia`,
	`resourceLangId == jupyter`,
	`resourceLangId == jvs`,
	`resourceLangId == jxa`,
	`resourceLangId == karate`,
	`resourceLangId == kel && resourceExtname == .kel`,
	`resourceLangId == kos`,
	`resourceLangId == kv`,
	`resourceLangId == latino`,
	`resourceLangId == less`,
	`resourceLangId == lmps`,
	`resourceLangId == lua && !isInDiffEditor && !virtualWorkspace && shellExecutionSupported`,
	`resourceLangId == lua && !llse:termianl`,
	`resourceLangId == lua && isFileSystemResource`,
	`resourceLangId == lua && llse:termianl`,
	`resourceLangId == lua || resourceLangId== javascript`,
	`resourceLangId == lua`,
	`resourceLangId == macro`,
	`resourceLangId == magicCode`,
	`resourceLangId == makefile`,
	`resourceLangId == maple`,
	`resourceLangId == markdown && !listMultipleSelection || resourceLangId == yaml && !listMultipleSelection`,
	`resourceLangId == markdown && !notebookType`,
	`resourceLangId == markdown && activeEditor == workbench.editors.files.textFileEditor`,
	`resourceLangId == markdown && config.notebook.showTyporaNavigation`,
	`resourceLangId == markdown && config.openInTypora.showContextMenuInExplorer`,
	`resourceLangId == markdown && dendron:pluginActive`,
	`resourceLangId == markdown && editorFocus && editorHasSelection`,
	`resourceLangId == markdown && editorTextFocus && editorHasSelection || resourceLangId == latex && editorTextFocus && editorHasSelection`,
	`resourceLangId == markdown && resourceDirname =~ /\\.tempNote$/ && editorTextFocus`,
	`resourceLangId == markdown && resourceFilename =~ /^(?!.*(\\.rtl\\.md)).*$/`,
	`resourceLangId == markdown && resourceFilename =~ /^(?:.*(\\.rtl\\.md)).*$/`,
	`resourceLangId == markdown && resourceFilename =~ /^edit-.*\\.md/ && editorTextFocus`,
	`resourceLangId == markdown || resourceLangId == html`,
	`resourceLangId == markdown || resourceLangId == yaml`,
	`resourceLangId == markdown`,
	`resourceLangId == matlab`,
	`resourceLangId == mdal`,
	`resourceLangId == mermaid`,
	`resourceLangId == mir2 && editorFocus && resourceFilename == QFunction-0.txt`,
	`resourceLangId == mir2 && editorFocus`,
	`resourceLangId == mir2 && editorTextFocus && !editorReadonly`,
	`resourceLangId == mir2 && editorTextFocus`,
	`resourceLangId == mond`,
	`resourceLangId == mp3`,
	`resourceLangId == mscgen`,
	`resourceLangId == msgenny`,
	`resourceLangId == mvbasic`,
	`resourceLangId == mvon`,
	`resourceLangId == mysql || resourceLangId == sql`,
	`resourceLangId == mysql`,
	`resourceLangId == naniscript`,
	`resourceLangId == napamacro`,
	`resourceLangId == nasm`,
	`resourceLangId == netlinx-library`,
	`resourceLangId == netlinx-source`,
	`resourceLangId == nlp && resourceExtname == .txt`,
	`resourceLangId == nlp || resourceLangId == dict`,
	`resourceLangId == nlp`,
	`resourceLangId == nsis`,
	`resourceLangId == nsl-assembler`,
	`resourceLangId == nupkg`,
	`resourceLangId == objectscript-class`,
	`resourceLangId == ocaml`,
	`resourceLangId == octave && config.octave.showRunIconInEditorTitleMenu`,
	`resourceLangId == octave`,
	`resourceLangId == odsl || resourceLangId == html`,
	`resourceLangId == odsl`,
	`resourceLangId == ojs || resourceLangId == omd || notebookType == ojs-notebook`,
	`resourceLangId == ojs || resourceLangId == omd`,
	`resourceLangId == ooxml`,
	`resourceLangId == openhab`,
	`resourceLangId == openxml`,
	`resourceLangId == overpy`,
	`resourceLangId == owscript`,
	`resourceLangId == oz && !editorHasSelection`,
	`resourceLangId == oz && editorHasSelection`,
	`resourceLangId == oz && editorTextFocus`,
	`resourceLangId == p4`,
	`resourceLangId == papyrus`,
	`resourceLangId == pascalabcnet`,
	`resourceLangId == pddl && resourceScheme == tpddl`,
	`resourceLangId == pddl`,
	`resourceLangId == pde`,
	`resourceLangId == pdf`,
	`resourceLangId == pharo`,
	`resourceLangId == php && !inDiffEditor`,
	`resourceLangId == php && config.phpAddProperty.contextMenuOptions.enable && config.phpAddProperty.contextMenuOptions.addProperty`,
	`resourceLangId == php && config.phpAddProperty.contextMenuOptions.enable && config.phpAddProperty.contextMenuOptions.appendProperty`,
	`resourceLangId == php && config.phpAddProperty.contextMenuOptions.enable && config.phpAddProperty.contextMenuOptions.breakConstructorIntoMultiline`,
	`resourceLangId == php && config.phpAddProperty.contextMenuOptions.enable && config.phpAddProperty.contextMenuOptions.changePropertyType`,
	`resourceLangId == php && config.phpAddProperty.contextMenuOptions.enable && config.phpAddProperty.contextMenuOptions.removeProperty`,
	`resourceLangId == php && config.phpAddProperty.contextMenuOptions.enable && config.phpAddProperty.contextMenuOptions.renameProperty`,
	`resourceLangId == php && phpstan:enabled`,
	`resourceLangId == php || resourceLangId == html`,
	`resourceLangId == php || resourceScheme == phpcompanion`,
	`resourceLangId == php`,
	`resourceLangId == plaintext`,
	`resourceLangId == plan`,
	`resourceLangId == plsql && config.plsql-language.oracleConnection.enable && editorTextFocus`,
	`resourceLangId == plsql && config.plsql-language.oracleConnection.enable`,
	`resourceLangId == postcss`,
	`resourceLangId == postgres`,
	`resourceLangId == postscript`,
	`resourceLangId == povray && resourceExtname != .inc`,
	`resourceLangId == powershell`,
	`resourceLangId == prisma`,
	`resourceLangId == profileBatch && !explorerResourceIsFolder`,
	`resourceLangId == profileBatch`,
	`resourceLangId == profileColumn && !explorerResourceIsFolder`,
	`resourceLangId == profileColumn`,
	`resourceLangId == profileData && !explorerResourceIsFolder`,
	`resourceLangId == profileData`,
	`resourceLangId == profileSerialData && !explorerResourceIsFolder`,
	`resourceLangId == profileSerialData`,
	`resourceLangId == profileTable && !explorerResourceIsFolder`,
	`resourceLangId == profileTable`,
	`resourceLangId == profileTrigger && !explorerResourceIsFolder`,
	`resourceLangId == profileTrigger`,
	`resourceLangId == prolog`,
	`resourceLangId == psl && !explorerResourceIsFolder`,
	`resourceLangId == psl && psl.hasDocumentationServer`,
	`resourceLangId == psl`,
	`resourceLangId == pvs`,
	`resourceLangId == pynsist`,
	`resourceLangId == python && !busyTests`,
	`resourceLangId == python && !inOutput`,
	`resourceLangId == python && !isInDiffEditor && !notebookEditorFocused && isWorkspaceTrusted`,
	`resourceLangId == python && !isInDiffEditor && !virtualWorkspace && shellExecutionSupported`,
	`resourceLangId == python && !notebookEditorFocused && isWorkspaceTrusted`,
	`resourceLangId == python && !python.datascience.ownsSelection`,
	`resourceLangId == python && !virtualWorkspace && shellExecutionSupported`,
	`resourceLangId == python && config.processing.py.isEnabled`,
	`resourceLangId == python && editorTextFocus`,
	`resourceLangId == python && lambdadebug.islambdadir`,
	`resourceLangId == python && ponicode:hasLastActiveWebview`,
	`resourceLangId == python && resourceFilename == setup.py`,
	`resourceLangId == python || explorerResourceIsFolder`,
	`resourceLangId == python || resourceLangId == javascript || resourceLangId == typescript`,
	`resourceLangId == python`,
	`resourceLangId == q`,
	`resourceLangId == qc && resourceScheme == 'file'`,
	`resourceLangId == ql && resourceExtname == .ql`,
	`resourceLangId == quickbook`,
	`resourceLangId == r`,
	`resourceLangId == racket && editorHasSelection`,
	`resourceLangId == racket && editorTextFocus`,
	`resourceLangId == react`,
	`resourceLangId == red`,
	`resourceLangId == reds`,
	`resourceLangId == restructuredtext`,
	`resourceLangId == review`,
	`resourceLangId == rhino`,
	`resourceLangId == rmd`,
	`resourceLangId == robot`,
	`resourceLangId == robotframework && resourceExtname == .robot && !isInDiffEditor`,
	`resourceLangId == rosbag`,
	`resourceLangId == rst`,
	`resourceLangId == ruby && !isInDiffEditor && resourceFilename =~ /.*_(test|spec).rb/`,
	`resourceLangId == ruby && !isInDiffEditor && resourceFilename =~ /^(?!.*(test|spec))/ && ruby-runner.railsPrimaryVersion > 0`,
	`resourceLangId == ruby && !isInDiffEditor && resourceFilename =~ /^(?!.*(test|spec))/`,
	`resourceLangId == ruby`,
	`resourceLangId == rust-ui-test-output`,
	`resourceLangId == rust`,
	`resourceLangId == sc2layout && config.sc2layout.treeview.visible == true`,
	`resourceLangId == scad && config.openscad.interface.showExportInContextMenus && config.openscad.interface.showCommandsInEditorTitleContextMenu`,
	`resourceLangId == scad && config.openscad.interface.showExportInContextMenus && config.openscad.interface.showCommandsInExplorerContextMenu`,
	`resourceLangId == scad && config.openscad.interface.showPreviewInContextMenus && config.openscad.interface.showCommandsInEditorTitleContextMenu`,
	`resourceLangId == scad && config.openscad.interface.showPreviewInContextMenus && config.openscad.interface.showCommandsInExplorerContextMenu`,
	`resourceLangId == scad`,
	`resourceLangId == scala`,
	`resourceLangId == scl`,
	`resourceLangId == scrypt`,
	`resourceLangId == scss && editorHasSelection`,
	`resourceLangId == scss`,
	`resourceLangId == sdfg && isWorkspaceTrusted`,
	`resourceLangId == sdfg`,
	`resourceLangId == simpl+`,
	`resourceLangId == solidity && resourceFilename =~ /\\.sol/`,
	`resourceLangId == solidity`,
	`resourceLangId == soql && resourceScheme == file`,
	`resourceLangId == sourcepawn && config.sourcepawn.showCompileIconInEditorTitleMenu`,
	`resourceLangId == sourcepawn`,
	`resourceLangId == spe`,
	`resourceLangId == spirv`,
	`resourceLangId == spl && config.ibm-streams.environment.enableOSStreamsBuilds == true`,
	`resourceLangId == spl && config.ibm-streams.environment.enableV43Builds == true`,
	`resourceLangId == spl`,
	`resourceLangId == splus-source`,
	`resourceLangId == sql && editorFocus`,
	`resourceLangId == sql`,
	`resourceLangId == squirrel && editorTextFocus`,
	`resourceLangId == squirrel`,
	`resourceLangId == stata`,
	`resourceLangId == stf`,
	`resourceLangId == strest`,
	`resourceLangId == stylus`,
	`resourceLangId == svelte && editorHasSelection`,
	`resourceLangId == svelte && resourceScheme == file`,
	`resourceLangId == svelte`,
	`resourceLangId == svg && !config.svg.preview.disable`,
	`resourceLangId == svg && ForceCodeActive && ForceCodeLoggedIn`,
	`resourceLangId == svg ||resourceLangId == xml`,
	`resourceLangId == svg`,
	`resourceLangId == swift`,
	`resourceLangId == t4`,
	`resourceLangId == tcl`,
	`resourceLangId == textile`,
	`resourceLangId == tinypng_file`,
	`resourceLangId == tlaplus || editorLangId == tlaplus_cfg`,
	`resourceLangId == tlaplus || resourceLangId == tlaplus_cfg`,
	`resourceLangId == ton-solidity && isLinux && solidity-support.isDebot == false`,
	`resourceLangId == ton-solidity && isLinux && solidity-support.isDebot`,
	`resourceLangId == ton-solidity && isLinux`,
	`resourceLangId == ton-solidity`,
	`resourceLangId == totvs_patch`,
	`resourceLangId == totvs_template`,
	`resourceLangId == tree`,
	`resourceLangId == true`,
	`resourceLangId == tsv && !config.csv-edit.hideOpenCsvEditorUiActions`,
	`resourceLangId == tsv`,
	`resourceLangId == turtle`,
	`resourceLangId == typescript && !inDebugMode`,
	`resourceLangId == typescript && !virtualWorkspace`,
	`resourceLangId == typescript && editorHasSelection`,
	`resourceLangId == typescript && editorTextFocus && !inDebugMode`,
	`resourceLangId == typescript && editorTextFocus`,
	`resourceLangId == typescript && isAngularComponent`,
	`resourceLangId == typescript && ponicode:hasLastActiveWebview`,
	`resourceLangId == typescript && testcaferunner.readyForUX && testcaferunner.canRerun`,
	`resourceLangId == typescript && testcaferunner.readyForUX && testcaferunner.chrome-canaryInstalled`,
	`resourceLangId == typescript && testcaferunner.readyForUX && testcaferunner.chromeInstalled`,
	`resourceLangId == typescript && testcaferunner.readyForUX && testcaferunner.chromiumInstalled`,
	`resourceLangId == typescript && testcaferunner.readyForUX && testcaferunner.edgeInstalled`,
	`resourceLangId == typescript && testcaferunner.readyForUX && testcaferunner.firefoxInstalled`,
	`resourceLangId == typescript && testcaferunner.readyForUX && testcaferunner.ieInstalled`,
	`resourceLangId == typescript && testcaferunner.readyForUX && testcaferunner.operaInstalled`,
	`resourceLangId == typescript && testcaferunner.readyForUX && testcaferunner.portableChromeInstalled`,
	`resourceLangId == typescript && testcaferunner.readyForUX && testcaferunner.portableFirefoxInstalled`,
	`resourceLangId == typescript && testcaferunner.readyForUX && testcaferunner.safariInstalled`,
	`resourceLangId == typescript || resourceLangId == javascript || resourceLangId == typescriptreact | resourceLangId == javascriptreact`,
	`resourceLangId == typescript || resourceLangId == typescriptreact`,
	`resourceLangId == typescript`,
	`resourceLangId == typescriptreact && !inDebugMode`,
	`resourceLangId == typescriptreact && config.jsx-sfc.icon.splitEditors`,
	`resourceLangId == typescriptreact && editorHasSelection`,
	`resourceLangId == typescriptreact && editorTextFocus && !inDebugMode`,
	`resourceLangId == typescriptreact`,
	`resourceLangId == uiflow`,
	`resourceLangId == usql`,
	`resourceLangId == vb`,
	`resourceLangId == vcd`,
	`resourceLangId == vdmsl && vdm-submenus-show == true || resourceLangId == vdmpp && vdm-submenus-show == true || resourceLangId == vdmrt && vdm-submenus-show == true`,
	`resourceLangId == velocity`,
	`resourceLangId == verilog && config.verilog.showRunIconInEditorTitleMenu`,
	`resourceLangId == verilog || resourceLangId == systemverilog  || resourceLangId == vhdl`,
	`resourceLangId == verilog || resourceLangId == systemverilog || resourceLangId == vhdl`,
	`resourceLangId == verilog`,
	`resourceLangId == vhdl || resourceLangId == verilog || resourceLangId == systemverilog`,
	`resourceLangId == vhdl`,
	`resourceLangId == viper`,
	`resourceLangId == visualforce && ForceCodeActive && ForceCodeLoggedIn`,
	`resourceLangId == vm && config.nand2tetris.showRunIconInEditorTitleMenu`,
	`resourceLangId == vscode_tinypng`,
	`resourceLangId == vsix`,
	`resourceLangId == vt100`,
	`resourceLangId == vue && config.volar.icon.splitEditors`,
	`resourceLangId == vue && editorHasSelection`,
	`resourceLangId == vue ||  resourceLangId == html `,
	`resourceLangId == vue || resourceLangId == markdown`,
	`resourceLangId == vue-html`,
	`resourceLangId == vue`,
	`resourceLangId == vulkan`,
	`resourceLangId == wasm`,
	`resourceLangId == wat`,
	`resourceLangId == winkyscript-bin`,
	`resourceLangId == winkyscript`,
	`resourceLangId == witcherscript`,
	`resourceLangId == wxml`,
	`resourceLangId == xlsx`,
	`resourceLangId == xml && !explorerResourceIsFolder`,
	`resourceLangId == xml && explorerResourceIsFolder && !explorerResourceIsRoot`,
	`resourceLangId == xml && resourceExtname == .svg`,
	`resourceLangId == xml && resourceFilename =~ /(.+\\.)?deploy.xml/`,
	`resourceLangId == xml || editorLangId == xml`,
	`resourceLangId == xml`,
	`resourceLangId == xu`,
	`resourceLangId == yaml && config.vs-kubernetes.resource-commands-on-files`,
	`resourceLangId == yaml`,
	`resourceLangId == yml`,
	`resourceLangId == yuml`,
	`resourceLangId == z`,
	`resourceLangId == zip && !explorerResourceIsRoot`,
	`resourceLangId == zip && explorerResourceIsRoot`,
	`resourceLangId == zip`,
	`resourceLangId ==ael`,
	`resourceLangId =~ /(actionscript|mxml)/`,
	`resourceLangId =~ /(javascript|typescript)/  && config.cypressHelper.menuItems.FindCustomCommandReferences`,
	`resourceLangId =~ /(javascript|typescript)/ && config.cypressHelper.menuItems.FindStepDefinitionReferences`,
	`resourceLangId =~ /(python)|(cntk)/ && resourceScheme == file`,
	`resourceLangId =~ /(spl|makefile)/ && defaultInstanceEnv == 'v4_streamingAnalytics'`,
	`resourceLangId =~ /(spl|makefile)/ && defaultInstanceEnv == 'v5_cpd' && consoleEnabled == true`,
	`resourceLangId =~ /(spl|makefile)/ && defaultInstanceEnv == 'v5_cpd'`,
	`resourceLangId =~ /(spl|makefile)/ && defaultInstanceEnv == 'v5_standalone'`,
	`resourceLangId =~ /(typescript|javascript)/ && config.cypressHelper.menuItems.GoToCustomCommand`,
	`resourceLangId =~ /Fortran/ && !isInDiffEditor && !virtualWorkspace && shellExecutionSupported`,
	`resourceLangId =~ /^(c|cpp)$/`,
	`resourceLangId =~ /^(html|vue|jsx)$/`,
	`resourceLangId =~ /^(spec-data|csv-(row|column)|dppmca|chiplot)$/`,
	`resourceLangId =~ /^c$|^cpp$/ && !config.EIDE.Option.DisableEditorContextMenu`,
	`resourceLangId =~ /^c$|^cpp$/ && !config.EIDE.Option.DisableExplorerContextMenu`,
	`resourceLangId =~ /^c$|^cpp$/ && cl.eide.projectActived && !config.EIDE.Option.DisableExplorerContextMenu`,
	`resourceLangId =~ /^c|cpp$/ && config.C_mantic.contextMenu.switchHeaderSource`,
	`resourceLangId =~ /^c|cpp$/ && config.CppHelper.ContextCopyImplementation`,
	`resourceLangId =~ /^c|cpp$/ && config.CppHelper.ContextCreateHeaderGuard`,
	`resourceLangId =~ /^c|cpp$/ && config.CppHelper.ContextCreateImplementationHere`,
	`resourceLangId =~ /^c|cpp$/ && config.CppHelper.ContextCreateImplementation`,
	`resourceLangId =~ /^objectscript/ && vscode-objectscript.connectActive`,
	`resourceLangId =~ /^sql$|^jinja-sql$/`,
	`resourceLangId =~ /^winkyscript/`,
	`resourceLangId =~ /bpmn|bpmn2/`,
	`resourceLangId =~ /dmn/`,
	`resourceLangId =~ /python|cpp/ || resourceExtname == .blink || resourceScheme == untitled`,
	`resourceLangId =~ /vue|html|react|typescript|javascript/`,
	`resourceLangId in bigqueryRunner.languageIds || resourceExtname in bigqueryRunner.extensions && bigqueryRunner.icon`,
	`resourceLangId in code-debuger:languages`,
	`resourceLangId in julia.supportedLanguageIds`,
	`resourceLangId in testely.supportedLangIds && resourceFilename =~ /^.+(.test.(\\w+))$/g`,
	`resourceLangId in testely.supportedLangIds && resourceFilename =~ /^.+(?<!.test.(\\w+))$/g`,
	`resourceLangId in vscode-jupytext.resourceLangIds`,
	`resourceLangId=='vue' && editorTextFocus && editorTextFocus && !editorReadonly`,
	`resourceLangId==autolisp`,
	`resourceLangId==javascript || resourceLangId==javascriptreact || resourceLangId==typescript || resourceLangId==typescriptreact || resourceLangId==vue`,
	`resourceLangId==json`,
	`resourceLangId==mongo`,
	`resourceLangId==postgres`,
	`resourcePath =~ /.*/node_modules/.*/`,
	`resourcePath =~ /.*Packs/|\\.+/`,
	`resourcePath =~ /.circleci/config.yml/`,
	`resourcePath =~ /.github/workflows/flat.yml$/ && activeEditor == WebviewEditor`,
	`resourcePath =~ /.github/workflows/flat.yml$/ && editorLangId == yaml && activeEditor == workbench.editors.files.textFileEditor`,
	`resourcePath =~ //app/design// || resourcePath =~ //view/(adminhtml|frontend|base)/(web|templates)// && resourceExtname in magentowizard.supportedCopyPathExt`,
	`resourcePath =~ //src/platform/tast-tests[^/]*/src/chromiumos/tast/(local|remote)/bundles/[^/]+/[^/]+/[^/]+.go$/`,
	`resourcePath =~ /\\.json/`,
	`resourcePath =~ /^.+(awsResource.json)$/`,
	`resourcePath =~ /apps/`,
	`resourcePath =~ /idn://.*/connector-rules/.*/`,
	`resourcePath =~ /model/`,
	`resourcePath =~ /software/`,
	`resourcePath =~ /transforms/`,
	`resourcePath in bp.rootFolders`,
	`resourcePath in vscode-friflo-post.openForPost`,
	`resourceScheme != cds-preview && resourceFilename =~ /(\\.cds$|\\.yaml$)/i`,
	`resourceScheme != cds-preview && resourceFilename =~ /(csn.json|\\.cds$)/i`,
	`resourceScheme != cds-preview && resourceFilename =~ /(csn.json|\\.cds$|\\.yaml$)/i`,
	`resourceScheme != codeql-zip-archive`,
	`resourceScheme != gistfs && editorHasSelection`,
	`resourceScheme != gistfs`,
	`resourceScheme != gitlive-remote-diff && isInDiffEditor`,
	`resourceScheme != jq && !config.jq.strictMode || resourceScheme != jq && resourceLangId == json`,
	`resourceScheme != oracle`,
	`resourceScheme != pr && resourceScheme != review && resourceScheme != filechange && resourcePath in github:unviewedFiles`,
	`resourceScheme != pr && resourceScheme != review && resourceScheme != filechange && resourcePath in github:viewedFiles`,
	`resourceScheme != untitled && editorLangId == quarto || resourceScheme != untitled && editorLangId == markdown`,
	`resourceScheme != vscode-ssi-viewer`,
	`resourceScheme == 'gl-job-log'`,
	`resourceScheme == 'gl-review' && resourceFilename != '' && isInDiffEditor`,
	`resourceScheme == 'ioe'`,
	`resourceScheme == 'qix' && resourceFilename == 'main.qvs' && !isInDiffEditor`,
	`resourceScheme == 'uiflow.preview'`,
	`resourceScheme == 'vscode-interactive' && !editorHoverVisible && !suggestWidgetVisible && !isComposing && !inSnippetMode && !exceptionWidgetVisible && !selectionAnchorSet && !LinkedEditingInputVisible && !renameInputVisible && !editorHasSelection && !accessibilityHelpWidgetVisible && !breakpointWidgetVisible && !findWidgetVisible && !markersNavigationVisible && !parameterHintsVisible && !editorHasMultipleSelections && !notificationToastsVisible`,
	`resourceScheme == 'vscode-interactive' && notebookKernel =~ /^ms-toolsai.jupyter\\// || resourceScheme == 'vscode-interactive' && !notebookKernel`,
	`resourceScheme == 'vscode-interactive'`,
	`resourceScheme == BetterSearch && editorTextFocus`,
	`resourceScheme == adt && editorLangId =~ /abap|cds/ && abapfs:showActivate `,
	`resourceScheme == adt && editorLangId =~ /abap|cds/ && abapfs:showActivate`,
	`resourceScheme == adt && resourceFilename =~ /.clas\\.abap$/`,
	`resourceScheme == adt`,
	`resourceScheme == atlascode.bbpr && config.atlascode.bitbucket.enabled`,
	`resourceScheme == atlascode.bbpr`,
	`resourceScheme == autojsvfs`,
	`resourceScheme == awsCloudWatchLogs`,
	`resourceScheme == awsResource && !isCloud9 && config.aws.experiments.jsonResourceModification`,
	`resourceScheme == cds-preview`,
	`resourceScheme == clips && clips-ide.terminalOpen`,
	`resourceScheme == codeblockfs`,
	`resourceScheme == codeql-zip-archive && !explorerResourceIsFolder && !listMultiSelection`,
	`resourceScheme == codeql-zip-archive && config.codeQL.canary`,
	`resourceScheme == codeql-zip-archive || explorerResourceIsFolder || resourceExtname == .zip`,
	`resourceScheme == codeql-zip-archive`,
	`resourceScheme == dax-table`,
	`resourceScheme == docker`,
	`resourceScheme == dubsettings`,
	`resourceScheme == ecmarkup-preview`,
	`resourceScheme == file && !explorerResourceIsFolder && isWorkspaceTrusted && openCloudConsoleCount && openCloudConsoleCount != 0`,
	`resourceScheme == file && !explorerResourceIsFolder`,
	`resourceScheme == file && config.awscloudshell.enableUpload == true`,
	`resourceScheme == file && console-ninja.activeEditorHasLogsOrErrors`,
	`resourceScheme == file && editorLangId == dart`,
	`resourceScheme == file && editorLangId =~ /^objectscript/ && vscode-objectscript.connectActive`,
	`resourceScheme == file && editorLangId in ext.supportedLanguages`,
	`resourceScheme == file && explorerResourceIsFolder`,
	`resourceScheme == file && isWorkspaceTrusted && openCloudConsoleCount && openCloudConsoleCount != 0`,
	`resourceScheme == file && resourceExtname == .cpp &&  resource =~ '/.*Codeforces.*_.*_.*_.*cpp/'`,
	`resourceScheme == file && resourceExtname == .csproj`,
	`resourceScheme == file && resourceExtname == .hpvs`,
	`resourceScheme == file && resourceExtname == .pro`,
	`resourceScheme == file && resourceExtname == .pvs && editorLangId == pvs`,
	`resourceScheme == file && resourceExtname == .pvs && pvs-server-active == true || explorerResourceIsFolder && pvs-server-active == true && editorLangId == pvs`,
	`resourceScheme == file && resourceExtname == .pvs && pvs-server-active == true || resourceScheme == file && resourceExtname == .tccs && pvs-server-active == true || resourceScheme == file && resourceExtname == .summary && pvs-server-active == true || resourceExtname == .pvs && editorLangId == pvs || resourceExtname == .hpvs && editorLangId == pvs`,
	`resourceScheme == file && resourceExtname == .pvs && pvs-server-active == true || resourceScheme == file && resourceExtname == .tccs && pvs-server-active == true || resourceScheme == file && resourceExtname == .summary && pvs-server-active == true`,
	`resourceScheme == file && resourceExtname == .py`,
	`resourceScheme == file && resourceExtname == .qrc`,
	`resourceScheme == file && resourceExtname == .ts`,
	`resourceScheme == file && resourceExtname == .ui`,
	`resourceScheme == file && resourceExtname == .vsix`,
	`resourceScheme == file && resourceLangId == yaml && resourceFilename =~ /trigger/ig`,
	`resourceScheme == file && resourcePath in vscodeExtJs.watchedProjectFiles`,
	`resourceScheme == file && test-my-code:WorkspaceActive && test-my-code:LoggedIn`,
	`resourceScheme == file && test-my-code:WorkspaceActive`,
	`resourceScheme == file`,
	`resourceScheme == ftp`,
	`resourceScheme == ftps`,
	`resourceScheme == functions`,
	`resourceScheme == gist && resourceLangId == html`,
	`resourceScheme == gist && resourceLangId == jade`,
	`resourceScheme == gist && resourceLangId == markdown`,
	`resourceScheme == gist`,
	`resourceScheme == gistfs`,
	`resourceScheme == gitlens && isFileSystemResource`,
	`resourceScheme == gitlens`,
	`resourceScheme == gitlive-remote-diff`,
	`resourceScheme == googledrive`,
	`resourceScheme == html`,
	`resourceScheme == ilspy`,
	`resourceScheme == isfs && vscode-objectscript.connectActive`,
	`resourceScheme == jsonl`,
	`resourceScheme == kafka`,
	`resourceScheme == linelog`,
	`resourceScheme == m5stackfs`,
	`resourceScheme == member || resourceScheme == streamfile`,
	`resourceScheme == mongodb-dly`,
	`resourceScheme == navXmlForHumans`,
	`resourceScheme == newIssue && config.files.autoSave != off`,
	`resourceScheme == ngin-res`,
	`resourceScheme == ngin-scn`,
	`resourceScheme == ngin-vfs && !explorerResourceIsFolder`,
	`resourceScheme == ngin-vfs || resourceScheme == ngin-scn`,
	`resourceScheme == ngin-vfs || resourceScheme == ngin-ufs`,
	`resourceScheme == ngin-vfs`,
	`resourceScheme == ngx`,
	`resourceScheme == objectscript && vscode-objectscript.connectActive`,
	`resourceScheme == openapi-42crunch`,
	`resourceScheme == perforce`,
	`resourceScheme == phpcompanion`,
	`resourceScheme == playground`,
	`resourceScheme == policy`,
	`resourceScheme == ref`,
	`resourceScheme == repo && resourceLangId == markdown`,
	`resourceScheme == repo`,
	`resourceScheme == rubic`,
	`resourceScheme == rustexpand`,
	`resourceScheme == s3-readonly`,
	`resourceScheme == sftp && explorerResourceIsFolder && !isWindows`,
	`resourceScheme == sftp && explorerResourceIsFolder && isWindows`,
	`resourceScheme == sftp`,
	`resourceScheme == sitecore-item`,
	`resourceScheme == sourcegraph && editorReadonly`,
	`resourceScheme == untitled`,
	`resourceScheme == visual`,
	`resourceScheme == vscode-vfs`,
	`resourceScheme == webhdfs && !resourceIsFile`,
	`resourceScheme == webhdfs`,
	`resourceScheme == webview-panel && activeWebviewPanelId == 'spec-data.preview'`,
	`resourceScheme == webview-panel && browse-lite-active && !browse-lite-debug-active`,
	`resourceScheme == webview-panel && browse-lite-active`,
	`resourceScheme == webview-panel && ngin:showWebviewExecutionActions == true`,
	`resourceScheme == wenyan`,
	`resourceScheme == wskActionCode`,
	`resourceScheme =~ /(file|untitled)/`,
	`resourceScheme =~ /^isfs(-readonly)?$/ && vscode-objectscript.connectActive`,
	`resourceScheme =~ /^review$/ && !isInDiffEditor`,
	`resourceScheme =~ /^review$/ && isInDiffEditor`,
	`resourceScheme =~ /^review$/`,
	`resourceScheme =~ /^untitled$|^file$/ && config.owtc.displayPosition == 'Bottom'`,
	`resourceScheme =~ /^untitled$|^file$/ && config.owtc.displayPosition == 'Top'`,
	`resourceScheme =~ /^untitled$|^file$/ && config.owtc.explorerContextMenu && config.owtc.displayPosition == 'Bottom'`,
	`resourceScheme =~ /^untitled$|^file$/ && config.owtc.explorerContextMenu && config.owtc.displayPosition == 'Top'`,
	`resourceScheme =~ /webview/ && r.browser.active`,
	`resourceScheme =~ /webview/ && r.helpPanel.active`,
	`resourceScheme =~ /webview/ && r.plot.active`,
	`resourceScheme =~ /webview/ && r.rmarkdown.preview.active && !r.rmarkdown.preview.autoRefresh`,
	`resourceScheme =~ /webview/ && r.rmarkdown.preview.active && r.rmarkdown.preview.autoRefresh`,
	`resourceScheme =~ /webview/ && r.rmarkdown.preview.active`,
	`resourceScheme==coq-view`,
	`resourceSet`,
	`resource`,
	`restoreEditors:key:right`,
	`resttext.alt.enter.enabled && editorTextFocus && !suggestWidgetVisible && editorLangId==restructuredtext`,
	`resttext.createGrid.enabled && editorTextFocus && !editorHasMultipleSelections && editorHasSelection && editorLangId==restructuredtext`,
	`resttext.editor.opening`,
	`resttext.enter.enabled && editorTextFocus && !suggestWidgetVisible && editorLangId==restructuredtext`,
	`resttext.shift.enter.enabled && editorTextFocus && !suggestWidgetVisible && editorLangId==restructuredtext`,
	`resttext.shift.tab.enabled && editorTextFocus && !inSnippetMode && !suggestWidgetVisible && editorLangId==restructuredtext`,
	`resttext.shift.tab.enabled && editorTextFocus && !suggestWidgetVisible && editorLangId==restructuredtext`,
	`resttext.tab.enabled && editorTextFocus && !inSnippetMode && !suggestWidgetVisible && editorLangId==restructuredtext && !inlineSuggestionVisible`,
	`resttext.tab.enabled && editorTextFocus && !inSnippetMode && !suggestWidgetVisible && editorLangId==restructuredtext`,
	`resttext.tab.enabled && editorTextFocus && !suggestWidgetVisible && editorLangId==restructuredtext`,
	`roblox-ts:compilerActive`,
	`roblox-ts:inSrcDir`,
	`rstPreviewFocus`,
	`rtags.callHierarchy.visible`,
	`rtags.inheritanceHierarchy.visible`,
	`rtags.reindexActiveFolder.visible`,
	`runConfigurationInitialized`,
	`runningOnCloudShell && !runningInTheia`,
	`sap-database-explorer.isVsCode`,
	`sap-database-explorer.schemaFilterAvailable && !sap-database-explorer.schemaFilterActive`,
	`sap-database-explorer.schemaFilterAvailable && sap-database-explorer.schemaFilterActive`,
	`sap-database-explorer:vsc_db_switch`,
	`sap.ux.applicationModeler.dataEditor.showDataEditorCommands`,
	`sap.ux.applicationModeler.flpEmbeddedMode.showFlpEmbeddedMode`,
	`sap.ux.applicationModeler.isSystemTreeEmpty`,
	`savConfig.showCommand`,
	`scmInputIsInFirstPosition && scmRepository && !suggestWidgetVisible`,
	`scmInputIsInLastPosition && scmRepository && !suggestWidgetVisible`,
	`scmProvider != tfvc`,
	`scmProvider == accurev && scmResourceGroup == kept`,
	`scmProvider == accurev && scmResourceGroup == modified`,
	`scmProvider == accurev`,
	`scmProvider == cc && scmResourceGroup == cc_checkedout && vscode-clearcase:enabled && resourcePath in vscode-clearcase:CheckedoutObjects`,
	`scmProvider == cc && scmResourceGroup == cc_checkedout && vscode-clearcase:enabled`,
	`scmProvider == cc && scmResourceGroup == cc_untracked && vscode-clearcase:enabled`,
	`scmProvider == cc && vscode-clearcase:enabled && !vscode-clearcase:DynView`,
	`scmProvider == cc && vscode-clearcase:enabled && resourcePath in vscode-clearcase:CheckedoutObjects`,
	`scmProvider == cc && vscode-clearcase:enabled`,
	`scmProvider == codeblock`,
	`scmProvider == cvs && scmResourceGroup == changeTree && scmResourceState == added`,
	`scmProvider == cvs && scmResourceGroup == changeTree && scmResourceState == deleted`,
	`scmProvider == cvs && scmResourceGroup == changeTree && scmResourceState == modified`,
	`scmProvider == cvs && scmResourceGroup == changeTree && scmResourceState == removed`,
	`scmProvider == cvs && scmResourceGroup == changeTree`,
	`scmProvider == cvs && scmResourceGroup == conflictTree && scmResourceState == conflict`,
	`scmProvider == cvs && scmResourceGroup == repositoryTree && scmResourceState == checkout`,
	`scmProvider == cvs && scmResourceGroup == repositoryTree && scmResourceState == directory`,
	`scmProvider == cvs && scmResourceGroup == repositoryTree && scmResourceState == merge`,
	`scmProvider == cvs && scmResourceGroup == repositoryTree && scmResourceState == patch`,
	`scmProvider == cvs && scmResourceGroup == repositoryTree && scmResourceState == removedFromRepo`,
	`scmProvider == cvs && scmResourceGroup == repositoryTree`,
	`scmProvider == cvs && scmResourceGroup == stagingTree && scmResourceState == added`,
	`scmProvider == cvs && scmResourceGroup == stagingTree && scmResourceState == modified`,
	`scmProvider == cvs && scmResourceGroup == stagingTree && scmResourceState == removed`,
	`scmProvider == cvs && scmResourceGroup == stagingTree`,
	`scmProvider == cvs && scmResourceGroup == untrackedTree && scmResourceState == untracked_file`,
	`scmProvider == cvs && scmResourceGroup == untrackedTree && scmResourceState == untracked_folder`,
	`scmProvider == cvs && scmResourceGroup == untrackedTree`,
	`scmProvider == cvs && scmResourceState != checkout && scmResourceState != deleted && scmResourceState != removed && scmResourceState != untracked_folder && scmResourceState != directory`,
	`scmProvider == cvs`,
	`scmProvider == deltanji && deltanjiPermitDefaultChangeRequest && scmResourceGroup != \\`,
	`scmProvider == deltanji && deltanjiPermitDefaultChangeRequest`,
	`scmProvider == deltanji && scmResourceGroup != \\`,
	`scmProvider == deltanji && scmResourceState == 'Checked out'`,
	`scmProvider == deltanji`,
	`scmProvider == dvc && dvc.commands.available`,
	`scmProvider == dvc && scmResourceGroup == committed`,
	`scmProvider == dvc && scmResourceGroup == notInCache && dvc.commands.available && !dvc.scm.command.running`,
	`scmProvider == dvc && scmResourceGroup == uncommitted && dvc.commands.available && !dvc.scm.command.running`,
	`scmProvider == dvc && scmResourceGroup == uncommitted && scmResourceState != uncommittedUnknown && scmResourceState != notInCache && dvc.commands.available && !dvc.scm.command.running`,
	`scmProvider == dvc && scmResourceGroup == untracked && dvc.commands.available && !dvc.scm.command.running`,
	`scmProvider == dvc`,
	`scmProvider == e4e.scm && scmResourceGroup == e4e.scm.group.changes`,
	`scmProvider == e4e.scm && scmResourceGroup == e4e.scm.group.mergeChanges`,
	`scmProvider == e4e.scm && scmResourceState != ELEMENT/REMOVED`,
	`scmProvider == e4e.scm`,
	`scmProvider == fossil`,
	`scmProvider == git && azdo:inReviewMode`,
	`scmProvider == git && config.git-buttons.showGitCheckout`,
	`scmProvider == git && config.git-buttons.showGitCleanAll`,
	`scmProvider == git && config.git-buttons.showGitCommitAllAmend`,
	`scmProvider == git && config.git-buttons.showGitCommitAllSigned`,
	`scmProvider == git && config.git-buttons.showGitCommitAll`,
	`scmProvider == git && config.git-buttons.showGitCommitStagedAmend`,
	`scmProvider == git && config.git-buttons.showGitCommitStagedSigned`,
	`scmProvider == git && config.git-buttons.showGitCommitStaged`,
	`scmProvider == git && config.git-buttons.showGitPublish`,
	`scmProvider == git && config.git-buttons.showGitPullFrom`,
	`scmProvider == git && config.git-buttons.showGitPullRebase`,
	`scmProvider == git && config.git-buttons.showGitPull`,
	`scmProvider == git && config.git-buttons.showGitPushTo`,
	`scmProvider == git && config.git-buttons.showGitPush`,
	`scmProvider == git && config.git-buttons.showGitShowOutput`,
	`scmProvider == git && config.git-buttons.showGitStageAll`,
	`scmProvider == git && config.git-buttons.showGitStashApplyLatest`,
	`scmProvider == git && config.git-buttons.showGitStashApply`,
	`scmProvider == git && config.git-buttons.showGitStashDrop`,
	`scmProvider == git && config.git-buttons.showGitStashIncludeUntracked`,
	`scmProvider == git && config.git-buttons.showGitStashPopLatest`,
	`scmProvider == git && config.git-buttons.showGitStashPop`,
	`scmProvider == git && config.git-buttons.showGitStash`,
	`scmProvider == git && config.git-buttons.showGitSyncRebase`,
	`scmProvider == git && config.git-buttons.showGitSync`,
	`scmProvider == git && config.git-buttons.showGitUndoCommit`,
	`scmProvider == git && config.git-buttons.showGitUnstageAll`,
	`scmProvider == git && config.git-graph.sourceCodeProviderIntegrationLocation == 'Inline'`,
	`scmProvider == git && config.git-graph.sourceCodeProviderIntegrationLocation == 'More Actions'`,
	`scmProvider == git && config.scm-buttons.showGitCheckout`,
	`scmProvider == git && config.scm-buttons.showGitCleanAll`,
	`scmProvider == git && config.scm-buttons.showGitCommitAllAmend`,
	`scmProvider == git && config.scm-buttons.showGitCommitAllSigned`,
	`scmProvider == git && config.scm-buttons.showGitCommitAll`,
	`scmProvider == git && config.scm-buttons.showGitCommitStagedAmend`,
	`scmProvider == git && config.scm-buttons.showGitCommitStagedSigned`,
	`scmProvider == git && config.scm-buttons.showGitCommitStaged`,
	`scmProvider == git && config.scm-buttons.showGitPublish`,
	`scmProvider == git && config.scm-buttons.showGitPullFrom`,
	`scmProvider == git && config.scm-buttons.showGitPullRebase`,
	`scmProvider == git && config.scm-buttons.showGitPull`,
	`scmProvider == git && config.scm-buttons.showGitPushTo`,
	`scmProvider == git && config.scm-buttons.showGitPush`,
	`scmProvider == git && config.scm-buttons.showGitShowOutput`,
	`scmProvider == git && config.scm-buttons.showGitStageAll`,
	`scmProvider == git && config.scm-buttons.showGitStashApplyLatest`,
	`scmProvider == git && config.scm-buttons.showGitStashApply`,
	`scmProvider == git && config.scm-buttons.showGitStashDrop`,
	`scmProvider == git && config.scm-buttons.showGitStashIncludeUntacked`,
	`scmProvider == git && config.scm-buttons.showGitStashPopLatest`,
	`scmProvider == git && config.scm-buttons.showGitStashPop`,
	`scmProvider == git && config.scm-buttons.showGitStash`,
	`scmProvider == git && config.scm-buttons.showGitUndoCommit`,
	`scmProvider == git && config.scm-buttons.showGitUnstageAll`,
	`scmProvider == git && scmResourceGroup != merge`,
	`scmProvider == git && scmResourceGroup == index`,
	`scmProvider == git && scmResourceGroup == merge`,
	`scmProvider == git && scmResourceGroup == workingTree`,
	`scmProvider == git && scmResourceGroup =~ /^workingTree|index$/`,
	`scmProvider == git `,
	`scmProvider == git`,
	`scmProvider == ngin`,
	`scmProvider == perforce && config.perforce.scmFileChanges == false`,
	`scmProvider == perforce && config.perforce.scmFileChanges == true`,
	`scmProvider == perforce && perforce.compatibilityMode != sourcedepot`,
	`scmProvider == perforce && scmResourceGroup != default && config.perforce.hideSubmitIcon == false`,
	`scmProvider == perforce && scmResourceGroup != default && config.perforce.swarmHost`,
	`scmProvider == perforce && scmResourceGroup != default`,
	`scmProvider == perforce && scmResourceGroup == default && config.perforce.hideSubmitIcon == false`,
	`scmProvider == perforce && scmResourceGroup == default`,
	`scmProvider == perforce && scmResourceGroup =~ /reres/`,
	`scmProvider == perforce && scmResourceGroup =~ /unres/`,
	`scmProvider == perforce && scmResourceGroup in perforce.changes.reresolvable`,
	`scmProvider == perforce && scmResourceGroup in perforce.changes.resolvable`,
	`scmProvider == perforce && scmResourceState =~ /opened/ && scmResourceGroup != default`,
	`scmProvider == perforce && scmResourceState =~ /opened/ || scmResourceState =~ /shelved/`,
	`scmProvider == perforce && scmResourceState =~ /opened/`,
	`scmProvider == perforce && scmResourceState =~ /reres/`,
	`scmProvider == perforce && scmResourceState =~ /shelved/`,
	`scmProvider == perforce && scmResourceState =~ /unres/`,
	`scmProvider == perforce`,
	`scmProvider == planningDomainsSession`,
	`scmProvider == plastic-scm && scmResourceGroup == status`,
	`scmProvider == plastic-scm`,
	`scmProvider == powerappsPortal`,
	`scmProvider == rcs && scmResourceGroup == loc`,
	`scmProvider == rcs`,
	`scmProvider == svn && scmResourceGroup == change_file`,
	`scmProvider == svn && scmResourceGroup == commit_file`,
	`scmProvider == svn`,
	`scmProvider == tfvc && scmResourceGroup != conflicts`,
	`scmProvider == tfvc && scmResourceGroup == conflicts`,
	`scmProvider == tfvc && scmResourceGroup == excluded`,
	`scmProvider == tfvc && scmResourceGroup == included`,
	`scmProvider == tfvc`,
	`scmProvider == vsls-scc && scmResourceGroup =~ /clean_/ && !liveshare:isReadOnlySession`,
	`scmProvider == vsls-scc && scmResourceGroup =~ /openChanges_/`,
	`scmProvider == vsls-scc && scmResourceGroup =~ /openFile_/`,
	`scmProvider == west && nrf-connect.west.invalid`,
	`scmProvider == west`,
	`scmProvider =~ /^ABAP/`,
	`scmProvider =~ /^git|^remoteHub:github/ && scmProviderRootUri in github:reposInReviewMode`,
	`scmProvider =~ /^git|^remoteHub:github/ && scmProviderRootUri in github:reposNotInReviewMode`,
	`scmProvider =~ /^remoteHub:/ && !gitHub:hasPullRequestExtension || scmProvider == remoteHub:azurerepos`,
	`scmProvider =~ /^remoteHub:/ && config.remoteHub.staging.enabled`,
	`scmProvider =~ /^remoteHub:/ && scmResourceGroup == changes && config.remoteHub.staging.enabled`,
	`scmProvider =~ /^remoteHub:/ && scmResourceGroup == conflicts && config.remoteHub.staging.enabled`,
	`scmProvider =~ /^remoteHub:/ && scmResourceGroup == staged && config.remoteHub.staging.enabled`,
	`scmProvider =~ /^remoteHub:/ && scmResourceGroup =~ /changes|conflicts|staged/`,
	`scmProvider =~ /^remoteHub:/ && scmResourceGroup =~ /changes|staged/`,
	`scmProvider =~ /^remoteHub:/`,
	`scmProvider =~ /^remoteHub:github/`,
	`scmProvider =~ /abapGit_/ && scmResourceGroup != staged`,
	`scmProvider =~ /abapGit_/ && scmResourceGroup != unstaged`,
	`scmProvider =~ /abapGit_/ && scmResourceGroup == staged`,
	`scmProvider =~ /abapGit_/`,
	`scmProvider =~ /git/ && gerrit:isUsingGerrit`,
	`scmRepository`,
	`scmResourceState == ngin-change-state`,
	`sdfg.showMenu.goto.cpp == true`,
	`sdfg.showMenu.goto.py == true`,
	`sdfg.showMenu.goto.sdfg == true`,
	`sdp.isScolvoProject`,
	`searchInputBoxFocus && searchViewletVisible`,
	`searchViewletFocus && !fileMatchOrFolderMatchFocus`,
	`searchViewletFocus && searchViewletVisible`,
	`searchViewletFocus`,
	`searchViewletVisible && hasSearchResult`,
	`searchViewletVisible`,
	`seito-openfile:enabled`,
	`selectedDebugger == 'compile'`,
	`selectedDebugger == 'cuda-add-edit'`,
	`selectedDebugger == 'cuda-config'`,
	`selectedDebugger == 'cuda-login'`,
	`selectedDebugger == 'default'`,
	`settingRowFocus`,
	`sfdcQcp:projectActive && hasActiveQcpFile`,
	`sfdcQcp:projectActive`,
	`sfdh:project_opened`,
	`sfdx:lwc_jest_file_focused && !sfdx:lwc_jest_is_watching_focused_file`,
	`sfdx:lwc_jest_file_focused && sfdx:lwc_jest_is_watching_focused_file`,
	`sfdx:lwc_jest_file_focused`,
	`sfdx:project_opened && !editorHasSelection`,
	`sfdx:project_opened && !inDebugMode`,
	`sfdx:project_opened && !sfdx:isv_debug_project && sfdx:default_username_has_change_tracking`,
	`sfdx:project_opened && editorHasSelection`,
	`sfdx:project_opened && editorLangId == 'apex'`,
	`sfdx:project_opened && resource =~ /.*/(lwc|aura)/.*(/[^/]+\\.(html|css|js|xml|svg|cmp|app|design|auradoc))?$/`,
	`sfdx:project_opened && resource =~ /.*/functions/.*/[^/]+(/[^/]+\\.(ts|js|java|json|toml))?$/ && resourceFilename != package.json && resourceFilename != package-lock.json && resourceFilename != tsconfig.json`,
	`sfdx:project_opened && resource =~ /.*/functions/.*/[^/]+(/[^/]+\\.(ts|js|java|json|toml))?$/`,
	`sfdx:project_opened && resource =~ /.*/lwc/[^/]+(/[^/]+\\.(html|css|js))?$/`,
	`sfdx:project_opened && resource =~ /.\\.(cls|apex|log)?$/`,
	`sfdx:project_opened && sfdx:default_username_has_no_change_tracking && resourceLangId != 'forcesourcemanifest' && editorIsOpen`,
	`sfdx:project_opened && sfdx:default_username_has_no_change_tracking && resourceLangId != 'forcesourcemanifest'`,
	`sfdx:project_opened && sfdx:default_username_has_no_change_tracking && resourceLangId == 'forcesourcemanifest'`,
	`sfdx:project_opened && sfdx:default_username_has_no_change_tracking`,
	`sfdx:project_opened && sfdx:has_cached_test_class`,
	`sfdx:project_opened && sfdx:has_cached_test_method`,
	`sfdx:project_opened && sfdx:has_default_username`,
	`sfdx:project_opened && sfdx:replay_debugger_extension`,
	`sfdx:project_opened || sfdx:internal_dev`,
	`sfdx:project_opened`,
	`sftkEnabled`,
	`sftp.enabled && !explorerResourceIsRoot && !explorerResourceIsFolder`,
	`sftp.enabled && !explorerResourceIsRoot`,
	`sftp.enabled && explorerResourceIsFolder`,
	`sftp.enabled && resourceScheme == file`,
	`sftp.enabled && resourceScheme == remote`,
	`sftp.enabled && scmProvider == git && !gitFreshRepository`,
	`sftp.enabled && scmProvider == git && scmResourceGroup == index && !gitFreshRepository`,
	`sftp.enabled && scmProvider == git && scmResourceGroup == workingTree && !gitFreshRepository`,
	`sftp.enabled && view == remoteExplorer && viewItem != file && !listMultiSelection`,
	`sftp.enabled && view == remoteExplorer && viewItem != file && viewItem != root`,
	`sftp.enabled && view == remoteExplorer && viewItem != file`,
	`sftp.enabled && viewItem != file`,
	`sftp.enabled`,
	`shellExecutionSupported && customExecutionSupported`,
	`shellExecutionSupported`,
	`sheller.showMenuMakeScriptExecutable`,
	`shiny.python.active && shellExecutionSupported`,
	`shouldShowHoi4Preview && resourceScheme != webview-panel`,
	`show-al-codeactions-references`,
	`showAffected`,
	`showAzureTemplateView`,
	`showBPMNtypes == true`,
	`showGoOutlinerBenchmarksView`,
	`showGoOutlinerTestsView`,
	`showJsonTreeView`,
	`showLegend`,
	`showOutline`,
	`showRailwayViewContainer`,
	`showReferenceView == true`,
	`showSidebar`,
	`showTaskDetailsView`,
	`showTestExplorerEditorContextMenu`,
	`showTestExplorerExpandButton`,
	`showTodaysTasks`,
	`showUnsavedFilesViewOnexplorer`,
	`showWelcomeScreen`,
	`sideBarFocus && activeViewlet == 'workbench.view.explorer' && explorerResourceIsFolder`,
	`sideBarFocus && activeViewlet == 'workbench.view.explorer' && isServerRunning`,
	`sideBarFocus && activeViewlet == 'workbench.view.explorer' && resourceExtname == .json || resourceExtname == .jsonc || resourceExtname == .js || resourceExtname == .har`,
	`sideBarFocus && activeViewlet == 'workbench.view.explorer' && resourceExtname == .json || resourceExtname == .jsonc || resourceExtname == .js`,
	`sideBarFocus && activeViewlet == 'workbench.view.explorer' && resourceExtname =~ /(\\.asc|\\.gpg)$/`,
	`sideBarFocus && activeViewlet == 'workbench.view.explorer'`,
	`sideBarFocus && activeViewlet == 'workbench.view.extension.test'`,
	`sideBarFocus && activeViewlet == 'workbench.view.scm' && !editorTextFocus && !findInputFocussed && !inQuickOpen`,
	`sideBarFocus && activeViewlet == 'workbench.view.scm' && config.git.enabled && !git.missing && gitOpenRepositoryCount != 0`,
	`sideBarFocus && activeViewlet == workbench.view.extension.objectExplorer`,
	`sideBarFocus && focusedView == vaultSecrets`,
	`sideBarFocus`,
	`sideBarVisible && explorerViewletVisible`,
	`sideBarVisible && focusedView == vsc-workspace-sidebar-webview-workspace`,
	`sideBySideEditorActive`,
	`sigasi.isExtensionEnabled && !editorReadonly`,
	`sigasi.isExtensionEnabled && editorLangId == systemverilog`,
	`sigasi.isExtensionEnabled && resourceLangId == systemverilog && !editorReadonly`,
	`sigasi.isExtensionEnabled && resourceLangId == systemverilog`,
	`sigasi.isExtensionEnabled && sigasi.preferences.view.paletteOpen.ready`,
	`sigasi.isExtensionEnabled`,
	`slideExplorerEnabled`,
	`slidev-enabled && resourceLangId == markdown`,
	`slidev-enabled`,
	`slitherCompatibleWorkspace`,
	`snippets.workspaceState == fileAvailable`,
	`snippetsmanagerCodeSnippetsEditorFocus`,
	`snowflake.isActiveAccountLoggedIn`,
	`snyk:advanced && !snyk:error && snyk:loggedIn && snyk:featuresSelected && snyk:codeEnabled && snyk:workspaceFound`,
	`snyk:error == 'blocking'`,
	`snyk:loggedIn && !snyk:featuresSelected && !snyk:error`,
	`snyk:loggedIn && snyk:featuresSelected && !snyk:codeEnabled && snyk:workspaceFound && !snyk:error`,
	`snyk:loggedIn && snyk:featuresSelected && snyk:codeEnabled && !snyk:codeLocalEngineEnabled && snyk:workspaceFound && !snyk:error`,
	`snyk:loggedIn && snyk:featuresSelected && snyk:codeEnabled && snyk:codeLocalEngineEnabled && snyk:workspaceFound && !snyk:error`,
	`snyk:loggedIn && snyk:featuresSelected && snyk:workspaceFound && !snyk:error`,
	`snyk:loggedIn`,
	`snyk:lsCodePreview && snyk:loggedIn && snyk:featuresSelected && snyk:codeEnabled && !snyk:codeLocalEngineEnabled && snyk:workspaceFound && !snyk:error`,
	`snyk:mode != 'manual' && snyk:mode != 'paused' && snyk:mode != 'throttled'`,
	`snyk:mode == 'manual'`,
	`snyk:mode == 'paused'`,
	`snyk:mode == 'throttled'`,
	`soicode.workspaceStatus == 'OutdatedWorkspaceFile_0.8.0'`,
	`soicode.workspaceStatus == CorruptedWorkspaceFile`,
	`soicode.workspaceStatus == NoWorkspaceOpen`,
	`soicode.workspaceStatus == Ready && editorLangId == cpp || soicode.workspaceStatus == Ready && editorLangId == stoml`,
	`soicode.workspaceStatus == Ready && view == soicode-tasks`,
	`soicode.workspaceStatus == UnknownVersionWorkspaceFile`,
	`sol.isDebugging`,
	`sol.serverIsReady && !config.sol.LanguageServer.EnableHistoryLogTree`,
	`solutionExplorer.viewInActivityBar && solutionExplorer.loadedFlag`,
	`solutionExplorer.viewInExplorer && solutionExplorer.loadedFlag`,
	`spark-ar-studio.cliAvailable`,
	`spark-ar-studio.extensionConnected || spark-ar-studio.cliAvailable`,
	`spark-ar-studio.extensionConnected`,
	`spfxProjectCheck && editorHasSelection`,
	`spfxProjectCheck`,
	`spresenseSdkVersionMajor == '1'`,
	`spresenseSdkVersionMajor`,
	`sqlite.explorer.show`,
	`sqltools.Results.active`,
	`sqltools.file.connectionAttached && editorLangId == sql`,
	`sshfs.openConnections > 0`,
	`sso.sub.show`,
	`stackScopes.stackGraph && !stackScopes.drawOnlyColorized`,
	`stackScopes.stackGraph && stackScopes.drawOnlyColorized`,
	`stackScopes.stackGraph && stackScopes.multisession && !stackScopes.colorizeMatches`,
	`stackScopes.stackGraph && stackScopes.multisession && stackScopes.colorizeMatches`,
	`stackScopes.stackGraph`,
	`stateManagementEnabled`,
	`states-diagram-focused`,
	`staticServerFolderPath`,
	`statsBar.cpuLoad`,
	`statsBar.loadavg`,
	`statsBar.memoUsage`,
	`statsBar.networkSpeed`,
	`statsBar.uptime`,
	`statusBarFocused`,
	`steedosdx:project_opened && resourceFilename == routes`,
	`steedosdx:project_opened && resourceFilename =~ /(\\.object.yml|\\.button.yml|buttons)$/`,
	`steedosdx:project_opened && resourceFilename =~ /(\\.object.yml|functions)$/`,
	`steedosdx:project_opened && resourceFilename =~ /(\\.object.yml|triggers)$/`,
	`steedosdx:project_opened`,
	`stepsize:currentView != 'new-issue-form'`,
	`stepsize:currentView == 'issue-single'`,
	`stepsize:currentView == 'new-issue-form'`,
	`stitch:contextAvailable || resourceFilename =~ /.integration.json$/`,
	`stitch:contextAvailable`,
	`stm32ForVSCodeReady`,
	`storyExplorer.initialLoadComplete && !storyExplorer.loadingStories && !storyExplorer.storybookConfigDetected`,
	`storyExplorer.initialLoadComplete && !storyExplorer.loadingStories && storyExplorer.storybookConfigDetected && !storyExplorer.storybookConfigParseFailed`,
	`storyExplorer.initialLoadComplete && !storyExplorer.loadingStories && storyExplorer.storybookConfigDetected && storyExplorer.storybookConfigParseFailed`,
	`storyExplorer.storybookPreviewFocused`,
	`storyExplorer.storybookWebviewsOpen`,
	`stripe.isNotCLIInstalled == false`,
	`stripe.isNotCLIInstalled == true`,
	`suggestWidgetHasFocusedSuggestion && suggestWidgetVisible && textInputFocus`,
	`suggestWidgetMultipleSuggestions && suggestWidgetVisible && textInputFocus`,
	`suggestWidgetVisible && textInputFocus && editorLangId =~ /javascript|typescript|javascriptreact|typescriptreact/ && config.editor.acceptSuggestionOnEnter != 'off'`,
	`suggestWidgetVisible && textInputFocus && editorLangId =~ /javascript|typescript|javascriptreact|typescriptreact/)`,
	`suggestWidgetVisible && textInputFocus && textInputFocus`,
	`suggestWidgetVisible && textInputFocus`,
	`suggestWidgetVisible && vim.active && vim.use<C-p> && !inDebugRepl || vim.mode == 'CommandlineInProgress' && vim.active && vim.use<C-p> && !inDebugRepl || vim.mode == 'SearchInProgressMode' && vim.active && vim.use<C-p> && !inDebugRepl`,
	`suggestWidgetVisible && vim.active && vim.use<C-p>`,
	`suggestWidgetVisible`,
	`supernova.mode.development && supernova.loggedIn`,
	`supernova.mode.development || supernova.mode.use`,
	`supernova.mode.use && !supernova.workspace.hasSupernovaConfig`,
	`supernova.mode.use && supernova.loggedIn`,
	`supernova.mode.use && supernova.workspace.hasSupernovaConfig`,
	`supportedRepo != RantaSource`,
	`supportedRepo == RantaSource && activeEditor != WebviewEditor && resourceFilename == app.config.json`,
	`supportedRepo == RantaSource && activeEditor != WebviewEditor && resourceFilename == biz.json`,
	`supportedRepo == RantaSource && activeEditor != WebviewEditor && resourceFilename == extension.json`,
	`supportedRepo == RantaSource && activeEditor != WebviewEditor && resourceFilename =~ /page.json$/`,
	`supportedRepo == RantaSource && explorerViewletVisible`,
	`supportedRepo == RantaSource`,
	`svd-viewer.svd.hasData`,
	`svgDevActive && !svgDevHostInput`,
	`svgDevActive`,
	`svgDevAddInteractive`,
	`svgPreviewFocus`,
	`svgPreviewIsFocused && !editorLangId`,
	`svgPreviewIsOpen == true`,
	`svgeditorWebviewFocus`,
	`swift-sfdc-active`,
	`swift.fileIsSnippet`,
	`swift.hasPackage`,
	`swift.packageHasPlugins`,
	`swimm.loginState == 'loggedIn'`,
	`swimm.loginState`,
	`t3lt.extensionActive`,
	`tabbingThroughSelections`,
	`tableMode && inTable && !suggestWidgetVisible`,
	`tableMode && inTable`,
	`tableMode && potentiallyInTable && !suggestWidgetVisible`,
	`tableMode && potentiallyInTable`,
	`tableMode`,
	`tabnine-assistant:capability`,
	`tabnine-assistant:enabled && tabnine-assistant:background`,
	`tabnine.notifications-ready`,
	`tabnine.save-snippets:enabled && editorHasSelection`,
	`tabnine.snippet-suggestion:enabled && tabnine.in-inline-suggestions || tabnine.inline-suggestion:enabled && tabnine.in-inline-suggestions`,
	`tabnine.snippet-suggestion:enabled`,
	`tabnine.tab-override && inlineSuggestionVisible && !editorTabMovesFocus`,
	`tabnine.tab-override && suggestWidgetHasFocusedSuggestion && suggestWidgetVisible && textInputFocus`,
	`tabnine.tabnine-navigation-ready`,
	`tabnine.tabnine-today-ready`,
	`tabular.data.view.visible && tabular.data.uri =~ /^(file:///)/`,
	`tabular.data.view.visible && tabular.data.uri =~ /^(https://raw.githubusercontent.com/)/`,
	`taskCommandsRegistered && !terminalFocus`,
	`taskCommandsRegistered`,
	`taskSelected`,
	`taskfile:showTasksExplorer`,
	`tdt4102-tools.buildtype && tdt4102-tools.buildtype != debug`,
	`tdt4102-tools.buildtype && tdt4102-tools.buildtype != debugoptimized`,
	`tdt4102-tools.buildtype && tdt4102-tools.buildtype != minsize`,
	`tdt4102-tools.buildtype && tdt4102-tools.buildtype != release`,
	`tdt4102-tools.buildtype`,
	`tekton.cluster`,
	`tekton.pipeline`,
	`tekton:pipelinePreview`,
	`tekton:zenMode`,
	`terminal.active`,
	`terminalEditorFocus && terminalFocus && terminalHasBeenCreated && resourceScheme == 'vscode-terminal' || terminalEditorFocus && terminalFocus && terminalProcessSupported && resourceScheme == 'vscode-terminal'`,
	`terminalFindFocused && terminalHasBeenCreated || terminalFindFocused && terminalProcessSupported || terminalFocus && terminalHasBeenCreated || terminalFocus && terminalProcessSupported`,
	`terminalFindFocused && terminalProcessSupported || terminalFocus && terminalProcessSupported`,
	`terminalFindFocused && terminalProcessSupported`,
	`terminalFindInputFocused && terminalHasBeenCreated || terminalFindInputFocused && terminalProcessSupported`,
	`terminalFindVisible && terminalFocus && terminalHasBeenCreated || terminalFindVisible && terminalFocus && terminalProcessSupported`,
	`terminalFindVisible && terminalFocus && terminalProcessSupported`,
	`terminalFocus && !accessibilityModeEnabled && terminalShellType == 'pwsh'`,
	`terminalFocus && julia.isActiveREPL && julia.isEvaluating`,
	`terminalFocus && panelPosition != 'bottom'`,
	`terminalFocus && panelPosition == 'bottom'`,
	`terminalFocus && psl.gtmDebug`,
	`terminalFocus && terminalHasBeenCreated && !accessibilityModeEnabled || terminalFocus && terminalProcessSupported && !accessibilityModeEnabled`,
	`terminalFocus && terminalHasBeenCreated && !terminalAltBufferActive || terminalFocus && terminalProcessSupported && !terminalAltBufferActive`,
	`terminalFocus && terminalHasBeenCreated && !terminalEditorFocus || terminalFocus && terminalProcessSupported && !terminalEditorFocus`,
	`terminalFocus && terminalHasBeenCreated && terminalIsOpen || terminalFocus && terminalIsOpen && terminalProcessSupported`,
	`terminalFocus && terminalHasBeenCreated && terminalTextSelected && !terminalFindVisible || terminalFocus && terminalProcessSupported && terminalTextSelected && !terminalFindVisible`,
	`terminalFocus && terminalHasBeenCreated && terminalTextSelected || terminalFocus && terminalProcessSupported && terminalTextSelected`,
	`terminalFocus && terminalHasBeenCreated || terminalFocus && terminalProcessSupported || terminalHasBeenCreated && terminalTabsFocus || terminalProcessSupported && terminalTabsFocus`,
	`terminalFocus && terminalHasBeenCreated || terminalFocus && terminalProcessSupported`,
	`terminalFocus && terminalHasBeenCreated`,
	`terminalFocus && terminalIsOpen && activePanel == 'workbench.panel.terminal'`,
	`terminalFocus && terminalProcessSupported && !terminalAltBufferActive`,
	`terminalFocus && terminalProcessSupported && resourceScheme == 'vscode-terminal'`,
	`terminalFocus && terminalProcessSupported && terminalProcessSupported && terminalTextSelected`,
	`terminalFocus && terminalProcessSupported && terminalTextSelected && !terminalFindVisible`,
	`terminalFocus && terminalProcessSupported && terminalTextSelected && terminalTextSelected`,
	`terminalFocus && terminalProcessSupported && terminalTextSelected`,
	`terminalFocus && terminalProcessSupported || terminalFocus && terminalWebExtensionContributedProfile`,
	`terminalFocus && terminalProcessSupported || terminalProcessSupported && terminalTabsFocus`,
	`terminalFocus && terminalProcessSupported`,
	`terminalFocus && terminalShellIntegrationEnabled && !accessibilityModeEnabled && terminalShellType == 'pwsh'`,
	`terminalFocus && terminalShellType == 'cmd'`,
	`terminalFocus && terminalTextSelected`,
	`terminalFocus`,
	`terminalHasBeenCreated && terminalTabsFocus && terminalTabsSingularSelection || terminalProcessSupported && terminalTabsFocus && terminalTabsSingularSelection`,
	`terminalHasBeenCreated && terminalTabsFocus || terminalIsOpen && terminalTabsFocus || terminalProcessSupported && terminalTabsFocus`,
	`terminalIsOpen && terminalTabsFocus || terminalProcessSupported && terminalTabsFocus`,
	`terminalIsOpen`,
	`terminalProcessSupported && terminalTabsFocus && terminalTabsSingularSelection`,
	`terminalProcessSupported && terminalTabsFocus`,
	`terminalProcessSupported || terminalWebExtensionContributedProfile`,
	`terminalProcessSupported`,
	`test-my-code:LoggedIn == false`,
	`test-my-code:LoggedIn`,
	`test-my-code:WorkspaceActive`,
	`testEnvironment`,
	`testExplorerVisible`,
	`testId in acceptanceTests.context.gfv`,
	`testId in go.profiledTests`,
	`testId in go.tests && testId =~ /\\?(test|benchmark)/`,
	`testId in go.tests`,
	`testId in jest.autoRun.off`,
	`testId in jest.autoRun.on`,
	`testId in jest.coverage.off`,
	`testId in jest.coverage.on`,
	`testId in jest.editor-update-snapshot`,
	`testId in jest.editor-view-snapshot`,
	`testId in jest.workspaceRoot`,
	`testcaferunner.readyForUX && testcaferunner.canRerun`,
	`testcaferunner.readyForUX`,
	`testing.canRefresh`,
	`testing.hasAnyResults`,
	`testing.isInPeek && !config.editor.stablePeek || testing.isPeekVisible && !config.editor.stablePeek`,
	`testing.isPeekVisible`,
	`testsDiscovered`,
	`textCompareEditorActive`,
	`textCompareEditorVisible && isInDiffEditor`,
	`textCompareEditorVisible`,
	`textInputFocus && !accessibilityModeEnabled`,
	`textInputFocus && !editorReadonly && config.Redo`,
	`textInputFocus && !editorReadonly && config.ShortcutMenuBar.redo`,
	`textInputFocus && !editorReadonly && config.ShortcutMenuBar.undo`,
	`textInputFocus && !editorReadonly && config.Undo`,
	`textInputFocus && !editorReadonly`,
	`textInputFocus && editorHasSelection`,
	`textInputFocus && editorLangId == 'latex'`,
	`textInputFocus && editorLangId == markdown`,
	`textInputFocus && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$/`,
	`textInputFocus && editorLangId =~ /^latex$|^latex-expl3$|^rsweave$|^jlweave$|^pweave$/`,
	`textInputFocus && findWidgetVisible`,
	`textInputFocus && suggestWidgetMultipleSuggestions && suggestWidgetVisible`,
	`textInputFocus`,
	`textUsmPreviewFocus`,
	`textilePreviewFocus`,
	`tiffPreviewFocus`,
	`timing:customToCustom:enabled`,
	`timing:customToEpoch:enabled`,
	`timing:customToIsoLocal:enabled`,
	`timing:customToIsoUtc:enabled`,
	`timing:epochToCustom:enabled`,
	`timing:epochToCustomTimezone:enabled`,
	`timing:epochToGps:enabled`,
	`timing:epochToIsoDuration:enabled`,
	`timing:epochToIsoLocal:enabled`,
	`timing:epochToIsoTimezone:enabled`,
	`timing:epochToIsoUtc:enabled`,
	`timing:epochToReadableDuration:enabled`,
	`timing:isoDurationToEpoch:enabled`,
	`timing:isoRfcToCustom:enabled`,
	`timing:isoRfcToEpoch:enabled`,
	`timing:nowAsCustom:enabled`,
	`timing:nowAsEpoch:enabled`,
	`timing:nowAsIsoLocal:enabled`,
	`timing:nowAsIsoUtc:enabled`,
	`timing:toggleInsertConvertedTimeUserLevel:enabled`,
	`titanium:notProject`,
	`titanium:toolingMissing`,
	`tizen.isTizenWorkspace && explorerResourceIsFolder && resourcePath in tizen.buildSupportedFolders`,
	`tizen.isTizenWorkspace && explorerResourceIsFolder && resourcePath in tizen.supportedFolders`,
	`tizen.isTizenWorkspace && explorerResourceIsFolder || resourceLangId == tidl`,
	`tlaplus.tlc.canRunAgain`,
	`tlaplus.tlc.isRunning`,
	`tms:isactive == true && inDebugMode == false`,
	`todo-embedded-filtered == false`,
	`todo-embedded-filtered == true`,
	`todomd:generic1FilterExists`,
	`todomd:generic2FilterExists`,
	`todomd:generic3FilterExists`,
	`todomd:isActive`,
	`todomd:isDev`,
	`toit.extensionActive`,
	`torizon:ccpp-debug-active`,
	`torizon:ccpp-release-active`,
	`torizon:isTCBProject`,
	`torizon:requiresSDK`,
	`torizon:torizonProjectLoaded`,
	`touchbarcommandshortcuts:firstEnabled == true`,
	`touchbarcommandshortcuts:secondEnabled == true`,
	`touchbarcommandshortcuts:thirdEnabled == true`,
	`treeViewEnabled`,
	`treeview.pinned && view == sidebar-outline`,
	`treeview.provider.dynamic`,
	`trinityOutlineEnabled`,
	`true`,
	`trunk.initializing`,
	`trunk.invalidConfig`,
	`trunk.invalidInitializationParams`,
	`trunk.statusClosed`,
	`trunk.upgradeRequired && workbenchState != empty`,
	`trunk.upgradeRequired`,
	`tsAstOutlineEnabled`,
	`tunnelCloseable && tunnelViewFocus`,
	`tunnelType == Forwarded && remoteName =~ /codespaces*.*/`,
	`tunnelViewFocus && tunnelType == 'Detected' && tunnelViewMultiSelection == 'undefined' || tunnelViewFocus && tunnelType == 'Forwarded' && tunnelViewMultiSelection == 'undefined'`,
	`tunnelViewFocus && tunnelType == 'Forwarded' && tunnelViewMultiSelection == 'undefined'`,
	`tuyaCoreReady && tuyaMultiEnvProject && view == tuya-ide.projectTasks`,
	`tuyaCoreReady && view == tuya-ide.projectTasks`,
	`tuyaCoreReady`,
	`tuyaProjectReady`,
	`twinBasicCustomCommand0Active == true && editorLangId =~ /twinbasic.*/`,
	`twinBasicCustomCommand10Active == true && editorLangId =~ /twinbasic.*/`,
	`twinBasicCustomCommand11Active == true && editorLangId =~ /twinbasic.*/`,
	`twinBasicCustomCommand12Active == true && editorLangId =~ /twinbasic.*/`,
	`twinBasicCustomCommand13Active == true && editorLangId =~ /twinbasic.*/`,
	`twinBasicCustomCommand14Active == true && editorLangId =~ /twinbasic.*/`,
	`twinBasicCustomCommand15Active == true && editorLangId =~ /twinbasic.*/`,
	`twinBasicCustomCommand16Active == true && editorLangId =~ /twinbasic.*/`,
	`twinBasicCustomCommand17Active == true && editorLangId =~ /twinbasic.*/`,
	`twinBasicCustomCommand18Active == true && editorLangId =~ /twinbasic.*/`,
	`twinBasicCustomCommand19Active == true && editorLangId =~ /twinbasic.*/`,
	`twinBasicCustomCommand1Active == true && editorLangId =~ /twinbasic.*/`,
	`twinBasicCustomCommand2Active == true && editorLangId =~ /twinbasic.*/`,
	`twinBasicCustomCommand3Active == true && editorLangId =~ /twinbasic.*/`,
	`twinBasicCustomCommand4Active == true && editorLangId =~ /twinbasic.*/`,
	`twinBasicCustomCommand5Active == true && editorLangId =~ /twinbasic.*/`,
	`twinBasicCustomCommand6Active == true && editorLangId =~ /twinbasic.*/`,
	`twinBasicCustomCommand7Active == true && editorLangId =~ /twinbasic.*/`,
	`twinBasicCustomCommand8Active == true && editorLangId =~ /twinbasic.*/`,
	`twinBasicCustomCommand9Active == true && editorLangId =~ /twinbasic.*/`,
	`twinBasicIsEnabled == true && editorLangId == 'twinbasic'`,
	`twinBasicIsEnabled == true && editorLangId == 'twinbasicLegacyClass'`,
	`twinBasicIsEnabled == true && editorLangId == 'twinbasicLegacyModule'`,
	`twinBasicIsEnabled == true && explorerResourceIsFolder && resourceFilename == 'MANIFEST'`,
	`twinBasicIsEnabled == true && explorerResourceIsFolder && resourceFilename == 'Resources'`,
	`twinBasicIsEnabled == true && explorerResourceIsFolder && resourceFilename == 'STRING'`,
	`twinBasicIsEnabled == true && resourceLangId == 'twinbasicBuildConfig'`,
	`twinBasicIsEnabled == true && resourceLangId == 'twinbasicBuildConfigACTIVE'`,
	`twinBasicIsEnabled == true`,
	`twinBasicPackage == true`,
	`typeHierarchyVisible && !config.editor.stablePeek`,
	`typeHierarchyVisible && typeHierarchyDirection == 'subtypes'`,
	`typeHierarchyVisible && typeHierarchyDirection == 'supertypes'`,
	`typeHierarchyVisible`,
	`udbActive && udbCopyLinkToTimeEnabled`,
	`udbActive && udbLastChangedEnabled`,
	`udbActive`,
	`uiflowPreviewFocus == uiflow`,
	`uiflowPreviewFocus`,
	`umathShowPreview`,
	`usefindtree`,
	`useproviewtree`,
	`v4pvhdlforprofessionals:enabled-no`,
	`v4pvhdlforprofessionals:enabled`,
	`v4pvhdlforprofessionals:search:enabled`,
	`vToolActive`,
	`vToolFileActive && vtoolShowToolbar3D`,
	`vToolFileActive`,
	`vToolPreviewActive && !vToolDebugActive`,
	`vToolPreviewActive && vToolDebugActive`,
	`vala-code:jsonOutline`,
	`vala-code:symbolOutline`,
	`validApigeeWorkspace && !config.cloudcode.beta.enableOneActivityBar && config.cloudcode.beta.enableApigeeExplorers`,
	`validApigeeWorkspace && config.cloudcode.beta.enableOneActivityBar && config.cloudcode.beta.enableApigeeExplorers`,
	`validApigeeWorkspace`,
	`variablesFocused`,
	`vbatis.entityEditorOn || vbatis.mapperEditorOn || vbatis.SettingOn`,
	`vbatis.entityEditorOn || vbatis.mapperEditorOn`,
	`vbatis.entityEditorOn`,
	`vdm-vscode.OpenRTLog`,
	`vdm-vscode.OpenVDMTools`,
	`vdm-vscode.changeVdmjProperties`,
	`vdm-vscode.ct.show == true`,
	`vdm-vscode.pog.run == true`,
	`vdm-vscode.translate.coverage == true && vdm-vscode.coverageOverlay.disable`,
	`vdm-vscode.translate.coverage == true && vdm-vscode.coverageOverlay.enable`,
	`venus:showOptionsMenu`,
	`veracode:scan:available  && resourceExtname =~ /(.asp|.css|.ehtml|.es|.es6|.handlebars|.hbs|.hjs'|.htm'|.html|.js|.json|.jsp|.jsx|.map|.mustache|.php|.ts|.tsx|.xhtml|.cs)/`,
	`veracode:scan:available && explorerResourceIsFolder`,
	`veracode:scan:available && resourceExtname =~ /(.asp|.css|.ehtml|.es|.es6|.handlebars|.hbs|.hjs'|.htm'|.html|.js|.json|.jsp|.jsx|.map|.mustache|.php|.ts|.tsx|.xhtml|.cs)/`,
	`veracode:scan:available == false`,
	`vercelLogViewFocus`,
	`versionIsOpen`,
	`versionlens.enabled && versionlens.providerActive && versionlens.providerBusy == false && versionlens.providerError == false  && textCompareEditorVisible == false`,
	`versionlens.enabled && versionlens.providerActive`,
	`versionlens.enabled == false && versionlens.providerActive && versionlens.providerBusy == false && versionlens.providerError == false && textCompareEditorVisible == false`,
	`versionlens.prereleasesEnabled && versionlens.enabled && versionlens.providerActive && versionlens.providerSupportsPrereleases && textCompareEditorVisible == false`,
	`versionlens.prereleasesEnabled == false && versionlens.enabled && versionlens.providerActive && versionlens.providerSupportsPrereleases && textCompareEditorVisible == false`,
	`versionlens.providerBusy && versionlens.providerActive && versionlens.providerError == false && textCompareEditorVisible == false`,
	`versionlens.providerError && versionlens.providerActive && textCompareEditorVisible == false`,
	`versionlens.show && versionlens.isActive && versionlens.inProgress && textCompareEditorVisible == false`,
	`versionlens.show && versionlens.isActive && versionlens.inProgress == false && textCompareEditorVisible == false`,
	`versionlens.show && versionlens.isActive`,
	`versionlens.show == false && versionlens.isActive && versionlens.inProgress == false && textCompareEditorVisible == false`,
	`versionlens.showDependencyStatuses && versionlens.show && versionlens.isActive && textCompareEditorVisible == false`,
	`versionlens.showDependencyStatuses == false && versionlens.show && versionlens.isActive && textCompareEditorVisible == false`,
	`versionlens.showTaggedVersions && versionlens.show && versionlens.isActive && textCompareEditorVisible == false`,
	`versionlens.showTaggedVersions == false && versionlens.show && versionlens.isActive && textCompareEditorVisible == false`,
	`view != gitflow.views.main`,
	`view != gitlens.views.fileHistory && viewItem =~ /gitlens:file\\b/`,
	`view != task`,
	`view ==  sap.ux.applicationModeler.sapSystems && viewItem == sapSystem`,
	`view == 'apidoc-explorer' && viewItem == 'apidoc-folder'`,
	`view == 'bigquery-authentication'`,
	`view == 'bigquery-tree-data-provider'`,
	`view == 'exercism.view.tracks' && viewItem == fileTreeNode:file`,
	`view == 'exercism.view.tracks' && viewItem == trackTreeNode`,
	`view == 'exercism.view.tracks' && viewItem =~ /:directory/`,
	`view == 'exercism.view.tracks' && viewItem =~ /exerciseTreeNode(?!:completed)/`,
	`view == 'exercism.view.tracks' && viewItem =~ /exerciseTreeNode/`,
	`view == 'exercism.view.tracks' && viewItem =~ /fileTreeNode/`,
	`view == 'exercism.view.tracks'`,
	`view == 'redmine-explorer-my-issues' && !hasSingleConfig`,
	`view == 'redmine-explorer-projects' && !hasSingleConfig`,
	`view == 'redmine-explorer-projects' && !redmine:treeViewStyle`,
	`view == 'redmine-explorer-projects' && redmine:treeViewStyle`,
	`view == 'redmine-explorer-projects' || view == 'redmine-explorer-my-issues'`,
	`view == 'snyk.views.analysis.code.security.old' || view == 'snyk.views.analysis.code.security' || view == 'snyk.views.analysis.code.quality.old' || view == 'snyk.views.analysis.code.quality' || view == 'snyk.views.analysis.oss' || view == 'snyk.views.welcome' || view == 'snyk.views.actions'`,
	`view == 'snyk.views.analysis.code.security.old' || view == 'snyk.views.analysis.code.security' || view == 'snyk.views.analysis.code.quality.old' || view == 'snyk.views.analysis.code.quality' || view == 'snyk.views.analysis.oss'`,
	`view == 'sshfs-configs' && viewItem == config`,
	`view == 'sshfs-configs' || view == 'sshfs-connections'`,
	`view == 'sshfs-configs'`,
	`view == 'sshfs-connections' && viewItem == connection`,
	`view == 'sshfs-connections' && viewItem == terminal`,
	`view == 'sshfs-connections'`,
	`view == 'workbench.explorer.fileView' && scopeToThis.scoped`,
	`view == ASN1VcxTreeView && viewItem == module || viewItem == type`,
	`view == ASN1VcxTreeView && viewItem == type`,
	`view == ApplicationInsightsExplorer`,
	`view == Autolisp-FindReplaceView`,
	`view == Autolisp-ProjectView && autolisp.hasProject`,
	`view == Autolisp-ProjectView && viewItem == lspFile`,
	`view == Autolisp-ProjectView && viewItem == project`,
	`view == Autolisp-ProjectView`,
	`view == AzureMLExplorer && (!viewItem || viewItem =~ /^AzureML((Compute(Instance|Cluster)?|InferenceCluster|AttachedCompute)Root|Data(setRoot|Container)|DatastoreRoot|(Batch|Online)?Endpoint(Root)?|Environment(Curated)?Root)|Experiment(Root)?|Model(Container|Root)|RunRemote(Cancellable)?|RunArtifact(Root|Path)|Subscription|Workspace(Default)?$/)`,
	`view == AzureMLExplorer && viewItem == AzureMLBatchDeployment`,
	`view == AzureMLExplorer && viewItem == AzureMLBatchEndpointRoot`,
	`view == AzureMLExplorer && viewItem == AzureMLBatchEndpoint`,
	`view == AzureMLExplorer && viewItem == AzureMLComputeClusterRoot`,
	`view == AzureMLExplorer && viewItem == AzureMLComputeInstanceRoot`,
	`view == AzureMLExplorer && viewItem == AzureMLComputeRoot`,
	`view == AzureMLExplorer && viewItem == AzureMLDataContainer`,
	`view == AzureMLExplorer && viewItem == AzureMLDataVersion`,
	`view == AzureMLExplorer && viewItem == AzureMLDatasetRoot`,
	`view == AzureMLExplorer && viewItem == AzureMLDatastoreRoot`,
	`view == AzureMLExplorer && viewItem == AzureMLEndpointRoot`,
	`view == AzureMLExplorer && viewItem == AzureMLEnvironmentRoot`,
	`view == AzureMLExplorer && viewItem == AzureMLExperimentRoot`,
	`view == AzureMLExplorer && viewItem == AzureMLExperiment`,
	`view == AzureMLExplorer && viewItem == AzureMLModelContainer`,
	`view == AzureMLExplorer && viewItem == AzureMLModelRoot`,
	`view == AzureMLExplorer && viewItem == AzureMLModelVersion`,
	`view == AzureMLExplorer && viewItem == AzureMLOnlineDeployment`,
	`view == AzureMLExplorer && viewItem == AzureMLOnlineEndpointRoot`,
	`view == AzureMLExplorer && viewItem == AzureMLOnlineEndpoint`,
	`view == AzureMLExplorer && viewItem == AzureMLRunArtifact`,
	`view == AzureMLExplorer && viewItem == AzureMLSubscription`,
	`view == AzureMLExplorer && viewItem == AzureMLWorkspaceDefault`,
	`view == AzureMLExplorer && viewItem == AzureMLWorkspace`,
	`view == AzureMLExplorer && viewItem =~ /^AzureML(ComputeCluster|InferenceCluster(Attached)?|AttachedCompute(?!Root).*)(?!Root)$/`,
	`view == AzureMLExplorer && viewItem =~ /^AzureML(ComputeCluster|InferenceCluster)$/`,
	`view == AzureMLExplorer && viewItem =~ /^AzureML(InferenceClusterAttached|AttachedCompute(?!Root).*)$/`,
	`view == AzureMLExplorer && viewItem =~ /^AzureMLAttachedComputeRoot$/`,
	`view == AzureMLExplorer && viewItem =~ /^AzureMLComputeInstance(?!Root).*$/`,
	`view == AzureMLExplorer && viewItem =~ /^AzureMLComputeInstance(Running|Stopped|Intermediate)SSHEnabled.*$/`,
	`view == AzureMLExplorer && viewItem =~ /^AzureMLComputeInstanceRunning.*$/`,
	`view == AzureMLExplorer && viewItem =~ /^AzureMLComputeInstanceStopped.*$/`,
	`view == AzureMLExplorer && viewItem =~ /^AzureMLDatastore(AutoCreated|Editable)?$/`,
	`view == AzureMLExplorer && viewItem =~ /^AzureMLDatastore(Editable)?$/`,
	`view == AzureMLExplorer && viewItem =~ /^AzureMLEnvironment(Curated)?$/`,
	`view == AzureMLExplorer && viewItem =~ /^AzureMLRunRemote(Cancellable)?$/`,
	`view == AzureMLExplorer && viewItem =~ /^AzureMLRunRemote(Cancellable)?|AzureMLRunArtifactRoot$/`,
	`view == AzureMLExplorer && viewItem =~ /^AzureMLRunRemoteCancellable$/`,
	`view == AzureMLExplorer && viewItem =~ /^AzureMLWorkspace(Default)?$/`,
	`view == AzureMLExplorer`,
	`view == BricksExplorer && viewItem  =~ /nodebricks*/`,
	`view == BricksExplorer && viewItem != nodebricksdiy`,
	`view == BricksExplorer && viewItem == bricksdiy`,
	`view == BricksExplorer && viewItem == nodebricksdiy`,
	`view == BricksExplorer && viewItem =~ /nodebricks*/ `,
	`view == BricksExplorer && viewItem =~ /nodebricks*/`,
	`view == BricksExplorer`,
	`view == BtcMarkerBarView`,
	`view == BuildView && viewItem == Build`,
	`view == BuildView`,
	`view == CatalogBrowser && sap-database-explorer.schemaFilterAvailable && !sap-database-explorer.schemaFilterActive`,
	`view == CatalogBrowser && sap-database-explorer.schemaFilterAvailable && sap-database-explorer.schemaFilterActive`,
	`view == CatalogBrowser && sap-database-explorer:vsc_dbx_select_statement && viewItem == Procedures || view == CatalogBrowser && sap-database-explorer:vsc_dbx_select_statement && viewItem == SynonymProcedures`,
	`view == CatalogBrowser && sap-database-explorer:vsc_dbx_select_statement && viewItem == Tables || view == CatalogBrowser && sap-database-explorer:vsc_dbx_select_statement && viewItem == Views || view == CatalogBrowser && sap-database-explorer:vsc_dbx_select_statement && viewItem == ColumnViews || view == CatalogBrowser && sap-database-explorer:vsc_dbx_select_statement && viewItem == Synonyms || view == CatalogBrowser && sap-database-explorer:vsc_dbx_select_statement && viewItem == PublicSynonyms || view == CatalogBrowser && sap-database-explorer:vsc_dbx_select_statement && viewItem == Functions || view == CatalogBrowser && sap-database-explorer:vsc_dbx_select_statement && viewItem == SynonymFunctions`,
	`view == CatalogBrowser && sap-database-explorer:vsc_dbx_select_statement && viewItem == Tables || view == CatalogBrowser && sap-database-explorer:vsc_dbx_select_statement && viewItem == Views || view == CatalogBrowser && sap-database-explorer:vsc_dbx_select_statement && viewItem == ColumnViews || view == CatalogBrowser && sap-database-explorer:vsc_dbx_select_statement && viewItem == Synonyms || view == CatalogBrowser && sap-database-explorer:vsc_dbx_select_statement && viewItem == PublicSynonyms`,
	`view == CatalogBrowser && viewItem != '' && viewItem != message && sap-database-explorer:vsc_dbx_copy_object_names`,
	`view == CatalogBrowser && viewItem != '' && viewItem != message && viewItem != Adapters && viewItem != AgentGroups && viewItem != Agents && viewItem != GraphWorkspaces && viewItem != RemoteSources && viewItem != RemoteSubscriptions && sap-database-explorer:vsc_dbx_create_statement`,
	`view == CatalogBrowser`,
	`view == CloudView`,
	`view == CodeScan.AllRules && viewItem == rule-off`,
	`view == CodeScan.AllRules && viewItem == rule-on`,
	`view == CodeScan.AllRules`,
	`view == CodeScan.IssueLocations`,
	`view == Connections`,
	`view == CppReferencesView && !refView.isGroupedByFile`,
	`view == CppReferencesView && refView.isGroupedByFile`,
	`view == CppSshTargetsView`,
	`view == DatabaseList  && viewItem == 'SAP HANA Database Explorer Connections'`,
	`view == DatabaseList && sap-database-explorer.isVsCode && viewItem == sap_database || viewItem == sap_database_temp`,
	`view == DatabaseList && sap-database-explorer.isVsCode`,
	`view == DatabaseList && viewItem == sap_database  && sap-database-explorer.isVsCode`,
	`view == DatabaseList && viewItem == sap_database || viewItem == sap_database_dbexplorer || viewItem == sap_database_dbexplorer_hdi || viewItem == sap_database_temp`,
	`view == DatabaseList && viewItem == sap_database_dbexplorer || viewItem == sap_database_dbexplorer_filecontainer || viewItem == sap_database_dbexplorer_hdi`,
	`view == DatabaseList && viewItem == sap_database_dbexplorer_hdi && sap-database-explorer:vsc_dbx_hdi_admin_connection`,
	`view == DatabaseList`,
	`view == Database`,
	`view == DatabricksConnections && !virtualWorkspace`,
	`view == DatabricksConnections && viewItem == INACTIVE`,
	`view == DatabricksConnections && virtualWorkspace`,
	`view == DatabricksConnections`,
	`view == DevicefileExlorer && viewItem == FileExplorer-Directory`,
	`view == DevicefileExlorer && viewItem == FileExplorer-File`,
	`view == DevicefileExlorer`,
	`view == DockerHubTreeView && viewItem == image`,
	`view == DockerHubTreeView`,
	`view == FEExplorer && viewItem == __root__`,
	`view == FEExplorer && viewItem == dir`,
	`view == FEExplorer && viewItem == file`,
	`view == FEExplorer && viewItem == model`,
	`view == FEExplorer && viewItem == utils`,
	`view == FEExplorer && viewItem =~ /(-|^)create(-|$)/`,
	`view == FEExplorer && viewItem =~ /^application(-|$)/`,
	`view == FEExplorer && viewItem =~ /^effects(-|$)/`,
	`view == FEExplorer && viewItem =~ /^model(-|$)/`,
	`view == FEExplorer && viewItem =~ /^models(-|$)/`,
	`view == FEExplorer && viewItem =~ /^page(-|$)/`,
	`view == FEExplorer && viewItem =~ /^reducers(-|$)/`,
	`view == FEExplorer`,
	`view == FXRantaOutline || view == FXOutlineInTreeView`,
	`view == ForceCode.codeCovDataProvider`,
	`view == FpcProjectExplorer && viewItem==fpcbuild`,
	`view == FpcProjectExplorer && viewItem==fpcproject`,
	`view == Frontpage`,
	`view == Functions`,
	`view == GDLOutline && viewItem == GUID`,
	`view == GDLOutline && viewItem == MainGUID`,
	`view == GDLOutline && viewItem == gdlpict`,
	`view == GDLOutline && viewItem == script`,
	`view == GDLOutline && viewItem == xmlsection`,
	`view == GDLOutline`,
	`view == GPMExplorer && viewItem != segmentation`,
	`view == GPMExplorer && viewItem == owner`,
	`view == GPMExplorer && viewItem == repository.stared`,
	`view == GPMExplorer && viewItem == repository`,
	`view == GPMExplorer && viewItem == source`,
	`view == GPMExplorer && viewItem == star`,
	`view == GPMExplorer`,
	`view == GistTree && viewItem == File`,
	`view == GistTree && viewItem == GistStarrd`,
	`view == GistTree && viewItem == Gist`,
	`view == GistTree && viewItem =~ /^Gist/`,
	`view == GitCherry.main && isMergeConflict == false`,
	`view == GitCherry.main && isMergeConflict == true`,
	`view == GitCherry.main`,
	`view == Health`,
	`view == HotTreeView.weibo`,
	`view == HotTreeView.zhihu`,
	`view == IOTDeviceViewer`,
	`view == IOTEventViewer`,
	`view == IOTNewsViewer`,
	`view == IntelSoftwareDevelopmentTools &&  viewItem == downloadedChildItem`,
	`view == IntelSoftwareDevelopmentTools && viewItem == child || viewItem == downloadedChildItem`,
	`view == IntelSoftwareDevelopmentTools && viewItem == child`,
	`view == IntelSoftwareDevelopmentTools`,
	`view == JSONTreeView && viewItem == collapNode`,
	`view == JSONTreeView && viewItem == leafNode`,
	`view == JSONTreeView && viewItem == rootNode`,
	`view == JimTreeView.weibo`,
	`view == L01  && viewItem == Depn_M`,
	`view == L01`,
	`view == L02-2  && viewItem == Mepn_AGN || viewItem == Mepn_AG`,
	`view == L02-2 && viewItem != Mepn_AG`,
	`view == L02-2 && viewItem == Mepn_AGN || viewItem == Mepn_AG`,
	`view == L02-2`,
	`view == M02  && viewItem == SCT_M`,
	`view == M02`,
	`view == Menu1 && viewItem == MenuItem`,
	`view == Menu2`,
	`view == MicrobitExplorer && viewItem == MicrobitFile`,
	`view == MicrobitExplorer`,
	`view == NocalhostSyncManage`,
	`view == ObjectScriptExplorer && viewItem =~ /^dataNode:/`,
	`view == ObjectScriptExplorer && viewItem =~ /^dataRootNode:(?!cspRootNode)/`,
	`view == ObjectScriptExplorer && viewItem =~ /^serverNode((?!:(%SYS|system):).)*$/`,
	`view == ObjectScriptExplorer && viewItem =~ /^serverNode((?!:extra:).)*$/`,
	`view == ObjectScriptExplorer && viewItem =~ /^serverNode((?!:generated:).)*$/`,
	`view == ObjectScriptExplorer && viewItem =~ /^serverNode.*:extra:/`,
	`view == ObjectScriptExplorer && viewItem =~ /^serverNode.*:generated:/`,
	`view == ObjectScriptExplorer && viewItem =~ /^serverNode.*:system:/`,
	`view == ObjectScriptExplorer`,
	`view == ObjectScriptProjectsExplorer && viewItem == dataNode:projectNode`,
	`view == ObjectScriptProjectsExplorer && viewItem == projectsServerNsNode:extra`,
	`view == ObjectScriptProjectsExplorer && viewItem =~ /^(dataNode:projectNode|dataRootNode)/`,
	`view == ObjectScriptProjectsExplorer && viewItem =~ /^dataNode(?!:projectNode$).*/`,
	`view == ObjectScriptProjectsExplorer && viewItem =~ /^projectsServerNsNode/`,
	`view == ObjectScriptProjectsExplorer && vscode-objectscript.projectsExplorerRootCount > 0`,
	`view == ObjectScriptProjectsExplorer`,
	`view == PAIContainerHDFS && viewItem && viewItem != PAIHdfsFile`,
	`view == PAIContainerHDFS && viewItem && viewItem != PAIHdfsRoot`,
	`view == PAIContainerHDFS`,
	`view == PAIContainerJobList && viewItem && viewItem == PAIJobListCluster`,
	`view == PAIContainerJobList`,
	`view == PAIContainerStorage && viewItem && viewItem == PAIStorageClusterRoot`,
	`view == PAIContainerStorage && viewItem && viewItem == PAIStorageFile`,
	`view == PAIContainerStorage && viewItem && viewItem == PAIStorageFolder`,
	`view == PAIContainerStorage && viewItem && viewItem == PAIStorageMountPointItem`,
	`view == PAIContainerStorage && viewItem && viewItem == PAIStoragePersonalItem`,
	`view == PAIContainerStorage && viewItem && viewItem == PAIStoragePersonalRoot`,
	`view == PAIContainerStorage && viewItem && viewItem == PAIStorageTeamItem`,
	`view == PAIContainerStorage && viewItem == PAIStoragePersonalItem`,
	`view == PAIContainerStorage && viewItem == PAIStoragePersonalRoot`,
	`view == PAIContainerStorage`,
	`view == PAIExplorer && viewItem == PAIConfiguration`,
	`view == PAIExplorer`,
	`view == PackageManagerExplorer && viewItem == package`,
	`view == PackageManagerExplorer && viewItem == registry`,
	`view == PackageManagerExplorer`,
	`view == PowerBICapacities`,
	`view == PowerBIGateways`,
	`view == PowerBIPipelines`,
	`view == PowerBIWorkspaces && viewItem =~ /.*,DATAFLOW,.*/`,
	`view == PowerBIWorkspaces && viewItem =~ /.*,DATASET,.*/ && viewItem =~ /.*,CREATE_KERNEL,.*/`,
	`view == PowerBIWorkspaces && viewItem =~ /.*,DATASET,.*/ && viewItem =~ /.*,REMOVE_KERNEL,.*/`,
	`view == PowerBIWorkspaces && viewItem =~ /.*,DATASET,.*/`,
	`view == PowerBIWorkspaces && viewItem =~ /.*,GROUP,.*/`,
	`view == PowerBIWorkspaces && viewItem =~ /.*,PARAMETER,.*/`,
	`view == PowerBIWorkspaces && viewItem =~ /.*,REFRESH,.*/ && viewItem =~ /.*,CANCEL,.*/`,
	`view == PowerBIWorkspaces && viewItem =~ /.*,REPORT,.*/`,
	`view == PowerBIWorkspaces`,
	`view == PowerShellCommands`,
	`view == Projects`,
	`view == QueryView && viewItem == QueryRoot`,
	`view == QueryView && viewItem == Query`,
	`view == QueryView && viewItem == WorkItem`,
	`view == QueryView`,
	`view == QuestionExplorer && viewItem == problem-favorite`,
	`view == QuestionExplorer && viewItem == problem`,
	`view == QuestionExplorer && viewItem =~ /problem*/`,
	`view == QuestionExplorer`,
	`view == REPLVariables && config.julia.workspace.showModules == false`,
	`view == REPLVariables && config.julia.workspace.showModules == true`,
	`view == REPLVariables && viewItem == juliakernel`,
	`view == REPLVariables && viewItem == juliarepl`,
	`view == REPLVariables && viewItem == juliatestprocess`,
	`view == REPLVariables && viewItem =~ /globalvariable/`,
	`view == REPLVariables && viewItem =~ /haslocation/`,
	`view == RSS-0`,
	`view == RSS-1`,
	`view == RSS-2`,
	`view == RSS-3`,
	`view == RSS-4`,
	`view == RSS-5`,
	`view == RSS-6`,
	`view == RSS-7`,
	`view == RSS-8`,
	`view == RSS-9`,
	`view == RailsSchema && viewItem == schemaTable`,
	`view == RedisExplorer`,
	`view == RosLocalTemplates && viewItem == rosLocalItem`,
	`view == RosLocalTemplates`,
	`view == RosRemoteTemplates && viewItem == rosRemoteItem`,
	`view == RosRemoteTemplates`,
	`view == RunConfigurations && viewItem == deployment`,
	`view == RunConfigurations && viewItem == deploymentgroup`,
	`view == RunConfigurations`,
	`view == S3TreeView`,
	`view == SCFLocalResource && viewItem == function`,
	`view == SCFLocalResource`,
	`view == SCFRemoteResource && viewItem != scfNotLogin && viewItem != region && viewItem!= namespace`,
	`view == SCFRemoteResource && viewItem == Nodejs10.15 || viewItem == Nodejs12.16`,
	`view == SCFRemoteResource`,
	`view == SerialDeviceProviderService && viewItem!=networklbl && viewItem!=ota`,
	`view == SerialDeviceProviderService && viewItem!=networklbl && viewItem==ota`,
	`view == SerialDeviceProviderService && viewItem!=networklbl`,
	`view == SerialDeviceProviderService`,
	`view == Settings && viewItem == AddToscaTemplate`,
	`view == Settings && viewItem == EditOrRemoveToscaTemplate || view == Settings && viewItem == DefaultTemplate`,
	`view == Settings && viewItem == EditOrRemoveToscaTemplate || view == Settings && viewItem == EditToscaTemplate`,
	`view == Settings && viewItem == openSettings`,
	`view == Settings && viewItem in SettingsEditToscaTemplateContext`,
	`view == SonarLint.AllRules && viewItem == rule-off`,
	`view == SonarLint.AllRules && viewItem == rule-on`,
	`view == SonarLint.AllRules`,
	`view == SonarLint.ConnectedMode && viewItem == sonarCloudGroup`,
	`view == SonarLint.ConnectedMode && viewItem == sonarQubeGroup`,
	`view == SonarLint.ConnectedMode && viewItem == sonarcloudConnection || viewItem == sonarqubeConnection`,
	`view == SonarLint.ConnectedMode && viewItem == sonarcloudConnection`,
	`view == SonarLint.ConnectedMode && viewItem == sonarqubeConnection`,
	`view == SonarLint.ConnectedMode && viewItem == workspaceFolder`,
	`view == SonarLint.ConnectedMode`,
	`view == SonarLint.IssueLocations`,
	`view == SonarLint.SecurityHotspots && viewItem == knownHotspotItem`,
	`view == SonarLint.SecurityHotspots && viewItem == newHotspotItem || viewItem == knownHotspotItem || viewItem == remoteHotspotItem`,
	`view == SonarLint.SecurityHotspots && viewItem == remoteHotspotItem`,
	`view == Storage`,
	`view == Subreddit`,
	`view == SubscriptionTree && viewItem == File`,
	`view == SubscriptionTree && viewItem =~ /^Gist/`,
	`view == SubscriptionTree && viewItem =~ /^User/`,
	`view == TOOL.file_tree && viewItem == file`,
	`view == TOOL.file_tree`,
	`view == TargetFrameworkSwitcherTreeView && viewItem == project-default`,
	`view == TargetFrameworkSwitcherTreeView && viewItem == targetFramework-nonActive`,
	`view == TargetFrameworkSwitcherTreeView`,
	`view == UDStudioView && viewItem == dashboard || viewItem == dashboardDesign`,
	`view == UDStudioView && viewItem == dashboardDesign`,
	`view == UDStudioView && viewItem == dashboard`,
	`view == UDStudioView && viewItem == endpoint-bp`,
	`view == UDStudioView && viewItem == endpoint`,
	`view == UDStudioView && viewItem == link`,
	`view == UDStudioView`,
	`view == UnrealProjectView || view == UVCHBrowser`,
	`view == UnrealProjectView`,
	`view == Users && viewItem == user`,
	`view == Users`,
	`view == WebKeyNote_doc_template`,
	`view == abapfs.abapgit  && viewItem == system`,
	`view == abapfs.abapgit &&  viewItem == repository_push`,
	`view == abapfs.abapgit && viewItem == repository || view == abapfs.abapgit && viewItem == repository_push `,
	`view == abapfs.abapgit && viewItem == repository || view == abapfs.abapgit && viewItem == repository_push`,
	`view == abapfs.abapgit`,
	`view == abapfs.atcFinds && !abapfs:atc:autorefreshOn`,
	`view == abapfs.atcFinds && !abapfs:atc:exemptFilterOn`,
	`view == abapfs.atcFinds && abapfs:atc:autorefreshOn`,
	`view == abapfs.atcFinds && abapfs:atc:exemptFilterOn`,
	`view == abapfs.atcFinds && viewItem == finding || view == abapfs.atcFinds && viewItem == finding_exempted`,
	`view == abapfs.atcFinds && viewItem == finding`,
	`view == abapfs.atcFinds`,
	`view == abapfs.dumps && viewItem == system`,
	`view == abapfs.favorites && viewItem == favourite`,
	`view == abapfs.transports && viewItem == tr_connection`,
	`view == abapfs.transports && viewItem == tr_object`,
	`view == abapfs.transports && viewItem == tr_unreleased`,
	`view == abapfs.transports && viewItem =~ /tr_(un)?released/ `,
	`view == abapfs.transports`,
	`view == abapfs.views.atcdocs`,
	`view == accounts && viewItem == account`,
	`view == accounts`,
	`view == acid-gitlab-mr-diffview && viewItem =~ /^added/`,
	`view == acid-gitlab-mr-diffview && viewItem =~ /^deleted/`,
	`view == acid-gitlab-mr-diffview && viewItem =~ /^modified/`,
	`view == acid-gitlab-mr-diffview && viewItem =~ /^renamed/`,
	`view == acid-gitlab-mr-diffview && viewItem =~ /review$/`,
	`view == acid-gitlab-mr-diffview`,
	`view == acid-gitlab-mr-list`,
	`view == acid-gitlab-proj-list && viewItem == proj`,
	`view == acid-gitlab-proj-list`,
	`view == actionComments && viewItem == $Comment`,
	`view == actionComments`,
	`view == actionsTree && viewItem == actions`,
	`view == actionsTree`,
	`view == active-scene-tree`,
	`view == activeTaskFunction && viewItem == activecommand`,
	`view == adb-helper.DeviceManager`,
	`view == adb-helper.ExplorerManager`,
	`view == adbDevices && viewItem == adbDevice`,
	`view == adbDevices`,
	`view == adonisjs.commands && viewItem == command`,
	`view == adonisjs.routes && !adonis-vscode-extension.view.routes.viewAsList`,
	`view == adonisjs.routes && adonis-vscode-extension.view.routes.viewAsList`,
	`view == adonisjs.routes && viewItem == route`,
	`view == adonisjs.routes`,
	`view == advplTestExplorer`,
	`view == aeCompositionOutline && viewItem == JsonValueArray`,
	`view == aeCompositionOutline && viewItem == JsonValueObject`,
	`view == aeCompositionOutline && viewItem == JsonValue`,
	`view == aeCompositionOutline && viewItem == PropertyGroup`,
	`view == aeCompositionOutline && viewItem == Property`,
	`view == aeCompositionOutline`,
	`view == agfexplorerview`,
	`view == alStudioScopeProvider && viewItem == alscope`,
	`view == alStudioScopeProvider && viewItem == alstudioscopeitem`,
	`view == allConflictTreeView`,
	`view == allSnipps && false`,
	`view == allSnipps`,
	`view == alsatianTests`,
	`view == altostraNoProject`,
	`view == altostraResources`,
	`view == amiga.customRegisters && viewItem == register`,
	`view == amiga.registers && viewItem == field`,
	`view == amiga.registers && viewItem == register`,
	`view == amiga.registers || view == amiga.customRegisters`,
	`view == analyzerView && analyzers.hasLogs`,
	`view == analyzerView && viewItem != isFolder`,
	`view == analyzerView && viewItem =~ /.*hasLogs.*/`,
	`view == analyzerView && viewItem =~ /.*isAnalyzer.*/`,
	`view == analyzerView && viewItem =~ /.*isFolder.*/`,
	`view == analyzerView && viewItem =~ /.*readMe.*/`,
	`view == analyzerView && viewItem =~ /moveDown/`,
	`view == analyzerView && viewItem =~ /moveUp/`,
	`view == analyzerView`,
	`view == angular.explorer && viewItem == ng:component`,
	`view == angular.explorer && viewItem == ng:module`,
	`view == angular.explorer && viewItem == ng:project`,
	`view == angular.explorer`,
	`view == angularExplorer`,
	`view == ansible-container-task`,
	`view == antEditorView && viewItem == antTarget`,
	`view == antEditorView`,
	`view == antRunnerView && viewItem == antDepends`,
	`view == antRunnerView && viewItem == antTarget`,
	`view == antRunnerView`,
	`view == apex-code-coverage-visualizer.test-view && filterClassesAll`,
	`view == apex-code-coverage-visualizer.test-view && filterClassesProject`,
	`view == apex-code-coverage-visualizer.test-view && filterCoverageAll`,
	`view == apex-code-coverage-visualizer.test-view && filterCoverageWarning`,
	`view == apex-code-coverage-visualizer.test-view`,
	`view == apexsql.server-explorer && viewItem == Server`,
	`view == apexsql.server-explorer && viewItem == User`,
	`view == apexsql.server-explorer && viewItem == Users`,
	`view == apexsql.server-explorer && viewItem == View`,
	`view == apexsql.server-explorer && viewItem =~ /^(Database|Server)$/`,
	`view == apexsql.server-explorer && viewItem =~ /^(Database|Table|ForeignKey|Index|Trigger|View|Function|Procedure|User)$/`,
	`view == apexsql.server-explorer && viewItem =~ /^(Database|Table|Trigger|View|Function|Procedure)$/`,
	`view == apexsql.server-explorer && viewItem =~ /^(Table|View)$/`,
	`view == apexsql.server-explorer && viewItem =~ /^(View|Table)$/`,
	`view == apexsql.server-explorer`,
	`view == apiConsole`,
	`view == apisView && viewItem == ApiRootTreeItem`,
	`view == apisView && viewItem == ApiValueTreeItem:AllowOfflineAccess`,
	`view == apisView && viewItem == ApiValueTreeItem:TokenLifetimeWeb`,
	`view == apisView && viewItem == ApiValueTreeItem:TokenLifetime`,
	`view == apisView && viewItem == ApiValueTreeItem`,
	`view == appium && viewItem == appiumServer`,
	`view == appium && viewItem == appiumSession`,
	`view == appium`,
	`view == applicationExplorer && viewItem == application`,
	`view == applicationExplorer`,
	`view == appmap.views.local && !appmap.recordingIsRunning`,
	`view == appmap.views.local && appmap.hasData`,
	`view == appmap.views.local && appmap.recordingIsRunning`,
	`view == appmap.views.local && viewItem == appmap.views.local.appMapCollection`,
	`view == appmap.views.local && viewItem == appmap.views.local.appMap`,
	`view == apps && config.apps-sdk.login == true`,
	`view == apps && viewItem == enact-app`,
	`view == apps && viewItem == js-service || viewItem == enact-app || viewItem == web-app`,
	`view == apps && viewItem == js-service || viewItem == enact-app`,
	`view == apps && viewItem == web-app || viewItem == enact-app || viewItem == js-service`,
	`view == apps && viewItem == web-app || viewItem == enact-app`,
	`view == appsTreeview && viewItem =~ /type:app/ && viewItem =~ /status:deleted/`,
	`view == appsTreeview && viewItem =~ /type:app/ && viewItem =~ /status:offline/`,
	`view == appsTreeview && viewItem =~ /type:app/ && viewItem =~ /status:online/`,
	`view == appsTreeview && viewItem =~ /type:page/`,
	`view == appsTreeview && viewItem =~ /type:view/`,
	`view == appsTreeview`,
	`view == appsView && viewItem == ApplicationRootTreeItem`,
	`view == appsView && viewItem == ApplicationTreeItem:CallbackUrls`,
	`view == appsView && viewItem == ApplicationValueTreeItem:CallbackUrl`,
	`view == appsView && viewItem == ApplicationValueTreeItem:Leeway`,
	`view == appsView && viewItem == ApplicationValueTreeItem:RotationType`,
	`view == appsView && viewItem == ApplicationValueTreeItem:TokenLifetime`,
	`view == appsView && viewItem == ApplicationValueTreeItem`,
	`view == apps`,
	`view == appstudioProjects`,
	`view == archetypePropertiesExplorer && viewItem == archetypeItem`,
	`view == archetypePropertiesExplorer`,
	`view == arxmlNavigationHelper.shortnames && viewItem == unique`,
	`view == arxmlNavigationHelper.shortnames`,
	`view == as3Tasks && viewItem == iAppTemplate`,
	`view == as3Tasks && viewItem == iApp`,
	`view == as3Tasks && viewItem == rule`,
	`view == as3Tasks`,
	`view == as3Tenants && viewItem == as3Tenant`,
	`view == as3Tenants`,
	`view == asaExplorer && viewItem == asaCluster`,
	`view == asaExplorer && viewItem == azureextensionui.azureSubscription`,
	`view == asaExplorer && viewItem == startableJob`,
	`view == asaExplorer && viewItem == stoppableJob`,
	`view == asaExplorer && viewItem =~ /(asaJob|startableJob)/`,
	`view == asaExplorer && viewItem =~ /(asaJob|stoppableJob|startableJob)/`,
	`view == asaExplorer && viewItem =~ /(azureextensionui.azureSubscription|asaJob|stoppableJob|startableJob|asaCluster|asaStandardCluster)/`,
	`view == asaExplorer`,
	`view == asdfSystems`,
	`view == askContainer.skill-actions`,
	`view == askContainer.skills-console`,
	`view == asmSymbolExplorer`,
	`view == assign-tags`,
	`view == ast.views.explorer`,
	`view == astResults && !ast-results-groupByDirectDependency`,
	`view == astResults && !ast-results-groupByFile`,
	`view == astResults && !ast-results-groupByLanguage`,
	`view == astResults && !ast-results-groupByQueryName`,
	`view == astResults && !ast-results-groupBySeverity`,
	`view == astResults && !ast-results-groupByState`,
	`view == astResults && !ast-results-groupByStatus`,
	`view == astResults && !ast-results-high`,
	`view == astResults && !ast-results-info`,
	`view == astResults && !ast-results-low`,
	`view == astResults && !ast-results-medium`,
	`view == astResults && ast-results-groupByDirectDependency`,
	`view == astResults && ast-results-groupByFile`,
	`view == astResults && ast-results-groupByLanguage`,
	`view == astResults && ast-results-groupByQueryName`,
	`view == astResults && ast-results-groupBySeverity`,
	`view == astResults && ast-results-groupByState`,
	`view == astResults && ast-results-groupByStatus`,
	`view == astResults && ast-results-high`,
	`view == astResults && ast-results-info`,
	`view == astResults && ast-results-low`,
	`view == astResults && ast-results-medium`,
	`view == astResults`,
	`view == astView && viewItem == astNode`,
	`view == astView && viewItem == ast`,
	`view == astView`,
	`view == ast`,
	`view == atlascode.views.bb.issuesTreeView && atlascode:isBBAuthenticated`,
	`view == atlascode.views.bb.issuesTreeView`,
	`view == atlascode.views.bb.pullrequestsTreeView`,
	`view == atlascode.views.helpTreeView`,
	`view == aurtools`,
	`view == automation-runbooks`,
	`view == availableNodeVersions && viewItem == availableVersionNode`,
	`view == availableNodeVersions`,
	`view == avdmanager-avd && viewItem == avd`,
	`view == avdmanager-avd`,
	`view == avdmanager-sdk-platforms && viewItem == sdk-pkg-installed-img`,
	`view == avdmanager-sdk-platforms || view == avdmanager-sdk-tools`,
	`view == avdmanager-sdk-platforms`,
	`view == avdmanager-sdk-tools`,
	`view == aws-cloudshell-view-1-sessions`,
	`view == aws.codecatalyst && !isCloud9`,
	`view == aws.explorer && !isCloud9`,
	`view == aws.explorer && viewItem == awsAppRunnerNode`,
	`view == aws.explorer && viewItem == awsAppRunnerServiceNode.PAUSED`,
	`view == aws.explorer && viewItem == awsAppRunnerServiceNode.RUNNING`,
	`view == aws.explorer && viewItem == awsCloudFormationNode`,
	`view == aws.explorer && viewItem == awsCloudFormationRootNode`,
	`view == aws.explorer && viewItem == awsCloudWatchLogNode`,
	`view == aws.explorer && viewItem == awsDocumentItemNodeWriteable`,
	`view == aws.explorer && viewItem == awsEcrNode`,
	`view == aws.explorer && viewItem == awsEcrRepositoryNode`,
	`view == aws.explorer && viewItem == awsEcrTagNode`,
	`view == aws.explorer && viewItem == awsEcsServiceNode.DISABLED`,
	`view == aws.explorer && viewItem == awsEcsServiceNode.ENABLED`,
	`view == aws.explorer && viewItem == awsIotCertsNode`,
	`view == aws.explorer && viewItem == awsIotNode`,
	`view == aws.explorer && viewItem == awsIotPoliciesNode`,
	`view == aws.explorer && viewItem == awsIotPolicyNode.Certificates`,
	`view == aws.explorer && viewItem == awsIotPolicyNode.WithVersions`,
	`view == aws.explorer && viewItem == awsIotPolicyVersionNode.NONDEFAULT`,
	`view == aws.explorer && viewItem == awsIotThingNode`,
	`view == aws.explorer && viewItem == awsIotThingsNode`,
	`view == aws.explorer && viewItem == awsLambdaNode || viewItem == awsRegionNode || viewItem == awsCloudFormationRootNode`,
	`view == aws.explorer && viewItem == awsLambdaNode || viewItem == awsRegionNode`,
	`view == aws.explorer && viewItem == awsRegionNode`,
	`view == aws.explorer && viewItem == awsRegistryItemNode`,
	`view == aws.explorer && viewItem == awsS3BucketNode`,
	`view == aws.explorer && viewItem == awsS3FileNode && !isCloud9`,
	`view == aws.explorer && viewItem == awsS3FileNode`,
	`view == aws.explorer && viewItem == awsS3Node`,
	`view == aws.explorer && viewItem == awsSchemaItemNode`,
	`view == aws.explorer && viewItem == awsSchemasNode`,
	`view == aws.explorer && viewItem == awsStateMachineNode`,
	`view == aws.explorer && viewItem == awsStepFunctionsNode`,
	`view == aws.explorer && viewItem == resourcesRootNode`,
	`view == aws.explorer && viewItem =~ /^(.*)(Creatable)(.*)(ResourceTypeNode)$/ && !isCloud9 && config.aws.experiments.jsonResourceModification`,
	`view == aws.explorer && viewItem =~ /^(.*)(Deletable)(.*)(ResourceNode)$/ && !isCloud9 && config.aws.experiments.jsonResourceModification`,
	`view == aws.explorer && viewItem =~ /^(.*)(Documented)(.*)(ResourceTypeNode)$/`,
	`view == aws.explorer && viewItem =~ /^(.*)(ResourceNode)$/`,
	`view == aws.explorer && viewItem =~ /^(.*)(Updatable)(.*)(ResourceNode)$/ && !isCloud9 && config.aws.experiments.jsonResourceModification`,
	`view == aws.explorer && viewItem =~ /^(awsApiGatewayNode)$/`,
	`view == aws.explorer && viewItem =~ /^(awsDocumentItemNode|awsDocumentItemNodeWriteable)$/`,
	`view == aws.explorer && viewItem =~ /^(awsEcsClusterNode|awsEcsContainerNode)$|^awsEcsServiceNode/`,
	`view == aws.explorer && viewItem =~ /^(awsEcsContainerNodeExec)(.*)$/ && !isCloud9`,
	`view == aws.explorer && viewItem =~ /^(awsEcsContainerNodeExec)(.*)$/`,
	`view == aws.explorer && viewItem =~ /^(awsIotCertificateNode.Things)/`,
	`view == aws.explorer && viewItem =~ /^(awsRegionFunctionNode|awsRegionFunctionNodeDownloadable)$/`,
	`view == aws.explorer && viewItem =~ /^(awsRegionFunctionNode|awsRegionFunctionNodeDownloadable|awsCloudFormationFunctionNode)$/`,
	`view == aws.explorer && viewItem =~ /^(awsRegionFunctionNode|awsRegionFunctionNodeDownloadable|awsCloudFormationFunctionNode|awsStateMachineNode|awsCloudFormationNode|awsCloudWatchLogNode|awsS3BucketNode|awsS3FolderNode|awsS3FileNode|awsApiGatewayNode|awsEcrRepositoryNode|awsIotThingNode)$|^(awsAppRunnerServiceNode|awsEcsServiceNode|awsIotCertificateNode|awsIotPolicyNode|awsIotPolicyVersionNode|awsMdeInstanceNode)/`,
	`view == aws.explorer && viewItem =~ /^(awsRegionFunctionNode|awsRegionFunctionNodeDownloadable|awsCloudFormationFunctionNode|awsStateMachineNode|awsCloudFormationNode|awsS3BucketNode|awsS3FolderNode|awsS3FileNode|awsApiGatewayNode|awsIotThingNode)$|^(awsAppRunnerServiceNode|awsIotCertificateNode|awsIotPolicyNode|awsIotPolicyVersionNode)/`,
	`view == aws.explorer && viewItem =~ /^(awsS3BucketNode|awsS3FolderNode)$/`,
	`view == aws.explorer && viewItem =~ /^(awsS3FileNode)$/`,
	`view == aws.explorer && viewItem =~ /^(awsS3FolderNode|awsS3FileNode)$/`,
	`view == aws.explorer && viewItem =~ /^awsIotCertificateNode.(Things|Policies).(ACTIVE|INACTIVE)$/`,
	`view == aws.explorer && viewItem =~ /^awsIotCertificateNode.(Things|Policies).ACTIVE$/`,
	`view == aws.explorer && viewItem =~ /^awsIotCertificateNode.(Things|Policies).INACTIVE$/`,
	`view == aws.explorer && viewItem =~ /^awsIotCertificateNode.(Things|Policies)/`,
	`view == aws.explorer && viewItem =~ /^awsIotCertificateNode.Policies/`,
	`view == aws.explorer && viewItem =~ /^awsIotPolicyVersionNode./`,
	`view == aws.explorer && viewItem =~ /awsAppRunnerServiceNode.[RUNNING|PAUSED|CREATE_FAILED]/`,
	`view == aws.explorer && viewItem =~ /awsEcrTagNode|awsEcrRepositoryNode/`,
	`view == aws.explorer || !aws.explorer.visible && view =~ /^aws/`,
	`view == aws.explorer`,
	`view == aws_ec2_containers && viewItem == detail_item`,
	`view == aws_ec2_containers && viewItem == running_container`,
	`view == aws_ec2_containers && viewItem == stopped_container`,
	`view == aws_ec2_containers`,
	`view == azALDevTools.DuplicateCodeTreeProvider`,
	`view == azALDevTools.SymbolsTreeProvider && !azALDevTools:alOutlineFollowCursor`,
	`view == azALDevTools.SymbolsTreeProvider && azALDevTools:alOutlineFollowCursor`,
	`view == azALDevTools.SymbolsTreeProvider && azALDevTools:alOutlineSortMode != category`,
	`view == azALDevTools.SymbolsTreeProvider && azALDevTools:alOutlineSortMode != name`,
	`view == azALDevTools.SymbolsTreeProvider && azALDevTools:alOutlineSortMode != position`,
	`view == azALDevTools.SymbolsTreeProvider && azALDevTools:alOutlineSortMode == category`,
	`view == azALDevTools.SymbolsTreeProvider && azALDevTools:alOutlineSortMode == name`,
	`view == azALDevTools.SymbolsTreeProvider && azALDevTools:alOutlineSortMode == position`,
	`view == azALDevTools.SymbolsTreeProvider && viewItem == PageObject`,
	`view == azALDevTools.SymbolsTreeProvider && viewItem == ReportObject`,
	`view == azALDevTools.SymbolsTreeProvider && viewItem == TableObject`,
	`view == azALDevTools.SymbolsTreeProvider`,
	`view == azALDevTools.WarningDirectivesTreeView`,
	`view == azDevOps && !config.cc-azdevops.onlyShowFavoriteOrganizations`,
	`view == azDevOps && !config.cc-azdevops.onlyShowMyBranches`,
	`view == azDevOps && config.cc-azdevops.onlyShowFavoriteOrganizations`,
	`view == azDevOps && config.cc-azdevops.onlyShowMyBranches`,
	`view == azDevOps && viewItem == branch`,
	`view == azDevOps && viewItem == build`,
	`view == azDevOps && viewItem == labelBelowRepository`,
	`view == azDevOps && viewItem == organization`,
	`view == azDevOps && viewItem == package`,
	`view == azDevOps && viewItem == pipeline`,
	`view == azDevOps && viewItem == project && config.cc-azdevops.previewMode`,
	`view == azDevOps && viewItem == project`,
	`view == azDevOps && viewItem == repository`,
	`view == azDevOps && viewItem == workitem`,
	`view == azDevOps`,
	`view == azLogicApps && viewItem == applicationSettingItem`,
	`view == azLogicApps && viewItem == applicationSettings`,
	`view == azLogicApps && viewItem == azFuncProductionSlot`,
	`view == azLogicApps && viewItem == azFuncProxies`,
	`view == azLogicApps && viewItem == azFuncSlot`,
	`view == azLogicApps && viewItem == azFuncSlots`,
	`view == azLogicApps && viewItem == azureextensionui.azureSubscription`,
	`view == azLogicApps && viewItem == deployment/github`,
	`view == azLogicApps && viewItem == deploymentsConnected`,
	`view == azLogicApps && viewItem == folder`,
	`view == azLogicApps && viewItem == logFiles`,
	`view == azLogicApps && viewItem == siteFiles`,
	`view == azLogicApps && viewItem =~ /Functions;/i`,
	`view == azLogicApps && viewItem =~ /Remote;.*;Function;/i`,
	`view == azLogicApps && viewItem =~ /Remote;.*;Workflow;/i`,
	`view == azLogicApps && viewItem =~ /^azFunc(Production|)Slot$/ && config.azureLogicAppsStandard.enableRemoteDebugging == true`,
	`view == azLogicApps && viewItem =~ /^azFunc(Production|)Slot$/`,
	`view == azLogicApps && viewItem =~ /^deployment//`,
	`view == azLogicApps && viewItem =~ /^deployments(C|Unc)onnected$/`,
	`view == azLogicApps`,
	`view == azdo:activePullRequest && azdo:hasAzdoRemotes`,
	`view == azdopr:azdo && viewItem =~ /description/`,
	`view == azdopr:azdo && viewItem =~ /pullrequest(:local)?:active|description/`,
	`view == azdopr:azdo && viewItem =~ /pullrequest(:local)?:nonactive|description/`,
	`view == azdopr:azdo && viewItem =~ /pullrequest:local:nonactive/`,
	`view == azdopr:azdo && viewItem =~ /pullrequest|description/`,
	`view == azdoprStatus:azdo && !fileListLayout:flat`,
	`view == azdoprStatus:azdo && fileListLayout:flat`,
	`view == azdoprStatus:azdo && viewItem =~ /commit/`,
	`view == azdoprStatus:azdo`,
	`view == azkeyvaulthelper`,
	`view == azure-work-management.open-backlogs`,
	`view == azure-work-management.open-boards && viewItem == workItem`,
	`view == azure-work-management.open-boards`,
	`view == azureActivityLog`,
	`view == azureApiManagementExplorer && viewItem == azureApiManagementApi && config.azureApiManagement.scaffoldAzureFunctions == true`,
	`view == azureApiManagementExplorer && viewItem == azureApiManagementApiOperation`,
	`view == azureApiManagementExplorer && viewItem == azureApiManagementApi`,
	`view == azureApiManagementExplorer && viewItem == azureApiManagementApis`,
	`view == azureApiManagementExplorer && viewItem == azureApiManagementAuthorizationAccessPolicies`,
	`view == azureApiManagementExplorer && viewItem == azureApiManagementAuthorizationAccessPolicy`,
	`view == azureApiManagementExplorer && viewItem == azureApiManagementAuthorizationProvider`,
	`view == azureApiManagementExplorer && viewItem == azureApiManagementAuthorizationProviders`,
	`view == azureApiManagementExplorer && viewItem == azureApiManagementAuthorization`,
	`view == azureApiManagementExplorer && viewItem == azureApiManagementAuthorizations`,
	`view == azureApiManagementExplorer && viewItem == azureApiManagementGatewayApi`,
	`view == azureApiManagementExplorer && viewItem == azureApiManagementGatewayApis`,
	`view == azureApiManagementExplorer && viewItem == azureApiManagementGatewayTreeItem`,
	`view == azureApiManagementExplorer && viewItem == azureApiManagementNamedValue`,
	`view == azureApiManagementExplorer && viewItem == azureApiManagementNamedValues`,
	`view == azureApiManagementExplorer && viewItem == azureApiManagementOperations`,
	`view == azureApiManagementExplorer && viewItem == azureApiManagementProductApi`,
	`view == azureApiManagementExplorer && viewItem == azureApiManagementProductApis`,
	`view == azureApiManagementExplorer && viewItem == azureApiManagementService`,
	`view == azureApiManagementExplorer && viewItem == azureApiManagementSubscriptionTreeItem`,
	`view == azureApiManagementExplorer && viewItem == azureApiManagementSubscriptions`,
	`view == azureApiManagementExplorer && viewItem == azureextensionui.azureSubscription`,
	`view == azureApiManagementExplorer`,
	`view == azureCache && viewItem == azureextensionui.azureSubscription`,
	`view == azureCache && viewItem == redisCache`,
	`view == azureCache && viewItem == redisClusterNode`,
	`view == azureCache && viewItem == redisDb`,
	`view == azureCache && viewItem =~ /redisCache/`,
	`view == azureEventGridSubscriptionExplorer && viewItem == azureEventGridSubscription`,
	`view == azureEventGridSubscriptionExplorer && viewItem == azureextensionui.azureSubscription`,
	`view == azureEventGridSubscriptionExplorer`,
	`view == azureEventGridTopicExplorer && viewItem == azureEventGridTopic`,
	`view == azureEventGridTopicExplorer && viewItem == azureextensionui.azureSubscription`,
	`view == azureEventGridTopicExplorer`,
	`view == azureIntegrationAccountsExplorer && viewItem == azIntegrationAccountAgreement`,
	`view == azureIntegrationAccountsExplorer && viewItem == azIntegrationAccountAgreements`,
	`view == azureIntegrationAccountsExplorer && viewItem == azIntegrationAccountMap`,
	`view == azureIntegrationAccountsExplorer && viewItem == azIntegrationAccountMaps`,
	`view == azureIntegrationAccountsExplorer && viewItem == azIntegrationAccountPartner`,
	`view == azureIntegrationAccountsExplorer && viewItem == azIntegrationAccountPartners`,
	`view == azureIntegrationAccountsExplorer && viewItem == azIntegrationAccountSchema`,
	`view == azureIntegrationAccountsExplorer && viewItem == azIntegrationAccountSchemas`,
	`view == azureIntegrationAccountsExplorer && viewItem == azIntegrationAccount`,
	`view == azureIntegrationAccountsExplorer && viewItem == azureextensionui.azureSubscription`,
	`view == azureIntegrationAccountsExplorer`,
	`view == azureLogicAppsExplorer && viewItem == azLogicAppsWorkflowCurrentVersion`,
	`view == azureLogicAppsExplorer && viewItem == azLogicAppsWorkflowRunAction`,
	`view == azureLogicAppsExplorer && viewItem == azLogicAppsWorkflowRunActions`,
	`view == azureLogicAppsExplorer && viewItem == azLogicAppsWorkflowRun`,
	`view == azureLogicAppsExplorer && viewItem == azLogicAppsWorkflowRuns`,
	`view == azureLogicAppsExplorer && viewItem == azLogicAppsWorkflowTrigger`,
	`view == azureLogicAppsExplorer && viewItem == azLogicAppsWorkflowTriggers`,
	`view == azureLogicAppsExplorer && viewItem == azLogicAppsWorkflowVersion`,
	`view == azureLogicAppsExplorer && viewItem == azLogicAppsWorkflowVersions`,
	`view == azureLogicAppsExplorer && viewItem == azLogicAppsWorkflow`,
	`view == azureLogicAppsExplorer && viewItem == azureextensionui.azureSubscription`,
	`view == azureLogicAppsExplorer`,
	`view == azurePolicy && viewItem == azureBuiltinDefinitions`,
	`view == azurePolicy && viewItem == azureCustomDefinitions`,
	`view == azurePolicy && viewItem == azurePolicyAssignments`,
	`view == azurePolicy && viewItem == azurePolicyInitiatives`,
	`view == azurePolicy && viewItem == azurePolicy`,
	`view == azurePolicy`,
	`view == azureRegistries && viewItem == tag`,
	`view == azureRegistries`,
	`view == azureResource && viewItem == azureProviderResourceLeaf`,
	`view == azureResource && viewItem == azureProviderResourceType`,
	`view == azureResource && viewItem == azureProviderResource`,
	`view == azureResource && viewItem == azureProvider`,
	`view == azureResource && viewItem == azureResourceGroup`,
	`view == azureResource && viewItem == azureResourceGroups`,
	`view == azureResource && viewItem == azureResourceProviders`,
	`view == azureResource && viewItem == azureResource`,
	`view == azureResourceGroups && viewItem == azAppSlot`,
	`view == azureResourceGroups && viewItem == azFuncSlots`,
	`view == azureResourceGroups && viewItem == azureBlobContainer`,
	`view == azureResourceGroups && viewItem == azureBlobDirectory`,
	`view == azureResourceGroups && viewItem == azureBlobFile`,
	`view == azureResourceGroups && viewItem == azureFileShareDirectory`,
	`view == azureResourceGroups && viewItem == azureFileShare`,
	`view == azureResourceGroups && viewItem == azureFile`,
	`view == azureResourceGroups && viewItem == azureStaticActionCompleted`,
	`view == azureResourceGroups && viewItem == azureStaticActionInProgress`,
	`view == azureResourceGroups && viewItem == azureStaticActions`,
	`view == azureResourceGroups && viewItem == azureStaticEnvironment`,
	`view == azureResourceGroups && viewItem == azureextensionui.azureSubscription`,
	`view == azureResourceGroups && viewItem == connections`,
	`view == azureResourceGroups && viewItem == cosmosDBConnection`,
	`view == azureResourceGroups && viewItem == deploymentNASlots`,
	`view == azureResourceGroups && viewItem == deploymentSlots`,
	`view == azureResourceGroups && viewItem == webJobs`,
	`view == azureResourceGroups && viewItem == сosmosDBConnections`,
	`view == azureResourceGroups && viewItem =~ /(azAppSlot)/`,
	`view == azureResourceGroups && viewItem =~ /(azAppWebApp|azAppSlot)/`,
	`view == azureResourceGroups && viewItem =~ /(focused).*(group)/`,
	`view == azureResourceGroups && viewItem =~ /(group).*(unfocused)/`,
	`view == azureResourceGroups && viewItem =~ /Remote;.*;Function;/i`,
	`view == azureResourceGroups && viewItem =~ /Remote;ReadWrite;Function;/i`,
	`view == azureResourceGroups && viewItem =~ /StaticWebApps/i && viewItem =~ /azureResourceTypeGroup/i`,
	`view == azureResourceGroups && viewItem =~ /appService.*(deployments(C|Unc)onnected)/`,
	`view == azureResourceGroups && viewItem =~ /appService.*applicationSettingItem(|NoSlots)/`,
	`view == azureResourceGroups && viewItem =~ /appService.*applicationSettingItem/`,
	`view == azureResourceGroups && viewItem =~ /appService.*applicationSettings/`,
	`view == azureResourceGroups && viewItem =~ /appService.*deployment//`,
	`view == azureResourceGroups && viewItem =~ /appService.*deployment/github/`,
	`view == azureResourceGroups && viewItem =~ /appService.*deployments(C|Unc)onnected/`,
	`view == azureResourceGroups && viewItem =~ /appService.*deploymentsConnected/`,
	`view == azureResourceGroups && viewItem =~ /appService.*folder/`,
	`view == azureResourceGroups && viewItem =~ /appService.*logFiles/`,
	`view == azureResourceGroups && viewItem =~ /appService.*siteFiles/`,
	`view == azureResourceGroups && viewItem =~ /applicationSettingItem.*azFunc/`,
	`view == azureResourceGroups && viewItem =~ /applicationSettingItem.*staticWebApps/`,
	`view == azureResourceGroups && viewItem =~ /applicationSettings.*azFunc/`,
	`view == azureResourceGroups && viewItem =~ /applicationSettings.*staticWebApps/`,
	`view == azureResourceGroups && viewItem =~ /azAppSlot/`,
	`view == azureResourceGroups && viewItem =~ /azAppWebApp/`,
	`view == azureResourceGroups && viewItem =~ /azFunc(Production|)Slot(?!s).*slot/`,
	`view == azureResourceGroups && viewItem =~ /azFunc(Production|)Slot(?!s)/`,
	`view == azureResourceGroups && viewItem =~ /azFunc(Production|)Slot/ && config.azureFunctions.enableJavaRemoteDebugging == true`,
	`view == azureResourceGroups && viewItem =~ /azFunc(Production|)Slot/ && config.azureFunctions.enableRemoteDebugging == true`,
	`view == azureResourceGroups && viewItem =~ /azFunc.*(deployments(C|Unc)onnected)/`,
	`view == azureResourceGroups && viewItem =~ /azFunc.*Functions;/i`,
	`view == azureResourceGroups && viewItem =~ /azFunc.*deployment//`,
	`view == azureResourceGroups && viewItem =~ /azFunc.*deployment/github/`,
	`view == azureResourceGroups && viewItem =~ /azFunc.*deploymentsConnected/`,
	`view == azureResourceGroups && viewItem =~ /azFunc.*folder/`,
	`view == azureResourceGroups && viewItem =~ /azFunc.*logFiles/`,
	`view == azureResourceGroups && viewItem =~ /azFunc.*siteFiles/`,
	`view == azureResourceGroups && viewItem =~ /azFuncProductionSlot/`,
	`view == azureResourceGroups && viewItem =~ /azFuncSlot(?!s)/`,
	`view == azureResourceGroups && viewItem =~ /azResource/i && viewItem =~ /^(?!.*containerEnvironment).*/i`,
	`view == azureResourceGroups && viewItem =~ /azureResource(?!Group)(?!Type)/`,
	`view == azureResourceGroups && viewItem =~ /azureResource(?!Type)/`,
	`view == azureResourceGroups && viewItem =~ /azureResource/`,
	`view == azureResourceGroups && viewItem =~ /azureResourceGroup/`,
	`view == azureResourceGroups && viewItem =~ /azureResourceTypeGroup.*(microsoft.documentdb/databaseaccounts|microsoft.dbforpostgresql/(servers|flexibleservers))/`,
	`view == azureResourceGroups && viewItem =~ /azureResourceTypeGroup.*microsoft.web/sites(?!/)/`,
	`view == azureResourceGroups && viewItem =~ /azureResourceTypeGroup/ && viewItem =~ /storageaccounts/i`,
	`view == azureResourceGroups && viewItem =~ /azureResourceTypeGroup/i && viewItem =~ /containerAppsEnvironment/i`,
	`view == azureResourceGroups && viewItem =~ /azureStatic(Environment|Action[^s]|Job|Step$)/`,
	`view == azureResourceGroups && viewItem =~ /azureStatic(Environment|Functions|Actions?|Job|Step|WorkflowGroup|LocalProject)|applicationSettings.*staticWebApps/`,
	`view == azureResourceGroups && viewItem =~ /azureStatic(Functions)|applicationSettings.*staticWebApps/`,
	`view == azureResourceGroups && viewItem =~ /azureStatic(Job|Step$)/`,
	`view == azureResourceGroups && viewItem =~ /azureStatic(WebApp|Environment)/`,
	`view == azureResourceGroups && viewItem =~ /azureStaticWebApp/`,
	`view == azureResourceGroups && viewItem =~ /azureStorageAccount/ && !isWeb && !isLinux && !remoteName`,
	`view == azureResourceGroups && viewItem =~ /azureStorageAccount/`,
	`view == azureResourceGroups && viewItem =~ /containerApp[^s]/i`,
	`view == azureResourceGroups && viewItem =~ /containerEnvironment/i`,
	`view == azureResourceGroups && viewItem =~ /cosmosDBDocumentServer(?![a-z])/i`,
	`view == azureResourceGroups && viewItem =~ /cosmosDBGraphAccount(?![a-z])/i`,
	`view == azureResourceGroups && viewItem =~ /cosmosDBMongoServer(?![a-z])/i`,
	`view == azureResourceGroups && viewItem =~ /cosmosDBTableAccount(?![a-z])/i`,
	`view == azureResourceGroups && viewItem =~ /group/`,
	`view == azureResourceGroups && viewItem =~ /ingress.+disabled/i`,
	`view == azureResourceGroups && viewItem =~ /ingress.+enabled/i`,
	`view == azureResourceGroups && viewItem =~ /ingress.+enabled|targetPort/i`,
	`view == azureResourceGroups && viewItem =~ /linuxVirtualMachine/`,
	`view == azureResourceGroups && viewItem =~ /microsoft.web/functionapp/i && viewItem =~ /azureResourceTypeGroup/i`,
	`view == azureResourceGroups && viewItem =~ /postgresServer(?![a-z])/i`,
	`view == azureResourceGroups && viewItem =~ /revisionmode:single|revision[^a-z]/i`,
	`view == azureResourceGroups && viewItem =~ /revisionmode:single|revisions$/i`,
	`view == azureResourceGroups && viewItem =~ /scale(?![a-z])/i`,
	`view == azureResourceGroups && viewItem =~ /scaleRules/i`,
	`view == azureResourceGroups && viewItem =~ /virtualmachine(?!s)/i`,
	`view == azureResourceGroups && viewItem =~ /virtualmachines/i && viewItem =~ /azureResourceTypeGroup/i`,
	`view == azureResourceGroups`,
	`view == azureResource`,
	`view == azureSpringApps && viewItem == azureSpringApps.app.instances`,
	`view == azureSpringApps && viewItem == azureSpringApps.apps`,
	`view == azureSpringApps && viewItem == azureextensionui.azureSubscription`,
	`view == azureSpringApps && viewItem =~ /^azureSpringApps\\.app(\\.instance)?;status-[Rr]unning;/i`,
	`view == azureSpringApps && viewItem =~ /^azureSpringApps\\.app;status-(running|stopped);.*;public-false;/i`,
	`view == azureSpringApps && viewItem =~ /^azureSpringApps\\.app;status-(running|stopped);.*;public-true;/i`,
	`view == azureSpringApps && viewItem =~ /^azureSpringApps\\.app;status-(running|stopped);/i`,
	`view == azureSpringApps && viewItem =~ /^azureSpringApps\\.app;status-.+;/i`,
	`view == azureSpringApps && viewItem =~ /^azureSpringApps\\.app;status-running;/i`,
	`view == azureSpringApps && viewItem =~ /^azureSpringApps\\.app;status-running;debugging-disabled;/i`,
	`view == azureSpringApps && viewItem =~ /^azureSpringApps\\.app;status-running;debugging-enabled;/i`,
	`view == azureSpringApps && viewItem =~ /^azureSpringApps\\.app;status-stopped;/i`,
	`view == azureSpringApps && viewItem =~ /^azureSpringApps\\.app\\.(jvmOptions|envVariables)$/`,
	`view == azureSpringApps && viewItem =~ /^azureSpringApps\\.app\\.(jvmOptions|scaleSettings)$/`,
	`view == azureSpringApps && viewItem =~ /^azureSpringApps\\.app\\.(jvmOptions|scaleSettings|envVariables)$/`,
	`view == azureSpringApps && viewItem =~ /^azureSpringApps\\.app\\.(jvmOption|envVariable)$/`,
	`view == azureSpringApps && viewItem =~ /^azureSpringApps\\.app\\.(jvmOption|scaleSetting|envVariable)$/`,
	`view == azureSpringApps && viewItem =~ /^azureSpringApps\\.app\\.envVariable(|s)$/`,
	`view == azureSpringApps && viewItem =~ /^azureSpringApps\\.app\\.instance;status-.+;/i`,
	`view == azureSpringApps && viewItem =~ /^azureSpringApps\\.app\\.instance;status-Running;/i`,
	`view == azureSpringApps`,
	`view == azureStorage && viewItem == blobContainerLabel`,
	`view == azureStorage && viewItem == blobContainer`,
	`view == azureStorage && viewItem == blob`,
	`view == azureStorage && viewItem == directory`,
	`view == azureStorage && viewItem == fileShareLabel`,
	`view == azureStorage && viewItem == fileShare`,
	`view == azureStorage && viewItem == file`,
	`view == azureStorage && viewItem == subscription`,
	`view == azureStorage && viewItem == tableLabel`,
	`view == azureStorage && viewItem == table`,
	`view == azureStorage`,
	`view == azureVirtualMachine && viewItem == subscription`,
	`view == azureVirtualMachine && viewItem == virtualMachine`,
	`view == azureVirtualMachine`,
	`view == azureWorkspace && viewItem == attachedStorageAccounts`,
	`view == azureWorkspace && viewItem == cosmosDBAttachedAccountsWithEmulator`,
	`view == azureWorkspace && viewItem == cosmosDBDocumentServerAttached`,
	`view == azureWorkspace && viewItem == cosmosDBGraphAccountAttached`,
	`view == azureWorkspace && viewItem == cosmosDBMongoServerAttached`,
	`view == azureWorkspace && viewItem == cosmosDBTableAccountAttached`,
	`view == azureWorkspace && viewItem == postgresServerAttached`,
	`view == azureWorkspace && viewItem =~ /^cosmosDBAttachedAccounts(?![a-z])/gi`,
	`view == azureWorkspace && viewItem =~ /cosmosDBAttachedAccounts(?![a-z])/gi`,
	`view == azureWorkspace`,
	`view == azureautomation && viewItem == azureAutomation3.Connections`,
	`view == azureautomation && viewItem == azureAutomation3.Credentials`,
	`view == azureautomation && viewItem == azureAutomation3.Runbooks`,
	`view == azureautomation && viewItem == azureAutomation3.Variables`,
	`view == azureautomation && viewItem == azureAutomation3.certificate`,
	`view == azureautomation && viewItem == azureAutomation3.connection`,
	`view == azureautomation && viewItem == azureAutomation3.credential`,
	`view == azureautomation && viewItem == azureAutomation3.job`,
	`view == azureautomation && viewItem == azureAutomation3.schedule`,
	`view == azureautomation && viewItem == azureAutomation3.variable`,
	`view == azureautomation && viewItem =~ /^azureAutomation3.runbookItem(Edit|New)/`,
	`view == azureautomation && viewItem =~ /^azureAutomation3.runbookItem(Edit|Published)/`,
	`view == azureautomation && viewItem =~ /^azureAutomation3.runbookItem(Published|Edit)/`,
	`view == azureautomation && viewItem =~ /^azureAutomation3.runbookItem(Published|Edit|New)(PowerShell|PowerShell7)/`,
	`view == azureautomation && viewItem =~ /^azureAutomation3.runbookItem/`,
	`view == azureautomation && viewItem =~ /azureAutomation/`,
	`view == azureautomation`,
	`view == azurerm-vscode-tools.template-outline && viewItem == functions@1`,
	`view == azurerm-vscode-tools.template-outline && viewItem == outputs@1`,
	`view == azurerm-vscode-tools.template-outline && viewItem == parameters@1`,
	`view == azurerm-vscode-tools.template-outline && viewItem == resources@1`,
	`view == azurerm-vscode-tools.template-outline && viewItem == variables@1`,
	`view == azurerm-vscode-tools.template-outline && viewItem =~ /functions@\\d/`,
	`view == azurerm-vscode-tools.template-outline && viewItem =~ /outputs@\\d/`,
	`view == azurerm-vscode-tools.template-outline && viewItem =~ /parameters@\\d/`,
	`view == azurerm-vscode-tools.template-outline && viewItem =~ /resources@\\d/`,
	`view == azurerm-vscode-tools.template-outline && viewItem =~ /variables@\\d/`,
	`view == azurerm-vscode-tools.template-outline`,
	`view == azuresphere.deviceExplorer`,
	`view == azuresphere.tenantExplorer && viewItem == login && !azsphereLoggedIn`,
	`view == azuresphere.tenantExplorer`,
	`view == ballerinaExplorerTreeView && viewItem == file || viewItem == folder`,
	`view == ballerinaExplorerTreeView && viewItem == folder`,
	`view == ballerinaExplorerTreeView`,
	`view == ballerinaProjectTree`,
	`view == ballerinaViewVariables`,
	`view == bazelWorkspace && viewItem == package`,
	`view == bazelWorkspace && viewItem == rule`,
	`view == bazelWorkspace && viewItem == testRule`,
	`view == bazelWorkspace`,
	`view == bb-spectacle-views.history.file && viewItem == commit`,
	`view == bb-spectacle-views.history.file && viewItem == pull-request`,
	`view == bb-spectacle-views.history.file`,
	`view == bb-spectacle-views.history.line && viewItem == commit`,
	`view == bb-spectacle-views.history.line && viewItem == pull-request`,
	`view == bb-spectacle-views.history.line`,
	`view == bb-spectacle-views.pull-requests && viewItem == pull-request`,
	`view == bb-spectacle-views.pull-requests`,
	`view == bctouch-window-device-list`,
	`view == bdd_refs`,
	`view == beamdasm.beamFilesTree`,
	`view == betterOutline`,
	`view == bibItemTree && viewItem == bibItem`,
	`view == bibItemTree && viewItem == header`,
	`view == bibItemTree`,
	`view == bigfish-container-view-pages && !bigfish.routes.empty`,
	`view == bigfish-container-view-pages`,
	`view == bigqueryRunner.resources && viewItem == table`,
	`view == bigqueryRunner.resources`,
	`view == bitComponents && viewItem == component`,
	`view == bitComponents`,
	`view == bitlab-vscode.taskpanel && viewItem == item`,
	`view == bitlab-vscode.taskpanel`,
	`view == blinktrust.dataPrivacy`,
	`view == blitzmax-build-options && viewItem == option`,
	`view == blitzmax-build-options && viewItem == workspaceRoot`,
	`view == blitzmax-build-options`,
	`view == blogCategoriesExplorer`,
	`view == blogPostExplorer && viewItem == BlogPostItem-diff`,
	`view == blogPostExplorer && viewItem == Category`,
	`view == blogPostExplorer && viewItem =~ /BlogPostItem(?!-create)/`,
	`view == blogPostExplorer && viewItem =~ /BlogPostItem*/`,
	`view == blogPostExplorer`,
	`view == boards-packages && viewItem == board`,
	`view == boards-packages && viewItem == package && config.cros-ide.testCoverage.enabled`,
	`view == boards-packages && viewItem == package && config.cros-ide.underDevelopment.testCoverage`,
	`view == boards-packages && viewItem == package`,
	`view == boards-packages && viewItem =~ /^board/`,
	`view == boards-packages`,
	`view == bookmarksByActiveGroup && viewItem != none && viewItem != file || view == bookmarksByInactiveGroups && viewItem != none && viewItem != file || view == bookmarksByFile && viewItem != none && viewItem != file`,
	`view == bookmarksByActiveGroup && viewItem != none || view == bookmarksByInactiveGroups && viewItem != none || view == bookmarksByFile && viewItem != none `,
	`view == bookmarksByInactiveGroups && viewItem == group`,
	`view == bookmarksExplorer && !bookmarks.isHidingPosition`,
	`view == bookmarksExplorer && !bookmarks.viewAsList`,
	`view == bookmarksExplorer && bookmarks.isHidingPosition`,
	`view == bookmarksExplorer && bookmarks.viewAsList`,
	`view == bookmarksExplorer && viewItem == BookmarkNodeBookmark`,
	`view == bookmarksExplorer && viewItem == BookmarkNodeFile`,
	`view == bookmarksExplorer`,
	`view == bookmarks`,
	`view == books`,
	`view == bookstruct`,
	`view == bpSystems && viewItem != predefinedServer && viewItem == bpSystem.server`,
	`view == bpSystems && viewItem != predefinedServer`,
	`view == bpSystems && viewItem == bpSystems.CloneModelDefinitions`,
	`view == bpSystems && viewItem == bpSystems.Onboard`,
	`view == bpSystems && viewItem == bpSystems.PingBpServer`,
	`view == bpSystems && viewItem == bpSystems.PullSSHKey`,
	`view == bpSystems && viewItem == bpSystems.PushSSHKey`,
	`view == bpSystems`,
	`view == branchchanges`,
	`view == bravo.modelChanges && bravo.hasReport && !config.bravo.showHidden`,
	`view == bravo.modelChanges && bravo.hasReport && !config.bravo.treeView`,
	`view == bravo.modelChanges && bravo.hasReport && config.bravo.showHidden`,
	`view == bravo.modelChanges && bravo.hasReport && config.bravo.treeView`,
	`view == bravo.modelChanges && bravo.hasReport`,
	`view == bravo.modelChanges && viewItem == hidden`,
	`view == bravo.modelChanges`,
	`view == bravo.powerbi`,
	`view == breeze-debugger && viewItem == extensionPoint`,
	`view == breeze-debugger`,
	`view == breeze-explorer && viewItem == debuggerItem`,
	`view == breeze-explorer`,
	`view == brewExplorer && viewItem == serviceItem`,
	`view == brewExplorer`,
	`view == browser-console-view && browser-console.browserIsStarting`,
	`view == browser-console-view && viewItem == showLine`,
	`view == bsv.workspace && viewItem == component`,
	`view == bsv.workspace && viewItem == external`,
	`view == bsv.workspace && viewItem == file`,
	`view == bsv.workspace && viewItem == folder`,
	`view == bsv.workspace && viewItem == invocation`,
	`view == bsv.workspace && viewItem == recentInvocations`,
	`view == bsv.workspace && viewItem == server_pid`,
	`view == btcView.huobi`,
	`view == bucketExplorer && viewItem == elan:folder || view == bucketExplorer &&viewItem == elan:bucket`,
	`view == bucketExplorer && viewItem == elan:object`,
	`view == bucketExplorer`,
	`view == build-tasks-lists && viewItem == taskNode`,
	`view == build-tasks-lists`,
	`view == build_runner_view && viewItem == file`,
	`view == buildhistory && viewItem =~ /^projectWithStatus|^job/`,
	`view == buildhistory && viewItem =~ /^projectWithoutStatus/`,
	`view == buildhistory && viewItem =~ /job;.*abortDeployAllowed/`,
	`view == buildhistory && viewItem =~ /job;.*quickDeployAllowed/`,
	`view == buildhistory && viewItem =~ /projectWithStatus;.*ciAbortDeployAllowed/`,
	`view == buildhistory && viewItem =~ /projectWithStatus;.*ciQuickDeployAllowed/`,
	`view == buildhistory && viewItem =~ /projectWithStatus;.*ciTriggerBuidAllowed/`,
	`view == buildhistory`,
	`view == buildkite-builds`,
	`view == buildkite-pipelines`,
	`view == bxjTreeView`,
	`view == cSpellRegExpView && viewItem == regexp`,
	`view == cSpellRegExpView`,
	`view == camelk.integrations`,
	`view == cardListOnline`,
	`view == cardList`,
	`view == cartridgesView`,
	`view == cassandraWorkbenchView && viewItem != cluster && viewItem != cluster-error && viewItem!=types  && viewItem!=aggregates  && viewItem!=functions  && viewItem!=materialized-views && viewItem!=primarykey && viewItem!=columns  && viewItem!=indexes`,
	`view == cassandraWorkbenchView && viewItem == aggregate_item`,
	`view == cassandraWorkbenchView && viewItem == aggregates`,
	`view == cassandraWorkbenchView && viewItem == cluster`,
	`view == cassandraWorkbenchView && viewItem == column_item`,
	`view == cassandraWorkbenchView && viewItem == function_item`,
	`view == cassandraWorkbenchView && viewItem == functions`,
	`view == cassandraWorkbenchView && viewItem == index_item`,
	`view == cassandraWorkbenchView && viewItem == keyspace`,
	`view == cassandraWorkbenchView && viewItem == materialized-views_item`,
	`view == cassandraWorkbenchView && viewItem == table`,
	`view == cassandraWorkbenchView && viewItem == type_item`,
	`view == cassandraWorkbenchView && viewItem == types`,
	`view == cassandraWorkbenchView`,
	`view == catalogs.view || view == craftWorkshopSessions.view || view == craftTagReferenceBattles.view`,
	`view == catalogs.view`,
	`view == cblite.explorer && viewItem == cblite.collectionItem`,
	`view == cblite.explorer && viewItem == cblite.databaseItem`,
	`view == cblite.explorer && viewItem == cblite.documentItem`,
	`view == cblite.explorer && viewItem == cblite.keyItem`,
	`view == cblite.explorer && viewItem == cblite.valueItem`,
	`view == ccls.callHierarchy`,
	`view == ccls.inheritanceHierarchy`,
	`view == ccls.memberHierarchy`,
	`view == cdExplorer && !config.codedeploy.isApplicationWorkspace`,
	`view == cdExplorer && config.codedeploy.isApplicationWorkspace`,
	`view == cdExplorer && viewItem == application && config.codedeploy.isApplicationWorkspace`,
	`view == cdExplorer && viewItem == application`,
	`view == cdExplorer && viewItem == deploymentGroup`,
	`view == cdExplorer && viewItem == deployment`,
	`view == cdExplorer && viewItem =~ /application|deployment|autoScalingGroups|autoscaling_/`,
	`view == cdExplorer && viewItem =~ /application|deployment|loadBalancer|ec2TagFilters|ec2TagFilter_|autoScalingGroups|autoscaling_|elb_|tg_|elbInfo_|blueGreen|dgSettings/`,
	`view == cdExplorer && viewItem =~ /autoscaling_/`,
	`view == cdExplorer && viewItem =~ /elb_|tg_/`,
	`view == cdExplorer`,
	`view == cell-tag`,
	`view == cfgTree && viewItem == cfgAppItem`,
	`view == cfgTree && viewItem == xcDiag`,
	`view == cfgTree`,
	`view == cfm-activitybar-projects && resourceFilename =~ /.cfm/ || resourceFilename =~ /.cfc/ || resourceFilename =~ /.html/ `,
	`view == cfm-activitybar-projects && viewItem != File`,
	`view == cfm-activitybar-projects && viewItem != Project && viewItem != LinkedFolder`,
	`view == cfm-activitybar-projects && viewItem != Project`,
	`view == cfm-activitybar-projects && viewItem != others`,
	`view == cfm-activitybar-projects && viewItem == Folder || viewItem == Project || viewItem == LinkedFolder`,
	`view == cfm-activitybar-projects && viewItem == Folder || viewItem == cfm || viewItem == cfc  || viewItem == others || viewItem == Project || viewItem == LinkedFolder`,
	`view == cfm-activitybar-projects && viewItem == Folder || viewItem == cfm || viewItem == cfc || viewItem == others`,
	`view == cfm-activitybar-projects && viewItem == LinkedFolder`,
	`view == cfm-activitybar-projects && viewItem == Project`,
	`view == cfm-activitybar-projects && viewItem == cfm || viewItem == cfc`,
	`view == cfm-activitybar-projects`,
	`view == cfserver && viewItem == server-started  && viewItem != Other-server`,
	`view == cfserver && viewItem == server-started && viewItem != Other-server`,
	`view == cfserver && viewItem == server-started`,
	`view == cfserver && viewItem == server-stopped && viewItem != Other-server`,
	`view == cfserver && viewItem == server-stopped`,
	`view == cfserver`,
	`view == chat-friends || view == chat-groups || view == chat-pinned`,
	`view == chat-friends || view == chat-groups`,
	`view == chat-friends`,
	`view == chat-groups`,
	`view == chat-pinned`,
	`view == checkPointExplorer && viewItem == checkPointItem`,
	`view == checkPointExplorer`,
	`view == children_model_treeview && viewItem != source`,
	`view == children_model_treeview`,
	`view == cics-view && viewItem =~ /(?=^cicslocalfile)(?=.*closed)/`,
	`view == cics-view && viewItem =~ /(?=^cicslocalfile)(?=.*open)/`,
	`view == cics-view && viewItem =~ /^cicscombinedlibrarytree.*/`,
	`view == cics-view && viewItem =~ /^cicscombinedlocalfiletree.*/`,
	`view == cics-view && viewItem =~ /^cicscombinedpipelinetree.*/`,
	`view == cics-view && viewItem =~ /^cicscombinedprogramtree.*/`,
	`view == cics-view && viewItem =~ /^cicscombinedtasktree.*/`,
	`view == cics-view && viewItem =~ /^cicscombinedtcpipstree.*/`,
	`view == cics-view && viewItem =~ /^cicscombinedtransactiontree.*/`,
	`view == cics-view && viewItem =~ /^cicscombinedurimapstree.*/`,
	`view == cics-view && viewItem =~ /^cicscombinedwebservicetree.*/`,
	`view == cics-view && viewItem =~ /^cicsdatasets.*/`,
	`view == cics-view && viewItem =~ /^cicsdatasets.filtered.*/`,
	`view == cics-view && viewItem =~ /^cicslibrary.*/`,
	`view == cics-view && viewItem =~ /^cicslibrary.filtered.*/`,
	`view == cics-view && viewItem =~ /^cicslocalfile.*/`,
	`view == cics-view && viewItem =~ /^cicslocalfile.disabled.*/`,
	`view == cics-view && viewItem =~ /^cicslocalfile.enabled.*/`,
	`view == cics-view && viewItem =~ /^cicslocalfile.unenabled.*/`,
	`view == cics-view && viewItem =~ /^cicspipeline.*/`,
	`view == cics-view && viewItem =~ /^cicsprogram.*/`,
	`view == cics-view && viewItem =~ /^cicsprogram.disabled.*/`,
	`view == cics-view && viewItem =~ /^cicsprogram.enabled.*/`,
	`view == cics-view && viewItem =~ /^cicsregion\\./`,
	`view == cics-view && viewItem =~ /^cicsregionscontainer.*/`,
	`view == cics-view && viewItem =~ /^cicssession.*/`,
	`view == cics-view && viewItem =~ /^cicstask.*/`,
	`view == cics-view && viewItem =~ /^cicstcpips.*/`,
	`view == cics-view && viewItem =~ /^cicstransaction.*/`,
	`view == cics-view && viewItem =~ /^cicstransaction.disabled.*/`,
	`view == cics-view && viewItem =~ /^cicstransaction.enabled.*/`,
	`view == cics-view && viewItem =~ /^cicstreelibrary.*/`,
	`view == cics-view && viewItem =~ /^cicstreelibrary.filtered.*/`,
	`view == cics-view && viewItem =~ /^cicstreelocalfile.*/`,
	`view == cics-view && viewItem =~ /^cicstreelocalfile.filtered.*/`,
	`view == cics-view && viewItem =~ /^cicstreepipeline.*/`,
	`view == cics-view && viewItem =~ /^cicstreepipeline.filtered.*/`,
	`view == cics-view && viewItem =~ /^cicstreeprogram.*/`,
	`view == cics-view && viewItem =~ /^cicstreeprogram.filtered.*/`,
	`view == cics-view && viewItem =~ /^cicstreetask.*/`,
	`view == cics-view && viewItem =~ /^cicstreetask.filtered.*/`,
	`view == cics-view && viewItem =~ /^cicstreetcpips.*/`,
	`view == cics-view && viewItem =~ /^cicstreetcpips.filtered.*/`,
	`view == cics-view && viewItem =~ /^cicstreetransaction.*/`,
	`view == cics-view && viewItem =~ /^cicstreetransaction.filtered.*/`,
	`view == cics-view && viewItem =~ /^cicstreeurimaps.*/`,
	`view == cics-view && viewItem =~ /^cicstreeurimaps.filtered.*/`,
	`view == cics-view && viewItem =~ /^cicstreewebservice.*/`,
	`view == cics-view && viewItem =~ /^cicstreewebservice.filtered.*/`,
	`view == cics-view && viewItem =~ /^cicsurimaps.*/`,
	`view == cics-view && viewItem =~ /^cicswebservice.*/`,
	`view == cics-view`,
	`view == circleciPipelinesTree`,
	`view == circularImports && !config.findUnusedExports.detectCircularImports`,
	`view == circularImports && config.findUnusedExports.detectCircularImports`,
	`view == circularImports && viewItem != nothingFound && viewItem != circularImport`,
	`view == ciscoEDM && viewItem == createDataLogic`,
	`view == ciscoEDM && viewItem == renameOrDeleteDataLogic`,
	`view == ciscoEDM && viewItem == viewOrRenameDataModel`,
	`view == citrix.view.citrix-github-explorer`,
	`view == citrix.view.citrix-scripts && viewItem == scriptfile`,
	`view == citrix.view.citrix-scripts && viewItem == scriptpackage`,
	`view == citrix.view.citrix-scripts`,
	`view == cl.eide.view.operations`,
	`view == cl.eide.view.projects && cl.eide.isWorkspaceProject`,
	`view == cl.eide.view.projects`,
	`view == clangd.ast`,
	`view == clangd.memoryUsage`,
	`view == clangd.typeHierarchyView`,
	`view == clangtidychecks && viewItem == checksAll`,
	`view == clangtidychecks && viewItem == checksCategory`,
	`view == clangtidychecks && viewItem == checksIssue`,
	`view == clangtidydiagnostics && viewItem == diagnosticCategory`,
	`view == clangtidydiagnostics && viewItem == diagnostic`,
	`view == clangtidydiagnostics && viewItem == file`,
	`view == clangtidydiagnostics`,
	`view == clickupTasksView`,
	`view == clipboardHistory`,
	`view == clockify-clients`,
	`view == clockify-projects`,
	`view == clockify-tags`,
	`view == clockify-tasks`,
	`view == clockify-timeentries`,
	`view == clockify-workspaces`,
	`view == cloudcode.cloudRunProperties`,
	`view == cloudlatex-commands`,
	`view == cloudmusic-account`,
	`view == cloudmusic-local && viewItem == LocalFileTreeItem`,
	`view == cloudmusic-local && viewItem == LocalLibraryTreeItem`,
	`view == cloudmusic-local`,
	`view == cloudmusic-playlist && viewItem == PlaylistItemTreeItem`,
	`view == cloudmusic-playlist && viewItem == QueueItemTreeItem`,
	`view == cloudmusic-playlist && viewItem == UserTreeItem`,
	`view == cloudmusic-queue && viewItem == QueueItemTreeItem || view == cloudmusic-queue && viewItem == LocalFileTreeItem || view == cloudmusic-queue && viewItem == ProgramTreeItem`,
	`view == cloudmusic-queue`,
	`view == cloudmusic-radio && viewItem == ProgramTreeItem`,
	`view == cloudmusic-radio && viewItem == RadioTreeItem`,
	`view == cloudmusic-radio`,
	`view == cloudrail.issues && !cloudrail.show_passed_rules`,
	`view == cloudrail.issues && cloudrail.show_passed_rules`,
	`view == cloudrail.issues`,
	`view == clusters && viewItem == cluster`,
	`view == clusters-list && viewItem == mongo`,
	`view == clusters-list`,
	`view == cmake.outline \u0026\u0026 !cmake:isBuilding \u0026\u0026 cmake:enableFullFeatureSet`,
	`view == cmake.outline \u0026\u0026 !cmake:isBuilding`,
	`view == cmake.outline \u0026\u0026 cmake:enableFullFeatureSet \u0026\u0026 viewItem =~ /canBuild=true/`,
	`view == cmake.outline \u0026\u0026 cmake:enableFullFeatureSet \u0026\u0026 viewItem =~ /canBuild=true|canRun=true/`,
	`view == cmake.outline \u0026\u0026 cmake:enableFullFeatureSet \u0026\u0026 viewItem =~ /canRun=true/`,
	`view == cmake.outline \u0026\u0026 cmake:enableFullFeatureSet \u0026\u0026 viewItem =~ /canRun=true|canBuild=true/ \u0026\u0026 viewItem =~ /isDefault=false/`,
	`view == cmake.outline \u0026\u0026 cmake:enableFullFeatureSet \u0026\u0026 viewItem =~ /nodeType=target/`,
	`view == cmake.outline \u0026\u0026 cmake:enableFullFeatureSet \u0026\u0026 viewItem =~ /type=EXECUTABLE/ \u0026\u0026 viewItem =~ /isLaunch=false/`,
	`view == cmake.outline \u0026\u0026 cmake:enableFullFeatureSet \u0026\u0026 viewItem =~ /type=EXECUTABLE/`,
	`view == cmake.outline \u0026\u0026 cmake:enableFullFeatureSet`,
	`view == cmake.outline \u0026\u0026 cmake:isBuilding \u0026\u0026 cmake:enableFullFeatureSet`,
	`view == cmake.outline \u0026\u0026 viewItem =~ /nodeType=file/ \u0026\u0026 viewItem =~ /cmakelists=true/`,
	`view == cmake.outline`,
	`view == cmsis-csolution.context && config.cmsis-csolution.experimentalFeatures`,
	`view == cmsis-csolution.context`,
	`view == cnblogs-account`,
	`view == cnblogs-post-categories-list`,
	`view == cnblogs-posts-list || view == cnblogs-posts-list-another`,
	`view == cnc-show-filecontent && vscode-isg-cnc.sidebarSorting == 'grouped'`,
	`view == cnc-show-filecontent && vscode-isg-cnc.sidebarSorting == 'lineByLine'`,
	`view == cnc-show-filecontent`,
	`view == cncList&&viewItem == customFile`,
	`view == cncList&&viewItem == openFolder`,
	`view == cncList`,
	`view == cobiSymbolOutline`,
	`view == cobolMonitorView`,
	`view == cobolflowview`,
	`view == code-review.list && !isFilteredByCommit`,
	`view == code-review.list && !isFilteredByFilename`,
	`view == code-review.list && !isFilteredByPriority`,
	`view == code-review.list && isFilteredByCommit`,
	`view == code-review.list && isFilteredByFilename`,
	`view == code-review.list && isFilteredByPriority`,
	`view == code-review.list && viewItem == comment`,
	`view == code-review.list`,
	`view == codeAnnotationView && viewItem == $CompleteNote`,
	`view == codeAnnotationView && viewItem == $PendingNote || view == codeAnnotationView && viewItem == $CompleteNote`,
	`view == codeAnnotationView && viewItem == $PendingNote || viewItem == $CompleteNote`,
	`view == codeAnnotationView && viewItem == $PendingNote`,
	`view == codeAnnotationView && viewItem == $menu-done || view == codeAnnotationView && viewItem == $menu-pending`,
	`view == codeAnnotationView && viewItem == $menu-done`,
	`view == codeAnnotationView && viewItem == $menu-pending`,
	`view == codeAnnotationView`,
	`view == codeFragments`,
	`view == codeQLAstViewer`,
	`view == codeQLDatabases && viewItem != currentDatabase`,
	`view == codeQLDatabases`,
	`view == codeQLEvalLogViewer`,
	`view == codeQLQueryHistory && !hasRemoteServer`,
	`view == codeQLQueryHistory && viewItem == remoteResultsItem`,
	`view == codeQLQueryHistory`,
	`view == codeQLVariantAnalysisRepositories && codeQLVariantAnalysisRepositories.configError == false`,
	`view == codeQLVariantAnalysisRepositories && viewItem =~ /canBeOpenedOnGitHub/`,
	`view == codeQLVariantAnalysisRepositories && viewItem =~ /canBeRemoved/`,
	`view == codeQLVariantAnalysisRepositories && viewItem =~ /canBeRenamed/`,
	`view == codeQLVariantAnalysisRepositories && viewItem =~ /canBeSelected/ && false`,
	`view == codeQLVariantAnalysisRepositories && viewItem =~ /canBeSelected/`,
	`view == codeQLVariantAnalysisRepositories`,
	`view == codeblock.beSystemsExplorer && viewItem == beSystemInstance`,
	`view == codeblock.beSystemsExplorer`,
	`view == codeblockFileRevisions && viewItem == codeblockFileRevision`,
	`view == codeforces && viewItem == topCategory`,
	`view == codeforces`,
	`view == codemap-own-view || view == codemap-explorer-view`,
	`view == codemap-settings-own-view || view == codemap-settings-explorer-view`,
	`view == codepalContests && viewItem==FutureContest`,
	`view == codepalContests && viewItem==contest`,
	`view == codepalContests && viewItem==contestproblem`,
	`view == codepalContests`,
	`view == codepalProblems && viewItem==problem`,
	`view == codepalProblems`,
	`view == codesandbox-participants && viewItem == client`,
	`view == codesandbox-projects && viewItem == branch`,
	`view == codesandbox-projects`,
	`view == codeshareShares`,
	`view == codesight-extension-status || view == codesight-setup-status`,
	`view == codesight-extension-status`,
	`view == codesight-sast-issues-found && !codesight.sastScanRunning && codesight.scanMode == 'auto'`,
	`view == codesight-sast-issues-found && !codesight.sastScanRunning && codesight.scanMode == 'manual'`,
	`view == codesight-sast-issues-found && !codesight.sastScanRunning && codesight.scanMode == 'remote'`,
	`view == codesight-sast-issues-found && codesight.sastScanRunning && codesight.scanMode == 'manual'`,
	`view == codesight-sast-issues-found`,
	`view == codesight-sca-issues-found && !codesight.bdScanRunning && codesight.bdToolAvailable`,
	`view == codesight-sca-issues-found && codesight.bdScanRunning && codesight.bdToolAvailable`,
	`view == codeswing.activeSwing`,
	`view == codetour.tours && !codetour:showingMarkers`,
	`view == codetour.tours && codetour:showingMarkers`,
	`view == codetour.tours`,
	`view == codeui.views.elements`,
	`view == coffeebreak.views.coffeeBreak && todo-embedded-expanded == false`,
	`view == coffeebreak.views.coffeeBreak && todo-embedded-expanded == true`,
	`view == coffeebreak.views.coffeeBreak && todo-embedded-filtered == false`,
	`view == coffeebreak.views.coffeeBreak && todo-embedded-filtered == true`,
	`view == coffeebreak.views.coffeeBreak && todo-embedded-filtered-due == false`,
	`view == coffeebreak.views.coffeeBreak && todo-embedded-filtered-due == true`,
	`view == coffeebreak.views.coffeeBreak && todo-embedded-filtered-owner == false`,
	`view == coffeebreak.views.coffeeBreak && todo-embedded-filtered-owner == true`,
	`view == coffeebreak.views.coffeeBreak && todo-embedded-hide-linked == false`,
	`view == coffeebreak.views.coffeeBreak && todo-embedded-hide-linked == true`,
	`view == coffeebreak.views.coffeeBreak && viewItem == todo-linked`,
	`view == coffeebreak.views.followUp && viewItem == file`,
	`view == coinsListView`,
	`view == collections && viewItem == collection`,
	`view == collections && viewItem == test`,
	`view == collections`,
	`view == com.genuitec.codetogether.team.treeview && codetogether.state.loggedIn == true`,
	`view == commandbox && viewItem == boxJSON`,
	`view == commandbox && viewItem == script`,
	`view == commandbox`,
	`view == commands && prodServerIsRunning == true`,
	`view == commands && serverIsRunning == true`,
	`view == commands && siteBuilt`,
	`view == commands.tree && viewItem == folder`,
	`view == commands.tree && viewItem =~ /command|folder/`,
	`view == commands.tree && viewItem =~ /command|folder|stringCommand/`,
	`view == commands.tree && viewItem =~ /command|stringCommand/`,
	`view == commands.tree`,
	`view == commands`,
	`view == commitViewProvider && git.commit.selected`,
	`view == commitViewProvider && viewItem == file && false`,
	`view == commitViewProvider && viewItem == file && git.commit.FileEntry.selectForComparison && false`,
	`view == commitViewProvider && viewItem == file`,
	`view == commitViewProvider`,
	`view == committedFiles && hasGitRepo`,
	`view == committedFiles && viewItem != folder && hasGitRepo`,
	`view == committedFiles && viewItem == folder && hasGitRepo`,
	`view == compareCommitViewProvider && git.commit.compare.compared`,
	`view == compareCommitViewProvider && viewItem == file`,
	`view == compareCommitViewProvider`,
	`view == components && viewItem == component`,
	`view == componentsTreeView`,
	`view == components`,
	`view == composerScriptsView && viewItem == ComposerScriptsTreeScript`,
	`view == conan.configure`,
	`view == configurationInfo`,
	`view == conflicts && viewItem == conflict-actions`,
	`view == connected-orgs`,
	`view == connection && viewItem =~ /^connection$|^mysqlSchema$|^pgDatabase$|^pgSchema$|^sqlite3Connection$|^msDatabase$|^msSchema$/`,
	`view == connection && viewItem =~ /^connection$|^pgDatabase$|^sqlite3Connection$|^msDatabase$/`,
	`view == connection && viewItem =~ /^connection$|^sqlite3Connection$/`,
	`view == connection && viewItem =~ /^mysqlSchema$|^table$|^pgSchema$|^sqlite3Connection$|^msSchema$/`,
	`view == connectionBrowser && viewItem == server`,
	`view == connectionBrowser`,
	`view == connectionProvider && viewItem == viewConnectionsDb2Item`,
	`view == connectionProvider && viewItem == viewConnectionsDb2`,
	`view == connectionProvider && viewItem == viewConnectionsTuningItem`,
	`view == connectionProvider && viewItem == viewConnectionsTuning`,
	`view == connection`,
	`view == connections && viewItem == activeConnection`,
	`view == connections && viewItem == connection`,
	`view == connections && viewItem == manualConnection`,
	`view == connections && viewItem =~ /^.*(manualConnection)|(scannedConnection)/`,
	`view == connectionsTree && viewItem == connectionsTreeItemInactive`,
	`view == connectionsTree`,
	`view == connections`,
	`view == content`,
	`view == contests && viewItem == PastContest`,
	`view == contests && viewItem == Problem`,
	`view == contests && viewItem == RunningContest`,
	`view == contests`,
	`view == contractTemplates`,
	`view == contracts && viewItem == code`,
	`view == contracts && viewItem == contract || viewItem == code`,
	`view == contracts && viewItem == contract`,
	`view == contracts`,
	`view == contributions && viewItem == project`,
	`view == contributions && viewItem == reference`,
	`view == contributions`,
	`view == copilot.explorer && angular`,
	`view == copilot.explorer && nestjs`,
	`view == copilot.explorer && react`,
	`view == copilot.explorer && stencil`,
	`view == copilot.explorer && viewItem == angular:Component`,
	`view == copilot.explorer && viewItem == angular:NgModule`,
	`view == copilot.explorer && viewItem == angular:project`,
	`view == copilot.explorer && vue`,
	`view == copilot.explorer`,
	`view == coreMetrics`,
	`view == cortex-arm-learning.peripherals && viewItem == fieldWO`,
	`view == cortex-arm-learning.peripherals && viewItem == field`,
	`view == cortex-arm-learning.peripherals && viewItem == peripheral`,
	`view == cortex-arm-learning.peripherals && viewItem == registerRO`,
	`view == cortex-arm-learning.peripherals && viewItem == registerRW`,
	`view == cortex-arm-learning.peripherals && viewItem == registerWO`,
	`view == cortex-arm-learning.peripherals && viewItem == register`,
	`view == cortex-arm-learning.peripherals`,
	`view == cortex-arm-learning.registers && viewItem == field`,
	`view == cortex-arm-learning.registers && viewItem == register`,
	`view == cortex-arm-learning.stack && viewItem == field`,
	`view == cortex-arm-learning.stack && viewItem == stack`,
	`view == cortex-debug.peripherals && debugState == stopped`,
	`view == cortex-debug.peripherals && viewItem == fieldWO`,
	`view == cortex-debug.peripherals && viewItem == field`,
	`view == cortex-debug.peripherals && viewItem == peripheral.pinned`,
	`view == cortex-debug.peripherals && viewItem == peripheral`,
	`view == cortex-debug.peripherals && viewItem == registerRO`,
	`view == cortex-debug.peripherals && viewItem == registerRW`,
	`view == cortex-debug.peripherals && viewItem == registerWO`,
	`view == cortex-debug.peripherals && viewItem == register`,
	`view == cortex-debug.peripherals && viewItem =~ /peripheral.*/`,
	`view == cortex-debug.peripherals`,
	`view == cortex-debug.registers && debugState == stopped &&  cortex-debug:registerUseNaturalFormat`,
	`view == cortex-debug.registers && debugState == stopped && !cortex-debug:registerUseNaturalFormat`,
	`view == cortex-debug.registers && debugState == stopped`,
	`view == cortex-debug.registers && viewItem == field`,
	`view == cortex-debug.registers && viewItem == register`,
	`view == cortexBuilderProvider && viewItem == AddedTreeViewItem`,
	`view == cortexBuilderProvider && viewItem == RootTreeViewItem`,
	`view == cosExplorer && !vscode-cos.explorer.showSystem`,
	`view == cosExplorer && vscode-cos.explorer.showSystem`,
	`view == cosExplorer`,
	`view == countries-stats`,
	`view == coverReport && viewItem == lineCoverageReport`,
	`view == coverageView && viewItem == coverageTest`,
	`view == coverageView`,
	`view == cpp.ast`,
	`view == cpp.memoryUsage`,
	`view == cpp.typeHierarchyView`,
	`view == cppView && viewItem != cppView.Group && viewItem != cppView.Sub`,
	`view == cppView`,
	`view == cquery.callHierarchy`,
	`view == cquery.inheritanceHierarchy`,
	`view == craftos-computers && viewItem == data-available`,
	`view == craftos-computers || view == craftos-monitors`,
	`view == craftos-computers`,
	`view == cratesExplorer && viewItem != crate && viewItem != lib && viewItem != deps && viewItem != examples && viewItem != testFiles && viewItem != benchFiles`,
	`view == cratesExplorer && viewItem != crate && viewItem != lib && viewItem != deps && viewItem != examples`,
	`view == create && !mintlify.isLoggedIn`,
	`view == create && mintlify.isLoggedIn`,
	`view == credentials && viewItem =~ /.*Token/`,
	`view == credentials && viewItem =~ /refreshable.*/`,
	`view == credentials`,
	`view == cros-ide-status`,
	`view == crossnoteTreeView`,
	`view == cs-script`,
	`view == cs.cds.viewContainers.cdsExplorer && viewItem =~ /^(.*)(canAddItem)(.*)$/`,
	`view == cs.cds.viewContainers.cdsExplorer && viewItem =~ /^(.*)(canAddToSolution)(.*)$/`,
	`view == cs.cds.viewContainers.cdsExplorer && viewItem =~ /^(.*)(canCreateCrmSvcUtilConfig)(.*)$/`,
	`view == cs.cds.viewContainers.cdsExplorer && viewItem =~ /^(.*)(canDeleteItem)(.*)$/`,
	`view == cs.cds.viewContainers.cdsExplorer && viewItem =~ /^(.*)(canEditItem)(.*)$/`,
	`view == cs.cds.viewContainers.cdsExplorer && viewItem =~ /^(.*)(canExportSolution)(.*)$/`,
	`view == cs.cds.viewContainers.cdsExplorer && viewItem =~ /^(.*)(canInspectItem)(.*)$/`,
	`view == cs.cds.viewContainers.cdsExplorer && viewItem =~ /^(.*)(canMoveSolution)(.*)$/`,
	`view == cs.cds.viewContainers.cdsExplorer && viewItem =~ /^(.*)(canOpenInApp)(.*)$/`,
	`view == cs.cds.viewContainers.cdsExplorer && viewItem =~ /^(.*)(canOpenInBrowser)(.*)$/`,
	`view == cs.cds.viewContainers.cdsExplorer && viewItem =~ /^(.*)(canOpenInEditor)(.*)$/`,
	`view == cs.cds.viewContainers.cdsExplorer && viewItem =~ /^(.*)(canRefreshItem)(.*)$/`,
	`view == cs.cds.viewContainers.cdsExplorer && viewItem =~ /^(.*)(canRemoveFromSolution)(.*)$/`,
	`view == cs.cds.viewContainers.cdsExplorer && viewItem =~ /^(.*)(canUnpackSolution)(.*)$/`,
	`view == cs.cds.viewContainers.cdsExplorer`,
	`view == cs.cds.viewContainers.templateExplorer && viewItem =~ /^(.*)(canAddItem)(.*)$/`,
	`view == cs.cds.viewContainers.templateExplorer && viewItem =~ /^(.*)(canCreateInWorkspace)(.*)$/`,
	`view == cs.cds.viewContainers.templateExplorer && viewItem =~ /^(.*)(canDeleteItem)(.*)$/`,
	`view == cs.cds.viewContainers.templateExplorer && viewItem =~ /^(.*)(canEditItem)(.*)$/`,
	`view == cs.cds.viewContainers.templateExplorer && viewItem =~ /^(.*)(canExportItem)(.*)$/`,
	`view == cs.cds.viewContainers.templateExplorer && viewItem =~ /^(.*)(canImportItem)(.*)$/`,
	`view == cs.cds.viewContainers.templateExplorer && viewItem =~ /^(.*)(canOpenItem)(.*)$/`,
	`view == cs.cds.viewContainers.templateExplorer && viewItem =~ /^(.*)(canRefreshItem)(.*)$/`,
	`view == cs.cds.viewContainers.templateExplorer`,
	`view == csse3010-debug.peripherals && viewItem == fieldWO`,
	`view == csse3010-debug.peripherals && viewItem == field`,
	`view == csse3010-debug.peripherals && viewItem == peripheral.pinned`,
	`view == csse3010-debug.peripherals && viewItem == peripheral`,
	`view == csse3010-debug.peripherals && viewItem == registerRO`,
	`view == csse3010-debug.peripherals && viewItem == registerRW`,
	`view == csse3010-debug.peripherals && viewItem == registerWO`,
	`view == csse3010-debug.peripherals && viewItem == register`,
	`view == csse3010-debug.peripherals && viewItem =~ /peripheral.*/`,
	`view == csse3010-debug.peripherals`,
	`view == csse3010-debug.registers && viewItem == field`,
	`view == csse3010-debug.registers && viewItem == register`,
	`view == ctreeview`,
	`view == cucumberjs_features`,
	`view == cucumberjs_steps`,
	`view == currentIssues || view == searchIssues`,
	`view == currentIssues`,
	`view == customSearchContent`,
	`view == customView && viewItem == customViewItem`,
	`view == customView`,
	`view == cvs-file-branches && viewItem == branch`,
	`view == cvs-file-revisions && viewItem == revision`,
	`view == cxm-checker-quick-test`,
	`view == cxm-lang-issues-found && viewItem == issue`,
	`view == cxm-lang-issues-found`,
	`view == cxm-lang-status`,
	`view == cxportalwin && viewItem == scan_node`,
	`view == cxportalwin && viewItem == server_node`,
	`view == cxportalwin`,
	`view == cxscanswin && viewItem == query_node`,
	`view == cxscanswin && viewItem == scan_node`,
	`view == dagTreeView`,
	`view == dart-support-view && viewItem =~ /^file-.*/ && viewItem in dart-support.hasBuildRunnerDep`,
	`view == dart-support-view && viewItem =~ /^file-.*/`,
	`view == dart-support-view && viewItem in dart-support.hasBuildRunnerDep && viewItem in dart-support.notRunning`,
	`view == dart-support-view && viewItem in dart-support.notRunning`,
	`view == dart-support-view && viewItem in dart-support.running`,
	`view == dart-support-view`,
	`view == dartDependencyTree && viewItem == dart-code:depDependenciesNode`,
	`view == dartDependencyTree && viewItem == dart-code:depDevDependenciesNode`,
	`view == dartDependencyTree`,
	`view == dartFlutterOutline && dart-code:widgetSupports:refactor.flutter.move.down`,
	`view == dartFlutterOutline && dart-code:widgetSupports:refactor.flutter.move.up`,
	`view == dartFlutterOutline && dart-code:widgetSupports:refactor.flutter.removeWidget`,
	`view == dartFlutterOutline && dart-code:widgetSupports:refactor.flutter.wrap.center`,
	`view == dartFlutterOutline && dart-code:widgetSupports:refactor.flutter.wrap.column`,
	`view == dartFlutterOutline && dart-code:widgetSupports:refactor.flutter.wrap.padding`,
	`view == dartFlutterOutline && dart-code:widgetSupports:refactor.flutter.wrap.row`,
	`view == dartFlutterOutline && viewItem == dart-code:isSelectedWidget && dart-code:widgetSupports:refactor.flutter.move.down`,
	`view == dartFlutterOutline && viewItem == dart-code:isSelectedWidget && dart-code:widgetSupports:refactor.flutter.move.up`,
	`view == dartFlutterOutline && viewItem == dart-code:isSelectedWidget && dart-code:widgetSupports:refactor.flutter.removeWidget`,
	`view == dartFlutterOutline && viewItem == dart-code:isSelectedWidget && dart-code:widgetSupports:refactor.flutter.wrap.center`,
	`view == dartFlutterOutline && viewItem == dart-code:isSelectedWidget && dart-code:widgetSupports:refactor.flutter.wrap.column`,
	`view == dartFlutterOutline && viewItem == dart-code:isSelectedWidget && dart-code:widgetSupports:refactor.flutter.wrap.padding`,
	`view == dartFlutterOutline && viewItem == dart-code:isSelectedWidget && dart-code:widgetSupports:refactor.flutter.wrap.row`,
	`view == dashboard && viewItem == noDefinition`,
	`view == dashboard`,
	`view == database.connections`,
	`view == databaseExplorer && viewItem == table`,
	`view == databaseExplorer`,
	`view == databases-list && viewItem == connection`,
	`view == databases-list && viewItem == databases`,
	`view == databases-list && viewItem == detail`,
	`view == databases-list`,
	`view == databricksClusters && viewItem =~ /.*,MANUAL,.*/`,
	`view == databricksClusters && viewItem =~ /.*,NOT_PINNED,.*/ && viewItem =~ /.*,MANUAL,.*/`,
	`view == databricksClusters && viewItem =~ /.*,PINNED,.*/ && viewItem =~ /.*,MANUAL,.*/`,
	`view == databricksClusters && viewItem =~ /.*,SHOW_DEFINITION,.*/`,
	`view == databricksClusters && viewItem =~ /.*,STARTED,.*/ && viewItem =~ /.*,KERNEL,.*/`,
	`view == databricksClusters && viewItem =~ /.*,STARTED,.*/ && viewItem =~ /.*,MANUAL,.*/`,
	`view == databricksClusters && viewItem =~ /.*,STARTED,.*/ && viewItem =~ /.*,NOKERNEL,.*/ && viewItem =~ /.*,MANUAL,.*/`,
	`view == databricksClusters && viewItem =~ /.*,STOPPED,.*/ && viewItem =~ /.*,MANUAL,.*/`,
	`view == databricksClusters`,
	`view == databricksFS &&  viewItem =~ /.*,DELETE,.*/`,
	`view == databricksFS && viewItem =~ /.*,ADDDIRECTORY,.*/`,
	`view == databricksFS && viewItem =~ /.*,ADDFILE,.*/`,
	`view == databricksFS && viewItem =~ /.*,DOWNLOAD,.*/`,
	`view == databricksFS && viewItem =~ /.*,UPLOAD,.*/`,
	`view == databricksFS`,
	`view == databricksJobs  && viewItem == JOB`,
	`view == databricksJobs  && viewItem == RUNNING_JOB`,
	`view == databricksJobs && viewItem == JOB || viewItem == JOB_RUN || viewItem == RUNNING_JOB`,
	`view == databricksJobs`,
	`view == databricksRepos && viewItem == REPOSITORY`,
	`view == databricksRepos`,
	`view == databricksSQL && viewItem == DATABASE`,
	`view == databricksSQL && viewItem == TABLE`,
	`view == databricksSQL`,
	`view == databricksSecrets && viewItem == SCOPE_DATABRICKS`,
	`view == databricksSecrets && viewItem == SECRET_DATABRICKS`,
	`view == databricksSecrets && viewItem =~ /^SECRET_.*/`,
	`view == databricksSecrets`,
	`view == databricksWorkspace && viewItem =~ /.*,COMPARE,.*/`,
	`view == databricksWorkspace && viewItem =~ /.*,DOWNLOAD,.*/`,
	`view == databricksWorkspace && viewItem =~ /.*,OPENEXPLORER,.*/`,
	`view == databricksWorkspace && viewItem =~ /.*,UPLOAD,.*/`,
	`view == databricksWorkspace`,
	`view == datalakeExplorer && viewItem == assemblyFolder`,
	`view == datalakeExplorer && viewItem == assembly`,
	`view == datalakeExplorer && viewItem == azureBlobContainer`,
	`view == datalakeExplorer && viewItem == azureBlobFolder`,
	`view == datalakeExplorer && viewItem == azureBlob`,
	`view == datalakeExplorer && viewItem == azureSubscription`,
	`view == datalakeExplorer && viewItem == creatableFolder`,
	`view == datalakeExplorer && viewItem == deletableItem`,
	`view == datalakeExplorer && viewItem == directory`,
	`view == datalakeExplorer && viewItem == file`,
	`view == datalakeExplorer && viewItem == folder`,
	`view == datalakeExplorer && viewItem == storageAccount`,
	`view == datalakeExplorer`,
	`view == datapackExplorer`,
	`view == datavirt`,
	`view == dataworkspace.views.main && azdataAvailable`,
	`view == dataworkspace.views.main && config.workbench.enablePreviewFeatures || config.sqlDatabaseProjects.enablePreviewFeatures`,
	`view == dataworkspace.views.main && viewItem == databaseProject.itemType.file.externalStreamingJob`,
	`view == dataworkspace.views.main && viewItem == databaseProject.itemType.folder || viewItem =~ /^databaseProject.itemType.file/ || viewItem == databaseProject.itemType.reference`,
	`view == dataworkspace.views.main && viewItem == databaseProject.itemType.folder || viewItem =~ /^databaseProject.itemType.file/`,
	`view == dataworkspace.views.main && viewItem == databaseProject.itemType.referencesRoot`,
	`view == dataworkspace.views.main && viewItem =~ /^(databaseProject.itemType.project|databaseProject.itemType.legacyProject)$/ && azdataAvailable`,
	`view == dataworkspace.views.main && viewItem =~ /^(databaseProject.itemType.project|databaseProject.itemType.legacyProject)$/ || viewItem == databaseProject.itemType.folder`,
	`view == dataworkspace.views.main && viewItem =~ /^(databaseProject.itemType.project|databaseProject.itemType.legacyProject)$/`,
	`view == dataworkspace.views.main && workbenchState == workspace`,
	`view == dataworkspace.views.main`,
	`view == dayone-pages && viewItem != dayonePageDir && viewItem != dayonePageFile`,
	`view == dayone-pages && viewItem != dayonePageFile`,
	`view == dayone-pages && viewItem == dayonePageDir`,
	`view == dayone-pages && viewItem == dayonePageFile`,
	`view == dayone-pages && viewItem == dayonePageRootDisable`,
	`view == dayone-pages && viewItem == dayonePageRootEnable`,
	`view == dayone-pages`,
	`view == db2connect && viewItem == db2connect.tree.aliasfolder`,
	`view == db2connect && viewItem == db2connect.tree.connection`,
	`view == db2connect && viewItem == db2connect.tree.database`,
	`view == db2connect && viewItem == db2connect.tree.function`,
	`view == db2connect && viewItem == db2connect.tree.functionfolder`,
	`view == db2connect && viewItem == db2connect.tree.indexfolder`,
	`view == db2connect && viewItem == db2connect.tree.schema || view == db2connect && viewItem == db2connect.tree.table || view == db2connect && viewItem == db2connect.tree.view || view == db2connect && viewItem == db2connect.tree.alias || view == db2connect && viewItem == db2connect.tree.trigger || view == db2connect && viewItem == db2connect.tree.function || view == db2connect && viewItem == db2connect.tree.storedprocedure || view == db2connect && viewItem == db2connect.tree.index`,
	`view == db2connect && viewItem == db2connect.tree.schema`,
	`view == db2connect && viewItem == db2connect.tree.spfolder`,
	`view == db2connect && viewItem == db2connect.tree.storedprocedure`,
	`view == db2connect && viewItem == db2connect.tree.table || view == db2connect && viewItem == db2connect.tree.view || view == db2connect && viewItem == db2connect.tree.alias`,
	`view == db2connect && viewItem == db2connect.tree.table`,
	`view == db2connect && viewItem == db2connect.tree.tablefolder`,
	`view == db2connect && viewItem == db2connect.tree.trigger`,
	`view == db2connect && viewItem == db2connect.tree.triggerfolder`,
	`view == db2connect && viewItem == db2connect.tree.view`,
	`view == db2connect && viewItem == db2connect.tree.viewfolder`,
	`view == db2connect`,
	`view == dbuxApplicationsView && !dbux.context.runtimeServerStarted`,
	`view == dbuxApplicationsView && !dbuxWebView.context.dbux-pathways.isActive`,
	`view == dbuxApplicationsView && dbux.context.runtimeServerStarted`,
	`view == dbuxApplicationsView && dbuxWebView.context.dbux-pathways.isActive`,
	`view == dbuxApplicationsView && viewItem == ApplicationNode`,
	`view == dbuxApplicationsView`,
	`view == dbuxCallStackView && viewItem == callStackNode.both.parent`,
	`view == dbuxCallStackView && viewItem == callStackNode.both.scheduler`,
	`view == dbuxDataFlowView && dbuxDataFlowView.context.filterModeName == None`,
	`view == dbuxDataFlowView && dbuxDataFlowView.context.filterModeName == ReadOnly`,
	`view == dbuxDataFlowView && dbuxDataFlowView.context.filterModeName == WriteOnly`,
	`view == dbuxDataFlowView && dbuxDataFlowView.context.searchModeName == ByAccessId`,
	`view == dbuxDataFlowView && dbuxDataFlowView.context.searchModeName == ByValueId`,
	`view == dbuxGlobalAnalysisView && !dbux.context.hasError`,
	`view == dbuxGlobalAnalysisView && dbux.context.hasError`,
	`view == dbuxProjectView || view == dbuxSessionView`,
	`view == dbuxProjectView`,
	`view == dbuxTraceDetailsView && !dbux.context.hasError`,
	`view == dbuxTraceDetailsView && !dbuxTraceDetailsView.context.hasTracesAtCursor`,
	`view == dbuxTraceDetailsView && dbux.context.hasError`,
	`view == dbuxTraceDetailsView && dbuxTraceDetailsView.context.editedWarning`,
	`view == dbuxTraceDetailsView && dbuxTraceDetailsView.context.hasTracesAtCursor`,
	`view == dc2definitions && listMultiSelection && viewItem == definition`,
	`view == dc2definitions && viewItem != definition`,
	`view == dc2definitions && viewItem == definition && !listMultiSelection`,
	`view == dc2definitions`,
	`view == debugInstances`,
	`view == debugger-compiled && juliaCompiledMode == false`,
	`view == debugger-compiled && juliaCompiledMode == true`,
	`view == debugger-compiled && viewItem == isRootCompiled`,
	`view == debugger-compiled && viewItem =~ /\\bcompiled\\b/`,
	`view == debugger-compiled && viewItem =~ /\\binterpreted\\b/ && viewItem =~ /\\bhasChildren\\b/`,
	`view == debugger-compiled && viewItem =~ /\\binterpreted\\b/`,
	`view == debugger-compiled`,
	`view == debugger-node-lists && viewItem == cudaNode`,
	`view == debugger-node-lists && viewItem == hpcNodeChild`,
	`view == debugger-node-lists && viewItem == hpcNode`,
	`view == debugger-node-lists`,
	`view == decentraland && decentraland.isDCL`,
	`view == dendron.backlinks && dendron:backlinksSortOrder == LastUpdated`,
	`view == dendron.backlinks && dendron:backlinksSortOrder == PathNames`,
	`view == dendron.backlinks`,
	`view == dendron.graph-panel && !dendron.graph-panel.showBacklinks`,
	`view == dendron.graph-panel && !dendron.graph-panel.showHierarchy`,
	`view == dendron.graph-panel && !dendron.graph-panel.showOutwardLinks`,
	`view == dendron.graph-panel && dendron.graph-panel.showBacklinks`,
	`view == dendron.graph-panel && dendron.graph-panel.showHierarchy`,
	`view == dendron.graph-panel && dendron.graph-panel.showOutwardLinks`,
	`view == dendron.graph-panel`,
	`view == dendron.treeView && dendron:devMode`,
	`view == dendron.treeView && dendron:treeviewItemLabelType == filename`,
	`view == dendron.treeView && dendron:treeviewItemLabelType == title`,
	`view == dendron.treeView && shellExecutionSupported`,
	`view == dendron.treeView && viewItem == note && shellExecutionSupported`,
	`view == dendron.treeView && viewItem == stub && shellExecutionSupported`,
	`view == dendron.treeView`,
	`view == deno`,
	`view == dependencies && viewItem == dependency`,
	`view == dependenciesManager && viewItem =~ /addGroup/ && DevOps.isXSpace3`,
	`view == dependenciesManager && viewItem =~ /deleteSingle/ && DevOps.isXSpace3`,
	`view == dependenciesManager && viewItem =~ /updateGroup/ && DevOps.isXSpace3`,
	`view == dependenciesManager && viewItem =~ /updateSingle/ && DevOps.isXSpace3`,
	`view == dependenciesManager`,
	`view == dependencies`,
	`view == dependencyManager && viewItem == gitRemote`,
	`view == dependencyManager && viewItem == localPath`,
	`view == dependencyManager`,
	`view == dependencyRelatedContent`,
	`view == dependencygraphExplorer-DependencyTree`,
	`view == deploy-gcc-task`,
	`view == deployments && viewItem == deployment`,
	`view == deployments`,
	`view == detailsContainers && remoteName == dev-container`,
	`view == detailsContainers && remoteName =~ /^(dev|attached)-container$/`,
	`view == detailsContainers && viewItem == devContainer`,
	`view == detailsContainers && viewItem =~ /^(image|volume)$/`,
	`view == detailsContainers`,
	`view == dev-tools-wl-3`,
	`view == devVolumes && viewItem =~ /^(devVolume)$/`,
	`view == devVolumes && viewItem =~ /^(devVolume|uniqueDevVolume)$/`,
	`view == devVolumes`,
	`view == developerLogsView`,
	`view == device-manager.devices`,
	`view == devices && viewItem == category-leased`,
	`view == devices && viewItem == category-owned`,
	`view == devices && viewItem == device-leased`,
	`view == devices && viewItem == device-owned`,
	`view == devices && viewItem =~ /^device-/ && config.cros-ide.underDevelopment.systemLogViewer`,
	`view == devices && viewItem =~ /^device-/`,
	`view == devices`,
	`view == didact.tutorials && viewItem == HeadingNode`,
	`view == didact.tutorials && viewItem == TutorialNode`,
	`view == didact.tutorials`,
	`view == digitaljs-proj-files && viewItem != root-circuit`,
	`view == digitaljs-proj-files && viewItem in digitaljs.script_not_running`,
	`view == digitaljs-proj-files && viewItem in digitaljs.script_running`,
	`view == digitaljs-proj-files`,
	`view == dimensionscm.RequestContainer`,
	`view == dimensionscm.RequestTree && viewItem == defaultRequest`,
	`view == dimensionscm.RequestTree && viewItem == request`,
	`view == dimensionscm.RequestTree`,
	`view == dimensionscm.ReviewContainer && dimensionscm.DimensionsContainer.DefineConnection == false`,
	`view == dimensionscm.ReviewContainer && viewItem == pullRequest`,
	`view == dimensionscm.ReviewContainer && viewItem == review`,
	`view == dimensionscm.ReviewContainer`,
	`view == discloud-apps`,
	`view == discovery-server-tree && viewItem == discovery-server`,
	`view == discovery-server-tree`,
	`view == divinity.storyOutline && viewItem != sharedGoal`,
	`view == divinity.storyOutline && viewItem == sharedGoal`,
	`view == divinity.storyOutline`,
	`view == dllc_cluster_one`,
	`view == dllc_cluster_service && viewItem == checked`,
	`view == dllc_cluster_service`,
	`view == dllc_cluster_terminals && viewItem == checked`,
	`view == dllc_cluster_terminals`,
	`view == dltLifecycleExplorer && viewItem =~ /^filterReport/`,
	`view == dltLifecycleExplorer && viewItem =~ /canSave/`,
	`view == dltLifecycleExplorer && viewItem =~ /canSetPosF/`,
	`view == dltLifecycleExplorer && viewItem =~ /canZoomIn/`,
	`view == dltLifecycleExplorer && viewItem =~ /canZoomOut/`,
	`view == dltLifecycleExplorer && viewItem =~ /filterAllowEdit/`,
	`view == dltLifecycleExplorer && viewItem =~ /filterDisabled/`,
	`view == dltLifecycleExplorer && viewItem =~ /filterEnabled/`,
	`view == dmbinder.components`,
	`view == dmbinder.generators`,
	`view == dmbinder.sources`,
	`view == dmbinder.templates`,
	`view == dmbinder`,
	`view == dockerCompose && viewItem == docker-compose:exited-container`,
	`view == dockerCompose && viewItem == docker-compose:project`,
	`view == dockerCompose && viewItem == docker-compose:running-container`,
	`view == dockerCompose && viewItem == docker-compose:service`,
	`view == dockerCompose`,
	`view == dockerContainers && viewItem == containerFile`,
	`view == dockerContainers && viewItem =~ /^(created|dead|exited|paused|terminated)Container$/i`,
	`view == dockerContainers && viewItem =~ /^(paused|restarting|running)Container$/i`,
	`view == dockerContainers && viewItem =~ /^runningContainer$/i && vscode-docker:aciContext != true`,
	`view == dockerContainers && viewItem =~ /^runningContainer$/i`,
	`view == dockerContainers && viewItem =~ /composeGroup$/i`,
	`view == dockerContainers && viewItem =~ /container$/i`,
	`view == dockerContainers && viewItem =~ /runningContainer$/i`,
	`view == dockerContainers || view == dockerImages || view == dockerVolumes || view == dockerNetworks`,
	`view == dockerContainers`,
	`view == dockerExplorerContainers`,
	`view == dockerExplorerImages`,
	`view == dockerImages && !vscode-docker:danglingShown`,
	`view == dockerImages && viewItem == image`,
	`view == dockerImages && vscode-docker:danglingShown`,
	`view == dockerImages`,
	`view == dockerNetworks && viewItem =~ /^customNetwork$/i`,
	`view == dockerNetworks && viewItem =~ /network$/i`,
	`view == dockerNetworks`,
	`view == dockerRegistries && viewItem == azure;DockerV2;Registry;`,
	`view == dockerRegistries && viewItem == azure;DockerV2;RegistryProvider;`,
	`view == dockerRegistries && viewItem == azure;DockerV2;Repository;`,
	`view == dockerRegistries && viewItem == azure;DockerV2;Tag;`,
	`view == dockerRegistries && viewItem == azureTaskRun`,
	`view == dockerRegistries && viewItem == azureTask`,
	`view == dockerRegistries && viewItem == azureextensionui.azureSubscription`,
	`view == dockerRegistries && viewItem == connectedRegistries`,
	`view == dockerRegistries && viewItem == invalidRegistryProvider`,
	`view == dockerRegistries && viewItem == registryConnectError`,
	`view == dockerRegistries && viewItem =~ /(DockerV2|DockerHubV2);Tag;/ && isAzureAccountInstalled`,
	`view == dockerRegistries && viewItem =~ /(DockerV2|DockerHubV2);Tag;/`,
	`view == dockerRegistries && viewItem =~ /.*;.*;(Repository|Registry|RegistryProvider);/`,
	`view == dockerRegistries && viewItem =~ /DockerV2;Tag;/`,
	`view == dockerRegistries && viewItem =~ /RegistryProvider;/`,
	`view == dockerRegistries && viewItem =~ /Repository;/`,
	`view == dockerRegistries && viewItem =~ /Tag;/`,
	`view == dockerRegistries && viewItem =~ /azure(Subscription|;DockerV2;Registry;)/`,
	`view == dockerRegistries && viewItem =~ /azure(Subscription|Tasks|Task|RunsWithoutTask)$/`,
	`view == dockerRegistries && viewItem =~ /azure(TaskRun|;DockerV2;Registry;)/`,
	`view == dockerRegistries && viewItem =~ /dockerHub;DockerHubV2;(Tag|Repository|Registry);/`,
	`view == dockerRegistries`,
	`view == dockerRegistryExplorer && viewItem == layerNode`,
	`view == dockerRegistryExplorer && viewItem == registryNode`,
	`view == dockerRegistryExplorer && viewItem == repositoryNode`,
	`view == dockerRegistryExplorer && viewItem == tagNode`,
	`view == dockerRegistryExplorer`,
	`view == dockerVolumes && viewItem == volume && vscode-docker:aciContext != true`,
	`view == dockerVolumes && viewItem == volume`,
	`view == dockerVolumes`,
	`view == docker`,
	`view == dockerfiletemplate && viewItem == templateWignore || viewItem == template`,
	`view == dockerfiletemplate && viewItem == templateWignore`,
	`view == docs && !docs.isSignedIn`,
	`view == docs && docs.isSignedIn`,
	`view == docsView.documentation && !docsView.documentationView.isPinned`,
	`view == docsView.documentation && docsView.documentationView.isPinned`,
	`view == documents && viewItem == document`,
	`view == dodona-activities && viewItem == activity-contentpage`,
	`view == dodona-activities && viewItem == activity-exercise`,
	`view == dodona-activities && viewItem == item-course`,
	`view == dodona-activities && viewItem == item-series`,
	`view == dodona-activities`,
	`view == dolphindb.dataview`,
	`view == dolphindb.explorer && viewItem == 'connected'`,
	`view == dolphindb.explorer && viewItem == 'var'`,
	`view == domainExplorer && viewItem == emptyNotes`,
	`view == domainExplorer`,
	`view == domain`,
	`view == dorajs.addonExplorer`,
	`view == dota2CssApiExplorer && viewItem == Class`,
	`view == dota2CssApiExplorer`,
	`view == dota2JSApiExplorer && viewItem == Class`,
	`view == dota2JSApiExplorer && viewItem == NodeItem`,
	`view == dota2JSApiExplorer`,
	`view == dota2PanelExplorer`,
	`view == dota2apiExplorer && viewItem == Class`,
	`view == dota2apiExplorer && viewItem == NodeItem`,
	`view == dota2apiExplorer`,
	`view == dothttpEnvView && viewItem == enabledEnvironment`,
	`view == dothttpEnvView && viewItem == environment`,
	`view == dothttpEnvView && viewItem == property`,
	`view == dothttpEnvView`,
	`view == dothttpHistory`,
	`view == dothttpPropView && viewItem == disabled`,
	`view == dothttpPropView && viewItem == enabled`,
	`view == dothttpPropView`,
	`view == dotnetInsights && viewItem == dependency`,
	`view == dotnetInsightsJit && viewItem == jitRootContext`,
	`view == dotnetSolutionExplorer`,
	`view == dotnetTestExplorer`,
	`view == dreammaker-objtree && viewItem == symbol`,
	`view == driveView`,
	`view == droneBuildFeed`,
	`view == dropletList && viewItem == DropletContentItem`,
	`view == dropletList`,
	`view == dslManagement`,
	`view == dssPlugins`,
	`view == dssProjects`,
	`view == dtsPanel && !noResourceFiles`,
	`view == dtsPanel`,
	`view == dubDependencies && viewItem == root`,
	`view == dubDependencies`,
	`view == duffle.bundleExplorer && viewItem == duffle.localBundle`,
	`view == duffle.bundleExplorer`,
	`view == duffle.credentialExplorer && viewItem = duffle.credentialset`,
	`view == duffle.credentialExplorer && viewItem == duffle.credentialset`,
	`view == duffle.credentialExplorer`,
	`view == duffle.installationExplorer && viewItem = duffle.installation`,
	`view == duffle.installationExplorer && viewItem == duffle.installation`,
	`view == duffle.installationExplorer`,
	`view == duffle.repoExplorer && viewItem == duffle.repoBundle`,
	`view == duffle.repoExplorer`,
	`view == dupfinder`,
	`view == duplicatedCode`,
	`view == durableFunctionsMonitorTreeView && viewItem == connectionStrings`,
	`view == durableFunctionsMonitorTreeView && viewItem == storageAccount-attached || viewItem == storedStorageAccount-attached`,
	`view == durableFunctionsMonitorTreeView && viewItem == storedStorageAccount-attached || viewItem == storedStorageAccount-detached`,
	`view == durableFunctionsMonitorTreeView && viewItem == taskHub-attached || viewItem == taskHub-detached`,
	`view == durableFunctionsMonitorTreeView && viewItem == taskHub-attached`,
	`view == durableFunctionsMonitorTreeView && viewItem == taskHub-detached`,
	`view == durableFunctionsMonitorTreeView`,
	`view == dvConnections && viewItem == connection`,
	`view == dvConnections`,
	`view == dvEntities && !devtools.entitiesFiltered`,
	`view == dvEntities && !devtools.entitiesSearched`,
	`view == dvEntities && devtools.entitiesFiltered`,
	`view == dvEntities && devtools.entitiesSearched`,
	`view == dvEntities && viewItem == entitymedata`,
	`view == dvEntities`,
	`view == dvWebResources && !devtools.wrFiltered`,
	`view == dvWebResources && !devtools.wrSearched`,
	`view == dvWebResources && devtools.wrFiltered`,
	`view == dvWebResources && devtools.wrSearched`,
	`view == dvWebResources`,
	`view == dvc.views.experimentsColumnsTree`,
	`view == dvc.views.experimentsFilterByTree && dvc.commands.available && viewItem != dvcRoot`,
	`view == dvc.views.experimentsFilterByTree && dvc.experiments.filtered`,
	`view == dvc.views.experimentsFilterByTree`,
	`view == dvc.views.experimentsSortByTree && dvc.commands.available && viewItem != dvcRoot`,
	`view == dvc.views.experimentsSortByTree && dvc.experiments.sorted`,
	`view == dvc.views.experimentsSortByTree`,
	`view == dvc.views.experimentsTree && !dvc.experiment.running && !dvc.experiment.checkpoints`,
	`view == dvc.views.experimentsTree && !dvc.experiment.running && dvc.experiment.checkpoints`,
	`view == dvc.views.experimentsTree && !dvc.experiment.running`,
	`view == dvc.views.experimentsTree && !dvc.experiments.filter.selected`,
	`view == dvc.views.experimentsTree && !dvc.experiments.webview.active && !dvc.experiment.running && !dvc.experiment.checkpoints`,
	`view == dvc.views.experimentsTree && !dvc.experiments.webview.active && !dvc.experiment.running && dvc.experiment.checkpoints`,
	`view == dvc.views.experimentsTree && !dvc.experiments.webview.active && dvc.experiment.stoppable`,
	`view == dvc.views.experimentsTree && !dvc.experiments.webview.active`,
	`view == dvc.views.experimentsTree && !dvc.plots.webview.active`,
	`view == dvc.views.experimentsTree && dvc.commands.available && viewItem =~ /^(checkpoint|experiment)$/ && !dvc.experiment.running`,
	`view == dvc.views.experimentsTree && dvc.commands.available && viewItem =~ /^(experiment|queued)$/ && !dvc.experiment.running`,
	`view == dvc.views.experimentsTree && dvc.commands.available && viewItem =~ /^(workspace|commit|experiment|queued)$/ && !dvc.experiment.running && !dvc.experiment.checkpoints`,
	`view == dvc.views.experimentsTree && dvc.commands.available && viewItem =~ /^(workspace|commit|experiment|queued)$/ && !dvc.experiment.running && dvc.experiment.checkpoints`,
	`view == dvc.views.experimentsTree && dvc.commands.available && viewItem =~ /^(workspace|commit|experiment|queued)$/ && !dvc.experiment.running`,
	`view == dvc.views.experimentsTree && dvc.experiments.filter.selected`,
	`view == dvc.views.experimentsTree && dvc.experiments.webview.active && !dvc.experiment.running && !dvc.experiment.checkpoints`,
	`view == dvc.views.experimentsTree && dvc.experiments.webview.active && !dvc.experiment.running && dvc.experiment.checkpoints`,
	`view == dvc.views.experimentsTree && dvc.experiments.webview.active && dvc.experiment.stoppable`,
	`view == dvc.views.experimentsTree && dvc.plots.webview.active`,
	`view == dvc.views.experimentsTree`,
	`view == dvc.views.plotsPathsTree`,
	`view == dvc.views.trackedExplorerTree && !dvc.scm.command.running`,
	`view == dvc.views.trackedExplorerTree && viewItem != virtual && !dvc.scm.command.running`,
	`view == dvc.views.trackedExplorerTree && viewItem != virtual`,
	`view == dvc.views.trackedExplorerTree && viewItem =~ /^.*Data$/ && !dvc.scm.command.running`,
	`view == dvc.views.trackedExplorerTree && viewItem =~ /^.*Data$/`,
	`view == dvc.views.trackedExplorerTree && viewItem =~ /^dir/`,
	`view == dvc.views.trackedExplorerTree && viewItem =~ /^dirData$/ && !dvc.scm.command.running`,
	`view == dvc.views.trackedExplorerTree && viewItem =~ /^file/ && resourceSelectedForCompare`,
	`view == dvc.views.trackedExplorerTree && viewItem =~ /^file/`,
	`view == dvc.views.trackedExplorerTree`,
	`view == dvt.Diagnostics && !dvt.isThreadDumpCollecting && dvt.isCalledFromVscode`,
	`view == dvt.Diagnostics && dvt.isThreadDumpCollecting && dvt.isCalledFromVscode`,
	`view == dvt.Diagnostics`,
	`view == dwControllersView`,
	`view == dwLogsView`,
	`view == dynamoExplorer && viewItem == dynamoTable`,
	`view == dynamoExplorer`,
	`view == e4e.treeView`,
	`view == ecl.watch.lite && ecl.watch.lite.hasWuid`,
	`view == effectiveMetadata`,
	`view == eggTest`,
	`view == electron-build-tools:configs && viewItem =~ /^active-config$|^config$/`,
	`view == electron-build-tools:configs`,
	`view == electron-build-tools:docs`,
	`view == electron-build-tools:patches && viewItem == patch`,
	`view == electron-build-tools:patches && viewItem == pull-request`,
	`view == electron-build-tools:patches && viewItem == repo`,
	`view == electron-build-tools:patches && viewItem == view-pull-request-patch`,
	`view == electron-build-tools:patches`,
	`view == elegance_list && elegance_mysql.compare_schema_selected && viewItem == schema && false`,
	`view == elegance_list && elegance_mysql.compare_table_selected && viewItem == table`,
	`view == elegance_list && viewItem == schema`,
	`view == elegance_list && viewItem == table`,
	`view == elegance_list && viewItem in elegance_mysql.compareTo.supportedItem`,
	`view == elegance_list`,
	`view == elementHistory && viewItem ==  element.element`,
	`view == elementHistory && viewItem == element.element`,
	`view == elementHistory`,
	`view == elpi.tracer`,
	`view == embeddedReports`,
	`view == encounter-plus-modules && viewItem == moduleProject`,
	`view == encounter-plus-modules`,
	`view == endpoints`,
	`view == entityExplorer && viewItem == entity`,
	`view == entityExplorer`,
	`view == envManager`,
	`view == envsView && viewItem == 'env-item'`,
	`view == envsView && viewItem == 'env-item-hidden'`,
	`view == envsView`,
	`view == eplScenarioTreeData && viewItem == eplscenario`,
	`view == eplScenarioTreeData`,
	`view == erlang-project-outline && viewItem == erlang_module_info`,
	`view == erlang-project-outline && viewItem == module_data`,
	`view == erlang-project-outline`,
	`view == espEFuseExplorer`,
	`view == espRainmaker && !rainmaker_logged_in`,
	`view == espRainmaker && viewItem == account`,
	`view == espRainmaker && viewItem == node`,
	`view == espRainmaker && viewItem == param`,
	`view == espRainmaker`,
	`view == esprojects && debug.conditionbks`,
	`view == esprojects && viewItem == file && essemi.compare.hasleft`,
	`view == esprojects && viewItem == file&&isWindows`,
	`view == esprojects && viewItem == file`,
	`view == esprojects && viewItem == group || viewItem == projectNode`,
	`view == esprojects && viewItem == group`,
	`view == esprojects && viewItem == projectNode`,
	`view == esprojects`,
	`view == etcd2view && etcdcluster.currentcluster && !etcd2.basicauth_enabled && !etcdcluster.tlsauth`,
	`view == etcd2view && etcdcluster.currentcluster && etcd2.basicauth_enabled && !etcd2.basicauth`,
	`view == etcd2view && etcdcluster.currentcluster && etcd2.basicauth_enabled && !etcdcluster.basicauth`,
	`view == etcd2view && etcdcluster.currentcluster && etcd2.basicauth_enabled && etcd2.basicauth`,
	`view == etcd2view && etcdcluster.currentcluster && etcd2.basicauth_enabled && etcdcluster.basicauth`,
	`view == etcd2view && etcdcluster.currentcluster && etcd2.basicauthroot && etcd2.basicauth_enabled && !etcdcluster.tlsauth`,
	`view == etcd2view && etcdcluster.currentcluster && etcd3.basicauthroot && etcd2.basicauth_enabled && !etcdcluster.tlsauth`,
	`view == etcd2view && etcdcluster.currentcluster && viewItem =~ /^etcdnode_dir$/ `,
	`view == etcd2view && etcdcluster.currentcluster && viewItem =~ /^etcdnode_dir$|^etcdnode_leaf$/ `,
	`view == etcd2view && etcdcluster.currentcluster`,
	`view == etcd2view && viewItem =~ /^etcdnode_dir$/ `,
	`view == etcd2view && viewItem =~ /^etcdnode_dir$|^etcdnode_leaf$/ `,
	`view == etcd3view && etcdcluster.currentcluster && !etcd3.basicauth_enabled && !etcdcluster.tlsauth`,
	`view == etcd3view && etcdcluster.currentcluster && etcd3.basicauth_enabled && !etcd3.basicauth`,
	`view == etcd3view && etcdcluster.currentcluster && etcd3.basicauth_enabled && etcd3.basicauth`,
	`view == etcd3view && etcdcluster.currentcluster && etcd3.basicauthroot && etcd3.basicauth_enabled && !etcdcluster.tlsauth`,
	`view == etcd3view && etcdcluster.currentcluster && viewItem =~ /^etcdnode_dir$/ `,
	`view == etcd3view && etcdcluster.currentcluster && viewItem =~ /^etcdnode_dir$|^etcdnode_leaf$/ `,
	`view == etcd3view && etcdcluster.currentcluster`,
	`view == etcd3view && viewItem =~ /^etcdnode_dir$|^etcdnode_leaf$/ `,
	`view == etcdclusterview && viewItem =~ /^etcdcluster(tlsauth)*(?<!current)$/`,
	`view == etcdclusterview && viewItem =~ /^etcdcluster.*/`,
	`view == etcdclusterview && viewItem =~ /^etcdclusterincerterr$/`,
	`view == etcdclusterview`,
	`view == eth-abi-interactive.abis && viewItem == abiFunction`,
	`view == eth-abi-interactive.abis && viewItem == abiInput`,
	`view == eth-abi-interactive.accounts`,
	`view == eth-abi-interactive.contracts`,
	`view == ev3devBrowser && ev3devBrowser.context.connected`,
	`view == ev3devBrowser && viewItem == ev3devBrowser.device.connected`,
	`view == ev3devBrowser && viewItem == ev3devBrowser.device.disconnected`,
	`view == ev3devBrowser && viewItem == ev3devBrowser.file.executable`,
	`view == ev3devBrowser && viewItem == ev3devBrowser.file.folder`,
	`view == ev3devBrowser && viewItem == ev3devBrowser.file`,
	`view == eventstudio.document-treeview && viewItem == document`,
	`view == eventstudio.document-treeview || view == eventstudio.menu-treeview`,
	`view == eventstudio.document-treeview || view == eventstudio.scenario-treeview || view == eventstudio.menu-treeview`,
	`view == eventstudio.document-treeview`,
	`view == eventstudio.menu-treeview`,
	`view == eventstudio.scenario-treeview && viewItem == scenario`,
	`view == example`,
	`view == exceptions && viewItem == exception`,
	`view == exceptions`,
	`view == explore.view.id && viewItem =~ /WrStudioTreeItem/`,
	`view == explore.view.id && viewItem =~ /canConnect=false/`,
	`view == explore.view.id && viewItem =~ /canConnect=true/`,
	`view == explore.view.id && viewItem =~ /isVSBBuild=true/`,
	`view == explore.view.id && viewItem =~ /type=bucket|type=file|type=prefix/`,
	`view == explore.view.id && viewItem =~ /type=bucket|type=prefix/`,
	`view == explore.view.id && viewItem =~ /type=file/`,
	`view == explore.view.id && viewItem =~ /type=file|type=prefix/`,
	`view == explore.view.id && viewItem =~ /type=gitlab_project/`,
	`view == explore.view.id && viewItem =~ /type=harbor_artifact/`,
	`view == explore.view.id && viewItem =~ /type=harbor_project/`,
	`view == explore.view.id && viewItem =~ /type=harbor_repo/`,
	`view == explore.view.id && viewItem =~ /type=lxbs_project/`,
	`view == explore.view.id && viewItem =~ /type=lxbs_root|type=gitlab_root/`,
	`view == explore.view.id && viewItem =~ /type=lxbs_root|type=vxbs_root/`,
	`view == explore.view.id && viewItem =~ /type=lxbs_root|type=vxbs_root|type=gitlab_root/`,
	`view == explore.view.id && viewItem =~ /type=minio_root/ `,
	`view == explore.view.id && viewItem =~ /type=my_reservations/`,
	`view == explore.view.id && viewItem =~ /type=physical_reservation/ && viewItem =~ /canUnreserve=true/`,
	`view == explore.view.id && viewItem =~ /type=physical_reservation|type=virtual_reservation/ && viewItem =~ /canUnreserve=true/`,
	`view == explore.view.id && viewItem =~ /type=physical_root|type=virtual_root/`,
	`view == explore.view.id && viewItem =~ /type=physical_target|type=virtual_target/`,
	`view == explore.view.id && viewItem =~ /type=prefix/`,
	`view == explore.view.id && viewItem =~ /type=sysreg_root|type=devreg_root/`,
	`view == explore.view.id && viewItem =~ /type=view_artifacts/`,
	`view == explore.view.id && viewItem =~ /type=virtual_reservation/ && viewItem =~ /canUnreserve=true/`,
	`view == explore.view.id && viewItem =~ /type=vxbs_root/`,
	`view == explore.view.id && viewItem =~ /type=vxbs_vip|type=vxbs_vsb|type=lxbs_project/`,
	`view == explore.view.id`,
	`view == explorer-bookmark && viewItem != directlySavedItem`,
	`view == explorer-bookmark && viewItem == directlySavedItem`,
	`view == explorer-bookmark`,
	`view == explorerTerminalCommands || view == composeTerminalCommands`,
	`view == exportall.view && viewItem == exportall.excluded && viewItem != exportall.header`,
	`view == exportall.view && viewItem == exportall.listener && viewItem != exportall.header`,
	`view == exportall.view`,
	`view == expressions && viewItem == annotation`,
	`view == expressions && viewItem == expression`,
	`view == expressions && viewItem == parameter`,
	`view == expressions`,
	`view == ext.cw.explorer && viewItem =~ /ext\\.cw\\.project\\.enabled*/`,
	`view == ext.cw.explorer`,
	`view == extension-points && viewItem == extensionPoint`,
	`view == extension-points`,
	`view == extension-scripts && viewItem == extensionScript`,
	`view == extension-scripts`,
	`view == extension.firefox.loadedScripts && viewItem == directory`,
	`view == extension.firefox.loadedScripts && viewItem == file`,
	`view == extension.vsJenkinsXExplorer  && viewItem == vsJenkinsX.pipelines.build.Running`,
	`view == extension.vsJenkinsXExplorer  && viewItem == vsJenkinsX.pipelines.build.hasUrls.Running`,
	`view == extension.vsJenkinsXExplorer && viewItem == vsJenkinsX.pipelines.build.Running`,
	`view == extension.vsJenkinsXExplorer && viewItem == vsJenkinsX.pipelines.build.hasUrls.Running`,
	`view == extension.vsJenkinsXExplorer && viewItem == vsJenkinsX.pipelines.build.hasUrls`,
	`view == extension.vsJenkinsXExplorer && viewItem == vsJenkinsX.pipelines.build`,
	`view == extension.vsJenkinsXExplorer && viewItem == vsJenkinsX.pipelines.repo`,
	`view == extension.vsJenkinsXExplorer && viewItem == vsJenkinsX.pipelines.stage.app`,
	`view == extension.vsJenkinsXExplorer && viewItem == vsJenkinsX.pipelines.stage.preview`,
	`view == extension.vsJenkinsXExplorer && viewItem == vsJenkinsX.pipelines.stage.pullRequest`,
	`view == extension.vsJenkinsXExplorer && viewItem == vsJenkinsX.pipelines.stage.update`,
	`view == extension.vsJenkinsXExplorer`,
	`view == extension.vsKubernetesExplorer && viewItem == linkerd.mesh`,
	`view == extension.vsKubernetesExplorer && viewItem == linkerd.meshedNamespace`,
	`view == extension.vsKubernetesExplorer && viewItem == linkerd.meshedPod`,
	`view == extension.vsKubernetesExplorer && viewItem == openshift.resource.csv.crdDescription`,
	`view == extension.vsKubernetesExplorer && viewItem == vsKubernetes.resource.bc`,
	`view == extension.vsKubernetesExplorer && viewItem == vsKubernetes.resource.dc`,
	`view == extension.vsKubernetesExplorer && viewItem == vsKubernetes.resource.pipelines`,
	`view == extension.vsKubernetesExplorer && viewItem == vsKubernetes.resource.task`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /^vsKubernetes\\.\\w*cluster$/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /containerfilenode/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /containerfoldernode/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /containernode/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /k8s-watchable/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /openShift\\.resource\\.build.*/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /openShift\\.resource\\.rc.*/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.\\w*cluster($|\\s)/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.\\w*cluster.*/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.\\w*cluster\\.inactive/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.file/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.helmRelease/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.helmhistory/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.helmrelease/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.kind/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.minikubeCluster/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.resource(?!\\.namespace).*/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.resource.*/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.resource\\.(deployment|job|rc|rs|statefulset)/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.resource\\.(pod|job)/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.resource\\.(pod|job|service|deployment)/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.resource\\.*/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.resource\\..+/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.resource\\./i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.resource\\.configmap/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.resource\\.cronjob/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.resource\\.deployment/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.resource\\.imagestream*/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.resource\\.namespace/`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.resource\\.namespace\\.inactive/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.resource\\.node/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.resource\\.pod/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.resource\\.project*/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.resource\\.route/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.resource\\.secret/i`,
	`view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.resource\\.service/i`,
	`view == extension.vsKubernetesExplorer`,
	`view == extension.vsKubernetesHelmRepoExplorer && viewItem =~ /vsKubernetes\\.((chart)|(chartversion))/i`,
	`view == extension.vsKubernetesHelmRepoExplorer`,
	`view == extensionManager && DevOps.isXSpace3`,
	`view == extensionManager && viewItem == extension && DevOps.isXSpace3`,
	`view == extensionManager && viewItem == uninstall && DevOps.isXSpace3`,
	`view == extensionManager && viewItem =~ /updateSingle/ && DevOps.isXSpace3`,
	`view == extensionManager`,
	`view == extensionPointManager`,
	`view == extensionView && viewItem == parent`,
	`view == extensionView`,
	`view == extrinsics && viewItem == extrinsic`,
	`view == extrinsics`,
	`view == f5Hosts && f5.device`,
	`view == f5Hosts && viewItem == f5Host`,
	`view == f5Hosts`,
	`view == factoriomods && viewItem =~ /haschangelog/`,
	`view == factoriomods && viewItem =~ /latest/ && viewItem =~ /haschangelog/`,
	`view == factoriomods && viewItem =~ /latest/`,
	`view == fakesharpersettings.dupfinder`,
	`view == fastView && viewItem == fastApp`,
	`view == fastView && viewItem == fastTemplateSet`,
	`view == fastView && viewItem == fastTemplate`,
	`view == fastView`,
	`view == fatshark-code-assist-Connections`,
	`view == fatshark-code-assist-RunSets`,
	`view == fatshark-code-assist-Targets`,
	`view == fauna-databases && viewItem != document`,
	`view == fauna-databases && viewItem == document`,
	`view == fauna-databases`,
	`view == fav-favorites && viewItem != group`,
	`view == fav-favorites && viewItem == folder`,
	`view == fav-favorites && viewItem == group-empty`,
	`view == fav-favorites && viewItem == group`,
	`view == fav-favorites`,
	`view == favoriteFolders`,
	`view == favorite`,
	`view == favorites && sort==ASC`,
	`view == favorites && sort==DESC`,
	`view == favorites && viewItem != FAVORITE_DIRECTORY && viewItem != FAVORITE_FILE && viewItem != FS_FILE && viewItem != FS_DIRECTORY`,
	`view == favorites && viewItem != FAVORITE_GROUP && viewItem != FAVORITE_DIRECTORY && viewItem != FAVORITE_FILE && viewItem != FS_FILE && viewItem != FS_DIRECTORY`,
	`view == favorites && viewItem == favoritesItem`,
	`view == favorites-own-view || view == favorites-explorer-view`,
	`view == favoritesActivity && sort==ASC`,
	`view == favoritesActivity && sort==DESC`,
	`view == favoritesActivity && viewItem != FAVORITE_DIRECTORY && viewItem != FAVORITE_FILE && viewItem != FS_FILE && viewItem != FS_DIRECTORY`,
	`view == favoritesActivity && viewItem != FAVORITE_GROUP && viewItem != FAVORITE_DIRECTORY && viewItem != FAVORITE_FILE && viewItem != FS_FILE && viewItem != FS_DIRECTORY`,
	`view == favoritesActivity`,
	`view == favoritesPanel`,
	`view == favorites`,
	`view == fcLocalResource && viewItem == flow`,
	`view == fcLocalResource && viewItem == function`,
	`view == fcLocalResource && viewItem == nas`,
	`view == fcLocalResource && viewItem == service`,
	`view == fcLocalResource && viewItem == template`,
	`view == fcLocalResource`,
	`view == fcRemoteResource && viewItem == function`,
	`view == fcRemoteResource && viewItem == service`,
	`view == fcRemoteResource`,
	`view == fcstesttree && viewItem == error`,
	`view == fcstesttree && viewItem == failTest `,
	`view == fcstesttree && viewItem == failTest`,
	`view == fcstesttree && viewItem == root`,
	`view == fcstesttree`,
	`view == fcstree && viewItem != root `,
	`view == fcstree && viewItem == root`,
	`view == fcstree`,
	`view == featureReuseView  && mobioos-forge.feature-reuse.startingPoint == FileSystem  && viewItem == 'file'`,
	`view == featureReuseView  && mobioos-forge.feature-reuse.startingPoint == FileSystem  && viewItem == 'folder'`,
	`view == featureReuseView  && mobioos-forge.feature-reuse.startingPoint == FileSystem && viewItem == 'folder'`,
	`view == featureReuseView  && viewItem == 'Forge Project'`,
	`view == featureReuseView && mobioos-forge.feature-reuse.startingPoint == Feature`,
	`view == featureReuseView && mobioos-forge.feature-reuse.startingPoint == FileSystem && viewItem == 'folder'`,
	`view == featureReuseView && mobioos-forge.feature-reuse.startingPoint == FileSystem`,
	`view == featureReuseView && viewItem == 'Forge Project'`,
	`view == featureReuseView`,
	`view == feinterview_collect`,
	`view == feinterview_tools`,
	`view == feinterview_weapparticle`,
	`view == feishu-ide-view && viewItem == compileMode`,
	`view == feishu-ide-view`,
	`view == fenixEnvironment`,
	`view == fenixQuickCreate`,
	`view == fenixRepositories`,
	`view == fetch-client.sideBar`,
	`view == file-git-history`,
	`view == file-tag-tree && viewItem == ROOT`,
	`view == file-tag-tree`,
	`view == fileAnchors`,
	`view == fileExplorer && viewItem == file`,
	`view == fileExplorer`,
	`view == fileTagsExplorer && viewItem == tag`,
	`view == fileTagsExplorer && viewItem == taggedFile`,
	`view == fileTagsExplorer`,
	`view == fileTree`,
	`view == filesExplorer`,
	`view == filters && viewItem =~ /-invisible/`,
	`view == filters && viewItem =~ /-visible/`,
	`view == filters && viewItem =~ /^lit/`,
	`view == filters && viewItem =~ /^unlit/`,
	`view == filters`,
	`view == findView`,
	`view == firebase-apps && firebase:apps:loaded`,
	`view == firebase-firestore && firebase:firestore:loaded`,
	`view == firebase-projects`,
	`view == firebird-explorer && viewItem == database`,
	`view == firebird-explorer && viewItem == field`,
	`view == firebird-explorer && viewItem == host`,
	`view == firebird-explorer && viewItem == table`,
	`view == firebird-explorer`,
	`view == firestore-explorer-view`,
	`view == fishBookView && viewItem == book`,
	`view == fishBookView && viewItem == detail`,
	`view == fishBookView`,
	`view == flat-source-view && viewItem == objects`,
	`view == flowBookmarksView && viewItem == dependency`,
	`view == flowBookmarksView && viewItem == flow`,
	`view == flowBookmarksView`,
	`view == flr && viewItem == file && !isMonitorEnabled`,
	`view == flr && viewItem == file && isMonitorEnabled`,
	`view == flr`,
	`view == fluig-servers && viewItem == serverItem`,
	`view == fluig-servers`,
	`view == flutter-coverage && viewItem == flutter-coverage`,
	`view == flutter-coverage`,
	`view == fnfRemoteResource`,
	`view == foam-vscode.orphans && foam-vscode.orphans-grouped-by-folder == false`,
	`view == foam-vscode.orphans && foam-vscode.orphans-grouped-by-folder == true`,
	`view == foam-vscode.placeholders && foam-vscode.placeholders-grouped-by-folder == false`,
	`view == foam-vscode.placeholders && foam-vscode.placeholders-grouped-by-folder == true`,
	`view == focusedFolderView && viewItem == focusedBaseFolder`,
	`view == focusedFolderView`,
	`view == foldersCompareAppService && foldersCompareContext.hasFolders`,
	`view == foldersCompareAppServiceOnlyA || view == foldersCompareAppServiceOnlyB`,
	`view == foldersCompareAppServiceOnlyA`,
	`view == foldersCompareAppServiceOnlyB`,
	`view == foldersCompareAppService`,
	`view == font-awesome.icons-view && !showCategoryBadge`,
	`view == font-awesome.icons-view && !showIconInfo`,
	`view == font-awesome.icons-view && !showIconName`,
	`view == font-awesome.icons-view && !sortByFeature`,
	`view == font-awesome.icons-view && showCategoryBadge`,
	`view == font-awesome.icons-view && showIconInfo`,
	`view == font-awesome.icons-view && showIconName`,
	`view == font-awesome.icons-view && sortByFeature`,
	`view == font-awesome.icons-view`,
	`view == forbeslindesay-taskrunner`,
	`view == forgeDataManagementView && viewItem == bucket`,
	`view == forgeDataManagementView && viewItem == object`,
	`view == forgeDataManagementView`,
	`view == forgeDesignAutomationView && viewItem == activity-alias`,
	`view == forgeDesignAutomationView && viewItem == activity-aliases`,
	`view == forgeDesignAutomationView && viewItem == activity-version`,
	`view == forgeDesignAutomationView && viewItem == appbundle-alias`,
	`view == forgeDesignAutomationView && viewItem == appbundle-aliases`,
	`view == forgeDesignAutomationView && viewItem == appbundle-version`,
	`view == forgeDesignAutomationView && viewItem == owned-activities`,
	`view == forgeDesignAutomationView && viewItem == owned-activity`,
	`view == forgeDesignAutomationView && viewItem == owned-appbundle`,
	`view == forgeDesignAutomationView && viewItem == owned-appbundles`,
	`view == forgeDesignAutomationView && viewItem == shared-activity`,
	`view == forgeDesignAutomationView && viewItem == shared-appbundle`,
	`view == forgeDesignAutomationView`,
	`view == forgeHubsView && viewItem == version`,
	`view == forgeMainView && viewItem == Configuration`,
	`view == forgeMainView && viewItem == Configurations`,
	`view == forgeMainView && viewItem == FeatureLocation:Feature:Not-Root`,
	`view == forgeMainView && viewItem == FeatureMaps && mobioos-forge.propagations.filter != None`,
	`view == forgeMainView && viewItem == FeatureMaps && mobioos-forge.propagations.filter.validated != Pending`,
	`view == forgeMainView && viewItem == FeatureMaps && mobioos-forge.propagations.filter.validated != Validated`,
	`view == forgeMainView && viewItem == FeatureMaps && mobioos-forge.propagations.startingPoint == Feature`,
	`view == forgeMainView && viewItem == FeatureMaps && mobioos-forge.propagations.startingPoint == File`,
	`view == forgeMainView && viewItem == FeatureMaps`,
	`view == forgeMainView && viewItem == FeatureModel:Constraint`,
	`view == forgeMainView && viewItem == FeatureModel:Constraints`,
	`view == forgeMainView && viewItem == FeatureModel:Feature:Not-Root`,
	`view == forgeMainView && viewItem == FeatureModel:Feature:Parent-Relation`,
	`view == forgeMainView && viewItem == FeatureModel:Feature:Presence`,
	`view == forgeMainView && viewItem == FeatureModel:Feature:Resource-Target-Folder`,
	`view == forgeMainView && viewItem == FeatureModel`,
	`view == forgeMainView && viewItem == ReplacementLibrary:Item`,
	`view == forgeMainView && viewItem == VariabilityModel`,
	`view == forgeMainView && viewItem =~ /^.*FeatureHasPendingPropagations$/`,
	`view == forgeMainView && viewItem =~ /^.*FileHasPathPropagation$/`,
	`view == forgeMainView && viewItem =~ /^FeatureModel:Feature:(?:Root|Not\\-Root)$/`,
	`view == forgeMainView && viewItem =~ /^FileHasPendingPropagations.*$/`,
	`view == forgeMainView && viewItem =~ /^Files|ForgeFileExplorer:Folder$/`,
	`view == forgeMainView && viewItem =~ /^ForgeFileExplorer:(?:Folder|File)$/`,
	`view == forgeMainView && viewItem =~ /^ForgeFileExplorer:Folder|ForgeFileExplorer:File$/`,
	`view == forgeMainView && viewItem =~ /^IsPending.*|IsValidated.*|IsPathPropagation.*$/`,
	`view == forgeMainView && viewItem =~ /^IsPending\\|.*$/`,
	`view == forgeMainView`,
	`view == forgeTfIdfView && viewItem == 'FeatureLocation:File|Folder'`,
	`view == forgeTfIdfView && viewItem == 'TfIdfTerm'`,
	`view == forgeTfIdfView`,
	`view == forgeWebhooksView && viewItem == event`,
	`view == forgeWebhooksView && viewItem == hook`,
	`view == forgeWebhooksView`,
	`view == fortifyView && viewItem == dependency`,
	`view == fortifyView`,
	`view == foundProjects && viewItem == node && config.netbeans.javaSupport.enabled`,
	`view == foundProjects && viewItem =~ /is:folder/`,
	`view == foundProjects && viewItem =~ /is:project/ && config.netbeans.javaSupport.enabled`,
	`view == foundProjects && viewItem =~ /is:project/ && viewItem =~ /^(?!.*is:projectRoot)/ && config.netbeans.javaSupport.enabled`,
	`view == fountain-outline`,
	`view == fridaApps && viewItem == 'app|running'`,
	`view == fridaApps && viewItem =~ /^app\\|/`,
	`view == fridaApps && viewItem =~ /^device\\|socket@/`,
	`view == fridaApps`,
	`view == fridaExtensionAppAccess`,
	`view == fridaExtensionAutomations`,
	`view == fridaExtensionRecent`,
	`view == fridaExtensionSuiteAccess`,
	`view == fridaPs && viewItem == 'process|running'`,
	`view == fridaPs && viewItem =~ /^device\\|socket@/`,
	`view == fridaPs && viewItem =~ /^process\\|/`,
	`view == fridaPs`,
	`view == frontMatter.explorer && frontMatter:has:modes == true`,
	`view == frontMatter.explorer || view == explorer`,
	`view == frontMatter.explorer`,
	`view == ftpExplorer`,
	`view == ftpkr.explorer`,
	`view == functionList`,
	`view == fund-list`,
	`view == fw-metadata`,
	`view == fw-statistics`,
	`view == fw-wordFrequencies && viewItem == wordStat`,
	`view == fw-wordFrequencies`,
	`view == g-gondola-assistant && viewItem == assistant-step-with-url`,
	`view == galasa-environment`,
	`view == galasa-ras && viewItem == localrun`,
	`view == galasa-ras`,
	`view == gauge:specExplorer`,
	`view == gcode.views.navTree`,
	`view == gcode.views.stats`,
	`view == gcoresExplorer && viewItem == can-bookmark`,
	`view == gcoresExplorer && viewItem == can-delete-author`,
	`view == gcoresExplorer && viewItem == can-delete-bookmark`,
	`view == gcoresExplorer && viewItem == can-delete-rss`,
	`view == gcoresExplorer && viewItem == can-play`,
	`view == gcoresExplorer && viewItem == can-stop`,
	`view == gcoresExplorer`,
	`view == gdUnit3TestExplorer`,
	`view == gemstone-logins`,
	`view == gemstone-sessions`,
	`view == genv.devices && viewItem == attach`,
	`view == genv.devices && viewItem == detach`,
	`view == genv.devices`,
	`view == genv.env && genv.installation.uninstalled`,
	`view == genv.env && viewItem == attached`,
	`view == genv.env && viewItem == detached`,
	`view == genv.env && viewItem == name`,
	`view == genv.envs`,
	`view == genv.system`,
	`view == gerrit:changeExplorer && viewItem =~ /filechange/ && viewItem =~ /modified/`,
	`view == gerrit:changeExplorer && viewItem =~ /filechange/`,
	`view == gerrit:changeExplorer && viewItem =~ /gerritchange/ && viewItem =~ /customPatchset/`,
	`view == gerrit:changeExplorer && viewItem =~ /gerritchange/ && viewItem =~ /noCurrent/`,
	`view == gerrit:changeExplorer && viewItem =~ /gerritchange/ && viewItem =~ /yesCurrent/`,
	`view == gerrit:changeExplorer && viewItem =~ /gerritchange/`,
	`view == gerrit:changeExplorer`,
	`view == gerrit:quickCheckout && viewItem =~ /quickCheckout/`,
	`view == gerrit:searchResults || view == gerrit:changeExplorer`,
	`view == gglobalVar`,
	`view == ginkgotestexplorer && viewItem == test`,
	`view == ginkgotestexplorer`,
	`view == ginkgotestfilesexplorer`,
	`view == gistpad.repos && gistpad:state == SignedIn`,
	`view == gistpad.repos && viewItem == gistpad.repoFile`,
	`view == gistpad.repos && viewItem =~ /^gistpad.(repo|swing)(.branch|Directory)?(.hasTours)?$/`,
	`view == gistpad.repos && viewItem =~ /^gistpad.(repo|wiki|swing)(.branch)?(.hasTours)?$/`,
	`view == gistpad.repos && viewItem =~ /^gistpad.(repo|wiki|swing)(.branch|Directory)?(.hasTours)?$/`,
	`view == gistpad.repos && viewItem =~ /^gistpad.(repo|wiki|swing)Directory$/`,
	`view == gistpad.repos && viewItem =~ /^gistpad.(repo|wiki|swing)File$/`,
	`view == gistpad.repos && viewItem =~ /^gistpad.repo(.branch)?(.hasTours)?$/`,
	`view == gistpad.repos && viewItem =~ /^gistpad.repo(.branch)?.hasTours$/`,
	`view == gistpad.repos && viewItem =~ /^gistpad.repo.branch(.hasTours)?$/`,
	`view == gistpad.repos && viewItem =~ /^gistpad.swing$/`,
	`view == gistpad.repos && viewItem =~ /^gistpad.wiki$/`,
	`view == gistpad.repos && viewItem =~ /^gistpad.wiki(Directory)?$/`,
	`view == gistpad.repos`,
	`view == gistpad.showcase`,
	`view == git-branch.view`,
	`view == git-file-explorer && gitFileExplorer.view != flat`,
	`view == git-file-explorer && gitFileExplorer.view != tree`,
	`view == gitCommits.commits && !gitCommits.commits.settings.viewAsTree`,
	`view == gitCommits.commits && gitCommits.commits.settings.viewAsTree`,
	`view == gitCommits.commits && viewItem == changeNode`,
	`view == gitCommits.commits && viewItem == commitNode`,
	`view == gitCommits.commits`,
	`view == gitCommits.remotes`,
	`view == gitCommits.stashes && viewItem == changeNode`,
	`view == gitCommits.stashes && viewItem == commitNode`,
	`view == gitCommits.stashes`,
	`view == gitHubIntegration`,
	`view == gitLFSLocks`,
	`view == gitStash`,
	`view == gitTreeCompare && !gitTreeCompare.viewAsList`,
	`view == gitTreeCompare && config.gitTreeCompare.diffMode == full`,
	`view == gitTreeCompare && config.gitTreeCompare.diffMode == merge`,
	`view == gitTreeCompare && gitOpenRepositoryCount != 1`,
	`view == gitTreeCompare && gitTreeCompare.viewAsList`,
	`view == gitTreeCompare && viewItem == file && !config.gitTreeCompare.openChanges`,
	`view == gitTreeCompare && viewItem == file && config.gitTreeCompare.openChanges`,
	`view == gitTreeCompare && viewItem == folder`,
	`view == gitTreeCompare && viewItem == ref && config.gitTreeCompare.diffMode == full`,
	`view == gitTreeCompare && viewItem == ref && config.gitTreeCompare.diffMode == merge`,
	`view == gitTreeCompare && viewItem == ref && gitOpenRepositoryCount != 1`,
	`view == gitTreeCompare && viewItem == ref`,
	`view == gitTreeCompare`,
	`view == git_hooks_view||view == git_hooks_scm`,
	`view == gitduckSharedFiles && gitduckCodeShareState == 4 && gitduckCodeShareIsHost && viewItem =~ /^(shared-)((file)|(directory))$/`,
	`view == gitduckSharedFiles && gitduckCodeShareState == 4 && gitduckCodeShareIsHost && viewItem =~ /^(unshared-)((file)|(directory))$/`,
	`view == gitduckSharedFiles && gitduckCodeShareState == 4 && gitduckCodeShareIsHost`,
	`view == gitduckSharedFiles && gitduckCodeShareState == 4`,
	`view == giteaIssues.opened-issues`,
	`view == gitexFlowActionsTreeView`,
	`view == gitflow.views.feature`,
	`view == gitflow.views.release`,
	`view == gitflowActionsTreeview`,
	`view == gitflowExplorer && viewItem == b`,
	`view == gitflowExplorer && viewItem == branch`,
	`view == gitflowExplorer && viewItem == bugfix ||  viewItem == bugfix_local`,
	`view == gitflowExplorer && viewItem == bugfix || viewItem == bugfix_local`,
	`view == gitflowExplorer && viewItem == bugfix_local`,
	`view == gitflowExplorer && viewItem == f`,
	`view == gitflowExplorer && viewItem == feature ||  viewItem == feature_local`,
	`view == gitflowExplorer && viewItem == feature || viewItem == feature_local`,
	`view == gitflowExplorer && viewItem == feature_local`,
	`view == gitflowExplorer && viewItem == h`,
	`view == gitflowExplorer && viewItem == hotfix ||  viewItem == hotfix_local`,
	`view == gitflowExplorer && viewItem == hotfix_local`,
	`view == gitflowExplorer && viewItem == origin_bugfix`,
	`view == gitflowExplorer && viewItem == origin_feature`,
	`view == gitflowExplorer && viewItem == origin_release`,
	`view == gitflowExplorer && viewItem == r`,
	`view == gitflowExplorer && viewItem == release ||  viewItem == release_local`,
	`view == gitflowExplorer && viewItem == release_local`,
	`view == gitflowExplorer && viewItem == s`,
	`view == gitflowExplorer && viewItem == support ||  viewItem == support_local`,
	`view == gitflowExplorer && viewItem == support || viewItem == support_local`,
	`view == gitflowExplorer && viewItem == support_local`,
	`view == gitflowExplorer && workspaceFolderCount > 1`,
	`view == gitflowExplorer`,
	`view == gitflowTags && viewItem == local`,
	`view == gitflowTags`,
	`view == github-actions.current-branch`,
	`view == github-actions.workflows || view == github-actions.settings`,
	`view == github-kejun-devtoys`,
	`view == github.codespaces.explorer && viewItem =~ /exportEnabled/`,
	`view == github.codespaces.explorer && viewItem =~ /removePathEnabled/`,
	`view == github.codespaces.explorer && viewItem =~ /viewCreationLogEnabled/`,
	`view == github.codespaces.performanceExplorer && viewItem =~ /copyIdToClipboardEnabled/`,
	`view == github.cweijan.mysql`,
	`view == github.cweijan.nosql && viewItem == folder`,
	`view == github.cweijan.nosql && viewItem == redisConnection`,
	`view == github.cweijan.nosql && viewItem == s3Connection`,
	`view == github.cweijan.nosql && viewItem == sshConnection`,
	`view == github.cweijan.nosql && viewItem == sshFileShell`,
	`view == github.cweijan.nosql && viewItem == sshUserGroup`,
	`view == github.cweijan.nosql && viewItem == sshUser`,
	`view == github.cweijan.nosql && viewItem =~ /(file|ftpFile)/`,
	`view == github.cweijan.nosql && viewItem =~ /(file|link)/`,
	`view == github.cweijan.nosql && viewItem =~ /(folder|file|ftpFile)/`,
	`view == github.cweijan.nosql && viewItem =~ /(folder|file|link)/`,
	`view == github.cweijan.nosql && viewItem =~ /(folder|file|s3Folder|s3File)/`,
	`view == github.cweijan.nosql && viewItem =~ /(folder|ftpFolder)/`,
	`view == github.cweijan.nosql && viewItem =~ /(folder|s3Folder)/`,
	`view == github.cweijan.nosql && viewItem =~ /(s3File)/`,
	`view == github.cweijan.nosql && viewItem =~ /(sshConnection|ftpConnection)/`,
	`view == github.cweijan.nosql && viewItem =~ /(sshConnection|ftpConnection|s3Connection)/`,
	`view == github.cweijan.nosql && viewItem =~ /(sshFolder|sshFavoriteFolder|sshFile|sshFavoriteFile)/`,
	`view == github.cweijan.nosql && viewItem =~ /^(folder|ftpFolder|file|ftpFile|redisFolder)$/`,
	`view == github.cweijan.nosql && viewItem =~ /^(redisKey|redisFolder)$/`,
	`view == github.cweijan.nosql && viewItem =~ /sshFolder|sshFavoriteFolder/`,
	`view == github.cweijan.nosql`,
	`view == github.cweijan.ssh && viewItem != connection`,
	`view == github.cweijan.ssh && viewItem == connection `,
	`view == github.cweijan.ssh && viewItem == connection`,
	`view == github.cweijan.ssh && viewItem == file`,
	`view == github.cweijan.ssh && viewItem == folder `,
	`view == github.cweijan.ssh && viewItem == folder`,
	`view == github.cweijan.ssh`,
	`view == github:activePullRequest && github:hasGitHubRemotes`,
	`view == github:createPullRequest && github:createPrPermissions != READ`,
	`view == githubRepoMgr.views.repositories && githubRepoMgr.userState == 'logged' && githubRepoMgr.canPublish == true`,
	`view == githubRepoMgr.views.repositories && githubRepoMgr.userState == 'logged'`,
	`view == gitlab.mrt`,
	`view == gitlabSnippetsExplorer-all && viewItem == host`,
	`view == gitlabSnippetsExplorer-mine && viewItem == host`,
	`view == gitlabSnippetsExplorer-stared && viewItem == snippet`,
	`view == gitlabSnippetsExplorer-stared`,
	`view == gitlens.views.welcome`,
	`view == gitlive && !git.live.view.org && !git.live.shareChanges.selected`,
	`view == gitlive && !git.live.view.org && git.live.shareChanges.selected`,
	`view == gitlive && !git.live.view.org`,
	`view == gitlive && git.live.view.org`,
	`view == gitlive`,
	`view == gitmob.CoAuthorsView`,
	`view == gitops.views.clusters && viewItem =~ /cluster;/ && viewItem =~ /notCurrentCluster;/`,
	`view == gitops.views.clusters && viewItem =~ /cluster;/`,
	`view == gitops.views.clusters && viewItem =~ /clusterGitOpsEnabled;/`,
	`view == gitops.views.clusters && viewItem =~ /clusterGitOpsNotEnabled;/`,
	`view == gitops.views.clusters`,
	`view == gitops.views.sources && viewItem =~ /(GitRepository;|OCIRepository;|HelmRepository;|Bucket;)/`,
	`view == gitops.views.sources && viewItem =~ /GitRepository;/`,
	`view == gitops.views.sources && viewItem =~ /GitRepository;|OCIRepository;/`,
	`view == gitops.views.sources`,
	`view == gitops.views.workloads && viewItem != Namespace;`,
	`view == gitops.views.workloads && viewItem =~ /(Kustomization;|HelmRelease;)/`,
	`view == gitops.views.workloads`,
	`view == gitstash.explorer && config.gitstash.explorer.buttons`,
	`view == gitstash.explorer && viewItem == repository`,
	`view == gitstash.explorer && viewItem == stash`,
	`view == gitstash.explorer && viewItem =~ /file/`,
	`view == gitstash.explorer && viewItem =~ /file:(modified|indexAdded)/`,
	`view == gitstash.explorer && viewItem =~ /file:(modified|indexAdded|renamed)/`,
	`view == gitstash.explorer && viewItem =~ /file:(untracked)/`,
	`view == gitstash.explorer`,
	`view == glFindings && veracode:filter:applied == false`,
	`view == glFindings && veracode:filter:applied == true`,
	`view == glFindings && viewItem == glFlawHeaders`,
	`view == glFindings`,
	`view == globalCommandExplorer && viewItem == file`,
	`view == globalCommandExplorer`,
	`view == gltfInspectData`,
	`view == gmAssetBrowser`,
	`view == gmVfs && viewItem == eventItem`,
	`view == gmVfs && viewItem == folderItem`,
	`view == gmVfs && viewItem =~ /objectItem/`,
	`view == gmVfs && viewItem =~ /resourceItem/`,
	`view == gmVfs`,
	`view == gmodAddonInfo && viewItem == description`,
	`view == gmodAddonInfo && viewItem == tags`,
	`view == gmodAddonInfo && viewItem == title`,
	`view == gmodAddonInfo && viewItem == type`,
	`view == gmodAddonInfo || view == gmodAddonWeapons || view == gmodWorkshop`,
	`view == gmodAddonInfo`,
	`view == gmodAddonWeapons && viewItem =~ /^weapons\\..*/`,
	`view == gmodAddonWeapons`,
	`view == gmodWorkshop && viewItem =~ /^item-.*/`,
	`view == gmodWorkshop`,
	`view == go.explorer`,
	`view == goTestExplorer`,
	`view == goTests && viewItem == gotest`,
	`view == goTests`,
	`view == gomod && viewItem != modules`,
	`view == gomod && viewItem == directory`,
	`view == gomod && viewItem == gosdk`,
	`view == gomod && viewItem == modules`,
	`view == gomod && viewItem == package`,
	`view == gomod`,
	`view == gondola-assistant && viewItem == assistant-step-with-url`,
	`view == gondola`,
	`view == googleHomeExtension.assistantSimulator && googleHomeExtension.hasImportedFile`,
	`view == googleHomeExtension.assistantSimulator && googleHomeExtension.hasTextQuery`,
	`view == googleHomeExtension.assistantSimulator && googleHomeExtension.scriptStatus != 'STOP'`,
	`view == googleHomeExtension.assistantSimulator && googleHomeExtension.scriptStatus == 'PAUSE'`,
	`view == googleHomeExtension.assistantSimulator && googleHomeExtension.scriptStatus == 'RUN'`,
	`view == googleHomeExtension.assistantSimulator && googleHomeExtension.scriptStatus == 'STOP'`,
	`view == googleHomeExtension.assistantSimulator`,
	`view == gpersonalVar`,
	`view == gpp.ProceduresTree`,
	`view == graalvm-installations && viewItem == graalvmComponentInstalled`,
	`view == graalvm-installations && viewItem == graalvmComponent`,
	`view == graalvm-installations && viewItem == graalvmConnectionError`,
	`view == graalvm-installations && viewItem == graalvmInstallation || viewItem == graalvmInstallationActive`,
	`view == graalvm-installations && viewItem == graalvmInstallation || viewItem == graalvmInstallationOut || viewItem == graalvmInstallationActive`,
	`view == graalvm-installations`,
	`view == gradleDaemonsView && config.gradle.showStoppedDaemons == false`,
	`view == gradleDaemonsView && config.gradle.showStoppedDaemons == true`,
	`view == gradleDaemonsView && viewItem =~ /^busy$|^idle$/`,
	`view == gradleDaemonsView`,
	`view == gradleTasksView && !gradle:gradleTasksCollapsed`,
	`view == gradleTasksView && gradle:gradleTasksCollapsed`,
	`view == gradleTasksView && javaLSReady`,
	`view == gradleTasksView && viewItem == PinnedTasks`,
	`view == gradleTasksView && viewItem == omitted`,
	`view == gradleTasksView && viewItem =~ /^pinned.*$/`,
	`view == gradleTasksView || view == gradleDefaultProjectsView`,
	`view == gradleTasksView`,
	`view == graphicalWatch && viewItem == watchVariable`,
	`view == graphicalWatch`,
	`view == gslang.pkgDependencies`,
	`view == gslang.profileRealtimeView && !gslangProfileRealtimeSplitCharts`,
	`view == gslang.profileRealtimeView && gslangProfileRealtimeSplitCharts`,
	`view == gslang.profileRealtimeView`,
	`view == gsppCompilerSettings && viewItem == compilerMenuItem`,
	`view == gtestExplorer && config.gtest-adapter.supportLocation`,
	`view == gtestExplorer`,
	`view == gulptasks:explorer && gulptasks:canExecute`,
	`view == gulptasks:explorer && gulptasks:canRestart`,
	`view == gulptasks:explorer && gulptasks:canTerminate`,
	`view == gulptasks:explorer`,
	`view == gzoltar && viewItem == folder`,
	`view == gzoltar`,
	`view == hanadb && viewItem == connection`,
	`view == hanadb && viewItem == database`,
	`view == hanadb && viewItem == table`,
	`view == hanadb`,
	`view == hanaeditor && viewItem == file`,
	`view == hashtagTree && config.markdown-hashtags.sorting.order != asc`,
	`view == hashtagTree && config.markdown-hashtags.sorting.order != desc`,
	`view == hashtagTree`,
	`view == haxe.cache && viewItem =~ /copyable/`,
	`view == haxe.cache && viewItem =~ /gotoable/`,
	`view == haxe.cache && viewItem =~ /reloadable/`,
	`view == haxe.dependencies && lixActive`,
	`view == haxe.dependencies && viewItem == file.md`,
	`view == haxe.dependencies && viewItem =~ /(file\\.*)/`,
	`view == haxe.dependencies && viewItem =~ /(folder)/`,
	`view == haxe.dependencies && viewItem =~ /(folder|file\\.*)/`,
	`view == haxe.dependencies`,
	`view == haxe.methods && haxeMethodsViewType == queue`,
	`view == haxe.methods && haxeMethodsViewType == timers`,
	`view == haxe.methods`,
	`view == hdinsight.treeview && viewItem != hiveSchemaNode && viewItem != ADLMountPointNode && viewItem != BlobContainerNode && viewItem != ADLFolder  && viewItem != ADLStorageInfo && viewItem != BlobFolderInfo && viewItem != BlobStorageInfo`,
	`view == hdinsight.treeview && viewItem == HdInsightCluster`,
	`view == hdinsight.treeview && viewItem =~ /(HiveTableInfo|stoppedPreviewQuery|startingRefresh|stoppedRefresh)/  && viewItem != HdInsightCluster && viewItem != HiveDataBaseInfo && viewItem != HiveFolderInfo && viewItem != HdInsightCluster && viewItem != startingPreviewQuery &&viewItem != hiveSchemaNode && viewItem != ADLMountPointNode && viewItem != BlobContainerNode && viewItem != ADLFolder  && viewItem != ADLStorageInfo && viewItem != BlobFolderInfo && viewItem != BlobStorageInfo`,
	`view == help && viewItem == openUrl`,
	`view == helpTreeView && waveAnalysis.extensionIsActive`,
	`view == helpView`,
	`view == help`,
	`view == hero-heroku && viewItem == app`,
	`view == hero-heroku && viewItem == dynoBranch`,
	`view == hero-heroku && viewItem == dyno`,
	`view == hero-heroku`,
	`view == hexo.categories`,
	`view == hexo.draft`,
	`view == hexo.post`,
	`view == hexo.tags`,
	`view == hidden-files`,
	`view == hilightWordsExplore`,
	`view == history && viewItem == historyItem`,
	`view == historyView && viewItem == historyItem`,
	`view == historyView`,
	`view == history`,
	`view == hlfNetworks && viewItem == network`,
	`view == hlfNetworks`,
	`view == hlfWallets && viewItem == organization`,
	`view == hlfWallets && viewItem == wallet`,
	`view == hlfWallets`,
	`view == hostProcessView && viewItem == runspace`,
	`view == hostProcessView`,
	`view == hostTreeProvider && viewItem == envConfig1`,
	`view == hostTreeProvider`,
	`view == hostsView && viewItem == host`,
	`view == hostsView`,
	`view == hpccPlatform && hpccPlatform.isAllWorkunits`,
	`view == hpccPlatform && hpccPlatform.isMyWorkunits`,
	`view == hpccPlatform && viewItem == ECLResultNode`,
	`view == hpccPlatform && viewItem =~ /^ECLWUNode/`,
	`view == hpccPlatform`,
	`view == hpccResources.bundles && viewItem == BundlesItemInstalled`,
	`view == hpccResources.bundles && viewItem == BundlesItem`,
	`view == hpccResources.bundles && viewItem =~ /^BundlesItem/`,
	`view == hpccResources.bundles`,
	`view == hpccResources.clientTools && viewItem == ClientToolsItemActive`,
	`view == hpccResources.clientTools && viewItem == ClientToolsItemDeactive`,
	`view == hpccResources.clientTools && viewItem =~ /^ClientToolsItem/`,
	`view == hpccResources.clientTools`,
	`view == htmlRelatedLinks && !htmlRelatedLinks:fileIsLocked`,
	`view == htmlRelatedLinks && htmlRelatedLinks:fileIsLocked`,
	`view == htmlRelatedLinks && viewItem == relatedFile`,
	`view == httpyacEnvironments && viewItem == 'env'`,
	`view == httpyacHistory && viewItem == 'responseItem'`,
	`view == httpyacHistory`,
	`view == httpyacSession && viewItem == 'session'`,
	`view == httpyacVariables || view == httpyacSession`,
	`view == huawei-cloud-toolkit && !isUseCloudIde && isLogin`,
	`view == huawei-cloud-toolkit && !isUseCloudIde`,
	`view == huawei-cloud-toolkit`,
	`view == huile8-mastered && viewItem == word`,
	`view == huile8-will-mastering && viewItem == word`,
	`view == i18n-manager-view && viewItem == tag`,
	`view == i18n-manager-view && viewItem == value`,
	`view == iar-project && iar-build.extendedProjectLoaded`,
	`view == iar-project && viewItem == group || viewItem == file`,
	`view == iar-project && viewItem == group`,
	`view == iar-project`,
	`view == iar-settings`,
	`view == iar-toolbar`,
	`view == iconfontList && viewItem == icon`,
	`view == iconfontList`,
	`view == idfAppTraceArchive`,
	`view == idfAppTracer`,
	`view == idfPartitionExplorer`,
	`view == idfSearchResults`,
	`view == ifsBrowser && viewItem == directory && workspaceFolderCount >= 1`,
	`view == ifsBrowser && viewItem == directory`,
	`view == ifsBrowser && viewItem == shortcut && workspaceFolderCount >= 1`,
	`view == ifsBrowser && viewItem == shortcut`,
	`view == ifsBrowser && viewItem == streamfile`,
	`view == ifsBrowser`,
	`view == ilovejuejin_recommend`,
	`view == ilovejuejin_tools`,
	`view == ilspyDecompiledMembers && viewItem == assemblyNode`,
	`view == ilspyDecompiledMembers`,
	`view == indigo-reports && config.IndigoRunner.reports.activityBarDisplayType =~ /^List$|^Shallow$/`,
	`view == indigo-reports && config.IndigoRunner.reports.activityBarDisplayType =~ /^Tree$|^Deep$/`,
	`view == indigo-reports`,
	`view == indigo-tests && config.IndigoRunner.tests.activityBarDisplayType == Tag`,
	`view == indigo-tests && config.IndigoRunner.tests.activityBarDisplayType =~ /^List$|^Shallow$/`,
	`view == indigo-tests && config.IndigoRunner.tests.activityBarDisplayType =~ /^Tree$|^Deep$/`,
	`view == indigo-tests && viewItem == testDirectory`,
	`view == indigo-tests && viewItem == testFeature`,
	`view == indigo-tests && viewItem == testFileIgnored`,
	`view == indigo-tests && viewItem == testFile`,
	`view == indigo-tests && viewItem == testScenario`,
	`view == indigo-tests && viewItem == testTag`,
	`view == indigo-tests`,
	`view == influxdb && viewItem == bucket`,
	`view == influxdb && viewItem == buckets`,
	`view == influxdb && viewItem == instance`,
	`view == influxdb && viewItem == script`,
	`view == influxdb && viewItem == scripts`,
	`view == influxdb && viewItem == task`,
	`view == influxdb && viewItem == tasks`,
	`view == influxdb`,
	`view == infracostProjects`,
	`view == inlineBookmarksExplorer`,
	`view == inspect-node && viewItem == editable_value`,
	`view == inspect-node && viewItem == remote_object`,
	`view == inspect-node`,
	`view == installedNodeVersions && viewItem == installedVersionNode`,
	`view == installedNodeVersions`,
	`view == instanceViewNew && display != False`,
	`view == instanceViewNew && regionProfileDisplay != False`,
	`view == instanceViewNew && viewItem == allInstances`,
	`view == instanceViewNew && viewItem == identityFile`,
	`view == instanceViewNew && viewItem == instance`,
	`view == instanceViewNew && viewItem == myInstances`,
	`view == instanceViewNew && viewItem == userLogin`,
	`view == instanceViewNew`,
	`view == instancesTreeView && viewItem == instance`,
	`view == instancesTreeView`,
	`view == integrations && viewItem == 'integration'`,
	`view == integrations`,
	`view == intel.oneAPISamples.tree && viewItem == sample`,
	`view == intel.oneAPISamples.tree`,
	`view == intelOneAPI.debug.simdview`,
	`view == intersystems-community_servermanager && isWindows`,
	`view == intersystems-community_servermanager && viewItem == project`,
	`view == intersystems-community_servermanager && viewItem == sorted`,
	`view == intersystems-community_servermanager && viewItem == starred.server.starred`,
	`view == intersystems-community_servermanager && viewItem =~ /\\.server\\.$/`,
	`view == intersystems-community_servermanager && viewItem =~ /\\.server\\./ && config.intersystemsServerManager.authentication.provider != intersystems-server-credentials`,
	`view == intersystems-community_servermanager && viewItem =~ /\\.server\\./`,
	`view == intersystems-community_servermanager && viewItem =~ /namespace$/`,
	`view == intersystems-community_servermanager && viewItem =~ /offline$/`,
	`view == intersystems-community_servermanager`,
	`view == interview`,
	`view == introduction && auth.isLoggedIn == false`,
	`view == introduction && auth.isLoggedIn == true`,
	`view == introduction`,
	`view == intuitaViewId || view == explorerIntuitaViewId`,
	`view == inventoryView && viewItem == config`,
	`view == inventoryView && viewItem == instance`,
	`view == inventoryView`,
	`view == invoke-events && viewItem == folder`,
	`view == invoke-events && viewItem == lambda`,
	`view == invoke-events`,
	`view == iobroker-javascript.changed-scripts && viewItem == scriptItem || viewItem == onlyLocalScriptItem`,
	`view == iobroker-javascript.changed-scripts && viewItem == scriptItem`,
	`view == iobroker-javascript.changed-scripts`,
	`view == iobroker-javascript.script-explorer && viewItem == scriptDirectory`,
	`view == iobroker-javascript.script-explorer && viewItem == scriptItem || viewItem == onlyLocalScriptItem`,
	`view == iobroker-javascript.script-explorer && viewItem == scriptItem || viewItem == onlyRemoteScriptItem`,
	`view == iobroker-javascript.script-explorer && viewItem == scriptItem`,
	`view == iobroker-javascript.script-explorer`,
	`view == ionic && viewItem == asset`,
	`view == ionic && viewItem == buildConfig`,
	`view == ionic && viewItem == lightbulb`,
	`view == ionic && viewItem == packages`,
	`view == ionic && viewItem == rebuild`,
	`view == ionic && viewItem == refreshDebug`,
	`view == ionic && viewItem == selectAction`,
	`view == ionic && viewItem == selectDevice`,
	`view == ionic && viewItem == stop`,
	`view == ionic && viewItem == upgrade`,
	`view == ionic`,
	`view == ionide.projectExplorer && viewItem == ionide.projectExplorer.projectExe`,
	`view == ionide.projectExplorer && viewItem == ionide.projectExplorer.project`,
	`view == ionide.projectExplorerInActivity`,
	`view == ionide.projectExplorer`,
	`view == iosreIDtabSectionApplications`,
	`view == iosreIDtabSectionFileSystem`,
	`view == iosreIDtabSectioniDevices`,
	`view == iotDpsExplorer && viewItem == IotDps`,
	`view == iotDpsExplorer && viewItem == azureextensionui.azureSubscription`,
	`view == iotDpsExplorer`,
	`view == iotHubDevices && viewItem == desired-distributed-twin-properies`,
	`view == iotHubDevices && viewItem == desired-mode-property`,
	`view == iotHubDevices && viewItem == desired-sampling-rate-property`,
	`view == iotHubDevices && viewItem == device`,
	`view == iotHubDevices && viewItem == devices-label`,
	`view == iotHubDevices && viewItem == distributed-tracing-setting`,
	`view == iotHubDevices && viewItem == edge-module`,
	`view == iotHubDevices && viewItem == edge`,
	`view == iotHubDevices && viewItem == endpoints-label`,
	`view == iotHubDevices && viewItem == event-hub`,
	`view == iotHubDevices && viewItem == events`,
	`view == iotHubDevices && viewItem == interfaces-label`,
	`view == iotHubDevices && viewItem == module`,
	`view == iotHubDevices && viewItem == modules-label`,
	`view == iotHubDevices && viewItem == reported-distributed-twin-properies`,
	`view == iotHubDevices`,
	`view == iotlink-debug.registers && viewItem == register`,
	`view == iotlink-debug.registers`,
	`view == ipPackage`,
	`view == ipView && viewItem == atcService`,
	`view == ipView && viewItem == ucsHeader`,
	`view == ipView && viewItem == ucsItem`,
	`view == ipView`,
	`view == iqView && viewItem == iQapp`,
	`view == iqView && viewItem == iQglobalApp`,
	`view == iqView && viewItem == iqScript`,
	`view == iqView && viewItem == iqTemplate`,
	`view == iqView`,
	`view == issues:github && github:hasGitHubRemotes`,
	`view == issues:github && viewItem == query`,
	`view == issues:github && viewItem =~ /^(link)?(current|continue)?issue/`,
	`view == issues:github && viewItem =~ /^(link)?continueissue/ && config.githubIssues.useBranchForIssues != on`,
	`view == issues:github && viewItem =~ /^(link)?continueissue/ && config.githubIssues.useBranchForIssues == on`,
	`view == issues:github && viewItem =~ /^(link)?continueissue/`,
	`view == issues:github && viewItem =~ /^(link)?currentissue/ && config.githubIssues.useBranchForIssues != on`,
	`view == issues:github && viewItem =~ /^(link)?currentissue/ && config.githubIssues.useBranchForIssues == on`,
	`view == issues:github && viewItem =~ /^(link)?currentissue/`,
	`view == issues:github && viewItem =~ /^(link)?issue/ && config.githubIssues.useBranchForIssues != on`,
	`view == issues:github && viewItem =~ /^(link)?issue/ && config.githubIssues.useBranchForIssues == on`,
	`view == issues:github && viewItem =~ /^(link)?issue/`,
	`view == issues:github && viewItem =~ /^link(current|continue)?issue/`,
	`view == issues:github`,
	`view == issuesExplorer && viewItem == IssueItem && gitEnabled == 1`,
	`view == issuesExplorer && viewItem == IssueItem`,
	`view == issuesExplorer`,
	`view == itemlog && viewItem == diffable`,
	`view == itemlog`,
	`view == iterationsManager && viewItem == crv && DevOps.isXSpace3`,
	`view == iterationsManager && viewItem == gitlab.navigate && DevOps.isXSpace3`,
	`view == iterationsManager && viewItem == iterative-information && DevOps.isXSpace3`,
	`view == iterationsManager && viewItem == merge && DevOps.isXSpace3`,
	`view == iterationsManager && viewItem == sendReview && DevOps.isXSpace3`,
	`view == iterationsManager && viewItem == setting && DevOps.isXSpace3`,
	`view == iterationsManager && viewItem =~ /def-view/ && DevOps.isXSpace3`,
	`view == iterationsManager && viewItem =~ /deploy/ && DevOps.isXSpace3`,
	`view == iterationsManager`,
	`view == jarvisVscode`,
	`view == javaProjectExplorer && !explorerResourceMoveableToTrash && viewItem =~ /java:(file|type|folder)(?=.*?\\b\\+uri\\b)/`,
	`view == javaProjectExplorer && !explorerResourceMoveableToTrash && viewItem =~ /java:(package|packageRoot)(?=.*?\\b\\+(source|resource)\\b)(?=.*?\\b\\+uri\\b)/`,
	`view == javaProjectExplorer && explorerResourceMoveableToTrash && viewItem =~ /java:(file|type|folder)(?=.*?\\b\\+uri\\b)/`,
	`view == javaProjectExplorer && explorerResourceMoveableToTrash && viewItem =~ /java:(package|packageRoot)(?=.*?\\b\\+(source|resource)\\b)(?=.*?\\b\\+uri\\b)/`,
	`view == javaProjectExplorer && java:serverMode == Standard && !java:noJavaProjects`,
	`view == javaProjectExplorer && java:serverMode == Standard && config.java.dependency.packagePresentation != flat`,
	`view == javaProjectExplorer && java:serverMode == Standard && config.java.dependency.packagePresentation == flat`,
	`view == javaProjectExplorer && java:serverMode == Standard && config.java.dependency.syncWithFolderExplorer != true`,
	`view == javaProjectExplorer && java:serverMode == Standard && config.java.dependency.syncWithFolderExplorer == true`,
	`view == javaProjectExplorer && viewItem =~ /java:(?=.*?\\b\\+uri\\b)/`,
	`view == javaProjectExplorer && viewItem =~ /java:(file|type|folder)(?=.*?\\b\\+uri\\b)/`,
	`view == javaProjectExplorer && viewItem =~ /java:(package|packageRoot)(?=.*?\\b\\+(source|resource)\\b)(?=.*?\\b\\+uri\\b)/`,
	`view == javaProjectExplorer && viewItem =~ /java:(package|packageRoot)(?=.*?\\b\\+source\\b)(?=.*?\\b\\+uri\\b)/`,
	`view == javaProjectExplorer && viewItem =~ /java:(type|package|packageRoot)(?=.*?\\b\\+uri\\b)(?=.*?\\b\\+test\\b)/`,
	`view == javaProjectExplorer && viewItem =~ /java:container(?=.*?\\b\\+maven\\b)/`,
	`view == javaProjectExplorer && viewItem =~ /java:container(?=.*?\\b\\+referencedLibrary\\b)/`,
	`view == javaProjectExplorer && viewItem =~ /java:jar(?=.*?\\b\\+referencedLibrary\\b)(?=.*?\\b\\+uri\\b)/`,
	`view == javaProjectExplorer && viewItem =~ /java:project(?=.*?\\b\\+java\\b)(?=.*?\\b\\+uri\\b)/`,
	`view == javaProjectExplorer && viewItem =~ /java:project(?=.*?\\b\\+maven|gradle\\b)(?=.*?\\b\\+uri\\b)/`,
	`view == javaProjectExplorer && viewItem =~ /java:project(?=.*?\\b\\+unmanagedFolder\\b)(?=.*?\\b\\+uri\\b)/`,
	`view == javaProjectExplorer && viewItem =~ /java:project(?=.*?\\b\\+uri\\b)(?=.*?\\b\\+(gradle)\\b)/`,
	`view == javaProjectExplorer && viewItem =~ /java:project(?=.*?\\b\\+uri\\b)(?=.*?\\b\\+(maven)\\b)/`,
	`view == javaProjectExplorer && viewItem =~ /java:workspace(?=.*?\\b\\+uri\\b)/ && java:serverMode == Standard`,
	`view == javaProjectExplorer`,
	`view == jbspaceProjects && viewItem == issue-resolved`,
	`view == jbspaceProjects && viewItem == issue-unresolved`,
	`view == jbspaceProjects && viewItem == issues`,
	`view == jbspaceProjects && viewItem == repositories`,
	`view == jbspaceProjects && viewItem == repository`,
	`view == jbspaceProjects`,
	`view == jbspaceTodos && viewItem == todo-closed`,
	`view == jbspaceTodos && viewItem == todo-open`,
	`view == jbspaceTodos`,
	`view == jefftpExplorer && viewItem == file`,
	`view == jefftpExplorer`,
	`view == jenkins-container-jobs`,
	`view == jenkins-container-nodes`,
	`view == jenkins-job`,
	`view == jenkinsExplorer`,
	`view == jestRunItTestsExplorer`,
	`view == jettyServerExplorer && viewItem && viewItem != war`,
	`view == jettyServerExplorer && viewItem == idleserver`,
	`view == jettyServerExplorer && viewItem == runningserver`,
	`view == jettyServerExplorer && viewItem == war`,
	`view == jettyServerExplorer`,
	`view == jfrog.xray && isCi`,
	`view == jfrog.xray && isLocal`,
	`view == jfrog.xray && jfrog.connection.status != signedOut`,
	`view == jfrog.xray`,
	`view == jh-datasets && viewItem == dataset-owned`,
	`view == jh-datasets && viewItem == version-container-owned`,
	`view == jh-datasets && viewItem =~ /^dataset/`,
	`view == jh-datasets`,
	`view == jh-joblist && viewItem == jh-downloadable`,
	`view == jh-joblist && viewItem == json-showable`,
	`view == jh-joblist && viewItem =~ /^job/`,
	`view == jh-joblist && viewItem =~ /job-link/`,
	`view == jh-joblist && viewItem =~ /job-results/`,
	`view == jh-joblist && viewItem =~ /job-running/`,
	`view == jh-joblist || view == jh-datasets`,
	`view == jh-joblist`,
	`view == jh-submit`,
	`view == jiraIssues && viewItem == issue`,
	`view == jiraIssues`,
	`view == jobExplorer`,
	`view == jobStatusTree`,
	`view == jobTree && viewItem == job-disabled`,
	`view == jobTree && viewItem =~ /build/`,
	`view == jobTree && viewItem =~ /inprogress/`,
	`view == jobTree && viewItem =~ /job/ && viewItem != job-disabled`,
	`view == jobTree && viewItem =~ /job/`,
	`view == jobTree && viewItem =~ /pipeline/`,
	`view == jobTree`,
	`view == jobView && viewItem == jobcompleted`,
	`view == jobView && viewItem == jobrunning`,
	`view == jobView`,
	`view == jobs-sub-view`,
	`view == joplin && viewItem != joplinNote.folder && viewItem != joplinNote.note`,
	`view == joplin && viewItem != joplinNote.folder`,
	`view == joplin && viewItem == joplinNote.folder`,
	`view == joplin`,
	`view == js.notebook.favorite`,
	`view == jsBrowserBreakpoints`,
	`view == jsExcludedCallers`,
	`view == jsonOutline`,
	`view == jsonTreeView`,
	`view == juejin.pin`,
	`view == juejin.post`,
	`view == julia-documentation`,
	`view == jupyterKernelsView && viewItem =~ /:startUsingLocalKernelSpec/`,
	`view == jupyterKernelsView && viewItem =~ /activeKernel:/`,
	`view == jupyterKernelsView`,
	`view == kafkaExplorer && viewItem == broker && !listMultiSelection`,
	`view == kafkaExplorer && viewItem == topic && !listMultiSelection`,
	`view == kafkaExplorer && viewItem == topics && !listMultiSelection`,
	`view == kafkaExplorer && viewItem =~ /^cluster$|^selectedCluster$/ && !listMultiSelection`,
	`view == kafkaExplorer && viewItem =~ /^cluster-rhosak$|^selectedCluster-rhosak$/ && !listMultiSelection`,
	`view == kafkaExplorer && viewItem =~ /^cluster.*$/ && !listMultiSelection`,
	`view == kafkaExplorer && viewItem =~ /^cluster.*$|^selectedCluster.*$|^topic$|^consumergroupitem$/ && !listMultiSelection`,
	`view == kafkaExplorer`,
	`view == karate-executions && viewItem =~ /^FeatureExecution$|^ScenarioExecution$|^ScenarioOutlineExecution$/`,
	`view == karate-executions`,
	`view == karate-network-logs && viewItem =~ /^NetworkLogPayload.*/`,
	`view == karate-network-logs && viewItem =~ /^NetworkLogPayload.+/`,
	`view == karate-network-logs && viewItem =~ /^NetworkLogRequest$|^NetworkRequestResponseLog$/`,
	`view == karate-network-logs && viewItem =~ /^NetworkLogResponse$|^NetworkRequestResponseLog$/`,
	`view == karate-network-logs`,
	`view == karate-reports && config.karateRunner.reports.activityBarDisplayType =~ /^List$|^Shallow$/`,
	`view == karate-reports && config.karateRunner.reports.activityBarDisplayType =~ /^Tree$|^Deep$/`,
	`view == karate-reports`,
	`view == karate-runtime && viewItem == karate-process`,
	`view == karate-tests && config.karateRunner.tests.activityBarDisplayType == Tag`,
	`view == karate-tests && config.karateRunner.tests.activityBarDisplayType =~ /^List$|^Shallow$/`,
	`view == karate-tests && config.karateRunner.tests.activityBarDisplayType =~ /^Tree$|^Deep$/`,
	`view == karate-tests && viewItem == testDirectory`,
	`view == karate-tests && viewItem == testFeature`,
	`view == karate-tests && viewItem == testFileIgnored`,
	`view == karate-tests && viewItem == testFile`,
	`view == karate-tests && viewItem == testScenario`,
	`view == karate-tests && viewItem == testTag`,
	`view == karate-tests`,
	`view == kbView && viewItem == toggle`,
	`view == kbView`,
	`view == kestrel.adela.view`,
	`view == kestrel.applet.view`,
	`view == kestrel.package.view && !kestrel.package.ctx.channel_stable`,
	`view == kestrel.package.view && kestrel.package.ctx.channel_stable`,
	`view == kestrel.package.view`,
	`view == keywordlist && viewItem == highlighter`,
	`view == keywordlist && viewItem == keyworditem`,
	`view == keywordlist`,
	`view == kils.callHierarchy`,
	`view == kils.inheritanceHierarchy`,
	`view == knativeEventingProjectExplorer`,
	`view == knativeFunctionProjectExplorer && viewItem == functions`,
	`view == knativeFunctionProjectExplorer && viewItem =~ /^(localDeployFunctions|deployFunctions)$/`,
	`view == knativeFunctionProjectExplorer && viewItem =~ /^(localDeployFunctions|localFunctions)$/`,
	`view == knativeFunctionProjectExplorer && viewItem =~ /^(localFunctionsEnablement|notConnectedLocalFunctionsEnablement)$/`,
	`view == knativeFunctionProjectExplorer && viewItem =~ /^(localFunctions|localDeployFunctions|localFunctionsEnablement)$/`,
	`view == knativeFunctionProjectExplorer && viewItem =~ /^(localFunctions|localDeployFunctions|localFunctionsEnablement|notConnectedLocalFunctions|notConnectedLocalFunctionsEnablement)$/`,
	`view == knativeFunctionProjectExplorer && viewItem =~ /^(localFunctions|localDeployFunctions|notConnectedLocalFunctions)$/`,
	`view == knativeFunctionProjectExplorer`,
	`view == knativeServingProjectExplorer && viewItem == service || viewItem == revision_tagged`,
	`view == knativeServingProjectExplorer && viewItem == service || viewItem =~ /revision.*/g`,
	`view == knativeServingProjectExplorer && viewItem == service_modified`,
	`view == knativeServingProjectExplorer && viewItem == service`,
	`view == knativeServingProjectExplorer && viewItem =~ /revision.*/g`,
	`view == knativeServingProjectExplorer`,
	`view == koloSidebarView && !kolo.settings.autoSelectLatestInvocation`,
	`view == koloSidebarView && kolo.settings.autoSelectLatestInvocation`,
	`view == koloSidebarView && viewItem == FrameHeader`,
	`view == koloSidebarView && viewItem == Invocation-selected`,
	`view == koloSidebarView && viewItem == Invocation`,
	`view == koloSidebarView`,
	`view == koverage && viewItem == FileCoverageNode`,
	`view == koverage`,
	`view == ksqlExplorer && viewItem == stream`,
	`view == ksqlExplorer && viewItem == table`,
	`view == kstm32.cdefs`,
	`view == kstm32.cincludes`,
	`view == kstm32.csources`,
	`view == kstm32.stdperiph`,
	`view == kubernator.treeView && viewItem =~ /\\bfolder\\b/`,
	`view == kubernator.treeView && viewItem =~ /\\bobject:PersistentVolumeClaim\\b/`,
	`view == kubernator.treeView && viewItem =~ /\\bobject:Pod\\b/`,
	`view == kubernator.treeView && viewItem =~ /\\bobject\\b/`,
	`view == kubernator.treeView && viewItem =~ /\\bobject\\b|\\bnamespace\\b/`,
	`view == kubernator.treeView`,
	`view == kubernetes.cloudExplorer && viewItem == kubernetes.noProviders`,
	`view == kubernetes.cloudExplorer && viewItem =~ /aks\\.cluster/i || view == extension.vsKubernetesExplorer && viewItem =~ /vsKubernetes\\.\\w*cluster$/i`,
	`view == kubernetes.cloudExplorer && viewItem =~ /aks\\.cluster/i`,
	`view == kubernetes.cloudExplorer && viewItem =~ /aks\\.subscription/i`,
	`view == kubernetes.cloudExplorer && viewItem =~ /kubernetes\\.providesKubeconfig/i`,
	`view == kubernetes.cloudExplorer`,
	`view == kunpeng_develope_project && isDevtoolsConfigured && !isDevtoolsLogined`,
	`view == kunpeng_develope_project && isDevtoolsConfigured && isDevtoolsLogined`,
	`view == kunpeng_develope_project && isWelcome == false`,
	`view == kunpeng_develope_project && isZh`,
	`view == kunpeng_develope_project`,
	`view == kustoExplorer && viewItem == cluster`,
	`view == kustoExplorer && viewItem == database && kusto.useProposedApi`,
	`view == kustoExplorer && viewItem == database`,
	`view == kustoExplorer && viewItem == table && kusto.useProposedApi`,
	`view == kustoExplorer && viewItem == table`,
	`view == kustoExplorer`,
	`view == l13DiffFavorites && viewItem == favoriteGroup`,
	`view == l13DiffFavorites && viewItem == subfavorite`,
	`view == l13DiffFavorites && viewItem =~ /favorite$/`,
	`view == l13DiffFavorites`,
	`view == l13DiffHistory && viewItem =~ /^history(-file|-folder)?$/`,
	`view == l13DiffHistory && viewItem =~ /^history(-folder)?$/`,
	`view == l13DiffHistory`,
	`view == l13ProjectsFavorites && viewItem == favoriteGroup`,
	`view == l13ProjectsFavorites && viewItem =~ /^(current-)?(favorite|subfavorite)-(folders?)$/`,
	`view == l13ProjectsFavorites && viewItem =~ /^(current-)?(favorite|subfavorite)-(folders?|git|subfolder|vscode|workspace)$/`,
	`view == l13ProjectsFavorites && viewItem =~ /^(current-)?subfavorite-(folders?|git|subfolder|vscode|workspace)$/`,
	`view == l13ProjectsFavorites`,
	`view == l13ProjectsTags`,
	`view == l13ProjectsWorkspaces && !config.security.workspace.trust.enabled && viewItem =~ /^(current-)?(sub)?project-folders?$/`,
	`view == l13ProjectsWorkspaces && config.security.workspace.trust.enabled && isWorkspaceTrusted && viewItem =~ /^current-(sub)?project-folders?$/`,
	`view == l13ProjectsWorkspaces && viewItem == color-picker`,
	`view == l13ProjectsWorkspaces && viewItem == tagGroup`,
	`view == l13ProjectsWorkspaces && viewItem == workspaceGroup`,
	`view == l13ProjectsWorkspaces && viewItem =~ /^((current-)?(project|subproject)-(git|subfolder|vscode|workspace)|unknown-project)$/`,
	`view == l13ProjectsWorkspaces && viewItem =~ /^(current-)?(project|subproject)-(folders?)$/`,
	`view == l13ProjectsWorkspaces && viewItem =~ /^(current-)?(project|subproject)-(folders?|git|subfolder|vscode|workspace)$/`,
	`view == l13ProjectsWorkspaces && viewItem =~ /^(current-)?subproject-(folders?|git|subfolder|vscode|workspace)$/`,
	`view == l13ProjectsWorkspaces && viewItem =~ /^(favorite|project|subfavorite|subproject)-(folder|git|subfolder|vscode)$/`,
	`view == l13ProjectsWorkspaces`,
	`view == lab50`,
	`view == lando-info && viewItem == link`,
	`view == lando-info && viewItem == service`,
	`view == lando-info && viewItem == string`,
	`view == lando-info || view == lando-list`,
	`view == lando-info`,
	`view == lando-list && viewItem == app`,
	`view == lando-list && viewItem == link`,
	`view == lando-list && viewItem == string`,
	`view == lando-list`,
	`view == lark-ide-view && viewItem == compileMode`,
	`view == lark-ide-view`,
	`view == latex-structure`,
	`view == latex-workshop-structure`,
	`view == launchdarklyFeatureFlags && viewItem == flagParentItem`,
	`view == launchdarklyFeatureFlags && viewItem == flagViewBrowser || view == launchdarklyMetrics && viewItem == metricViewBrowser`,
	`view == launchdarklyFeatureFlags && viewItem == rollout`,
	`view == launchdarklyFeatureFlags && viewItem == variationDefault`,
	`view == launchdarklyFeatureFlags && viewItem == variationOff`,
	`view == launchdarklyFeatureFlags`,
	`view == launchdarklyFlagList && viewItem == child`,
	`view == launchdarklyFlagList && viewItem == flagParentItem`,
	`view == launchdarklyFlagList`,
	`view == launchdarklyMetrics && viewItem == metricParentItem`,
	`view == layouts_list && editorIsOpen`,
	`view == layouts_list && viewItem != child`,
	`view == layouts_list && viewItem == child`,
	`view == layouts_list`,
	`view == ldap-explorer-view-bookmarks`,
	`view == ldap-explorer-view-cacerts || view == ldap-explorer-view-connections || view == ldap-explorer-view-tree || view == ldap-explorer-view-bookmarks`,
	`view == ldap-explorer-view-cacerts || view == ldap-explorer-view-tree || view == ldap-explorer-view-bookmarks`,
	`view == ldap-explorer-view-cacerts`,
	`view == ldap-explorer-view-connections`,
	`view == ldap-explorer-view-tree`,
	`view == leekFundView.binance  && viewItem != category`,
	`view == leekFundView.binance`,
	`view == leekFundView.forex`,
	`view == leekFundView.fund && viewItem != category  && viewItem!=nodata`,
	`view == leekFundView.fund && viewItem != category`,
	`view == leekFundView.fund && viewItem == category`,
	`view == leekFundView.fund`,
	`view == leekFundView.news`,
	`view == leekFundView.stock  && viewItem != category && viewItem!=nodata`,
	`view == leekFundView.stock  && viewItem != category`,
	`view == leekFundView.stock && viewItem != category  && viewItem!=nodata`,
	`view == leekFundView.stock && viewItem != category && viewItem!=nodata`,
	`view == leekFundView.stock`,
	`view == leetCodeExplorer && viewItem == problem-favorite`,
	`view == leetCodeExplorer && viewItem == problem`,
	`view == leetCodeExplorer && viewItem =~ /problem*/`,
	`view == leetCodeExplorer`,
	`view == liberty-dev`,
	`view == libraryExample`,
	`view == libraryListView && code-for-ibmi:hasProfiles != true`,
	`view == libraryListView && viewItem == currentLibrary`,
	`view == libraryListView && viewItem == library`,
	`view == libraryListView`,
	`view == liferayServerExplorer && viewItem && viewItem == Started || viewItem == Stopped`,
	`view == liferayServerExplorer && viewItem && viewItem == Started`,
	`view == liferayServerExplorer && viewItem && viewItem == Stopped`,
	`view == liferayServerExplorer && viewItem`,
	`view == liferayServerExplorer`,
	`view == linuxDevCmdView`,
	`view == lispPackages && viewItem == export`,
	`view == lispPackages && viewItem == package`,
	`view == lispPackages`,
	`view == lispRepl`,
	`view == lispThreads`,
	`view == live-server-virtual-files`,
	`view == live-server-websocket-clients`,
	`view == live-sync-for-aws-cloud9-view-1-environments && viewItem == connectedEnvironment`,
	`view == live-sync-for-aws-cloud9-view-1-environments && viewItem == connectingEnvironment`,
	`view == live-sync-for-aws-cloud9-view-1-environments && viewItem == disconnectedEnvironment`,
	`view == live-sync-for-aws-cloud9-view-1-environments`,
	`view == live-sync-for-aws-cloud9-view-3-chat`,
	`view == live-workspace-remote && viewItem == file`,
	`view == live-workspace-remote`,
	`view == liveHost && viewItem == hostItem0`,
	`view == liveHost && viewItem == hostItem1`,
	`view == liveHost && viewItem =~ /hostItem/i`,
	`view == liveHost`,
	`view == liveshare.contacts && liveshare:hasAvailableContacts`,
	`view == liveshare.contacts && liveshare:hasPublishPresenceProviders && liveshare:status == available`,
	`view == liveshare.contacts && liveshare:hasPublishPresenceProviders && liveshare:status == away`,
	`view == liveshare.contacts && liveshare:hasPublishPresenceProviders && liveshare:status == busy`,
	`view == liveshare.contacts && liveshare:hasPublishPresenceProviders && liveshare:status == doNotDisturb`,
	`view == liveshare.contacts && liveshare:presenceSignedStatus == signedOut`,
	`view == liveshare.contacts && liveshare:presenceSignedStatus == signingIn`,
	`view == liveshare.devtools`,
	`view == liveshare.session  && viewItem =~ /^participants.guest$|^participants.guest.followed$|participants.guest.elevated$|^participants.guest.followed.elevated/`,
	`view == liveshare.session && !liveshare:isReadOnlySession && viewItem =~ /^participants.guest.anonymous$|^participants.guest.followed.anonymous$/`,
	`view == liveshare.session && !liveshare:isReadOnlySession && viewItem =~ /^participants.guest.elevated.anonymous$|^participants.guest.followed.elevated.anonymous$/`,
	`view == liveshare.session && liveshare:hasCollaborators`,
	`view == liveshare.session && liveshare:isReadOnlySession && viewItem =~ /^participants.guest$|^participants.guest.followed$|^participants.guest.anonymous$|^participants.guest.followed.anonymous$/`,
	`view == liveshare.session && liveshare:isReadOnlySession && viewItem =~ /^participants.guest.elevated$|^participants.guest.followed.elevated$|^participants.guest.elevated.anonymous$|^participants.guest.followed.elevated.anonymous$/`,
	`view == liveshare.session && liveshare:state != Shared && liveshare:state != SharingInProgress && liveshare:state != JoiningInProgress && liveshare:state != Joined`,
	`view == liveshare.session && liveshare:state == Joined`,
	`view == liveshare.session && liveshare:state == Shared && viewItem == servers`,
	`view == liveshare.session && liveshare:state == Shared && viewItem == terminals`,
	`view == liveshare.session && liveshare:state == Shared`,
	`view == liveshare.session && viewItem == Whiteboard`,
	`view == liveshare.session && viewItem == contacts.invited`,
	`view == liveshare.session && viewItem == mobTimer`,
	`view == liveshare.session && viewItem == servers.local`,
	`view == liveshare.session && viewItem == servers.remote`,
	`view == liveshare.session && viewItem == terminals.local.readonly && !liveshare:isReadOnlySession && !liveshare:readOnlyTerminalsEnforced`,
	`view == liveshare.session && viewItem == terminals.local.readonly`,
	`view == liveshare.session && viewItem == terminals.local.readwrite && !liveshare:isReadOnlySession`,
	`view == liveshare.session && viewItem == terminals.local.readwrite`,
	`view == liveshare.session && viewItem == terminals.remote`,
	`view == liveshare.session && viewItem =~ /(participants.participant|participants.guest)(?!.followed)/`,
	`view == liveshare.session && viewItem =~ /participants.guest/`,
	`view == liveshare.session && viewItem =~ /participants.participant.followed|participants.guest.followed/`,
	`view == liveshare.session && viewItem =~ /participants.participant|participants.guest/`,
	`view == liveshare.session.explorer && !liveshare:isReadOnlySession && viewItem =~ /^participants.guest.anonymous$|^participants.guest.followed.anonymous$/`,
	`view == liveshare.session.explorer && !liveshare:isReadOnlySession && viewItem =~ /^participants.guest.elevated.anonymous$|^participants.guest.followed.elevated.anonymous$/`,
	`view == liveshare.session.explorer && liveshare:hasCollaborators`,
	`view == liveshare.session.explorer && liveshare:isReadOnlySession && viewItem =~ /^participants.guest$|^participants.guest.followed$|^participants.guest.anonymous$|^participants.guest.followed.anonymous$/`,
	`view == liveshare.session.explorer && liveshare:isReadOnlySession && viewItem =~ /^participants.guest.elevated$|^participants.guest.followed.elevated$|^participants.guest.elevated.anonymous$|^participants.guest.followed.elevated.anonymous$/`,
	`view == liveshare.session.explorer && liveshare:state != Shared && liveshare:state != SharingInProgress && liveshare:state != JoiningInProgress && liveshare:state != Joined`,
	`view == liveshare.session.explorer && liveshare:state == Joined`,
	`view == liveshare.session.explorer && liveshare:state == Shared && viewItem == servers`,
	`view == liveshare.session.explorer && liveshare:state == Shared && viewItem == terminals`,
	`view == liveshare.session.explorer && liveshare:state == Shared`,
	`view == liveshare.session.explorer && viewItem == contacts.invited`,
	`view == liveshare.session.explorer && viewItem == servers.local`,
	`view == liveshare.session.explorer && viewItem == servers.remote`,
	`view == liveshare.session.explorer && viewItem == terminals.local.readonly && !liveshare:isReadOnlySession && !liveshare:readOnlyTerminalsEnforced`,
	`view == liveshare.session.explorer && viewItem == terminals.local.readonly`,
	`view == liveshare.session.explorer && viewItem == terminals.local.readwrite && !liveshare:isReadOnlySession`,
	`view == liveshare.session.explorer && viewItem == terminals.local.readwrite`,
	`view == liveshare.session.explorer && viewItem == terminals.remote`,
	`view == liveshare.session.explorer && viewItem =~ /(participants.participant|participants.guest)(?!.followed)/`,
	`view == liveshare.session.explorer && viewItem =~ /^participants.guest$|^participants.guest.followed$|participants.guest.elevated$|^participants.guest.followed.elevated/`,
	`view == liveshare.session.explorer && viewItem =~ /participants.guest/`,
	`view == liveshare.session.explorer && viewItem =~ /participants.participant.followed|participants.guest.followed/`,
	`view == liveshare.session.explorer && viewItem =~ /participants.participant|participants.guest/`,
	`view == liveshare.spaces && viewItem =~ /^space/`,
	`view == local-supergraph-designs && viewItem == operationSummaryTreeItem`,
	`view == local-supergraph-designs && viewItem == operationTreeItem`,
	`view == local-supergraph-designs && viewItem == subgraphSummaryTreeItem`,
	`view == local-supergraph-designs && viewItem == subgraphTreeItem`,
	`view == local-supergraph-designs && viewItem == supergraphApiSchemaTreeItem`,
	`view == local-supergraph-designs && viewItem == supergraphSchemaTreeItem`,
	`view == local-supergraph-designs`,
	`view == localCiJobs && viewItem == isRunning`,
	`view == localCiJobs`,
	`view == localCiLicense`,
	`view == localHistoryDiffBrowser && !config.local-history.browser.descending`,
	`view == localHistoryDiffBrowser && config.local-history.browser.descending`,
	`view == localHistoryDiffBrowser && viewItem == commitDiffItem`,
	`view == localHistoryDiffBrowser && viewItem == patchDiffItem`,
	`view == localHistoryFileBrowser`,
	`view == local`,
	`view == locatorDependencies && viewItem == cc-locator`,
	`view == locatorDependencies && viewItem == cc-store-server`,
	`view == locatorDependencies && viewItem == cc-store`,
	`view == locatorDependencies || view == serverLocatorDependencies`,
	`view == locatorDependencies`,
	`view == locators && ChromeEnabled && viewItem == element`,
	`view == locators && WebdriverEnabled && viewItem == element`,
	`view == locators`,
	`view == logExplorer`,
	`view == logView && fileOps.running`,
	`view == logView && updating.running`,
	`view == logView`,
	`view == logpointExplorer && viewItem == logpoint`,
	`view == logpointExplorer`,
	`view == logsExplorer && viewItem != rel`,
	`view == luatide-activity-project && viewItem == ActivityTreeItemShow`,
	`view == luatide-activity-project`,
	`view == luatide-history-project`,
	`view == lumenNode && viewItem == Lumen-Development-Session`,
	`view == luoguExplorer && viewItem == problem`,
	`view == luoguExplorer`,
	`view == lytics.accounts.explorer && viewItem == 'account'`,
	`view == lytics.accounts.explorer`,
	`view == lytics.campaigns.explorer && viewItem == 'campaign'`,
	`view == lytics.campaigns.explorer && viewItem == 'campaignVariation'`,
	`view == lytics.campaigns.explorer`,
	`view == lytics.queries.explorer && viewItem == 'query'`,
	`view == lytics.queries.explorer && viewItem == 'table'`,
	`view == lytics.queries.explorer`,
	`view == lytics.segmentcollections.explorer && viewItem == 'segment'`,
	`view == lytics.segmentcollections.explorer && viewItem == 'segmentcollection'`,
	`view == lytics.segmentml.explorer && viewItem == 'segmentml'`,
	`view == lytics.segmentml.explorer`,
	`view == lytics.segments.explorer && viewItem == 'segment'`,
	`view == lytics.segments.explorer`,
	`view == lytics.settings.explorer && viewItem == 'account-setting'`,
	`view == lytics.settings.explorer && viewItem == 'account-setting-editable'`,
	`view == lytics.settings.explorer`,
	`view == lytics.streams.explorer && viewItem == 'field'`,
	`view == lytics.streams.explorer && viewItem == 'stream'`,
	`view == lytics.streams.explorer`,
	`view == lytics.subscriptions.explorer && viewItem == 'subscription'`,
	`view == lytics.subscriptions.explorer`,
	`view == lytics.tables.explorer && viewItem == 'field'`,
	`view == lytics.tables.explorer && viewItem == 'field-identifier'`,
	`view == lytics.tables.explorer && viewItem == 'table'`,
	`view == lytics.tables.explorer`,
	`view == lytics.topics.explorer && viewItem == 'topic'`,
	`view == lytics.topics.explorer`,
	`view == m5stack && viewItem == COM`,
	`view == m5stack && viewItem == file`,
	`view == m5stack && viewItem == folder`,
	`view == machineList&&viewItem != openFolder`,
	`view == machineList&&viewItem == customFile`,
	`view == machineList&&viewItem == openFolder`,
	`view == machineList`,
	`view == main-view || view == filter-view`,
	`view == mainTreeView && viewItem == commits`,
	`view == mainTreeView && viewItem == highlightCommit`,
	`view == mainTreeView && viewItem == selectedCommit`,
	`view == make && viewItem == MakefileTarget`,
	`view == make && viewItem == Makefile`,
	`view == makeHidden.ViewPane.hiddenItems`,
	`view == make`,
	`view == makefile.outline && makefile:localDebugFeature`,
	`view == makefile.outline && makefile:localRunFeature`,
	`view == makefile.outline && viewItem =~ /nodeType=buildTarget/`,
	`view == makefile.outline && viewItem =~ /nodeType=configuration/`,
	`view == makefile.outline && viewItem =~ /nodeType=launchTarget/`,
	`view == makefile.outline`,
	`view == malloyConnections`,
	`view == malloySchema && viewItem == query`,
	`view == malloySchema`,
	`view == mapperExplorer && viewItem == mapper`,
	`view == mapperExplorer`,
	`view == maquetteDataOutline && viewItem == maquette-entity`,
	`view == maquetteDataOutline && viewItem == maquette-scene`,
	`view == maquetteDataOutline`,
	`view == maquetteFilesOutline && viewItem == file`,
	`view == maquetteFilesOutline`,
	`view == markdownTags`,
	`view == marquee && viewItem == LinkedSnippet`,
	`view == marquee && viewItem == LinkedTodo`,
	`view == marquee && viewItem == notesHeadline`,
	`view == marquee && viewItem == snippetsHeadline`,
	`view == marquee && viewItem == todosHeadline`,
	`view == marquee && viewItem =~ /Note/`,
	`view == marquee && viewItem =~ /Snippet|LinkedSnippet/`,
	`view == marquee && viewItem =~ /Todo|LinkedTodo/`,
	`view == marquee`,
	`view == materialDesignIconsExplorer && viewItem == mdiIcon`,
	`view == materialDesignIconsExplorer && viewItem == mdiSearch`,
	`view == materialDesignIconsExplorer`,
	`view == maudit-explorer`,
	`view == mavenProjects && config.maven.view != flat`,
	`view == mavenProjects && config.maven.view != hierarchical`,
	`view == mavenProjects && javaLSReady`,
	`view == mavenProjects && viewItem == maven:dependenciesMenu`,
	`view == mavenProjects && viewItem == maven:favoritesMenu`,
	`view == mavenProjects && viewItem == maven:favorites`,
	`view == mavenProjects && viewItem == maven:lifecycle`,
	`view == mavenProjects && viewItem == maven:menu`,
	`view == mavenProjects && viewItem == maven:pluginGoal`,
	`view == mavenProjects && viewItem == maven:plugin`,
	`view == mavenProjects && viewItem == maven:project`,
	`view == mavenProjects && viewItem =~ /maven:dependency(?!.*?\\b\\+root\\b)/`,
	`view == mavenProjects && viewItem =~ /maven:dependency(?=.*?\\b\\+conflict\\b)/`,
	`view == mavenProjects && viewItem =~ /maven:dependency/`,
	`view == mavenProjects`,
	`view == maxPlus`,
	`view == mbaasTree`,
	`view == mdocProjects && viewItem == project`,
	`view == mdocProjects && viewItem == search`,
	`view == mdocProjects && viewItem =~ /^docNode$|^project$/)`,
	`view == mdocProjects`,
	`view == memo && viewItem == memoFile`,
	`view == memo`,
	`view == meson-project && viewItem == meson-target`,
	`view == meson-project`,
	`view == metadata && viewItem == component`,
	`view == metadata && viewItem =~ /(folder|type)/`,
	`view == metadata && viewItem =~ /(type|component|folder)/`,
	`view == metadata`,
	`view == metalevelReferences && viewItem == file`,
	`view == metalevelReferences && viewItem == folder`,
	`view == metalevelReferences && viewItem == source`,
	`view == metalsPackages && viewItem == project`,
	`view == metalsPackages`,
	`view == microbit-explorer-view && viewItem == MicrobitFile`,
	`view == microbit-explorer-view`,
	`view == microsoft-todo-unoffcial.listView && !starFilter`,
	`view == microsoft-todo-unoffcial.listView && starFilter`,
	`view == microsoft-todo-unoffcial.listView`,
	`view == microsoft-todo-unoffcial.taskDetailsView`,
	`view == midway && viewItem == decorator`,
	`view == midway && viewItem == faasEvent`,
	`view == midway && viewItem == faasFun`,
	`view == midway && viewItem == faas`,
	`view == midway && viewItem == findEnv`,
	`view == midway`,
	`view == minimizedGroups && viewItem == editorDocument`,
	`view == minimizedGroups && viewItem == editorGroup`,
	`view == mme2kPowerApps && (viewItem == 'PowerAppsAPI-OAuth 2.0' || viewItem == Solution`,
	`view == mme2kPowerApps && (viewItem == Environment || viewItem == Solution)`,
	`view == mme2kPowerApps && viewItem == CanvasApp`,
	`view == mme2kPowerApps && viewItem == CloudFlow`,
	`view == mme2kPowerApps && viewItem == Connector`,
	`view == mme2kPowerApps && viewItem == PowerApp`,
	`view == mme2kPowerApps && viewItem == Solution`,
	`view == mme2kPowerApps`,
	`view == mobioos-forge`,
	`view == mocha&&!runAutoPlay`,
	`view == mocha&&runAutoPlay`,
	`view == mocha&&viewItem==rootTests`,
	`view == mocha&&viewItem==testDescriber`,
	`view == mocha&&viewItem==testItem`,
	`view == mocha`,
	`view == model_test_treeview && viewItem != source`,
	`view == model_test_treeview`,
	`view == moduleExplorer && viewItem == graphListContext`,
	`view == moduleExplorer && viewItem == hubItemContext`,
	`view == moduleExplorer && viewItem == remoteAdapterListContext`,
	`view == moduleExplorer && viewItem == remoteDeviceAdapterContext`,
	`view == moduleExplorer && viewItem == topologyListContext`,
	`view == moduleExplorer && viewItem =~ /^InstanceItemContext.*$/`,
	`view == moduleExplorer && viewItem =~ /^graphItemContext.*$/`,
	`view == moduleExplorer && viewItem =~ /^livePipelineItemContext.*$/`,
	`view == moduleExplorer && viewItem =~ /^topologyItemContext.*$/`,
	`view == moduleExplorerView && viewItem == unlicensed`,
	`view == moduleExplorer`,
	`view == moduleView && viewItem == uninstall`,
	`view == moduleView && viewItem == update`,
	`view == moduleView`,
	`view == modulesTreeView`,
	`view == mongoDBConnectionExplorer && viewItem == collectionTreeItem`,
	`view == mongoDBConnectionExplorer && viewItem == connectedConnectionTreeItem`,
	`view == mongoDBConnectionExplorer && viewItem == databaseTreeItem`,
	`view == mongoDBConnectionExplorer && viewItem == disconnectedConnectionTreeItem`,
	`view == mongoDBConnectionExplorer && viewItem == documentListTreeItem`,
	`view == mongoDBConnectionExplorer && viewItem == documentTreeItem`,
	`view == mongoDBConnectionExplorer && viewItem == fieldTreeItem`,
	`view == mongoDBConnectionExplorer && viewItem == indexListTreeItem`,
	`view == mongoDBConnectionExplorer && viewItem == schemaTreeItem`,
	`view == mongoDBConnectionExplorer`,
	`view == mongoDBPlaygroundsExplorer && viewItem == playgroundsTreeItem`,
	`view == mongoDBPlaygroundsExplorer`,
	`view == mongoExplorer && viewItem == collection`,
	`view == mongoExplorer && viewItem == database`,
	`view == mongoExplorer && viewItem == databases`,
	`view == mongoExplorer && viewItem == document`,
	`view == mongoExplorer && viewItem == inspector`,
	`view == mongoExplorer`,
	`view == mongoRunner`,
	`view == monorepoDependencies && viewItem == dependency`,
	`view == monorepoDependencies && viewItem == root`,
	`view == mqttProfiles`,
	`view == ms-edge-launcher-list`,
	`view == msg.connections && viewItem == connectionMySQL`,
	`view == msg.connections && viewItem == mrsDbObject`,
	`view == msg.connections && viewItem == mrsSchema`,
	`view == msg.connections && viewItem == mrsService`,
	`view == msg.connections && viewItem == mrs`,
	`view == msg.connections && viewItem == schemaItemMySQL && !showsModalDialog`,
	`view == msg.connections && viewItem =~ /^connection.*/`,
	`view == msg.connections && viewItem =~ /^schema(Table|View|Routine)?Item.*/`,
	`view == msg.connections && viewItem =~ /^schemaEventItem.*/`,
	`view == msg.connections && viewItem =~ /^schemaItem.*/ && !showsModalDialog`,
	`view == msg.connections && viewItem =~ /^schemaRoutineItem.*/`,
	`view == msg.connections && viewItem =~ /^schemaTableItem.*/`,
	`view == msg.connections && viewItem =~ /^schemaTriggerItem.*/`,
	`view == msg.connections && viewItem =~ /^schemaViewItem.*/`,
	`view == msg.connections`,
	`view == msg.consoles && viewItem == session`,
	`view == msg.consoles`,
	`view == msg.oci && viewItem == mdsBastion`,
	`view == msg.oci && viewItem == mdsCompartment`,
	`view == msg.oci && viewItem == mdsComputeInstance`,
	`view == msg.oci && viewItem == mdsConfigProfile`,
	`view == msg.oci && viewItem == mdsLoadBalancer`,
	`view == msg.oci`,
	`view == msg.scripts && viewItem == script`,
	`view == mtlAPIs && viewItem == api`,
	`view == mtlProject`,
	`view == mtlScript && viewItem == file`,
	`view == mtlScript`,
	`view == mwbTable && viewItem == table`,
	`view == mwbTable`,
	`view == my_info`,
	`view == my_repo`,
	`view == mysql && viewItem == connection`,
	`view == mysql && viewItem == database`,
	`view == mysql && viewItem == table`,
	`view == mysql`,
	`view == navigation && viewItem == matches || view == navigation && viewItem == matchline || view == navigation && viewItem == recentline`,
	`view == nbaTreeView && viewItem == singleMatch`,
	`view == nbaTreeView`,
	`view == neo-visual-devtracker.rpcServerExplorer && viewItem == expressNodeMulti`,
	`view == neo-visual-devtracker.rpcServerExplorer && viewItem == expressNode`,
	`view == neo-visual-devtracker.rpcServerExplorer && viewItem == url`,
	`view == neo-visual-devtracker.rpcServerExplorer`,
	`view == neo3-visual-devtracker.views.blockchains && viewItem != express`,
	`view == neo3-visual-devtracker.views.blockchains && viewItem == express`,
	`view == neo3-visual-devtracker.views.blockchains`,
	`view == neo3-visual-devtracker.views.contracts`,
	`view == neo3-visual-devtracker.views.wallets`,
	`view == netsuitesdf && viewItem == sdf-file`,
	`view == netsuitesdf && viewItem == sdf-object-folder`,
	`view == netsuitesdf && viewItem == sdf-object`,
	`view == news-hack-top-stories`,
	`view == nexusExplorer`,
	`view == nga-custom && viewItem == dir`,
	`view == nga-custom`,
	`view == ngin.databases  && viewItem =~ /^((?!folder).)*$/`,
	`view == ngin.databases && viewItem =~ /.*canChangeProduct.*/ && viewItem =~ /.*connectionEnabled.*/`,
	`view == ngin.databases && viewItem =~ /.*connectionEnabled.*/`,
	`view == ngin.databases && viewItem =~ /.*folder.*/`,
	`view == ngin.databases && viewItem =~ /.*ufsTreeVisible.*/ && viewItem =~ /.*connectionEnabled.*/`,
	`view == ngin.databases && viewItem =~ /^((?!canChangeProduct).)*$/ && viewItem =~ /.*connectionEnabled.*/`,
	`view == ngin.databases && viewItem =~ /^((?!folder).)*$/`,
	`view == ngin.databases && viewItem =~ /^((?!ufsTreeVisible).)*$/ && viewItem =~ /.*connectionEnabled.*/`,
	`view == ngin.databases`,
	`view == ngin.scratchpads`,
	`view == ngin.variables`,
	`view == ngrx.explorer`,
	`view == ni-control-panel && isWindows`,
	`view == ni-control-panel && viewItem == nativeimage.agent`,
	`view == ni-control-panel && viewItem == nativeimage.configure`,
	`view == ni-control-panel && viewItem == nativeimage.outputDir`,
	`view == ninja-rangeExplorer && ninja-rangeExplorer.hasCollapsed == true`,
	`view == ninja-rangeExplorer && ninja-rangeExplorer.hasExpanded == true`,
	`view == ninja-rangeExplorer && viewItem =~ /,ninja-consolidate-ranges,/`,
	`view == ninja-rangeExplorer && viewItem =~ /,ninja-copy-ranges,/`,
	`view == ninja-rangeExplorer && viewItem =~ /,ninja-goto-def,/`,
	`view == ninja-rangeExplorer && viewItem =~ /,ninja-sync,/`,
	`view == ninja-rangeExplorer`,
	`view == ninjaDebug`,
	`view == ninjaFavourites`,
	`view == ninjaRelease`,
	`view == node-modules-viewer && viewItem == dependency`,
	`view == node-modules-viewer && viewItem == file`,
	`view == node-modules-viewer`,
	`view == nodeDependencies && viewItem == dependencies`,
	`view == nodeDependencies && viewItem == dependency`,
	`view == nodeDependencies && viewItem == devDependencies`,
	`view == nodeDependencies && viewItem == outdatedDependency`,
	`view == nodeDependencies1 && viewItem == dependency`,
	`view == nodeDependencies1`,
	`view == nodeDependencies2 && viewItem == dependency`,
	`view == nodeDependencies2`,
	`view == nodeDependencies3 && viewItem == dependency`,
	`view == nodeDependencies3`,
	`view == nodeDependencies4 && viewItem == dependency`,
	`view == nodeDependencies4`,
	`view == nodeDependencies5 && viewItem == dependency`,
	`view == nodeDependencies5`,
	`view == nodeDependencies`,
	`view == nodeProjects && viewItem == projectFolder`,
	`view == nodeProjects && viewItem == project`,
	`view == nodeTree && viewItem == executor-active`,
	`view == nodeTree && viewItem == node-enabled`,
	`view == nodeTree && viewItem =~ /node/ && viewItem != node-disconnected`,
	`view == nodeTree && viewItem =~ /node/ && viewItem != node-enabled`,
	`view == nodeTree && viewItem =~ /node/`,
	`view == nodeTree`,
	`view == nodemcu-tools.devices && viewItem == nodemcu-device-connected`,
	`view == nodemcu-tools.devices && viewItem == nodemcu-device`,
	`view == nodemcu-tools.devices && viewItem =~ /^nodemcu-file/`,
	`view == nodemcu-tools.devices`,
	`view == nodes && viewItem == node`,
	`view == nodes`,
	`view == normTree`,
	`view == nosql-schema && viewItem == schema`,
	`view == nosql-schema && viewItem == table`,
	`view == nosql-schema`,
	`view == notebookTags`,
	`view == notes && viewItem == note`,
	`view == notesView && viewItem == note`,
	`view == notesView`,
	`view == notes`,
	`view == notion.todo`,
	`view == now-ref-view.tree`,
	`view == nowExplorer && viewItem == aliasNode`,
	`view == nowExplorer && viewItem == deploymentNode`,
	`view == nowExplorer`,
	`view == npmExplorer && viewItem == dependency`,
	`view == npmExplorer && viewItem == npmTask`,
	`view == npmExplorer`,
	`view == npmScripts && viewItem == script`,
	`view == npmScripts`,
	`view == nrf-connect.activeAppView && viewItem =~ /^nrf-connect.image.*/`,
	`view == nrf-connect.activeConfView`,
	`view == nrf-connect.allAppsView && viewItem == nrf-connect.app`,
	`view == nrf-connect.allAppsView`,
	`view == nrf-connect.devicesView && viewItem == noDevices`,
	`view == nrf-connect.devicesView && viewItem =~ /^nrf-connect.device.*/`,
	`view == nrf-connect.devicesView`,
	`view == nrf-connect.dtsTreeView`,
	`view == nrf-terminal-view && !nordic-semiconductor.nrf-terminal:running && nordic-semiconductor.nrf-terminal:prev-config`,
	`view == nrf-terminal-view && !nordic-semiconductor.nrf-terminal:running`,
	`view == nrf-terminal-view && nordic-semiconductor.nrf-terminal:connected-in-session`,
	`view == nrf-terminal-view && nordic-semiconductor.nrf-terminal:running`,
	`view == nrf-terminal-view`,
	`view == ns-news`,
	`view == nvidia-gpus && !config.nvidia-smi-plus.refresh.autoRefresh`,
	`view == nvidia-gpus && config.nvidia-smi-plus.refresh.autoRefresh`,
	`view == nvidia-gpus`,
	`view == nxCloud`,
	`view == nxCommands`,
	`view == nxHelpAndFeedback && viewItem == connectToNxCloud && isNxWorkspace`,
	`view == nxProjects && viewItem != target`,
	`view == nxProjects && viewItem == project && isNxWorkspace`,
	`view == nxProjects && viewItem == project || viewItem == target`,
	`view == nxProjects && viewItem == target`,
	`view == nxProjects`,
	`view == nxRunTarget`,
	`view == nxtApiView`,
	`view == objectBrowser && viewItem == filter && workspaceFolderCount >= 1`,
	`view == objectBrowser && viewItem == filter`,
	`view == objectBrowser && viewItem == member`,
	`view == objectBrowser && viewItem =~ /^object/`,
	`view == objectBrowser`,
	`view == objectExplorer && viewItem != Folder`,
	`view == objectExplorer && viewItem != disconnectedServer`,
	`view == objectExplorer && viewItem == Server`,
	`view == objectExplorer && viewItem =~ /^(AggregateFunction|PartitionFunction|ScalarValuedFunction|StoredProcedure|TableValuedFunction|View)$/`,
	`view == objectExplorer && viewItem =~ /^(AggregateFunction|PartitionFunction|ScalarValuedFunction|TableValuedFunction)$/`,
	`view == objectExplorer && viewItem =~ /^(Database)$/`,
	`view == objectExplorer && viewItem =~ /^(Database|Table)$/`,
	`view == objectExplorer && viewItem =~ /^(Database|Table|View|AggregateFunction|PartitionFunction|ScalarValuedFunction|StoredProcedure|TableValuedFunction)$/`,
	`view == objectExplorer && viewItem =~ /^(Server)$/`,
	`view == objectExplorer && viewItem =~ /^(Server|Database|Table|View|AggregateFunction|PartitionFunction|ScalarValuedFunction|StoredProcedure|TableValuedFunction)$/`,
	`view == objectExplorer && viewItem =~ /^(Server|Table)$/`,
	`view == objectExplorer && viewItem =~ /^(StoredProcedure)$/`,
	`view == objectExplorer && viewItem =~ /^(Table)$/`,
	`view == objectExplorer && viewItem =~ /^(Table|View)$/`,
	`view == objectExplorer && viewItem =~ /^(Table|View|AggregateFunction|PartitionFunction|ScalarValuedFunction|Schema|StoredProcedure|TableValuedFunction|User|UserDefinedTableType)$/`,
	`view == objectExplorer && viewItem =~ /^(Table|View|AggregateFunction|PartitionFunction|ScalarValuedFunction|Schema|StoredProcedure|TableValuedFunction|User|UserDefinedTableType|Trigger|DatabaseTrigger|Index|Key|User|DatabaseRole|ApplicationRole)$/`,
	`view == objectExplorer && viewItem =~ /^(Table|View|AggregateFunction|PartitionFunction|ScalarValuedFunction|StoredProcedure|TableValuedFunction)$/`,
	`view == objectExplorer && viewItem =~ /^(User)$/`,
	`view == objectExplorer && viewItem =~ /^(View)$/`,
	`view == objectExplorer && viewItem =~ /^(View|AggregateFunction|ScalarValuedFunction|StoredProcedure|TableValuedFunction|Trigger)$/`,
	`view == objectExplorer && viewItem =~ /^(View|StoredProcedure|AggregateFunction|ScalarValuedFunction|TableValuedFunction)$/`,
	`view == objectExplorer && viewItem =~ /^(disconnectedServer|Server)$/`,
	`view == objectExplorer && viewItem =~ /^(disconnectedServer|Server|Database)$/`,
	`view == objectExplorer && viewItem =~ /^Table$|^View$/`,
	`view == objectExplorer`,
	`view == objects`,
	`view == ocaml-sandbox && viewItem == package-with-doc`,
	`view == ocaml-sandbox`,
	`view == ocaml-switches && viewItem == opam-switch`,
	`view == ocaml-switches && viewItem == package-with-doc`,
	`view == ocaml-switches`,
	`view == ociimagesview`,
	`view == oclOutline`,
	`view == odslRepository && viewItem =~ /^item:.*$/`,
	`view == odslRepository && viewItem =~ /^service:.*$/`,
	`view == oipide && viewItem == API`,
	`view == oipide && viewItem == Method`,
	`view == oipide && viewItem == Version`,
	`view == oipide`,
	`view == omi.view.github`,
	`view == onlineDevicesView`,
	`view == ooxmlViewer && viewItem == file`,
	`view == ooxmlViewer`,
	`view == openFiles && viewItem == file`,
	`view == openFiles && viewItem == group`,
	`view == openFiles`,
	`view == openXmlExplorer && viewItem == openxml`,
	`view == openXmlExplorer`,
	`view == openapi && viewItem == 'openapi'`,
	`view == openapi`,
	`view == openhabItems && viewItem != statelessItem && viewItem != statelessGroup`,
	`view == openhabItems`,
	`view == openhabThings && viewItem == thing`,
	`view == openhabThings`,
	`view == openshiftComponentTypesView && viewItem == devfileRegistry && viewItem != DefaultdevfileRegistry`,
	`view == openshiftComponentTypesView && viewItem == devfileRegistry`,
	`view == openshiftComponentTypesView`,
	`view == openshiftComponentsView && !config.openshiftToolkit.experimentalFeatures`,
	`view == openshiftComponentsView && config.openshiftToolkit.experimentalFeatures && viewItem =~ /openshift\\.component.*\\.dev-nrn.*/`,
	`view == openshiftComponentsView && config.openshiftToolkit.experimentalFeatures`,
	`view == openshiftComponentsView && viewItem =~ /openshift\\.component.*/`,
	`view == openshiftComponentsView && viewItem =~ /openshift\\.component.*\\.deb-nrn.*/ && viewItem =~ /openshift\\.component.*\\.dev-run.*/`,
	`view == openshiftComponentsView && viewItem =~ /openshift\\.component.*\\.dep-nrn.*/ || viewItem =~ /openshift\\.component.*\\.dep-run.*/`,
	`view == openshiftComponentsView && viewItem =~ /openshift\\.component.*\\.dev-nrn.*/`,
	`view == openshiftComponentsView && viewItem =~ /openshift\\.component.*\\.dev-run.*/`,
	`view == openshiftComponentsView && viewItem =~ /openshift\\.component.*\\.dev-stp.*/`,
	`view == openshiftComponentsView`,
	`view == openshiftProjectExplorer && isVSCode == 1`,
	`view == openshiftProjectExplorer && viewItem == componentNoContext || viewItem == component || viewItem == componentNotPushed || viewItem == componentOther`,
	`view == openshiftProjectExplorer && viewItem == loginRequired`,
	`view == openshiftProjectExplorer && viewItem == openshift.k8sContext && isLoggedIn`,
	`view == openshiftProjectExplorer && viewItem == openshift.k8sContext || viewItem == openshift.project`,
	`view == openshiftProjectExplorer && viewItem == openshift.k8sContext`,
	`view == openshiftProjectExplorer && viewItem == openshift.k8sObject`,
	`view == openshiftProjectExplorer && viewItem == openshift.openConfigFile`,
	`view == openshiftProjectExplorer && viewItem == openshift.project`,
	`view == openshiftProjectExplorer && viewItem == service`,
	`view == openshiftProjectExplorer && viewItem =~ /openshift\\.component.*/ || viewItem == componentNotPushed`,
	`view == openshiftProjectExplorer`,
	`view == oracleBookmarkExplorer && viewItem =~ /(bookmarkItemOne|firstBookmarkItem|middleBookmarkItem|lastBookmarkItem)/`,
	`view == oracleBookmarkExplorer && viewItem =~ /(middleBookMarkGroup|firstBookmarkGroup)/`,
	`view == oracleBookmarkExplorer && viewItem =~ /(middleBookMarkGroup|lastBookmarkGroup)/`,
	`view == oracleBookmarkExplorer && viewItem =~ /(middleBookmarkItem|firstBookmarkItem)/`,
	`view == oracleBookmarkExplorer && viewItem =~ /(middleBookmarkItem|lastBookmarkItem)/`,
	`view == oracleBookmarkExplorer`,
	`view == oracleCloudExplorer  && viewItem == ociWorkloadItem`,
	`view == oracleCloudExplorer && viewItem == ociDedicatedDatabaseItem`,
	`view == oracleCloudExplorer && viewItem == ociNonDedicatedDatabaseItem`,
	`view == oracleCloudExplorer && viewItem == ociRootNodeItem`,
	`view == oracleCloudExplorer && viewItem =~ /(ociNonDedicatedDatabaseItem|ociDedicatedDatabaseItem)/ && dbAddConnectionPresent`,
	`view == oracleCloudExplorer && viewItem =~ /(ociNonDedicatedDatabaseItem|ociDedicatedDatabaseItem)/`,
	`view == oracleCloudExplorer`,
	`view == oracleDBObjectExplorer && viewItem == Function`,
	`view == oracleDBObjectExplorer && viewItem == PackageBodyPrivateMethod`,
	`view == oracleDBObjectExplorer && viewItem == PackageMethod`,
	`view == oracleDBObjectExplorer && viewItem == Package`,
	`view == oracleDBObjectExplorer && viewItem == Procedure`,
	`view == oracleDBObjectExplorer && viewItem =~ /(DisconnectedConnection|ErroredConnection)/`,
	`view == oracleDBObjectExplorer && viewItem =~ /ConnectedConnection/`,
	`view == oracleDBObjectExplorer && viewItem =~ /Connection/ && dbAddConnectionPresent`,
	`view == oracleDBObjectExplorer && viewItem =~ /Connection/`,
	`view == oracleDBObjectExplorer && viewItem =~ /^(ConnectedConnection|DisconnectedConnection|ErroredConnection)/`,
	`view == oracleDBObjectExplorer && viewItem =~ /^(DefaultConnectedConnection|DefaultDisconnectedConnection|DefaultErroredConnection)/`,
	`view == oracleDBObjectExplorer && viewItem =~ /^(Package|PackageBody)$/`,
	`view == oracleDBObjectExplorer && viewItem =~ /^(Procedure|Function|PackageMethod)$/`,
	`view == oracleDBObjectExplorer && viewItem =~ /^(Procedure|Function|Package|PackageBody|Trigger)$/`,
	`view == oracleDBObjectExplorer && viewItem =~ /^(Procedure|Function|Trigger)$/`,
	`view == oracleDBObjectExplorer && viewItem =~ /^(Relational Table)$/`,
	`view == oracleDBObjectExplorer && viewItem =~ /^(Relational Table|Object Table|XML Table|Relational View|Object View|XML View|Materialized View)$/`,
	`view == oracleDBObjectExplorer && viewItem =~ /^(Relational Table|Object Table|XML Table|Relational View|Object View|XML View|Materialized View|Procedure|Function|Package|Synonym)$/`,
	`view == oracleDBObjectExplorer && viewItem =~ /^(Relational Table|Object Table|XML Table|Relational View|Object View|XML View|Materialized View|Synonym)$/`,
	`view == oracleDBObjectExplorer`,
	`view == oracleQueryHistoryExplorer && viewItem == HistoryItem`,
	`view == oracleQueryHistoryExplorer`,
	`view == org-list`,
	`view == outline-map-view && outline-map.defaultMaxDepthSet`,
	`view == outline-map-view && outline-map.pin-status == 1`,
	`view == outline-map-view && outline-map.pin-status == 2`,
	`view == outline-map-view && outline-map.pin-status == 3`,
	`view == outline`,
	`view == outlinerBenchmarksView && viewItem == symbol`,
	`view == outlinerBenchmarksView`,
	`view == outlinerTestsView && viewItem == symbol`,
	`view == outlinerTestsView`,
	`view == outputView`,
	`view == overview && config.findUnusedExports.debug`,
	`view == overview && viewItem == folder`,
	`view == overview`,
	`view == p42.assistant`,
	`view == p5-projects-view`,
	`view == p5sketchExplorer && viewItem == directory`,
	`view == p5sketchExplorer && viewItem == library`,
	`view == p5sketchExplorer && viewItem =~ /^file$|^sketch/`,
	`view == p5sketchExplorer && viewItem =~ /^sketch/ && viewItem != sketch:html`,
	`view == p5sketchExplorer && viewItem =~ /^sketch/ && viewItem != sketch:script`,
	`view == p5sketchExplorer && viewItem =~ /^sketch/`,
	`view == p5sketchExplorer`,
	`view == package-apiView`,
	`view == package-udpView || view == package-colorview || view == package-color02view || || view == package-color03view`,
	`view == package-udpView`,
	`view == packageDependencies && viewItem != local`,
	`view == packageDependencies && viewItem == editing`,
	`view == packageDependencies && viewItem == local`,
	`view == packageDependencies && viewItem == remote`,
	`view == packageDependencies`,
	`view == packageEditor && fast-sfdc-active && fast-sfdc-configured && viewItem != downloading`,
	`view == packageEditor && fast-sfdc-active && fast-sfdc-configured`,
	`view == packageExplorer`,
	`view == packageMetrics.views.dependencyTree:explorer &&  packageMetrics:packageJsonActive`,
	`view == packagePanel`,
	`view == packages && viewItem == package`,
	`view == packagesView && viewItem == package`,
	`view == packagesView`,
	`view == packages`,
	`view == pageView && WebdriverEnabled && viewItem == element`,
	`view == pageView && viewItem == element || viewItem == file`,
	`view == pageView && viewItem == file`,
	`view == pageView`,
	`view == pages && viewItem == page`,
	`view == pages`,
	`view == papyrus-projects && config.papyrus.fallout4.enabled`,
	`view == papyrus-projects && config.papyrus.skyrim.enabled`,
	`view == papyrus-projects && config.papyrus.skyrimSpecialEdition.enabled`,
	`view == papyrus-projects`,
	`view == parallelExplorer && viewItem == parallel.item.component`,
	`view == parallelExplorer`,
	`view == parallelFolders && viewItem == dependency`,
	`view == parallelFolders`,
	`view == parametersView`,
	`view == parent_model_treeview && viewItem != source`,
	`view == parent_model_treeview`,
	`view == payaraMicroExplorer || view == payaraMicro`,
	`view == payaraServerExplorer || view == payaraServer`,
	`view == pddl.tests.explorer && viewItem != test`,
	`view == pddl.tests.explorer && viewItem == manifest`,
	`view == pddl.tests.explorer && viewItem == test`,
	`view == pddl.tests.explorer`,
	`view == pendingInstaSnapshots`,
	`view == perfPanelbeforeConfigured && !isPerfadvisorConfigured`,
	`view == perfPanelbeforeConfigured && isPerfadvisorConfigured`,
	`view == perfPanelbeforeConfigured`,
	`view == perfadvisorTools && !isPerfadvisorConfigured`,
	`view == perfadvisorTools && isPerfadvisorConfigured`,
	`view == perfadvisorTools`,
	`view == perforce.searchChangelists && viewItem == fileFilter`,
	`view == perforce.searchChangelists && viewItem == fileFilters`,
	`view == perforce.searchChangelists && viewItem == filterItem-val`,
	`view == perforce.searchChangelists && viewItem == filterRoot`,
	`view == perforce.searchChangelists && viewItem == results-pinned`,
	`view == perforce.searchChangelists && viewItem == results-unpinned`,
	`view == perforce.searchChangelists && viewItem == searchNow-auto`,
	`view == perforce.searchChangelists && viewItem == searchNow-manual`,
	`view == perforce.searchChangelists && viewItem =~ /fileResult/ && viewItem =~ /diffable/`,
	`view == perforce.searchChangelists && viewItem =~ /fileResult/ && viewItem =~ /openable/`,
	`view == perforce.searchChangelists && viewItem =~ /results/`,
	`view == peripherals && viewItem == register || viewItem == bit`,
	`view == petsView`,
	`view == picassoTools && viewItem == picassoToolItem.run`,
	`view == picassoTools && viewItem == picassoToolItem.stop`,
	`view == picassoTools`,
	`view == pingcode-work-items && viewItem != do-not-show`,
	`view == pingcode-work-items`,
	`view == pinned-stats`,
	`view == pinnedSites && viewItem == site`,
	`view == pinnedSites`,
	`view == pip-manager-installed && viewItem == canUpdate`,
	`view == pip-manager-installed`,
	`view == pipeline && viewItem == pipeline`,
	`view == pipelineTree && viewItem == pipeline-linked`,
	`view == pipelineTree && viewItem == pipeline`,
	`view == pipelineTree`,
	`view == pipeline`,
	`view == pipelines && viewItem == config || viewItem == params || viewItem == script`,
	`view == pipelines && viewItem == config`,
	`view == pipelines && viewItem == running`,
	`view == pipelines && viewItem == stopped`,
	`view == pipelines`,
	`view == piral-available-commands`,
	`view == piral-workspace-info && viewItem == showFullContextPlugin`,
	`view == piral-workspace-info && viewItem == showWebLinkContextPlugin`,
	`view == piral-workspace-info`,
	`view == pivotaly.view.currentAndBacklog`,
	`view == pivotaly.view.membercycle`,
	`view == pivotaly.view.storyinfo`,
	`view == plan-graphs-explorer`,
	`view == planemo-config && viewItem == directoryItem`,
	`view == planemo-config`,
	`view == platformExplorer && viewItem == apiFilter`,
	`view == platformExplorer && viewItem == api`,
	`view == platformExplorer && viewItem == collectionFilter`,
	`view == platformExplorer && viewItem == collection`,
	`view == platformExplorer && viewItem == collections`,
	`view == platformExplorer && viewItem == favoriteApiFilter`,
	`view == platformExplorer && viewItem == favoriteCollection`,
	`view == platformExplorer && viewItem =~ /^collection$|^api$|^favoriteCollection$/`,
	`view == platformExplorer && viewItem =~ /^collection$|^favoriteCollection$/`,
	`view == platformExplorer`,
	`view == platformio-debug.memory`,
	`view == platformio-debug.peripherals && viewItem == field`,
	`view == platformio-debug.peripherals && viewItem == registerRO`,
	`view == platformio-debug.peripherals && viewItem == registerRW`,
	`view == platformio-debug.peripherals && viewItem == registerWO`,
	`view == platformio-debug.peripherals`,
	`view == platformio-debug.registers && viewItem == field`,
	`view == platformio-debug.registers && viewItem == register`,
	`view == platformio-debug.registers`,
	`view == playfabExplorer && viewItem == studio`,
	`view == playfabExplorer && viewItem == title`,
	`view == playfabExplorer`,
	`view == plugins`,
	`view == pm2-processes`,
	`view == policyEvaluation`,
	`view == ponicodeIgnoredFiles && viewItem == file`,
	`view == ponicodeTreeView && !ponicodeShowGitDiff:active`,
	`view == ponicodeTreeView && !ponicodeTreeView:active`,
	`view == ponicodeTreeView && !ponicodeTreeView:tree`,
	`view == ponicodeTreeView && ponicodeShowGitDiff:active`,
	`view == ponicodeTreeView && ponicodeTreeView:active`,
	`view == ponicodeTreeView && ponicodeTreeView:tree`,
	`view == ponicodeTreeView && viewItem == file`,
	`view == ponicodeTreeView && viewItem == function`,
	`view == portingTools && !isCloudIDE && !isRunInWeb`,
	`view == portingTools && isZhCn`,
	`view == portingTools && isportconfigured && isportlogined && !isPortAdmin`,
	`view == portingTools && isportconfigured && isportlogined && !isRunInWeb`,
	`view == portingTools && isportconfigured && isportlogined && isPortAdmin && !isRunInWeb`,
	`view == portingTools && isportconfigured && isportlogined && isPortAdmin`,
	`view == portingTools && isportconfigured && isportlogined &&!isRunInWeb`,
	`view == portingTools && isportconfigured && isportlogined`,
	`view == portingTools && viewItem == depReportF || viewItem == depReportS && !isCloudIDE`,
	`view == portingTools && viewItem == depReportF || viewItem == depReportS`,
	`view == portingTools && viewItem == depReportS`,
	`view == portingTools && viewItem == pkgRebuildF || viewItem == pkgRebuildS && !isCloudIDE`,
	`view == portingTools && viewItem == pkgRebuildF || viewItem == pkgRebuildS`,
	`view == portingTools && viewItem == pkgRebuildS`,
	`view == portingTools && viewItem == portReportS || viewItem == portReportF`,
	`view == portingTools && viewItem == portReportS`,
	`view == portingTools && viewItem == portSource && hasportreports`,
	`view == portingTools && viewItem == portSource`,
	`view == portingTools && viewItem == portingAppraise && hasdepreports`,
	`view == portingTools && viewItem == portingAppraise`,
	`view == portingTools && viewItem == portingPrecheck`,
	`view == portingTools && viewItem == softBuild && hassoftwarepkgreports`,
	`view == portingTools && viewItem == softBuild`,
	`view == portingTools`,
	`view == pos-list-sidebar && viewItem == posItem`,
	`view == pos-list-sidebar`,
	`view == postTweets`,
	`view == postgres && viewItem == postgres.tree.column`,
	`view == postgres && viewItem == postgres.tree.connection`,
	`view == postgres && viewItem == postgres.tree.database`,
	`view == postgres && viewItem == postgres.tree.function-folder`,
	`view == postgres && viewItem == postgres.tree.schema`,
	`view == postgres && viewItem == postgres.tree.table`,
	`view == postgres && viewItem == vscode-postgres.tree.column`,
	`view == postgres && viewItem == vscode-postgres.tree.connection`,
	`view == postgres && viewItem == vscode-postgres.tree.database`,
	`view == postgres && viewItem == vscode-postgres.tree.function-folder`,
	`view == postgres && viewItem == vscode-postgres.tree.function`,
	`view == postgres && viewItem == vscode-postgres.tree.schema`,
	`view == postgres && viewItem == vscode-postgres.tree.table`,
	`view == postgres`,
	`view == postmanCollectionExplorer && viewItem == collection`,
	`view == postmanCollectionExplorer && viewItem == request`,
	`view == postmanCollectionExplorer`,
	`view == postmanTestExplorer`,
	`view == ppCLICommands && viewItem == cli-command`,
	`view == ppToolBox`,
	`view == pr:github && viewItem == query`,
	`view == pr:github && viewItem =~ /description/`,
	`view == pr:github && viewItem =~ /pullrequest(.*):notification/`,
	`view == pr:github && viewItem =~ /pullrequest(:local)?:active|description/`,
	`view == pr:github && viewItem =~ /pullrequest(:local)?:nonactive|description/`,
	`view == pr:github && viewItem =~ /pullrequest:local:nonactive/`,
	`view == pr:github && viewItem =~ /pullrequest|description/`,
	`view == prStatus:github && !fileListLayout:flat`,
	`view == prStatus:github && fileListLayout:flat`,
	`view == prStatus:github && viewItem =~ /commit/`,
	`view == prStatus:github`,
	`view == prevalidationchanges && viewItem == stagedcontainer`,
	`view == prevalidationchanges && viewItem == stageditem`,
	`view == prevalidationchanges && viewItem == unstagedcontainer`,
	`view == prevalidationchanges && viewItem == unstageditem`,
	`view == prevalidationchanges`,
	`view == prevalidations && viewItem == prevalidation`,
	`view == prevalidations`,
	`view == privateExtensions && viewItem == extension-not-installed`,
	`view == privateExtensions && viewItem == extension-update-available`,
	`view == privateExtensions.extensions && viewItem == registry.user`,
	`view == privateExtensions.extensions && workbenchState != empty`,
	`view == privateExtensions.extensions`,
	`view == privateExtensions.recommended && workbenchState != empty`,
	`view == privateExtensions`,
	`view == processManagerTreeView`,
	`view == profilesView && viewItem == profile`,
	`view == profilesView`,
	`view == programBreakpointView && viewItem == programBreakpoint`,
	`view == programBreakpointView`,
	`view == project-urls`,
	`view == projectHome && viewItem == app`,
	`view == projectHome&& viewItem == app`,
	`view == projectHome&& viewItem == h5`,
	`view == projectHome&& viewItem == jsn`,
	`view == projectHome&& viewItem == miniapp`,
	`view == projectHome&& viewItem == mp`,
	`view == projectHome&& viewItem == vue`,
	`view == projectHome`,
	`view == projectManager && viewItem == project`,
	`view == projectManager`,
	`view == projectReferences`,
	`view == project`,
	`view == projects.views.activity_bar.all && viewItem == group`,
	`view == projects.views.activity_bar.all && viewItem == project`,
	`view == projects.views.activity_bar.all`,
	`view == projects.views.explorer.all && viewItem == group`,
	`view == projects.views.explorer.all && viewItem == project`,
	`view == projects.views.explorer.all`,
	`view == projectsExplorerAny`,
	`view == projectsExplorerFavorites && !projectManager.viewAsList`,
	`view == projectsExplorerFavorites && projectManager.hiddenGitWelcome`,
	`view == projectsExplorerFavorites && projectManager.viewAsList`,
	`view == projectsExplorerFavorites && viewItem == ProjectNodeKind`,
	`view == projectsExplorerFavorites`,
	`view == projectsExplorerGit`,
	`view == projectsExplorerMercurial`,
	`view == projectsExplorerSVN`,
	`view == projectsExplorerVSCode`,
	`view == proof-explorer-view && viewItem != root`,
	`view == proof-explorer-view && viewItem == root`,
	`view == proof-explorer-view`,
	`view == proof-mate-view && viewItem != proofmate-hint && viewItem != proofmate-hints && viewItem != proofmate-sketchpad && viewItem != proofmate-label`,
	`view == proof-mate-view && viewItem != proofmate-hint && viewItem != proofmate-hints && viewItem != proofmate-sketchpad`,
	`view == proof-mate-view && viewItem != proofmate-hint && viewItem != proofmate-hints`,
	`view == proof-mate-view && viewItem == proofmate-clip && viewItem !== proofmate-sketchpad`,
	`view == proof-mate-view && viewItem == proofmate-clip`,
	`view == proof-mate-view && viewItem == proofmate-label`,
	`view == proof-mate-view && viewItem == proofmate-sketchpad || view == proof-mate-view && viewItem == proofmate-label`,
	`view == proof-mate-view && viewItem == proofmate-sketchpad`,
	`view == proof-mate-view && viewItem == proofmate-suggestion`,
	`view == proof-mate-view`,
	`view == propertyExplorer && viewItem == edit`,
	`view == propertyExplorer`,
	`view == propertyList`,
	`view == protoOutline && viewItem == 2`,
	`view == protoOutline`,
	`view == protos && viewItem == proto`,
	`view == protos`,
	`view == providerView && viewItem == providerContainer`,
	`view == providerView && viewItem == providerItem`,
	`view == providerView`,
	`view == proviewtree`,
	`view == pttTree && viewItem == board`,
	`view == pttTree`,
	`view == pubspec.view && !dart_sharp.pubCancel`,
	`view == pubspec.view && dart_sharp.pubCancel`,
	`view == pubspecManager`,
	`view == pullRequestReviewPanel && !isDraftPullRequest`,
	`view == pullRequestReviewPanel && viewItem == addOptionalReviewer`,
	`view == pullRequestReviewPanel && viewItem == addRequiredReviewer`,
	`view == pullRequestReviewPanel && viewItem == addWorkItem`,
	`view == pullRequestReviewPanel && viewItem == createdBy && !isAbandonedPullRequest && !isCompletedPullRequest`,
	`view == pullRequestReviewPanel && viewItem == createdBy && !isDraftPullRequest && !isAbandonedPullRequest  && !isCompletedPullRequest`,
	`view == pullRequestReviewPanel && viewItem == createdBy && !isDraftPullRequest && !isAbandonedPullRequest && !isCompletedPullRequest`,
	`view == pullRequestReviewPanel && viewItem == createdBy && isAbandonedPullRequest`,
	`view == pullRequestReviewPanel && viewItem == createdBy && isDraftPullRequest && !isAbandonedPullRequest`,
	`view == pullRequestReviewPanel && viewItem == removeReviewer`,
	`view == pullRequestReviewPanel && viewItem == removeWorkItem`,
	`view == pullRequestReviewPanel`,
	`view == pullRequestsExplorer`,
	`view == puppetFacts`,
	`view == puppetfile`,
	`view == pymakr-devices-tree`,
	`view == pymakr-projects-tree && viewItem =~ /project$/`,
	`view == pymakr-projects-tree`,
	`view == pytest`,
	`view == pythonDebugImageWatch && viewItem =~ /svifpd:.*-expressionItem/`,
	`view == pythonDebugImageWatch && viewItem =~ /svifpd:nonTrackedVariable-.*/`,
	`view == pythonDebugImageWatch && viewItem =~ /svifpd:nonTrackedVariable-.*?image.*/ || view == pythonDebugImageWatch && viewItem =~ /svifpd:trackedVariable-.*?image.*/`,
	`view == pythonDebugImageWatch && viewItem =~ /svifpd:nonTrackedVariable-.*?plot.*/ || view == pythonDebugImageWatch && viewItem =~ /svifpd:trackedVariable-.*?plot.*/`,
	`view == pythonDebugImageWatch && viewItem =~ /svifpd:nonTrackedVariable-.*?tensor.*/ || view == pythonDebugImageWatch && viewItem =~ /svifpd:trackedVariable-.*?tensor.*/`,
	`view == pythonDebugImageWatch && viewItem =~ /svifpd:trackedVariable-.*/`,
	`view == pythonDebugImageWatch`,
	`view == pythonEnvironments && python.envManager.pythonIsNotInstalled`,
	`view == pythonEnvironments && viewItem =~ /^env:/`,
	`view == pythonEnvironments && viewItem =~ /^env:canBeDeleted:/`,
	`view == pythonEnvironments && viewItem =~ /^envType:canCreate:/`,
	`view == pythonEnvironments`,
	`view == python_tests && !busyTests`,
	`view == python_tests && busyTests`,
	`view == python_tests && discoveringTests`,
	`view == python_tests && hasFailedTests && !busyTests`,
	`view == python_tests && viewItem == testFile && !busyTests`,
	`view == python_tests && viewItem == testFile`,
	`view == python_tests && viewItem == testFunction && !busyTests`,
	`view == python_tests && viewItem == testFunction`,
	`view == python_tests && viewItem == testSuite && !busyTests`,
	`view == python_tests && viewItem == testSuite`,
	`view == python_tests`,
	`view == q-explorer && viewItem == qdict`,
	`view == q-explorer && viewItem == qfunction`,
	`view == q-explorer && viewItem == qtable`,
	`view == q-explorer && viewItem == qvar`,
	`view == q-explorer`,
	`view == q-servers && viewItem == qconn`,
	`view == q-servers`,
	`view == qbs-project && qbs:extension-activated`,
	`view == qc-sidebar-not-commited`,
	`view == qpProjectTree`,
	`view == qtaTreeView && viewItem == pyfile`,
	`view == qtaTreeView && viewItem == testcase`,
	`view == qtaTreeView`,
	`view == quarto-assist && !quarto.assistView.isPinned`,
	`view == quarto-assist && quarto.assistView.isPinned`,
	`view == query-history && viewItem == history`,
	`view == queryHistory && config.mssql.enableQueryHistoryFeature && !config.mssql.enableQueryHistoryCapture`,
	`view == queryHistory && config.mssql.enableQueryHistoryFeature && config.mssql.enableQueryHistoryCapture`,
	`view == queryHistory && viewItem == queryHistoryNode`,
	`view == queryHistoryProvider && viewItem =~ /WithFailure/`,
	`view == queryHistoryProvider && viewItem =~ /WithSummary/`,
	`view == queryHistoryProvider && viewItem =~ /^QueryHistory(?!StoredProcedure)/`,
	`view == queryHistoryProvider`,
	`view == queryHistory`,
	`view == questions && viewItem == memo`,
	`view == questions`,
	`view == queueTree`,
	`view == quickEntries && viewItem == quickEntry`,
	`view == quickRunPanel`,
	`view == quickScriptView && viewItem == quickscript`,
	`view == quickScriptView`,
	`view == quickSshExplorer && viewItem == sshnode`,
	`view == qunit-test-results`,
	`view == rHelpPages && viewItem =~ /_QUICKPICK_/`,
	`view == rHelpPages && viewItem =~ /_addToFavorites_/`,
	`view == rHelpPages && viewItem =~ /_clearCache_/`,
	`view == rHelpPages && viewItem =~ /_filterPackages_/`,
	`view == rHelpPages && viewItem =~ /_installPackages_/`,
	`view == rHelpPages && viewItem =~ /_openInNewPanel_/`,
	`view == rHelpPages && viewItem =~ /_removeFromFavorites_/`,
	`view == rHelpPages && viewItem =~ /_removePackage_/`,
	`view == rHelpPages && viewItem =~ /_searchPackage_/`,
	`view == rHelpPages && viewItem =~ /_showAllPackages_/`,
	`view == rHelpPages && viewItem =~ /_showOnlyFavorites_/`,
	`view == rHelpPages && viewItem =~ /_summarizeTopics_/`,
	`view == rHelpPages && viewItem =~ /_unsummarizeTopics_/`,
	`view == rHelpPages && viewItem =~ /_updateInstalledPackages_/`,
	`view == rHelpPages && viewItem =~ /_updatePackage_/`,
	`view == rHelpPages`,
	`view == rLiveShare && r.liveShare:aborted`,
	`view == rLiveShare`,
	`view == rabbitmq.connections && viewItem == connected`,
	`view == rabbitmq.connections && viewItem == not-connected`,
	`view == rabbitmq.connections`,
	`view == rabbitmq.exchanges && viewItem != error`,
	`view == rabbitmq.exchanges`,
	`view == rabbitmq.queues && viewItem != error`,
	`view == rabbitmq.queues`,
	`view == rad4-project-manager && viewItem == configuration`,
	`view == rad4-project-manager && viewItem == package`,
	`view == rad4-project-manager && viewItem == project || viewItem ==  package || viewItem == configuration`,
	`view == rad4-project-manager && viewItem == project || viewItem == package || viewItem == configuration`,
	`view == rad4-project-manager && viewItem == project`,
	`view == rad4-project-manager`,
	`view == rank`,
	`view == ratings`,
	`view == raycast && viewItem == argument`,
	`view == raycast && viewItem == command-arguments`,
	`view == raycast && viewItem == command-preferences`,
	`view == raycast && viewItem == command`,
	`view == raycast && viewItem == commands`,
	`view == raycast && viewItem == interval`,
	`view == raycast && viewItem == mode`,
	`view == raycast && viewItem == preference`,
	`view == raycast && viewItem == preferences`,
	`view == raycast`,
	`view == rdsDataView && viewItem == rdsData || viewItem == rdsDataTable || viewItem == rdsDataColumn`,
	`view == rdsDataView && viewItem == rdsDataTable`,
	`view == rdsDataView && viewItem == rdsData`,
	`view == rdsDataView || view == rdsFileView`,
	`view == rdsDataView`,
	`view == rdsFileView  && viewItem == rdsFolder || viewItem == rdsDrive`,
	`view == rdsFileView && viewItem == rdsFile_local || viewItem == rdsFile_remote`,
	`view == rdsFileView && viewItem == rdsFolder`,
	`view == rdsFileView`,
	`view == reactHierarchy && viewItem == node`,
	`view == reactHierarchy`,
	`view == readhubBlockchainList`,
	`view == readhubNewsList`,
	`view == readhubTechNewsList`,
	`view == readhubTopicList`,
	`view == recentTasksView  && viewItem =~ /^(pinned)?debugTask(WithArgs)?WithTerminals.*$|^(pinned)?task(WithArgs)?WithTerminals.*$|^(pinned)?runningTask(WithArgs)?WithTerminals.*$|^(pinned)?runningDebugTask(WithArgs)?WithTerminals.*$/`,
	`view == recentTasksView && viewItem =~ /^(pinned)?debugTask(WithArgs)?WithTerminals.*$|^(pinned)?task(WithArgs)?WithTerminals.*$|^(pinned)?runningTask(WithArgs)?WithTerminals.*$|^(pinned)?runningDebugTask(WithArgs)?WithTerminals.*$/`,
	`view == recentTasksView && viewItem =~ /^debugTask.*$|^task.*$/`,
	`view == recentTasksView`,
	`view == redis-item && viewItem == redisNode`,
	`view == redis-item && viewItem == redisServerNode`,
	`view == redis-item`,
	`view == redisExplorer && viewItem == redisNode`,
	`view == redisExplorer`,
	`view == redisTree && viewItem == connection`,
	`view == redisTree && viewItem == db`,
	`view == redisTree && viewItem == group || viewItem == consumer`,
	`view == redisTree && viewItem == key`,
	`view == redisTree`,
	`view == redisXplorer && viewItem == redisNode`,
	`view == redisXplorer && viewItem == redisServerNode`,
	`view == redisXplorer`,
	`view == references-view.tree && reference-list.hasResult && reference-list.source == beanHierarchy && beanHierarchy:direction != dependencies`,
	`view == references-view.tree && reference-list.hasResult && reference-list.source == beanHierarchy && beanHierarchy:direction != injectedInto`,
	`view == references-view.tree && reference-list.hasResult && reference-list.source == javaTypeHierarchy && typeHierarchySymbolKind != 10`,
	`view == references-view.tree && reference-list.hasResult && reference-list.source == javaTypeHierarchy && viewItem != 'false'`,
	`view == references-view.tree && reference-list.hasResult && reference-list.source == javaTypeHierarchy`,
	`view == reflectionView && viewItem == type`,
	`view == reflectionView`,
	`view == refreshView`,
	`view == regionViewer`,
	`view == registeredCommands`,
	`view == registers && viewItem == Cycles`,
	`view == registers && viewItem == register`,
	`view == remix-controls`,
	`view == remix-plugins`,
	`view == remoteDevice`,
	`view == remoteExplorer && viewItem != file`,
	`view == remoteExplorer && viewItem != folder`,
	`view == remoteExplorer && viewItem != root && !listMultiSelection`,
	`view == remoteExplorer && viewItem != root && viewItem != file && !listMultiSelection`,
	`view == remoteExplorer && viewItem != root && viewItem == file && !listMultiSelection`,
	`view == remoteExplorer && viewItem != root && viewItem == file`,
	`view == remoteExplorer && viewItem != root`,
	`view == remoteExplorer && viewItem == file && !config.sftp.downloadWhenOpenInRemoteExplorer`,
	`view == remoteExplorer && viewItem == file && config.sftp.downloadWhenOpenInRemoteExplorer`,
	`view == remoteExplorer && viewItem == root`,
	`view == remoteExplorer`,
	`view == remoteHub.views.workspaceRepositories`,
	`view == remoteTargets`,
	`view == remote`,
	`view == removed-list`,
	`view == replHistory && viewItem == evalText`,
	`view == replHistory`,
	`view == repoExplorer`,
	`view == repolog && viewItem == commit`,
	`view == repolog && viewItem == diffable`,
	`view == repolog && viewItem == userrepo`,
	`view == repolog && viewItem =~ /.*repo/`,
	`view == repolog`,
	`view == repositories && viewItem == build`,
	`view == requestGroup`,
	`view == requested-list || view == used-by-list || view == affected-list`,
	`view == result-tags`,
	`view == results`,
	`view == resync && viewItem == comment`,
	`view == resync`,
	`view == rethinkdbexplorerconnections && viewItem == server`,
	`view == rethinkdbexplorerconnections`,
	`view == rethinkdbexplorerdbviewer && viewItem == table`,
	`view == rethinkdbexplorerdbviewer`,
	`view == rethinkdbexplorerhistory && viewItem == previousQueryHeader`,
	`view == rethinkdbexplorerhistory`,
	`view == rhamtExplorerView && viewItem == report`,
	`view == rhamtExplorerView && viewItem == results`,
	`view == rhamtExplorerView && viewItem =~ /hasQuickfix/i`,
	`view == rhamtExplorerView && viewItem =~ /hasReport/i`,
	`view == rhamtExplorerView && viewItem =~ /hasResults-isActive/i`,
	`view == rhamtExplorerView && viewItem =~ /hasResults-notActive/i`,
	`view == rhamtExplorerView && viewItem =~ /isComplete/i`,
	`view == rhamtExplorerView && viewItem =~ /isIncomplete/i`,
	`view == rhamtExplorerView && viewItem =~ /issue/i`,
	`view == rhamtExplorerView && viewItem =~ /quickfix-container/i`,
	`view == rhamtExplorerView && viewItem =~ /rhamtConfiguration/i`,
	`view == rhamtExplorerView`,
	`view == rhinoDocumentation`,
	`view == rhinoPipelines`,
	`view == rhinoScripts`,
	`view == rmxPlugins && viewItem == options`,
	`view == rmxPlugins`,
	`view == roadrunnerTasks && viewItem == activeTask`,
	`view == roadrunnerTasks && viewItem == task`,
	`view == roadrunnerTasks`,
	`view == robocorp-cloud-tree && viewItem == cloudLoginItem`,
	`view == robocorp-cloud-tree && viewItem == cloudLogoutItem`,
	`view == robocorp-cloud-tree && viewItem == vaultConnected`,
	`view == robocorp-cloud-tree && viewItem == vaultDisconnected`,
	`view == robocorp-resources-tree && robocorp-code:single-robot-selected && viewItem == inputWorkItemDir`,
	`view == robocorp-resources-tree && robocorp-code:single-robot-selected && viewItem == locatorEntry`,
	`view == robocorp-resources-tree && robocorp-code:single-robot-selected && viewItem == newBrowserLocator`,
	`view == robocorp-resources-tree && robocorp-code:single-robot-selected && viewItem == newImageLocator`,
	`view == robocorp-resources-tree && robocorp-code:single-robot-selected && viewItem == newWebRecorder`,
	`view == robocorp-resources-tree && robocorp-code:single-robot-selected && viewItem == newWindowsLocator`,
	`view == robocorp-resources-tree && robocorp-code:single-robot-selected && viewItem == workItemsRoot`,
	`view == robocorp-resources-tree && viewItem == locatorsRoot`,
	`view == robocorp-resources-tree && viewItem == outputWorkItem || viewItem == inputWorkItem`,
	`view == robocorp-resources-tree && viewItem == outputWorkItem`,
	`view == robocorp-robot-content-tree && robocorp-code:single-robot-selected && viewItem == directoryItem`,
	`view == robocorp-robot-content-tree && robocorp-code:single-robot-selected && viewItem == fileItem`,
	`view == robocorp-robot-content-tree && robocorp-code:single-robot-selected`,
	`view == robocorp-robot-content-tree`,
	`view == robocorp-robots-tree && viewItem == actionsInRobotItem`,
	`view == robocorp-robots-tree && viewItem == robotItem`,
	`view == robocorp-robots-tree && viewItem == taskItem`,
	`view == robocorp-robots-tree`,
	`view == robot.view.documentation && !robot.view.documentation.isPinned`,
	`view == robot.view.documentation && robot.view.documentation.isPinned`,
	`view == rocket-mv-basic-group`,
	`view == rojo-ui.view.explorer && viewItem != rojo-ui.sessionContainer`,
	`view == rojo-ui.view.explorer`,
	`view == rokuRegistryView`,
	`view == romLocal`,
	`view == romRemote`,
	`view == routeDefinitionsView`,
	`view == rpcList`,
	`view == rss-accounts && viewItem != local`,
	`view == rss-accounts && viewItem == local`,
	`view == rss-accounts`,
	`view == rss-articles`,
	`view == rss-favorites && viewItem == article`,
	`view == rss-feeds`,
	`view == rt_project_view  && viewItem == project_root`,
	`view == rt_project_view && viewItem == project_file`,
	`view == rt_project_view && viewItem == project_root`,
	`view == rt_project_view`,
	`view == rtags.callHierarchy`,
	`view == rtags.inheritanceHierarchy`,
	`view == rtos-views.rtos`,
	`view == rubygems.explorer && viewItem == rubygems.explorer.node.gemspec`,
	`view == rubygems.explorer`,
	`view == run-config && viewItem == configureRunSettings && config.netbeans.javaSupport.enabled`,
	`view == runme.launcher && !runme.launcher.isExpanded`,
	`view == runme.launcher && runme.launcher.isExpanded`,
	`view == runnables && false`,
	`view == runnables && viewItem =~ /.*,kubernetesPods,.*/`,
	`view == runnables && viewItem =~ /.*,uri,.*/`,
	`view == runnables && viewItem =~ /.*,workDir,.*/`,
	`view == runnables`,
	`view == runs && viewItem == directory || viewItem == work_subdirectory`,
	`view == runs && viewItem == file || viewItem == directory || viewItem == work_subdirectory`,
	`view == runs && viewItem == file`,
	`view == runs && viewItem == work_subdirectory`,
	`view == runs`,
	`view == saBookmark`,
	`view == sampleProviderView && viewItem == sample`,
	`view == sampleProviderView`,
	`view == sandboxesView && go-playground.sandboxDirSpecified`,
	`view == sandboxesView && viewItem == 'sandbox-item'`,
	`view == sandboxesView`,
	`view == sap.ux.applicationModeler.configExplorer && viewItem == configFtfsFolder`,
	`view == sap.ux.applicationModeler.configExplorer && viewItem =~ /^configFtfsAppFile$|^configFtfsFolder$|^configFtfsPageFile.*$/`,
	`view == sap.ux.applicationModeler.configExplorer && viewItem =~ /^configFtfsAppFile$|^configFtfsPageFile.*$|^configFtfsCustomPageFile.*$/`,
	`view == sap.ux.applicationModeler.configExplorer && viewItem =~ /^configFtfsPageFile_addable$|^configFtfsCustomPageFile_addable$/`,
	`view == sap.ux.applicationModeler.configExplorer`,
	`view == sap.ux.applicationModeler.sapSystems`,
	`view == sass-snippets && viewItem != root`,
	`view == sass-snippets && viewItem != snippet`,
	`view == sass-snippets && viewItem == root`,
	`view == sass-snippets && viewItem == snippet`,
	`view == sass-snippets && viewItem!= snippet`,
	`view == sass-snippets`,
	`view == sassVariables`,
	`view == save-commands-view && viewItem == child-global || viewItem == child-workspace`,
	`view == save-commands-view && viewItem == parent-global`,
	`view == save-commands-view && viewItem == parent-workspace`,
	`view == save-commands-view`,
	`view == savedSearches && viewItem == savedSearch`,
	`view == savedSearches`,
	`view == sc2layoutMainView && viewItem == element`,
	`view == sc2layoutMainView`,
	`view == scanView && viewItem == scanServer`,
	`view == scanView && viewItem == scanStatusHeader`,
	`view == scanView`,
	`view == schemaBrowser && viewItem == schema`,
	`view == schemaBrowser`,
	`view == schemaExplorer && viewItem == connection`,
	`view == schemaExplorer && viewItem == field`,
	`view == schemaExplorer && viewItem == object &&  1 == 2`,
	`view == schemaExplorer && viewItem == object`,
	`view == schemaExplorer`,
	`view == scm-local-branches && scmLocalBranches:isSingleRepository`,
	`view == scm-local-branches && viewItem == activeBranch`,
	`view == scm-local-branches && viewItem == branch`,
	`view == scm-local-branches && viewItem == repo`,
	`view == scm-local-branches`,
	`view == scmTreeDataProvider && codestream:status == signedIn`,
	`view == scratches`,
	`view == scrypt.contracts.deployed && viewItem == Instance`,
	`view == scrypt.contracts.deployed && viewItem == ValueParam`,
	`view == scrypt.contracts.deployed && viewItem == abiCallable`,
	`view == scrypt.contracts.deployed && viewItem == tx`,
	`view == scrypt.contracts.deployed`,
	`view == scrypt.contracts.local && viewItem == ContractViewable`,
	`view == scrypt.contracts.local`,
	`view == scrypt.dataStack && viewItem == stackElement`,
	`view == sdfgAnalysis`,
	`view == sdfgBreakpoints`,
	`view == sdfgOutline`,
	`view == searchIssues`,
	`view == searchResults && viewItem == 'true'`,
	`view == searchResults && viewItem`,
	`view == searchResults`,
	`view == searchView`,
	`view == secretExplorer && viewItem == secret`,
	`view == secretExplorer`,
	`view == securityIssues && viewItem == finding`,
	`view == securityIssues && viewItem == ignoreFinding`,
	`view == securityIssues && viewItem == ignoreRule`,
	`view == securityIssues && viewItem == rule `,
	`view == securityIssues && viewItem == rule`,
	`view == securityIssues`,
	`view == securityRules && viewItem == ignoreRule`,
	`view == securityRules && viewItem == rule || viewItem == ignoreRule`,
	`view == securityRules && viewItem == rule`,
	`view == securityRules`,
	`view == selDebugger && WebdriverEnabled `,
	`view == select-debugger || view == debugger-node-lists || view == cuda-config-server || view == cuda-login`,
	`view == select-debugger || view == ssh-targets-lists || view == debugger-node-lists || view == cuda-config-server || view == cuda-login`,
	`view == seo-results`,
	`view == sequenceView && sequence.hasPat`,
	`view == sequenceView && viewItem == tokenize`,
	`view == sequenceView && viewItem =~ /file|fileHasLogs/`,
	`view == sequenceView && viewItem =~ /file|folder|hasKB|hasLog|hasLoghasKB/`,
	`view == sequenceView && viewItem =~ /file|hasKB|hasLog|hasLoghasKB/`,
	`view == sequenceView && viewItem =~ /file|hasKB|hasLog|hasLoghasKB|folder/`,
	`view == sequenceView && viewItem =~ /hasKB|hasLoghasKB/`,
	`view == sequenceView && viewItem =~ /hasLog/`,
	`view == sequenceView && viewItem =~ /hasLog|hasLoghasKB/`,
	`view == sequenceView && viewItem =~ /stub|folder|tokenize/`,
	`view == sequenceView`,
	`view == serialport && viewItem == serialport-item`,
	`view == serialport`,
	`view == serverExplorer && viewItem == running`,
	`view == serverExplorer && viewItem == stop`,
	`view == serverExplorer`,
	`view == serverLocatorDependencies && viewItem == cc-store-server`,
	`view == serverlessFunctions && viewItem == Tests`,
	`view == serverlessFunctions && viewItem == dependency`,
	`view == serverlessFunctions && viewItem == testCase`,
	`view == serverlessFunctions`,
	`view == serverlessOutline`,
	`view == serversManagement && viewItem == EnvironmentConnected`,
	`view == serversManagement && viewItem == Environment`,
	`view == serversManagement && viewItem == ServiceConnected`,
	`view == serversManagement && viewItem =~ /Environment/`,
	`view == serversManagement && viewItem =~ /^Environment/i`,
	`view == serversManagement`,
	`view == serviceDefinitionsView`,
	`view == serviceWorkerPanel`,
	`view == servicebus-namespaces && viewItem == namespace`,
	`view == servicebus-namespaces && viewItem == queue`,
	`view == servicebus-namespaces && viewItem == queuelist`,
	`view == servicebus-namespaces && viewItem == subscription`,
	`view == servicebus-namespaces && viewItem == topic`,
	`view == servicebus-namespaces && viewItem == topiclist`,
	`view == servicebus-namespaces`,
	`view == sessionsView && viewItem == session`,
	`view == sessionsView`,
	`view == sfdx-hardis-commands`,
	`view == sfdx-hardis-plugins`,
	`view == sfdx-hardis-status`,
	`view == sfdx.force.lightning.lwc.test.view && viewItem =~ /(lwcTest)(_.*|\\b)/`,
	`view == sfdx.force.lightning.lwc.test.view && viewItem =~ /(lwcTestGroup)(_.*|\\b)/`,
	`view == sfdx.force.lightning.lwc.test.view`,
	`view == sfdx.force.tasks.view`,
	`view == sfdx.force.test.view && viewItem =~ /(apexTest)(_.*|\\b)/`,
	`view == sfdx.force.test.view && viewItem =~ /(apexTest|apexTestGroup)(_Pass|_Skip|\\b)/`,
	`view == sfdx.force.test.view && viewItem =~ /(apexTest|apexTestGroup)_Fail/`,
	`view == sfdx.force.test.view && viewItem =~ /apexTestGroup/`,
	`view == sfdx.force.test.view`,
	`view == sfdx.force.view.checkpoint && sfdx:project_opened`,
	`view == shalldnDictionary && viewItem == dictItem`,
	`view == shalldnTags && !shaldnTags.grouped`,
	`view == shalldnTags && shaldnTags.grouped`,
	`view == shalldnTags && viewItem == tagTreeLeaf`,
	`view == shalldnTags && viewItem == tagTreeNode`,
	`view == shortcutsProvider`,
	`view == sidebar-outline`,
	`view == sidebarMarkdownNotes.webview`,
	`view == sidebar_test_id1`,
	`view == sigasi.hierarchy && !sigasi.hierarchy.trace`,
	`view == sigasi.hierarchy && !sigasi.onlyShowInstantiations`,
	`view == sigasi.hierarchy && sigasi.hierarchy.trace`,
	`view == sigasi.hierarchy && sigasi.onlyShowInstantiations`,
	`view == sigasi.hierarchy`,
	`view == sigasi.projectView && !sigasi.projectView.trace`,
	`view == sigasi.projectView && !sigasi.projectViewVirtualSelection`,
	`view == sigasi.projectView && sigasi.projectView.trace`,
	`view == sigasi.projectView && viewItem =~ /(isHdl|isFolder)/`,
	`view == sigasi.projectView && viewItem =~ /(isPhysical|isLinked)/ && !listMultiSelection`,
	`view == sigasi.projectView && viewItem =~ /(isPhysical|isLinked)/`,
	`view == sigasi.projectView && viewItem =~ /isPhysical/ && !listMultiSelection`,
	`view == sigasi.projectView && viewItem =~ /isProject/`,
	`view == sigasi.projectView`,
	`view == sitecoreExplorer && viewItem == connection`,
	`view == sitecoreExplorer && viewItem == database`,
	`view == sitecoreExplorer && viewItem == item`,
	`view == sitecoreExplorer`,
	`view == skosOutline`,
	`view == skynovel-dev && viewItem == 'Node.js'`,
	`view == skynovel-dev`,
	`view == skynovel-doc && viewItem == 'skynovel.doc/0'`,
	`view == skynovel-doc && viewItem == 'skynovel.doc/1'`,
	`view == skynovel-doc && viewItem == 'skynovel.doc/2'`,
	`view == skynovel-doc && viewItem == 'skynovel.doc/3'`,
	`view == skynovel-doc && viewItem == 'skynovel.doc/4/0'`,
	`view == skynovel-doc && viewItem == 'skynovel.doc/4/1'`,
	`view == skynovel-doc && viewItem == 'skynovel.doc/5/0'`,
	`view == skynovel-doc && viewItem == 'skynovel.doc/5/1'`,
	`view == skynovel-doc && viewItem == 'skynovel.doc/5/2'`,
	`view == skynovel-doc && viewItem == 'skynovel.doc/6/0'`,
	`view == skynovel-doc && viewItem == 'skynovel.doc/6/1'`,
	`view == skynovel-doc && viewItem == 'skynovel.doc/6/2'`,
	`view == skynovel-doc && viewItem == 'skynovel.doc/6/3'`,
	`view == skynovel-doc && viewItem == 'skynovel.doc/6/4'`,
	`view == skynovel-doc && viewItem == 'skynovel.doc/6/5'`,
	`view == skynovel-ws && viewItem == 'skynovel.devCnvFile'`,
	`view == skynovel-ws && viewItem == 'skynovel.devCrypto'`,
	`view == skynovel-ws && viewItem == 'skynovel.devPackFreem'`,
	`view == skynovel-ws && viewItem == 'skynovel.devPackLinux'`,
	`view == skynovel-ws && viewItem == 'skynovel.devPackMac'`,
	`view == skynovel-ws && viewItem == 'skynovel.devPackMacArm64'`,
	`view == skynovel-ws && viewItem == 'skynovel.devPackWin'`,
	`view == skynovel-ws && viewItem == 'skynovel.devPackWin32'`,
	`view == skynovel-ws && viewItem == 'skynovel.devPrjSet'`,
	`view == skynovel-ws && viewItem == 'skynovel.devReBuild'`,
	`view == skynovel-ws && viewItem == 'skynovel.devSnUpd'`,
	`view == skynovel-ws && viewItem == 'skynovel.devTaskApp'`,
	`view == skynovel-ws && viewItem == 'skynovel.devTaskAppStop'`,
	`view == skynovel-ws && viewItem == 'skynovel.devTaskWeb'`,
	`view == skynovel-ws && viewItem == 'skynovel.devTaskWebStop'`,
	`view == slidesExplorer && viewItem == slideNode`,
	`view == slidesExplorer`,
	`view == slither-detector-filters`,
	`view == slither-explorer`,
	`view == slnbrw && viewItem == no-solution`,
	`view == slnbrw && viewItem == project-cps`,
	`view == slnbrw && viewItem == project-file`,
	`view == slnbrw && viewItem == project-folder`,
	`view == slnbrw && viewItem == project-referenced-package-cps`,
	`view == slnbrw && viewItem == project-referenced-packages-cps`,
	`view == slnbrw && viewItem == project-referenced-project-cps`,
	`view == slnbrw && viewItem == project-referenced-projects-cps`,
	`view == slnbrw && viewItem == project-standard`,
	`view == slnbrw && viewItem == solution-cps`,
	`view == slnbrw && viewItem == solution-file`,
	`view == slnbrw && viewItem == solution-folder`,
	`view == slnbrw && viewItem == solution`,
	`view == slnbrw`,
	`view == slnexpl && viewItem == no-solution`,
	`view == slnexpl && viewItem == project-cps`,
	`view == slnexpl && viewItem == project-file`,
	`view == slnexpl && viewItem == project-folder`,
	`view == slnexpl && viewItem == project-referenced-package-cps`,
	`view == slnexpl && viewItem == project-referenced-packages-cps`,
	`view == slnexpl && viewItem == project-referenced-project-cps`,
	`view == slnexpl && viewItem == project-referenced-projects-cps`,
	`view == slnexpl && viewItem == project-standard`,
	`view == slnexpl && viewItem == solution-cps`,
	`view == slnexpl && viewItem == solution-file`,
	`view == slnexpl && viewItem == solution-folder`,
	`view == slnexpl && viewItem == solution`,
	`view == slnexpl`,
	`view == slsConsoleTree && viewItem == dynamodb-change`,
	`view == slsConsoleTree && viewItem == dynamodb-changes`,
	`view == slsConsoleTree && viewItem == dynamodb-items`,
	`view == slsConsoleTree && viewItem == local-file`,
	`view == slsConsoleTree && viewItem == service`,
	`view == slsConsoleTree`,
	`view == smartContracts && viewItem == contract`,
	`view == smartContracts`,
	`view == smartLogEventsExplorer && viewItem != selected`,
	`view == smartLogEventsExplorer && viewItem == selected`,
	`view == snarferBrowser && viewItem == project`,
	`view == snarferBrowser`,
	`view == snippetExplorer && viewItem == group`,
	`view == snippetExplorer && viewItem == snippet`,
	`view == snippetExplorer`,
	`view == snippetTree`,
	`view == snippets.view && !config.snippets.viewer.combineLanguageSnippets`,
	`view == snippets.view && !config.snippets.viewer.sortSnippetsByName`,
	`view == snippets.view && config.snippets.viewer.combineLanguageSnippets`,
	`view == snippets.view && config.snippets.viewer.sortSnippetsByName`,
	`view == snippets.view && viewItem != snippetLanguage`,
	`view == snippets.view && viewItem == snippet`,
	`view == snippets.view`,
	`view == snippetsExplorer && viewItem =~ /^\\S*:(down|up&down)$/`,
	`view == snippetsExplorer && viewItem =~ /^\\S*:(up|up&down)$/`,
	`view == snippetsExplorer && viewItem =~ /^snippet(:\\S+)?$/`,
	`view == snippetsExplorer && viewItem =~ /^snippetFolder(:\\S+)?$/`,
	`view == snippetsExplorerView`,
	`view == snippetsExplorer`,
	`view == snippetsmanager-snippetsView-ExtensionSnippetsExplorerView`,
	`view == snippetsmanager-snippetsView-UserSnippetsExplorerView`,
	`view == snippetsmanager-snippetsView-WorkspaceSnippetsExplorerView`,
	`view == snowflakeAccount`,
	`view == snowflakeObjectExplorer && viewItem != group || view == snowflakeQueryHistoryTree`,
	`view == snowflakeObjectExplorer`,
	`view == snowflakeQueryHistoryTree && viewItem != loading`,
	`view == snowflakeQueryHistoryTree && viewItem == loading`,
	`view == snowflakeQueryHistoryTree`,
	`view == soicode-tasks && viewItem == soitask || view == soicode-tasks && viewItem == task || view == soicode-tasks && viewItem == solution`,
	`view == solDebugView && viewItem == solStreamDirectory`,
	`view == solDebugView && viewItem == solStreamFile`,
	`view == solDependencyTree`,
	`view == solHistoryLog && viewItem == solHistoryListElementWithVar`,
	`view == solHistoryLog && viewItem == solVariableHistoryTick`,
	`view == solHistoryLog && viewItem == solVariableHistory`,
	`view == solHistoryLog && viewItem =~ /solHistoryListElement.*/`,
	`view == solHistoryLog`,
	`view == solReferenceTree`,
	`view == solTraceabilityTree || view == solSystemRequirementsTree`,
	`view == solidity-va-cockpit-explorer && viewItem =~ /\\.sol/ || view == solidity-va-cockpit-topLevelContracts`,
	`view == solidity-va-cockpit-explorer && viewItem =~ /^((?!\\.sol).)*$/`,
	`view == solidity-va-cockpit-explorer || view == solidity-va-cockpit-topLevelContracts || view == solidity-va-cockpit-flatFiles`,
	`view == solidity-va-cockpit-explorer`,
	`view == solidity-va-cockpit-flatFiles`,
	`view == solidity-va-cockpit-topLevelContracts`,
	`view == sonarqubeStatus.quickInfo`,
	`view == sourcegraph.files && sourcegraph.removeRepository`,
	`view == span-vscode && viewItem == device`,
	`view == span-vscode`,
	`view == sparkar-cli.build.view && viewItem == project`,
	`view == sparkar-cli.devices.view && viewItem == device`,
	`view == spring.apps && !viewItem`,
	`view == spring.apps && viewItem == BootApp_inactive`,
	`view == spring.apps && viewItem == BootApp_running`,
	`view == spring.apps`,
	`view == spring.beans && spring.beans:showMode != all && spring.beans:hasLiveProcess == true`,
	`view == spring.beans && spring.beans:showMode != defined && spring.beans:hasLiveProcess == true`,
	`view == spring.beans && viewItem == liveProcess`,
	`view == spring.beans && viewItem == spring:bean`,
	`view == spring.beans && viewItem =~ /^bootApp(?=.*?\\b\\+noActuator\\b)(?=.*?\\b\\+running\\b)/`,
	`view == spring.beans && viewItem =~ /^bootApp/`,
	`view == spring.beans`,
	`view == spring.mappings && spring.mappings:showMode != all && spring.mappings:hasLiveProcess == true`,
	`view == spring.mappings && spring.mappings:showMode != defined && spring.mappings:hasLiveProcess == true`,
	`view == spring.mappings && viewItem == liveProcess`,
	`view == spring.mappings && viewItem =~ /^bootApp(?=.*?\\b\\+noActuator\\b)(?=.*?\\b\\+running\\b)/`,
	`view == spring.mappings && viewItem =~ /^bootApp/`,
	`view == spring.mappings && viewItem =~ /spring:endpoint(?=.*?\\b\\+GET\\b)/`,
	`view == spring.mappings`,
	`view == sqlite.explorer && viewItem == sqlite.columnItem`,
	`view == sqlite.explorer && viewItem == sqlite.databaseItem`,
	`view == sqlite.explorer && viewItem == sqlite.tableItem`,
	`view == sqlite.explorer`,
	`view == sqlnotebook-connections &&  viewItem == database`,
	`view == sqltoolsViewBookmarksExplorer && viewItem == bookmark.item`,
	`view == sqltoolsViewBookmarksExplorer && viewItem == history.item`,
	`view == sqltoolsViewBookmarksExplorer`,
	`view == sqltoolsViewConnectionExplorer && viewItem == connectedConnection`,
	`view == sqltoolsViewConnectionExplorer && viewItem == connection`,
	`view == sqltoolsViewConnectionExplorer && viewItem =~ /^(connection|connectedConnection)$/`,
	`view == sqltoolsViewConnectionExplorer && viewItem =~ /^connection\\.(.+)/`,
	`view == sqltoolsViewConnectionExplorer && viewItem =~ /^connection\\.(table|view)$/`,
	`view == sqltoolsViewConnectionExplorer && viewItem =~ /^connection\\.(table|view|materializedView)$/`,
	`view == sqltoolsViewConnectionExplorer`,
	`view == sqltoolsViewConsoleMessages`,
	`view == sqltoolsViewHistoryExplorer && viewItem == history.item`,
	`view == sqltoolsViewHistoryExplorer`,
	`view == ssh-targets-lists && isZh || view == select-debugger && isZh || view == cuda-config-server && isZh || view == cuda-login && isZh`,
	`view == ssh-targets-lists && viewItem == profileNode`,
	`view == ssh-targets-lists || view == select-debugger || view == cuda-config-server || view == cuda-login`,
	`view == ssh-targets-lists`,
	`view == starters`,
	`view == state.management.explorer && viewItem == ngrx:actionType`,
	`view == state.management.explorer && viewItem == ngrx:action`,
	`view == state.management.explorer && viewItem == ngrx:actions`,
	`view == state.management.explorer && viewItem == ngrx:effect`,
	`view == state.management.explorer && viewItem == ngrx:effects`,
	`view == state.management.explorer && viewItem == ngrx:ngmodule`,
	`view == state.management.explorer && viewItem == ngrx:ofType`,
	`view == state.management.explorer && viewItem == ngrx:property`,
	`view == state.management.explorer && viewItem == ngrx:reducer`,
	`view == state.management.explorer && viewItem == ngrx:select`,
	`view == state.management.explorer && viewItem == ngrx:selector`,
	`view == state.management.explorer && viewItem == ngrx:state`,
	`view == state.management.explorer && viewItem == ngrx:store`,
	`view == state.management.explorer && viewItem == redux:action`,
	`view == state.management.explorer && viewItem == redux:connect`,
	`view == state.management.explorer && viewItem == redux:reducer`,
	`view == state.management.explorer && viewItem == redux:store`,
	`view == state.management.explorer && viewItem == vuex:action`,
	`view == state.management.explorer && viewItem == vuex:getter`,
	`view == state.management.explorer && viewItem == vuex:mutation`,
	`view == state.management.explorer && viewItem == vuex:state`,
	`view == state.management.explorer && viewItem == vuex:store`,
	`view == statementTrace`,
	`view == states && viewItem == state`,
	`view == statusBarParam`,
	`view == steedos_metadata && viewItem == component`,
	`view == steedos_metadata && viewItem =~ /(folder|type)/`,
	`view == steedos_metadata`,
	`view == stickodeMainView`,
	`view == stitch.modelTree && viewItem == SCENARIO_FILE`,
	`view == stmTreeWeekly`,
	`view == storiesPanel || view == stories-full`,
	`view == story-entries && viewItem == story-item`,
	`view == story-entries && viewItem == version-item`,
	`view == story-entries`,
	`view == storyExplorer.storiesView && viewItem == story`,
	`view == storyExplorer.storiesView`,
	`view == streamsAppServices && viewItem == appServiceTreeItem`,
	`view == streamsAppServices && viewItem == get_appServiceEndpointPathTreeItem`,
	`view == streamsAppServices && viewItem == post_appServiceEndpointPathTreeItem`,
	`view == streamsDetails && viewItem == detailTreeItem`,
	`view == streamsInstances && viewItem == baseImageTreeItem`,
	`view == streamsInstances && viewItem == cpdJobRunLogsLabelTreeItem`,
	`view == streamsInstances && viewItem == finished_cpdJobTreeItem`,
	`view == streamsInstances && viewItem == v5_cpd_cpdProject_jobTreeItem`,
	`view == streamsInstances && viewItem =~ /(v5_cpd_.*jobTreeItem|v5_standalone_jobTreeItem)/`,
	`view == streamsInstances && viewItem =~ /^(?:finished_)?cpdJobTreeItem/`,
	`view == streamsInstances && viewItem =~ /auth.*cpdSpaces.*v5_cpd.*_instanceTreeItem/`,
	`view == streamsInstances && viewItem =~ /auth_.*_instanceTreeItem/`,
	`view == streamsInstances && viewItem =~ /consoleEnabled_.*_instanceTreeItem/`,
	`view == streamsInstances && viewItem =~ /cpdJobRunLogTreeItem/`,
	`view == streamsInstances && viewItem =~ /cpdJobRunTreeItem/`,
	`view == streamsInstances && viewItem =~ /cpdJobTreeItem/`,
	`view == streamsInstances && viewItem =~ /cpdProjectTreeItem/`,
	`view == streamsInstances && viewItem =~ /cpdSpaceTreeItem/`,
	`view == streamsInstances && viewItem =~ /finished_cpdJobRunTreeItem/`,
	`view == streamsInstances && viewItem =~ /instanceTreeItem/`,
	`view == streamsInstances && viewItem =~ /jobTreeItem/`,
	`view == streamsInstances && viewItem =~ /notAuth_.*_instanceTreeItem/`,
	`view == streamsInstances && viewItem =~ /notDefault_.*_instanceTreeItem/`,
	`view == streamsInstances && viewItem =~ /running_cpdJobRunTreeItem/`,
	`view == streamsInstances && viewItem =~ /v5_cpd_.*jobTreeItem/`,
	`view == streamsInstances && viewItem =~ /v5_cpd_instanceTreeItem/`,
	`view == streamsInstances`,
	`view == streamsJobs && viewItem =~ /streamsJobsTreeItem/`,
	`view == streamsToolkits && viewItem == local_label_toolkitTreeItem`,
	`view == streamsToolkits && viewItem == toolkitTreeItem`,
	`view == streamsToolkits`,
	`view == stripeEventsView`,
	`view == stripeLogsView`,
	`view == studio-graphs && viewItem == preloadedWorkbenchFile`,
	`view == studio-graphs && viewItem == studioGraphTreeItem`,
	`view == studio-graphs && viewItem == studioGraphVariantTreeItem`,
	`view == studio-graphs`,
	`view == studio-operations && viewItem == studioOperationTreeItem`,
	`view == stunicons-sidebar`,
	`view == subscribe`,
	`view == substrateAccounts`,
	`view == substrateContracts`,
	`view == substrateMarketplace && viewItem == pallet || view == substrateMarketplace && viewItem == palletInstalled || view == substrateMarketplace && viewItem == palletNoNode`,
	`view == substrateMarketplace && viewItem == pallet`,
	`view == substrateNodes`,
	`view == substrateProcesses && viewItem == theia`,
	`view == substrateProcesses`,
	`view == suggestedConflictTreeView`,
	`view == suggestedDockerImages`,
	`view == super-stock-favorite`,
	`view == supernovaDesignSystemContentPanel`,
	`view == supernovaExporterBrowserPanel`,
	`view == svd-viewer.svd && debugState == stopped`,
	`view == svd-viewer.svd && viewItem == fieldWO`,
	`view == svd-viewer.svd && viewItem == field`,
	`view == svd-viewer.svd && viewItem == peripheral.pinned`,
	`view == svd-viewer.svd && viewItem == peripheral`,
	`view == svd-viewer.svd && viewItem == registerRO`,
	`view == svd-viewer.svd && viewItem == registerRW`,
	`view == svd-viewer.svd && viewItem == registerWO`,
	`view == svd-viewer.svd && viewItem == register`,
	`view == svd-viewer.svd && viewItem =~ /peripheral.*/`,
	`view == svd-viewer.svd`,
	`view == svn`,
	`view == swagger-doc && viewItem == swaggerItem`,
	`view == swagger-doc`,
	`view == swaggerExplorer && viewItem == treeItemProject || viewItem == treeItemConfig`,
	`view == swaggerExplorer && viewItem == treeItemSource`,
	`view == swaggerExplorer`,
	`view == swaggerhubApis && viewItem == apiVersion`,
	`view == swaggerhubApis`,
	`view == swaggerhubDomains && viewItem == domainVersion`,
	`view == swaggerhubDomains`,
	`view == swift-i18n-helper.codeView && viewItem == keyword`,
	`view == swift-i18n-helper.codeView`,
	`view == swimm.docs`,
	`view == symbolOutline`,
	`view == symdump && viewItem == symdumpconnectionnode`,
	`view == symdump && viewItem =~ /symdumpdatasetnode/ && viewItem =~ /CICS/`,
	`view == symdump && viewItem =~ /symdumpdatasetnode/ && viewItem =~ /^((?!filtered).)*$/`,
	`view == symdump && viewItem =~ /symdumpdatasetnode/ && viewItem =~ /filtered/`,
	`view == symdump && viewItem =~ /symdumpdatasetnode/`,
	`view == symdump && viewItem =~ /symdumpdumpnode/ && viewItem =~ /Batch/`,
	`view == symdump && viewItem =~ /symdumpdumpnode/ && viewItem =~ /^((?!locked).)*$/`,
	`view == symdump && viewItem =~ /symdumpdumpnode/ && viewItem =~ /locked/`,
	`view == symdump && viewItem =~ /symdumpdumpnode/`,
	`view == symdump && viewItem =~ /symdumpprotsymnode/ && viewItem =~ /^((?!dss).)*$/`,
	`view == symdump && viewItem =~ /symdumpprotsymnode/ && viewItem =~ /dss/`,
	`view == symdump && viewItem =~ /symdumpprotsymnode/`,
	`view == symdump && viewItem =~ /symdumpprotsymsnode/`,
	`view == symdump && viewItem =~ /symdumpreportnode/`,
	`view == symdump`,
	`view == syntax-visualizer`,
	`view == systemObjectDefinitionsView && viewItem == attribute`,
	`view == systemObjectDefinitionsView && viewItem == baseNodeName`,
	`view == systemObjectDefinitionsView && viewItem == definition`,
	`view == systemObjectDefinitionsView && viewItem == groupAttribute`,
	`view == systemObjectDefinitionsView && viewItem == group`,
	`view == systemObjectDefinitionsView && viewItem == preferenceValue`,
	`view == systemObjectDefinitionsView`,
	`view == t3lt-passages-list && !t3lt.storyMap`,
	`view == t3lt-passages-list && t3lt.storyMap`,
	`view == t3lt-passages-list`,
	`view == tab-groups-groups && viewItem == group`,
	`view == tab-groups-groups && viewItem =~ /^split$|^file$/`,
	`view == tab-groups-groups`,
	`view == tabster && viewItem == item`,
	`view == tabster && viewItem == tabs`,
	`view == tabsterHot && viewItem == item`,
	`view == tabsterHot && viewItem == tabs`,
	`view == tabsterHot`,
	`view == tabster`,
	`view == targetView && kestudiocode.iec.tasksViewVisible`,
	`view == targetView && viewItem == plc`,
	`view == targetsContainers && !remote-containers:needsDockerStartOrInstall && !remote-containers:noContainersWithFolder && !remote-containers:noContainersWithoutFolder`,
	`view == targetsContainers && viewItem == attachedFolder`,
	`view == targetsContainers && viewItem =~ /^(active|running)(Dev|)Container/`,
	`view == targetsContainers && viewItem =~ /^(active|running|exited)(Dev|)Container/`,
	`view == targetsContainers && viewItem =~ /^(active|running|exited)Container/`,
	`view == targetsContainers && viewItem =~ /^(dev|attached|exited|running).*Folder$/`,
	`view == targetsContainers && viewItem =~ /^(running|exited)(Dev|)Container/`,
	`view == targetsContainers && viewItem =~ /^exited(Dev|)Container/`,
	`view == targetsContainers && viewItem =~ /active.*Folder/`,
	`view == targetsContainers && viewItem =~ /activeDevContainer/`,
	`view == targetsWsl && viewItem == folder`,
	`view == targetsWsl && viewItem == inactiveDistro`,
	`view == targetsWsl && viewItem =~ /^(inactive|active)Distro$/`,
	`view == targetsWsl`,
	`view == task-manager-tasks && viewItem == runningTask`,
	`view == task-manager-tasks && viewItem == task`,
	`view == task-manager-tasks`,
	`view == taskExplorerSideBar`,
	`view == taskExplorer`,
	`view == taskOutline`,
	`view == task`,
	`view == taskfile && viewItem == running-task`,
	`view == taskfile && viewItem == task`,
	`view == taskfile`,
	`view == tasks-context && !config.tasks-context.hideCompletedTasks`,
	`view == tasks-context && config.tasks-context.hideCompletedTasks`,
	`view == tasks-context && viewItem == file`,
	`view == tasks-context && viewItem =~ /task:.*/`,
	`view == tasks-context && viewItem =~ /task:[^:]*:complete/`,
	`view == tasks-context && viewItem =~ /task:[^:]*:incomplete/`,
	`view == tasks-context && viewItem =~ /task:active.*/`,
	`view == tasks-context && viewItem =~ /task:inactive.*/`,
	`view == tasks-context`,
	`view == tasks`,
	`view == tastPanel && viewItem == case`,
	`view == tastPanel && viewItem == suite`,
	`view == tastPanel`,
	`view == tcb.views.scf`,
	`view == tdqtDatabases`,
	`view == tdqtQueries`,
	`view == tdt4102-templates && viewItem == leafnode`,
	`view == teammates && !mintlify.isLoggedIn`,
	`view == teammates && mintlify.isLoggedIn`,
	`view == teammates && viewItem == member`,
	`view == teamsfx-accounts && viewItem == checkSideloading`,
	`view == teamsfx-accounts && viewItem == selectSubscription`,
	`view == teamsfx-accounts && viewItem == signedinAzure`,
	`view == teamsfx-accounts && viewItem == signedinM365`,
	`view == teamsfx-accounts && viewItem =~ /^signinM365$|^signinAzure$/`,
	`view == teamsfx-accounts`,
	`view == teamsfx-deployment && viewItem =~ /^fx-extension.(provision|build|deploy|publish)$/`,
	`view == teamsfx-deployment`,
	`view == teamsfx-development && viewItem =~ /^fx-extension.(create|openSamples|openManifest)$/`,
	`view == teamsfx-development`,
	`view == teamsfx-environment && viewItem == openResourceGroupInPortal`,
	`view == teamsfx-environment && viewItem == openSubscriptionInPortal`,
	`view == teamsfx-environment && viewItem =~ /^local$/`,
	`view == teamsfx-environment && viewItem =~ /environment-provisioned|local-existing-app/`,
	`view == teamsfx-environment && viewItem =~ /environment/ && !fx-extension.isV3Enabled`,
	`view == teamsfx-environment`,
	`view == teamsfx-help-and-feedback`,
	`view == tektonCustomTreeView`,
	`view == tektonPipelineExplorerView`,
	`view == templates-view && viewItem == insert`,
	`view == templates-view`,
	`view == templatesExplorer`,
	`view == terminal-manager`,
	`view == terminal-tools && viewItem == dependencies`,
	`view == terminal-tools && viewItem == devDependencies`,
	`view == terminal-tools && viewItem == globalDependencies`,
	`view == terminal-tools`,
	`view == terminalSnipps`,
	`view == terminalsView`,
	`view == teroshdl_dependencies_tree_view`,
	`view == teroshdl_tree_view && viewItem == hdl_library`,
	`view == teroshdl_tree_view && viewItem == hdl_source`,
	`view == teroshdl_tree_view && viewItem == project`,
	`view == teroshdl_tree_view && viewItem == teroshdl`,
	`view == teroshdl_tree_view && viewItem == test_cocotb`,
	`view == teroshdl_tree_view && viewItem == test_edalize`,
	`view == teroshdl_tree_view && viewItem == test_title`,
	`view == teroshdl_tree_view && viewItem == test_vunit`,
	`view == teroshdl_tree_view && viewItem == title_vhdl_sources`,
	`view == teroshdl_tree_view`,
	`view == terraWdioHelper`,
	`view == terraform-project && viewItem == blueprint-entity`,
	`view == terraform-project && viewItem == category`,
	`view == terraform-project && viewItem == entity-child`,
	`view == terraform-project && viewItem == environment-entity`,
	`view == terraform-project && viewItem == file`,
	`view == terraform-project && viewItem == folder-blueprint`,
	`view == terraform-project && viewItem == folder-environment`,
	`view == terraform-project && viewItem == folder`,
	`view == terraform-project && viewItem == node`,
	`view == terraform-project && viewItem == parseErrorNode`,
	`view == terraform-project`,
	`view == terraform.modules`,
	`view == terraform.providers && viewItem == moduleProviderHasDocs`,
	`view == terraform.providers`,
	`view == terrastate.terrastate && viewItem == deployed-resource`,
	`view == terrastate.terrastate && viewItem == top-module`,
	`view == terrastate.terrastate && viewItem =~ /^((dormant|deployed)-resource)$/`,
	`view == terrastate.terrastate && viewItem =~ /^((top-)?module)$/`,
	`view == terrastate.terrastate`,
	`view == test-case-lists && viewItem == firstNode`,
	`view == test-case-lists && viewItem == testCaseNode`,
	`view == test-case-lists && viewItem == testProfileNode`,
	`view == test-case-lists && viewItem == testSuiteNode`,
	`view == test-case-lists`,
	`view == test-explorer && !testsLoading`,
	`view == test-explorer && !testsRunning`,
	`view == test-explorer && isAngularEnviroment`,
	`view == test-explorer && karmaTestExplorer.allowProjectSelection`,
	`view == test-explorer && showTestExplorerExpandButton`,
	`view == test-explorer && testsLoading`,
	`view == test-explorer && testsRunning`,
	`view == test-explorer && viewItem != error`,
	`view == test-explorer && viewItem == collection`,
	`view == test-explorer && viewItem == debuggableSuiteWithSource && config.ply.previewEnabled`,
	`view == test-explorer && viewItem == debuggableSuiteWithSource`,
	`view == test-explorer && viewItem == debuggableSuite`,
	`view == test-explorer && viewItem == debuggableTestWithSource && config.ply.previewEnabled`,
	`view == test-explorer && viewItem == debuggableTestWithSource`,
	`view == test-explorer && viewItem == debuggableTest`,
	`view == test-explorer && viewItem == error`,
	`view == test-explorer && viewItem == suiteWithSource && config.ply.previewEnabled`,
	`view == test-explorer && viewItem == suiteWithSource`,
	`view == test-explorer && viewItem == suite`,
	`view == test-explorer && viewItem == testWithSource && config.ply.previewEnabled`,
	`view == test-explorer && viewItem == testWithSource`,
	`view == test-explorer && viewItem == test`,
	`view == test-explorer`,
	`view == testApi6Example && viewItem == file`,
	`view == testApi6Global && viewItem != root`,
	`view == testApi6Global`,
	`view == testApi6History && viewItem == file`,
	`view == testApi6History`,
	`view == testApi6Local && viewItem == cmd`,
	`view == testApi6Local && viewItem == file`,
	`view == testApi6Local && viewItem == folder`,
	`view == testApi6Local`,
	`view == testApi6Profile`,
	`view == testDataProvider && viewItem == dataType`,
	`view == testDataProvider && viewItem == testDataSpec`,
	`view == testDataProvider`,
	`view == testOutline && singleFolder`,
	`view == testOutline`,
	`view == testSuiteprovider && viewItem == testSpec`,
	`view == testSuiteprovider && viewItem == testType`,
	`view == testSuiteprovider && viewItem == tests`,
	`view == testSuiteprovider`,
	`view == textView && text.hasLogs`,
	`view == textView && viewItem =~ /.*HasLogs.*/`,
	`view == textView && viewItem =~ /.*dir.*/`,
	`view == textView && viewItem =~ /.*file.*/`,
	`view == textView && viewItem =~ /.*moveDown.*/`,
	`view == textView && viewItem =~ /.*moveUp.*/`,
	`view == textView && viewItem =~ /dir/)`,
	`view == textView && viewItem =~ /dir/`,
	`view == textView && viewItem =~ /dirHasNonText/`,
	`view == textView && viewItem =~ /file/`,
	`view == textView`,
	`view == textxLanguages && viewItem == project`,
	`view == textxLanguages`,
	`view == tfsec.issueview && viewItem == TFSEC_CODE`,
	`view == tfsec.issueview && viewItem == TFSEC_FILE_LOCATION`,
	`view == tfsec.issueview && viewItem == TFSEC_SEVERITY`,
	`view == tfsec.issueview`,
	`view == themes`,
	`view == thunder-client-sidebar`,
	`view == ticode-tiup-cluster && viewItem == cluster-global-config`,
	`view == ticode-tiup-cluster && viewItem == cluster-instance-conf-file`,
	`view == ticode-tiup-cluster && viewItem == cluster-instance`,
	`view == ticode-tiup-cluster && viewItem == cluster-name`,
	`view == ticode-tiup-cluster && viewItem =~ /^cluster-component-tidb/`,
	`view == ticode-tiup-cluster && viewItem =~ /^cluster-component/`,
	`view == ticode-tiup-cluster && viewItem =~ /^cluster-component|^cluster-instance$/`,
	`view == ticode-tiup-cluster`,
	`view == ticode-tiup-playground && viewItem == playground-cluster`,
	`view == ticode-tiup-playground && viewItem == playground-config`,
	`view == ticode-tiup-playground && viewItem =~ /^playground-instance-tidb/`,
	`view == ticode-tiup-playground && viewItem =~ /^playground-instance/`,
	`view == ticode-tiup-playground`,
	`view == ticode-topo-manager && viewItem == topo-cluster-added`,
	`view == ticode-topo-manager && viewItem == topo-file-private-key`,
	`view == ticode-topo-manager && viewItem == topo-file-topology`,
	`view == ticode-topo-manager && viewItem == topo-file-vagrantfile`,
	`view == ticode-topo-manager && viewItem == topo-vm`,
	`view == ticode-topo-manager && viewItem =~ /^topo-file/ && viewItem != topo-file-private-key`,
	`view == ticode-topo-manager`,
	`view == tidalcycles-soundbrowser-sounds && viewItem == sound || viewItem == dir`,
	`view == tidalcycles-soundbrowser-sounds && viewItem == sound`,
	`view == tidalcycles-soundbrowser-sounds`,
	`view == timeCapsule && viewItem == snapshot`,
	`view == timeCapsule`,
	`view == timelink.views.fileExplorer || view == timelink.views.translationNeededExplorer || view == timelink.views.fileWithErrorsExplorer || view == timelink.views.fileWithWarningsExplorer || view == timelink.views.importReadyExplorer`,
	`view == titanium.view.buildExplorer && !config.titanium.build.liveview`,
	`view == titanium.view.buildExplorer && config.titanium.build.liveview`,
	`view == titanium.view.buildExplorer && titanium:build:running`,
	`view == titanium.view.buildExplorer && viewItem == DeviceNode`,
	`view == titanium.view.buildExplorer && viewItem == RecentNode`,
	`view == titanium.view.buildExplorer && viewItem =~ /DeviceNode|DistributeNode/`,
	`view == titanium.view.buildExplorer && viewItem =~ /PlatformNode|DistributeNode/`,
	`view == titanium.view.buildExplorer && viewItem =~ /PlatformNode|TargetNode|OSVerNode|DeviceNode/`,
	`view == titanium.view.buildExplorer`,
	`view == titanium.view.helpExplorer && titanium:update:hasUpdates`,
	`view == titanium.view.helpExplorer && viewItem == UpdateNode`,
	`view == titanium.view.helpExplorer && viewItem == UpdatesNode`,
	`view == tmcView && test-my-code:LoggedIn`,
	`view == tmcView && viewItem == child`,
	`view == tmsFiles && viewItem == tmsFile`,
	`view == tmsFiles && viewItem == tmsRoot`,
	`view == tmsFiles`,
	`view == tocTrees`,
	`view == todo && viewItem == entry`,
	`view == todo && viewItem == group`,
	`view == todo-list`,
	`view == todo.views.0doing`,
	`view == todo.views.1files && todo-files-expanded == false`,
	`view == todo.views.1files && todo-files-expanded == true`,
	`view == todo.views.1files && todo-files-open-button == true`,
	`view == todo.views.2embedded && todo-embedded-all == false`,
	`view == todo.views.2embedded && todo-embedded-all == true`,
	`view == todo.views.2embedded && todo-embedded-expanded == false`,
	`view == todo.views.2embedded && todo-embedded-expanded == true`,
	`view == todo.views.2embedded && todo-embedded-filtered == false`,
	`view == todo.views.2embedded && todo-embedded-filtered == true`,
	`view == todo.views.3critical`,
	`view == todoPlusExplorer && viewItem == MetadataExplorerNode`,
	`view == todoPlusExplorer && viewItem == OptionExplorerNode`,
	`view == todoPlusExplorer && viewItem == ReminderOptionExplorerNode`,
	`view == todoPlusExplorer && viewItem == TodoItemExplorerNode.completed`,
	`view == todoPlusExplorer && viewItem == TodoItemExplorerNode`,
	`view == todo`,
	`view == todomd.contexts`,
	`view == todomd.projects`,
	`view == todomd.tags`,
	`view == todomd.tasks && !todomd:filterActive`,
	`view == todomd.tasks && todomd:filterActive`,
	`view == todomd.tasks`,
	`view == todomd.webviewTasks`,
	`view == toitDeviceView && viewItem == application`,
	`view == toitDeviceView && viewItem =~ /device.*/`,
	`view == toitDeviceView`,
	`view == toitSerialView && viewItem == serial-port`,
	`view == toitSerialView`,
	`view == tomcatServerExplorer && !viewItem`,
	`view == tomcatServerExplorer && viewItem && viewItem != war`,
	`view == tomcatServerExplorer && viewItem == idleserver`,
	`view == tomcatServerExplorer && viewItem == runningserver`,
	`view == tomcatServerExplorer && viewItem == war`,
	`view == tomcatServerExplorer`,
	`view == toolbox`,
	`view == top-stories`,
	`view == torizonapplist && viewItem == clearablestring`,
	`view == torizonapplist && viewItem == collection`,
	`view == torizonapplist && viewItem == configuration`,
	`view == torizonapplist && viewItem == item`,
	`view == torizonapplist && viewItem == string`,
	`view == torizonapplist`,
	`view == torizondevicelist && viewItem == container`,
	`view == torizondevicelist && viewItem == device`,
	`view == torizondevicelist && viewItem == folder`,
	`view == torizondevicelist && viewItem == image`,
	`view == torizondevicelist && viewItem == runningcontainer`,
	`view == torizondevicelist && viewItem == stoppedcontainer`,
	`view == torizondevicelist`,
	`view == torizonobjectdump && viewItem == editableproperty`,
	`view == torizonobjectdump`,
	`view == toscaLiteTypes && viewItem != property && viewItem != property-proxy && viewItem != servicetemplate && viewItem != servicetemplate-proxy`,
	`view == toscaLiteTypes && viewItem != resource-proxy && viewItem != property-proxy && viewItem != servicetemplate-proxy`,
	`view == toscaLiteTypes`,
	`view == totvs_server && viewItem == envSectionNotCurrent`,
	`view == totvs_server && viewItem == serverItem && tds-vscode.isServerP20OrGreater`,
	`view == totvs_server && viewItem == serverItemNotConnected`,
	`view == totvs_server && viewItem == serverItem`,
	`view == totvs_server`,
	`view == touchelf-device-view`,
	`view == touchelf-file-view && viewItem == dir`,
	`view == touchelf-file-view && viewItem == file`,
	`view == touchelf-file-view`,
	`view == toysView && viewItem == 'toy' && go-playground.sandboxDirSpecified`,
	`view == toysView && viewItem == 'user-root'`,
	`view == toysView && viewItem == 'user-toy' && go-playground.sandboxDirSpecified`,
	`view == toysView && viewItem == 'user-toy'`,
	`view == toysView`,
	`view == tracepointExplorer && viewItem == tracepoint`,
	`view == tracepointExplorer`,
	`view == transfer && kestudiocode.iec.transferToPlcVisible && kestudiocode.multipleItemsSelected`,
	`view == transfer && viewItem == 'TRA' && kestudiocode.iec.transferToPlcVisible`,
	`view == transformationHistory`,
	`view == transformationList && isWorkspaceTrusted`,
	`view == transformationList`,
	`view == translationProgress`,
	`view == treeLocalHistory && viewItem == localHistoryGrp || view == treeLocalHistoryExplorer && viewItem == localHistoryGrp`,
	`view == treeLocalHistory && viewItem == localHistoryItem || view == treeLocalHistoryExplorer && viewItem == localHistoryItem`,
	`view == treeLocalHistory || view == treeLocalHistoryExplorer`,
	`view == trelloTreeView && viewItem == list`,
	`view == trelloTreeView`,
	`view == trelloViewFavoriteList`,
	`view == trending`,
	`view == trinityOutline`,
	`view == trivy.issueview`,
	`view == trond-snekvik.devicetree.ctx && devicetree:dirtyConfig`,
	`view == trond-snekvik.devicetree.ctx`,
	`view == truffle-vscode.truffle && viewItem == genericproject`,
	`view == truffle-vscode.truffle && viewItem == localnetwork`,
	`view == truffle-vscode.truffle && viewItem == localproject`,
	`view == truffle-vscode.truffle && viewItem == network`,
	`view == truffle-vscode.truffle && viewItem == project`,
	`view == truffle-vscode.truffle`,
	`view == truffle-vscode.views.dashboard`,
	`view == truffle-vscode.views.deployments`,
	`view == truffle-vscode.views.explorer && viewItem != file`,
	`view == truffle-vscode.views.explorer && viewItem == file`,
	`view == truffle-vscode.views.explorer && viewItem == root`,
	`view == truffle-vscode.views.explorer`,
	`view == trunkTreeView && !trunk.grayOutNonBlockingIssuesEnabled || view == explorerTrunkTreeView && !trunk.grayOutNonBlockingIssuesEnabled`,
	`view == trunkTreeView && trunk.grayOutNonBlockingIssuesEnabled || view == explorerTrunkTreeView && trunk.grayOutNonBlockingIssuesEnabled`,
	`view == trunkTreeView && trunk.linterQueueStrategy == auto || view == explorerTrunkTreeView && trunk.linterQueueStrategy == auto`,
	`view == trunkTreeView && trunk.linterQueueStrategy == onDemand || view == explorerTrunkTreeView && trunk.linterQueueStrategy == onDemand`,
	`view == trunkTreeView`,
	`view == tsAstOutline`,
	`view == tsDev.baselines && viewItem == edited`,
	`view == tsDev.baselines`,
	`view == tta.attachmentView`,
	`view == tta.cfView && viewItem == folder`,
	`view == tta.cfView`,
	`view == tta.snowView && viewItem == after`,
	`view == tta.snowView && viewItem == before`,
	`view == tta.snowView && viewItem == data`,
	`view == tta.snowView && viewItem == planFolder || viewItem == suiteFolder || viewItem == caseFolder`,
	`view == tta.snowView && viewItem == planFolder`,
	`view == tta.snowView && viewItem == suiteFolder`,
	`view == tta.snowView`,
	`view == tuningHistoryProvider && viewItem == VE`,
	`view == tuningHistoryProvider && viewItem == viewTuningHistoryItem`,
	`view == tuningHistoryProvider`,
	`view == tunnelExplorerView`,
	`view == tuya-debug.memory`,
	`view == tuya-debug.peripherals && viewItem == field`,
	`view == tuya-debug.peripherals && viewItem == registerRO`,
	`view == tuya-debug.peripherals && viewItem == registerRW`,
	`view == tuya-debug.peripherals && viewItem == registerWO`,
	`view == tuya-debug.peripherals`,
	`view == tuya-debug.registers && viewItem == field`,
	`view == tuya-debug.registers && viewItem == register`,
	`view == tuya-debug.registers`,
	`view == twitchChat`,
	`view == twitchHighlighterTreeView || view == twitchHighlighterTreeView-explorer || view == twitchHighlighterTreeView-debug`,
	`view == twitterTimeline &&  viewItem == tweet-head-fav`,
	`view == twitterTimeline && viewItem == tweet-head-fav-r`,
	`view == twitterTimeline && viewItem == tweet-head-fav`,
	`view == twitterTimeline && viewItem == tweet-head-nofav-r`,
	`view == twitterTimeline && viewItem == tweet-head-nofav`,
	`view == twitterTimeline`,
	`view == txt-reader-menu.list`,
	`view == txtsyntaxHighlightExplore`,
	`view == typeChallenges.questions`,
	`view == tzcomponents && viewItem == component`,
	`view == tzcomponents`,
	`view == tznodeDependencies && viewItem == dependencies`,
	`view == tznodeDependencies && viewItem == devDependencies`,
	`view == tznodeDependencies && viewItem == outdatedDependency`,
	`view == tznodeDependencies`,
	`view == tznpmScripts && viewItem == script`,
	`view == tznpmScripts`,
	`view == tzpages && viewItem == page`,
	`view == tzpages`,
	`view == tzquickEntries && viewItem == quickEntry`,
	`view == ui5ApiReference`,
	`view == ui5Explorer`,
	`view == unit.test.explorer.vsTestTree1`,
	`view == unit.test.explorer.vsTestTree`,
	`view == universalConfigProviderView && viewItem == configFile`,
	`view == universalConfigProviderView && viewItem == configFolder`,
	`view == universalConfigProviderView`,
	`view == universalConnectionProviderView && viewItem == connection`,
	`view == universalConnectionProviderView`,
	`view == universalDashboardProviderView && viewItem == dashboardComponent`,
	`view == universalDashboardProviderView && viewItem == dashboardFramework`,
	`view == universalDashboardProviderView && viewItem == dashboard`,
	`view == universalDashboardProviderView`,
	`view == universalEndpointProviderView && viewItem == endpoint`,
	`view == universalEndpointProviderView`,
	`view == universalScriptProviderView && viewItem == job`,
	`view == universalScriptProviderView && viewItem == script`,
	`view == universalScriptProviderView`,
	`view == unoteFiles`,
	`view == unusedExports && viewItem != nothingFound`,
	`view == unusedExports && viewItem == file || viewItem == fileNotUsed`,
	`view == unusedExports && viewItem == fileNotUsed`,
	`view == unusedExports`,
	`view == upcoming-events && viewItem == event`,
	`view == upcoming-events`,
	`view == update && viewItem == script-1`,
	`view == update && viewItem == script-2`,
	`view == update && viewItem == script-3`,
	`view == update && viewItem == script-4`,
	`view == update && viewItem == script-5`,
	`view == update && viewItem == script-6`,
	`view == update && viewItem == script-config-ng`,
	`view == update && viewItem == script-ncu-apply`,
	`view == update && viewItem == script-ncu`,
	`view == update && viewItem == script-post-upd`,
	`view == uploadBrowser && viewItem == project`,
	`view == uploadBrowser`,
	`view == upsourceView && viewItem == review`,
	`view == upsourceView`,
	`view == user-active-contests`,
	`view == user-active-submissions`,
	`view == userWall &&  viewItem == tweet-head-fav`,
	`view == userWall && viewItem == tweet-head-fav-r`,
	`view == userWall && viewItem == tweet-head-fav`,
	`view == userWall && viewItem == tweet-head-nofav-r`,
	`view == userWall && viewItem == tweet-head-nofav`,
	`view == userWall`,
	`view == v2ex-collection && viewItem == dir`,
	`view == v2ex-collection`,
	`view == v2ex-custom && viewItem == dir`,
	`view == v2ex-custom`,
	`view == v2ex-explore && viewItem == dir`,
	`view == v2ex-explore`,
	`view == validationPanel`,
	`view == variableView && viewItem == variable`,
	`view == variableView`,
	`view == variablesView`,
	`view == vaultSecrets && viewItem == connection`,
	`view == vaultSecrets && viewItem == path`,
	`view == vaultSecrets && viewItem == secret`,
	`view == vaultSecrets && viewItem == server`,
	`view == vaultSecrets`,
	`view == vb-machines`,
	`view == vbaMacros`,
	`view == vbox && viewItem == vboxInsance`,
	`view == vbox`,
	`view == venus.textView`,
	`view == venusAssembly`,
	`view == veracodeUnifiedExplorer && viewItem == flaw`,
	`view == veracodeUnifiedExplorer && viewItem == sandbox`,
	`view == veracodeUnifiedExplorer`,
	`view == versions`,
	`view == vex-sidebar-view && viewItem == brainNameItem `,
	`view == vex-sidebar-view && viewItem == controllerAtmelItem && vexrobotics.vexcode.debugEnabled == true`,
	`view == vex-sidebar-view && viewItem == controllerRadioItem && vexrobotics.vexcode.debugEnabled == true`,
	`view == vex-sidebar-view && viewItem == dfuRecoverItem && vexcode.vexosBrainUpdateInProgress != true && vexcode.vexosControllerUpdateInProgress != true `,
	`view == vex-sidebar-view && viewItem == progItem `,
	`view == vex-sidebar-view && viewItem == teamNumberItem `,
	`view == vex-sidebar-view && viewItem == v5BrainItem `,
	`view == vex-sidebar-view && viewItem == vexosControllerUpdateItem && vexcode.vexosBrainUpdateInProgress != true && vexcode.vexosControllerUpdateInProgress != true `,
	`view == vex-sidebar-view && viewItem == vexosUpdateItem && vexcode.vexosBrainUpdateInProgress != true && vexcode.vexosControllerUpdateInProgress != true`,
	`view == vex-sidebar-view && viewItem =~ /BrainItem$/ `,
	`view == vex-sidebar-view && viewItem =~ /brainItem|vexosUpdateItem/ && vexrobotics.vexcode.debugEnabled == true`,
	`view == vex-sidebar-view && viewItem =~ /vexosControllerUpdateItem|controllerItem/ && vexrobotics.vexcode.debugEnabled == true`,
	`view == vhdlHierachy`,
	`view == vide-v4p-vhdl-hierarchy`,
	`view == vide-v4p-vhdl-library`,
	`view == vide-v4p-vhdl-search`,
	`view == vide-v4p-vhdl-snippets`,
	`view == view-activitybar || view == view-explorer`,
	`view == view-git-branch-merged || view == view-git-branch-merged2`,
	`view == view-leaf-pkg && viewItem == context-leaf-pkg-filter-user`,
	`view == view-leaf-pkg && viewItem == context-leaf-pkg-filters-container`,
	`view == view-leaf-pkg && viewItem =~ /context-leaf-pkg-(.*)-documented(.*)/`,
	`view == view-leaf-pkg && viewItem =~ /context-leaf-pkg-(installed|available)(.*)/`,
	`view == view-leaf-pkg`,
	`view == view-leaf-prf && viewItem =~ /context-leaf-pkg-(installed|available)(.*)/`,
	`view == view-leaf-prf && viewItem =~ /context-leaf-pkg-installed-upgradable(.*)/`,
	`view == view-leaf-prf && viewItem =~ /context-leaf-prf-.*/`,
	`view == view-leaf-prf && viewItem =~ /context-leaf-prf-other.*/`,
	`view == view-leaf-prf`,
	`view == view-leaf-rmt && viewItem == context-leaf-rmt-disabled`,
	`view == view-leaf-rmt && viewItem == context-leaf-rmt-enabled`,
	`view == view-leaf-rmt && viewItem =~ /context-leaf-rmt-.*/`,
	`view == view-leaf-rmt`,
	`view == view-legato-system && viewItem =~ /context-legato-adef(.)/`,
	`view == view-legato-system && viewItem =~ /context-legato-adef(.*)/`,
	`view == view-legato-system && viewItem =~ /context-legato-apps(.)/`,
	`view == view-legato-system && viewItem =~ /context-legato-apps(.*)/`,
	`view == view-legato-system && viewItem =~ /context-legato-cdef(.)/`,
	`view == view-legato-system && viewItem =~ /context-legato-cdef(.*)/`,
	`view == view-legato-system && viewItem =~ /context-legato-sdef(.*)/`,
	`view == view.mark`,
	`view == view.readLater`,
	`view == viewConfigurations && viewItem == iotconfiguration`,
	`view == viewConfigurations && viewItem == iotenviroment`,
	`view == viewConfigurations && viewItem == iotenviroments`,
	`view == viewConfigurations`,
	`view == viewDevices && viewItem == iotdevice`,
	`view == viewDevices && viewItem == iotdto_off`,
	`view == viewDevices && viewItem == iotdto_on`,
	`view == viewDevices && viewItem == iotdtos`,
	`view == viewDevices && viewItem == iotgpios`,
	`view == viewDevices && viewItem == iotitemtree`,
	`view == viewDevices && viewItem == iotpackage_installed`,
	`view == viewDevices && viewItem == iotpackages_not_installed`,
	`view == viewDevices && viewItem == iotpackages`,
	`view == viewDevices`,
	`view == virtual-folders`,
	`view == visual-token-designer.tokenArtifactExplorer`,
	`view == visual-token-designer.tokenDefinitionExplorer`,
	`view == visual-token-designer.tokenFormulaExplorer`,
	`view == visualvm-control-panel && !visualvm.handleProjectProcess && visualvm.featureSet >= 1`,
	`view == visualvm-control-panel && !visualvm.windowToFront && visualvm.featureSet >= 2`,
	`view == visualvm-control-panel && viewItem == visualvm.configure`,
	`view == visualvm-control-panel && viewItem == visualvm.cpuSampler || viewItem == visualvm.memorySampler`,
	`view == visualvm-control-panel && viewItem == visualvm.cpuSampler`,
	`view == visualvm-control-panel && viewItem == visualvm.heapDump`,
	`view == visualvm-control-panel && viewItem == visualvm.jfr`,
	`view == visualvm-control-panel && viewItem == visualvm.memorySampler`,
	`view == visualvm-control-panel && viewItem == visualvm.process`,
	`view == visualvm-control-panel && viewItem == visualvm.threadDump`,
	`view == visualvm-control-panel && visualvm.featureSet >= 1`,
	`view == visualvm-control-panel && visualvm.handleProjectProcess && visualvm.featureSet >= 1`,
	`view == visualvm-control-panel && visualvm.preselectView != 1 && visualvm.featureSet >= 1`,
	`view == visualvm-control-panel && visualvm.preselectView != 2 && visualvm.featureSet >= 1`,
	`view == visualvm-control-panel && visualvm.preselectView != 3 && visualvm.featureSet >= 1`,
	`view == visualvm-control-panel && visualvm.preselectView != 4 && visualvm.featureSet >= 1`,
	`view == visualvm-control-panel && visualvm.preselectView == 1 && visualvm.featureSet >= 1`,
	`view == visualvm-control-panel && visualvm.preselectView == 2 && visualvm.featureSet >= 1`,
	`view == visualvm-control-panel && visualvm.preselectView == 3 && visualvm.featureSet >= 1`,
	`view == visualvm-control-panel && visualvm.preselectView == 4 && visualvm.featureSet >= 1`,
	`view == visualvm-control-panel && visualvm.projectOpen && !visualvm.sourcesIntegration && visualvm.featureSet >= 2`,
	`view == visualvm-control-panel && visualvm.projectOpen && visualvm.sourcesIntegration && visualvm.featureSet >= 2`,
	`view == visualvm-control-panel && visualvm.windowToFront && visualvm.featureSet >= 2`,
	`view == vmssoftware.project-dep.projectDependencies && viewItem == depends`,
	`view == vmssoftware.project-dep.projectDependencies && viewItem == folder`,
	`view == vmssoftware.project-dep.projectDependencies`,
	`view == vmssoftware.project-dep.projectDescription && viewItem == buildName`,
	`view == vmssoftware.project-dep.projectDescription && viewItem == editable`,
	`view == vmssoftware.project-dep.projectDescription`,
	`view == vncServers`,
	`view == vplcontrol`,
	`view == vsagile && viewItem == rootBoard`,
	`view == vsagile`,
	`view == vsc-packages-activitybar-folders && vsc-packages-folders-loaded`,
	`view == vsc-postman-side`,
	`view == vsc-workspace-sidebar-webview-workspace && vsc-workspace-sidebar-list-loaded && !config.workspaceSidebar.showFolderHierarchy`,
	`view == vsc-workspace-sidebar-webview-workspace && vsc-workspace-sidebar-list-loaded && config.workspaceSidebar.showFolderHierarchy`,
	`view == vsc-workspace-sidebar-webview-workspace && vsc-workspace-sidebar-list-loaded`,
	`view == vscode-anchor-view-programs && viewItem == program`,
	`view == vscode-anchor-view-programs`,
	`view == vscode-anchor-view-tests && viewItem == test`,
	`view == vscode-anchor-view-tests`,
	`view == vscode-cnb.ings-list-webview && vscode-cnb.isAuthorized && !vscode-cnb.ingsList.isRefreshing && vscode-cnb.ingsList.pageIndex > 2`,
	`view == vscode-cnb.ings-list-webview`,
	`view == vscode-connection-explorer && viewItem == connection`,
	`view == vscode-connection-explorer`,
	`view == vscode-dapr.views.applications && viewItem == application`,
	`view == vscode-dapr.views.applications`,
	`view == vscode-docker.views.dockerContexts && viewItem =~ /Context/i`,
	`view == vscode-docker.views.dockerContexts && viewItem =~ /^customContext/i`,
	`view == vscode-docker.views.dockerContexts`,
	`view == vscode-edge-devtools-view.targets && viewItem == cdpTarget || viewItem == cdpTargetClosing`,
	`view == vscode-edge-devtools-view.targets && viewItem == cdpTargetProperty`,
	`view == vscode-edge-devtools-view.targets && viewItem == cdpTarget`,
	`view == vscode-edge-devtools-view.targets`,
	`view == vscode-embedded.rtosViewer && vscode-embedded.rtosViewer.activeRtos != 'unknown'`,
	`view == vscode-embedded.rtosViewer && vscode-embedded.rtosViewer.activeRtos == 'threadx'`,
	`view == vscode-embedded.rtosViewer && vscode-embedded.rtosViewer.activeRtos == 'zephyr'`,
	`view == vscode-embedded.rtosViewer && vscode-embedded.rtosViewer.activeRtos =~ /^threadx$|^zephyr$/`,
	`view == vscode-embedded.webview.registerView`,
	`view == vscode-fontawesome-gallery-sidebar`,
	`view == vscode-journal-view-explorer`,
	`view == vscode-journal-view`,
	`view == vscode-js-profile-flame.realtime && !vscodeJsProfileFlameSplitCharts`,
	`view == vscode-js-profile-flame.realtime && vscodeJsProfileFlameSplitCharts`,
	`view == vscode-js-profile-flame.realtime`,
	`view == vscode-notion-bookmarks`,
	`view == vscode-notion-recents`,
	`view == vscode-openpgp.privateKeysView || view == vscode-openpgp.publicKeysView`,
	`view == vscode-openpgp.privateKeysView`,
	`view == vscode-openpgp.publicKeysView`,
	`view == vscode-rapidapi-client.panel && rapidapi:paw:signedIn && rapidapi:paw:cloudSync`,
	`view == vscode-rapidapi-client.panel && rapidapi:paw:signedIn == false`,
	`view == vscode-rapidapi-client.panel && rapidapi:paw:signedIn`,
	`view == vscode-rapidapi-client.panel`,
	`view == vscode-sailpoint-identitynow.view && viewItem == connector-rule`,
	`view == vscode-sailpoint-identitynow.view && viewItem == connector-rules`,
	`view == vscode-sailpoint-identitynow.view && viewItem == disabledWorkflow`,
	`view == vscode-sailpoint-identitynow.view && viewItem == enabledWorkflow`,
	`view == vscode-sailpoint-identitynow.view && viewItem == identity-profile`,
	`view == vscode-sailpoint-identitynow.view && viewItem == identity-profiles`,
	`view == vscode-sailpoint-identitynow.view && viewItem == lifecycle-state`,
	`view == vscode-sailpoint-identitynow.view && viewItem == provisioning-policies`,
	`view == vscode-sailpoint-identitynow.view && viewItem == provisioning-policy`,
	`view == vscode-sailpoint-identitynow.view && viewItem == schema`,
	`view == vscode-sailpoint-identitynow.view && viewItem == schemas`,
	`view == vscode-sailpoint-identitynow.view && viewItem == service-desk-integration`,
	`view == vscode-sailpoint-identitynow.view && viewItem == service-desk-integrations`,
	`view == vscode-sailpoint-identitynow.view && viewItem == source`,
	`view == vscode-sailpoint-identitynow.view && viewItem == sources`,
	`view == vscode-sailpoint-identitynow.view && viewItem == tenant`,
	`view == vscode-sailpoint-identitynow.view && viewItem == transform`,
	`view == vscode-sailpoint-identitynow.view && viewItem == transforms`,
	`view == vscode-sailpoint-identitynow.view && viewItem == workflows`,
	`view == vscode-sailpoint-identitynow.view && viewItem =~ /XXX$/i`,
	`view == vscode-sailpoint-identitynow.view && viewItem =~ /^(source|identity-profile|connector-rule|transform)$/i`,
	`view == vscode-sailpoint-identitynow.view && viewItem =~ /workflow$/i`,
	`view == vscode-sailpoint-identitynow.view`,
	`view == vscode-solution-explorer && viewItem == solution`,
	`view == vscode-spotify-playlists`,
	`view == vscode-spotify-tracks`,
	`view == vscode-tye.views.services && viewItem =~ /application/`,
	`view == vscode-tye.views.services && viewItem =~ /attachable/ && viewItem =~ /replica/`,
	`view == vscode-tye.views.services && viewItem =~ /attachable/ && viewItem =~ /service/`,
	`view == vscode-tye.views.services && viewItem =~ /browsable/`,
	`view == vscode-tye.views.services && viewItem =~ /hasLogs/`,
	`view == vscode-tye.views.services`,
	`view == vscode-vercel-deployments && viewItem == deployment || viewItem == deploymentLog`,
	`view == vscode-vercel-deployments && viewItem == deploymentLog`,
	`view == vscode-vercel-deployments`,
	`view == vscode-vercel-projects`,
	`view == vscode-vercel-teams`,
	`view == vscode-webresource-explorer && viewItem == webresource`,
	`view == vscodeOne &&  viewItem == see`,
	`view == vscodeOne`,
	`view == vscodeWorkspaceSwitcherViewInActivityBar && !vscodeWorkspaceSwitcherViewContainerTreeViewShow`,
	`view == vscodeWorkspaceSwitcherViewInActivityBar && viewItem != folder && vscodeWorkspaceSwitcherViewContainerDeleteWorkspaceButtonShow`,
	`view == vscodeWorkspaceSwitcherViewInActivityBar && viewItem != folder`,
	`view == vscodeWorkspaceSwitcherViewInActivityBar && viewItem == folder`,
	`view == vscodeWorkspaceSwitcherViewInActivityBar && vscodeWorkspaceSwitcherViewContainerTreeViewShow`,
	`view == vscodeWorkspaceSwitcherViewInActivityBar`,
	`view == vscodeWorkspaceSwitcherViewInExplorer && !vscodeWorkspaceSwitcherViewContainerTreeViewShow`,
	`view == vscodeWorkspaceSwitcherViewInExplorer && viewItem != folder && vscodeWorkspaceSwitcherViewContainerDeleteWorkspaceButtonShow`,
	`view == vscodeWorkspaceSwitcherViewInExplorer && viewItem != folder`,
	`view == vscodeWorkspaceSwitcherViewInExplorer && viewItem == folder`,
	`view == vscodeWorkspaceSwitcherViewInExplorer && vscodeWorkspaceSwitcherViewContainerTreeViewShow`,
	`view == vscodeWorkspaceSwitcherViewInExplorer`,
	`view == vscodeZhihu &&  viewItem == see`,
	`view == vscodeZhihu`,
	`view == vsmp.fav`,
	`view == vsmp.mediaList && viewItem == search`,
	`view == vsmp.mediaList`,
	`view == vsmp.openFolder`,
	`view == vsnotes`,
	`view == vstest.explorer.vsTestTree`,
	`view == vthostExplorer && viewItem =~ /^vthost-connect$/i`,
	`view == vthostExplorer && viewItem =~ /^vthost/i`,
	`view == vthostExplorer`,
	`view == vthostHostpadExplorer && viewItem =~ /^vthostpad/i`,
	`view == vtoolInspectData`,
	`view == vueView`,
	`view == vulnerabilitiesView`,
	`view == watchpointView && viewItem == watchpointCondition`,
	`view == watchpointView && viewItem == watchpoint`,
	`view == watchpointView`,
	`view == watchpoints`,
	`view == waveAnalysisTreeView && viewItem == NotRunning || viewItem == Issue && waveAnalysis.extensionIsActive`,
	`view == waveAnalysisTreeView && viewItem == Running && waveAnalysis.extensionIsActive`,
	`view == waveAnalysisTreeView && waveAnalysis.extensionIsActive`,
	`view == weaveDependencies`,
	`view == weaveScenarios && viewItem == activeScenario`,
	`view == weaveScenarios && viewItem == inputItem`,
	`view == weaveScenarios && viewItem == inputs`,
	`view == weaveScenarios && viewItem == scenario`,
	`view == weaveScenarios && viewItem == transformationItem`,
	`view == webososeDevices && viewItem == device`,
	`view == webososeDevices && viewItem == installed || viewItem == running`,
	`view == webososeDevices && viewItem == installed`,
	`view == webososeDevices && viewItem == running`,
	`view == webososeDevices`,
	`view == webostvDevices && viewItem == device`,
	`view == webostvDevices && viewItem == installed || viewItem == running`,
	`view == webostvDevices && viewItem == installed`,
	`view == webostvDevices && viewItem == running`,
	`view == webostvDevices`,
	`view == webostvSimulator && viewItem == simulator`,
	`view == webostvSimulator`,
	`view == weiboNewsItem`,
	`view == wikiView && viewItem == 'wiki-node-info'`,
	`view == wikilens.wiki`,
	`view == wordupProjectView && viewItem == projectPaused`,
	`view == wordupProjectView && viewItem == projectRunning`,
	`view == wordupProjectView && viewItem == projectStopped`,
	`view == wordupProjectView && viewItem == project`,
	`view == wordupProjectView`,
	`view == workSpaceCommandExplorer && viewItem == file`,
	`view == workSpaceCommandExplorer`,
	`view == workbench.debug.variablesView || view == workbench.debug.watchExpressionsView && debug.switchVarFormat`,
	`view == workbench.explorer.fileView && editorIsOpen && config.locateThisDocument.explorerIcons.show && config.locateThisDocument.explorerIcons.locateActiveDocument && resourceScheme == file`,
	`view == workbench.explorer.fileView && editorIsOpen && config.locateThisDocument.explorerIcons.show && config.locateThisDocument.explorerIcons.openActiveDocumentInFileExplorer && resourceScheme == file && remoteName == ''`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount == 1 && config.git-buttons-fileview.pullContext || view == workbench.explorer.fileView && config.git-buttons-fileview.pull`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount == 1 && config.git-buttons-fileview.pushContext || view == workbench.explorer.fileView && config.git-buttons-fileview.push`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount > 1 && config.git-buttons-fileview.pullContext`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount > 1 && config.git-buttons-fileview.pushContext`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.addRemote`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.branchFrom`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.branch`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.checkoutDetached`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.checkout`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.cherryPick`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.cleanAllTracked`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.cleanAllUntracked`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.cleanAll`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.clean`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.cloneRecursive`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.clone`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.closeAllDiffEditors`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.close`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.commitAllAmendNoVerify`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.commitAllAmend`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.commitAllNoVerify`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.commitAllSignedNoVerify`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.commitAllSigned`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.commitAll`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.commitEmptyNoVerify`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.commitEmpty`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.commitNoVerify`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.commitStagedAmendNoVerify`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.commitStagedAmend`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.commitStagedNoVerify`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.commitStagedSignedNoVerify`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.commitStagedSigned`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.commitStaged`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.commit`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.createTag`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.deleteBranch`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.deleteTag`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.fetchAll`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.fetchPrune`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.fetch`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.ignore`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.init`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.merge`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.openAllChanges`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.openChange`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.openFile2`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.openFile`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.openHEADFile`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.openRepository`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.publish`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.pullFrom`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.pullRebase`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.pushForce`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.pushTags`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.pushToForce`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.pushTo`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.pushWithTagsForce`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.pushWithTags`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.rebaseAbort`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.rebase`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.refresh`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.removeRemote`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.renameBranch`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.rename`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.restoreCommitTemplate`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.revealInExplorer`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.revertChange`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.revertSelectedRanges`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.setLogLevel`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.showOutput`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.stageAllMerge`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.stageAllTracked`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.stageAllUntracked`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.stageAll`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.stageChange`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.stageSelectedRanges`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.stage`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.stashApplyLatest`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.stashApply`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.stashDropAll`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.stashDrop`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.stashIncludeUntracked`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.stashPopLatest`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.stashPop`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.stash`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.syncRebase`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.sync`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.undoCommit`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.unstageAll`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.unstageSelectedRanges`,
	`view == workbench.explorer.fileView && gitOpenRepositoryCount >= 1 && config.git-buttons-fileview.unstage`,
	`view == workbench.explorer.fileView && workspaceFolderCount == 1 && config.locateThisDocument.explorerIcons.show && config.locateThisDocument.explorerIcons.openWorkspaceRootFolderInFileExplorer && remoteName == ''`,
	`view == workbench.explorer.fileView`,
	`view == workbench.panel.comments`,
	`view == workbench.panel.markers.view`,
	`view == workbench.view.explorer`,
	`view == workbench.view.testing && hasFailedTests && !virtualWorkspace && shellExecutionSupported`,
	`view == workbench.view.testing`,
	`view == workflowExplorer && !workflowExplorer:hasCollapsible`,
	`view == workflowExplorer && isExtensionReady && !isSettingsChanged`,
	`view == workflowExplorer && isExtensionReady && isSettingsChanged`,
	`view == workflowExplorer && isExtensionReady`,
	`view == workflowExplorer && workflowExplorer:hasCollapsible`,
	`view == workingSets && viewItem == workingSetItem`,
	`view == workingSets && viewItem == workingSet`,
	`view == workingSets`,
	`view == workspace-explorer-view && viewItem != workspace-overview`,
	`view == workspace-explorer-view && viewItem == tcc || view == workspace-explorer-view && viewItem == theorem`,
	`view == workspace-explorer-view && viewItem == tccs-overview`,
	`view == workspace-explorer-view && viewItem == theory`,
	`view == workspace-explorer-view && viewItem == workspace-overview`,
	`view == workspace-explorer-view`,
	`view == workspaceAnchors`,
	`view == workspaceExplorer  && config.workspaceExplorer.workspaceStorageDirectory != ''`,
	`view == workspaceExplorer && config.workspaceExplorer.workspaceStorageDirectory != ''`,
	`view == workspaceExplorer && viewItem == folder && config.workspaceExplorer.enableCustomIconSearch == true`,
	`view == workspaceExplorer && viewItem == folder`,
	`view == workspaceExplorer && viewItem == workspaceFile && config.workspaceExplorer.enableCustomIconSearch == true`,
	`view == workspaceExplorer && viewItem == workspaceFile`,
	`view == workspaceExplorer`,
	`view == workspaceProject || view == workspaceProjectExt`,
	`view == workspaceRelatedContent`,
	`view == workspaceViewer && !r.liveShare:isGuest`,
	`view == workspaceViewer && viewItem == rootNode`,
	`view == workspaceViewer`,
	`view == worldwide-stats`,
	`view == wp-cli && viewItem == plugin`,
	`view == wp-cli && viewItem == section`,
	`view == wp-cli`,
	`view == wrapplicationview && viewItem =~ /type=project_root/`,
	`view == wrapplicationview`,
	`view == wrresourceview && resourceview.showDeleteCommand`,
	`view == wrresourceview && viewItem == localFsRoot`,
	`view == wrresourceview && viewItem =~ /canConnect=false/`,
	`view == wrresourceview && viewItem =~ /canConnect=true/ && viewItem =~ /canLaunch=false/`,
	`view == wrresourceview && viewItem =~ /canDelOrRename=true/`,
	`view == wrresourceview && viewItem =~ /canLaunch=false/ && viewItem =~ /canTracecompass=true/`,
	`view == wrresourceview && viewItem =~ /canOpenBySVRCP=true/`,
	`view == wrresourceview && viewItem =~ /canSvStart=true/`,
	`view == wrresourceview && viewItem =~ /canSvStop=true/`,
	`view == wrresourceview && viewItem =~ /isLocalFileExists=true/`,
	`view == wrresourceview && viewItem =~ /isNeedLogin=true/`,
	`view == wrresourceview && viewItem =~ /isParentNode=false/ && viewItem =~ /hasUrlLink=true/`,
	`view == wrresourceview && viewItem =~ /isSim=true/ && viewItem =~ /canLaunch=false/`,
	`view == wrresourceview && viewItem =~ /isSim=true/ && viewItem =~ /canLaunch=true/`,
	`view == wrresourceview && viewItem =~ /isSim=true/`,
	`view == wrresourceview`,
	`view == wsSnippetsExplorer && snippets.workspaceState == fileAvailable`,
	`view == wsSnippetsExplorer && viewItem =~ /^\\S*:(down|up&down)$/ && snippets.workspaceState == fileAvailable`,
	`view == wsSnippetsExplorer && viewItem =~ /^\\S*:(up|up&down)$/ && snippets.workspaceState == fileAvailable`,
	`view == wsSnippetsExplorer && viewItem =~ /^snippet(:\\S+)?$/ && snippets.workspaceState == fileAvailable`,
	`view == wsSnippetsExplorer && viewItem =~ /^snippet(:\\S+)?$/`,
	`view == wsSnippetsExplorer && viewItem =~ /^snippetFolder(:\\S+)?$/ && snippets.workspaceState == fileAvailable`,
	`view == wsSnippetsExplorer`,
	`view == wskActivations`,
	`view == wskEntities`,
	`view == wskdeploy`,
	`view == wulingshan-browse`,
	`view == wulingshan-collect`,
	`view == wulingshan-menu && rwulingshan.panel != collect`,
	`view == wulingshan-menu`,
	`view == wxContainers && viewItem =~ /^(local)_(local|created|dead|exited|paused|terminated)$/i`,
	`view == wxContainers && viewItem =~ /^(local|proxy)_(local|created|dead|exited|paused|terminated)$/i`,
	`view == wxContainers && viewItem =~ /^(local|proxy)_(paused|restarting|running)$/i`,
	`view == wxContainers && viewItem =~ /^(local|proxy)_running$/i`,
	`view == wxContainers && viewItem =~ /^local_(created|dead|exited|paused|terminated|paused|restarting|running)$/i`,
	`view == wxContainers && viewItem =~ /^local_(paused|restarting|running)$/i`,
	`view == wxContainers && viewItem =~ /^running_running$/i`,
	`view == wxContainers`,
	`view == xPackActions && viewItem == action`,
	`view == xPackActions && viewItem == command`,
	`view == xPackActions && viewItem == configuration`,
	`view == xPackActions && viewItem == package`,
	`view == xPackActions`,
	`view == xmakeExplorer && viewItem == target`,
	`view == xmakeExplorer`,
	`view == xmakeOptions`,
	`view == yandex-music-play-lists && yandexMusic.isAuthorized`,
	`view == yandex-music-play-lists || view == yandex-music-chart || view == yandex-music-recommendations`,
	`view == yandex-music-search && yandexMusic.hasSearchResult`,
	`view == yandex-music-search`,
	`view == z-reader-menu && zreader.panel != collect`,
	`view == z-reader-menu && zreader.panel == collect`,
	`view == z-reader-menu && zreader.panel == local`,
	`view == z-reader-menu && zreader.panel == online`,
	`view == z-reader-menu`,
	`view == z-reader-mini-menu`,
	`view == z-reader-plus-menu && zreader.panel != collect`,
	`view == z-reader-plus-menu && zreader.panel == collect`,
	`view == z-reader-plus-menu && zreader.panel == local`,
	`view == z-reader-plus-menu && zreader.panel == online`,
	`view == z-reader-plus-menu`,
	`view == zeplin.views.barrels && viewItem =~ /\\bbarrel\\b/`,
	`view == zeplin.views.barrels && viewItem =~ /\\bpinnable\\b/`,
	`view == zeplin.views.barrels || view == zeplin.views.activity`,
	`view == zeplin.views.barrels`,
	`view == zeplin.views.pinned && zeplin:sidebar:anyPinnedItems`,
	`view == zeplin.views.pinned`,
	`view == zeus-build-explorer && !runBridge`,
	`view == zeus-build-explorer && !stopDev`,
	`view == zeus-build-explorer && runBridge`,
	`view == zeus-build-explorer && stopDev`,
	`view == zeus-build-explorer`,
	`view == zeus-status-explorer && viewItem =~ /statusDeleteItem/`,
	`view == zeus-status-explorer && viewItem =~ /statusEditItem/`,
	`view == zeus-status-explorer && viewItem =~ /statusSelectTargetItem/`,
	`view == zeus-status-explorer`,
	`view == zeus-user-explorer`,
	`view == zhihu-collection && viewItem == collect-item`,
	`view == zhihu-collection`,
	`view == zhihu-feed && viewItem == event`,
	`view == zhihu-feed && viewItem == feed`,
	`view == zhihu-feed`,
	`view == zhihu-hotStories`,
	`view == zhihuDailyContent`,
	`view == zipExplorer && viewItem != zip`,
	`view == zipExplorer && viewItem == zip`,
	`view == zipExplorer`,
	`view == zowe.ds.explorer && viewItem == profile_fav && !listMultiSelection`,
	`view == zowe.ds.explorer && viewItem =~ /^(?!.*_fav.*)session.*/ && !listMultiSelection`,
	`view == zowe.ds.explorer && viewItem =~ /^(?!.*_fav.*)session.*/`,
	`view == zowe.ds.explorer && viewItem =~ /^(?!.*_fav|.*isFilterSearch)(member|migr|ds|pds).*/`,
	`view == zowe.ds.explorer && viewItem =~ /^(?=.*_validate=false.*)session.*/ && !listMultiSelection`,
	`view == zowe.ds.explorer && viewItem =~ /^(?=.*_validate=true.*)session.*/ && !listMultiSelection`,
	`view == zowe.ds.explorer && viewItem =~ /^(ds.*|^pds.*)/`,
	`view == zowe.ds.explorer && viewItem =~ /^(ds|pds).*/ && !listMultiSelection`,
	`view == zowe.ds.explorer && viewItem =~ /^(member.*|ds.*)/`,
	`view == zowe.ds.explorer && viewItem =~ /^(pds|ds|migr).*_fav.*/`,
	`view == zowe.ds.explorer && viewItem =~ /^(pds|migr|member|ds|vsam).*/`,
	`view == zowe.ds.explorer && viewItem =~ /^ds.*/ && !listMultiSelection`,
	`view == zowe.ds.explorer && viewItem =~ /^ds.*/`,
	`view == zowe.ds.explorer && viewItem =~ /^fileError.*/`,
	`view == zowe.ds.explorer && viewItem =~ /^member.*/  && !listMultiSelection`,
	`view == zowe.ds.explorer && viewItem =~ /^member.*/ && !listMultiSelection`,
	`view == zowe.ds.explorer && viewItem =~ /^member.*/`,
	`view == zowe.ds.explorer && viewItem =~ /^migr.*/`,
	`view == zowe.ds.explorer && viewItem =~ /^pds.*/ && !listMultiSelection`,
	`view == zowe.ds.explorer && viewItem =~ /^pds.*/`,
	`view == zowe.ds.explorer && viewItem =~ /^session.*_fav.*/ && !listMultiSelection`,
	`view == zowe.ds.explorer`,
	`view == zowe.explorer &&  viewItem =~ /^(?!.*_fav.*)session.*/`,
	`view == zowe.explorer &&  viewItem =~ /^profile.*/`,
	`view == zowe.explorer && viewItem =~ /^ds.*/`,
	`view == zowe.explorer && viewItem =~ /^member.*/`,
	`view == zowe.explorer && viewItem =~ /^pds.*/`,
	`view == zowe.jobs.explorer && viewItem == profile_fav && !listMultiSelection`,
	`view == zowe.jobs.explorer && viewItem =~ /^(?!.*_fav.*)(server|job).*/`,
	`view == zowe.jobs.explorer && viewItem =~ /^(?!.*_fav.*)job.*/ && !listMultiSelection`,
	`view == zowe.jobs.explorer && viewItem =~ /^(?!.*_fav.*)job.*/`,
	`view == zowe.jobs.explorer && viewItem =~ /^(?!.*_fav.*)server.*/  && !listMultiSelection`,
	`view == zowe.jobs.explorer && viewItem =~ /^(?!.*_fav.*)server.*/ && !listMultiSelection`,
	`view == zowe.jobs.explorer && viewItem =~ /^(?!.*_fav.*)server.*/`,
	`view == zowe.jobs.explorer && viewItem =~ /^(?=.*_validate=false.*)server.*/ && !listMultiSelection`,
	`view == zowe.jobs.explorer && viewItem =~ /^(?=.*_validate=true.*)server.*/ && !listMultiSelection`,
	`view == zowe.jobs.explorer && viewItem =~ /^job.*_fav.*/`,
	`view == zowe.jobs.explorer && viewItem =~ /^server.*_fav.*/ && !listMultiSelection`,
	`view == zowe.jobs.explorer && viewItem =~ /^spool.*/`,
	`view == zowe.jobs.explorer`,
	`view == zowe.uss.explorer && viewItem == profile_fav && !listMultiSelection`,
	`view == zowe.uss.explorer && viewItem =~ /^(?!(directory|ussSession|favorite|profile_fav))/`,
	`view == zowe.uss.explorer && viewItem =~ /^(?!(ussSession.*|profile.*)).*_fav.*/`,
	`view == zowe.uss.explorer && viewItem =~ /^(?!(ussSession.*|profile.*|directory.*)).*_fav.*/`,
	`view == zowe.uss.explorer && viewItem =~ /^(?!(ussSession|favorite|.*_fav))/`,
	`view == zowe.uss.explorer && viewItem =~ /^(?!(ussSession|favorite|profile_fav))/ && !listMultiSelection`,
	`view == zowe.uss.explorer && viewItem =~ /^(?!.*_fav.*)(textFile.*|binaryFile.*|directory.*)/`,
	`view == zowe.uss.explorer && viewItem =~ /^(?!.*_fav.*)ussSession.*/ && !listMultiSelection`,
	`view == zowe.uss.explorer && viewItem =~ /^(?=.*_validate=false.*)ussSession.*/ && !listMultiSelection`,
	`view == zowe.uss.explorer && viewItem =~ /^(?=.*_validate=true.*)ussSession.*/ && !listMultiSelection`,
	`view == zowe.uss.explorer && viewItem =~ /^(directory|(?!.*_fav.*)ussSession.*)/ && !listMultiSelection`,
	`view == zowe.uss.explorer && viewItem =~ /^binaryFile.*/`,
	`view == zowe.uss.explorer && viewItem =~ /^directory.*/ && !listMultiSelection`,
	`view == zowe.uss.explorer && viewItem =~ /^directory.*/`,
	`view == zowe.uss.explorer && viewItem =~ /^textFile.*/`,
	`view == zowe.uss.explorer && viewItem =~ /^textFile|binaryFile/ && !listMultiSelection`,
	`view == zowe.uss.explorer && viewItem =~ /^ussSession.*/ && !listMultiSelection`,
	`view == zowe.uss.explorer && viewItem =~ /^ussSession.*_fav.*/ && !listMultiSelection`,
	`view == zowe.uss.explorer`,
	`view === extension.vsKubernetesHelmRepoExplorer`,
	`view === reactHierarchy`,
	`view =~ '/(servers)/' && !viewItem`,
	`view =~ '/(servers)/' && viewItem =~ '/^(RSPConnected)/'`,
	`view =~ '/(servers)/' && viewItem =~ '/^(RSPStarted|RSPConnected)/'`,
	`view =~ '/(servers)/' && viewItem =~ '/^(RSPStarted|RSPStarting|RSPStopping)/'`,
	`view =~ '/(servers)/' && viewItem =~ '/^(RSPUnknown|RSPStopped)/'`,
	`view =~ '/(servers)/' && viewItem =~ '/^(Started|Debugging)/'`,
	`view =~ '/(servers)/' && viewItem =~ '/^(Started|Debugging|Unknown)/'`,
	`view =~ '/(servers)/' && viewItem =~ '/^(Starting|Started|Debugging|Stopping|Stopped)/'`,
	`view =~ '/(servers)/' && viewItem =~ '/^(Stopped|Unknown)/'`,
	`view =~ '/(servers)/' && viewItem =~ '/^Stopped/'`,
	`view =~ '/(servers)/' && viewItem =~ /^(Starting|Started|Debugging|Stopping|Stopped|Unknown)/'`,
	`view =~ '/(servers)/' && viewItem =~ /^(Starting|Started|Debugging|Stopping|Stopped|Unknown)/`,
	`view =~ '/(servers)/' && viewItem =~ /^(Starting|Started|Debugging|Stopping|Stopped|^Unknown)/`,
	`view =~ '/(servers)/' && viewItem =~ /^(Starting|Stopping|Unknown)/`,
	`view =~ '/(servers)/' && viewItem =~ /^(Synchronized|[+|-|Full]* Publish|Unknown)/`,
	`view =~ '/(servers)/'`,
	`view =~ /(allProjectIssues)/`,
	`view =~ /(allProjectMrs)/`,
	`view =~ /(atlascode.views.bb.pipelinesTreeView)/ && atlascode:isBBAuthenticated`,
	`view =~ /(atlascode.views.bb.pipelinesTreeView)/`,
	`view =~ /(atlascode.views.jira.customJql)/ && atlascode:isJiraAuthenticated`,
	`view =~ /(atlascode.views.jira.customJql)/`,
	`view =~ /(azdopr|azdoprStatus):azdo/ && viewItem =~ /description/`,
	`view =~ /(azdopr|azdoprStatus):azdo/ && viewItem =~ /filechange(.*):unviewed/`,
	`view =~ /(azdopr|azdoprStatus):azdo/ && viewItem =~ /filechange(.*):viewed/`,
	`view =~ /(azdopr|azdoprStatus):azdo/ && viewItem =~ /filechange/`,
	`view =~ /(azdopr|azdoprStatus):azdo/ && viewItem =~ /filechange:MODIFY/`,
	`view =~ /(azureWorkspace|azureResourceGroups)/ && viewItem =~ /Function;.*;Disabled;/i`,
	`view =~ /(azureWorkspace|azureResourceGroups)/ && viewItem =~ /Function;.*;Enabled;/i`,
	`view =~ /(azureWorkspace|azureResourceGroups)/ && viewItem =~ /Function;/i`,
	`view =~ /(azureWorkspace|azureResourceGroups)/ && viewItem =~ /Function;Http;/i`,
	`view =~ /(azureWorkspace|azureResourceGroups)/ && viewItem =~ /Local;ReadWrite;Function;/i`,
	`view =~ /(currentBranchInfo|issuesAndMrs)/ && gitlab.sidebarView == list`,
	`view =~ /(currentBranchInfo|issuesAndMrs)/ && gitlab.sidebarView == tree`,
	`view =~ /(currentBranchInfo|issuesAndMrs)/`,
	`view =~ /(currentBranchInfo|issuesAndMrs|snippetNotes|utilityTools)/`,
	`view =~ /(dbuxTraceDetailsView|dbuxGlobalAnalysisView|dbuxApplicationsView)/ && !dbuxWebView.context.dbux-graph.isActive`,
	`view =~ /(dbuxTraceDetailsView|dbuxGlobalAnalysisView|dbuxApplicationsView)/ && dbuxWebView.context.dbux-graph.isActive`,
	`view =~ /(gitlab-management-pr-assigned)/`,
	`view =~ /(gitlab-management-pr-closed)/`,
	`view =~ /(gitlab-management-pr-merged)/`,
	`view =~ /(gitlab-management-pr-opened)/`,
	`view =~ /(gitlab-management-repo)/`,
	`view =~ /(gitops.views.sources|gitops.views.workloads)/ && viewItem =~ /(GitRepository;|OCIRepository;|Kustomization;|HelmRelease;|HelmRepository;)/ && viewItem =~ /notSuspend;/`,
	`view =~ /(gitops.views.sources|gitops.views.workloads)/ && viewItem =~ /(GitRepository;|OCIRepository;|Kustomization;|HelmRelease;|HelmRepository;)/ && viewItem =~ /suspend;/`,
	`view =~ /(issuesAssignedToMe)/`,
	`view =~ /(issuesAssignedToMe|issuesCreatedByMe|allProjectIssues)/`,
	`view =~ /(issuesAssignedToMe|issuesCreatedByMe|allProjectIssues|mrsCreatedByMe|mrsAssignedToMe|allProjectMrs|currentBranchInfo)/`,
	`view =~ /(issuesCreatedByMe)/`,
	`view =~ /(mrsAssignedToMe)/`,
	`view =~ /(mrsCreatedByMe)/`,
	`view =~ /(mrsCreatedByMe|mrsAssignedToMe|allProjectMrs)/`,
	`view =~ /(pr|prStatus):github/ && viewItem =~ /description/`,
	`view =~ /(pr|prStatus):github/ && viewItem =~ /description:changesSinceReview:active/`,
	`view =~ /(pr|prStatus):github/ && viewItem =~ /description:changesSinceReview:inactive/`,
	`view =~ /(pr|prStatus):github/ && viewItem =~ /filechange/`,
	`view =~ /(pr|prStatus):github/ && viewItem =~ /filechange:MODIFY/`,
	`view =~ /Frontpage|Subreddit/ && config.RedditViewerWidget.OpenType == browser`,
	`view =~ /Frontpage|Subreddit/ && config.RedditViewerWidget.OpenType == redditviewer`,
	`view =~ /RSS/ && config.RSSReader.OpenType != OpenEditor`,
	`view =~ /RSS/ && config.RSSReader.OpenType != OpenLink`,
	`view =~ /^(gitops.views.clusters)$/ && viewItem =~ /(Deployment;)/`,
	`view =~ /^(gitops.views.sources|gitops.views.workloads)$/`,
	`view =~ /^(ninjaDebug|ninjaRelease|ninjaFavourites)$/`,
	`view =~ /^(xplot.online|xplot.manager|xplot.workspace.online)$/ && viewItem =~ /^(sshFolder|sshLink|ftpFolder|ftpLink)$/`,
	`view =~ /^(xplot.online|xplot.offline|xplot.manager|xplot.workspace.online)$/ && viewItem =~ /^(ssh|ftp|sshWorkSpace|ftpWorkSpace)$/`,
	`view =~ /^(xplot.online|xplot.offline|xplot.manager|xplot.workspace.online|xplot.workspace.offline)$/ && viewItem =~ /^(ssh|ftp|sshWorkSpace|ftpWorkSpace|sshFolder|sshLink|ftpFolder|ftpLink)$/`,
	`view =~ /^(xplot.online|xplot.offline|xplot.manager|xplot.workspace.online|xplot.workspace.offline)$/ && viewItem =~ /^(ssh|sshWorkSpace)$/`,
	`view =~ /^(zowe.explorer|zowe.ds.explorer)/ && viewItem =~ /^(ds|ds_fav|member|member_fav|vsam|vsam_fav).*/`,
	`view =~ /^ObjectScript(Projects)?Explorer$/ && viewItem =~ /^dataNode:/`,
	`view =~ /^avdmanager-sdk/ && viewItem == sdk-pkg`,
	`view =~ /^avdmanager-sdk/ && viewItem =~ /^sdk-pkg-installed/`,
	`view =~ /^chat.treeView(?!.onboarding)/`,
	`view =~ /^dimensionscm\\.Request*/ && dimensionscm.RequestTree.Visible && viewItem == defaultRequest`,
	`view =~ /^dimensionscm\\.Request*/ && dimensionscm.RequestTree.Visible && viewItem == request`,
	`view =~ /^dimensionscm\\.Request*/ && viewItem == defaultRequest`,
	`view =~ /^dimensionscm\\.Request*/ && viewItem == request`,
	`view =~ /^gistpad.gists(.explorer)?$/ && gistpad:state == SignedIn && gistpad:groupType != none`,
	`view =~ /^gistpad.gists(.explorer)?$/ && gistpad:state == SignedIn && gistpad:groupType == none`,
	`view =~ /^gistpad.gists(.explorer)?$/ && gistpad:state == SignedIn && gistpad:sortOrder == alphabetical`,
	`view =~ /^gistpad.gists(.explorer)?$/ && gistpad:state == SignedIn && gistpad:sortOrder == updatedTime`,
	`view =~ /^gistpad.gists(.explorer)?$/ && gistpad:state == SignedIn`,
	`view =~ /^gitlens\\.views\\.(branches|commits|fileHistory|lineHistory|remotes|repositories|tags)/ && !config.gitlens.views.showRelativeDateMarkers`,
	`view =~ /^gitlens\\.views\\.(branches|commits|fileHistory|lineHistory|remotes|repositories|tags)/ && config.gitlens.views.showRelativeDateMarkers`,
	`view =~ /^gitlens\\.views\\.(branches|commits|fileHistory|lineHistory|remotes|repositories|tags|worktrees)/ && !config.gitlens.views.showRelativeDateMarkers`,
	`view =~ /^gitlens\\.views\\.(branches|commits|fileHistory|lineHistory|remotes|repositories|tags|worktrees)/ && config.gitlens.views.showRelativeDateMarkers`,
	`view =~ /^gitlens\\.views\\.(fileHistory|lineHistory)/ && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+committed\\b)/ && gitlens:hasRemotes`,
	`view =~ /^gitlens\\.views\\.(fileHistory|lineHistory)/ && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+committed\\b)/`,
	`view =~ /^gitlens\\.views\\.branches/ && !config.gitlens.views.branches.avatars`,
	`view =~ /^gitlens\\.views\\.branches/ && !config.gitlens.views.branches.pullRequests.enabled && !config.gitlens.views.branches.pullRequests.showForBranches`,
	`view =~ /^gitlens\\.views\\.branches/ && !config.gitlens.views.branches.showBranchComparison`,
	`view =~ /^gitlens\\.views\\.branches/ && config.gitlens.views.branches.avatars`,
	`view =~ /^gitlens\\.views\\.branches/ && config.gitlens.views.branches.files.layout == auto`,
	`view =~ /^gitlens\\.views\\.branches/ && config.gitlens.views.branches.files.layout == list`,
	`view =~ /^gitlens\\.views\\.branches/ && config.gitlens.views.branches.files.layout == tree`,
	`view =~ /^gitlens\\.views\\.branches/ && config.gitlens.views.branches.pullRequests.enabled && config.gitlens.views.branches.pullRequests.showForBranches`,
	`view =~ /^gitlens\\.views\\.branches/ && config.gitlens.views.branches.showBranchComparison`,
	`view =~ /^gitlens\\.views\\.branches/`,
	`view =~ /^gitlens\\.views\\.commits/ && !config.gitlens.views.commits.avatars`,
	`view =~ /^gitlens\\.views\\.commits/ && !config.gitlens.views.commits.pullRequests.enabled && !config.gitlens.views.commits.pullRequests.showForBranches`,
	`view =~ /^gitlens\\.views\\.commits/ && !config.gitlens.views.commits.showBranchComparison`,
	`view =~ /^gitlens\\.views\\.commits/ && !gitlens:views:commits:myCommitsOnly`,
	`view =~ /^gitlens\\.views\\.commits/ && config.gitlens.views.commits.avatars`,
	`view =~ /^gitlens\\.views\\.commits/ && config.gitlens.views.commits.files.layout == auto`,
	`view =~ /^gitlens\\.views\\.commits/ && config.gitlens.views.commits.files.layout == list`,
	`view =~ /^gitlens\\.views\\.commits/ && config.gitlens.views.commits.files.layout == tree`,
	`view =~ /^gitlens\\.views\\.commits/ && config.gitlens.views.commits.pullRequests.enabled && config.gitlens.views.commits.pullRequests.showForBranches`,
	`view =~ /^gitlens\\.views\\.commits/ && config.gitlens.views.commits.showBranchComparison`,
	`view =~ /^gitlens\\.views\\.commits/ && gitlens:plus:enabled`,
	`view =~ /^gitlens\\.views\\.commits/ && gitlens:views:commits:myCommitsOnly`,
	`view =~ /^gitlens\\.views\\.commits/`,
	`view =~ /^gitlens\\.views\\.contributors/ && !config.gitlens.views.contributors.avatars`,
	`view =~ /^gitlens\\.views\\.contributors/ && !config.gitlens.views.contributors.showAllBranches`,
	`view =~ /^gitlens\\.views\\.contributors/ && !config.gitlens.views.contributors.showStatistics`,
	`view =~ /^gitlens\\.views\\.contributors/ && config.gitlens.views.contributors.avatars`,
	`view =~ /^gitlens\\.views\\.contributors/ && config.gitlens.views.contributors.files.layout == auto`,
	`view =~ /^gitlens\\.views\\.contributors/ && config.gitlens.views.contributors.files.layout == list`,
	`view =~ /^gitlens\\.views\\.contributors/ && config.gitlens.views.contributors.files.layout == tree`,
	`view =~ /^gitlens\\.views\\.contributors/ && config.gitlens.views.contributors.showAllBranches`,
	`view =~ /^gitlens\\.views\\.contributors/ && config.gitlens.views.contributors.showStatistics`,
	`view =~ /^gitlens\\.views\\.contributors/`,
	`view =~ /^gitlens\\.views\\.fileHistory/ && !config.gitlens.views.fileHistory.avatars`,
	`view =~ /^gitlens\\.views\\.fileHistory/ && !gitlens:views:fileHistory:cursorFollowing && !config.gitlens.advanced.fileHistoryShowAllBranches && !config.gitlens.advanced.fileHistoryFollowsRenames`,
	`view =~ /^gitlens\\.views\\.fileHistory/ && !gitlens:views:fileHistory:cursorFollowing && !config.gitlens.advanced.fileHistoryShowAllBranches && config.gitlens.advanced.fileHistoryFollowsRenames`,
	`view =~ /^gitlens\\.views\\.fileHistory/ && !gitlens:views:fileHistory:cursorFollowing && !config.gitlens.advanced.fileHistoryShowAllBranches`,
	`view =~ /^gitlens\\.views\\.fileHistory/ && !gitlens:views:fileHistory:cursorFollowing && config.gitlens.advanced.fileHistoryShowAllBranches`,
	`view =~ /^gitlens\\.views\\.fileHistory/ && !gitlens:views:fileHistory:cursorFollowing`,
	`view =~ /^gitlens\\.views\\.fileHistory/ && config.gitlens.views.fileHistory.avatars`,
	`view =~ /^gitlens\\.views\\.fileHistory/ && gitlens:views:fileHistory:canPin && !gitlens:views:fileHistory:editorFollowing`,
	`view =~ /^gitlens\\.views\\.fileHistory/ && gitlens:views:fileHistory:canPin && gitlens:views:fileHistory:editorFollowing`,
	`view =~ /^gitlens\\.views\\.fileHistory/ && gitlens:views:fileHistory:cursorFollowing`,
	`view =~ /^gitlens\\.views\\.fileHistory/`,
	`view =~ /^gitlens\\.views\\.home/`,
	`view =~ /^gitlens\\.views\\.lineHistory/ && !config.gitlens.views.lineHistory.avatars`,
	`view =~ /^gitlens\\.views\\.lineHistory/ && !gitlens:views:lineHistory:editorFollowing`,
	`view =~ /^gitlens\\.views\\.lineHistory/ && config.gitlens.views.lineHistory.avatars`,
	`view =~ /^gitlens\\.views\\.lineHistory/ && gitlens:views:lineHistory:editorFollowing`,
	`view =~ /^gitlens\\.views\\.lineHistory/`,
	`view =~ /^gitlens\\.views\\.remotes/ && !config.gitlens.views.remotes.avatars`,
	`view =~ /^gitlens\\.views\\.remotes/ && !config.gitlens.views.remotes.pullRequests.enabled && !config.gitlens.views.remotes.pullRequests.showForBranches`,
	`view =~ /^gitlens\\.views\\.remotes/ && config.gitlens.views.remotes.avatars`,
	`view =~ /^gitlens\\.views\\.remotes/ && config.gitlens.views.remotes.files.layout == auto`,
	`view =~ /^gitlens\\.views\\.remotes/ && config.gitlens.views.remotes.files.layout == list`,
	`view =~ /^gitlens\\.views\\.remotes/ && config.gitlens.views.remotes.files.layout == tree`,
	`view =~ /^gitlens\\.views\\.remotes/ && config.gitlens.views.remotes.pullRequests.enabled && config.gitlens.views.remotes.pullRequests.showForBranches`,
	`view =~ /^gitlens\\.views\\.remotes/`,
	`view =~ /^gitlens\\.views\\.repositories/ && !config.gitlens.views.repositories.avatars`,
	`view =~ /^gitlens\\.views\\.repositories/ && config.gitlens.views.repositories.autoRefresh && !gitlens:views:repositories:autoRefresh`,
	`view =~ /^gitlens\\.views\\.repositories/ && config.gitlens.views.repositories.autoRefresh && gitlens:views:repositories:autoRefresh`,
	`view =~ /^gitlens\\.views\\.repositories/ && config.gitlens.views.repositories.avatars`,
	`view =~ /^gitlens\\.views\\.repositories/ && config.gitlens.views.repositories.files.layout == auto`,
	`view =~ /^gitlens\\.views\\.repositories/ && config.gitlens.views.repositories.files.layout == list`,
	`view =~ /^gitlens\\.views\\.repositories/ && config.gitlens.views.repositories.files.layout == tree`,
	`view =~ /^gitlens\\.views\\.repositories/ && viewItem =~ /gitlens:branches\\b/ && !config.gitlens.views.repositories.branches.showBranchComparison`,
	`view =~ /^gitlens\\.views\\.repositories/ && viewItem =~ /gitlens:branches\\b/ && config.gitlens.views.repositories.branches.showBranchComparison`,
	`view =~ /^gitlens\\.views\\.repositories/`,
	`view =~ /^gitlens\\.views\\.searchAndCompare\\b/ && !config.gitlens.views.searchAndCompare.avatars`,
	`view =~ /^gitlens\\.views\\.searchAndCompare\\b/ && !gitlens:views:searchAndCompare:keepResults`,
	`view =~ /^gitlens\\.views\\.searchAndCompare\\b/ && config.gitlens.views.searchAndCompare.avatars`,
	`view =~ /^gitlens\\.views\\.searchAndCompare\\b/ && config.gitlens.views.searchAndCompare.files.layout == auto`,
	`view =~ /^gitlens\\.views\\.searchAndCompare\\b/ && config.gitlens.views.searchAndCompare.files.layout == list`,
	`view =~ /^gitlens\\.views\\.searchAndCompare\\b/ && config.gitlens.views.searchAndCompare.files.layout == tree`,
	`view =~ /^gitlens\\.views\\.searchAndCompare\\b/ && gitlens:views:searchAndCompare:keepResults`,
	`view =~ /^gitlens\\.views\\.searchAndCompare\\b/`,
	`view =~ /^gitlens\\.views\\.stashes/ && config.gitlens.views.stashes.files.layout == auto`,
	`view =~ /^gitlens\\.views\\.stashes/ && config.gitlens.views.stashes.files.layout == list`,
	`view =~ /^gitlens\\.views\\.stashes/ && config.gitlens.views.stashes.files.layout == tree`,
	`view =~ /^gitlens\\.views\\.stashes/`,
	`view =~ /^gitlens\\.views\\.tags/ && !config.gitlens.views.tags.avatars`,
	`view =~ /^gitlens\\.views\\.tags/ && config.gitlens.views.tags.avatars`,
	`view =~ /^gitlens\\.views\\.tags/ && config.gitlens.views.tags.files.layout == auto`,
	`view =~ /^gitlens\\.views\\.tags/ && config.gitlens.views.tags.files.layout == list`,
	`view =~ /^gitlens\\.views\\.tags/ && config.gitlens.views.tags.files.layout == tree`,
	`view =~ /^gitlens\\.views\\.tags/`,
	`view =~ /^gitlens\\.views\\.timeline/`,
	`view =~ /^gitlens\\.views\\.worktrees/ && !config.gitlens.views.worktrees.avatars`,
	`view =~ /^gitlens\\.views\\.worktrees/ && !config.gitlens.views.worktrees.pullRequests.enabled && !config.gitlens.views.worktrees.pullRequests.showForBranches`,
	`view =~ /^gitlens\\.views\\.worktrees/ && !config.gitlens.views.worktrees.showBranchComparison`,
	`view =~ /^gitlens\\.views\\.worktrees/ && config.gitlens.views.worktrees.avatars`,
	`view =~ /^gitlens\\.views\\.worktrees/ && config.gitlens.views.worktrees.files.layout == auto`,
	`view =~ /^gitlens\\.views\\.worktrees/ && config.gitlens.views.worktrees.files.layout == list`,
	`view =~ /^gitlens\\.views\\.worktrees/ && config.gitlens.views.worktrees.files.layout == tree`,
	`view =~ /^gitlens\\.views\\.worktrees/ && config.gitlens.views.worktrees.pullRequests.enabled && config.gitlens.views.worktrees.pullRequests.showForBranches`,
	`view =~ /^gitlens\\.views\\.worktrees/ && config.gitlens.views.worktrees.showBranchComparison`,
	`view =~ /^gitlens\\.views\\.worktrees/`,
	`view =~ /^gradleTasksView$$|^recentTasksView$/ && viewItem =~ /^(pinned)?debugTask.*$|^(pinned)?task.*$/`,
	`view =~ /^gradleTasksView$/`,
	`view =~ /^gradleTasksView$|^pinnedTasksView$|^recentTasksView$|^gradleDefaultProjectsView$/ && viewItem =~ /^debugTask(WithTerminals)?$|^task(WithTerminals)?$/`,
	`view =~ /^gradleTasksView$|^recentTasksView$/ && viewItem =~ /^(pinned)?debugTask(WithTerminals)?$/`,
	`view =~ /^gradleTasksView$|^recentTasksView$/ && viewItem =~ /^(pinned)?debugTask(WithTerminals)?$|^(pinned)?task(WithTerminals)?$/`,
	`view =~ /^gradleTasksView$|^recentTasksView$/ && viewItem =~ /^(pinned)?debugTask.*$/ || viewItem =~ /^(pinned)?runningDebugTask.*$/ && allowParallelRun == true`,
	`view =~ /^gradleTasksView$|^recentTasksView$/ && viewItem =~ /^(pinned)?debugTask.*$/`,
	`view =~ /^gradleTasksView$|^recentTasksView$/ && viewItem =~ /^(pinned)?runningTask.*$|^(pinned)?runningDebugTask.*$/ && !allowParallelRun`,
	`view =~ /^gradleTasksView$|^recentTasksView$/ && viewItem =~ /^cancellingTask.*$/`,
	`view =~ /^gradleTasksView$|^recentTasksView$/ && viewItem =~ /^debugTask(WithTerminals)?.*$|^task(WithTerminals)?.*$/`,
	`view =~ /^gradleTasksView$|^recentTasksView$|^gradleDefaultProjectsView$/ && viewItem =~ /^(pinned)?debugTask(WithTerminals)?$|^(pinned)?task(WithTerminals)?$/`,
	`view =~ /^gradleTasksView$|^recentTasksView$|^gradleDefaultProjectsView$/ && viewItem =~ /^(pinned)?debugTask.*$|^(pinned)?task.*$/ || viewItem =~ /^(pinned)?runningTask.*$|^(pinned)?runningDebugTask.*$/ && allowParallelRun == true`,
	`view =~ /^gradleTasksView$|^recentTasksView$|^gradleDefaultProjectsView$/ && viewItem =~ /^(pinned)?debugTask.*$|^(pinned)?task.*$/`,
	`view =~ /^jenkins\\.views/ && viewItem == jenkins:job`,
	`view =~ /^jenkins\\.views/ && viewItem == jenkins:view`,
	`view =~ /^l13Projects(Favorites|Workspaces)$/ && viewItem =~ /^((current-)?(favorite|project|subfavorite|subproject)-(folders?|git|subfolder|vscode|workspace)|unknown-project)$/`,
	`view =~ /^l13Projects(Favorites|Workspaces)$/ && viewItem =~ /^(current-)?(favorite|project|subfavorite|subproject)-(folders?|git|subfolder|vscode|workspace)$/`,
	`view =~ /^l13Projects(Favorites|Workspaces)$/ && viewItem =~ /^(current-)?(favorite|project|subfavorite|subproject)-(folder|git|vscode)$/`,
	`view =~ /^l13Projects(Favorites|Workspaces)$/ && viewItem =~ /^(favoriteGroup|workspaceGroup)$/`,
	`view =~ /^l13Projects(Favorites|Workspaces)$/ && viewItem =~ /^(favorite|project|subfavorite|subproject)-(folders?|git|subfolder|vscode|workspace)$/`,
	`view =~ /^l13Projects(Favorites|Workspaces)$/ && viewItem =~ /^(favorite|project|subfavorite|subproject)-(folder|git|vscode)$/`,
	`view =~ /^l13Projects(Tags|Workspaces)$/ && viewItem == tag`,
	`view =~ /^liveshare\\.session(\\.explorer)?$/ && viewItem == audio.participants`,
	`view =~ /^liveshare\\.session(\\.explorer)?$/ && viewItem == audio.self.muted`,
	`view =~ /^liveshare\\.session(\\.explorer)?$/ && viewItem == audio.self`,
	`view =~ /^privateExtensions/ && viewItem =~ /available/`,
	`view =~ /^privateExtensions/ && viewItem =~ /extension/`,
	`view =~ /^privateExtensions/ && viewItem =~ /installed|update/`,
	`view =~ /^privateExtensions/ && viewItem =~ /update/`,
	`view =~ /^tabsTreeView/ && !tabGroup.groups:allCollapsed`,
	`view =~ /^tabsTreeView/ && !tabGroup.sortMode:enabled`,
	`view =~ /^tabsTreeView/ && tabGroup.groups:allCollapsed`,
	`view =~ /^tabsTreeView/ && tabGroup.sortMode:enabled`,
	`view =~ /^tabsTreeView/ && viewItem == 'group'`,
	`view =~ /^tabsTreeView/ && viewItem == 'grouped-tab'`,
	`view =~ /^tabsTreeView/ && viewItem =~ /tab/`,
	`view =~ /^tabsTreeView/`,
	`view =~ /^tekton(CustomTree)View/`,
	`view =~ /^tekton(CustomTree|PipelineExplorer)View/  && viewItem == pipeline`,
	`view =~ /^tekton(CustomTree|PipelineExplorer)View/  && viewItem == pipelineresource`,
	`view =~ /^tekton(CustomTree|PipelineExplorer)View/ && viewItem == clustertask`,
	`view =~ /^tekton(CustomTree|PipelineExplorer)View/ && viewItem == pipeline`,
	`view =~ /^tekton(CustomTree|PipelineExplorer)View/ && viewItem == pipelinenode`,
	`view =~ /^tekton(CustomTree|PipelineExplorer)View/ && viewItem == pipelinerun`,
	`view =~ /^tekton(CustomTree|PipelineExplorer)View/ && viewItem == task`,
	`view =~ /^tekton(CustomTree|PipelineExplorer)View/ && viewItem == taskrun`,
	`view =~ /^tekton(CustomTree|PipelineExplorer)View/ && viewItem == tr`,
	`view =~ /^tekton(CustomTree|PipelineExplorer)View/ && viewItem == triggertemplates`,
	`view =~ /^tekton(CustomTree|PipelineExplorer)View/ && viewItem =~ /^(pipelinerun|pipelineRunFinished)$/`,
	`view =~ /^tekton(CustomTree|PipelineExplorer)View/ && viewItem =~ /^(pipelinerun|pipelineRunFinished|taskrun|tr)$/`,
	`view =~ /^tekton(CustomTree|PipelineExplorer)View/ && viewItem =~ /^(pipeline|pipelinerun|pipelineRunFinished|clustertask|pipelineresource|taskrun|task|triggertemplates|triggerbinding|eventlistener|conditions|clustertriggerbinding|tr)$/`,
	`view =~ /^tekton(CustomTree|PipelineExplorer)View/ && viewItem =~ /^(pipeline|pipelinerun|pipelineRunFinished|clustertask|pipelineresource|taskrun|triggertemplates|triggerbinding|eventlistener|conditions|task|clustertriggerbinding|tr)$/`,
	`view =~ /^tekton(CustomTree|PipelineExplorer)View/`,
	`view =~ /^tekton(CustomTree|PipelineExplorer|Debug)View/`,
	`view =~ /^tekton(Debug)View/ && viewItem == debugger`,
	`view =~ /^tekton(PipelineExplorer)View/ && viewItem =~ /^(pipeline|pipelinerun|pipelineRunFinished|clustertask|task|taskrun|pipelineresource|triggertemplates|triggerbinding|clustertriggerbinding|eventlistener|conditions)$/`,
	`view =~ /^tekton(PipelineExplorer)View/`,
	`view =~ /^vrdev\\.views\\.explorer/`,
	`view =~ /^workspaceManager\\.views/ && viewItem =~ /workspaceManager:workspace/`,
	`view =~ /^workspaceManager\\.views/ && viewItem =~ /workspaceManager:workspace\\b(?!.*?\\b\\+favorite\\b)/`,
	`view =~ /^workspaceManager\\.views/ && viewItem =~ /workspaceManager:workspace\\b(?!.*?\\b\\+group\\b)/`,
	`view =~ /^workspaceManager\\.views/ && viewItem =~ /workspaceManager:workspace\\b(?=.*?\\b\\+favorite\\b)/`,
	`view =~ /^workspaceManager\\.views/ && viewItem =~ /workspaceManager:workspace\\b(?=.*?\\b\\+group\\b)/`,
	`view =~ /^workspaceManager\\.views\\.workspace/`,
	`view =~ /^xplot.online$|^xplot.manager$/`,
	`view =~ /^xplot.online$|^xplot.manager$|^xplot.workspace.online$/ && viewItem  =~ /^(sshFolder|sshFile|ftpFolder|ftpFile)$/`,
	`view =~ /^xplot.online$|^xplot.manager$|^xplot.workspace.online$/ && viewItem  =~ /^(sshWorkSpace|ftpWorkSpace|sshFolder|sshLink|sshFile|ftpFolder|ftpLink|ftpFile)$/`,
	`view =~ /^xplot.online$|^xplot.manager$|^xplot.workspace.online$/ && viewItem =~ /(sshFolder|sshLink|ftpFolder|ftpLink)$/`,
	`view =~ /^xplot.online$|^xplot.manager$|^xplot.workspace.online$/ && viewItem =~ /^(sshFolder|sshLink)$/`,
	`view =~ /^xplot.online$|^xplot.manager$|^xplot.workspace.online$/ && viewItem =~ /^(sshFolder|sshLink|ftpFolder|ftpLink)$/ `,
	`view =~ /^xplot.online$|^xplot.manager$|^xplot.workspace.online$/ && viewItem =~ /^(sshFolder|sshLink|ftpFolder|ftpLink)$/`,
	`view =~ /^xplot.online$|^xplot.manager$|^xplot.workspace.online$/ && viewItem =~ /^(sshFolder|sshLink|sshFile)$/ `,
	`view =~ /^xplot.online$|^xplot.manager$|^xplot.workspace.online$/ && viewItem =~ /^(sshFolder|sshLink|sshFile|ftpFolder|ftpLink|ftpFile)$/ `,
	`view =~ /^xplot.online$|^xplot.manager$|^xplot.workspace.online$/ && viewItem =~ /^(sshFolder|sshLink|sshFile|ftpFolder|ftpLink|ftpFile)$/`,
	`view =~ /^xplot.online$|^xplot.manager$|^xplot.workspace.online$/ && viewItem =~ /^(sshWorkSpace|ftpWorkSpace|sshFolder|sshLink|ftpFolder|ftpLink)$/`,
	`view =~ /^xplot.online$|^xplot.manager$|^xplot.workspace.online$/ && viewItem =~ /^(ssh|ftp|sshWorkSpace|ftpWorkSpace)$/ `,
	`view =~ /^xplot.online$|^xplot.manager$|^xplot.workspace.online$/ && viewItem =~ /^(ssh|ftp|sshWorkSpace|ftpWorkSpace)$/`,
	`view =~ /^xplot.online$|^xplot.manager|^xplot.workspace.online$/ && viewItem =~ /^(sshFolder|sshLink|sshFile|ftpFolder|ftpLink|ftpFile)$/ `,
	`view =~ /^xplot.online$|^xplot.offline$|^xplot.manager$/ && viewItem =~ /^(ssh|ftp|sshWorkSpace|ftpWorkSpace)$/`,
	`view =~ /^xplot.online$|^xplot.offline$|^xplot.manager$/ && viewItem =~ /^(ssh|ftp|sshWorkSpace|ftpWorkSpace|group)$/`,
	`view =~ /^xplot.online$|^xplot.offline$|^xplot.manager$|^xplot.workspace.online$/ && viewItem =~ /^(ssh|ftp|sshWorkSpace|ftpWorkSpace)$/`,
	`view =~ /^xplot.online$|^xplot.offline$|^xplot.manager$|^xplot.workspace.online$/ && viewItem =~ /^(ssh|sshWorkSpace)$/`,
	`view =~ /^xplot.online$|^xplot.offline$|^xplot.manager$|^xplot.workspace.online$/ && viewItem =~ /^(ssh|workspace|group)$/`,
	`view =~ /^xplot.online$|^xplot.offline$|^xplot.manager$|^xplot.workspace.online$|^xplot.workspace.offline$/ && viewItem =~ /^(ssh|ftp|sshWorkSpace|ftpWorkSpace)$/`,
	`view =~ /^xplot.online$|^xplot.offline$|^xplot.manager$|^xplot.workspace.online$|^xplot.workspace.offline$/ && viewItem =~ /^(ssh|ftp|sshWorkSpace|ftpWorkSpace|group|sshFolder|sshLink|sshFile|ftpFolder|ftpLink|ftpFile)$/`,
	`view =~ /^xplot.online$|^xplot.offline$|^xplot.manager$|^xplot.workspace.online$|^xplot.workspace.offline$/ && viewItem =~ /^group$/`,
	`view =~ /^xplot.online$|^xplot.offline$|^xplot.manager$|^xplot.workspace.online$|^xplot.workspace.offline$/ && xplot.console.switch == OFF`,
	`view =~ /^xplot.online$|^xplot.offline$|^xplot.manager$|^xplot.workspace.online$|^xplot.workspace.offline$/ && xplot.console.switch == ON`,
	`view =~ /^xplot.online$|^xplot.offline$|^xplot.manager$|^xplot.workspace.online$|^xplot.workspace.offline$/ && xplot.debug == OFF`,
	`view =~ /^xplot.online$|^xplot.offline$|^xplot.manager$|^xplot.workspace.online$|^xplot.workspace.offline$/ && xplot.debug == ON`,
	`view =~ /^xplot.online$|^xplot.offline$|^xplot.manager$|^xplot.workspace.online$|^xplot.workspace.offline$/`,
	`view =~ /^xplot.workspace.online$|^xplot.workspace.offline$/ && viewItem =~ /^(ssh|ftp|sshWorkSpace|ftpWorkSpace)$/ `,
	`view =~ /alStudio/`,
	`view =~ /alStudioALObject/`,
	`view =~ /alStudioOpenEditors/`,
	`view =~ /alStudioScope/`,
	`view =~ /azure(ResourceGroups|Workspace)/ && viewItem == MongoCollection`,
	`view =~ /azure(ResourceGroups|Workspace)/ && viewItem == MongoDocument`,
	`view =~ /azure(ResourceGroups|Workspace)/ && viewItem == cosmosDBDocumentCollection`,
	`view =~ /azure(ResourceGroups|Workspace)/ && viewItem == cosmosDBDocumentDatabase`,
	`view =~ /azure(ResourceGroups|Workspace)/ && viewItem == cosmosDBDocument`,
	`view =~ /azure(ResourceGroups|Workspace)/ && viewItem == cosmosDBDocumentsGroup`,
	`view =~ /azure(ResourceGroups|Workspace)/ && viewItem == cosmosDBGraphDatabase`,
	`view =~ /azure(ResourceGroups|Workspace)/ && viewItem == cosmosDBGraph`,
	`view =~ /azure(ResourceGroups|Workspace)/ && viewItem == cosmosDBStoredProcedure`,
	`view =~ /azure(ResourceGroups|Workspace)/ && viewItem == cosmosDBStoredProceduresGroup`,
	`view =~ /azure(ResourceGroups|Workspace)/ && viewItem == mongoDb`,
	`view =~ /azure(ResourceGroups|Workspace)/ && viewItem == postgresDatabase`,
	`view =~ /azure(ResourceGroups|Workspace)/ && viewItem == postgresFunction`,
	`view =~ /azure(ResourceGroups|Workspace)/ && viewItem == postgresFunctions`,
	`view =~ /azure(ResourceGroups|Workspace)/ && viewItem == postgresStoredProcedure`,
	`view =~ /azure(ResourceGroups|Workspace)/ && viewItem == postgresStoredProcedures`,
	`view =~ /azure(ResourceGroups|Workspace)/ && viewItem == postgresTable`,
	`view =~ /azure(ResourceGroups|Workspace)/ && viewItem == postgresTables`,
	`view =~ /azure(Workspace|ResourceGroups)/i && viewItem == attachedStorageAccounts`,
	`view =~ /azure(Workspace|ResourceGroups)/i && viewItem == azureBlobContainer && !isWeb && !isLinux && !remoteName`,
	`view =~ /azure(Workspace|ResourceGroups)/i && viewItem == azureBlobContainerGroup`,
	`view =~ /azure(Workspace|ResourceGroups)/i && viewItem == azureBlobContainer`,
	`view =~ /azure(Workspace|ResourceGroups)/i && viewItem == azureBlobDirectory`,
	`view =~ /azure(Workspace|ResourceGroups)/i && viewItem == azureBlobFile`,
	`view =~ /azure(Workspace|ResourceGroups)/i && viewItem == azureFile || viewItem == azureBlobFile`,
	`view =~ /azure(Workspace|ResourceGroups)/i && viewItem == azureFileShare && !isWeb && !isLinux && !remoteName`,
	`view =~ /azure(Workspace|ResourceGroups)/i && viewItem == azureFileShare || viewItem == azureBlobContainer`,
	`view =~ /azure(Workspace|ResourceGroups)/i && viewItem == azureFileShareDirectory || viewItem == azureBlobDirectory`,
	`view =~ /azure(Workspace|ResourceGroups)/i && viewItem == azureFileShareDirectory`,
	`view =~ /azure(Workspace|ResourceGroups)/i && viewItem == azureFileShareGroup`,
	`view =~ /azure(Workspace|ResourceGroups)/i && viewItem == azureFileShare`,
	`view =~ /azure(Workspace|ResourceGroups)/i && viewItem == azureFile`,
	`view =~ /azure(Workspace|ResourceGroups)/i && viewItem == azureQueue && !isWeb && !isLinux && !remoteName`,
	`view =~ /azure(Workspace|ResourceGroups)/i && viewItem == azureQueueGroup`,
	`view =~ /azure(Workspace|ResourceGroups)/i && viewItem == azureQueue`,
	`view =~ /azure(Workspace|ResourceGroups)/i && viewItem == azureStorageAccount-attached`,
	`view =~ /azure(Workspace|ResourceGroups)/i && viewItem == azureTable && !isWeb && !isLinux && !remoteName`,
	`view =~ /azure(Workspace|ResourceGroups)/i && viewItem == azureTableGroup`,
	`view =~ /azure(Workspace|ResourceGroups)/i && viewItem == azureTable`,
	`view =~ /azureWorkspace/ && viewItem =~ /cosmosDBDocumentServer(?![a-z])/i`,
	`view =~ /azureWorkspace/ && viewItem =~ /cosmosDBGraphAccount(?![a-z])/i`,
	`view =~ /azureWorkspace/ && viewItem =~ /postgresServer(?![a-z])/i`,
	`view =~ /babelAstExplorer/`,
	`view =~ /byted-ide.gallery/ && update-button == true`,
	`view =~ /byted-ide.gallery/`,
	`view =~ /calendar/ && !calendar-is-authorized`,
	`view =~ /calendar/ && !calendar-is-filtered && calendar-tree-has-content && calendar-is-authorized`,
	`view =~ /calendar/ && calendar-is-authorized`,
	`view =~ /calendar/ && calendar-is-filtered && calendar-tree-has-content && calendar-is-authorized`,
	`view =~ /calendar/ && calendar-show-collapse && calendar-tree-has-content && calendar-is-authorized`,
	`view =~ /calendar/ && calendar-show-expand && calendar-tree-has-content && calendar-is-authorized`,
	`view =~ /calendar/ && viewItem =~ /canDelete/`,
	`view =~ /calendar/ && viewItem =~ /canEdit/`,
	`view =~ /calendar/ && viewItem =~ /canOpen/`,
	`view =~ /calendar/ && viewItem =~ /canSetLocation/`,
	`view =~ /calendar/ && viewItem =~ /canSetReminder/`,
	`view =~ /checkpointsTreeView.*/ && viewItem == checkpoint`,
	`view =~ /checkpointsTreeView.*/ && viewItem == file`,
	`view =~ /checkpointsTreeView.*/ && viewItem == selectedCheckpoint`,
	`view =~ /checkpointsTreeView.*/`,
	`view =~ /cloudcode(.unified)?.apiExplorer/`,
	`view =~ /cloudcode(.unified)?.apigee.emulator/ && viewItem =~ /^.*\\'apigee.container.add\\'.*$/`,
	`view =~ /cloudcode(.unified)?.apigee.emulator/ && viewItem =~ /^.*\\'apigee.container.delete\\'.*$/`,
	`view =~ /cloudcode(.unified)?.apigee.emulator/ && viewItem =~ /^.*\\'apigee.container.reset\\'.*$/`,
	`view =~ /cloudcode(.unified)?.apigee.emulator/ && viewItem =~ /^.*\\'apigee.container.start\\'.*$/`,
	`view =~ /cloudcode(.unified)?.apigee.emulator/ && viewItem =~ /^.*\\'apigee.container.stop\\'.*$/`,
	`view =~ /cloudcode(.unified)?.apigee.emulator/ && viewItem =~ /^.*\\'apigee.emulator.copyurl\\'.*$/`,
	`view =~ /cloudcode(.unified)?.apigee.emulator/ && viewItem =~ /^.*\\'apigee.emulator.setup\\'.*$/`,
	`view =~ /cloudcode(.unified)?.apigee.emulator/ && viewItem =~ /^.*\\'apigee.emulator.update\\'.*$/`,
	`view =~ /cloudcode(.unified)?.apigee.emulator/`,
	`view =~ /cloudcode(.unified)?.apigee.explorer/ && viewItem =~ /^.*\\'apigee.clone\\'.*$/`,
	`view =~ /cloudcode(.unified)?.apigee.explorer/ && viewItem =~ /^.*\\'apigee.delete\\'.*$/`,
	`view =~ /cloudcode(.unified)?.apigee.explorer/ && viewItem =~ /^.*\\'apigee.environments.createMultiple\\'.*$/`,
	`view =~ /cloudcode(.unified)?.apigee.explorer/ && viewItem =~ /^.*\\'apigee.proxies.createProxyMultiple\\'.*$/`,
	`view =~ /cloudcode(.unified)?.apigee.explorer/ && viewItem =~ /^.*\\'apigee.proxies.createSharedFlowBundleMultiple\\'.*$/`,
	`view =~ /cloudcode(.unified)?.apigee.explorer/ && viewItem =~ /^.*\\'apigee.rename\\'.*$/`,
	`view =~ /cloudcode(.unified)?.apigee.explorer/ && viewItem =~ /^.*\\'apigee.tests.multiple\\'.*$/`,
	`view =~ /cloudcode(.unified)?.apigee.explorer/`,
	`view =~ /cloudcode(.unified)?.awsLambdaImporterExplorer/`,
	`view =~ /cloudcode(.unified)?.cloudRunExplorer/ && viewItem == CloudRunAnthosRevisionObject`,
	`view =~ /cloudcode(.unified)?.cloudRunExplorer/ && viewItem == CloudRunAnthosServiceObject`,
	`view =~ /cloudcode(.unified)?.cloudRunExplorer/ && viewItem == CloudRunManagedServiceObject`,
	`view =~ /cloudcode(.unified)?.cloudRunExplorer/ && viewItem == CloudRunServiceRevisionObject`,
	`view =~ /cloudcode(.unified)?.cloudRunExplorer/ && viewItem == urlObject`,
	`view =~ /cloudcode(.unified)?.cloudRunExplorer/`,
	`view =~ /cloudcode(.unified)?.gceExplorer/ && viewItem != VmInstanceNetworkInterfacesObject && viewItem != VmInstanceExternalIPsObject && viewItem != VmInstanceActiveFirewallRulesObject && viewItem != VmInstanceAssociatedDisksObject && viewItem != VmInstanceFirewallAllowDenyRulesObject`,
	`view =~ /cloudcode(.unified)?.gceExplorer/ && viewItem == RunningVmInstanceObject`,
	`view =~ /cloudcode(.unified)?.gceExplorer/ && viewItem == VmInstanceDiskObject`,
	`view =~ /cloudcode(.unified)?.gceExplorer/ && viewItem == VmInstanceFirewallRuleObject`,
	`view =~ /cloudcode(.unified)?.gceExplorer/ && viewItem == VmInstanceObject || view =~ /cloudcode(.unified)?.gceExplorer/ && viewItem == RunningVmInstanceObject`,
	`view =~ /cloudcode(.unified)?.gceExplorer/`,
	`view =~ /cloudcode(.unified)?.gcfExplorer/ && viewItem == CloudFunctionFilesObject`,
	`view =~ /cloudcode(.unified)?.gcfExplorer/ && viewItem == CloudFunctionObject`,
	`view =~ /cloudcode(.unified)?.gcfExplorer/ && viewItem == LocalFunctionHeadingObject`,
	`view =~ /cloudcode(.unified)?.gcfExplorer/ && viewItem == LocalFunctionObject`,
	`view =~ /cloudcode(.unified)?.gcfExplorer/`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && !runningInTheia`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem != leafObject`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == defaultKubeConfigObject`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == inactiveKubeContextObject`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == inactiveKubeNamespace || view =~ /cloudcode(.unified)?.kubernetes.localDevExplorer/ && viewItem == inactiveKubeNamespace`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == inactiveKubeNamespace`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == kubeConfigKeyNode || view =~ /cloudcode(.unified)?.kubernetes.localDevExplorer/ && viewItem == kubeConfigKeyNode`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == kubeConfigMapNode || view =~ /cloudcode(.unified)?.kubernetes.localDevExplorer/ && viewItem == kubeConfigMapNode`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == kubeConfigPathObject`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == kubeContainer || cloudcode.kubernetes.localDevExplorer && viewItem == kubeContainer`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == kubeContainer || view =~ /cloudcode(.unified)?.kubernetes.localDevExplorer/ && viewItem == kubeContainer`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == kubeDeploymentCollection || view =~ /cloudcode(.unified)?.kubernetes.localDevExplorer/ && viewItem == kubeDeploymentCollection`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == kubeDeploymentReplicaObject`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == kubeEvent || view =~ /cloudcode(.unified)?.kubernetes.localDevExplorer/ && viewItem == kubeEvent`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == kubeGenericCollection || view =~ /cloudcode(.unified)?.kubernetes.localDevExplorer/ && viewItem == kubeGenericCollection`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == kubeIngressObject || view =~ /cloudcode(.unified)?.kubernetes.localDevExplorer/ && viewItem == kubeIngressObject`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == kubeIngressObject`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == kubeNodeObject`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == kubePodCollection || view =~ /cloudcode(.unified)?.kubernetes.localDevExplorer/ && viewItem == kubePodCollection`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == kubeResource || view =~ /cloudcode(.unified)?.kubernetes.localDevExplorer/ && viewItem == kubeResource`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == kubeResource`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == kubeSecretNode || view =~ /cloudcode(.unified)?.kubernetes.localDevExplorer/ && viewItem == kubeSecretNode`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == kubectlIPObject || view =~ /cloudcode(.unified)?.kubernetes.localDevExplorer/ && viewItem == kubectlIPObject`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == kubectlIPObject`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == leafObject`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem == urlObject`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem =~ /^cluster-/ && viewItem =~ /-(auto)?gke-/`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem =~ /^cluster-/ && viewItem =~ /-active$/`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem =~ /^cluster-/ && viewItem =~ /-inactive$/`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem =~ /^cluster-/ && viewItem =~ /-proxied-/`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem =~ /^cluster-/`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem =~ /^cluster-provider-/`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem =~ /kube(AutoGke)?NodeObject/ || view =~ /cloudcode(.unified)?.kubernetes.localDevExplorer/ && viewItem =~ /kube(AutoGke)?NodeObject/`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem =~ /kube(System)?DeploymentObject/`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem =~ /kube(System)?PodObject/ || view =~ /cloudcode(.unified)?.kubernetes.localDevExplorer/ && viewItem =~ /kube(System)?PodObject/`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem =~ /kube(System)?PodObject/`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem =~ /kube(System|AutoGkeSystem)?DeploymentObject/ || cloudcode.kubernetes.localDevExplorer && viewItem =~ /kube(System|AutoGkeSystem)?DeploymentObject/`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem =~ /kube(System|AutoGkeSystem)?DeploymentObject/ || view =~ /cloudcode(.unified)?.kubernetes.localDevExplorer/ && viewItem =~ /kube(System|AutoGkeSystem)?DeploymentObject/`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem =~ /kube(System|AutoGkeSystem)?PodObject/ || cloudcode.kubernetes.localDevExplorer && viewItem =~ /kube(System|AutoGkeSystem)?PodObject/`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/ && viewItem =~ /kube(System|AutoGkeSystem)?PodObject/ || view =~ /cloudcode(.unified)?.kubernetes.localDevExplorer/ && viewItem =~ /kube(System|AutoGkeSystem)?PodObject/`,
	`view =~ /cloudcode(.unified)?.kubectlExplorer/`,
	`view =~ /cloudcode(.unified)?.kubernetes.localDevExplorer/ && viewItem == K8sLocalDevPortForwardUrlTreeItem`,
	`view =~ /cloudcode(.unified)?.kubernetes.localDevExplorer/ && viewItem == K8sLocalDevSessionCollectionOff`,
	`view =~ /cloudcode(.unified)?.kubernetes.localDevExplorer/ && viewItem == K8sLocalDevSessionCollection`,
	`view =~ /cloudcode(.unified)?.kubernetes.localDevExplorer/`,
	`view =~ /cloudcode(.unified)?.secretsExplorer/ && viewItem != SecretObject && viewItem != SecretsPropertiesVersionObject_Enabled && viewItem != SecretsPropertiesVersionObject_Disabled`,
	`view =~ /cloudcode(.unified)?.secretsExplorer/ && viewItem == SecretObject`,
	`view =~ /cloudcode(.unified)?.secretsExplorer/ && viewItem == SecretsPropertiesLabelObject`,
	`view =~ /cloudcode(.unified)?.secretsExplorer/ && viewItem == SecretsPropertiesLabelsObject`,
	`view =~ /cloudcode(.unified)?.secretsExplorer/ && viewItem == SecretsPropertiesPermissionObject`,
	`view =~ /cloudcode(.unified)?.secretsExplorer/ && viewItem == SecretsPropertiesPermissionsObject`,
	`view =~ /cloudcode(.unified)?.secretsExplorer/ && viewItem == SecretsPropertiesVersionObject_Disabled`,
	`view =~ /cloudcode(.unified)?.secretsExplorer/ && viewItem == SecretsPropertiesVersionObject_Enabled`,
	`view =~ /cloudcode(.unified)?.secretsExplorer/ && viewItem == SecretsPropertiesVersionsObject`,
	`view =~ /cloudcode(.unified)?.secretsExplorer/`,
	`view =~ /com\\.genuitec\\.codetogether\\.presence\\.(treeview|webview)/ && !codetogether.state.privateDeployment && codetogether.state.loggedIn == true && codetogetherParticipantSession == false`,
	`view =~ /com\\.genuitec\\.codetogether\\.presence\\.(treeview|webview)/ && !codetogether.state.privateDeployment && codetogether.state.loggedIn == false && codetogether.session.anotherParticipant == false`,
	`view =~ /com\\.genuitec\\.codetogether\\.presence\\.(treeview|webview)/ && codetogether.session.active == true && codetogether.state.socketTimeout == false`,
	`view =~ /contrastTree/`,
	`view =~ /currentBranchInfo/ && viewItem =~ /cancellable-job/`,
	`view =~ /currentBranchInfo/ && viewItem =~ /cancellable-pipeline/`,
	`view =~ /currentBranchInfo/ && viewItem =~ /executable-job/`,
	`view =~ /currentBranchInfo/ && viewItem =~ /pending-job/`,
	`view =~ /currentBranchInfo/ && viewItem =~ /retryable-job/`,
	`view =~ /currentBranchInfo/ && viewItem =~ /retryable-pipeline/`,
	`view =~ /currentBranchInfo/ && viewItem =~ /with-artifacts/`,
	`view =~ /currentBranchInfo/ && viewItem =~ /with-trace/`,
	`view =~ /currentBranchInfo/ && viewItem =~ /with-url/`,
	`view =~ /cweijan.+?ql/ && viewItem == column`,
	`view =~ /cweijan.+?ql/ && viewItem == connection`,
	`view =~ /cweijan.+?ql/ && viewItem == database`,
	`view =~ /cweijan.+?ql/ && viewItem == dockerConnection`,
	`view =~ /cweijan.+?ql/ && viewItem == dockerContainerGroup`,
	`view =~ /cweijan.+?ql/ && viewItem == dockerContainerPaused`,
	`view =~ /cweijan.+?ql/ && viewItem == dockerContainerRunning`,
	`view =~ /cweijan.+?ql/ && viewItem == dockerContainerStopped`,
	`view =~ /cweijan.+?ql/ && viewItem == dockerImageGroup`,
	`view =~ /cweijan.+?ql/ && viewItem == dockerImage`,
	`view =~ /cweijan.+?ql/ && viewItem == dockerNetworkGroup`,
	`view =~ /cweijan.+?ql/ && viewItem == dockerVolumeGroup`,
	`view =~ /cweijan.+?ql/ && viewItem == esIndexGroup`,
	`view =~ /cweijan.+?ql/ && viewItem == esIndex`,
	`view =~ /cweijan.+?ql/ && viewItem == esStreamGroup`,
	`view =~ /cweijan.+?ql/ && viewItem == eventGroup`,
	`view =~ /cweijan.+?ql/ && viewItem == event`,
	`view =~ /cweijan.+?ql/ && viewItem == favorite`,
	`view =~ /cweijan.+?ql/ && viewItem == functionGroup`,
	`view =~ /cweijan.+?ql/ && viewItem == function`,
	`view =~ /cweijan.+?ql/ && viewItem == indexGroup`,
	`view =~ /cweijan.+?ql/ && viewItem == index`,
	`view =~ /cweijan.+?ql/ && viewItem == mongoCollection`,
	`view =~ /cweijan.+?ql/ && viewItem == neo4jConnection`,
	`view =~ /cweijan.+?ql/ && viewItem == packageGroup`,
	`view =~ /cweijan.+?ql/ && viewItem == package`,
	`view =~ /cweijan.+?ql/ && viewItem == procedureGroup`,
	`view =~ /cweijan.+?ql/ && viewItem == procedure`,
	`view =~ /cweijan.+?ql/ && viewItem == queryGroup`,
	`view =~ /cweijan.+?ql/ && viewItem == query`,
	`view =~ /cweijan.+?ql/ && viewItem == table && false`,
	`view =~ /cweijan.+?ql/ && viewItem == tableGroup`,
	`view =~ /cweijan.+?ql/ && viewItem == table`,
	`view =~ /cweijan.+?ql/ && viewItem == triggerGroup`,
	`view =~ /cweijan.+?ql/ && viewItem == trigger`,
	`view =~ /cweijan.+?ql/ && viewItem == userGroup`,
	`view =~ /cweijan.+?ql/ && viewItem == user`,
	`view =~ /cweijan.+?ql/ && viewItem == viewGroup`,
	`view =~ /cweijan.+?ql/ && viewItem == view`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /(?<!(ftp|neo4j|s3|es))(database|connection|redisDB)$/i`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /(?<!(ssh|tree|subTable))(group|sshConnection|sshFolder|sshFavoriteFolder|link|folder|redisDB|favorite|ftpConnection|connection|redisConnection|esIndex)$/i`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /(database|catalog)/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /(database|schema)/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /(database|schema|connection)/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /(database|schema|redisDB|redisConnection|redisFolder)/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /(dockerContainerRunning|dockerContainerPaused)/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /(folder|file|treeGroup|s3Folder|s3File)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /(folder|s3Folder)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /(redisDB|redisConnection)/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /(s3Folder|s3File)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /(sshFavoriteFolder|sshFavoriteFile|linkFavorite|sshFavoriteFileShell)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /(sshFolder|sshFavoriteFolder|sshFile|sshFavoriteFile)/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /(sshFolder|sshFile|link|sshFileShell)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /(treeGroup)/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(catalog|database|esConnection|esIndex)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(catalog|database|esConnection|esIndex|mongoTable)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(catalog|esConnection|sshConnection|folder|redisConnection|redisDB|redisFolder|connection|tableGroup|viewGroup|systemViewGroup|triggerGroup|procedureGroup|functionGroup|ftpConnection|ftpFolder)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(catalog|esConnection|sshConnection|folder|redisConnection|redisDB|redisFolder|connection|tableGroup|viewGroup|systemViewGroup|triggerGroup|procedureGroup|functionGroup|table|ftpConnection|ftpFolder)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(catalog|user|database|esColumn|column|table|view|esIndex|procedure|function|trigger|mongoTable|redisKey)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(columnGroup|column)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(connection|catalog)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(connection|esConnection|redisConnection)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(connection|esConnection|redisConnection|ftpConnection|sshConnection)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(database|connection)$/i`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(dockerImage|dockerNetwork|dockerVolume|dockerContainerStopped|dockerContainerRunning|dockerContainerPaused)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(dockerNetwork|dockerVolume)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(esConnection|ftpConnection|ftpFolder)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(neo4jRelationGroup|viewGroup)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(schema|database|esConnection|esIndex|mongoCollection)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(schema|user|database|esColumn|column|table|view|esIndex|info|procedure|package|function|trigger|mongoCollection|redisKey|neo4jLabel|neo4jRelation|folder|s3Folder|file|s3File|sshUser)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(table)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(tableGroup|subTableGroup|neo4jNodeGroup)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(tableGroup|viewGroup|neo4jNodeGroup|neo4jRelationGroup|redisDB)$/i`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(table|column)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(table|neo4jLabel|neo4jRelation)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(table|view)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(table|view|neo4jLabel|neo4jRelation)$/ && false`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(table|view|neo4jLabel|neo4jRelation)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(table|view|neo4jRelation|neo4jLabel|esIndex|mongoCollection)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /^(view)$/`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /connection$/i`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /dockerContainer/ && viewItem != dockerContainerGroup`,
	`view =~ /cweijan.+?ql/ && viewItem =~ /sshFolder|sshFavoriteFolder/`,
	`view =~ /cweijan.+?ql/`,
	`view =~ /discord-chat/ && discord-can-mute`,
	`view =~ /discord-chat/ && discord-can-unmute`,
	`view =~ /discord-chat/ && discord-channel-has-unread`,
	`view =~ /discord-chat/ && discord-channel-selected`,
	`view =~ /discord-chat/ && discord-server-has-unread && discord-can-mute`,
	`view =~ /discord-chat/ && discord-server-selected`,
	`view =~ /discord-chat/ && discord-show-unread-only == false`,
	`view =~ /discord-chat/ && discord-show-unread-only == true`,
	`view =~ /discord-chat/ && viewItem == channel`,
	`view =~ /discord-chat/ && viewItem == server`,
	`view =~ /discord-chat/`,
	`view =~ /gerrit-view/ && gerrit-view-filtered == false`,
	`view =~ /gerrit-view/ && gerrit-view-filtered == true`,
	`view =~ /gerrit-view/ && gerrit-view-has-changed == true`,
	`view =~ /gerrit-view/ && gerrit-view-show-changed-only == false`,
	`view =~ /gerrit-view/ && gerrit-view-show-changed-only == true`,
	`view =~ /gerrit-view/ && viewItem == showMenu`,
	`view =~ /gerrit-view/`,
	`view =~ /git-history.history/`,
	`view =~ /gitlabSnippetsExplorer-(all|mine)/ && viewItem == snippet`,
	`view =~ /gitlabSnippetsExplorer/ && viewItem == snippet`,
	`view =~ /gitlens\\.views\\.(?!(fileHistory|lineHistory)\\b)/ && viewItem =~ /gitlens:file(:results|\\b(?=.*?\\b\\+(committed|stashed)\\b))/`,
	`view =~ /gitlens\\.views\\.(?!commits|branches\\b)/ && viewItem =~ /gitlens:commit\\b/`,
	`view =~ /gitlens\\.views\\.(?!fileHistory|lineHistory)\\b/ && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+conflicted\\b)/`,
	`view =~ /gitlens\\.views\\.(fileHistory|lineHistory)\\b/ && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+(committed|stashed)\\b)/`,
	`view =~ /gitlens\\.views\\.(fileHistory|lineHistory)\\b/ && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+committed\\b)/`,
	`view =~ /gitlens\\.views\\.(fileHistory|lineHistory)\\b/ && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+conflicted\\b)/`,
	`view =~ /gitlens\\.views\\.(fileHistory|lineHistory\\b)/ && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+committed\\b)/`,
	`view =~ /gitlens\\.views\\.(fileHistory|lineHistory\\b)/ && viewItem =~ /gitlens:file\\b(?=.*?\\b\\+committed\\b)\\b/`,
	`view =~ /gitlens\\.views\\.branches/ && config.gitlens.views.branches.branches.layout == list`,
	`view =~ /gitlens\\.views\\.branches/ && config.gitlens.views.branches.branches.layout == tree`,
	`view =~ /gitlens\\.views\\.branches\\b/ && viewItem =~ /gitlens:compare:branch\\b/`,
	`view =~ /gitlens\\.views\\.commits\\b/ && viewItem =~ /gitlens:compare:branch\\b/`,
	`view =~ /gitlens\\.views\\.remotes/ && config.gitlens.views.remotes.branches.layout == list`,
	`view =~ /gitlens\\.views\\.remotes/ && config.gitlens.views.remotes.branches.layout == tree`,
	`view =~ /gitlens\\.views\\.repositories\\b/ && viewItem =~ /gitlens:(compare:branch(?=.*?\\b\\+root\\b)|branches|branch(?=.*?\\b\\+commits\\b)|reflog|remotes|stashes|status:upstream|tags)\\b/`,
	`view =~ /gitlens\\.views\\.repositories\\b/ && viewItem =~ /gitlens:compare:branch(?!.*?\\b\\+root\\b)\\b/`,
	`view =~ /gitlens\\.views\\.tags/ && config.gitlens.views.tags.branches.layout == list`,
	`view =~ /gitlens\\.views\\.tags/ && config.gitlens.views.tags.branches.layout == tree`,
	`view =~ /hackmd.tree.my-notes/`,
	`view =~ /hackmd.tree.recent-notes/`,
	`view =~ /hackmd.tree.team-notes/`,
	`view =~ /hackmd.tree/ && viewItem == file`,
	`view =~ /hexo.(draft|post)/`,
	`view =~ /huile8-will-mastering|huile8-mastered/`,
	`view =~ /i18n-ally-locales-progress/ && viewItem =~ /hide/`,
	`view =~ /i18n-ally-locales-progress/ && viewItem =~ /notdisply/`,
	`view =~ /i18n-ally-locales-progress/ && viewItem =~ /notsource/`,
	`view =~ /i18n-ally-locales-progress/ && viewItem =~ /show/`,
	`view =~ /i18n-ally-locales-tree/ && viewItem =~ /node/ && viewItem =~ /writable/`,
	`view =~ /i18n-ally-locales-tree/ && viewItem =~ /node/`,
	`view =~ /i18n-ally-locales-tree/`,
	`view =~ /i18n-ally-locales-usage/ && viewItem =~ /idle/`,
	`view =~ /i18n-ally-locales-usage/ && viewItem =~ /usage_idle/`,
	`view =~ /i18n-ally-locales-usage/`,
	`view =~ /i18n-ally-locales/ && viewItem =~ /editable/`,
	`view =~ /i18n-ally-locales/ && viewItem =~ /fulfillable/`,
	`view =~ /i18n-ally-locales/ && viewItem =~ /key_idle/`,
	`view =~ /i18n-ally-locales/ && viewItem =~ /node/ && viewItem =~ /writable/`,
	`view =~ /i18n-ally-locales/ && viewItem =~ /node/`,
	`view =~ /i18n-ally-locales/ && viewItem =~ /open-in-editor/`,
	`view =~ /i18n-ally-locales/ && viewItem =~ /openable/`,
	`view =~ /i18n-ally-locales/ && viewItem =~ /translatable/`,
	`view =~ /i18n-ally-locales/ && viewItem =~ /translation-candidate/`,
	`view =~ /issuesAndMrs/ && viewItem == changed-file-item`,
	`view =~ /issuesAndMrs/ && viewItem == mr-item-from-same-project`,
	`view =~ /issuesAndMrs/ && viewItem == multiple-projects-detected`,
	`view =~ /issuesAndMrs/ && viewItem == no-project-detected`,
	`view =~ /issuesAndMrs/ && viewItem == selected-project`,
	`view =~ /locators|pageView/ && BrowserName == Chrome`,
	`view =~ /locators|pageView/ && BrowserName == Edge`,
	`view =~ /locators|pageView/ && BrowserName == Firefox`,
	`view =~ /locators|pageView/ && WebdriverEnabled`,
	`view =~ /luaide_todo/ && luaide_todo-tree == false`,
	`view =~ /luaide_todo/ && luaide_todo-tree == true`,
	`view =~ /luaide_todo/`,
	`view =~ /pr:/ && viewItem =~ /pullrequest(:local)?:nonactive|description/`,
	`view =~ /projectsExplorer(Favorites|VSCode|Git|SVN|Any|Mercurial)/ && viewItem == ProjectNodeKind && isLinux`,
	`view =~ /projectsExplorer(Favorites|VSCode|Git|SVN|Any|Mercurial)/ && viewItem == ProjectNodeKind && isMac`,
	`view =~ /projectsExplorer(Favorites|VSCode|Git|SVN|Any|Mercurial)/ && viewItem == ProjectNodeKind && isWindows`,
	`view =~ /projectsExplorer(Favorites|VSCode|Git|SVN|Any|Mercurial)/ && viewItem == ProjectNodeKind`,
	`view =~ /projectsExplorer(VSCode|Git|SVN|Any|Mercurial)/ && viewItem == ProjectNodeKind`,
	`view =~ /slidev-preview/`,
	`view =~ /slidev-slides/`,
	`view =~ /task-running/ && viewItem == current`,
	`view =~ /task-running/ && viewItem == other`,
	`view =~ /task-running/ && viewItem == running`,
	`view =~ /task-running/ && viewItem == unstart`,
	`view =~ /todo-tree/ && todo-tree-expanded == false && todo-tree-show-expand-button == true`,
	`view =~ /todo-tree/ && todo-tree-expanded == false`,
	`view =~ /todo-tree/ && todo-tree-expanded == true && todo-tree-show-expand-button == true`,
	`view =~ /todo-tree/ && todo-tree-expanded == true`,
	`view =~ /todo-tree/ && todo-tree-filtered == false && todo-tree-show-filter-button == true`,
	`view =~ /todo-tree/ && todo-tree-filtered == false`,
	`view =~ /todo-tree/ && todo-tree-filtered == true && todo-tree-show-filter-button == true`,
	`view =~ /todo-tree/ && todo-tree-flat == false && todo-tree-tags-only == false && todo-tree-show-view-style-button == true`,
	`view =~ /todo-tree/ && todo-tree-flat == false && todo-tree-tags-only == true && todo-tree-show-view-style-button == true`,
	`view =~ /todo-tree/ && todo-tree-flat == false`,
	`view =~ /todo-tree/ && todo-tree-flat == true && todo-tree-tags-only == false && todo-tree-show-view-style-button == true`,
	`view =~ /todo-tree/ && todo-tree-flat == true || todo-tree-tags-only == true`,
	`view =~ /todo-tree/ && todo-tree-folder-filter-active`,
	`view =~ /todo-tree/ && todo-tree-global-filter-active`,
	`view =~ /todo-tree/ && todo-tree-grouped-by-sub-tag == false && todo-tree-has-sub-tags == true`,
	`view =~ /todo-tree/ && todo-tree-grouped-by-sub-tag == false && todo-tree-show-group-by-sub-tag-button == true && todo-tree-has-sub-tags == true`,
	`view =~ /todo-tree/ && todo-tree-grouped-by-sub-tag == true && todo-tree-has-sub-tags == true`,
	`view =~ /todo-tree/ && todo-tree-grouped-by-sub-tag == true && todo-tree-show-group-by-sub-tag-button == true && todo-tree-has-sub-tags == true`,
	`view =~ /todo-tree/ && todo-tree-grouped-by-tag == false && todo-tree-show-group-by-tag-button == true`,
	`view =~ /todo-tree/ && todo-tree-grouped-by-tag == false`,
	`view =~ /todo-tree/ && todo-tree-grouped-by-tag == true && todo-tree-show-group-by-tag-button == true`,
	`view =~ /todo-tree/ && todo-tree-grouped-by-tag == true`,
	`view =~ /todo-tree/ && todo-tree-scan-mode != 'current file'`,
	`view =~ /todo-tree/ && todo-tree-scan-mode != 'open files'`,
	`view =~ /todo-tree/ && todo-tree-scan-mode != 'workspace only'`,
	`view =~ /todo-tree/ && todo-tree-scan-mode != 'workspace'`,
	`view =~ /todo-tree/ && todo-tree-scan-mode == 'current file' && todo-tree-show-scan-mode-button == true`,
	`view =~ /todo-tree/ && todo-tree-scan-mode == 'open files' && todo-tree-show-scan-mode-button == true`,
	`view =~ /todo-tree/ && todo-tree-scan-mode == 'workspace only' && todo-tree-show-scan-mode-button == true`,
	`view =~ /todo-tree/ && todo-tree-scan-mode == 'workspace' && todo-tree-show-scan-mode-button == true`,
	`view =~ /todo-tree/ && todo-tree-show-export-button == true`,
	`view =~ /todo-tree/ && todo-tree-show-refresh-button == true`,
	`view =~ /todo-tree/ && todo-tree-tags-only == false && todo-tree-show-reveal-button == true`,
	`view =~ /todo-tree/ && todo-tree-tags-only == false`,
	`view =~ /todo-tree/ && viewItem == file`,
	`view =~ /todo-tree/ && viewItem == folder`,
	`view =~ /todo-tree/`,
	`view =~ /todo\\.views/ && viewItem == file`,
	`view =~ /todomd.(tasks)/`,
	`view =~ /todomd.(tasks|due|projects|tags|contexts|generic1|generic2|generic3)/ && viewItem == taskWithLink`,
	`view =~ /todomd.(tasks|due|projects|tags|contexts|generic1|generic2|generic3)/ && viewItem =~ /(task|taskWithLink)/`,
	`view =~ /todomd.(webviewTasks)/`,
	`view =~ /vdm-vscode.ct.testView/ && vdm-vscode.ct.show-cancel-button == false`,
	`view =~ /vdm-vscode.ct.testView/ && vdm-vscode.ct.show-cancel-button == true`,
	`view =~ /vdm-vscode.ct.testView/ && vdm-vscode.ct.show-execute-filter-button == true`,
	`view =~ /vdm-vscode.ct.testView/ && vdm-vscode.ct.show-verdict-filter-button == false`,
	`view =~ /vdm-vscode.ct.testView/ && vdm-vscode.ct.show-verdict-filter-button == true`,
	`view =~ /vdm-vscode.ct.testView/`,
	`view =~ /vscode-journal-view/ && !vscode-journal-view-is-filtered`,
	`view =~ /vscode-journal-view/ && vscode-journal-view-is-filtered`,
	`view =~ /vscode-journal-view/ && vscode-journal-view-show-collapse`,
	`view =~ /vscode-journal-view/ && vscode-journal-view-show-expand`,
	`view =~ /vscode-journal-view/`,
	`view =~ /wallaby.traceExplorer/ && !wallaby.paused`,
	`view =~ /wallaby.traceExplorer/ && wallaby.hasAnyEnabledBreakpoints && !wallaby.paused`,
	`view =~ /wallaby.traceExplorer/ && wallaby.traceBeingNavigated && !wallaby.paused`,
	`view =~ /wrresourceview|explore.view.id/ && viewItem =~ /canAttach=true/`,
	`view =~ /wrresourceview|explore.view.id/ && viewItem =~ /canCommonRefresh=true/`,
	`view =~ /wrresourceview|explore.view.id/ && viewItem =~ /canTerminate=true/`,
	`view =~/^mybatisx-log/`,
	`view =~/todo-tree/ && todo-tree-can-toggle-compact-folders == true`,
	`view =~/todo-tree/`,
	`view in ext:allFavoriteViews && viewItem =~ /^resource(Child)?$|^uri\\.resource(Child)?$/`,
	`view in ext:allFavoriteViews && viewItem =~ /resource(\\.dir)?$/`,
	`view in ext:allFavoriteViews`,
	`view==citrix.view.citrixdeveloper`,
	`viewContainer.workbench.view.debug.enabled`,
	`viewContainer.workbench.view.explorer.enabled`,
	`viewContainer.workbench.view.extensions.enabled`,
	`viewItem != dotnet-solution-explorer.solution && viewItem != dotnet-solution-explorer.project && viewItem != dotnet-solution-explorer.directory && viewItem != dotnet-solution-explorer.project-reference && viewItem != dotnet-solution-explorer.project-reference-list && viewItem != dotnet-solution-explorer.nuget-package&& viewItem != dotnet-solution-explorer.nuget-package-version && viewItem != dotnet-solution-explorer.project-nuget-package-list  && viewItem != dotnet-solution-explorer.project-dependencies`,
	`viewItem != dotnet-solution-explorer.solution && viewItem != dotnet-solution-explorer.project && viewItem != dotnet-solution-explorer.project-reference && viewItem != dotnet-solution-explorer.project-reference-list && viewItem != dotnet-solution-explorer.nuget-package && viewItem != dotnet-solution-explorer.nuget-package-version && viewItem != dotnet-solution-explorer.project-nuget-package-list  && viewItem != dotnet-solution-explorer.project-dependencies`,
	`viewItem != dotnet-solution-explorer.solution && viewItem != dotnet-solution-explorer.project && viewItem != dotnet-solution-explorer.project-reference && viewItem != dotnet-solution-explorer.project-reference-list && viewItem != dotnet-solution-explorer.nuget-package&& viewItem != dotnet-solution-explorer.nuget-package-version && viewItem != dotnet-solution-explorer.project-nuget-package-list  && viewItem != dotnet-solution-explorer.project-dependencies`,
	`viewItem != folder && resourceLangId == plan`,
	`viewItem != standardGroup && view == codeui.views.elements`,
	`viewItem != untitled && !isMac && !isWeb`,
	`viewItem != untitled && isMac`,
	`viewItem != untitled`,
	`viewItem == 'FreeIOE.Application'`,
	`viewItem == 'FreeIOE.Device'`,
	`viewItem == 'FreeIOE.Event'`,
	`viewItem == 'bq-gcp-project'`,
	`viewItem == 'bq-table'`,
	`viewItem == 'db' && isSkaffolderProject`,
	`viewItem == 'device|usb'`,
	`viewItem == 'hook' && view == git_hooks_view||view == git_hooks_scm`,
	`viewItem == 'org-secrets'`,
	`viewItem == 'reference.references'`,
	`viewItem == 'reference.variable' || viewItem == 'reference.this'`,
	`viewItem == 'resource' && isSkaffolderProject`,
	`viewItem == 'resource' || viewItem == 'module' || viewItem == 'service' || viewItem == 'db' && isSkaffolderProject`,
	`viewItem == 'scope.frame' || viewItem == 'scope.this' || viewItem == 'scope.function' || viewItem == 'scope.module' || viewItem == 'reference.frame' || viewItem == 'reference.this'`,
	`viewItem == 'scope.session'`,
	`viewItem == 'scope.this' || viewItem == 'scope.variable'`,
	`viewItem == 'secret' || viewItem == 'org-secret'`,
	`viewItem == 'secret'`,
	`viewItem == 'secrets'`,
	`viewItem == ACTIVED_GROUP && view == cl.eide.view.projects`,
	`viewItem == AdbApk`,
	`viewItem == AdbDevice`,
	`viewItem == AdbFile`,
	`viewItem == AdbFolder`,
	`viewItem == AzFSPackageDatabase`,
	`viewItem == AzFSPackageDemoData`,
	`viewItem == AzFSPackageStandardApp || viewItem == AzFSPackageAdditionalApp || viewItem == AzFSPackageAllApp`,
	`viewItem == BOParent`,
	`viewItem == BO`,
	`viewItem == BizNode`,
	`viewItem == Build Site`,
	`viewItem == COMPILE_CONFIGURATION && view == cl.eide.view.projects`,
	`viewItem == COMPILE_CONFIGURATION_ITEM && view == cl.eide.view.projects`,
	`viewItem == COMPONENT_GROUP && view == cl.eide.view.projects`,
	`viewItem == Clean Cache`,
	`viewItem == ComponentItem && editorIsOpen`,
	`viewItem == ComponentItem`,
	`viewItem == CppSshTargetsView.targetLeafCanSetActive || viewItem == CppSshTargetsView.targetLeafRemovableCanSetActive`,
	`viewItem == CppSshTargetsView.targetLeafRemovable || viewItem == CppSshTargetsView.targetLeafRemovableCanSetActive`,
	`viewItem == DEFINE_GROUP && view == cl.eide.view.projects`,
	`viewItem == DEPENDENCE && view == cl.eide.view.projects`,
	`viewItem == Develop Server`,
	`viewItem == ELEMENT_IN_PLACE || viewItem == ELEMENT_UP_THE_MAP`,
	`viewItem == EXCFILE_ITEM || viewItem == FILE_ITEM || viewItem == SRCREF_FILE_ITEM || viewItem == ITEM || viewItem == DEPENDENCE_ITEM || viewItem == INC_ITEM || viewItem == DEFINE_ITEM || viewItem == LIB_ITEM || viewItem == SOURCE_ITEM && view == cl.eide.view.projects`,
	`viewItem == EXCFILE_ITEM || viewItem == V_EXCFILE_ITEM && view == cl.eide.view.projects`,
	`viewItem == EXCFOLDER || viewItem == V_EXCFOLDER && view == cl.eide.view.projects`,
	`viewItem == ErrorNode`,
	`viewItem == ExplorerCheckResultNode`,
	`viewItem == FAVORITE_DIRECTORY && clipboard == true`,
	`viewItem == FAVORITE_DIRECTORY`,
	`viewItem == FAVORITE_FILE`,
	`viewItem == FAVORITE_GROUP`,
	`viewItem == FILE_ITEM || viewItem == V_FILE_ITEM && view == cl.eide.view.projects`,
	`viewItem == FILTERED`,
	`viewItem == FILTER_VALUE`,
	`viewItem == FILTER`,
	`viewItem == FOLDER || viewItem == FOLDER_ROOT && view == cl.eide.view.projects`,
	`viewItem == FOLDER || viewItem == V_FOLDER || viewItem == FOLDER_ROOT || viewItem == V_FOLDER_ROOT && view == cl.eide.view.projects`,
	`viewItem == FOLDER || viewItem == V_FOLDER || viewItem == V_FOLDER_ROOT && view == cl.eide.view.projects`,
	`viewItem == FOLDER_ROOT && view == cl.eide.view.projects`,
	`viewItem == FS_DIRECTORY && clipboard == true`,
	`viewItem == FS_DIRECTORY`,
	`viewItem == FS_FILE`,
	`viewItem == FolderItem`,
	`viewItem == FrameSpan || viewItem == NestedSQLQueryTreeItem`,
	`viewItem == FrameSpan`,
	`viewItem == GeneratorItem`,
	`viewItem == INC_GROUP && view == cl.eide.view.projects`,
	`viewItem == INC_ITEM || viewItem == DEFINE_ITEM || viewItem == LIB_ITEM || viewItem == SOURCE_ITEM && view == cl.eide.view.projects`,
	`viewItem == Info`,
	`viewItem == Install Gatsby`,
	`viewItem == Invocation || viewItem == Invocation-selected`,
	`viewItem == JsonFile`,
	`viewItem == KeyVaultSecret`,
	`viewItem == LIB_GROUP && view == cl.eide.view.projects`,
	`viewItem == LOCATION || viewItem == LOCATION/NON_EXISTING || viewItem == LOCATION/INVALID_CREDENTIALS || viewItem == LOCATION/INVALID_CONNECTION || viewItem == LOCATION/WITH_MAP || viewItem == LOCATION_PROFILE/WITH_MAP`,
	`viewItem == LOCATION || viewItem == LOCATION_PROFILE || viewItem == LOCATION/WITH_MAP || viewItem == LOCATION_PROFILE/WITH_MAP`,
	`viewItem == LOCATION || viewItem == LOCATION_PROFILE || viewItem == LOCATION_PROFILE/NON_EXISTING || viewItem == LOCATION/NON_EXISTING || viewItem == LOCATION_PROFILE/INVALID_CONNECTION || viewItem == LOCATION_PROFILE/INVALID_CREDENTIALS || viewItem == LOCATION/INVALID_CREDENTIALS || viewItem == LOCATION/INVALID_CONNECTION|| viewItem == LOCATION/WITH_MAP || viewItem == LOCATION_PROFILE/WITH_MAP`,
	`viewItem == LOCATION || viewItem == LOCATION_PROFILE`,
	`viewItem == LOCATION/WITH_MAP || viewItem == LOCATION_PROFILE/WITH_MAP`,
	`viewItem == LOCATION_PROFILE/INVALID_CONNECTION || viewItem == LOCATION/INVALID_CONNECTION`,
	`viewItem == LOCATION_PROFILE/INVALID_CREDENTIALS || viewItem == LOCATION/INVALID_CREDENTIALS`,
	`viewItem == LOCATION_PROFILE/NON_EXISTING || viewItem == LOCATION/NON_EXISTING || viewItem == LOCATION_PROFILE/INVALID_CONNECTION || viewItem == LOCATION_PROFILE/INVALID_CREDENTIALS || viewItem == LOCATION/INVALID_CREDENTIALS || viewItem == LOCATION/INVALID_CONNECTION`,
	`viewItem == MODParent`,
	`viewItem == MOD`,
	`viewItem == ModuleNodeIsLocal`,
	`viewItem == ModuleNodeIsRemote`,
	`viewItem == New Site`,
	`viewItem == OUTPUT_FILE_ITEM || viewItem == FILE_ITEM || viewItem == EXCFILE_ITEM || viewItem == V_FILE_ITEM || viewItem == V_EXCFILE_ITEM && view == cl.eide.view.projects`,
	`viewItem == OUTPUT_FOLDER || viewItem == FOLDER_ROOT_DEPS || viewItem == FOLDER_ROOT || viewItem == V_FOLDER_ROOT && view == cl.eide.view.projects`,
	`viewItem == PACK && view == cl.eide.view.projects`,
	`viewItem == PACK_GROUP && view == cl.eide.view.projects`,
	`viewItem == PROJECT && view == cl.eide.view.projects`,
	`viewItem == PROJECT || viewItem == V_FOLDER || viewItem == V_FOLDER_ROOT || viewItem == V_FILE_ITEM || viewItem == V_EXCFILE_ITEM && view == cl.eide.view.projects`,
	`viewItem == PROJECT || viewItem == V_FOLDER_ROOT || viewItem == V_FOLDER || viewItem == V_EXCFOLDER && view == cl.eide.view.projects`,
	`viewItem == PageNode`,
	`viewItem == Param`,
	`viewItem == PlaylistItemTreeItem`,
	`viewItem == ProgramTreeItem`,
	`viewItem == ProjectObject`,
	`viewItem == Project`,
	`viewItem == Published`,
	`viewItem == QueueItemTreeItem || viewItem == LocalFileTreeItem || viewItem == ProgramTreeItem`,
	`viewItem == QueueItemTreeItem || viewItem == ProgramTreeItem`,
	`viewItem == QueueItemTreeItem`,
	`viewItem == RadioTreeItem`,
	`viewItem == RantaSource`,
	`viewItem == RequestHeader`,
	`viewItem == SERVICE || viewItem == SERVICE/INVALID_CREDENTIALS || viewItem == SERVICE/INVALID_CONNECTION`,
	`viewItem == SERVICE || viewItem == SERVICE_PROFILE || viewItem == SERVICE_PROFILE/NON_EXISTING || viewItem == SERVICE_PROFILE/INVALID_CREDENTIALS || viewItem == SERVICE_PROFILE/INVALID_CONNECTION || viewItem == SERVICE/NON_EXISTING || viewItem == SERVICE/INVALID_CREDENTIALS || viewItem == SERVICE/INVALID_CONNECTION`,
	`viewItem == SERVICE || viewItem == SERVICE_PROFILE`,
	`viewItem == SERVICE_PROFILE/NON_EXISTING || viewItem == SERVICE_PROFILE/INVALID_CREDENTIALS || viewItem == SERVICE_PROFILE/INVALID_CONNECTION || viewItem == SERVICE/NON_EXISTING || viewItem == SERVICE/INVALID_CREDENTIALS || viewItem == SERVICE/INVALID_CONNECTION`,
	`viewItem == SETTINGS_ITEM && view == cl.eide.view.projects`,
	`viewItem == SOLUTION && view == cl.eide.view.projects && cl.eide.enable.active`,
	`viewItem == SOLUTION && view == cl.eide.view.projects`,
	`viewItem == SPF`,
	`viewItem == Serve Site`,
	`viewItem == SourceItem`,
	`viewItem == Source`,
	`viewItem == Target`,
	`viewItem == TemplateItem`,
	`viewItem == TestItem`,
	`viewItem == UPLOAD_OPTION || viewItem == UPLOAD_OPTION_Shell && view == cl.eide.view.projects`,
	`viewItem == UPLOAD_OPTION_ITEM && view == cl.eide.view.projects`,
	`viewItem == UPLOAD_OPTION_Shell && view == cl.eide.view.projects`,
	`viewItem == V_FILE_ITEM || viewItem == V_EXCFILE_ITEM && view == cl.eide.view.projects`,
	`viewItem == V_FOLDER_ROOT || viewItem == V_FOLDER || viewItem == V_EXCFOLDER && view == cl.eide.view.projects`,
	`viewItem == aMaterializedView`,
	`viewItem == aScriptValidate`,
	`viewItem == aStorage`,
	`viewItem == aTable`,
	`viewItem == aTaggedSharedScript`,
	`viewItem == aUntaggedSharedScript`,
	`viewItem == accountTreeItem`,
	`viewItem == account`,
	`viewItem == actionableFolder || viewItem == deletableFile`,
	`viewItem == actionableFolder`,
	`viewItem == activeIDE`,
	`viewItem == add-author || viewItem == add-repo-author || viewItem == remove-author || viewItem == primary-author`,
	`viewItem == add-author`,
	`viewItem == add-repo-author`,
	`viewItem == aks.subscription`,
	`viewItem == alertRenew`,
	`viewItem == app && view == apps`,
	`viewItem == appId`,
	`viewItem == app_approved && view == apps`,
	`viewItem == app_approved_changed && view == apps`,
	`viewItem == app_public && view == apps`,
	`viewItem == application-rest-endpoint`,
	`viewItem == application`,
	`viewItem == apps.androidApp.fingerprint`,
	`viewItem == apps.androidApp.fingerprintsFolder`,
	`viewItem == apps.androidApp`,
	`viewItem == apps.iosApp`,
	`viewItem == article`,
	`viewItem == autoScalingGroups`,
	`viewItem == availableNetwork.supportAdd`,
	`viewItem == awsAuthNode && !isCloud9`,
	`viewItem == awsAuthNode`,
	`viewItem == awsCdkRootNode`,
	`viewItem == awsCdkStateMachineNode`,
	`viewItem == awsCodeCatalystNodeSaved`,
	`viewItem == awsCodeWhispererNodeSaved`,
	`viewItem == backlogStory`,
	`viewItem == blockerTitle`,
	`viewItem == bookmark`,
	`viewItem == branch-item`,
	`viewItem == bugfix`,
	`viewItem == build:pull-request`,
	`viewItem == buildArtifactLabel`,
	`viewItem == build`,
	`viewItem == card`,
	`viewItem == cartridgeDirectory`,
	`viewItem == caseElement`,
	`viewItem == cc-locator`,
	`viewItem == cc-node || viewItem == cc-store-server || viewItem == cc-store`,
	`viewItem == cc-node`,
	`viewItem == cc-store || viewItem == cc-store-server`,
	`viewItem == cclsGoto`,
	`viewItem == changed && view == apps`,
	`viewItem == changed || viewItem == conflict`,
	`viewItem == changed`,
	`viewItem == class && treeview.provider.dynamic`,
	`viewItem == class`,
	`viewItem == clipBoard`,
	`viewItem == clipItem`,
	`viewItem == cluster-instance`,
	`viewItem == cnb-local-drafts-folder`,
	`viewItem == cnb-post-cached`,
	`viewItem == cnb-post-category`,
	`viewItem == cnblogs-posts-search-results-entry`,
	`viewItem == code && view == apps`,
	`viewItem == codingRoom`,
	`viewItem == collection.permissions`,
	`viewItem == collection.rule`,
	`viewItem == collection.rules`,
	`viewItem == collection`,
	`viewItem == columnFamily`,
	`viewItem == column`,
	`viewItem == completeTask`,
	`viewItem == connection && view == apps`,
	`viewItem == connection`,
	`viewItem == connections && view == apps`,
	`viewItem == contacts.selfContact`,
	`viewItem == container`,
	`viewItem == context_function`,
	`viewItem == cpp.dbgModuleProperty`,
	`viewItem == cqueryGoto`,
	`viewItem == createableItemAtRoot`,
	`viewItem == ct.activateProposedAPIs`,
	`viewItem == ct.sharedPorts && !codetogether.session.anotherParticipant`,
	`viewItem == currentConn && ForceCodeActive`,
	`viewItem == customGoal`,
	`viewItem == customScripts || viewItem == sharedScripts`,
	`viewItem == dart-code:depDependencyPackageNode || viewItem == dart-code:depDevDependencyPackageNode`,
	`viewItem == database.valueEntry`,
	`viewItem == database`,
	`viewItem == databases`,
	`viewItem == db`,
	`viewItem == dbuxGlobalAnalysisView.node.recordedPackageNode`,
	`viewItem == dbuxGlobalAnalysisView.node.searchNode`,
	`viewItem == dbuxPdgView.PDGExerciseNode && !dbuxProjectView.context.isBusy`,
	`viewItem == dbuxProjectView.projectNode.RunningInBackground`,
	`viewItem == dbuxSessionView.openWorkspaceNode`,
	`viewItem == dbuxSessionView.runNode#dbux`,
	`viewItem == dbuxSessionView.runNode`,
	`viewItem == dbuxSessionView.tagNode && false`,
	`viewItem == dbuxTraceDetailsView.navigationNode`,
	`viewItem == dbuxTraceDetailsView.node.asyncRootEdgesTDNode`,
	`viewItem == dbuxTraceDetailsView.node.traceValueNode`,
	`viewItem == dbuxTraceDetailsView.traceObjectTDNodeRoot.withObjectValue`,
	`viewItem == deletableFile`,
	`viewItem == deployed-resource`,
	`viewItem == deploymentGroup`,
	`viewItem == deploymentGroups`,
	`viewItem == deployment`,
	`viewItem == derivative`,
	`viewItem == devhub`,
	`viewItem == deviceGroup`,
	`viewItem == deviceLeaf || viewItem == deviceLeafCanSetActive`,
	`viewItem == deviceLeafCanSetActive`,
	`viewItem == devicetree.board`,
	`viewItem == devicetree.ctx`,
	`viewItem == devicetree.overview`,
	`viewItem == devicetree.shield`,
	`viewItem == document`,
	`viewItem == dormant-resource`,
	`viewItem == dotnet-solution-explorer.directory || viewItem == dotnet-solution-explorer.project && viewItem != dotnet-solution-explorer.project-reference && viewItem != dotnet-solution-explorer.project-reference-list && viewItem != dotnet-solution-explorer.nuget-package&& viewItem != dotnet-solution-explorer.nuget-package-version && viewItem != dotnet-solution-explorer.project-nuget-package-list  && viewItem != dotnet-solution-explorer.project-dependencies`,
	`viewItem == dotnet-solution-explorer.directory || viewItem == dotnet-solution-explorer.project`,
	`viewItem == dotnet-solution-explorer.directory`,
	`viewItem == dotnet-solution-explorer.project-reference-list`,
	`viewItem == dotnet-solution-explorer.project-reference`,
	`viewItem == dotnet-solution-explorer.project`,
	`viewItem == dotnet-solution-explorer.razor`,
	`viewItem == dotnet-solution-explorer.solution`,
	`viewItem == dv.datasource || viewItem == dv.datasource.ref`,
	`viewItem == dv.datasource.property`,
	`viewItem == dv.datasource`,
	`viewItem == dv.datasources`,
	`viewItem == dv.environment.variable || viewItem == dv.environment.variable.ref`,
	`viewItem == dv.environment.variable`,
	`viewItem == dv.environment`,
	`viewItem == dv.project`,
	`viewItem == dv.schema`,
	`viewItem == dwLogFile`,
	`viewItem == ec2TagFilters`,
	`viewItem == environment`,
	`viewItem == eventItem`,
	`viewItem == fcTestClass && ForceCodeActive`,
	`viewItem == feature`,
	`viewItem == feed`,
	`viewItem == feedrunning || viewItem == feedsuccess || viewItem == feedfailure || viewItem == feedkilled`,
	`viewItem == feedrunning`,
	`viewItem == feedsuccess || viewItem == feedfailure || viewItem == feedkilled`,
	`viewItem == fenix-env`,
	`viewItem == fenix-quick`,
	`viewItem == fenix-recommended`,
	`viewItem == fenix-repo`,
	`viewItem == fenix-template || viewItem == fenix-quick`,
	`viewItem == fenix-template`,
	`viewItem == file `,
	`viewItem == fileCli`,
	`viewItem == filter-href`,
	`viewItem == filter`,
	`viewItem == firestore.collection`,
	`viewItem == firestore.documentField`,
	`viewItem == firestore.document`,
	`viewItem == folder || viewItem == collection && view == postmanCollectionExplorer`,
	`viewItem == folder || viewItem == rootPluginFolder`,
	`viewItem == folderNode`,
	`viewItem == folder`,
	`viewItem == followedUserGists`,
	`viewItem == forceCodeTask && ForceCodeActive`,
	`viewItem == fragment`,
	`viewItem == function && view == apps`,
	`viewItem == function`,
	`viewItem == functions && view == apps`,
	`viewItem == functions.ofTriggerType.https`,
	`viewItem == gistDirectory.editable`,
	`viewItem == gistFile.editable`,
	`viewItem == gists && gistpad:codeSwingEnabled`,
	`viewItem == gists || viewItem == followedUserGists`,
	`viewItem == gists`,
	`viewItem == gitLFSLock`,
	`viewItem == gitex-bugfix`,
	`viewItem == gitex-feature`,
	`viewItem == gitex-hotfix`,
	`viewItem == gitex-release`,
	`viewItem == gitex-support`,
	`viewItem == githubRepoMgr.context.clonedRepo`,
	`viewItem == githubRepoMgr.context.notClonedRepo`,
	`viewItem == gitlab`,
	`viewItem == gitlens:date-marker && config.gitlens.views.showRelativeDateMarkers`,
	`viewItem == go:explorer:envfile`,
	`viewItem == go:test:file`,
	`viewItem == hidefiles.file.active || viewItem == hidefiles.file.active.visible || viewItem == hidefiles.file.inactive || viewItem == hidefiles.file.inactive.visible`,
	`viewItem == hidefiles.file.active.visible`,
	`viewItem == hidefiles.file.active`,
	`viewItem == hidefiles.profile.active || viewItem == hidefiles.profile.inactive`,
	`viewItem == hidefiles.profile.inactive || viewItem == hidefiles.profile.showall`,
	`viewItem == hidefiles.setup`,
	`viewItem == hidefiles.subprofile`,
	`viewItem == host:closed`,
	`viewItem == host:connected`,
	`viewItem == host`,
	`viewItem == hotfix`,
	`viewItem == inProgressResultsItem || viewItem == inProgressRemoteResultsItem`,
	`viewItem == incomingChange:modified`,
	`viewItem == incompleteTask`,
	`viewItem == indexTemplate`,
	`viewItem == index`,
	`viewItem == indexes`,
	`viewItem == interface && treeview.provider.dynamic`,
	`viewItem == interpretedResultsItem || viewItem == rawResultsItem || viewItem == remoteResultsItem || viewItem == cancelledResultsItem || viewItem == cancelledRemoteResultsItem`,
	`viewItem == interpretedResultsItem`,
	`viewItem == ionide.projectExplorer.file`,
	`viewItem == ionide.projectExplorer.projRef`,
	`viewItem == ionide.projectExplorer.projectExe`,
	`viewItem == ionide.projectExplorer.projectLoadFailed`,
	`viewItem == ionide.projectExplorer.projectLoading`,
	`viewItem == ionide.projectExplorer.projectNotLoaded`,
	`viewItem == ionide.projectExplorer.projectNotRestored`,
	`viewItem == ionide.projectExplorer.projectRefList`,
	`viewItem == ionide.projectExplorer.project`,
	`viewItem == ionide.projectExplorer.solution`,
	`viewItem == issue`,
	`viewItem == item`,
	`viewItem == jar`,
	`viewItem == jiraIssue && atlascode:isJiraAuthenticated`,
	`viewItem == jobElement`,
	`viewItem == kilsGoto`,
	`viewItem == kubernetes.cloudExplorer.cloud.Kind`,
	`viewItem == kubernetes.cloudExplorer.cloud.k3d`,
	`viewItem == layoutinspectorrunning`,
	`viewItem == layoutinspectorstopped`,
	`viewItem == libertyGradleProject || viewItem == libertyGradleProjectContainer`,
	`viewItem == libertyMavenProject || viewItem == libertyGradleProject || viewItem == libertyMavenProjectContainer || viewItem == libertyGradleProjectContainer`,
	`viewItem == libertyMavenProject || viewItem == libertyMavenProjectContainer`,
	`viewItem == libertyMavenProjectContainer || viewItem == libertyGradleProjectContainer`,
	`viewItem == libraryFile || viewItem == libraryFolder`,
	`viewItem == libraryFolder || viewItem == libraryRootFolder`,
	`viewItem == likedTrack`,
	`viewItem == link`,
	`viewItem == listItem`,
	`viewItem == list`,
	`viewItem == liveloadToolItem.running`,
	`viewItem == liveloadToolItem.stopped`,
	`viewItem == liveshare.pomodoro.controlsitem && liveshare.pomodoro.state == running`,
	`viewItem == liveshare.pomodoro.controlsitem && liveshare.pomodoro.state == stopped`,
	`viewItem == liveshare.pomodoro.root`,
	`viewItem == lldb.moduleProperty`,
	`viewItem == loadBalancer`,
	`viewItem == loadingPayaraLocal || viewItem == runningPayaraLocal || viewItem == loadingPayaraRemote || viewItem == runningPayaraRemote`,
	`viewItem == loadingPayaraLocal || viewItem == runningPayaraLocal || viewItem == stoppedPayaraLocal || viewItem == loadingPayaraRemote || viewItem == runningPayaraRemote || viewItem == stoppedPayaraRemote`,
	`viewItem == loadingPayaraLocal || viewItem == runningPayaraLocal || viewItem == stoppedPayaraLocal`,
	`viewItem == loadingPayaraMicro || viewItem == runningPayaraMicro || viewItem == stoppedPayaraMicro`,
	`viewItem == loadingPayaraMicro || viewItem == runningPayaraMicro`,
	`viewItem == logItem`,
	`viewItem == log_group`,
	`viewItem == loggedInConn && ForceCodeActive`,
	`viewItem == logined || viewItem == myInstances || viewItem == allInstances`,
	`viewItem == main-href`,
	`viewItem == manifestCommand`,
	`viewItem == manifest`,
	`viewItem == materializedViewList`,
	`viewItem == mdsDbSystem || viewItem == mdsDbSystemHW`,
	`viewItem == mdsDbSystemHW`,
	`viewItem == mdsHWCluster`,
	`viewItem == member || viewItem == streamfile`,
	`viewItem == member`,
	`viewItem == method`,
	`viewItem == modifiedFile`,
	`viewItem == module && view == apps`,
	`viewItem == module_approved && view == apps`,
	`viewItem == module_public && view == apps`,
	`viewItem == module`,
	`viewItem == modules && view == apps`,
	`viewItem == more-authors`,
	`viewItem == mrsAuthApp`,
	`viewItem == mrsContentSet`,
	`viewItem == mrsDbObject`,
	`viewItem == mrs`,
	`viewItem == ngObject.component`,
	`viewItem == ngObject.module`,
	`viewItem == ngObject.service`,
	`viewItem == non-scratch-org || viewItem == scratch-org`,
	`viewItem == non-scratch-org`,
	`viewItem == notLoggedInConn && ForceCodeActive`,
	`viewItem == notetree.note`,
	`viewItem == notetree.notebook`,
	`viewItem == nrf-connect.action.build && !nrf-connect.build.isTFM`,
	`viewItem == nrf-connect.action.flash`,
	`viewItem == nrf-connect.app || viewItem == nrf-connect.filetree.node`,
	`viewItem == nrf-connect.app`,
	`viewItem == nrf-connect.device.aliased`,
	`viewItem == nrf-connect.field.writeable`,
	`viewItem == nrf-connect.filetree.node || viewItem == nrf-connect.filetree.dtsNode`,
	`viewItem == nrf-connect.filetree.node`,
	`viewItem == nrf-connect.register.event`,
	`viewItem == nrf-connect.register.task`,
	`viewItem == nrf-connect.register.writeable`,
	`viewItem == odx.revealable`,
	`viewItem == openDocumentation`,
	`viewItem == openSettings && codetogether.team.connection.status == connected`,
	`viewItem == openSettings`,
	`viewItem == origin`,
	`viewItem == osFeedType || viewItem == updatePolicy`,
	`viewItem == package-list-item`,
	`viewItem == packman`,
	`viewItem == page `,
	`viewItem == participantConnectedPort`,
	`viewItem == participantDisconnectedPort`,
	`viewItem == payara-application`,
	`viewItem == picassoToolItem.run`,
	`viewItem == picassoToolItem.stop`,
	`viewItem == pin`,
	`viewItem == pinned`,
	`viewItem == pipelineBuild && atlascode:isBBAuthenticated`,
	`viewItem == pm2-process`,
	`viewItem == post`,
	`viewItem == primary-author`,
	`viewItem == privateKey || viewItem == publicKey`,
	`viewItem == process`,
	`viewItem == product-node || viewItem == sub-project-node`,
	`viewItem == product-node`,
	`viewItem == productLeaf || viewItem == deviceGroupLeaf || viewItem == email`,
	`viewItem == productLeaf || viewItem == deviceGroupLeaf`,
	`viewItem == product`,
	`viewItem == project-folder`,
	`viewItem == project-item && ast-results.isScanEnabled`,
	`viewItem == project-item`,
	`viewItem == projectFolder || viewItem == recipeFolder || viewItem == webappRootFolder || viewItem == wikiFolder || viewItem == wikiArticle || viewItem == recipe || viewItem == webappFolder || viewItem == rootPluginFolder || viewItem == createableItemAtRoot ||  viewItem == libraryRootFolder `,
	`viewItem == project`,
	`viewItem == qiitaItems`,
	`viewItem == rawResultsItem || viewItem == interpretedResultsItem`,
	`viewItem == rawResultsItem`,
	`viewItem == readhubInstantView`,
	`viewItem == readhubRefEntry`,
	`viewItem == ready`,
	`viewItem == redis`,
	`viewItem == release`,
	`viewItem == remote-explorer.remoteFolder`,
	`viewItem == remote-explorer.remoteTunnelSignIn`,
	`viewItem == remote-explorer.sshGroup`,
	`viewItem == remote-explorer.tunnelGroup`,
	`viewItem == remote-explorer.tunnelRemoteTarget || viewItem == remote-explorer.sshRemoteTarget`,
	`viewItem == remote-explorer.tunnelRemoteTarget`,
	`viewItem == remoteResultsItem || viewItem == inProgressRemoteResultsItem || viewItem == cancelledRemoteResultsItem`,
	`viewItem == remoteResultsItem`,
	`viewItem == remove-author`,
	`viewItem == repository || viewItem == repository_push`,
	`viewItem == repository`,
	`viewItem == resolvedBlocker`,
	`viewItem == rojo-ui.sessionContainer`,
	`viewItem == rpc && view == apps`,
	`viewItem == rpcs && view == apps`,
	`viewItem == rtagsLocation`,
	`viewItem == rule`,
	`viewItem == run-set`,
	`viewItem == run`,
	`viewItem == runningPayaraLocal || viewItem == runningPayaraRemote`,
	`viewItem == runningPayaraMicro`,
	`viewItem == runningPayaraRemote`,
	`viewItem == running`,
	`viewItem == samplesItem`,
	`viewItem == savedNetwork.disabled`,
	`viewItem == savedNetwork.enabled || viewItem == savedNetwork.disabled`,
	`viewItem == savedNetwork.enabled`,
	`viewItem == scan-item`,
	`viewItem == schemaTableItemMySQL || viewItem == schemaViewItemMySQL || viewItem == schemaRoutineItemMySQL`,
	`viewItem == schema`,
	`viewItem == scratch || viewItem == scratch.selected`,
	`viewItem == scratch-org`,
	`viewItem == scratchGist`,
	`viewItem == scratch`,
	`viewItem == script`,
	`viewItem == selected`,
	`viewItem == sharedGoal || viewItem == customGoal`,
	`viewItem == sharedGoal`,
	`viewItem == sharedScriptTag`,
	`viewItem == sharedScripts`,
	`viewItem == shortcutNode`,
	`viewItem == snippet`,
	`viewItem == snippetsmanager-snippetsView-Explorer-FileItem`,
	`viewItem == snippetsmanager-snippetsView-Explorer-Item`,
	`viewItem == solRequirementItem`,
	`viewItem == source-artifact-node`,
	`viewItem == spaceItem`,
	`viewItem == spring:bean`,
	`viewItem == step`,
	`viewItem == stoppedPayaraLocal || viewItem == runningPayaraRemote || viewItem == loadingPayaraRemote || viewItem == stoppedPayaraRemote`,
	`viewItem == stoppedPayaraLocal`,
	`viewItem == stoppedPayaraMicro || viewItem == runningPayaraMicro`,
	`viewItem == stoppedPayaraMicro`,
	`viewItem == stoppedPayaraRemote`,
	`viewItem == storageItem`,
	`viewItem == storageList`,
	`viewItem == suiteFolder || viewItem == caseFolder`,
	`viewItem == swing.directory`,
	`viewItem == swing.file`,
	`viewItem == symfony-vscode.searchItem.parameter`,
	`viewItem == symfony-vscode.searchItem.route`,
	`viewItem == symfony-vscode.searchItem.service`,
	`viewItem == symfony-vscode.service`,
	`viewItem == table || viewItem == view || viewItem == alias || viewItem == constraint || viewItem == function || viewItem == variable || viewItem == index || viewItem == procedure || viewItem == sequence || viewItem == package || viewItem == trigger || viewItem == type`,
	`viewItem == table || viewItem == view || viewItem == alias`,
	`viewItem == tableList`,
	`viewItem == table`,
	`viewItem == target`,
	`viewItem == taskItem `,
	`viewItem == taskItem`,
	`viewItem == taskTitle`,
	`viewItem == teamItem`,
	`viewItem == teamManage`,
	`viewItem == team`,
	`viewItem == tenantId || viewItem == productId || viewItem == deviceGroupId || viewItem == SSID || viewItem == email || viewItem == IP Address || viewItem == MAC Address`,
	`viewItem == terminal-not-shared`,
	`viewItem == terminal-read-only || viewItem == terminal-read-write || viewItem == terminal-shared || viewItem == terminal-not-shared`,
	`viewItem == terminal-read-only`,
	`viewItem == terminal-shared`,
	`viewItem == terraWdioSpec || viewItem == terraWdioSnapshot`,
	`viewItem == terraWdioSpecGroup`,
	`viewItem == test && vdm-vscode.ct.show-interpret-button`,
	`viewItem == test && view == apps`,
	`viewItem == testSuite`,
	`viewItem == testWithSource`,
	`viewItem == test_edalize || viewItem == test_cocotb || viewItem == test_vunit && view == teroshdl_tree_view`,
	`viewItem == test`,
	`viewItem == todo-ticked`,
	`viewItem == todo-unticked`,
	`viewItem == todoistTask`,
	`viewItem == trace && vdm-vscode.ct.show-execute-filter-button`,
	`viewItem == trace || viewItem == testgroup`,
	`viewItem == trace`,
	`viewItem == track || viewItem == likedTrack`,
	`viewItem == track`,
	`viewItem == trait && treeview.provider.dynamic`,
	`viewItem == trialRoot`,
	`viewItem == trialTestCase`,
	`viewItem == unpin`,
	`viewItem == unresolvedBlocker`,
	`viewItem == urlName`,
	`viewItem == user && codetogether.session.active`,
	`viewItem == user.email`,
	`viewItem == user.id`,
	`viewItem == user`,
	`viewItem == version`,
	`viewItem == viewTitle `,
	`viewItem == vlocode:datapack:category`,
	`viewItem == vlocode:datapack:object`,
	`viewItem == vlocode:datapack:sobjectType`,
	`viewItem == vlocode:datapack:sobject`,
	`viewItem == vlocode:jobYaml`,
	`viewItem == vmRunning && view == vb-machines`,
	`viewItem == vmStopped && view == vb-machines`,
	`viewItem == watching`,
	`viewItem == webhook && view == apps`,
	`viewItem == webhooks && view == apps`,
	`viewItem == wifiStatus`,
	`viewItem == wikiArticle || viewItem == wikiFolder`,
	`viewItem == wikiArticle`,
	`viewItem == wikilens.directory`,
	`viewItem == wikilens.page`,
	`viewItem == workspace`,
	`viewItem == wskAction`,
	`viewItem == wskEndpoint`,
	`viewItem == wskNamespace`,
	`viewItem == wskPackage`,
	`viewItem == wskTrigger`,
	`viewItem == 按钮`,
	`viewItem == 调试`,
	`viewItem =~ /#call/`,
	`viewItem =~ /#cherryPick/`,
	`viewItem =~ /#compareToDefaultBranchOnAzure/`,
	`viewItem =~ /#compareToDefaultBranchOnBitbucket/`,
	`viewItem =~ /#compareToDefaultBranchOnGitHub/`,
	`viewItem =~ /#compareToDefaultBranchOnGitLab/`,
	`viewItem =~ /#deleteWorkingCopy/`,
	`viewItem =~ /#openBranchOnAzure/`,
	`viewItem =~ /#openBranchOnBitbucket/`,
	`viewItem =~ /#openBranchOnGitHub/`,
	`viewItem =~ /#openBranchOnGitLab/`,
	`viewItem =~ /#openDiffWithYourLocal/`,
	`viewItem =~ /#openFileOnAzure/`,
	`viewItem =~ /#openFileOnBitbucket/`,
	`viewItem =~ /#openFileOnGitHub/`,
	`viewItem =~ /#openFileOnGitLab/`,
	`viewItem =~ /#openFileOnYourLocal/`,
	`viewItem =~ /#openIssueOnAzure/`,
	`viewItem =~ /#openIssueOnBitbucket/`,
	`viewItem =~ /#openIssueOnGitHub/`,
	`viewItem =~ /#openIssueOnGitLab/`,
	`viewItem =~ /#openIssueOnJira/`,
	`viewItem =~ /#openProfileOnAzure/`,
	`viewItem =~ /#openProfileOnBitbucket/`,
	`viewItem =~ /#openProfileOnGitHub/`,
	`viewItem =~ /#openProfileOnGitLab/`,
	`viewItem =~ /#openPullRequestOnAzure/`,
	`viewItem =~ /#openPullRequestOnBitbucket/`,
	`viewItem =~ /#openPullRequestOnGitHub/`,
	`viewItem =~ /#openPullRequestOnGitLab/`,
	`viewItem =~ /#share/`,
	`viewItem =~ /#traceContainer/`,
	`viewItem =~ /(?!.*selectedScopeAllScanned.*)codesight:.*scopeState.*/`,
	`viewItem =~ /(?!.*selectedScopeCurrentFile.*)codesight:.*scopeState.*/`,
	`viewItem =~ /(?!.*selectedScopeDismissed.*)codesight:.*scopeState.*/`,
	`viewItem =~ /(?<!statics)_(cloned|running)$/`,
	`viewItem =~ /(?<!statics)_cloned$/`,
	`viewItem =~ /(?<!statics)_running$/`,
	`viewItem =~ /(^workload-(deployment|statefulSet|job|daemonSet|cronjob|pod|crd-resources)-dev-(?!(developing|starting|develop_starting|vpn_)))|(^network-service-(?!vpn))/i`,
	`viewItem =~ /(activePort|inactivePort)-manual/`,
	`viewItem =~ /(appwriteProject)/`,
	`viewItem =~ /(busy|mixed)#.+Children#.+?#project$/`,
	`viewItem =~ /(go:explorer:envtree|go:explorer:envitem)/ && workspaceFolderCount > 0`,
	`viewItem =~ /(idle|busy)#.+#device/`,
	`viewItem =~ /(mixed|offline)Children#.+?#project$/`,
	`viewItem =~ /(project|active)/`,
	`viewItem =~ /(pullrequest|pipelineBuild|bitbucketIssue)/`,
	`viewItem =~ /(uNoteFile)/`,
	`viewItem =~ /(uNoteFile:ord)/`,
	`viewItem =~ /(uNoteFolder)/`,
	`viewItem =~ /(uNoteFolder:ord)/`,
	`viewItem =~ /(uNoteFolder:uord)/`,
	`viewItem =~ /(var)$/`,
	`viewItem =~ /.+#.+#device/`,
	`viewItem =~ /.+#project#device/`,
	`viewItem =~ /AdbFolder|AdbFile/`,
	`viewItem =~ /Children#dev#project$/`,
	`viewItem =~ /Children#no-dev#project$/`,
	`viewItem =~ /DiagnosticItem/ && viewItem =~ /WithApplyAutofix/`,
	`viewItem =~ /DiagnosticItem/ && viewItem =~ /WithIgnoreIssue/`,
	`viewItem =~ /DiagnosticItem/`,
	`viewItem =~ /FailureCountItem/`,
	`viewItem =~ /FailureMessageItem/`,
	`viewItem =~ /FileItem/ && viewItem =~ /WithFormatFile/`,
	`viewItem =~ /FileItem/`,
	`viewItem =~ /GTask$/`,
	`viewItem =~ /GTaskList/`,
	`viewItem =~ /GTaskSubItem|GTask$/`,
	`viewItem =~ /QuokkaAllowToCopyData/`,
	`viewItem =~ /QuokkaAllowToCopyPath/`,
	`viewItem =~ /SFDXHARDIS_CONTEXT_COMMAND\\b/`,
	`viewItem =~ /SFDXHARDIS_CONTEXT_PLUGIN\\b/`,
	`viewItem =~ /WallabyAllowToCopyData/ && !wallaby.paused`,
	`viewItem =~ /WallabyAllowToCopyPath/ && !wallaby.paused`,
	`viewItem =~ /WallabyAllowToGoToSource/ && !wallaby.paused`,
	`viewItem =~ /\\.openshift\\.inactiveProject/i`,
	`viewItem =~ /\\bjira\\b/`,
	`viewItem =~ /\\bpinned\\b/`,
	`viewItem =~ /\\bzeplinLink\\b/`,
	`viewItem =~ /\\|running$/`,
	`viewItem =~ /^(?!.*(verilogNature))/`,
	`viewItem =~ /^(?!.*(vhdlNature))/`,
	`viewItem =~ /^(?!.*_fav.*)ussSession.*/  && !listMultiSelection`,
	`viewItem =~ /^(?!.*_fav.*)ussSession.*/ && !listMultiSelection`,
	`viewItem =~ /^(?!.*_fav.*)ussSession.*/`,
	`viewItem =~ /^(appwriteProject)$/`,
	`viewItem =~ /^(editable)/`,
	`viewItem =~ /^(function)$/`,
	`viewItem =~ /^(gists|followedUser).gist/`,
	`viewItem =~ /^(gists|starredGists|followedUser).gist$/`,
	`viewItem =~ /^(gists|starredGists|followedUser).gist.notebook$/`,
	`viewItem =~ /^(gists|starredGists|followedUser).gist.swing.block$/`,
	`viewItem =~ /^(gists|starredGists|followedUser).gist/`,
	`viewItem =~ /^(permission)$/`,
	`viewItem =~ /^(permissions)$/`,
	`viewItem =~ /^(starredGists|followedUser).gist/`,
	`viewItem =~ /^(tag)$/`,
	`viewItem =~ /^(vars)$/`,
	`viewItem =~ /^(viewer:|)application-(.*)-installed/i`,
	`viewItem =~ /^(viewer:|)workload-(deployment|statefulSet|daemonSet|job|cronJob|pod|crd-resources)-dev-(?!vpn_)/i`,
	`viewItem =~ /^(viewer:|)workload-(deployment|statefulSet|job|daemonSet|cronjob|pod|pod-Running|crd-resources)-dev-(?!vpn_)/i`,
	`viewItem =~ /^(workload-(deployment|statefulSet|job|daemonSet|cronjob|pod|crd-resources)|network-service)(.*)vpn/i`,
	`viewItem =~ /^(workload-(deployment|statefulSet|job|daemonSet|cronjob|pod|crd-resources)|network-service)(.*)vpn_unhealthy/i`,
	`viewItem =~ /^(workload|network|configuration|storage)-\\w+(-\\w+)?(-(complete|running|starting|unknown))?$/i`,
	`viewItem =~ /^.*\\'apigee.environments.create.deployments\\'.*$/`,
	`viewItem =~ /^.*\\'apigee.environments.create.flowhooks\\'.*$/`,
	`viewItem =~ /^.*\\'apigee.environments.create.targetserver\\'.*$/`,
	`viewItem =~ /^.*\\'apigee.environments.create\\'.*$/`,
	`viewItem =~ /^.*\\'apigee.proxies.createPolicy\\'.*$/`,
	`viewItem =~ /^.*\\'apigee.proxies.createProxyBundle\\'.*$/`,
	`viewItem =~ /^.*\\'apigee.proxies.createProxyEndpoint\\'.*$/`,
	`viewItem =~ /^.*\\'apigee.proxies.createSharedFlowBundle\\'.*$/`,
	`viewItem =~ /^.*\\'apigee.proxies.createSharedFlow\\'.*$/`,
	`viewItem =~ /^.*\\'apigee.proxies.createTargetEndpoint\\'.*$/`,
	`viewItem =~ /^.*\\'apigee.proxies.importProxyBundle\\'.*$/`,
	`viewItem =~ /^.*\\'apigee.proxies.importSharedFlowBundle\\'.*$/`,
	`viewItem =~ /^.*\\'apigee.resource.create\\'.*$/`,
	`viewItem =~ /^.*\\'apigee.resource.import\\'.*$/`,
	`viewItem =~ /^.*\\'apigee.tests.createApp\\'.*$/`,
	`viewItem =~ /^.*\\'apigee.tests.createDeveloper\\'.*$/`,
	`viewItem =~ /^.*\\'apigee.tests.createMap\\'.*$/`,
	`viewItem =~ /^.*\\'apigee.tests.createProduct\\'.*$/`,
	`viewItem =~ /^.*\\'apigee.tests.createTestBundle\\'.*$/`,
	`viewItem =~ /^activePort/ && !codetogether.state.privateDeployment && codetogether.team.hasActiveTeams`,
	`viewItem =~ /^activePort/ && codetogether.state.privateDeployment`,
	`viewItem =~ /^application-(.*)-installed/i`,
	`viewItem =~ /^application-nodefault-installed/i`,
	`viewItem =~ /^application-nodefault-installed[-\\w]*-server$/i`,
	`viewItem =~ /^awsCodeWhispererNode/ && !isCloud9 && CODEWHISPERER_TERMS_ACCEPTED`,
	`viewItem =~ /^awsCodeWhispererNode/ && !isCloud9`,
	`viewItem =~ /^build/`,
	`viewItem =~ /^clipHistoryItem:/`,
	`viewItem =~ /^clipHistoryItem:file/`,
	`viewItem =~ /^cnb-post-cached/`,
	`viewItem =~ /^cnb-post/ && viewItem != cnb-post-category`,
	`viewItem =~ /^codetour.tour(.active)?$/`,
	`viewItem =~ /^codetour.tour(.primary)?$/`,
	`viewItem =~ /^codetour.tour(.primary)?(.active)?$/`,
	`viewItem =~ /^codetour.tour(.primary)?(.recording)?(.active)?$/ && gitOpenRepositoryCount != 0`,
	`viewItem =~ /^codetour.tour(.primary)?(.recording)?(.active)?$/`,
	`viewItem =~ /^codetour.tour(.primary)?(.recording)?.active$/`,
	`viewItem =~ /^codetour.tour(.primary)?.recording/`,
	`viewItem =~ /^codetour.tour(.recording)?(.active)?$/`,
	`viewItem =~ /^codetour.tour(.recording)?.active/`,
	`viewItem =~ /^codetour.tour.primary(.recording)?(.active)?$/`,
	`viewItem =~ /^codetour.tourStep.hasPrevious/`,
	`viewItem =~ /^codetour.tourStep/ && !codetour:recording`,
	`viewItem =~ /^codetour.tourStep/ && codetour:recording`,
	`viewItem =~ /^codetour.tourStep/`,
	`viewItem =~ /^database\\.(entry|parentEntry|valueEntry)/`,
	`viewItem =~ /^devicetree.adc.*/`,
	`viewItem =~ /^devicetree.node.*/ && viewItem =~ /enabled=false/`,
	`viewItem =~ /^devicetree.node.*/ && viewItem =~ /enabled=true/`,
	`viewItem =~ /^devicetree.node.*/`,
	`viewItem =~ /^devicetree.node.bus*/`,
	`viewItem =~ /^devspace-local/i`,
	`viewItem =~ /^devspace-server/i`,
	`viewItem =~ /^devspace/i`,
	`viewItem =~ /^execution[^s]*$/`,
	`viewItem =~ /^executions$/`,
	`viewItem =~ /^functionSettings$/`,
	`viewItem =~ /^functions.ofTriggerType/`,
	`viewItem =~ /^gistFile/`,
	`viewItem =~ /^gists.gist.swing$/`,
	`viewItem =~ /^gists.gist/`,
	`viewItem =~ /^kubeconfig-local/i`,
	`viewItem =~ /^kubeconfig-server$/i`,
	`viewItem =~ /^kubeconfig/i`,
	`viewItem =~ /^ninjaTarget(Debug|Release|Favourite)$/`,
	`viewItem =~ /^ninjaTarget(Debug|Release|Favourite)-favourite$/`,
	`viewItem =~ /^ninjaTarget(Debug|Release|Favourite).*$/`,
	`viewItem =~ /^nrf-connect.action.(flash|build)$/`,
	`viewItem =~ /^nrf-connect.action.(menuconfig|guiconfig|kconfig)$/`,
	`viewItem =~ /^nrf-connect.action.debug(.bound)?$/`,
	`viewItem =~ /^nrf-connect.action.debug.*$/`,
	`viewItem =~ /^nrf-connect.action.debug.bound$/`,
	`viewItem =~ /^nrf-connect.app/`,
	`viewItem =~ /^nrf-connect.comPort.*/`,
	`viewItem =~ /^nrf-connect.device.*/`,
	`viewItem =~ /^nrf-connect.image/ && viewItem =~ /linked=false/ && viewItem =~ /root=true/`,
	`viewItem =~ /^nrf-connect.image/ && viewItem =~ /linked=true/ && viewItem =~ /root=true/`,
	`viewItem =~ /^nrf-connect.image/ && viewItem =~ /root=false/`,
	`viewItem =~ /^nrf-connect.image/ && viewItem =~ /root=true/`,
	`viewItem =~ /^nrf-connect.image/`,
	`viewItem =~ /^nrf-connect.rttSupported.*/`,
	`viewItem =~ /^object/`,
	`viewItem =~ /^sharedPort/`,
	`viewItem =~ /^starredGists.gist/`,
	`viewItem =~ /^syncAssociate-(.*)disassociate/i`,
	`viewItem =~ /^syncAssociate-(.*)override/i`,
	`viewItem =~ /^syncAssociate-(.*)resume/i`,
	`viewItem =~ /^syncAssociate-other/i`,
	`viewItem =~ /^tags$/`,
	`viewItem =~ /^workload-(deployment|statefulSet|daemonSet|job|cronJob|pod|crd-resources)-dev-(?!vpn_).*(?<!duplicate-self)$/i`,
	`viewItem =~ /^workload-(deployment|statefulSet|daemonSet|pod-Running|daemonSet|cronjob|job|pod|crd-resources)-dev-(?!vpn_)/i`,
	`viewItem =~ /^workload-(deployment|statefulSet|job|daemonSet|cronjob|pod|crd-resources)-dev-(?!(developing-duplicate|developing-replace-self|starting|vpn_healthy|vpn_unhealthy))/i`,
	`viewItem =~ /^workload-(deployment|statefulSet|job|daemonSet|cronjob|pod|crd-resources)-dev-(?!(developing|starting|develop_starting|vpn_))/i`,
	`viewItem =~ /^workload-(deployment|statefulSet|job|daemonSet|cronjob|pod|crd-resources)-dev-(?!vpn_)/i`,
	`viewItem =~ /^workload-(deployment|statefulSet|job|daemonSet|cronjob|pod|crd-resources)-dev-(developing|develop_starting)/i`,
	`viewItem =~ /^workload-(deployment|statefulSet|job|daemonSet|cronjob|pod|pod-Running|crd-resources)-dev-(?!vpn_)/i`,
	`viewItem =~ /_(cloned|running)$/`,
	`viewItem =~ /_unClone$/`,
	`viewItem =~ /action-goToNewRoom/`,
	`viewItem =~ /action-inviteOthers/ || viewItem == sessionContainer || viewItem == participantSessionContainer`,
	`viewItem =~ /action-leaveSession/ || viewItem == participantSessionContainer`,
	`viewItem =~ /action-openSessionInBrowser/`,
	`viewItem =~ /action-refreshAccess/`,
	`viewItem =~ /action-showUpsellNotification/`,
	`viewItem =~ /action-stopSharing/ || viewItem == sessionContainer`,
	`viewItem =~ /active/`,
	`viewItem =~ /blueGreen|dgSettings/`,
	`viewItem =~ /canAlias=true/`,
	`viewItem =~ /canOpenInBrowser/`,
	`viewItem =~ /cap:delete/`,
	`viewItem =~ /circleci:job:approval:awaitingApproval$/`,
	`viewItem =~ /circleci:job:build/`,
	`viewItem =~ /circleci:job:build:running/`,
	`viewItem =~ /circleci:pipeline/`,
	`viewItem =~ /circleci:pipelineError:config$/ || viewItem =~ /circleci:pipelineError:permission$/`,
	`viewItem =~ /circleci:pipelineError:config$/`,
	`viewItem =~ /circleci:workflow/`,
	`viewItem =~ /circleci:workflow:running/`,
	`viewItem =~ /circleciJob(?::?\\w*)(?::approval)/`,
	`viewItem =~ /circleciJob(?::?\\w*)(?::build)(?::?\\w*)(?::running)/`,
	`viewItem =~ /circleciJob(?::?\\w*)(?::build)/`,
	`viewItem =~ /circleciJob/`,
	`viewItem =~ /circleciPipeline/ || viewItem =~ /circleciWorkflow/ || viewItem =~ /circleciJob(?::?\\w*)(?::build)/`,
	`viewItem =~ /circleciPipeline/ || viewItem =~ /circleciWorkflow/ || viewItem =~ /circleciJob/`,
	`viewItem =~ /circleciWorkflow(?::?\\w*)(?::failed)/`,
	`viewItem =~ /circleciWorkflow(?::?\\w*)(?::running)/`,
	`viewItem =~ /circleciWorkflow/`,
	`viewItem =~ /class:ddl.DBConnection/`,
	`viewItem =~ /class:oracle.compartment.CompartmentItem/`,
	`viewItem =~ /class:oracle.database.DatabaseItem/`,
	`viewItem =~ /class:oracle.devops.BuildPipelineItem/`,
	`viewItem =~ /class:oracle.devops.DevopsProjectItem/`,
	`viewItem =~ /class:oracle/`,
	`viewItem =~ /class:org.netbeans.api.db.explorer.DatabaseConnection/`,
	`viewItem =~ /codesight:.*(search).*/`,
	`viewItem =~ /codesight:.*(sourceFilter).*/`,
	`viewItem =~ /codesight:.*cancellableScanJob.*/`,
	`viewItem =~ /codesight:.*copyable.*/`,
	`viewItem =~ /codesight:.*dismissableIssue.*/`,
	`viewItem =~ /codesight:.*editBlackDuckServerInfo.*/`,
	`viewItem =~ /codesight:.*editCoverityServerInfo.*/`,
	`viewItem =~ /codesight:.*exportLogs.*/`,
	`viewItem =~ /codesight:.*openOnboarding.*/`,
	`viewItem =~ /codesight:.*openScanToolConfig.*/`,
	`viewItem =~ /codesight:.*scaSortStateDown.*/`,
	`viewItem =~ /codesight:.*scaSortStateUp.*/`,
	`viewItem =~ /codesight:.*scopeState.*/ && viewItem =~ /codesight:.*selectedScopeAllScanned.*/`,
	`viewItem =~ /codesight:.*scopeState.*/ && viewItem =~ /codesight:.*selectedScopeCurrentFile.*/`,
	`viewItem =~ /codesight:.*scopeState.*/ && viewItem =~ /codesight:.*selectedScopeDismissed.*/`,
	`viewItem =~ /codesight:.*sortStateDown.*/`,
	`viewItem =~ /codesight:.*sortStateUp.*/`,
	`viewItem =~ /codesight:.*unDismissableIssue.*/`,
	`viewItem =~ /codesight:.*viewSastScanDetails.*/`,
	`viewItem =~ /codesight:.*viewScaScanDetails.*/`,
	`viewItem =~ /codetour.tourStep.*(.hasNext)/`,
	`viewItem =~ /container_(cloned|running)$/`,
	`viewItem =~ /dbuxProjectView\\.exerciseNode/ && !dbuxProjectView.context.isBusy`,
	`viewItem =~ /dbuxProjectView\\.exerciseNode/`,
	`viewItem =~ /dbuxProjectView\\.exerciseNode\\.(None|Done)\\..*/ && !dbuxProjectView.context.isBusy`,
	`viewItem =~ /dbuxProjectView\\.exerciseNode\\.(None|Done)\\..*/ && dbuxProjectView.context.isBusy`,
	`viewItem =~ /dbuxProjectView\\.exerciseNode\\..*\\..*/ && !dbuxProjectView.context.isBusy && dbux.context.nodeEnv == development`,
	`viewItem =~ /dbuxProjectView\\.exerciseNode\\.RunningInBackground\\..*/`,
	`viewItem =~ /dbuxProjectView\\.exerciseNode\\.RunningInBackground\\.hasWebsite/`,
	`viewItem =~ /dbuxProjectView\\.projectNode/ && !dbuxProjectView.context.isBusy`,
	`viewItem =~ /dbuxProjectView\\.projectNode/`,
	`viewItem =~ /dgSettings/`,
	`viewItem =~ /ec2TagFilter_/`,
	`viewItem =~ /editable=true/`,
	`viewItem =~ /gardener\\.configurable/i`,
	`viewItem =~ /gardener\\.folder.project/i`,
	`viewItem =~ /gardener\\.folder\\.shoot/i`,
	`viewItem =~ /gardener\\.landscape/i`,
	`viewItem =~ /gardener\\.listable/i`,
	`viewItem =~ /gardener\\.project/i`,
	`viewItem =~ /gardener\\.shellable/i`,
	`viewItem =~ /gardener\\.shoot/i`,
	`viewItem =~ /gardener\\.targetable/i`,
	`viewItem =~ /githubRepoMgr\\.context\\.(not)?clonedRepo/i`,
	`viewItem =~ /gitlens:(?!(file|message|date-marker)\\b)/`,
	`viewItem =~ /gitlens:(?!stash\\b)/`,
	`viewItem =~ /gitlens:(?=(autolinked:issue|branch|commit|contributor|folder|history:line|pullrequest|remote|repository|repo-folder|stash|tag|worktree)\\b)/`,
	`viewItem =~ /gitlens:(?=(branch|commit|contributor|folder|history:line|pullrequest|remote|repository|repo-folder|stash|tag)\\b)/`,
	`viewItem =~ /gitlens:(branch\\b(?!.*?\\b\\+current\\b)|commit\\b|stash\\b|tag\\b)/`,
	`viewItem =~ /gitlens:(branch|commit|file\\b(?=.*?\\b\\+committed\\b)|stash|tag)\\b/`,
	`viewItem =~ /gitlens:(branch|commit|stash|tag)\\b/ && gitlens:views:canCompare`,
	`viewItem =~ /gitlens:(branch|commit|stash|tag)\\b/`,
	`viewItem =~ /gitlens:(branch|commit|stash|tag|file\\b(?=.*?\\b\\+committed\\b))\\b/`,
	`viewItem =~ /gitlens:(branch|compare|folder|results|search|status)\\b/`,
	`viewItem =~ /gitlens:(branch|tag)\\b/`,
	`viewItem =~ /gitlens:(commit|file\\b(?=.*?\\b\\+committed\\b))/ && gitlens:hasRemotes`,
	`viewItem =~ /gitlens:(commit|stash)\\b/`,
	`viewItem =~ /gitlens:(commit|stash|results:files)\\b/`,
	`viewItem =~ /gitlens:(compare:picker:ref|(compare|search):results(?!:)\\b(?!.*?\\b\\+pinned\\b))\\b(?!:(commits|files))/`,
	`viewItem =~ /gitlens:(compare:picker|(compare|search):results(?!:)\\b(?!.*?\\b\\+pinned\\b))\\b(?!:(commits|files))/`,
	`viewItem =~ /gitlens:(compare|search):results(?!:)\\b(?!.*?\\b\\+pinned\\b)/`,
	`viewItem =~ /gitlens:(compare|search):results(?!:)\\b(?=.*?\\b\\+pinned\\b)/`,
	`viewItem =~ /gitlens:(file\\b(?=.*?\\b\\+committed\\b)|history:(file|line)|status:file)\\b/ && gitlens:hasRemotes`,
	`viewItem =~ /gitlens:(file|history:(file|line)|status:file)\\b/`,
	`viewItem =~ /gitlens:(history:(file|line)|status:file)\\b/ && gitlens:hasRemotes`,
	`viewItem =~ /gitlens:(history:(file|line)|status:file)\\b/`,
	`viewItem =~ /gitlens:autolinked:issue\\b/`,
	`viewItem =~ /gitlens:autolinked:item\\b/`,
	`viewItem =~ /gitlens:autolinked:items\\b/`,
	`viewItem =~ /gitlens:branch\\b(?!.*?\\b\\+current\\b)/`,
	`viewItem =~ /gitlens:branch\\b(?!.*?\\b\\+starred\\b)/`,
	`viewItem =~ /gitlens:branch\\b(?=.*?\\b\\+(tracking|remote)\\b)/`,
	`viewItem =~ /gitlens:branch\\b(?=.*?\\b\\+current\\b)/`,
	`viewItem =~ /gitlens:branch\\b(?=.*?\\b\\+starred\\b)/`,
	`viewItem =~ /gitlens:branch\\b(?=.*?\\b\\+tracking\\b)/`,
	`viewItem =~ /gitlens:branches\\b(?=.*?\\b\\+remotes\\b)/`,
	`viewItem =~ /gitlens:branches\\b/ && config.gitlens.views.repositories.branches.layout == list`,
	`viewItem =~ /gitlens:branches\\b/ && config.gitlens.views.repositories.branches.layout == tree`,
	`viewItem =~ /gitlens:commit\\b/ && gitlens:hasRemotes`,
	`viewItem =~ /gitlens:commit\\b/`,
	`viewItem =~ /gitlens:compare:(branch(?=.*?\\b\\+comparing\\b)|results(?!:))\\b/`,
	`viewItem =~ /gitlens:compare:branch\\b(?=.*?\\b\\+comparing\\b)/`,
	`viewItem =~ /gitlens:compare:branch\\b(?=.*?\\b\\+root\\b)(?=.*?\\b\\+current\\b)(?=.*?\\b\\+branch\\b)/`,
	`viewItem =~ /gitlens:compare:branch\\b(?=.*?\\b\\+root\\b)(?=.*?\\b\\+current\\b)(?=.*?\\b\\+working\\b)/`,
	`viewItem =~ /gitlens:compare:results(?!:)\\b(?!.*?\\b\\+working\\b)/`,
	`viewItem =~ /gitlens:compare:results(?!:)\\b/`,
	`viewItem =~ /gitlens:compare:results:commits\\b/`,
	`viewItem =~ /gitlens:contributor\\b(?!.*?\\b\\+current\\b)/`,
	`viewItem =~ /gitlens:contributor\\b/`,
	`viewItem =~ /gitlens:contributors\\b/`,
	`viewItem =~ /gitlens:file(?!.*?\\b\\+(staged|unstaged))\\b/`,
	`viewItem =~ /gitlens:file\\b((?=.*?\\b\\+(committed|stashed)\\b)|:results)/`,
	`viewItem =~ /gitlens:file\\b((?=.*?\\b\\+committed\\b)|:results)/ && gitlens:hasRemotes`,
	`viewItem =~ /gitlens:file\\b((?=.*?\\b\\+committed\\b)|:results)/`,
	`viewItem =~ /gitlens:file\\b(?!.*?\\b\\+(conflicted|stashed|staged|unstaged)\\b)/`,
	`viewItem =~ /gitlens:file\\b(?!.*?\\b\\+(conflicted|unstaged)\\b)/`,
	`viewItem =~ /gitlens:file\\b(?!.*?\\b\\+conflicted\\b)/`,
	`viewItem =~ /gitlens:file\\b/ && gitlens:views:canCompare:file`,
	`viewItem =~ /gitlens:file\\b/`,
	`viewItem =~ /gitlens:folder\\b(?=.*?\\b\\+working\\b)/`,
	`viewItem =~ /gitlens:pager\\b/`,
	`viewItem =~ /gitlens:pullrequest\\b/`,
	`viewItem =~ /gitlens:remote\\b(?!.*?\\b\\+default\\b)/`,
	`viewItem =~ /gitlens:remote\\b(?=.*?\\b\\+default\\b)/`,
	`viewItem =~ /gitlens:remote\\b/`,
	`viewItem =~ /gitlens:remotes\\b/ && config.gitlens.views.repositories.branches.layout == list`,
	`viewItem =~ /gitlens:remotes\\b/ && config.gitlens.views.repositories.branches.layout == tree`,
	`viewItem =~ /gitlens:repo-folder\\b(?!.*?\\b\\+starred\\b)/`,
	`viewItem =~ /gitlens:repo-folder\\b(?=.*?\\b\\+starred\\b)/`,
	`viewItem =~ /gitlens:repo-folder\\b/ && gitlens:hasRemotes`,
	`viewItem =~ /gitlens:repo-folder\\b/ && gitlens:plus:enabled`,
	`viewItem =~ /gitlens:repo-folder\\b/`,
	`viewItem =~ /gitlens:repository\\b(?!.*?\\b\\+starred\\b)/`,
	`viewItem =~ /gitlens:repository\\b(?=.*?\\b\\+starred\\b)/`,
	`viewItem =~ /gitlens:repository\\b/ && gitlens:hasRemotes`,
	`viewItem =~ /gitlens:repository\\b/`,
	`viewItem =~ /gitlens:results:files\\b(?=.*?\\b\\+filterable\\b)(?!.*?\\b\\+filtered\\b)/`,
	`viewItem =~ /gitlens:results:files\\b(?=.*?\\b\\+filterable\\b)(?=.*?\\b\\+filtered\\b)/`,
	`viewItem =~ /gitlens:results:files\\b(?=.*?\\b\\+filterable\\b)/`,
	`viewItem =~ /gitlens:results:files\\b(?=.*?\\b\\+filtered\\b)/`,
	`viewItem =~ /gitlens:search:results(?!:)\\b/`,
	`viewItem =~ /gitlens:worktree\\b(?!.*?\\b\\+active\\b)/`,
	`viewItem =~ /gitlens:worktree\\b(?=.*?\\b\\+active\\b)/ && workspaceFolderCount != 1`,
	`viewItem =~ /gitlens:worktree\\b/`,
	`viewItem =~ /go:explorer:env/ && workspaceFolderCount > 0`,
	`viewItem =~ /i18n-ally-hard-string-item/`,
	`viewItem =~ /i18n-ally-hard-string-root/ && i18n-ally.extract.autoDetect == false`,
	`viewItem =~ /i18n-ally-hard-string-root/ && i18n-ally.extract.autoDetect == true`,
	`viewItem =~ /i18n-ally-hard-string-root/`,
	`viewItem =~ /incomingChange:(added|modified)/`,
	`viewItem =~ /incomingChange:(modified|deleted)/`,
	`viewItem =~ /incomingChange:*/`,
	`viewItem =~ /jfrog.source.code.scan.jumpToSource.enabled/`,
	`viewItem =~ /jfrog.xray.excludeDependency.enabled/`,
	`viewItem =~ /jfrog.xray.showInProjectDesc.enabled/`,
	`viewItem =~ /jfrog.xray.updateDependency.enabled/`,
	`viewItem =~ /job/ && viewItem =~ /completed/`,
	`viewItem =~ /job/ || viewItem =~ /step/`,
	`viewItem =~ /jobWithSsh/`,
	`viewItem =~ /joinCreateTeam/`,
	`viewItem =~ /k3d\\.cluster/`,
	`viewItem =~ /k3d\\.clusterServerGrowable/`,
	`viewItem =~ /k3d\\.nodeAgentRemovable/`,
	`viewItem =~ /k3d\\.nodeServerRemovable/`,
	`viewItem =~ /kernelSpec:/ || viewItem =~ /remote:activeKernel:/`,
	`viewItem =~ /kind\\.cluster/`,
	`viewItem =~ /kubernetes\\.cloudExplorer\\.cloud\\.Gardener/i`,
	`viewItem =~ /menu=general/`,
	`viewItem =~ /microapps_(cloned|running)$/`,
	`viewItem =~ /node-cid=true/`,
	`viewItem =~ /offline#.+#device/`,
	`viewItem =~ /onlineChildren#.+?#project$/`,
	`viewItem =~ /opak8s\\.folder\\.policies/i`,
	`viewItem =~ /opak8s\\.policy/i`,
	`viewItem =~ /project/`,
	`viewItem =~ /property-cid=true/`,
	`viewItem =~ /rdispatch/ || viewItem =~ /wdispatch/`,
	`viewItem =~ /remoteHub:viewItem:change\\b(?=.*?\\b\\+apply\\b)/`,
	`viewItem =~ /remoteHub:viewItem:change\\b/`,
	`viewItem =~ /remoteHub:viewItem:workspaceRepository\\b(?!.*?\\b\\+active\\b)/`,
	`viewItem =~ /remoteHub:viewItem:workspaceRepository\\b(?=.*?\\b\\+apply\\b)/`,
	`viewItem =~ /run/ && viewItem =~ /cancelable/`,
	`viewItem =~ /run/ && viewItem =~ /rerunnable/`,
	`viewItem =~ /run/`,
	`viewItem =~ /search=true/`,
	`viewItem =~ /task-completed/`,
	`viewItem =~ /task-notcompleted/`,
	`viewItem =~ /task-notstarred/`,
	`viewItem =~ /task-starred/`,
	`viewItem =~ /task/`,
	`viewItem =~ /todo-(un)?ticked/`,
	`viewItem =~ /type:view/ || viewItem =~ /type:layout/`,
	`viewItem =~ /verilogNature/`,
	`viewItem =~ /vhdlNature/`,
	`viewItem =~ /vrdev:element:kind:(action|workflow|config|resource)\\b/`,
	`viewItem =~ /vrdev:element:kind:actions\\b/`,
	`viewItem =~ /vrdev:element:kind:blueprint\\b/`,
	`viewItem =~ /vrdev:element:kind:configs\\b/`,
	`viewItem =~ /vrdev:element:kind:package\\b/`,
	`viewItem =~ /vrdev:element:kind:property\\b(?!.*?\\+multi\\b)\\b/`,
	`viewItem =~ /vrdev:element:kind:resources\\b/`,
	`viewItem =~ /vrdev:element:kind:workflow\\b/`,
	`viewItem =~ /vrdev:element:kind:workflows\\b/`,
	`viewItem =~ /vsKubernetes\\.\\w*cluster($|[^.])/i`,
	`viewItem =~ /vsKubernetes\\.resource\\.(node|pod|namespace|service)/i`,
	`viewItem =~ /vsKubernetes\\.resource\\.(pipelinerun|pipelineRunFinished|taskrun)/i`,
	`viewItem =~ /workflow/ && viewItem =~ /dispatch/`,
	`viewItem =~ /workflow/ && viewItem =~ /pinnable/`,
	`viewItem =~ /workflow/ && viewItem =~ /pinned/`,
	`viewItem =~ /workflow/`,
	`viewItem =~ /workspace/`,
	`viewItem =~/(?<!statics)_(cloned|running)$/`,
	`viewItem in WORKSPACE_OUTLINE.RANTA_SOURCE_MODULE_TYPES`,
	`viewItem in mbaas.scriptTags || viewItem == aUntaggedSharedScript|| viewItem == aTaggedSharedScript`,
	`viewItem in mbaas.scriptTags`,
	`viewItem`,
	`vim.mode == 'Normal' && editorTextFocus && inputFocus && notebookEditorFocused && notebookEditorCursorAtBoundary != 'bottom' && notebookEditorCursorAtBoundary != 'none'`,
	`vim.mode == 'Normal' && editorTextFocus && inputFocus && notebookEditorFocused && notebookEditorCursorAtBoundary != 'none' && notebookEditorCursorAtBoundary != 'top'`,
	`virtualWorkspace && false`,
	`virtualWorkspace == vsls`,
	`visualvm.featureSet >= 2`,
	`vjeko-al-objid.active == true`,
	`vlocode.conditionalContextMenus == false || resourcePath in vlocode.datapacks`,
	`vlocode.conditionalContextMenus == false || resourcePath in vlocode.metadata`,
	`vlocode.orgSelected != true`,
	`vlocode.orgSelected == true`,
	`vlocodeSalesforceSupport && vlocodeSalesforceDeveloperLogs && vlocode.orgSelected`,
	`vmssoftware.synchronizer.isInAction`,
	`volar.activated`,
	`volar.foundViteDir`,
	`voodoo.builder.enabled == false`,
	`voodoo.builder.enabled == true`,
	`vsSharperCsharp && editorFocus && !editorReadonly`,
	`vsSharperCsharp`,
	`vsc-packages-folder`,
	`vscode-arduino:showExampleExplorer`,
	`vscode-clearcase:enabled && !vscode-clearcase:DynView`,
	`vscode-clearcase:enabled && resourcePath in vscode-clearcase:CheckedoutObjects`,
	`vscode-clearcase:enabled && resourcePath not in vscode-clearcase:CheckedoutObjects`,
	`vscode-clearcase:enabled`,
	`vscode-cnb.isAuthorized && vscode-cnb.isTargetWorkspace`,
	`vscode-cnb.isAuthorized`,
	`vscode-dapr.views.applications.state == 'notInitialized'`,
	`vscode-dapr.views.applications.state == 'notInstalled'`,
	`vscode-dapr.views.applications.state == 'notRunning'`,
	`vscode-note.files-explorer`,
	`vscode-objectscript.connectActive && editorLangId == objectscript-class`,
	`vscode-objectscript.connectActive && resourceScheme == file`,
	`vscode-objectscript.connectActive && resourceScheme == isfs && explorerResourceIsRoot`,
	`vscode-objectscript.connectActive && resourceScheme == isfs || vscode-objectscript.connectActive && !editorIsOpen`,
	`vscode-objectscript.connectActive && resourceScheme == isfs`,
	`vscode-objectscript.connectActive && resourceScheme =~ /^isfs(-readonly)?$/ && resource =~ /project%3D/ && !explorerResourceIsRoot`,
	`vscode-objectscript.connectActive && resourceScheme =~ /^isfs(-readonly)?$/ && resource =~ /project%3D/ && explorerResourceIsRoot`,
	`vscode-objectscript.connectActive && resourceScheme =~ /^isfs(-readonly)?$/ || vscode-objectscript.connectActive && !editorIsOpen`,
	`vscode-objectscript.connectActive && resourceScheme =~ /^isfs(-readonly)?$/`,
	`vscode-objectscript.connectActive`,
	`vscode-objectscript.explorerRootCount == 0`,
	`vscode-objectscript.projectsExplorerRootCount == 0 && workspaceFolderCount <= 0`,
	`vscode-objectscript.projectsExplorerRootCount == 0 && workspaceFolderCount > 0`,
	`vscode-pets.position == 'explorer'`,
	`vscode-sassc.isSassProject && resourceFilename == sassconfig.json`,
	`vscode-serial-monitor.monitor1 == 'active'`,
	`vscode-serial-monitor.monitor2 == 'active'`,
	`vscode-sshclient.hostConnect-explorer`,
	`vscode-sshclient.hostConnectLog-explorer`,
	`vscode-sshclient.hostGroup-explorer`,
	`vscode-sshclient.hostHostPad-explorer`,
	`vscode-tsc.isTsProject && resourceFilename == tsconfig.json`,
	`vscode-tye.views.services.state == 'notInstalled'`,
	`vscode-tye.views.services.state == 'notRunning'`,
	`vscodeBazelHaveBazelWorkspace`,
	`vscodeEasyMotionJumping`,
	`vscodeKubernetesDescribeContext`,
	`vscodeKubernetesTreeViewContext`,
	`vscodePostgresResultFocus`,
	`vscodeWorkspaceSwitcherExplorerShow && vscodeWorkspaceSwitcherViewContainersShow`,
	`vscodeWorkspaceSwitcherViewInActivityBarShow && vscodeWorkspaceSwitcherViewContainersShow`,
	`vscodeWorkspaceSwitcherViewInExplorerShow && vscodeWorkspaceSwitcherViewContainersShow`,
	`vshaxeActivated && enableHaxeServerView`,
	`vshaxeActivated`,
	`vshaxeDebugToolsActivated && view == haxe.methods`,
	`vslsSccOpenRepositoryCount != 0 && !isInDiffEditor && resourceScheme == vsls`,
	`vslsSccOpenRepositoryCount != 0 && isInDiffEditor && resourceScheme =~ /^vsls$|^vsls-scc$|^vslsexternal$/`,
	`vuedx.debug && editorLangId == vue`,
	`wallaby.feature.pause && wallaby.started && !wallaby.paused`,
	`wallaby.feature.showLineValuesTimings && wallaby.started && activeEditor && !wallaby.paused`,
	`wallaby.feature.smartStart && !wallaby.paused`,
	`wallaby.started && !wallaby.inspectorOpened && wallaby.feature.inspections && !wallaby.paused`,
	`wallaby.started && !wallaby.paused`,
	`wallaby.started && !wallaby.traceBeingNavigated && !inDebugMode && !wallaby.paused`,
	`wallaby.started && activeEditor && !wallaby.paused`,
	`wallaby.started && activeEditor && wallaby.feature.debugger && !wallaby.paused`,
	`wallaby.started && activeEditor && wallaby.feature.inspections && !wallaby.paused`,
	`wallaby.started && activeEditor && wallaby.feature.profiler && !wallaby.traceBeingNavigated && !wallaby.paused`,
	`wallaby.started && activeEditor && wallaby.feature.testFilters && !wallaby.paused`,
	`wallaby.started && activeEditor && wallaby.feature.trackableValues && wallaby.fileHasRemovableInlineValues && !wallaby.paused`,
	`wallaby.started && activeEditor && wallaby.feature.trackableValues && wallaby.lineHasRemovableInlineValues && !wallaby.paused`,
	`wallaby.started && controllerId == wallaby && testId in wallaby.testingMenu.canGoToError && wallaby.hasValidStats && !wallaby.paused`,
	`wallaby.started && controllerId == wallaby && testId in wallaby.testingMenu.canGoToFile && wallaby.hasValidStats && !wallaby.paused`,
	`wallaby.started && controllerId == wallaby && testId in wallaby.testingMenu.canGoToTest && wallaby.hasValidStats && !wallaby.paused`,
	`wallaby.started && controllerId == wallaby && testId in wallaby.testingMenu.canUpdateSnapshot && wallaby.hasValidStats && !wallaby.paused`,
	`wallaby.started && controllerId == wallaby && testId in wallaby.testingMenu.isFile && wallaby.hasValidStats && !wallaby.paused`,
	`wallaby.started && editorLangId != wallaby-inspector && !wallaby.paused`,
	`wallaby.started && editorTextFocus && !wallaby.paused`,
	`wallaby.started && editorTextFocus && editorLangId == wallaby-inspector && wallaby.feature.inspections && !wallaby.paused`,
	`wallaby.started && editorTextFocus && wallaby.feature.inspections && !wallaby.paused`,
	`wallaby.started && wallaby.feature.diagnostics`,
	`wallaby.started && wallaby.feature.exclusiveTestRun && explorerResourceIsFolder && resourceFilename != .idea && resourceFilename != .vs && resourceFilename != .vscode && resourceFilename != node_modules && !wallaby.paused || wallaby.started && wallaby.feature.exclusiveTestRun && resourceLangId == javascript && resourceFilename =~ /^(?!wallaby).*\\.js$/ && resourceFilename != jest.config.js && !wallaby.paused || wallaby.started && wallaby.feature.exclusiveTestRun && resourceLangId == javascriptreact && !wallaby.paused || wallaby.started && wallaby.feature.exclusiveTestRun && resourceLangId == typescript && !wallaby.paused || wallaby.started && wallaby.feature.exclusiveTestRun && resourceLangId == typescriptreact && !wallaby.paused || wallaby.started && wallaby.feature.exclusiveTestRun && resourceLangId == coffeescript && !wallaby.paused || wallaby.started && wallaby.feature.exclusiveTestRun && resourceLangId == vue && !wallaby.paused`,
	`wallaby.started && wallaby.feature.exclusiveTestRun && explorerResourceIsFolder && resourceFilename != .idea && resourceFilename != .vs && resourceFilename != .vscode && resourceFilename != node_modules || wallaby.started && wallaby.feature.exclusiveTestRun && resourceLangId == javascript && resourceFilename =~ /^(?!wallaby).*\\.js$/ && resourceFilename != jest.config.js || wallaby.started && wallaby.feature.exclusiveTestRun && resourceLangId == javascriptreact || wallaby.started && wallaby.feature.exclusiveTestRun && resourceLangId == typescript || wallaby.started && wallaby.feature.exclusiveTestRun && resourceLangId == typescriptreact || wallaby.started && wallaby.feature.exclusiveTestRun && resourceLangId == coffeescript || wallaby.started && wallaby.feature.exclusiveTestRun && resourceLangId == vue`,
	`wallaby.started && wallaby.feature.inspections && !wallaby.paused`,
	`wallaby.started && wallaby.feature.testStory && wallaby.traceBeingNavigated && !wallaby.paused || wallaby.started && wallaby.feature.testStory && activeEditor && !wallaby.paused`,
	`wallaby.started && wallaby.feature.trackableValues && !wallaby.paused`,
	`wallaby.started && wallaby.feature.trackableValues && !wallaby.uncoveredToggled && !wallaby.lineHasRemovableInlineValues && wallaby.fileHasRemovableInlineValues && editorTextFocus && !suggestWidgetVisible && !findWidgetVisible && !renameInputVisible && !referenceSearchVisible && !inReferenceSearchEditor && !quickFixWidgetVisible && !parameterHintsVisible && !editorHasSelection && !wallaby.paused`,
	`wallaby.started && wallaby.feature.trackableValues && !wallaby.uncoveredToggled && wallaby.lineHasRemovableInlineValues && editorTextFocus && !suggestWidgetVisible && !findWidgetVisible && !renameInputVisible && !referenceSearchVisible && !inReferenceSearchEditor && !quickFixWidgetVisible && !parameterHintsVisible && !wallaby.paused`,
	`wallaby.started && wallaby.inspectorOpened && editorTextFocus && wallaby.feature.inspections && !wallaby.paused`,
	`wallaby.started && wallaby.inspectorOpened && wallaby.feature.inspections && !wallaby.paused`,
	`wallaby.started && wallaby.reportedAtLeastOnceLogValue`,
	`wallaby.started && wallaby.testFilterToggled && editorTextFocus && !wallaby.paused`,
	`wallaby.started && wallaby.testFilterToggled && wallaby.feature.testFilters && !wallaby.paused`,
	`wallaby.started && wallaby.traceBeingNavigated && !inDebugMode && !wallaby.paused`,
	`wallaby.started && wallaby.traceBeingNavigated && !wallaby.paused`,
	`wallaby.started && wallaby.traceBeingNavigated && !wallaby.testStoryUnlocked && resourceScheme == wallaby-test-timeline && !wallaby.paused`,
	`wallaby.started && wallaby.traceBeingNavigated && resourceScheme != wallaby-test-timeline && !wallaby.paused || wallaby.started && wallaby.traceBeingNavigated && !wallaby.testStoryUnlocked && !wallaby.paused`,
	`wallaby.started && wallaby.traceBeingNavigated && wallaby.hasAnyEnabledBreakpoints && resourceScheme != wallaby-test-timeline && !wallaby.paused || wallaby.started && wallaby.traceBeingNavigated && wallaby.hasAnyEnabledBreakpoints && !wallaby.testStoryUnlocked && !wallaby.paused`,
	`wallaby.started && wallaby.traceBeingNavigated && wallaby.testStoryUnlocked && resourceScheme == wallaby-test-timeline && !wallaby.paused`,
	`wallaby.started && wallaby.uncoveredToggled && !wallaby.paused`,
	`wallaby.started && wallaby.uncoveredToggled && editorTextFocus && !wallaby.paused`,
	`wallaby.started`,
	`wallaby.traceBeingNavigated`,
	`watchExpressionsFocused && !expressionSelected`,
	`watchExpressionsFocused`,
	`waveAnalysis.extensionIsActive`,
	`waveAnalysis.signedIn && waveAnalysis.extensionIsActive`,
	`waveAnalysis.signedIn && waveAnalysis.showUriHelpComponentGovernanceScan && waveAnalysis.extensionIsActive`,
	`waveAnalysis.signedIn && waveAnalysis.showUriHelpFindRepo && waveAnalysis.extensionIsActive`,
	`webPreviewFocus`,
	`webostv.cliInstalled == false`,
	`webviewFindWidgetEnabled && !editorFocus && activeEditor == 'WebviewEditor'`,
	`webviewFindWidgetFocused && !editorFocus && activeEditor == 'WebviewEditor'`,
	`webviewFindWidgetFocused && !editorFocus && activeEditor == 'workbench.editor.extension'`,
	`webviewFindWidgetVisible && !editorFocus && activeEditor == 'WebviewEditor'`,
	`webviewId == 'sarif' && webviewSection == 'isGithubAlert'`,
	`webviewItem == gitlens:wip`,
	`webviewItem =~ /gitlens:(?!stash\\b)/`,
	`webviewItem =~ /gitlens:(branch|commit|contributor|pullrequest|stash|tag)\\b/`,
	`webviewItem =~ /gitlens:(branch|commit|stash|tag)\\b/`,
	`webviewItem =~ /gitlens:(commit|stash|wip)\\b/`,
	`webviewItem =~ /gitlens:branch\\b(?!.*?\\b\\+(current|remote)\\b)/`,
	`webviewItem =~ /gitlens:branch\\b(?=.*?\\b\\+remote\\b)(?!.*?\\b\\+current\\b)/`,
	`webviewItem =~ /gitlens:graph:columns\\b/ && webviewItemValue =~ /\\bauthor\\b/`,
	`webviewItem =~ /gitlens:graph:columns\\b/ && webviewItemValue =~ /\\bdatetime\\b/`,
	`webviewItem =~ /gitlens:graph:columns\\b/ && webviewItemValue =~ /\\bsha\\b/`,
	`webviewItem =~ /gitlens:graph:columns\\b/ && webviewItemValue =~ /^(?:(?!\\bauthor\\b).)*$/`,
	`webviewItem =~ /gitlens:graph:columns\\b/ && webviewItemValue =~ /^(?:(?!\\bdatetime\\b).)*$/`,
	`webviewItem =~ /gitlens:graph:columns\\b/ && webviewItemValue =~ /^(?:(?!\\bsha\\b).)*$/`,
	`webviewItem =~ /gitlens:pullrequest\\b/`,
	`webviewItem =~ /gitlens:tag\\b/`,
	`webviewItemGroup =~ /gitlens:refGroup\\b(?!.*?\\b\\+current\\b)/`,
	`webviewItem`,
	`webviewSection == 'object-details-table'`,
	`webviewSection == 'query-results-table'`,
	`wheel:themeActive && isWorkspaceTrusted`,
	`wheel:themeActive`,
	`whichkeyVisible && sideBarVisible && explorerViewletVisible`,
	`whichkeyVisible`,
	`wikilens:isWikiWorkspace`,
	`workSpaceHasGit && !GitHooks.viewContainerDisplay`,
	`workSpaceHasGit && GitHooks.viewContainerDisplay`,
	`workbench.explorer.emptyView.active`,
	`workbench.explorer.openEditorsView.active`,
	`workbench.panel.markers.view.active`,
	`workbench.panel.output.active`,
	`workbench.panel.repl.view.active`,
	`workbench.scm.active`,
	`workbench.view.search.active && neverMatch =~ /doesNotMatch/`,
	`workbenchState != empty && !decentraland.isDCL && !decentraland.isEmpty`,
	`workbenchState != empty && !decentraland.isDCL && decentraland.isEmpty`,
	`workbenchState != empty && !editorIsOpen`,
	`workbenchState != empty && !p5-server.explorer.loaded`,
	`workbenchState != empty && decentraland.isDCL`,
	`workbenchState != empty && editorIsOpen`,
	`workbenchState != empty && editorLangId == html`,
	`workbenchState != empty && explorerResourceIsFolder`,
	`workbenchState != empty && launchJsonStatus == None && isWorkspaceTrusted`,
	`workbenchState != empty && p5-server.explorer.loaded`,
	`workbenchState != empty && resourceLangId == html`,
	`workbenchState != empty && workspaceFolderCount != 0`,
	`workbenchState != empty`,
	`workbenchState != workspace`,
	`workbenchState == empty || vscode-maven:activated`,
	`workbenchState == empty`,
	`workbenchState == folder || workbenchState == workspace`,
	`workbenchState == folder`,
	`workbenchState == workspace && !serenji.activated`,
	`workbenchState == workspace && isProjectsViewEmpty`,
	`workbenchState == workspace`,
	`workbenchState!=empty`,
	`workbenchState=='workspace' && explorerResourceIsRoot || workbenchState=='folder'`,
	`workbenchState==empty`,
	`workbenchState`,
	`workspace-manager:enabled && config.workspace-manager.views.favorites.enabled && !workspace-manager:favoritesEmpty && config.workspace-manager.views.favorites.location == explorer`,
	`workspace-manager:enabled && config.workspace-manager.views.groups.enabled && config.workspace-manager.views.groups.location == explorer`,
	`workspace-manager:enabled && config.workspace-manager.views.workspaces.enabled && config.workspace-manager.views.workspaces.location == explorer`,
	`workspace-manager:enabled`,
	`workspace-owner-unconfigured`,
	`workspace-sync-unconfigured && !workspace-owner-unconfigured`,
	`workspaceFolderCount != 0 && !remoteName && !virtualWorkspace`,
	`workspaceFolderCount != 0 && config.objectscript.showExplorer == true`,
	`workspaceFolderCount != 0 && operational == false`,
	`workspaceFolderCount != 0 && operational == true`,
	`workspaceFolderCount != 0 && peacock:liveshare == true`,
	`workspaceFolderCount != 0 && remoteName == dev-container && isRecoveryContainer`,
	`workspaceFolderCount != 0 && remoteName =~ /^(wsl|ssh-remote)$/`,
	`workspaceFolderCount != 0 || codetour:inTour`,
	`workspaceFolderCount != 0`,
	`workspaceFolderCount == 0 && !isMac`,
	`workspaceFolderCount == 0 && isMac`,
	`workspaceFolderCount == 0`,
	`workspaceFolderCount == 1 && explorerResourceIsFolder`,
	`workspaceFolderCount == 1`,
	`workspaceFolderCount > 0 && !virtualWorkspace`,
	`workspaceFolderCount > 0 && github:loadingPrsTree`,
	`workspaceFolderCount > 0 `,
	`workspaceFolderCount > 0`,
	`workspaceFolderCount > 1`,
	`workspaceFolderCount >= 1 && ( resourceLangId == asa || resourceLangId == asax || resourceLangId == ascx || resourceLangId == ashx || resourceLangId == asmx || resourceLangId == asp || resourceLangId == aspx || resourceLangId == axd || resourceLangId == cshtml || resourceLangId == ejs || resourceLangId == htm || resourceLangId == html || resourceLangId == inc || resourceLangId == jsp || resourceLangId == jspf || resourceLangId == jspx || resourceLangId == mas || resourceLangId == master || resourceLangId == mi || resourceLangId == php || resourceLangId == shtml || resourceLangId == skin || resourceLangId == tag || resourceLangId == vm || resourceLangId == xhtml || resourceLangId == as || resourceLangId == javascriptreact || resourceLangId == javascript  || resourceLangId == typescriptreact )`,
	`workspaceFolderCount >= 1 && code-for-ibmi:connected == true`,
	`workspaceHasPackageJSON`,
	`workspaceIsRails`,
	`workspacePlatform != webworker && !RAWLabs.raw.isDemo`,
	`workspacePlatform != webworker`,
	`workspacePlatform == linux && showInstallPythonTile`,
	`workspacePlatform == linux`,
	`workspacePlatform == mac && showInstallPythonTile`,
	`workspacePlatform == mac`,
	`workspacePlatform == windows && showInstallPythonTile`,
	`workspacePlatform == windows`,
	`xmakeEnabled && debuggersAvailable && debugState == 'inactive'`,
	`xmakeEnabled`,
	`xmlTreeViewEnabled`,
	`xpack:showScriptExplorer`,
	`xsdNavigatorActive`,
	`yandexMusic.isPlaying`,
	`yang-diagram-focused`,
	`yeomanUI.Focused`,
	`zenn-editor.activated && resourceLangId in zenn-editor.previewable-language-ids && zenn-editor.active-text-editor-is-previewable`,
	`zenn-editor.activated && view == zenn`,
	`zenn-editor.activated`,
	`zeplin:loggedIn`,
];
